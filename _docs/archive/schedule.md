| 2026-09-19 12:01 | 55 | M=04-移动开发/04-推送与消息 **首批** 5 demo（大类严格轮转第 4 位 → 04-移动开发，sub_pos[04]=3 → 索引表第 55 项 推送与消息；上轮 03-系统编程。该子类此前**零 demo**，仅 2026-09-17 建过索引 README，本轮按待研究清单铺开）：**397 APNs请求与响应**（请求头语义 apns-id 规范 UUID / expiration 0 只投递一次、非 0 存储到该时刻、priority 10·5·1 且默认 10、collapse-id ≤64 字节；4096 与 VoIP 5120 上限；push-type 与载荷一致性；**每 bundle ID 只存 1 条**且短时多条不保证最新；10 个状态码 + 32 个 reason；不可重试 6 个 / 延迟重试仅 TooManyRequests / 5XX / 修后重试；410 不算 error condition；timestamp 仅伴随 Unregistered，Py 73 断言）· **398 载荷与aps字典**（自定义键写进 aps 被静默忽略、alert 字符串即 body、badge 0 清除、critical 的 sound 字典 volume 0~1、interruption-level 四档、relevance-score 0~1 而 Live Activity 例外、loc-args **按 %@ 出现顺序**替换、4096 按 UTF-8 字节计，Py 53 断言）· **399 静默推送与后台唤醒**（content-available=1 且不得含 alert/sound/badge、push-type=background + priority=5、新的顶掉旧的 / 被杀则丢弃 / 启动即投递、2~3 条每小时滑窗节流、30 秒预算，Py 37 断言）· **400 设备令牌生命周期**（不可跨 App 复用、每次启动注册且幂等、换 token 即轮换、一用户多设备、APNs 4 个令牌级 reason 与 FCM UNREGISTERED 才清理、PayloadTooLarge 与 TooManyRequests 不是令牌失效，Py 35 断言）· **401 FCMv1消息模型**（目标四选一、/topics/ 前缀剥离、**两套 priority**、visibility/proxy 大写化、TTL 3s 与 3.500000000s、remove_null_values 保留 False 与 0、content_available 只认严格 True 写数值 1、analytics_label 1~50，Py 70 断言） | S3=索引同步（04-推送与消息/README 重写：子类定位 + 已完成 5 行 + 待研究勾掉 1 项并新增 1 项 + 参考资料改按「Apple 官方 5 篇 / FCM 官方 SDK 源码 / 厂商博客」三档著录；04-移动开发/README：04-推送与消息 行说明改写、已完成列表 +5 行、待研究勾掉推送一项） | +5 | 401 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search**，全部 curl / urllib 直抓原文后本地提取实读 —— Apple 官方文档 5 篇（走 `developer.apple.com/tutorials/data/documentation/<path>.json` 的 DocC JSON 接口，此前 09-17 轮 HTML 外壳取不到正文：sending-notification-requests-to-apns、handling-notification-responses-from-apns、generating-a-remote-notification、pushing-background-updates-to-your-app、registering-your-app-with-apns，共 3.4 万字符正文）+ FCM 官方 Admin SDK 源码 2 份（firebase-admin-python 的 messaging.py 23.8KB 与 _messaging_encoder.py 35.2KB，经 jsDelivr 镜像取到；**firebase.google.com 与 developers.google.com 本轮 exit 000 完全不可达**，故 FCM 侧结论一律以官方 SDK 源码的校验与编码规则为限，不写未经核实的运行期数值）；Python 自检 5 份**全部实跑 268 断言全绿**（73+53+37+35+70），Swift ×3 与 Kotlin ×2 无本机工具链走人工审查 + `bracket_check.py` 全 BALANCED；行数审计：源文件 0 处超 300 行（3 处超限当场拆 —— apns_request 446→268+183、payload 356→234+127、fcm_message 422→293+134，拆完重跑断言数不变），8 篇 README 全部 ≤200 行；**开发期实跑与机械检查抓出的问题 5 处**：①存储语义首版断言把「最新优先」写反（seq 更旧的一条不会覆盖），改为断言 stored_kept_old 并另建 keep_latest=False 对照来复现文档「不保证最新」②resolve_alert 的入参是 alert 值而非整个 payload，测试多包了一层导致 unknown alert keys: ['alert']③alert 字典缺类型校验使 `{"title":1}` 通过，补 Table 2 的字符串 / 字符串数组约束④令牌库初版按 token 建主键，跨 App 复用同一 token 会静默覆盖元数据，改为显式抛错（文档原文：同一 token 不能给多个 app 用）⑤节流窗口断言取 t=3600 时旧发送仍未完全出窗，改成 t=3620 才是真正的恢复点；槽位自检 ✓（shell date 开局 12:01:28，锁 12:00 槽）；轮间保险 67 分钟（上轮 10:54） | notify: ok(HTTP 200 errcode=0 errmsg=ok,单次 curl --data-binary 未重试) |
| 2026-09-19 15:12 | 39 | M=05-AI与机器学习/05-经典机器学习 **第三批** 5 demo（大类严格轮转第 5 位 → 05-AI与机器学习，sub_pos[05]=4 → 索引表第 39 项 经典机器学习；上轮 04-移动开发。该子类此前 10 个 demo 已覆盖闭合，本轮按目录 README 待研究清单再开 5 项）：**402 高斯混合模型与EM**（Cholesky 求 log 行列式与马氏距离、四协方差闭式、reg_covar 只加对角、BIC/AIC 参数计数，Py 28 断言）· **403 DBSCAN密度聚类**（KDD-96 Definition 1–6、直接密度可达非对称、min_samples 含自身、每点至多一次区域查询、环数据 ARI 1.0000，Py 31 断言）· **404 层次聚类与连接准则**（Lance-Williams 递推四准则、Ward ΔSSE、距离阵取 d²/2、链式效应 single 2.18 vs ward 383.82，Py 28 断言）· **405 线性判别与二次判别**（ω_k 与 ω_k0 闭式、白化均值 PCA 至多 K−1 维、shrinkage 目标 tr(Σ)/p·I、对角 QDA ≡ GaussianNB 差 ½·d·log2π，Py 18 断言）· **406 概率校准与Brier分数**（Murphy 分解、低 Brier ≠ 校准好的反例、Platt 目标平滑与缩放阈值 30、isotonic 并列改 AUC、temperature 不改 argmax，Py 26 断言） | S3=索引同步（05-经典机器学习 README：已完成表 10→15 行、待研究勾 5 项、参考资料新增「2026-09-19 第三批」10 条实读链接 + 3 条纯文献著录；05-AI与机器学习 README：子类目行说明改 15 demo、已完成列表 +5 行） | +5 | 406 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search**，全部 curl / urllib 直抓原文后本地提取实读 —— scikit-learn 官方文档 4 篇（mixture 64KB / clustering 246KB / lda_qda 61KB / calibration 72KB）+ scikit-learn 源码 5 份（_gaussian_mixture.py 36KB / _dbscan.py 21KB / _agglomerative.py 49KB / discriminant_analysis.py 45KB / calibration.py 63KB，走 jsDelivr 镜像）+ Ester KDD-1996 原论文 PDF 631KB 6 页（aaai.org；dbs.ifi.lmu.de 只回 30KB 截断、cdn.aaai.org 403，故换源）；Python 自检 5 份**全部实跑 131 断言全绿**（28+31+28+18+26），Go 无本机工具链走 bracket_check 全 BALANCED + go_sanity + go_crossref 全 OK；行数审计：0 处超 300 行（2 处超限当场拆 —— 406 的 calibration.go 325→253+78、402 的 gmm.go 473→三文件），README 全部 ≤200 行（最高 135）；**开发期实跑与机械检查抓出的问题 6 处**：①Go 侧 `map[int]*bucket` 在键缺失时是 nil，取字段必 panic，改值类型回写 ②Go 侧多返回值赋给单变量编译不过（`relRaw := f()` → `relRaw, _, _ := f()`）③Platt 缩放阈值初版写成 1e4，回读源码确认是 **30**（由 logit(eps)≈−36 近似），Python/Go 双改并加 D6 边界断言 ④层次聚类 Ward 树高恰为朴素重算的 2 倍，根因是平方距离下的齐次递推，距离阵改取 d²/2 ⑤校准 G1 断言写成「所有分箱真实率都低于预测」，实际低分箱是**不够自信**，方向相反 ⑥temperature 用例初版 logits 完全可分（acc 1.0）导致 T→0，改 20% 标签翻转后 T=1.4427 才符合「过度自信 ⇒ T>1」；槽位自检 ✓（shell date 开局 14:00:41，锁 14:00 槽）；轮间保险 84 分钟（上轮 12:36） | notify: ok(errcode=0,单次 urllib 未重试;发送前已确认 schedule.md 无本轮记录) |
| 2026-09-19 16:02 | 26 | M=06-DevOps/04-Kubernetes **第二批** 5 demo（大类严格轮转第 6 位 → 06-DevOps，sub_pos[06]=3 → 索引表第 26 项 Kubernetes；上轮 05-AI与机器学习。该子类已有 6 个旧 demo，本轮按目录 README 待研究清单再开 5 项）：**407 Scheduler调度框架**（调度周期与绑定周期二分；13 扩展点 PreEnqueue→QueueSort→PreFilter→Filter→PostFilter→PreScore→Score→NormalizeScore→Reserve→Permit→PreBind→Bind→PostBind；Filter **节点内短路**；PostFilter **仅在无可行节点时**运行且只提名不绑定；NormalizeScore **逐插件各自归一化**再加权（n1=100、n2=25 ⇒ 加权后 50 与 12.5）；Reserve 任一失败 ⇒ **逆序 Unreserve 全部已执行 Reserve 插件**；Permit approve/deny/wait 后两态都回退；Bind 先到先得短路，Py 27 断言）· **408 HPA水平扩缩容**（`ceil(current × cur/target)` 且 tolerance 0.1 双向 isWithin；利用率 `metricsTotal×100/requestsTotal` **整数除法**得 33 而非 33.33 从而翻越容差边界；缺失 Pod 缩容按 `max(100,target)%`、扩容按 0%；未就绪 Pod **仅 `usageRatio>1.0`** 才按 0%；方向反转 hold；`calculateScaleUpLimit=max(2×current,4)`；稳定窗口**扩容取 min、缩容取 max**，Py 40 断言）· **409 NetworkPolicy网络策略**（隔离按方向分别判定；多策略**可加**成并集、与顺序无关；规则内 peers 是 OR 与 ports 是 AND；**同一 from 元素**内 ns+pod 选择器是 AND、写成两个元素才是 OR；podSelector 单独出现只作用于策略所在 ns、有 nsSelector 时作用于其选中的 ns；ipBlock/except；policyTypes 缺省推导，Py 39 断言）· **410 ConfigMap与Secret投影更新**（AtomicWriter 的 `..data` 符号链接 + 时间戳目录 + rename **目录级原子替换**；validatePath 5 条禁令先于写入；shouldWritePayload 内容未变则**整卷 no-op**（不刷新 `..data`）；可见符号链接只为路径**第一段**建一次；subPath 挂载与 env 注入永不更新；延迟 = kubelet 同步周期 + 缓存传播延迟，Py 49 断言）· **411 HelmChart模板渲染**（values 四级优先级 `values.yaml < 父 chart < −f < −−set`；`null` 删键否则深合并；管道值作为函数**最后一个参数**（`repeat 5`、`join ", "`、`default DEFAULT GIVEN`）；InstallOrder 36 种 Kind；`lessByKind` 三分支稳定排序，Py 55 断言） | S1=拆分（10 处超限当场拆：5 个 Python 拆出 `selfcheck_<mod>.py`；5 个 Go 拆出 `<mod>_model.go`，其中 scheduler 另拆 `scheduler_framework_plugins.go`；5 个 C 拆出 `<mod>_impl.h`。用 `.scratch/split_batch.py` 保证逐字节搬运，拆完重跑 5 份自检断言数不变 27/40/39/49/55）· S3=索引同步（04-Kubernetes/README：已完成表 +5 行 407-411、待研究勾掉 5 项并新增拓扑分布约束/CSI/准入控制/kubelet 驱逐 4 项、新增「历史欠账」块记录仍超行的 reconciler.c 369、pod_lifecycle.c 349、pod_lifecycle.go 320、reconciler.go 315、ipvs_lb.c 311 与 2 篇 >200 行 README；06-DevOps/README：新增 407-411 行） | +5 | 411 | 权威来源：**0 WebSearch / 9 WebFetch / 0 agentic_search**（均在配额内）+ 6 份 curl 直抓原文：kubernetes.io 官方文档 5 篇（scheduling-framework、HPA walkthrough、NetworkPolicy、ConfigMap、另 helm.sh 的 chart_template_guide/functions_and_pipelines 与 values_files 计入 9 次），源码 6 份走 jsDelivr（`pkg/controller/podautoscaler/horizontal.go` 与 `replica_calculator.go`、`utilization.go`、`kubelet/pkg/util/manager` 的 `atomic_writer.go`、Helm v3.19.0 `kind_sorter.go`、`pkg/release/interface.go`）**逐行回读确认常数与分支**，不凭模型记忆写数值；Python 自检 5 份**全部实跑 210 断言全绿**（27+40+39+49+55），Go/C 无本机工具链走 `bracket_check.py` 全 BALANCED + `go_sanity.py` + `go_crossref.py` + `c_sanity.py --tu`；行数审计：源文件 0 处超 300 行（拆后最大 293）、8 篇 README 全部 ≤200 行（最高 191）；**开发期实跑与机械检查抓出的问题 8 处**：①Unreserve 断言只写了自己，实际是**全部 Reserve 插件的逆序** ②HPA 扩容稳定窗口默认 0 秒不体现「取 min」，需显式 60 秒用例才验到拖曳 ③NetworkPolicy 用 `podSelector: {}` 做方向不对称用例会全拒，改用 `{"role":"db"}` 并另建 deny-all 用例 ④「role=backend 应被拒」静默通过，根因是 default 命名空间自身带 project 标签，把标签挪到独立 ns ⑤C 侧 `strtok_r_simple` 非可移植、且 `snprintf` 源目的重叠是 UB，改 `split_once` + 临时缓冲 ⑥helm `parse_set("a[0]=x")` KeyError，改正则 token 递归构造并补函数首 token 派发 ⑦`helm_render_model.go` 301 行，把 isEmpty 移回主文件降到 287 ⑧**两个静态检查工具本身有缺陷并已修复**：`go_crossref.py` 把 Go rune 字面量当字符串起点吞掉后续 func（误报 EvalAction/Render 未定义），加 `RUNE_RE` 并在 STR_RE 之前剥离；`c_sanity.py` 把字符串内逗号当参数分隔符（误报 `join ", "` 传 3 参），`count_args` 改先剥离字面量与注释；槽位自检 ✓（shell date 开局 16:00:30，锁 16:00 槽）；轮间保险 50 分钟（上轮 15:12）通过 | notify: ok(errcode=0 errmsg=ok,单次 curl --data-binary 发送未重试;发送前已确认 schedule.md 无本轮记录) |
| 2026-09-19 18:48 | 30 | M=07-数据存储/04-搜索引擎 **首批补充 5 demo**（大类严格轮转第 7 位 → 07-数据存储，sub_pos[07]=3 → 索引表第 30 项 搜索引擎；上轮 06-DevOps。该子类已有 4 个 demo，本轮按目录 README 待研究清单再开 5 项）：**412 分析器链与中文分词**（char filter 0+ → tokenizer 恰好 1 → token filter 0+，均按序；tokenizer 记录 position/offset 而 token filter **不得改这两者** ⇒ 停用词留下位置空洞 position 仍 1/2/3；同义词 posInc=0 即 overlap token；IK 的 `arbitrator.process(ctx, cfg.isUseSmart())` 证明 max_word 与 smart 是同一批词元的裁决开关；discount_overlaps=true 把 0-increment 重叠词排除出 norm，而粒度改 dl/avgdl 会一路改分数，Py 16 断言）· **413 bool查询与评分合并**（must/should 计分且**相加**、filter/must_not 走 filter context 不计分且可缓存；纯 filter 全 0 分、match_all+filter 与 constant_score 等效 1.0；msm 默认「有 should 且无 must/filter ⇒ 1 否则 0」，6 种规格、百分比向下取整、clamp 到 [1,n]，4 子句 75% 与 -25% 同为 3 而 5 子句为 3 与 4，算出 0 也兜底成 1，Py 25 断言）· **414 分布式检索两阶段与深分页**（query 阶段每片各取 top(from+size) 只回 doc id + sort values、fetch 只去贡献命中的分片；3 片 from=9900 size=100 ⇒ 30000 条候选，from+size>10000 被 max_result_window 拒；search_after 严格大于游标 + PIT from 必须 0 或 -1 + 唯一 tiebreaker；search_type 本地 IDF vs 全局 IDF 可让排序**翻转**，Py 14 断言）· **415 DocValues列式存储与聚合**（倒排 term→docs vs 列存 doc→term；text 默认关、annotated_text 不支持、wildcard 与 columnar 不允许关；doc-value-only 要扫整列；OrdinalMap 的段内 ord↔全局 ord packed-ints 映射，官方注释明说要归并排序全部 term 且占 RAM，Py 18 断言）· **416 IVFPQ向量量化**（IVF 只探 nprobe 个列表默认 1；PQ 的 dsub=d/M、ksub=2^nbits、码字 ceil(M·nbits/8) 字节，编码的是**残差**；ADC 的 M×ksub 距离表**恒等于** ‖r−decode(code)‖²；SDC 查 M×ksub×ksub 表且 ≠ADC；nprobe 增大召回单调不降但 nprobe=nlist 仍 <1.0，Py 20 断言） | S1=拆分（analyzer_chain.py 321 行超 300 ⇒ 拆出 selfcheck_analyzer_chain.py，模型 228 + 自检 99，拆完重跑 16 断言不变）· S3=索引同步（04-搜索引擎/README：已完成 4→9 行、待研究勾掉 IK 分词与 IVF-PQ 两项并新增高亮/精排/suggester 三项；07-数据存储/README：已完成 +5 行、待研究勾掉中文 IK 一项） | +5 | 416 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search**，全部 curl 直抓原文后本地提取实读 —— Elastic 官方文档 8 篇（analyzer-anatomy、analysis、analysis-analyzers、index-modules-similarity、norms、query-dsl-bool-query、query-dsl-minimum-should-match、search-search、paginate-search-results，其中 paginate 页 1.9 MB 首次 25 s 超时，改 60 s 后取全）+ Lucene 源码 2 份（BooleanQuery.java、OrdinalMap.java，走 jsDelivr @main）+ Faiss 源码 3 份（IndexIVF.h、IndexIVFPQ.h、impl/ProductQuantizer.h 与 .cpp）+ IK 源码 1 份（infinilabs/analysis-ik 的 IKSegmenter.java @master）；常数一律回读源码/文档确认（BM25 k1=1.2 b=0.75 discount_overlaps=true、norm ~1 byte/doc/field、max_result_window=10000、max_concurrent_shard_requests=5、IndexIVF nprobe 默认 1、IndexIVFPQ「Each residual vector is encoded as a product quantizer code」），不凭记忆写数值；Python 自检 5 份**全部实跑 93 断言全绿**（16+25+14+18+20），C/Go 无本机工具链走 `bracket_check.py` 全 BALANCED + `c_sanity.py --tu` + `go_sanity.py --spec check=2` + `go_crossref.py` 全 OK；行数审计：源文件 0 处超 300（1 处超限当场拆）、5 篇 README 全部 ≤200 行（最高 163）；**开发期实跑抓出的问题 5 处**：①tokenizer 首 token 的 posInc 误写成 0（Lucene 默认恒为 1），导致 discount_overlaps 断言把首个词也算成重叠词 ②中文 demo 第二句「万岁」不在词典 ⇒ 该文档不可完全切分、max_word 候选为 0，补词典后才得到非退化的 dl 对比 ③「dl 更长的文档分数更低」这条应然断言在 avgdl 同时变化时方向不定，改为固定 avgdl 的单变量对照 + b=0 的关闭归一化对照 ④IVFPQ 初版断言「nprobe=nlist ⇒ 召回=1.0」失败，根因是 ADC 距离本身是量化近似，穷举也到不了 1.0 —— 改成断言「穷举是该量化器的召回上界且 <1.0」，这才是 PQ 有损的真实含义 ⑤C 版 `strtok_r_simple` 在 `parse_msm` 之后定义导致隐式声明，加前向声明修复；槽位自检 ✓（shell date 开局 18:00:55，锁 18:00 槽）；轮间保险 53 分钟（上轮 17:07）通过 | notify: ok(errcode=0 errmsg=ok,单次 curl --data-binary 发送未重试;发送前已确认 schedule.md 无本轮记录) |
| 2026-09-19 21:04 | 31 | M=08-安全/01-密码学 **深水区 5 demo**(大类严格轮转第 8 位 → 08-安全,sub_pos[08]=0 → 索引表第 31 项 密码学;上轮 07-数据存储。该子类已有 13 个 demo,本轮按目录 README 待研究清单再开 5 项):**417 TLS 1.3 密钥调度**(RFC 8446 §7.1;Early/Handshake/Master 五层秘密链只有首个 Extract、后续全为 Expand;`HkdfLabel` = uint16 BE 长度 + 1 字节 label 长 + 1 字节 context 长且 label 强制带 `"tls13 "` 前缀(故 `"derived"` 的 label 长度是 13 不是 7);`Derive-Secret` 的 Context 是**截至该层的握手报文转录哈希**,改一字节 ServerHello 即改变此后全部密钥;traffic 秘密 Expand 出 key/iv,每记录 nonce = **左填充序号 XOR 静态 IV**;KeyUpdate 的 `traffic upd` 只更新本方向、request_update 由接收方发自己的 update,Py 30 断言)· **418 国密 SM2/SM3/SM4**(SM3 前馈是 **XOR 不是加**(OpenSSL `ctx->A ^= A`)、消息扩展 `W′_j = W_j ⊕ W_{j+4}`、常量 T_j 前 12 轮 79cc4519 后 48 轮 7a879d8a 并按 j 循环左移;SM4 是 32 轮非平衡 Feistel,解密是**同一套轮密钥逆序**;**轮函数 L 与密钥扩展 L′ 是两个不同的线性层**(L 含 2/10/18/24,L′ 含 13/23);SM2 的 `r = e + x1` 把摘要绑进签名 ⇒ 复用 k 时 **r 也不同**,泄漏式是 `r2 − r1 ≡ e2 − e1`,`Z_A` 含 ID 与曲线参数故换任一即验签失败,Py 24 断言)· **419 BLS 聚合签名**(配对 `e(aP,bQ) = e(P,Q)^(ab)` ⇒ 同消息下 n 个签名合 1、验证式长度恒定;玩具超奇异曲线 y²=x³+x over F_16252507 上**真 Miller 循环**求 f_{r,P}(Q) + 最终幂 `^((p+1)/r)`;η_T 用**扭映射** ψ(x,y)=(−x, i·y) 保证 e(P,P)≠1;**恶意公钥攻击**取 pk = −Σ pk_i 自造签名,`CoreAggregateVerify` 通过而标准 `AggregateVerify` 因消息互异拒绝 ⇒ 三道防线 Basic/消息增强 `H(pk‖m)`/PoP,Py 28 断言)· **420 后量子 ML-KEM**(FIPS 203;256 整除 q−1 而 512 不整除(q=3329)⇒ **不完全 NTT** 只到 128 个二次因子,乘法走 `basemul`;参考实现带 **Montgomery 因子 R=2^16**,`invntt(ntt(a)) == a·R` 而非 a;CBD 采样 η=2 掩码 0x55555555、η=3 掩码 0x00249249,一次抽两个系数;**压缩有损**是 K-PKE 不能直接当 KEM 的原因,必须套 FO 变换,且 FO 用**隐式拒绝** `J(z‖c)` 避免解密失败成为 CCA 预言机,Py 36 断言)· **421 Groth16 zk-SNARK**(R1CS → QAP:每组约束拉格朗日插值出 U/V/W,「全部满足」等价于 `t(X) = Π(X − r_i)` **整除** `U·V − W`;可信设置编码 τ 的各次幂与 α/β/γ/δ,**有毒废料**到手即可用 Sim 伪造任意陈述的证明;证明仅 3 个群元素,验证 3 次配对 `e(A,B) = e(α,β)·e(Σ a_i·σ_pub_i, γ)·e(C, δ)`;`a_0 = 1` 恒真、公开向量取 `a_0..a_ℓ` 共 ℓ+1 个,Sim 伪造的求和范围是**公开输入 0..ℓ 而非 witness**,Py 28 断言) | S1=拆分(4 处超限当场拆:`sm234.go` 335 → 拆 `sm2_model.go` 得 232+101;`bls_aggregate.py` 310 → 拆 `selfcheck_bls.py` 得 216+94;`groth16_qap.go` 326 → 拆 `polyfield.go` 得 207+123;`mlkem.py` 331 → 删 schoolbook + 压缩 ZETAS 排版降到 300)· S3=索引同步(01-密码学/README:已完成表 +5 行 417-421、待研究勾掉 TLS1.3/国密/BLS/后量子/零知识 5 项并新增 ML-DSA·门限签名·可搜索加密·PKI 4 项;08-安全/README:已完成表 +5 行、待研究新增「密码学深水区整批 ✓」与 ML-DSA 待办;completed.md 归档 417-421 并 rotate 保留 322-421) | +5 | 421 | 权威来源:**0 WebSearch / 0 WebFetch / 0 agentic_search**,全部 curl 直抓原文后本地提取实读 —— RFC 8446(TLS 1.3)+ RFC 8998(国密套件)+ draft-irtf-cfrg-bls-signature-05 + NIST.FIPS.203.pdf(转文本)+ eprint 2016/260 Groth16 原论文(转文本)+ Boneh-Lynn-Shacham Asiacrypt 2001 + OpenSSL 源码 5 份(sm3.c/sm3_local.h/sm4.c/sm2_sign.c/ec_curve.c)+ GmSSL src/sm4.c + pq-crystals kyber 参考实现 8 份(params.h/ntt.c/poly.c/cbd.c/reduce.c/reduce.h/indcpa.c/kem.c,走 jsDelivr);**常数与结构一律回读源码确认**不凭记忆(SM3 常量与 XOR 前馈、SM4 两个线性层、FIPS 203 的 q=3329/η/CBD 掩码/压缩位数、Kyber 的 128 个 ZETA);Python 自检 5 份**全部实跑 146 断言全绿**(30+24+28+36+28),Go/C 无本机工具链走 `bracket_check.py` 全 BALANCED + `go_sanity.py` + `go_crossref.py` + `c_sanity.py --tu` 全 OK;行数审计:源文件 **0 处超 300**(拆后最大 300),5 篇 README 全部 ≤200 行(最高 190);**开发期实跑与机械检查抓出的问题 12 处**:①`HkdfLabel` 断言写 label 长 11 而实际 13(漏算 `"tls13 "` 6 字节)②ap_ctx 初版把 ServerFinished 当哈希用导致转录变化不传导,改为 `transcript(ch,sh,sf)` ③SM3 前馈误写成加法(按 SHA-256 直觉),官方向量 `abc` 即失败,回读 OpenSSL 确认是 XOR ④SM4 密钥扩展误用轮函数的 L,轮密钥向量 `F12186F9` 失败,补 L′ ⑤SM2 断言「复用 k ⇒ r 相同」失败,根因是 r 含摘要,改写为 `r2−r1 ≡ e2−e1` 且 `r1 != r2`,保留 d 恢复断言 ⑥BLS Python 原型 `psi()` 返回结构错与 Miller 循环 T 为 None 两个 TypeError ⑦ML-KEM `ZETAS` 手抄漏 1 个(127 而非 128)⇒ IndexError,全部回源码重生成 ⑧NTT 的 `start = j + length`(C 写法)直译成 Python 索引错,改为 `start += 2*length` ⑨`invntt(ntt(a)) == a` 失败,实测比例恰为 R=2^16 ⇒ 恢复 Montgomery `fqmul` 并断言等于 `a·R` ⑩压缩误差理论上界 ⌈q/2^5⌉=105 而实测 104,且用例初版没先 compress,改对并如实记录差异 ⑪Groth16 断言「A/B 对调必失败」不成立 —— 符号双线性群是交换的,**如实改为记录该模型局限**并写进 README §7 ⑫Groth16 伪造初版对 witness 区间求和导致伪造验证不过,回读论文确认 Sim 的求和覆盖**公开输入 0..ℓ**;槽位自检 ✓(shell date 开局 20:01:34,锁 20:00 槽);轮间保险 120 分钟(上轮 18:01)通过 | notify: ok(errcode=0 errmsg=ok,单次 curl --data-binary @file 发送未重试;发送前已确认 schedule.md 无本轮记录) |
| 2026-09-19 22:32 | 53 | M=09-语言学习/TypeScript **首批 5 demo**(大类严格轮转第 9 位 → 09-语言学习,sub_pos[09]=3 → 索引表第 53 项 TypeScript;上轮 08-安全。该子类此前 0 demo,按目录 README 路线图的前 5 项开线):**422 结构类型与名义类型**(结构子类型只按 target 成员递归比对;**对象字面量新鲜度** —— 直接写入的字面量才查多余 key(`TS2561`),落到变量后冷却但保留「至少一个共同属性」底线(`TS2559`);`private`/`protected` 要求成员来自同一份声明,是结构系统里唯一的名义成分;`Empty<T>` 因 T 未被消费而与 `Empty<string>` 等价;branding 编译期互斥(`TS2322`/`TS2345`)而运行时零痕迹,fixture 4 + 断言 13)· **423 条件类型与分发**(裸类型参数触发分配律,`[T] extends [any]` 立即停止;`never` 是**零元联合**,分发到空集得 `never` 而非 `never[]`;`infer` 挖局部类型且重载只取**最后一个**签名;真分支收紧泛型使 `T["message"]` 合法(`TS2536` 只在没判定时出现),fixture 3 + 断言 9)· **424 映射类型与修饰符**(`-?`/`-readonly` 剥修饰符,无前缀默认 `+`;`as` 重映射可改名或产出 `never` 删键;**同态**只在键集原样写 `keyof T` 时成立,改成 `Extract<keyof T, string>` 会静默丢可选标记(`TS2741`)与数字索引签名(`TS2739` 缺 Symbol.iterator),fixture 3 + 断言 9)· **425 类型收窄与守卫**(typeof 只有 8 取值且 `typeof null === "object"` ⇒ `TS18047`;truthiness 吞掉 `0`/`""`;`in` 看原型链且 optional 成员**两侧都在**;类型谓词可被 `filter` 复用但**从不校验实现**(撒谎谓词导致运行时 TypeError);`never` 赋值做穷尽性检查(`TS2322`);CFA 三边界:函数调用**不**重置属性收窄、闭包内读取 `TS18048`、重新赋值丢弃收窄(`TS2339`),fixture 5 + 断言 14)· **426 方差与可赋值性**(参数默认**双变**且官方承认 unsound,`strictFunctionTypes` **只管函数类型属性**、方法简写依旧双变;TS 4.7 的 `out`/`in`/`in out` 标注,标错 `TS2636`;相互递归类型让方差**推断失手**,加 `<in out T>` 后报错从「`Foo<string>`→`Foo<unknown>`」侧移到另一侧 —— 标注替换了比较路径而非单纯加严,fixture 6 + 断言 8) | S1=索引同步(TypeScript/README:新增「已完成 demo」表 422-426 含各自哨兵错误码、待研究勾掉「结构类型与名义类型」「分布式条件类型陷阱」两项并新增「声明合并与模块」;09-语言学习/README:TypeScript 状态 🗺 已登记 → ✅ 已开线、demo 0 → 5、补 3 行主线说明)· S3=STATE 指针推进(top_pos 8→9 即下轮 10-逆向工程、sub_pos[09] 3→0、last_top 09-语言学习)+ 归档(completed.md append 422-426 并 rotate 保留 327-426、schedule.md 本轮行) | +5 | 426 | 权威来源:**0 WebSearch / 0 agentic_search / 6 WebFetch**(硬约束 ≤15/≤5/≤10),全部为官方一手:TypeScript Handbook 的 Conditional Types、Mapped Types、Narrowing、Type Compatibility、Object Types(Excess Property Checks / Index Signatures)五篇 + Announcing TypeScript 4.7(Optional Variance Annotations for Type Parameters);**所有类型层结论均由本机 TypeScript 5.6.3 `tsc --strict --target es2020 --noEmit` 实测**,README 引用的错误码与 tsc 输出逐字一致(含 4.7 公告原句 "as implied by variance annotation");自检方式创新:每个 demo 的 `types/*.ts` 用 `// EXPECT: TSxxxx` 头部声明期望错误码集合,selfcheck.py 调用 tsc 并**精确比对集合**(多报少报都失败),同说明 **`bivariance_strict.ts` / `bivariance_off.ts` 两份逐字相同的代码分别以默认 strict 与 `--strictFunctionTypes false` 编译**,把该开关的作用域钉死;运行期部分用 Node 22.22 内置类型擦除直跑 `.ts`,**全部 5 份实跑通过共 53 条断言**(13+9+9+14+8),21 个编译期 fixture 全绿(4+3+3+5+6);行数审计:源文件 0 处超 300(最大 selfcheck 94、主 TS 106)、5 篇 README 全部 ≤200 行(最高 114);**开发期实跑抓出的问题 8 处**:①`ReturnType<typeof f>` 写在值位置报 `TS2693`,改用类型别名中转 ②branding fixture 并行发两次 Edit 导致第二次丢失、错误码未按预期更新 ③「`(string\|number)[]` 不可赋给分发结果」这条负向判据因**数组协变**其实通过,改用反方向(把混合数组塞回结果)才有效 ④`Object.keys(Object("u-1"))` 返回字符下标使断言失败,改用 `Object.getOwnPropertySymbols` ⑤同态 fixture 原本期望 `TS2322`,实测是 `TS2741`(缺属性)+`TS2739`(缺 Symbol.iterator),如实改标注 ⑥CFA 预判「函数调用会重置属性收窄」被实测**证伪**(TS 不建模副作用),改为记录真实行为并把它列为坑 ⑦variance 运行时初版把「读到 undefined」写成了成功断言,方向反了 2 条 ⑧`(x ?? 0)` 把 NaN 消掉了,去掉兜底才还原真实行为;槽位自检 ✓(shell date 开局 22:00:51,锁 22:00 槽);轮间保险 58 分钟(上轮 21:04)通过 | notify: ok(errcode=0 errmsg=ok,单次 curl --data-binary 发送未重试;发送前已确认本轮行无既有 notify 记录) |
| 2026-09-20 02:55 | 37 | M=11-性能分析/01-系统级剖析 **第四批 5 demo**（大类严格轮转第 10 位 → 11-性能分析，sub_pos[11]=0 → 索引表第 37 项 系统级剖析；上轮 10-逆向工程由 00:00 槽消费。该子类已有 15 个 demo，本轮按目录 README 待研究清单再开 5 项）：**432 CPU利用率口径与IPC**（%CPU 实为 non-idle time，内存停顿周期算在里面；IPC<1.0 内存停顿 / >1.0 指令受限且 1.0 是作者拍的自校准线；4-wide 上 IPC 0.78 只有峰值 19.5%，%STL 高达 80.5%；工具口径 busy=100−idle 与「真空闲线程」=idle+iowait 两个口径**重叠恰为 iowait**；自旋锁/平均值掩盖突发/变主频/超线程四类误导，Py 29 断言）· **433 RPSRFS与中断亲和**（RPS 是 RSS 的软件实现，flow hash 对 rps_cpus 列表取模 + IPI；RFS 两表联动与**防乱序三条判据** —— 旧 CPU 队列 head ≥ 记录的 tail / current 未设置 / current 已下线，宁可不切也不乱序；flow limit 队列过半后统计**最近 256 个报文**、占比过半丢大流新包，默认 4096 桶；XPS 换队列要 ooo_okay；位图**最低字在前**，Py 35 断言）· **434 OffWake调度延迟归因**（off-CPU 时间**含调度延迟**即时间膨胀；唯一插桩点 finish_task_switch() 结尾且 sleeptime **清零而非删除**；`(唤醒者栈, 阻塞栈)` 联合归因后塔数 2→3；MySQL 等待工作栈占 95% 而请求同步路径仅 5%，Py 29 断言）· **435 差分火焰图**（宽度取 after、颜色取 2−1；颜色只反映帧**自身**贡献 —— 帧 a 自身 delta 5 而子树 45；`-n` 归一化让「负载翻倍」从全红变全白；`--negate` 同时换宽度来源；消失路径只能靠 elided 图补，本例 22.22%，Py 35 断言）· **436 BPF环形缓冲区**（一块共享缓冲区同时解决 perfbuf 的内存浪费与跨 CPU 不保序；reserve 自旋锁串行 ⇒ 保留有序、commit 无锁，但**慢生产者挡住后面已提交的记录**；NMI 可能抢不到锁；自节流通知，Py 35 断言） | S1=完成被中断的 431（00:00 槽在写第 5 个 demo 时被截断，本轮补 README：Itanium C++ ABI §5.1 全文 curl 落地后逐段抽取，40 条自检断言全绿 + 3 个 Go 文件 bracket/sanity/crossref 全 OK）· S3=索引同步（01-系统级剖析/README：已完成表 +5 行 432-436、待研究勾掉中断再平衡与调度延迟火焰图化两项并新增唤醒链上溯/TSA/perf c2c 三项、参考资料 +5 条；11-性能分析/README：待研究新增本批与 60 秒分析的换源说明；10-逆向工程/01-二进制逆向/README：**补录 00:00 槽被截断未归档的 427-430 与 431 共 5 行**、待研究勾掉 4 项、参考资料 +1 条；completed.md 归档 427-436 共 10 行并 rotate 保留 100；schedule.md 本轮行） | +5 | 436 | 权威来源：**1 WebSearch / 6 WebFetch / 0 agentic_search**（硬约束 ≤15/≤5/≤10）+ 2 次 curl 直抓原文（Itanium C++ ABI 全文 290 KB 本地抽取 §5.1 正文 88 KB 逐段回读；BPF ringbuf 头文件两次换源失败，故改为只在 README 标注「文档未给位号」而不写无据断言）；**60 秒分析因原文在 netflixtechblog → medium.com 被墙，本轮换做 CPU 利用率口径**，并在 11-性能分析/README 待研究里标注待换源；Python 自检 6 份**全部实跑 198 断言全绿**（29+35+29+35+35+40），Go 8 份无本机工具链走 `bracket_check.py` 全 BALANCED + `go_sanity.py` + `go_crossref.py` 全 OK；行数审计：源文件 0 处超 300（最大 ringbuf.py 212、mangling.py 287）、6 篇 README 全部 ≤200 行（最高 149）；**开发期实跑抓出的问题 4 处**：①「busy + 真空闲线程 = 100」是应然错误，实测为 100+iowait —— 两个口径**都**含 iowait，改断言为「重叠恰为 iowait」并补「扣掉 iowait 才是真在干活」②offwake 构造用例时把 sleeptime 设成 0.0 起点，被伪代码的「0 = 没在睡」哨兵吃掉导致返回 None，改用 t=1 起点并注释说明 ③子树 delta 手算成 35 实为 45（5+20+20），改对并保留「自身≠子树」这条核心断言 ④差分火焰图 strip_hex 后键变成 `app;foo+`，断言要按剥完的形态写；槽位自检 ✓（shell date 开局 02:00:28，锁 02:00 槽）；轮间保险 118 分钟（上轮 00:02）通过；**00:00 槽指针补正**：该槽产出 427-430 后写第 5 个 demo 时被截断，STATE 指针未回写，本轮按磁盘实测补正 top_pos 9→10、sub_pos[10] 0→1 | notify: ok(errcode=0 errmsg=ok,单次 curl --data-binary @file 发送未重试;发送前已确认 schedule.md 无本轮记录) |
| 2026-09-20 04:38 | 3 | M=01-游戏开发/04-游戏引擎 **游戏引擎首批 5 demo**（大类严格轮转第 1 位 → 01-游戏开发，sub_pos[01]=3 → 索引表第 3 项 游戏引擎；上轮 11-性能分析。该子类只有 1 个旧 demo（ECS/sparse set），本轮按目录 README 待研究清单展开）：**437 Archetype 原型存储**（Unity Entities 概念文档实读：16KiB chunk、每组件一个数组加 entity id 数组、容量等于 16384 除以单实体字节数得 819、chunk 内最后一个实体填空而非跨块搬、增删组件换 archetype 并搬走全部组件实测 3 次写入、查询按 archetype 组件集合超集匹配且列表可缓存、结构变更只能主线程且产生 sync point，Py 27 断言）· **438 固定时间步与 accumulator 插值**（Gaffer《Fix Your Timestep!》全文实读：60fps 与 30fps 跑同样 1 秒得到逐位相同的状态而可变步长两者不同、frameTime 大于 0.25 秒钳位、alpha 取当前步与前一步之间而非外插、死亡螺旋用上一帧耗时计入本帧的反馈建模（单步 12ms 发散到 25 步、8ms 收敛到稳态 8 步、限流后不发散但 accumulator 继续累积）、高刷 200fps 下不插值有 59 帧画面不动，Py 22 断言）· **439 Unity C# Job System 与 Burst**（Unity Manual 五篇加 Burst 1.8 包文档实读：安全系统在 Schedule 时刻判冲突、job 数据被复制只有 NativeContainer 可见、Complete 兼清理安全状态、ParallelFor 一次只偷剩余批次的一半且偷完不丢不重、分配器 Temp 1 帧与 TempJob 4 帧、HPC# 禁托管对象且 string 与 char 与 decimal 与多维数组均不支持，Py 29 断言）· **440 Unreal Nanite 虚拟化几何**（SIGGRAPH 2021《Nanite A Deep Dive》155 页 PDF 逐页抽文本加 Epic 官方文档：128 三角形 cluster、图划分分组后简化 50% 再切分、DAG 而非树、组内共享 unioned error 与包围球靠同输入同输出并行一致、parent 误差大于阈值且自身误差不大于阈值的局部判据、构建期强制误差单调使 cut 唯一（4 个阈值下 2048 条路径各命中 1 个）、ParentError 剪枝把评估从 127 降到 63、visibility buffer 材质每像素求值一次，Py 27 断言）· **441 Godot 节点生命周期与场景树**（class_node 类参考加两篇教程实读：enter_tree 父先于子而 ready 子先于父、onready 在 ready 之前、挂到游离父节点不触发回调、queue_free 延迟到帧末且该帧仍跑一次 process、free 立即且引用变 null、process 随帧率而 physics_process 固定 60 次每秒（1 秒内两种帧率物理都是 60 而 process 是 61 与 31），Py 28 断言） | S3=索引同步（04-游戏引擎/README：已完成表改为 6 行含 ID 与核心机制、待研究勾掉 archetype 与 Unity DOTS 两项并新增资源管线与 Unreal 反射与渲染图三项、子领域新增 02-Archetype 与 03-固定时间步；01-游戏开发/README：已完成列表新增 5 行；Unity/Unreal/Godot 三个子目录 README：各新增「已完成 demo」块并勾掉对应待研究项） | +5 | 441 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search**，全部 curl 直抓原文后本地抽取实读 —— Unity 官方文档 13 页（job-system、job-system-jobs、creating-jobs、job-dependencies、parallel-for-jobs、thread-safe-types、Entities 1.0.16 的 concepts-archetypes 与 concepts-structural-changes 与 toc、Burst 1.8 的 index 与 csharp-language-support 与 csharp-type-support）+ SIGGRAPH 2021 Nanite 演讲 PDF 155 页（pypdf 逐页抽文本后按关键词检索）+ Epic 官方 Nanite 文档页 + Godot 文档 4 页（class_node 1.6MB、nodes_and_scene_instances、idle_and_physics_processing、scene_tree）+ Gaffer《Fix Your Timestep!》全文；**所有数值与判据一律回读原文确认**不凭记忆（16KiB、128 三角形、一次偷一半、TempJob 4 帧、Burst 不支持类型清单、enter_tree 与 ready 的顺序、physics 默认 60）；Python 自检 5 份**全部实跑 133 断言全绿**（27+22+29+27+28），Go 6 份无本机工具链走 `bracket_check.py` 全 BALANCED + `go_sanity.py` + `go_crossref.py` 全 OK；行数审计：3 处超限当场拆（Python 拆出 `selfcheck_jobsystem.py` / `selfcheck_nanite.py` / `selfcheck_nodelifecycle.py`，Go 拆出 `jobsystem_model.go`，拆后重跑断言数不变 29/27/28），拆后源文件 0 处超 300（最大 archetype.py 281），5 篇 README 全部 ≤200 行；**开发期实跑与机械检查抓出的问题 7 处**：①Nanite 初版把「同组共享误差」做成 parent 共享，导致沿途误差不降反升、任何阈值下都只选叶簇（相机拉远断言 near 等于 far 恒 64），改回组内共享并把误差量级改成随层数翻倍后才出现真正的 cut ②Nanite 剪枝初版没对 DAG 去重，同一簇被多个 parent 引到导致评估数膨胀到 512、选中集合重复，加 visited 集合后 63 且结果一致 ③Nanite「顶层 parent 孩子数等于组大小」断言失败，根因是最后一层组只剩 2 个成员，改为按实际成员数断言并另建一层满组断言 ④Job System「job 数据被复制」初版写成 `scale == 99 or True` 这类恒真伪断言，改写为调用方字典不变的真断言 ⑤work stealing 初版按轮流发牌分配批次，四个 worker 同时干完永远不发生偷取，改为连续分段加确定性批耗时后才观测到偷取，且断言每一次偷取都恰好取一半 ⑥固定时间步的 alpha 断言初版拿初始状态当 previous，实际 previous 是上一个物理步的状态，改为返回每帧的 previous 与 current 再断言渲染值落在两者之间 ⑦Godot demo 拆分后 PackedScene 仍用模块级全局 world，自检里赋值不到模型模块的全局导致 AttributeError，改为 instantiate 显式传 world；槽位自检 ✓（shell date 开局 04:00:52，锁 04:00 槽）；轮间保险 65 分钟（上轮 02:55）通过 | notify: notify: ok(errcode=0 errmsg=ok,单次 curl --data-binary 未重试;发送前已确认 schedule.md 无本轮 notify 记录) |
| 2026-09-20 06:46 | 8 | M=02-Web开发/01-前端框架 第三批 5 demo(大类轮转第 1 位 → 02-Web开发,sub_pos[02]=0 → 索引表第 8 项 前端框架;上轮 01-游戏开发):Svelte 5 runes 编译式响应性(三层订阅 Source/Derived/Effect;**pull 语义** —— 改 state 后 derived 只置 dirty、下次读取才重算,clean 后再读不重算;依赖 = 同步读到的槽位,分支切换后旧依赖必须解绑否则仍被唤醒;只有值真变才推进 version ⇒ 3→4→3 下游一次不重跑;teardown 紧邻且先于重跑但读到的是**新值**;深代理写只改 Source、**原对象永不被 mutate**;数组 length 自身是可订阅槽位,否则 push 唤不醒按长度遍历的读者)、React RSC 与 Flight 线协议(行状态机 ROW_ID→ROW_TAG→ROW_LENGTH→BY_LENGTH/BY_NEWLINE;rowID 十六进制累加;带**字节**长度的 tag 仅 15 个 `T A O o b U S s L l G g M m V`,其余 A-Z 加 `# r x` 按换行,**未知 tag 分支不前进指针**故 `1:hello:world` 第二段仍是 payload;20 余种 `$` 引用前缀表,`$$x` 是 slice(1) 不是 slice(2);`$L` 流式占位持 chunk 本身、行到达时兑现回填;JSON 装不下的 undefined/BigInt/-0/NaN 全靠前缀塞回)、浏览器渲染管线(几何属性→layout+paint+composite、外观→paint+composite、**已提升层**的 opacity/transform→只 composite 而未提升元素仍要重绘;layout 与 reflow 是同一件事的两次;图片无尺寸 → 尺寸已知后 reflow;**强制同步布局**:读写交错 5 次 = 5 次布局 vs 批量写后读一次 = 1 次,差 4 次;层归属「后代跟最近层祖先 unless 自己需要层」;16.67ms 主线程预算)、WebComponents(element 五态与升级幂等;*"upgrades only apply to elements in the document"*;构造函数里 attributes/children 不可见亦不可新增;reaction 队列三后果 —— connectedCallback 可多次、**入队后断开仍执行且 isConnected=false**、处理中新入队追加到同轮尾部;connectedMoveCallback 取代默认 disconnected+connected;attributeChangedCallback 只认 observedAttributes)、ESM 实时绑定(Link/Evaluate 两阶段 DFS;函数提升来自 InitializeEnvironment 而 let/const 只建绑定不初始化 ⇒ TDZ;**live binding 活的是绑定不是推导值** —— 导出方改 x 后导入侧读到新值而 `let y = x+1` 仍是快照;导入侧赋值 TypeError;循环依赖函数可调、读未初始化 let 抛 ReferenceError;A←B←C 中 B 抛错时 A、B 记 EvaluationError 而 **C 保持 evaluated 且无 error**;链接错误则 A/B 变 unlinked 而 C 保持 linked;ResolveExport 三态含 ambiguous,`export *` 也是依赖) | S3=索引同步(02-Web开发/01-前端框架/README 子领域 +4 项(Svelte/WebComponents/浏览器渲染管线/模块系统)、已完成 +5 行 442-446、待研究勾掉「Server Components 架构(RSC + Flight)」与「Svelte runes 与 Signals 关系」并新增 Resumability/Island、View Transitions、WebGPU 三项;02-Web开发/README 已完成列表 +5 行)+ 归档 rotate(completed 100+5→105→tail-100 = ID **347..446**;schedule 30+1→31→tail-30)+ STATE §二/§三 滚动 | +5 | 446 | 权威来源:**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部改用 `curl` 直抓 + Python 本地提取(不消耗检索额度):Svelte 官方 `documentation/docs/02-runes/` 四篇 markdown(jsDelivr,`$state` 等文件名含 `$` 须单引号防 bash 展开)、React 源码 `ReactFlightClient.js`(189KB,行状态机与 parseModelString 前缀表逐段提取)、MDN《How browsers work》(svelte.dev 与 react.dev 页面是客户端渲染、正文提取仅 2.8K/13.5K 需换源;web.dev 属 Google 系本机不可达)、WHATWG HTML `custom-elements.html`(248KB)、ECMA-262 `ecmascript-language-scripts-and-modules.html`(219KB);Python ×10 文件**实跑断言全绿 141 项**(29+31+26+27+28);Go ×5 文件无本机工具链走人工审查 + `bracket_check.py` 全 BALANCED + `go_sanity.py` 全 OK;人工审查拦下 1 处(初判 `byLength` 分支漏 `i += take` 会死循环,回读文件确认已存在);行数审计:源文件 142~282 全 ≤300、README 98~148 全 ≤200,无须拆分;开发期修 bug 与断言错 9 处 —— ①**Svelte 旧依赖不解绑**致切走的分支仍被唤醒(C5 失败,是模型 bug);②数组 length 未建模成 Source 致 push 唤不醒(I2 失败,模型 bug);③teardown 读到新值属**真实现象**,原断言方向错,改为「顺序正确 + 读到的是已更新值」两条;④Flight 未知 tag 分支多写 `i++` 吃掉首字母(`hello:world`→`ello:world`),与源码 else 无 i++ 不符;⑤`$$` 取 `value.slice(1)` 而非 slice(2);⑥行收尾未 `chunk.resolve()` 致所有 `$L`/`$@` 永远 pending;⑦带长度行之间**无分隔符**,测试里多加 `\n` 会污染下一行 rowID(10-48=-38);⑧**负向断言前提不成立** —— WebComponents 基类自己就定义了 `connectedMoveCallback`,导致「未定义时走默认两段」恒假(排查耗时最长的一条);⑨ESM 导出表建在依赖链接之后 ⇒ 循环依赖误判「没有该导出」,拆成 createLocalBindings + resolveImports 两步;另 ESM 测试把 `imports=[("g",...)]` 写成与模块名 `good` 不一致,变成另一条错误路径;大类轮换 ✓ 上轮 01-游戏开发,下一轮取循环第 2 位 → 03-系统编程;轮间保险通过(开局 shell date 实得 **2026-09-20 06:00:46**,距上轮 last_run 2026-09-20 04:38 = **82 分钟 > 45 阈值**);槽位自检 ✓ 06:00 槽落地 +46 秒(含会话启动),无 slot_mismatch;commit 序列 998dde2 占位锁 → e19d02d(442) → 025b37b(443) → e2b3216(444) → f311ec9(445) → 1ad05e0(446) → 本行所在收尾,均 push OK;**commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成**,无手写估算 | notify: ok(HTTP 200 errcode=0 errmsg=ok,python urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录) |
| 2026-09-20 08:51 | 12 | M=03-系统编程/01-网络编程 第四批 5 demo(大类严格轮转第 2 位 → 03-系统编程,sub_pos[03]=0 → 索引表第 12 项 网络编程;上轮 02-Web开发。开局 shell date=08:05:06,last_run 06:46 距 79 分钟通过保险;槽位自检通过):非阻塞 IO 与 EAGAIN 语义(read(2) 两条 ERRORS 条目与短读语义、MSG_DONTWAIT 按调用覆盖、部分写、非阻塞 connect 三态、忙轮询 7 次空转 vs 就绪通知 1 次挂起而数据面等价);TCP 流量控制与零窗口(RFC 9293 收发双端 SWS 四判据与接收端三段划分、零窗口探测 RTO 起指数退避、MAY-8 与 MUST-37、RFC 7323 shift 上限 14 与 SYN 不缩放、量化损失与窗口回缩);HTTP/2 帧与 HPACK(9 字节帧头、整数编码边界是严格小于 2^N-1 故 127 编为 FF 00、indexed 索引 0 非法而字面量索引 0 合法、静态表 61 项、条目 +32 与尾部逐出);io_uring 网络 IO(res 单通道 -errno、user_data 必需、同方向不得重叠、IOSQE_IO_LINK 只保执行顺序、SQPOLL 零系统调用、IOPOLL 不适用于网络);Unix socket 凭证传递(SO_PASSCRED 逐消息而 SO_PEERCRED 是 connect 时刻快照、默认取 real UID、autobind 抽象地址上限 2^20、SO_RCVBUF 对 AF_UNIX 无效) | S1=索引同步(01-网络编程/README 已完成表 +5 行、待研究勾 4 项并补 QUIC 与 eBPF 两项、新增参考资料 13 条;03-系统编程/README +1 组 5 条);S3=归档(completed 105→tail-100 即 ID 352..451、schedule 31→tail-30)与 STATE §二/§三 滚动 | +5 | 451 | 权威来源:**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 13 份原文全部 curl、urllib 直抓后本地切段解析(RFC 9293 263KB、RFC 7323 106KB、RFC 9113 192KB、RFC 7541 118KB、read(2)、recv(2)、socket(7)、tcp(7)、unix(7)、credentials(7)、io_uring(7)、io_uring_setup(2)、io_uring_enter(2)),远低于硬约束;HPACK 静态表 61 项由脚本从附录 A 逐条抽取而非手抄。质量:Python 自检 ×5 全部实跑 **210 项断言全绿**(33、47、61、36、33);Go ×9 文件无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity 全过;行数审计整改 3 处超限(h2hpack_model.py 335→拆 h2_frame.py 97 与 hpack.py 228、h2hpack.go 337→拆出 hpack_table.go 87、iouring.go 378→拆出 selfcheck_iouring.go 130),终态源文件全 ≤284、README 全 ≤187。开发期修 bug 7 处:io_uring 链切分把末元素重复追加、乱序注入只作用于链内导致永不触发、submit 未重置 SQPOLL 空闲计时、TCP 用例未隔离判据(1) 对(3) 的遮蔽、consume 被 user 钳零、右边界前移量算错、凭证自检跨节复用被 setuid 污染的进程对象 | notify: ok(HTTP 200 errcode=0 errmsg=ok,python urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录) |
| 2026-09-20 10:45 | 16 | M=04-移动开发/01-iOS 首批 5 demo(大类严格轮转第 4 位 → 04-移动开发,sub_pos[04]=0 → 索引表第 16 项 iOS;上轮 03-系统编程。开局 shell date=10:09:12,last_run 08:52 距 77 分钟通过保险;槽位自检 10:00 槽 +9 分钟含会话启动,记 slot_late 无跨槽错配):**452 Swift6严格并发**(SE-0414 隔离区四类与三条合并规则、`var` 重赋值「被闭包捕获才合并否则遗忘」、弱传递下所有权仍在调用方故 deinit 在作用域末尾、nonisolated async 返回后区域回 disconnected、类弱传递后只读 let Sendable 字段、Sendable 隐式一致四档与同文件限制、SE-0412 全局变量「不可变且 Sendable」,Py 42 断言)· **453 SwiftUI布局协议**(父提议子决定、三提案语义、`replacingUnspecifiedDimensions` 只换未指定轴、只需两个必需方法、`makeCache` 使测量 8→4、`ViewThatFits` 按提供顺序挑第一个放得下的、`AnyLayout` 换布局不毁状态,Py 26 断言)· **454 Combine背压**(Demand 累加四则、零欠量一个元素都不发、请求 max(3) 后不发 finished、sink 一上手要 unlimited、flatMap(maxPublishers:) 限并发订阅数而非丢弃上游、buffer/collect/debounce/throttle 四种替代背压,Py 30 断言)· **455 图片解码与ImageIO**(4032×3024 全解码 48,771,072 字节 vs 200px 缩略图 120,000 字节差 406 倍、Always 绕过内嵌 vs IfAbsent 才生成 vs 都不给返回 NULL、MaxPixelSize 约束长边不放大、WithTransform 先换轴再缩、ShouldCacheImmediately 提前解码、渐进式必须交全部累计数据与六状态、GetCount 不含缩略图而 HEIF 主图下标可非 0、PDF 需 FromImage 才是 72dpi,Py 30 断言)· **456 NotificationCenter投递**(queue=nil 即在投递线程同步执行、name/object 为 nil 表示不筛选、中心强持有 token 与 block 拷贝、一次性通知在 block 里移除自己、投递期间移除本轮仍收到、weak vs strong self 用 Python GC 实测,Py 29 断言) | S1=索引同步(01-iOS/README 已完成 +5 行 452-456、待研究勾掉 Swift 6 严格并发 / SwiftUI 布局系统 / Combine / 图片解码与 ImageIO / NotificationCenter 五项并新增 Swift6 sending 标注 / LayoutValueKey / HEIF 辅助数据 / notifications 异步序列 四项;并发编程/README 子领域补 Swift-Concurrency 与 Swift6严格并发两行;事件循环/README 子领域补 NotificationCenter投递;**新建缺失的 UI框架/README 与 渲染/README 两篇**(此前只有目录无索引);04-移动开发/README 已完成 +5 行、待研究新增本批一行)+ S3=归档 append 与 STATE §二/§三 滚动 | +5 | 456 | 权威来源:**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 curl 与 urllib 直抓原文后本地解析,不消耗检索额度:Swift 三份提案(SE-0414 74KB / SE-0302 44KB / SE-0412 8KB,jsDelivr 需 `-L` 跟随重定向且 raw.githubusercontent 间歇 exit 56,靠重试拿到)+ Apple DocC JSON 共 18 份(SwiftUI Layout / ProposedViewSize / ViewThatFits / AnyLayout / LayoutSubview / LayoutSubviews / sizeThatFits / placeSubviews、Combine 概览 / Subscribers.Demand / Publishers.FlatMap / 背压文章 / Publisher / Subscriber / Subscription / PassthroughSubject、Foundation NotificationCenter / addObserver / post、ImageIO CGImageSource / 四个函数页 / 状态枚举),DocC 正文需自写抽取器(引用节点只有 identifier 没有 title,要从路径末段取符号名);**所有数值与判据一律回读原文确认**不凭记忆(46.5 MiB 是按 4 字节/像素的显式假设并在 README 标注、406 倍为实测比值、maxPublishers 语义只取官方「并发订阅数上限」一句、PDF 72dpi 与 UnexpectedEOF 均出自原文)。质量:Python ×10 文件**实跑断言全绿 157 项**(42+26+30+30+29);Swift ×4 + Objective-C ×2 无本机工具链走人工审查 + `bracket_check.py` 全 BALANCED;行数审计:3 处超限当场拆(452/453/454 各拆一个 selfcheck,455 拆一个),终态源文件 78~236 全 ≤300、5 篇 README 151~163 全 ≤200;开发期实跑修 bug 与断言错 9 处 —— ①**Sendable 递归防护把根类型自己命中 memo**,导致任何 struct 都判成 Sendable(最隐蔽的一条:通过即错判,靠调试打印才定位);②`implicit_sendable` 对泛型一律返回 False,与 SE-0302「X`<T: Sendable>` 隐式一致」矛盾;③`Type` 漏写 `self.is_actor` 致 AttributeError;④区域模型里 `let y = x` 用手工改 loc 并不真正合并成员 → 补 `init_binding`/`reassign` 两个语义化方法;⑤`var` 被闭包捕获的用例原写法只是普通 merge,体现不出 3a 与 3b 的差别;⑥invalid 区域用例两个 Env 共享同一 regions 列表,改 domain 互相覆盖 → 改为直接在 join 点统一两个域;⑦Layout 缓存用例因 `place_subviews` 复用 `_last_sizes` 导致「无缓存也只量 4 次」,改成按 cache 决定要不要重问;⑧AnyLayout 断言写在 relayout 之后,读到的已是 VStack 结果 → 前后各断言一次;⑨flatMap 初版把超限的上游元素直接丢弃,与 Combine 需求语义不符 → 改为「达到上限就不再向上游索要」并在 README 标注口径 | notify: ok(HTTP 200 errcode=0 errmsg=ok,python urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录) |
| 2026-09-20 12:48 | 40 | M=05-AI与机器学习/06-语音与多模态 **首批 5 demo**（大类严格轮转第 5 位 → 05-AI与机器学习，sub_pos[05]=5 → 索引表第 40 项 语音与多模态；上轮 04-移动开发。该子类此前只有索引 README、0 个 demo，本轮按待研究清单开线）：**457 CTC损失与解码**（softmax 比标签集多一个单元即 blank；B 映射先合并相邻重复再去 blank 故 `B("a-a")="aa"` 而 `B("aa")="a"`；l' 长 2 乘长度 加 1；终值两项是互不相交的两类路径；**零区是剪枝规则不是恒等式**，不置零则 Σln C_t 不等于 ln p；αβ 对 y 是 2 次故式(15) 出现平方，式(16) 与中心差分偏差小于 1e-6；best path 的漏报集非空，Py 34 断言）· **458 logMel频谱图**（Whisper 硬编码 16k/400/160、30 秒 480000 样本、center=True 得 3001 帧丢最后一帧得 3000、功率谱取模的平方、Hann 周期窗；Slaney mel 分段线性加对数与 HTK 在 1000Hz 处分叉；三角滤波器取 n_mels 加 2 个端点、slaney 归一化再乘 2 除以带宽；未归一化峰值是**小于等于 1** 而非恒等于 1；三步后处理 clamp 后 log10、压到 8 个 log10 单位、(x+4)/4，Py 45 断言）· **459 Whisper解码与失败回退**（6 档温度；**no_speech 加 logprob 双条件的静音豁免**，四个分支全覆盖；gzip 压缩比检测卡住；avg_logprob 分母是长度加 1；max_candidates 等于 round(beam 乘 patience)；长度归一与 NMT 惩罚选出不同序列；时间戳规则里序列长度小于 2 视为倒数第二个也是时间戳，Py 51 断言）· **460 CLIP双塔对比损失**（两路 L2 归一后点积即余弦；axis=0 与 axis=1 是两个不同 softmax 方向；τ 初始化 ln(1/0.07) 即 14.2857、训练 clip 到 100；**增大 scale 只在正样本已排第一时降 loss**，随机批反而从 2.07 升到 24.3；合成梯度逐行求和不为 0，Py 35 断言）· **461 Wav2Vec2对比学习与量化**（掩码 p=0.065 是起点比例，被掩码约 49%、平均跨度 14.7 因跨度可重叠，实测 49.45% 与 14.72 与论文吻合；κ=0.1、K=100、负采样加 1 技巧加 neg_is_pos 兜底；G=2、V=320 得 102400 码字且下标高位在前；温度 2 按 0.999995 衰减到 0.5；两个 perplexity 按组求和满值 2V；脚注 2 形式与式(4) 方向相反；编码器 1 秒正好 49 帧，Py 48 断言） | S3=索引同步（06-语音与多模态/README：新增「已完成 demo」表 5 行含各自核心机制、待研究勾掉 CTC / log-Mel / Wav2Vec2 / CLIP 四项并新增音频-语言融合、HuBERT、流式 ASR、Conformer、Whisper DTW 五项；05-AI与机器学习/README：06 行补「首批 5 demo 完成」、已完成列表新增 5 行、待研究勾掉 CLIP 原理）+ 归档（completed.md 追加 457-461 并 rotate 保留 362..461、schedule.md 本轮行）+ STATE §二/§三 滚动 | +5 | 461 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 curl 直抓原文后本地解析，不消耗检索额度：Graves ICML 2006 CTC 论文 PDF 8 页（pypdf 逐页抽文本后全文回读）、arXiv 2006.11477 wav2vec 2.0 论文 PDF 19 页、arXiv 2103.00020 CLIP 论文 PDF 48 页（首次 curl 被 90 秒超时截断到 1.8MB，用 `-C -` 续传拿到完整 6.8MB）、openai/whisper 的 audio.py 与 decoding.py 与 transcribe.py 与 utils.py 与 tokenizer.py（jsDelivr）、openai/CLIP 的 model.py（jsDelivr）、facebookresearch/fairseq 的 wav2vec2.py 与 gumbel_vector_quantizer.py（jsDelivr）、librosa 0.10.2 官方 filters.mel / mfcc / stft 三个文档页 + filters.py 与 convert.py 两份源码（librosa.org/doc/main 路径是 404，须用 0.10.2）；**所有数值一律回读原文确认**不凭记忆（62 个 softmax 单元、480000 样本、3001/3000 帧、logstep 等于 ln(6.4)/27、温度序列 6 档、阈值 2.4 与 −1.0 与 0.6、ln(1/0.07)、102400 码字、49 Hz）；Python 自检 5 份**全部实跑 213 断言全绿**（34+45+51+35+48）；Go 5 组无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity 全过 + go_crossref 全 OK；行数审计：4 处超限当场拆（Go 的 ctc 359 拆成 251+115、whisper_decode 305 拆成 178+121、wav2vec2 337 拆成 233+110），终态源文件 110~281 全 ≤300、5 篇 README 151~199 全 ≤200（457 的 README 初版 223 行，压三段后 199）；**开发期实跑抓出的问题 11 处**：①B 映射测试误用字符串短横当 blank，实际 BLANK 是 −1 哨兵，两个论文原例断言全红 ②l' 长为 1 的空标签走了 `la[-1][-2]` 越界成 `la[-1][0]`，概率被算成两倍，1559 个穷举用例里挂了 200 个 ③**零区不是自动成立的**，最初按恒等式断言 4 处失败，改为「未 prune 时零区有非零值 + prune 不改变 ln p」两条 ④缩放恒等式 Σln C_t 只在 prune 后成立，不 prune 时不相等（实测 −4.1499 对 −4.2315）⑤mel 三角峰值**不恒等于 1**，因离散 bin 未必落在顶点，改为「≤1 + 提高 n_fft 后趋近 1」 ⑥Whisper 静音豁免用例初版给了 logprob −0.2（不触发回退），改成压缩比 3.0 才真正验到「nsp 高但 logprob 不低不豁免」⑦两条相同前缀的 beam 会被去重成一条，最初拿两条一样的 beam 断言 completed 恒成立失败，改成不同前缀 ⑧CLIP 随机特征的 loss 可以超过 ln(n)（4.77 对 2.08），原断言方向错了 ⑨「scale 越大 loss 越低」只在正样本已排第一时成立，随机批相反，拆成两条断言 ⑩CLIP 合成梯度逐行求和不为 0，只有两个单向分量各自为 0 ⑪wav2vec2 的熵带 1e-7 平滑，one-hot 的 perplexity 是 1 减 1e-7 而非严格 1；另 完美上下文的 Lm 精确值是 log1p(100 乘 e 的 −10 次方) 而非一阶近似；槽位自检通过（shell date 开局 12:01:45，锁 12:00 槽）；轮间保险 76 分钟（上轮 10:45）通过；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200 errcode=0 errmsg=ok，单次 curl --data-binary 发送未重试；发送前已确认本轮无既有 notify 记录) |
| 2026-09-20 14:41 | 41 | M=06-DevOps/05-IaC与配置管理 第四批 5 demo（大类严格轮转第 6 位 → 06-DevOps，sub_pos[06]=4 → 索引表第 41 项 IaC与配置管理；上轮 05-AI与机器学习。开局 shell date=2026-09-20 14:01:04，last_run 12:48 距 73 分钟通过保险；槽位自检 14:00 槽 +64 秒含会话启动，无 slot_mismatch）：**462 Terraform重构块与状态搬迁**（plan 前先改 state 地址再造计划；from/to 只要一侧带实例键就按具体实例匹配，否则按整资源匹配并保留实例键 a[0]→b[0]；实例级到无键目标丢掉键 d[2]→d；move 链让 a 与 b 两个起点都直达 c；加 count 自动搬 0 号除非有 moved 提及该资源，**for_each 无此兜底**旧对象被销毁；新实例忽略 moved；removed 默认 destroy=true 销毁真实资源、destroy=false 只移出 state；import 的 to 必须匹配已有 resource 块、id 与 identity 互斥，Py 35 断言）· **463 lifecycle元参数与依赖图变换**（**CBD 沿依赖边向被依赖方传播且传递**并写进 state，被隐式置位后再写 false 会被拒因为会成环；替换次序 CBD 时先建后拆否则先拆后建，销毁图上 CBD 让边翻转；prevent_destroy 挡得住 replace 挡不住删配置；ignore_changes 只在 update 生效、all 时永不 update，ForceNew 分支官方未规定已标口径；replace_triggered_by 只能引用托管资源且触发条件只有 update/replace，Py 34 断言）· **464 插件协议发现与版本选择**（协议是版本化接口 protobuf+gRPC，主版本划兼容性 v6→CLI 1.0+、v5→0.12+，次版本叠加取双方较小并标注口径；版本选择四条 lock 优先 → **已安装里最新的可接受即使 registry 有更新** → registry 最新可接受 → 都没有则 init 失败；协议版本同时是 registry 发现的筛选元数据；`~>` 只允许最右分量递增；go-plugin 握手先校验 magic cookie 再校验 protocol version，Py 33 断言）· **465 Ansible集合与依赖解析**（`collections:` 只是有序搜索路径不安装东西；**角色不继承 playbook 的 collections，角色没定义自己的也不继承**；lookup/filter/test 即使在搜索路径里也必须用 FQCN；module_utils 走 ansible_collections.{ns}.{coll}.plugins.module_utils.{util}；requires_ansible 是 PEP440 但**截断预发布段**故 2.11.0b1 满足 >=2.11；plugin_routing 三态 redirect/deprecation/tombstone，Py 30 断言）· **466 计划期unknown值传播**（依据 v1.9.8 源码 objchange.go：属性级三分支 computed 且 config 为 null 取 prior、有 NestedType 递归、否则一律取 config；唯一例外 optionalValueNotComputable；list 按下标、map 按键、set 是按非 computed 属性匹配的**启发式**；整体 unknown 只来自 dynamic+unknown for_each；**known after apply 不是 ProposedNew 产生的**，新建时 computed 取到的是 null 补 unknown 是 provider 的 PlanResourceChange；data source 用整体 unknown 的 prior 靠 cty 短路传播，Py 30 断言） | S3=索引同步（05-IaC与配置管理/README 已完成 +5 段 462-466、待研究勾掉 removed/import 声明式迁移与 Ansible Collections 两项并把 OpenTofu state 互操作与 ansible-builder EE 拆出保留；06-DevOps/README 已完成表新增 462-466 行）+ 归档（completed 100+5→tail-100 = ID 367..466；schedule 本轮行）与 STATE §二/§三 滚动 | +5 | 461→466 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 直抓原文后本地切段解析，不消耗检索额度：developer.hashicorp.com 七篇（Refactor modules 335KB、block/moved、block/removed 403KB、block/import 294KB、language/import、meta-arguments/lifecycle 338KB、plugin/terraform-plugin-protocol、plugin/how-terraform-works、providers/requirements 297KB、expressions/types、resources/behavior）、docs.ansible.com 四篇（developing_collections、developing_collections_structure 54KB、collections_using_playbooks、galaxy/user_guide 58KB）、**hashicorp/terraform@v1.9.8 源码 `internal/plans/objchange/objchange.go` 16KB 逐段实读**（ProposedNew/proposedNewAttributes/proposedNewNestingList/Map/Set/optionalValueNotComputable/PlannedDataResourceObject）、hashicorp/go-plugin@v1.6.0 README 7.8KB。curl 对本机返回 200 但 size 0，**必须改用 urllib 并带 User-Agent** 才能拿到正文（带 `-w %{size_download}` 的探测会误判成不可达）。质量：Python 自检 5 份**全部实跑 162 断言全绿**（35+34+33+30+30）；Go 11 个文件无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity 全过 + go_crossref 五目录全 OK；行数审计：1 处超限当场拆（462 的 Go 模型 424→260+173），终态源文件 99~274 全 ≤300、5 篇 README 123~150 全 ≤200。开发期实跑修 bug 与断言错 6 处：①地址解析把 `module.a[2]` 在方括号外的点切开（module 与其名字成对取回）②`str(Addr)` 拼模块前缀漏分隔符 → `module.aaws_instance.example` ③**实例级到无键目标没丢键**（d[2]→d 恒等），根因是 `_rewrite` 一律用 addr.key，须按 instance_level 决定取 to.key 还是 addr.key ④for_each 键未加引号致 D2/D4/F 系列误判 ⑤import 的 `to in desired` 拿元组列表判等，应取地址集合 ⑥E5 原断言「忽略项仍会走 replace」是**模型知识不是官方口径**，改为标注口径后的「不进 diff 故不触发」。另 `negotiate` 初版写了恒真的 `or True` 条件（伪断言形态），已换成有效判定；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200 errcode=0 errmsg=ok，urllib 单次调用未重试，发送前已确认本轮无既有 notify 记录) |
| 2026-09-20 16:48 | 45 | M=07-数据存储/05-图数据库 **第三批 5 demo**（大类严格轮转第 7 位 → 07-数据存储，sub_pos[07]=4 → 索引表第 45 项 图数据库；上轮 06-DevOps。开局 shell date=2026-09-20 16:01:01，last_run 14:46 距 74 分钟通过保险；槽位自检 16:00 槽 +61 秒含会话启动，无 slot_mismatch）：**467 Cypher路径匹配模式**（默认 TRAIL 只禁关系重复不禁节点重复；`DIFFERENT RELATIONSHIPS` 只是把默认写出来、**不改变语义**，`REPEATABLE ELEMENTS`=WALK 放开全部唯一性，`ACYCLIC` 再加节点唯一且**蕴含 TRAIL**；七桥图定长 5 有向 2 条、定长 6 无向 48 条、定长 7 **0 条**即欧拉结论，同串 WALK 2759 条是 TRAIL 的 57 倍；路由器网 ACYCLIC A→Z 无向 80 条、有向仅 15 条且会漏掉官方的 A-B-E-G-J-I-Z；无向遍历下两条反向关系算两条边，Py 87 断言）· **468 Cypher null 与三值逻辑**（官方 9 行真值表逐格断言；AND 被 false 吸收、OR 被 true 吸收而 **XOR 无吸收律**；`2 IN [1,2,null]` 是 true 而 `2 IN [1,null,3]` 是 null；**`null IN []` 是 false 却 `null IN [1,2,3]` 是 null**，空列表把未知变成确定；类型谓词对 null 恒 true 故 `IS :: STRING` **挡不住 null**；升序 null 最后降序最前，Py 100 断言）· **469 搜索性能索引**（四类索引各一张 Supported predicates 表；**Range 是默认却解不了 ENDS WITH 与 CONTAINS**，Text 靠 trigram 索引 developer→dev/eve/vel/elo/lop/ope/per 才解得了；Token lookup 只解标签与类型且建库自带两个；**撞上同名约束时即使 `IF NOT EXISTS` 也报错**，故约束名检查必须早于幂等短路；复合索引只收录含**全部**指定属性的实体，Py 50 断言）· **470 Neo4j 约束**（existence/type/key 均为 Enterprise Edition；**Key = 存在性 + 唯一性**，比单纯 UNIQUE 多拦属性缺失；类型表达式支持 `LIST<STRING NOT NULL>`、联合类型与 `VECTOR<INT32>(42)`；`IF NOT EXISTS` 命中同名（哪怕类型不同）或同类型同模式时只发通知，Py 49 断言）· **471 Louvain 与模块度**（式(1) Q 与式(2) ΔQ；**净增益 = ΔQ_add(C) 减 ΔQ_add(D 去掉 i)** 而非 ΔQ_add(C) > 0，实测 0.163265 减（-0.071429）得 -0.234694 与重算 Q 精确相等；聚合自环必须记 **A_ii = 2s** 否则 m 与聚合前后 Q 都不守恒，已用反向断言固定；ring of 30 cliques 第一阶段 30 个团、第二阶段两两合并 15 个且 Q 由 0.8758 升到 0.8879，只有两个团时不合并即分辨率极限征兆，Py 48 断言） | S3=索引同步（05-图数据库/README：已完成表 +5 行、待研究勾掉本批 5 项并新增全文与向量索引、因果集群、图算法、graph type 四项；修正「尚未产生 demo」的过期抬头；07-数据存储/README：图数据库条目改为三批 15 demo 并新增 5 行）+ 归档（completed 100+5→tail-100 即 ID 372..471、schedule 本轮行）与 STATE §二/§三 滚动 | +5 | 466→471 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 带 User-Agent 直抓原文后本地解析，不消耗检索额度：neo4j.com/docs/cypher-manual/current 七页（patterns 落地页、patterns/unique-relationship-paths、patterns/acyclic-paths、patterns/primer、patterns/fixed-length-paths、values-and-types/working-with-null、clauses/order-by）、indexes 三页（indexes-for-search-performance、indexes/search-performance-indexes/create-indexes、indexes/search-performance-indexes/index-hints）、constraints 三页（constraints、constraints/syntax、constraints/managing-constraints）、GDS Louvain 一页、**arXiv 0803.0476v2 原始论文 PDF 12 页**（pypdf 抽取后逐段回读）。**踩坑：neo4j.com 文档站是 Antora，导航靠 JS，落地页里抓不到子页链接；`search-index.js` 也不存在**，最终用「按章节标题猜 slug + 批量探活」拿到真实路径（正确 slug 是 `patterns/unique-relationship-paths` 与 `indexes/search-performance-indexes/create-indexes`，不是直觉的 `unique-relationships` 与 `indexes-for-search-performance/create-indexes`）。质量：Python 自检 5 份**全部实跑 334 断言全绿**（87+100+50+49+48）；Go 12 个文件无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity 全过 + go_crossref 全 OK；行数审计：1 处超限当场拆（louvain.go 332→175+163），终态源文件 146~291 全 ≤300、5 篇 README 125~173 全 ≤200。**开发期实跑修 bug 与断言错 7 处**：①demo467 路由 ACYCLIC 官方只公布十个中间路由器占比未公布总数，我按「H 占 25% 即 10 条」推出总数 40 而实测 80 —— 占比全部吻合、分母须为 8 的倍数，故 40 是**我自己的错误推断**，已改为断言 80 并在 README 注明官方只公布百分比 ②最小图 TRAIL 两跳期望 1 实际 2 —— 忘了无向遍历下 r1(X→Y) 与 r2(Y→X) 是两条不同的边 ③demo469 默认建索引只查了重名没查同模式同类型，漏掉官方「同 schema 同 type 也报错」 ④demo470 的 IF NOT EXISTS 用例我自己用了一个全新名字，根本没撞上名字冲突（官方 Example 29 是同名不同类型），改正后才发现名字冲突优先于类型匹配 ⑤demo471 初版把「ΔQ_add(C) > 0」当移动条件，与论文「先移除再评估」不符，实测会做出降低 Q 的移动 ⑥demo471 聚合自环期望 6 实际 16 —— 漏算了节点 a 自带的 5 权自环 ⑦demo471 ring_of_cliques(2,5) 的环会退化成一条重边（m=22 而非 21），分辨率极限用例改用手工构造的两团图。另：demo468 初版有 3 处伪断言（`eq("1 + null", None, None)` 这类恒真的 None==None、`type_predicate` 写成常量 True），已换成 cy_arith/cy_lt/cy_sin 与带类型表的真实实现；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200 errcode=0 errmsg=ok，curl --data-binary 单次调用未重试，发送前已确认 schedule.md 本轮无既有 notify 记录) |
| 2026-09-20 18:00 | 32 | M=08-安全/01-Web安全 **第三批 5 demo**（大类严格轮转第 8 位 → 08-安全，`sub_pos[08]=1` → 索引表第 32 项 Web安全；上轮 07-数据存储。开局 shell date=2026-09-20 18:00:55，`last_run 16:50` 距 70 分钟通过保险；槽位自检 18:00 槽 +55 秒含会话启动，无 slot_mismatch）：**472 DPoP与发送方约束令牌**（RFC 9449 §4.3 十二条校验逐条实现，官方 Figure 9 的 cnf.jkt 与 Figure 14 的 ath 均由实现算出，§7 的 AT1 与 AT2 替换被 ath 拦下，§8 AS 用 400、§9 RS 用 401 下发且两套 nonce 独立，§11.1 jti 单用，对照 RFC 8705 的 x5t#S256 无尾随等号，Py 35 断言）· **473 TrustedTypes与DOMXSS**（W3C Trusted Types §3.4 四条分支，**default policy 返回 null 时强制抛 TypeError 而 report-only 返回原始值**，§3.8 属性表 on 开头事件处理器 → TrustedScript、iframe srcdoc → TrustedHTML，§4.2.4 sample 等于 sink 名加竖线加前 40 字符，§4.2.5 的 `'none'` 与其它值并存时被忽略、`'allow-duplicates'` 允许重名、空指令值禁建一切 policy，§4.2.1.1 javascript: 预导航检查改 URL，Py 47 断言）· **474 CSP strict-dynamic与违规报告**（CSP3 §6.7.3.2 遇到 nonce 或 hash 或 strict-dynamic 立即短路，故同一列表对 style 允许全部内联而对 script 不允许；§6.7.3.3 的 strict-dynamic 分支要求 type 是 script 且元素非 parser-inserted，createElement 放行而 document.write 被拦，nonce 不适用于 script attribute；§6.7.2.6 空源列表等价于 `'none'` 且 `'none'` 与其它表达式并存时不起作用；§2.4 sample 前 40 字符、外部文件违规不带 sample；§8.5 Strict CSP 要求 base-uri 为 `'self'` 或 `'none'`，Py 44 断言）· **475 SRI与完整性策略**（摘要是标准 base64 不是 base64url，官方 sha384 与 sha512 两个向量反向验证；§3.3.3 只验最强那批，故 sha384 对加 sha512 错会失败而反过来通过；§3.3.4 空集即放行、跨源无 crossorigin 直接失败；§3.8 Integrity-Policy 是 RFC 9651 Dictionary 且 sources 缺省即 inline，§3.8.2 只对 cors 与 same-origin 且有元数据时豁免，Py 44 断言）· **476 COOPCOEP跨源隔离**（HTML §7.1.3 match 四条规则使 unsafe-none 与任何非 unsafe-none 组合都要切换浏览上下文组；COEP §2.3 fail-open 表逐行实现，**require-corp 加 require-corp 因合并成 list 不是 token 而回落 unsafe-none**；§3.2.1 require-corp 下不写 CORP 等于按 same-origin 处理、same-site 要求主机同站且发起源 https 或响应 HTTPS state 为 none、只有 navigate 对 unsafe-none 短路，且 **CORP 独立于 COEP 生效**；§4.3 子文档须自声明 require-corp，Py 60 断言） | S1=索引同步（01-Web安全/README 新增「第三批」5 行表、待研究勾掉 DPoP 与 mTLS 发送方约束 / CSP 进阶 / 跨窗口隔离 / SRI 四项并新增令牌受众校验 aud / HTTP 请求走私 / DOM Clobbering / 开放重定向 四项；08-安全/README 已完成表 +5 行、待研究新增本批一行）+ S3=归档（completed 100 加 5 后 rotate 保留 100 行即 ID 377..476；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 471→476 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 带 User-Agent 直抓原文后本地解析，不消耗检索额度：RFC 9449 全文 92KB、RFC 8705 全文 60KB、W3C Trusted Types 规范 538KB HTML 抽 91KB 正文、W3C CSP3 1.01MB HTML 抽 187KB 正文、W3C SRI 213KB HTML 抽 35KB 正文、WICG COEP 规范 172KB 抽 38KB 正文、WHATWG Fetch 的 CORP 小节、WHATWG HTML 的 browsers.html 抽 COOP 章节 65KB、MDN 的 COOP 头页面。质量：Python 自检 5 份**全部实跑 230 断言全绿**（35 加 47 加 44 加 44 加 60）；Go 12 个文件无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity 全过 + go_crossref 五目录全 OK；行数审计：3 处超限当场拆（dpop.go 365 拆成 152 加 205、trustedtypes.go 366 拆成 273 加 96、csp_strict.go 346 拆成 226 加 121、dpop.py 306 拆成 214 加 107），终态源文件 93..292 全 ≤300、5 篇 README 138..167 全 ≤200。**开发期实跑修 bug 与断言错 5 处**：①自查抓出两条伪断言（DPoP 里 `not rs.check_proof(...) or True` 的恒真式、Trusted Types 里 `if False else None` 的占位函数），分别改成「RS 自己的 nonce 通过而 AS 的 nonce 被 RS 拒」与真实的 `token_request_accepts` ②Trusted Types 初版只给 policy 实现了 createHTML 就去造 TrustedScript，在 throwIfMissing 处直接抛错，补一个三回调齐全的 policy ③SRI 断言「去 padding 的 base64 值应匹配」失败 —— 规范只规定把短横换加号、下划线换斜杠，**未规定 padding**，已改为断言标准 base64 并在 README 标注口径 ④COOP 的 CORP 断言「embedder 是 unsafe-none 时 CORP 不参与判定」方向错了 —— 只有 navigate 这个 mode 对 unsafe-none 短路，CORP **独立于 COEP** 生效，改成三条对照断言 ⑤CSP 官方 sha384 与 sha512 向量用 base64url 算出后与原文差一个字符，核对后确认 SRI 用的是**标准 base64**。另：项目 `MEMORY.md` 已达 14KB 触发注入截断，本轮开局先精简重写到 6KB；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok（HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件 + curl --data-binary 单次调用未重试，发送前已确认 schedule.md 本轮无既有 notify 记录） |
| 2026-09-20 20:00 | 34 | M=09-语言学习/Python **第四批 5 demo**（大类严格轮转第 9 位 → 09-语言学习，`sub_pos[09]=0` → 索引表第 34 项 Python；上轮 08-安全。开局 shell date=2026-09-20 20:00:57，`last_run 18:00` 距 120 分钟通过保险；槽位自检 20:00 槽 +57 秒含会话启动，无 slot_mismatch）：**477 迭代器协议与 itertools/functools**（容器与迭代器的分野、`iter(callable, sentinel)` 丢弃哨兵值、PEP 479 把生成器内的 StopIteration 变 RuntimeError；groupby 只切连续相同键且分组与主迭代器共享源、推进后旧分组被抽干；tee 的共享链表与嵌套展平 lookahead；**`lru_cache` 默认 maxsize 是 128**，单参数经 fasttypes 捷径把键摊平成裸值，实测 `f(True)` 命中 `f(1.0)` 的条目而 `f(1)` 与 `f(1.0)` 反而不共享，关键字实参顺序参与键；singledispatch 沿 MRO 找最近实现、两个互不相干的隐式 ABC 触发 Ambiguous dispatch，10 组断言）· **478 MRO 与 C3 线性化**（按 CPython `Objects/typeobject.c` 的 pmerge 复刻：`to_merge` 最后一项是基类元组、`tail_contains` 只查 remain 之后的剩余部分，与真实 `__mro__` 逐类对拍；局部优先与单调性；严重顺序冲突找不到好头即 consistent method resolution order；`super(X, obj)` 的落点由 `type(obj)` 的 MRO 决定，D2(A2,B2) 上 `super(A2, d)` 得到兄弟类 B2；零参 super 依赖 `__class__` 单元变量；元类自定义 mro 被无条件采纳、剔除基类后方法静默消失，9 组断言）· **479 参数绑定与调用约定**（PEP 570 六条合法三条非法形式逐条 compile 验证；语义角落 —— `def f(name, **kw)` 里 name 永远进不了 kw 而加了斜杠就可以；PEP 3102 的 keyword-only 无默认即必填；手写绑定算法与 `inspect.Signature.bind` 在 5 组正常加 3 组错误用例上一致，空的可变位置与可变关键字不进 arguments；BoundArguments 默认值需 apply_defaults，6 组断言）· **480 运算符与特殊方法分派**（NotImplemented 单例与 3.14 起布尔求值报错；反射方法的三态：左返回 NotImplemented 才调右、右为子类且改写反射时反射**先**被调用、同类型不试反射；**特殊方法设成 None 是阻断回退而非促成回退**；`__iadd__` 缺失或返回 NotImplemented 会回退并重新绑定名字；比较运算符的反射配对与 is 兜底；重写 `__eq__` 把 `__hash__` 隐式置 None，10 组断言）· **481 作用域闭包与单元变量**（整块扫描导致先读后赋是 UnboundLocalError；nonlocal 是**编译期**检查、运行时 try/except 抓不到须用 compile 触发；闭包共享同一个 cell 对象、co_cellvars 对 co_freevars 与 LOAD_DEREF；迟绑定三个 lambda 全返回 2；推导式是隐式函数作用域不泄漏循环变量但海象绑定在包含作用域；**类作用域对推导式不可见，有同名全局就静默用全局**，8 组断言） | S3=索引同步（Python/README 已完成表 +5 行、待研究勾掉迭代器协议并新增 MRO 与参数绑定等 7 项；09-语言学习/README 计数 16 改 21 并补第四批综述）+ 归档（completed 100 加 5 后 rotate 保留 100 行即 ID 382..481；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 476→481 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 带 User-Agent 直抓原文后本地解析，不消耗检索额度：docs.python.org 语言参考 datamodel 464KB 抽 129KB、executionmodel 22KB、itertools 39KB、functools 28KB、inspect 53KB、functional HOWTO 47KB、PEP 570 与 3102 与 479 与 227 四篇、python.org 的 Python 2.3 MRO 说明 20KB；**CPython 源码** `Objects/typeobject.c` 399KB（`tail_contains` / `pmerge` / `mro_implementation_unlocked` / `mro_check` / `check_duplicates`）、`Lib/functools.py` 43KB（`_make_key` 的 fasttypes 捷径、`lru_cache(maxsize=128, typed=False)`、`_find_impl` 的 Ambiguous 分支）、`Modules/itertoolsmodule.c` 119KB。**踩坑：抓 C 源码不能用 HTML 剥离脚本**（`<` 会被当成标签起点吞掉，pmerge 的循环判据全被破坏），必须原样保存字节；jsDelivr 用 urllib 会被 WinError 10054 重置，换 `curl -sSL` 带重试即可。质量：5 份自检**全部实跑 43 组断言全绿**（10 加 9 加 6 加 10 加 8）；行数审计：2 处超限当场拆（迭代器目录按「模型 main.py 加 selfcheck_itertools.py」拆成 132 加 300、运算符目录拆成 127 加 191），终态源文件 127..300 全 ≤300、5 篇 README 155..176 全 ≤200。**开发期实跑修 bug 与断言错 7 处**：①手写 tee 的 `_Tee` 定义在工厂函数内部，每次调用都是新类对象，嵌套 tee 的 isinstance 恒为 False（提至模块层解决）②lookahead 断言分叉时机写错 —— 必须在推进之后再 tee，否则拿到的是当前值不是下一个 ③**`lru_cache` 单参数「1 与 1.0 同条目」是我按文档推测的错误断言**，实跑发现未命中（裸 int 键对 tuple 键），真实行为反而是 `f(True)` 命中 `f(1.0)`，已按实测改写并在 README 注明 ④`my_bind` 把空的 *args 与 **kw 塞进结果，与 inspect 对不上（官方不收录空容器）⑤作用域 demo 里 `nonlocal` 的 SyntaxError 用法 try/except 包调用根本抓不到，改成 compile 源码串触发 ⑥全局名写错（`GV2` 与类里的 `G` 不同名）导致本该演示的「静默用全局」退化成 NameError ⑦清理 2 处伪断言（恒真的 `y is not None` 与 `A().m.__globals__ is not None`）；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok（HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件 + curl --data-binary 单次调用未重试，发送前已确认 schedule.md 本轮无既有 notify 记录） |
| 2026-09-20 22:40 | 36 | M=10-逆向工程/02-移动端逆向 **第二批 5 demo**（大类严格轮转第 10 位 → 10-逆向工程，`sub_pos[10]=1` → 索引表第 36 项 移动端逆向；上轮 09-语言学习。开局 shell date=2026-09-20 22:00:45，`last_run 20:39` 距 81 分钟通过保险；槽位自检 22:00 槽 +45 秒含会话启动，无 slot_mismatch；占位锁前复查 git log 与 status 干净，未复现 09-16 与 09-17 的并发补跑抢位）：**482 DEX文件格式解析**（按 AOSP 官方 dex-format 构造并回读一个 039 版 dex；官方 LEB128 四行示例在三种解读下对拍；MUTF-8 的 U+0000 编成 c0 80、增补字符拆代理对；**checksum 覆盖 signature 故必须先写 signature 再算 adler32**；method_idx_diff 是差分故列表须按 method_idx 升序且 abstract 与 native 仍要登记；padding 只在 tries_size 非零且 insns_size 奇数时出现，Py 109 断言）· **483 smali与Dalvik指令编码**（格式 ID 三字符规则与 11 个类型字母位宽；35c 的第五个参数寄存器在 G 半字节；3rc 的范围是 C 到 C 加 A 减 1；三个 payload 的单元数公式；分支偏移不得为 0，Py 281 断言）· **484 FridaStalker指令级跟踪**（GumEventType 按位且六个结构体大小 4/32/32/16/24/24，故不能按固定步长切二进制流；不调 keep 即丢指令；memoryAccess 非 open 不允许插桩；trustThreshold 与队列的默认值；garbageCollect 须在 unfollow 之后，Py 77 断言）· **485 iOS砸壳与加密镜像**（LC_ENCRYPTION_INFO_64 为 0x2C 且长 24 字节、cryptid 偏移 16；**cryptoff 是文件偏移须经所在段换算成 vmaddr**；整页 dump 与 patch cryptid；MH_DYLIB_IN_CACHE 的语义，Py 56 断言）· **486 反调试与越狱检测对抗**（java 与 native 与 syscall 三层；只 hook 一层必然失败、三层全 hook 才成功；改环境可让派生检查一起熄灭；**分散不等于独立**，TracerPid 与 TRACEME 共享同一根因；MASTG-0354 的 12 个敏感 API，Py 83 断言） | S1=索引同步（02-移动端逆向/README 已完成表 +5 行、待研究勾掉本批 5 项并新增 class-dump 与 jadx 对照与 JNI 动态注册与 Android 加固壳 四项；10-逆向工程/README 待研究 +2 行、参考资料 +3 条）+ S2=类目自动拓展（新增索引 57 固件与嵌入式逆向 与 58 WebAssembly）+ S3=STATE 二节三节滚动与归档（completed 100 加 5 后 rotate 保留 100 行即 ID 387..486；schedule 本轮行） | +5 | 481→486 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 与 curl 带 User-Agent 直抓原文后本地解析，不消耗检索额度：source.android.google.cn 的 dex-format 459KB、dalvik-bytecode 404KB、instruction-formats 362KB 三篇官方中文镜像（source.android.com 本轮不可达，改走 google.cn 镜像成功）；frida.re/docs/javascript-api/ 457KB（Stalker 全节）；frida-gum 的 gum/gumevent.h 1.4KB（jsDelivr）；apple-oss-distributions/xnu 的 EXTERNAL_HEADERS/mach-o/loader.h 71KB（jsDelivr）；mas.owasp.org 的 MASTG-TEST-0046 与 0354 与 0240 与 0241 四页（MASTG 是 mkdocs 静态站，sitemap.xml 为空，用 404 页的导航链接清单反查真实 slug）。质量：Python 自检 5 份**全部实跑 606 项断言全绿**（109 加 281 加 77 加 56 加 83）；Go 8 个文件无本机工具链走人工审查加 bracket_check 全 BALANCED 加 go_sanity 全过加 go_crossref 全 OK；JavaScript agent 1 份无设备未实跑。行数审计：4 处超限当场拆（dex_format.py 537 拆成 dex_leb128 152 加 dex_build 216 加 dex_format 185、dex_format.go 354 拆成 258 加 103、dalvik_isa.py 348 拆成 231 加 119、stalker 与 macho 与 anti 三目录一次过），终态源文件 98..291 全 ≤300、5 篇 README 143..185 全 ≤200。**开发期实跑修 bug 与断言错 8 处**：①DEX 第一版先算 checksum 再填 signature，回读校验失败 —— 官方口径是 checksum 覆盖 signature，顺序必须反过来 ②method_idx_diff 为负触发断言 —— 构造时把方法顺序打乱了，class_data 的 direct_methods 必须按 method_idx 升序 ③abstract 方法被整个剔出 class_data —— 官方要求照样登记只是 code_off 为 0 ④21s 的 B 字段当无符号读，往返在 -2500 处失败 —— s 是有符号短整型而同长度的 21c 与 21h 是无符号 ⑤用 op=0x00（nop 是 10x）去测 23x 与 32x 等纯格式，decode 查表得到 10x —— 给 decode 加了显式 fmt 参数 ⑥反汇编用 %-24s 对齐导致 "goto +3" 匹配不上，改成先拼再 strip ⑦Mach-O 用 5 字节切片赋 6 字节的 "__TEXT"，文件被顶长 1 字节使所有偏移整体错位 —— 改成按 16 字节定长 segname 写入 ⑧反调试阈值断言写错 —— tracer_pid 非 0 会同时点亮 native 与 syscall 两条检查，它们**不是独立信号**，已改为用相互独立的事实做阈值并在 README 里写明这条；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件加 curl --data-binary 单次调用未重试，发送前已确认本轮行是 pending) |
| 2026-09-21 00:00 | 38 | M=11-性能分析/02-应用级剖析 **第二批 5 demo**（大类严格轮转第 11 位 → 11-性能分析，`sub_pos[11]=1` → 索引表第 38 项 应用级剖析；上轮 10-逆向工程。开局 shell date=2026-09-21 00:00:54，`last_run 2026-09-20 22:40` 距 80 分钟通过保险；槽位自检 00:00 槽 +54 秒含会话启动，无 slot_mismatch。ID 487-491） | S2=类目拓展（新增 11-性能分析/06-数据库性能 与 07-网络与传输性能 两个子类目并各写 README，索引表登记为第 59、60 项；同时把已建目录但索引表缺失的 04-全链路性能 与 05-容量规划与性能建模 补登记为第 61、62 项）；S3=索引同步（02-应用级剖析/README 已完成 +5 行、待研究勾掉本批 5 项并补 5 条新待研究、参考资料 +8 条；11-性能分析/README 子领域表 +2 行、已建子类目 +2 行、待拓展换成 08/09、待研究 +1 行、参考资料 +2 条） | +5 | 486→491 | 权威来源：**1 WebSearch / 0 WebFetch / 0 agentic_search** —— 其余全部走 python urllib 与 curl 带 User-Agent 直抓原文后本地解析：google/pprof 的 doc/README.md 与 profile/filter.go 与 driver_focus.go 与 commands.go 与 graph.go 与 proto/profile.proto（6 份源码与文档，过滤与裁剪语义逐行对照）、go.dev/blog/execution-traces-2024 与 flight-recorder、pkg.go.dev/runtime/trace（execution-tracer-2024 是 404，靠 WebSearch 命中 blog/all 索引后改抓 execution-traces-2024）、benfred/py-spy 的 README.md 与 src/cython.rs、async-profiler 的 README.md 与 docs/CpuSamplingEngines.md 与 docs/StackWalkingModes.md（目录清单走 GitHub contents API 反查）、openjdk JEP 328 与 349、go.dev/doc/go1.24 与 golang/go 的 src/testing/benchmark.go、PostgreSQL Using EXPLAIN 与 RFC 9002。质量：Python ×5 全部**实跑**，合计 152 断言（38+23+33+31+27）；Go ×5 无本机工具链（人工审查 + bracket_check.py 全 BALANCED + go_sanity.py 5 文件通过） | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件 + urllib 单次 POST 未重试，发送前已确认本轮行是 pending) |
| 2026-09-21 02:00 | 4 | M=01-游戏开发/05-物理 **第一批 5 demo**（大类严格轮转第 1 位 → 01-游戏开发，`sub_pos[01]=4` → 索引表第 4 项 物理；上轮 11-性能分析。开局 shell date=2026-09-21 02:00:27，`last_run 2026-09-21 00:45` 距 75 分钟通过保险；槽位自检 02:00 槽 +27 秒含会话启动，无 slot_mismatch。ID 492-496） | S3=索引同步（05-物理/README 新增「已完成 demo」5 行表、待研究勾掉四叉树与 BVH 加速及 Verlet 与 PBD 布料并新增车辆物理等 4 项；碰撞检测与刚体与柔体两个子 README 各补已完成表与待研究勾选；01-游戏开发/README 的 05-物理 状态由待研究改进行中、已完成 demo +5 行）+ 归档（completed 100 加 5 后 rotate 保留 100 行即 ID 397..496；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 491→496 | 权威来源：**1 WebSearch / 0 WebFetch / 0 agentic_search** —— 其余全部走 urllib 与 curl 带 User-Agent 直抓原文后本地解析：dyn4j 的 SAT 与 EPA 教程与源码、Box2D v3.1 的 dynamic_tree.c-h 与 contact_solver.c 与 solver.c-h 与 types.c 与 constants.h 与 physics_world.c 与 broad_phase.c 与 aabb.c（jsDelivr，v3 文件名由 GitHub tree API 反查得到 dynamic_tree.c 而非 b2_dynamic_tree.cpp）、pbrt 的 BVH 章节、gameprogrammingpatterns 的空间分区章节、Müller 2006 posBasedDyn.pdf 与 Macklin 2016 XPBD.pdf 与 Müller 2020 PBDBodies.pdf、Ten Minute Physics 的 14-cloth.html 与 09-xpbd.pdf。质量：Python 自检 5 份**全部实跑**，合计 383 断言（70 加 90 加 56 加 93 加 74）。行数审计：2 处超限当场拆（BroadPhase main.py 512 拆成 spatial.py 157 加 tree.py 263 加 main.py 176、布料 main.py 先拆 mesh.py 后仍 340 再把二面角弯曲提出 bending.py 88，终态 88..263 全 ≤300），5 篇 README 经压行后 182 加 189 加 200 加 200 加 200 全 ≤200。**开发期实跑修 bug 与断言错 12 处**：①SAT 里 EPA 的官方数值我先用了记忆中的 0.9369932479745464，重算后应为 0.9370425713316364，三处一并改 ②三重积在真实多边形数据上退化抛 ZeroDivisionError，改为「官方版崩溃 / 绕序安全版可用 / 两者在非退化输入上一致」的对照断言而不是回避 ③AABB「贴着」我断言不相交 —— Box2D 用的是闭区间，贴着算相交、1e-7 的缝才算不相交 ④`mark_moved` 传不到根 —— 真因是 rebuild 新建了叶子对象，外部持有的旧叶子被孤立，补 `insert_existing` 复用原对象 ⑤所有盒子重合时 BVH 一对都报不出来 —— 叶子内有多个图元且无兄弟可交叉，必须枚举叶子内部两两配对 ⑥PBD 距离约束的 Δp 符号写反（a 在原点、b 在 x=2 时应是 a 走 +0.5、b 走 -0.5）⑦「XPBD 20 次迭代比 PBD 20 次迭代更收敛」是我按应然写的伪断言 —— 实跑发现 XPBD 在各迭代数下结果**完全相同**，已改成「PBD 单调下降 / XPBD 与迭代数无关 / 伸长正比于 α」这组真实差分 ⑧Jacobi 链尾断言失败 —— 建的链条中间约束本来就违规，改成只让第一根违规后 GS 一轮传到链尾而 Jacobi 不动 ⑨布料 `stretch_ids` 得到 23 条而非 12 —— 真因是官方 `findTriNeighbors` 的「排序后两两扫」漏配跨偶数与奇数边界的重复边（8 条共享边只配出 1 条），补字典归桶的修正版并断言官方 2 组 / 修正版 16 组 ⑩布料质量断言按角点写死失败，改成按实际邻接三角形数逐顶点核对加总质量守恒 ⑪布料挂在 y≤0 会被地面投影整块拍平，给 `make_grid` 加 `y0` 参数 ⑫二面角求解器从 0.615 过冲到 1.670 —— 逐顶点各做完整牛顿步，改成四个顶点共享同一个缩放因子 s。另记一个口径：平铺布的静止二面角是 **π 不是 0**（两侧三角形 n1·n2 = -1）。commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件加 curl --data-binary 单次调用未重试，发送前已确认本轮行是 pending) |
| 2026-09-21 04:49 | 9 | M=02-Web开发/02-后端 **第二批 5 demo**（大类严格轮转第 2 位 → 02-Web开发，`sub_pos[02]=1` → 索引表第 9 项 后端；上轮 01-游戏开发。开局 shell date=2026-09-21 04:00:35，`last_run 2026-09-21 02:54` 距 66 分钟通过保险；槽位自检 04:00 槽 +35 秒含会话启动，无 slot_mismatch。ID 497-501）：**497 优雅关闭与连接排空**（Go net/http server.go 实读：Shutdown 只关 idle 与超 5 秒的 StateNew，活跃连接留活口只能靠 ctx 到期，Close 才无差别全关；轮询 1ms 起翻倍夹 500ms 带 10% 抖动且首次 closeIdleConns 在等待之前；doKeepAlives 被 shuttingDown 一票否决；k8s preStop 超期只有一次性 +2 秒，Py 34 断言）· **498 中间件与责任链**（Django 洋葱短路语义 + process_exception 逆序致外层统一异常处理不可靠 + Express 靠 arity 4 识别错误处理器 + next('route') 仅 METHOD 栈生效，Py 28 断言）· **499 HTTP缓存与条件请求**（RFC 9111 严格大于判据、s-maxage 仅共享缓存、Expires 减 Date、有显式过期即禁启发式、Age 三口径、Vary 缺席只能对缺席、RFC 9110 §13.2.2 六步优先级中 If-Match 仅对 origin 生效，Py 56 断言）· **500 HTTP2帧与流多路复用**（RFC 9113 九字节帧头与 Length 不含帧头、SETTINGS 六参数初始值、官方 -44KB 流控例按差值调整、SETTINGS 改不了连接窗口；HPACK 附录 C.1 与 C.3.1 官方向量逐字节对拍、条目大小名字加值加 32，Py 52 断言）· **501 JWT与JWS会话认证**（签名输入是 ASCII(header.payload)、base64url 无 padding、附录 A.1 官方 HMAC 向量逐字符相同、exp 严格小于而 nbf 大于等于、aud 缺席即拒、alg=none 靠白名单挡，Py 37 断言） | S3=索引同步（02-后端/README 子领域表 +5 行与已完成表 +5 行；02-Web开发/README 已完成表 +5 行）+ 归档（completed 100 加 5 后 rotate 保留 100 行即 ID 402..501；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 496→501 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 与 curl 带 User-Agent 直抓原文后本地解析，不消耗检索额度：RFC 9111 全文 84KB、RFC 9110 全文 502KB（抽 §13.2.2 前置条件优先级）、RFC 9113 全文 191KB（抽 §4.1 帧格式与 §6.5.2 SETTINGS 与 §6.9.2 初始窗口）、RFC 7541 全文 117KB（抽附录 A 静态表 61 项与附录 C.1/C.3.1 示例）、RFC 7519 全文 63KB 与 RFC 7515 全文 131KB（抽 A.1 HMAC 示例与 A.5 无保护 JWS、§4.1 注册声明）、golang/go 的 src/net/http/server.go 145KB（Shutdown/Close/closeIdleConns/trackListener/doKeepAlives 逐行核对）、kubernetes.io 的 pod-lifecycle 608KB HTML 抽 Pod termination 章节、expressjs.com 的 using-middleware 221KB 抽四参数与 next('route') 两节、docs.djangoproject.com 的 middleware 78KB 抽 onion 与三个钩子。质量：Python 自检 5 份**全部实跑 207 断言全绿**（34 加 28 加 56 加 52 加 37）；Go 6 个文件无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity 全过。**开发期实跑修 bug 与断言错 8 处**：①Shutdown 夹具含永久活跃连接，无 deadline 时轮询循环永不结束直接抛 RuntimeError —— 真实语义就是无限等待，改为「由 ctx 到期收场」并顺带把这条做成 Shutdown 与 Close 的差异断言 ②Age 断言漏算 resident_time（now 减 response_time），实测 110 而非 50 ③Date 超前场景本想验 apparent_age 夹零，算下来两条路径都给 0 无法区分，改成「Date 超前 40s 与 Date 等于 response_time 结果相同」的等价断言 ④Vary 归一化本模型不重排，实测 gzip,br 对 br,gzip 不匹配 —— 规范只许可「按该字段规范已知等价」的重排，改为断言空白归一化并把不重排写成显式结论 ⑤帧头 Stream ID 字节区间写错（写成 6..9 实为 5..9），pack 写 4 字节而 unpack 只读 3 字节导致 0x7FFFFFFF 解成 0xFFFFFF ⑥pack_frame 忘了拼接 payload，帧长恒为 9 ⑦HTTP/2 流控官方 -44KB 只在「先发 60KB 再按差值调整」时才出现，直接令差值会得到 -49151，已按规范口径改成 4095 加 (16384 减 65535) ⑧HPACK 官方 hex 我误数成 15 字节实为 20 字节。另：main.py 三处超 300 行当场拆（HTTP 缓存拆出 precond.py、Go 侧拆成 cache.go/precond.go 与 h2.go/hpack.go 两组同包文件），拆完重跑断言数不变；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件加 curl --data-binary 单次调用未重试，发送前已确认本轮行是 pending) |
| 2026-09-21 06:50 | 13 | M=03-系统编程/02-进程与线程 加 02-协程 **第二批 5 demo**（大类严格轮转第 3 位 → 03-系统编程，`sub_pos[03]=1` → 索引表第 13 项 进程线程协程；上轮 02-Web开发。开局 shell date=2026-09-21 06:00:53，`last_run 2026-09-21 04:52` 距 68 分钟通过保险；槽位自检 06:00 槽 +53 秒含会话启动，无 slot_mismatch。ID 502-506）：**502 futex 原语**（futex(2) 与 futex(7) 与 FUTEX_WAIT(2const) 与 uapi/linux/futex.h 与 kernel/futex/waitwake.c；「比较-并-阻塞」对照组证伪丢失唤醒、FUTEX_OP 位编码逐位对拍内核解码、WAKE_OP 无条件唤醒 uaddr1、PI 取值策略与继承传递，Py 77 断言）· **503 自旋锁与信号量**（POSIX spin_lock 加 sem_overview(7) 加 sem_wait(3)；TAS 插队 6 次 vs ticket 零插队、单核不可抢占死锁、点名的优先级反转 3 tick 拖成 103 tick、信号量 EINTR 时值不变与命名信号量 251 字符内核持久，Py 52 断言）· **504 内存序与原子操作**（cppreference memory_order 加 C11 n1570 加 Go 内存模型文档；穷举交错与传播做 litmus，MP 与 SB 与 release sequence 与 CoRR 四类共 18 断言）· **505 Go GMP 调度器**（runtime2.go 加 proc.go 加 design/24543；runq 256 加 runnext、61 tick 一次全局且批量 128、窃取靠头一半、sysmon 20us 到 10ms、forcePreemptNS 10ms 与异步抢占绕过 unsafe-point，Py 55 断言）· **506 C++20 协程状态机**（cppreference Coroutines；无栈状态与生命周期、awaiter 四種返回、对称转移栈深 1 vs 递归 64、按引用参数悬垂，Py 34 断言） | S3=索引同步（02-进程与线程/README 已完成 +3 行与待研究勾掉自旋锁与信号量并把 Go GMP 移到 02-协程；02-协程/README 已完成 +2 行并把两条待研究换成 Rust async 与 Lua 协程；03-系统编程/README 已完成 demo +1 批 5 行）+ 归档（completed 100 加 5 后 rotate 保留 100 行即 ID 407..506；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 501→506 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 curl 带 User-Agent 直抓原文后本地解析，不消耗检索额度：man7 的 futex(2) 34KB、futex(7) 12KB、FUTEX_WAIT(2const) 5KB、sem_overview(7) 13KB、sem_wait(3)、cppreference 的 coroutines 149KB 与 memory_order 61KB、Open Group 的 pthread_spin_lock、C11 草案 n1570.pdf 1.69MB、torvalds/linux 的 include/uapi/linux/futex.h 与 kernel/futex/waitwake.c 21KB、golang/go 的 src/runtime/proc.go 243KB 与 runtime2.go 57KB、golang/proposal 的 design/24543-non-cooperative-preemption.md 24KB、golang/go 的 doc/go_mem.html 27KB（go.dev/ref/mem 走 https 握手失败，改 jsDelivr 取回）。质量：Python 自检 5 份**全部实跑 236 断言全绿**（77 加 52 加 18 加 55 加 34）；Go 4 个文件无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity 全过，C 与 C++ 共 4 文件同走人工审查 + bracket_check + c_sanity --tu。**开发期实跑修 bug 与断言错 9 处**：①futex 的 WAKE_OP 用例给 uaddr2 的等待者传了初值 0 而该 word 实为 3，等待根本没入队导致「比较成立才唤醒」恒假 ②futex_model.py 362 行超 300，把 PI 段拆成 futex_pi.py 后重跑断言数不变（77），拆出的模块忘记 import EAGAIN 与 MASK32 ③内存序模型最初把 op 三元组补 None 补在尾部，导致 (kind, var, order) 的 order 落进 val 而 order 变 None，acquire 分支从不触发，MP 的 (1,0) 一直出现 ④release sequence 首版断言写成「读到 2 也必见 data=1」，但 RMW 若先读到初值则新值也是 2 且不在释放序列里 —— 改为用 +2 区分来源，断言「读到 3 必见 data=1」且「读到 2 时 (2,0) 出现」，反而成了更有力的对照 ⑤fence 操作的 var 传 None 进了 vars 集合导致 sorted 崩，已在 vars 收集处过滤 ⑥GMP 的全局队列断言把 300-128 误写成 300-128+127 ⑦协程的「普通函数不能 co_await」首版用「有没有 co_ 关键字」判断，普通函数当然也含关键字，改成「返回类型能否给出 promise_type」这一真实判据 ⑧GMP 的 naive 栈深对照最初想让调用方 resume，实际反模式是 await_suspend 里直接 resume 下一个，改用 suspend_cb 才测出 64 ⑨futex_pi.py 拆分时 docstring 里 futex(2) 误写成 futext(2)，已 sed 回改。commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件加 urllib 单次 POST 未重试，发送前已确认本轮行是 pending) |
| 2026-09-21 09:03 | 17 | M=04-移动开发/02-Android **第二批 5 demo**（大类严格轮转第 4 位 → 04-移动开发，`sub_pos[04]=1` → 索引表第 17 项 Android；上轮 03-系统编程。开局 shell date=2026-09-21 08:06:55，`last_run 2026-09-21 06:50` 距 76 分钟通过保险；槽位自检 08:00 槽 +6 分 55 秒含会话启动与读文件，无 slot_mismatch。ID 507-511）：**507 类型安全导航**（androidx navigation 的 serialization 包 RouteBuilder 加 RouteEncoder 加 RouteDecoder 加 NavGraph 加 NavGraphNavigator 实读；route 基准串即 serializer.descriptor.serialName，PATH 与 QUERY 只由 computeParamType 一个判据决定即集合类型或可选元素走 query，多值参数重复同一 key 而非逗号拼接，argMap 值恒为字符串列表且标量也包单元素，缺 NavType 抛 Cannot find NavType；RouteDecoder 对缺失元素是跳过不是填 null，只有「存在但为 null 且非可空」才抛 Unexpected null value —— 两种失败语义必须分开；导航到 NavGraph 压栈的是 startDestination 而非图自身，startDestination 必须是直接子节点，startRoute 带字面值时先精确后模式匹配，参数合并是解析值先写、外部传入后写故外部优先；Py 31 断言）· **508 JNI 互调机制**（Oracle JNI 规范 Chapter 2 与 OpenJDK jni.h 实读；符号名 mangling 的转义表 下划线1 等于下划线、下划线2 等于分号、下划线3 等于左方括号、下划线0XXXX 用四位小写 hex，长名的双下划线后**只含参数签名**既不含括号也不含返回类型故 (D)I 与 (D)Z 符号同名，链接先短名后长名且只与同库内 native 方法比；JNIEnv 是线程局部不可跨线程传，传给 native 的对象全是局部引用且随方法返回整表失效，全局引用是强引用阻止 GC 而弱全局不阻止，PushLocalFrame 与 PopLocalFrame 释放整帧只保留 result，JNI_OnLoad 返回不受支持版本即加载失败；Py 50 断言）· **509 Retrofit 动态代理与适配器**（square/retrofit 主干 Retrofit 加 ServiceMethod 加 HttpServiceMethod 加 RequestFactory 加 Platform 实读；create 递归校验父接口泛型参数、InvocationHandler 三分支、RequestFactory 的全部硬错误消息、带 body 的方法只有 POST 加 PUT 加 PATCH、suspend 先解包 Response 再包装成 Call 并补 SkipCallbackExecutor、nextCallAdapter 的 skipPast 起点在找不到时退化为从头找、Platform 按 java.vm.name 分支；Py 59 断言）· **510 Room 连接池与 ORM 映射**（androidx room3-runtime 的 ConnectionPoolConfiguration 加 RoomConnectionManager 加 androidMain 选池分支，配 AOSP SQLiteConnectionPool；WAL 默认 4 读 1 写且读写数必须大于 0，内存库强制单连接，打开流程双保险拦递归初始化，失败后退避 500 毫秒重试并仅在三条件齐全时删库重开，configureDatabase 的 PRAGMA 顺序与迁移事务，非 WAL 时 journal_mode 是 TRUNCATE 而非 DELETE 且 synchronous 变 FULL，**非首个连接不设 journal_mode 也不做迁移**，框架层池主连接唯一与 30 秒忙阈值只打日志；Py 59 断言）· **511 Compose 快照与 LazyColumn 复用**（androidx compose runtime Snapshot.kt 与 foundation LazyListMeasure 加 LazyList 实读；状态是多版本 StateRecord 链，碰撞写的唯一定义是「自 take 之后有别的已 apply 快照写过该状态」，空修改永不失败，innerApplyLocked 三分支，previous 的 id 恰为 PreexistingSnapshotId 等于 1 时豁免冲突判定，官方注释明确不保证可串行化；LazyColumn 空集重置、越界钳位、间隔只加在非末项、beyondBounds 与 pinned，滚动锚点按 key 找回且包在 withoutReadObservation 里避免自激重组；Py 46 断言） | S1=索引同步（02-Android/README 子领域表 +2 行、已完成表 +5 行、待研究勾 5 项并新增 5 项；04-移动开发/README 已完成表 +5 行与子目录说明更新）+S2=类目拓展（新增 2 个子域 `02-Android/NDK与JNI/` 与 `02-Android/数据持久化/`，各补 README 含子领域表、已完成表、待研究、参考资料） | +5 | 506→511 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 带 User-Agent 直抓原文后本地切段解析，不消耗检索额度：androidx 主干 navigation 的 NavGraph 加 NavGraphBuilder 加 NavGraphNavigator 加 NavType 加 NavDestination 加 serialization 的 RouteBuilder 加 RouteEncoder 加 RouteDecoder 加 NavTypeConverter、navigation-compose 的 NavHost；compose runtime 的 Snapshot.kt（110KB）与 SnapshotState.kt、compose foundation 的 LazyList 加 LazyListMeasure 加 LazyListState；room3 的 ConnectionPoolConfiguration 加 RoomConnectionManager 加 androidMain 选池分支 加 RoomDatabase 加 RoomOpenDelegate 加 Transactor；AOSP 的 SQLiteConnectionPool.java 与 SQLiteSession.java；square/retrofit 的 Retrofit 加 ServiceMethod 加 HttpServiceMethod 加 RequestFactory 加 Platform；Oracle JNI 规范 design 加 types 加 functions 加 invocation 四篇（本地 HTML 剥离后定位原文段落）、OpenJDK 主干 jni.h。**androidx 仓库中 room 模块已迁至 room3 顶层目录（room/ 已 404），经 tree API 反查确认**。质量：Python 自检 5 份**全部实跑 245 断言全绿**（31 加 50 加 59 加 59 加 46）；Kotlin 5 个文件与 C 1 个文件无本机工具链（which gcc cc clang go rustc 全空）走人工审查 + bracket_check 全 BALANCED。**开发期实跑修 bug 与断言错 7 处**：①findNode 首版只做精确匹配，导致「startRoute 带字面参数值」场景恒找不到 —— 官方紧接着的 `if (startRoute != startDestination.route)` 分支就是模式匹配的证据，补齐「精确优先、未命中再按占位符匹配」②matchingArgs 直接用了 URL 片段字符串而未过 NavType 解析，使 `Detail/77` 的 id 是 "77" 而非 77 —— 官方 matchRoute 的 matchingArgs 由 NavType.parseAndPut 写入，应为解析后的对象③JNI 长名首版把括号与返回类型也 mangling 进去，与规范 canonical 例子 `Java_pkg_Cls_f__ILjava_lang_String_2` 不符④弱全局测试首版写成「先建全局再建弱全局」，而全局是强引用会钉住对象，GC 永远不生效 —— 改为对照两条（只有弱引用会被回收、有全局引用则不会）⑤EnsureLocalCapacity 断言误用了默认容量 16 而该夹具容量是 2⑥SQLiteConnectionPool 的 _wake_waiters 漏了「非主失败再回退主连接」的顺序，与 waitForConnection 的获取顺序不一致，导致主连接被释放后高优先级 waiter 永不被唤醒⑦close() 首版连已 acquired 的连接也一并关闭，而官方注释明确允许「池关闭后再 release」，改为只关空闲连接、持有的等 release 时再关。行数审计：本轮新增 28 个文件，源文件 0 处超 300 行（最大 main.py 297），3 篇 README 超 200 行当场分两轮压缩到恰好 200（Compose 快照 212→202→200、Retrofit 205→203→200、Room 205→203→200）；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件加 curl --data-binary 单次调用未重试，发送前已确认 schedule.md 无本轮既有 ok 记录) |
| 2026-09-21 11:14 | 48 | M=05-AI与机器学习/07-特征工程与降维 **第二批 5 demo**（大类严格轮转第 5 位 → 05-AI与机器学习，`sub_pos[05]=6` → 索引表第 48 项 特征工程与降维；上轮 04-移动开发。开局 shell date=2026-09-21 10:09:18，`last_run 2026-09-21 09:03` 距 66 分钟通过保险；槽位自检 10:00 槽 +9 分 18 秒含会话启动与读文件，记 `slot_late`，无 `slot_mismatch`。ID 512-516）：**512 特征选择的稳定性**（Nogueira/Sechidis/Brown JMLR 18(174) 与 Meinshausen-Bühlmann 稳定性选择原文实读：判据不是"跑两次看重合"而是对重抽样求期望；Φ̂ 取每列入选频率方差除以零模型期望方差再取补且零模型下期望恰为 0；Kuncheva IC 做了机会校正但只在"每次固定选 k 个"时有定义，Jaccard 与 Dice 无校正故 H₀ 期望随 k 与 d 漂移，三者不可互替；稳定性选择按入选频率筛变量并给 PFER 上界；两篇原文重抽样口径不同即 bootstrap 有放回 n 个 vs subsample 无放回半个 n。实测 k 从 3 增到 30 时 Jaccard 0.4644 → 0.3510 → 0.4176 **非单调**回弹而 Φ̂ 0.5861 → 0.4126 → 0.1727 单调下降；常量 k 下逐对 Kuncheva 平均与 Φ̂ 之差 = -2.082e-17；纯噪声 d=60 k=8 M=80 重复 12 次，阈值 0.75 时 V=0.42 上界 2.13、阈值 0.90 时 V=0.08 上界 1.33，上界不紧；4 列互为副本时 F 值全等 31.028359257590 且选出集合恒为 [2, 3]，Py 52 断言）· **513 幂变换与分位数变换**（Box-Cox 1964 与 Yeo-Johnson 2000 原式加 scipy stats/morestats.py 源码；Box-Cox 只接受严格正数且 λ=0 退化为 log，Yeo-Johnson 四分支让负数走 2−λ 镜像；λ 的估计目标即剖面对数似然，scipy 的 `*_llf` 就是它；**两种 λ 口径必须分清** —— `boxcox()` 走 MLE 而 `boxcox_normmax()` 默认走 pearsonr；源码实读确认 `BOUNDS_THRESHOLD = 1e-07`。实测分位数-uniform 的 KS 距离 0.8645(nq=200) 而分位数-normal 只有 0.8334，参照精确正态得分(Blom) 0.8612 —— normal 偏低是因为地标取 i/(n_quantiles−1) 且两端夹到 ±5.1993；nq 越小台阶越粗(0.8334 → 0.7842)；10/10/10 三台阶被压成 0.000000 / 0.500000 / 1.000000 且台阶内极差全 0，Py 58 断言）· **514 分箱与样条**（sklearn 1.9.1 的 preprocessing/_discretization.py 与 _data.py 与 polynomial.py 源码；KBinsDiscretizer 三种 strategy 与三种编码且 onehot 返回稀疏；SplineTransformer 的基个数 n_splines = n_knots + degree − 1，单位基走 Cox-de Boor 递推；periodic 专治 12/31 到 1/1 跳变。实测 1 / −0.5 / 0.5 / 1.5 同余于 0.5 故 periodic 下三行**完全相同**；periodic 的 n_features_out_ = n_knots − 1；`knots=quantile` 用 `np.nanpercentile` **默认 linear** 与 KBinsDiscretizer 默认的 averaged_inverted_cdf 不是同一口径且 base 结允许重复(实测 4 个 0.0000)，Py 257 断言）· **515 稀疏矩阵降维路线**（sklearn 1.9.1 的 tsvd.py 与 utils/extmath.py 与 decomposition/ipca.py 源码；TruncatedSVD **不中心化**故 comp0 就是均值方向(实测余弦 0.9999999785)，代价是不中心化的秩-(k+1) 约当于中心化的秩-k；`flip_sign=False` 后手动 `svd_flip(u_based_decision=False)`，`X_transformed = X @ components_.T` 而**不是** UΣ；IncrementalPCA 的 `batch_size_ = 5 * n_features`（**1.9.1 已从旧的 10 改成 5**），partial_fit 叠 `[S·V ; 中心化批次 ; 均值修正]`，均值修正 = sqrt((seen/total)·n_batch) × (last_mean − batch_mean)，均值方差走 Chan-Golub-LeVeque 两遍校正，Py 163 断言）· **516 时间序列因果特征**（pandas 3.0.6 的 rolling/window 官方文档加 sklearn 1.9.1 `model_selection/_split.py` 源码与 TimeSeriesSplit API；滚动窗口默认右闭故 `rolling_mean(x,5)[i]` **含 x[i] 本身**，`center=True` 只把末端右移 (window−1)//2 故偶数窗偏左，`closed` 四取值各挪不超过 1 格，`std` 默认 ddof=1 且 window=1 时是缺失而非 0；`gap` 是"从每个训练集末端剔除、留在测试集之前的样本数"，**完全不移动测试集**故只治标签侧越界；`n_splits` 不小于 2 的校验在父类 `_BaseKFold.__init__` 属构造期。核心方法是把"因果/错位/泄漏"三档用**支持集**(扰动 x[j] 看第 i 行变不变)机械切开 —— 判据是「第 t 行不得依赖 x[j] 且 j 大于 t」，故含当期但在 y_t = x_{t+1} 设定下已知的"错位"档未来下标数是 **0 即不是泄漏**，只有 center 与 shift(-1) 是真泄漏。实测泄漏版在时序 CV 下照样拿 0.8285；随机 KFold 均值 0.5276 对比时序 CV 0.3633；落差最小的恰是泄漏最重的(0.0638 对比因果 0.1755)故「落差小即可信」是错的判据；`gap` 从 0 到 3 把"标签伸进测试期的训练行数"从 3 降到 0 而 CV R² 几乎不动；E6 探针实测错位 0/2838、center 264/3102(= 3 种子 × 88，恰为每点贡献 t=j−1 与 t=j−2 两个未来位置)，Py 246 断言） | S1=索引同步（`07-特征工程与降维/README` 已完成表 +5 行与待研究勾掉 5 项并新增 3 项、参考资料新增「第二批(6-10)新读的来源」9 条；`05-AI与机器学习/README` 的 07 行扩写为 10 demo 并把待研究勾 5 项新增 2 项）+ 归档（completed 100 加 5 后 rotate 保留 100 行即 ID 417..516；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 511→516 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 与 curl 带 User-Agent 直抓原文后本地解析，不消耗检索额度：pandas 的 `pandas.DataFrame.rolling` API 71KB 与 Windowing operations 用户指南 116KB、scikit-learn 的 TimeSeriesSplit API 138KB 与 Common pitfalls 110KB（抽 §12.2 Data leakage 原文定义）、scikit-learn 1.9.1 源码 `model_selection/_split.py` 112KB（TimeSeriesSplit 的 `_split` 逐行与两条硬校验原文、`_BaseKFold.__init__` 的 n_splits 校验、docstring 里 n_splits=3/test_size=2/gap=2 的示例输出被 516 自检直接当数值锚点）、`preprocessing/_discretization.py` 20KB、`preprocessing/_data.py` 132KB、`utils/extmath.py` 51KB、`decomposition/ipca.py` 16KB、`decomposition/_truncated_svd.py` 11KB、`polynomial.py` 50KB、`mutual_info.py` 20KB、`feature_selection/_from_model.py` 19KB、`feature_selection/_univariate.py` 40KB、scipy `stats/morestats.py` 176KB（boxcox 与 yeojohnson 的剖面似然与 BOUNDS_THRESHOLD）、Nogueira JMLR 17-514 全文 822KB 文本加 PDF 1.37MB、Meinshausen-Bühlmann stability selection PDF 2.28MB。质量：Python 自检 **5 份全部实跑 776 断言全绿**（52 加 58 加 257 加 163 加 246）；对拍脚本与 pandas 3.0.6 加 scikit-learn 1.9.1 逐项比输出 **OK 1439 / BAD 0**；Go 7 个文件无本机工具链走人工审查 + bracket_check 全 BALANCED + go_sanity OK + go_crossref 无重名无未定义调用，另用 Python 逐行转写 go/*.go 与已验证的 python/ 模块比输出 **OK 3304 / FAIL 0**。**开发期实跑修 bug 与断言错 12 处**：①`svd_jacobi` 的 Vt 组装写成 `V[i][j] for i in range(n)` 的转置式，使每行完全相同(秩 1)，而奇异值仍对到 1e-15 故极难发现 —— 靠运行时探针比对 V 与 Vt 的秩才定位②`_incremental_mean_and_var` 初次 `awk` 取段漏了函数体，重读 `sed -n` 才拿到 Chan-Golub-LeVeque 组合项③自检里 9 处断言错(把行置换后的 PL 当三角阵、范围查找器的余弦缺 oversampling、Eckart-Young 用 Frobenius 而非 dmax、`flip_sign` 两分支判据写重)④`selfcheck_rolling` 的 `count(closed='both')` 首行期望值与「左端裁剪后仍是 1 点」不符、`pct_change` 分母位置写错(分母是**前一期**故 0 值出现在 i=3 而非 i=2)、`left/neither/both` 的集合关系式漏了 start−1 这一点⑤`selfcheck_model` 的 3×3 方程组手推解写错(实为 [3,2,1])、端到端 R² 阈值设成 0.9 而实测 0.8896⑥`selfcheck_splits` 把 `ts` 默认起点算成 10−2×3=4 之外的 3、`raises()` 忘了 `split()` 是生成器不迭代就不执行(三条硬校验用例全假绿)⑦E6 探针首版把"含当期"误判为读未来，prose 与测量直接矛盾，改为未来依赖/当期/下一期三轴分离并把 54 处硬编码分母改成从测量取⑧`features.py` 与 `main.py` 各写一份变体定义(同一概念两处实现)，合并为 features 单一来源⑨`main.py` 导入不存在的 `linalg_rand` 与 `rolling_mean/shift` 漏 import⑩Go 侧 `go_sanity.py .` 传目录报 PermissionError，须显式传文件列表；`qrHouse` 在拆文件时丢失已补回；`sqrtF` 牛顿迭代改为 `math.Sqrt` 并修复导入⑪转写脚本里 `R.Rng` 应为 `rng.Rng`、`go_causal_mean` 少传 shift_by 参数⑫README 首版 230 行超限，把 18 条注意事项移入 `NOTES.md` 并按 §三 注明口读口径。行数审计：新增 19 文件，源文件 0 处超 300 行(最大 go/main.go 234、python/main.py 230)，README 198 行、NOTES 60 行；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 经 urllib 单次 POST 未重试，发送前已确认 schedule.md 无本轮既有 ok 记录，发送成功后才写入本行) |
| 2026-09-21 12:52 | 42 | M=06-DevOps/06-SRE与可靠性工程 **首批 5 demo**（大类严格轮转第 6 位 → 06-DevOps，`sub_pos[06]=5` → 索引表第 42 项 SRE与可靠性工程；该子类此前**零 demo**，本轮首批铺开；上轮 05-AI与机器学习。开局 shell date=2026-09-21 12:01:45，`last_run 2026-09-21 11:14` 距 47 分钟通过保险；槽位自检 12:00 槽 +1 分 45 秒含会话启动，无 slot_mismatch。ID 517-521）：**517 SLI窗口与聚合口径**（Prometheus `promql/functions.go` 的 extrapolatedRate 全文与 OpenSLO v1 规范实读；区间向量左开右闭、`rate` 与 `increase` 同一条 factor 只差一次除法、外推阈值 = 平均间隔乘 1.1 且超了退化为半间隔、counter **零点钳制**的闭式是「increase = 裸差 + 首点值」实测 45 加 10 = 55 而非 67.5、`irate` 只看最后两点；OpenSLO 侧 rolling 长度恒定而 calendar 随日历变，同一段 2 小时故障 rolling 30d 要到 4/20 才恢复、calendar 1M 在 4/1 就清零，`rawType` 写反会让 0.997 被读成 0.003，Py 64 断言）· **518 错误预算与燃烧率**（sloth `internal/alert/window.go` 的 getBurnRateFactor 与 OpenSLO budgetingMethod 实读；99.9% 加 30d = 43.2 分钟、燃烧率与 SLO 周期无关但阈值与 SLO 有关、30d 四档 14.4/6/3/1 而 **28d 整体按 28/30 缩放**、TTE 两条独立推导必须合上；三种 budgetingMethod 的差别是加权方式，同一份数据给出 0.989237 / 0.083333 / 0.541667，收敛的唯一条件是切片比值只取 0 或 1 而非流量均匀，Py 79 断言）· **519 多窗口多燃烧率告警**（sloth `alert_rules_v1` 的 mwmbAlertTpl 与 google-30d/28d YAML 实读；表达式是「(短窗 and 长窗) or (短窗 and 长窗)」、阈值 = 燃烧率乘错误预算率、短窗的作用是**叫停**——10 分钟故障后 MWMB 第 13 分钟停而只看 1h 长窗的静态阈值拖到第 68 分钟差 55 分钟，负控是把短窗拉长到与长窗同宽即退化回 68；慢燃 0.12% 在静态 1% 阈值下完全漏报，分界点可手算 10 分钟故障要 r 大于 0.0864 才 page 且比较是严格大于，Py 67 断言）· **520 退避与抖动**（AWS 官方模拟器 `aws-arch-backoff-simulator` 源码与同名博客实读；四种策略逐行转写 base=5ms cap=2000ms，**纯指数退避一点工都不省**——工作量与不退避同为 5050（闭式 N(N+1)/2）而耗时长 94 倍，加 jitter 后降到 454/387/367；Decorrelated Jitter **有状态**且 n 被忽略、下界恒为 base 故非单调；模型必须做时间槽离散化否则四种策略会给出同一个数字 199，Py 61 断言）· **521 过载保护与自适应并发**（Envoy 自适应并发与过载管理器官方文档实读；梯度控制器三公式、headroom 不可配置且等于 sqrt(limit)、**buffer 是百分数不是小数**、稳态闭式 `L = 1/(1-g)²` 实测与 400 步迭代吻合到 6.9e-06、headroom 恒为正故**过冲后阻尼收敛**峰值 140.47 高出稳态 0.33、minRTT 要连续 5 个窗口踩到下限才重算且 jitter=0 会让 20 个 host 全部同时进入测量窗口、threshold 是严格大于的方波而 scaled 是三段斜坡、cgroup 没配 limit 时压力恒为 0，Py 68 断言） | S3=索引同步（06-SRE与可靠性工程/README 重写：新增已完成 demo 表 5 行、待研究勾掉 4 项并新增 3 项、参考资料新增「第二批新读的来源」11 条并注明 sre.google 不可达的转引口径；06-DevOps/README 的子领域说明与已完成表各加 1 行）+ 归档（completed 100 加 5 后 rotate 保留 100 行即 ID 422..521；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 516→521 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 curl 与 urllib 带 User-Agent 直抓原文后本地解析，不消耗检索额度：OpenSLO v1 规范 README 30KB（timeWindow rolling 与 calendar、ratioMetric 三形态、budgetingMethod 三口径、AlertCondition 的 burnrate）；prometheus.io 的 querying/basics 与 querying/functions（左开右闭原文、rate/increase/irate 定义）；Prometheus 源码 `promql/functions.go` 107KB（extrapolatedRate 逐行转写对象）；slok/sloth 的 internal/alert/window.go（getBurnRateFactor 与 Google 默认预算百分比注释）、internal/alert/windows/google-30d.yaml 与 google-28d.yaml（四档长短窗数值）、internal/plugin/slo/core/alert_rules_v1/plugin.go（mwmbAlertTpl 全文）；aws.amazon.com 的 Exponential Backoff And Jitter 1.26MB 与 aws-samples/aws-arch-backoff-simulator 的 src/backoff_simulator.py（四种策略源码）；envoyproxy.io 的 adaptive_concurrency_filter 40KB 与 overload_manager 74KB。**sre.google 本轮三种通道均不可达**（curl schannel 连接重置、urllib 握手超时、WebFetch fetch failed），SRE workbook 数值经 sloth 源码与其 windows YAML 转引并在 README 标注。质量：Python 自检 **5 份全部实跑 339 断言全绿**（64 加 79 加 67 加 61 加 68）；Go 12 个文件无本机工具链（which go gcc clang rustc 全空）走人工审查 + bracket_check 全 BALANCED + go_sanity 全过 + go_crossref 无重名无未定义调用。**开发期实跑修 bug 与断言错 14 处**：①517 的 E3 首版用例外推 factor 恰好整除（increase=3.0 整数）根本没验到「非整数」，重设采样点得 2.5 并补等距负控 ②517 的 E5 首版回绕值与正常值巧合相等（都 60），改成 45s 回绕并在同一序列上对照 counter 与 gauge 两种口径 ③517 的 E4b 手算漏了**计数器零点钳制**（以为按 avg/2 外推得 67.5，实测 55），闭式应是「裸差 + 首点值」 ④517 的 E7 断言把 `is_calendar` 写反 ⑤517 的 E8 窗口在 4/1 00:00 刚开导致除零，改取 06:00 ⑥518 的 E8 首版负控「流量均匀三者收敛」是**错的**（实测 timeslices 0.6 对 occurrences 0.986），真正的收敛条件是切片比值只取 0 或 1 ⑦518 自检里中文引号嵌进 Python 字符串导致 SyntaxError ⑧519 的 E5 首版时间线没有健康前缀、窗口被截断使均值失真，补 120 分钟前缀并改扫错误率 ⑨519 的 E2 断言用 `" and "` 匹配失败（模板是 ` and `）；E6 用字面量 0.0144 比「恰好等于阈值」根本没验到——阈值实为 0.014400000000000013 ⑩519 的 X 右闭断言首版取样点设计错误（左开右闭两点被同时排除），改用三点 1/5/9 并加「左闭的 7.0 被排除」反证 ⑪520 首版模型**没有分辨力**——连续随机永不碰撞，四种策略都给出 199，加时间槽离散化后才有 454/387/367 的区分，并把「不离散时恒为 199」做成负控断言 ⑫520 的 E8 首版跨种子零方差（因为结果恒定），改扫 8 个种子并把排序稳定性做成断言 ⑬521 的 **buffer 量纲写错**（按 `minRTT × buffer_pct` 把 10 当 0.10 用，B 变 500ms、梯度恒大于 1、并发放大到 1e13），改为除 100 并用「sampleRTT = minRTT × (1 + buffer/100) 时梯度恰好为 1」锁死 ⑭521 的 E10 断言写成「单调爬升」是错的——headroom 恒为正导致**过冲后阻尼收敛**（137.29 → 140.47 峰值 → 140.11 → 140.149），改为断言过冲幅度与峰值位置。行数审计：新增 24 个文件，源文件 0 处超 300 行（最大 python/mwmb.py 141、go/adaptive.go 159），521 的 README 218 行超限，把完整注意事项移入 NOTES.md 并压缩到恰好 200 行；commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件经 urllib 单次 POST 未重试，发送前已确认 schedule.md 无本轮既有 ok 记录) |
| 2026-09-21 14:47 | 27 | M=07-数据存储/01-关系型 **第四批 5 demo**（大类严格轮转第 6 位 → 07-数据存储，`sub_pos[07]=0` → 索引表第 27 项 01-关系型；该子类原有 13 个 demo）：522 分片与路由键空间（Vitess）/ 523 EXPLAIN ANALYZE 读数口径 / 524 并行查询 worker 数公式 / 525 autovacuum 阈值与 XID 回绕四线 / 526 声明式分区与分区裁剪。统一 Python（可执行自检）+ Go（无工具链，人工审查 + bracket_check + go_sanity + go_crossref） | S3=索引同步（01-关系型 README 已完成表 +5 行、待研究勾掉分库分表与慢查询分析并新增 4 条；07-数据存储 README 补齐 01-关系型此前漏登记的 5 个 demo + 本轮 5 个） | +5 | 521→526 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search**，全部 urllib 直抓——PostgreSQL 18 官方文档 6 篇（using-explain / routine-vacuuming / ddl-partitioning / parallel-query 三节 / runtime-config-autovacuum / runtime-config-query / runtime-config-resource）+ 官方源码 5 份（`allpaths.c` 的 `compute_parallel_worker`、`varsup.c` 的 `SetTransactionIdLimit`、`autovacuum.c` 的 `relation_needs_vacanalyze`、`partprune.c` 的 `PartClauseMatchStatus`、`plancat.c`）+ Vitess《Sharding》文档与 `vindexes` 四个源码 + Citus data_modeling + pingcap tidb-storage。Python 五份自检**实跑 244 项断言全绿**（73/33/38/48/52）；Go 14 文件三项静态检查全过；行数 0 处超限（源 ≤300 最大 go/bounds.go 241，README ≤200 最大 144）。本轮踩坑：① **手算期望值错了 4 组**——522 的 `["-40","40-"]` 其实是合法分区（终点为空）、`reverse_bits` 下 id=3 落在 `c0-` 故 [1,3,5] 并非同片；524 的 100000 页 log3 是 5 不是 4、索引侧是 7、取小后 5；**被测代码自始至终正确**，改的是断言。② **Go 的 uint64 装不下 2^64**：`splitShard` 里 `uint64(1)<<63 * 2` 会静默溢出成 0、`routeRange` 用 `^uint64(0)` 当上界会在 `MaxUint64` 处漏判，两处改用 `math/big` 与 Python 端对齐。③ 526 的 `sorted()` 对 `p10_20` 与 `p1_10` 的字典序与阅读直觉相反，OR 断言改用集合比较。④ 526 的 `BETWEEN 1 AND 10` 命中**两个**分区（闭区间含 10 而 10 属第二片），闭区间转半开只对离散键严格成立，已在 README 标注口径。⑤ 523 的 `main.py` 把中文引号写成 ASCII 直引号触发 `SyntaxError`，改成中文引号后通过。⑥ **归档表回归**：`schedule.md` 上一轮行被写成了跨 3 行的断行（第 28-30 行不以竖线开头），本轮合并复原后再跑 `archive_check` 两张表均 OK | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 文件经 urllib 单次 POST 未重试，发送前已确认 schedule.md 无本轮既有 ok 记录) |
| 2026-09-21 16:52 | 33 | M=08-安全/02-网络安全 **第三批 5 demo**(大类严格轮转第 8 位 → 08-安全,`sub_pos[08]=2` → 索引表第 33 项 02-网络安全;上轮 07-数据存储。开局 shell date=2026-09-21 16:00:46,16:00 槽 +46 秒,无 slot_mismatch;`last_run 2026-09-21 14:48` 距 72 分钟通过保险。ID 527-531):**527 TCP 盲注入与 RFC 5961**(RFC 5961 全文 + Linux `net/ipv4/tcp_input.c` 232KB 与 `sysctl_net_ipv4.c` 46KB 实读;RST 三分支与 SYN 的 irrespective 差异、`tcp_reset_check` 三态与 SACK 最右边缘、ACK 判据从 2^31 收到 65537、`tcp_challenge_ack_allowed` 每秒复位成随机区间故单秒上限是 limit 加 half 减 1 而非 limit、`__tcp_oow_rate_limited` 的严格小于判据;Py 68 断言)**528 DNS 缓存投毒与 RFC 5452**(RFC 5452 全文实读;P_s 与 P_cs 公式、化简常数 1638400、TTL=3600 与 TTL=60 共五组概率全部复现、生日攻击的线性放大;三处文档口径不一致记录不改正:§8.1 用 N=1、§7 的 4 Mbit/s 是线性近似、§8 的 285 Gb/s 来自 4 Mbit/s 而非 416 Mbit/s;Py 44 断言)**529 证书透明度 Merkle 审计**(RFC 9162 全文与 RFC 6962 实读;MTH 域分隔、§2.1.5 的 7 叶树四个包含性证明与三个一致性证明逐字节对上、fn 与 sn 双游标、first 为 2 的幂先补 first_hash、SUBPROOF 的 m 大于 k 分支置 false、栈式树头与递归在 n=0..20 对拍;Py 675 断言)**530 SSH 传输层与密钥交换**(RFC 4253 与 RFC 8308 全文实读;padding 三约束与最小性闭式、KEX 三条件与 cipher 单条件的差异(rsa2048-sha256 需加密型密钥)、六字母密钥派生与追加式扩展、session_id 终身不变、ext-info-c 与 ext-info-s 名字故意不同;Py 401 断言)**531 IPv6 隐私地址与 SLAAC**(RFC 4941 与 RFC 8981 全文实读;MD5 链取左 64 位清 U/L 与 PRF 从最低位取不清 U/L 的方向相反、生命周期只有 preferred 减 DESYNC、严格大于 REGEN_ADVANCE 才创建、Preferred 为 0 的 RA 不触发新建;Py 65 断言) | S3=索引同步(02-网络安全 README 子领域表加 5 行与已完成表加 5 行与待研究勾 5 项新增 2 项;08-安全 README 已完成表加 5 行与待研究新增 1 行)+ 归档(completed 100 加 5 后 rotate 保留 100 行即 ID 432..531;schedule 本轮行)与 STATE 二节三节滚动 | +5 | 526→531 | 权威来源:**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 与 curl 带 User-Agent 直抓原文后本地解析,不消耗检索额度:rfc-editor.org 的 rfc5961.txt 45KB、rfc5452.txt 37KB、rfc9162.txt 128KB、rfc6962.txt 55KB、rfc4253.txt 68KB、rfc8308.txt 29KB、rfc4941.txt 57KB、rfc8981.txt 55KB、rfc4861.txt 235KB、rfc4862.txt 72KB;jsDelivr 上的 torvalds/linux `net/ipv4/tcp_input.c` 232KB 与 `net/ipv4/sysctl_net_ipv4.c` 46KB。**`tcp_ipv4.c` 三条通道全部失败**(raw.githubusercontent 连接重置、jsDelivr 报 Package size exceeded 50 MB、git.kernel.org 握手超时),故 challenge_ack_limit 与 invalid_ratelimit 的**默认值不硬编码**,一律以参数注入并在 README 标注为发行版相关。质量:Python 自检 5 份**全部实跑 1253 断言全绿**(68 加 44 加 675 加 401 加 65);Go 11 个文件无本机工具链(which go gcc clang rustc 全空)走人工审查 + bracket_check 全 BALANCED + go_sanity 全过 + go_crossref 无重名无未定义调用。**开发期实跑修 bug 与断言错 11 处**:①527 首版多条断言共用同一个 sock,第二条被 per-socket 限速吃掉(不是判定错而是状态串味),改为每次断言新建 sock 与 netns,并把「同一 jiffy 内第二次 challenge 被限速」做成显式断言②527 的 SYN 与 RST 判定混用了同一 sk 导致 last_oow_ack_time 残留③527 的 sweep 闭式首版写成 d 整除 wnd 加 1,但步长为窗口时第 k 个猜测恰好落在 nxt 之前 r 处属窗外,要等到第 k 加 1 个才命中,只有 r 等于 0 才是 k 加 1;改正后与实扫在 64 个起点上零差异④527 的窗口 65535 期望值手算成 32768 而精确值是 32768.5,改用截断而非四舍五入⑤527 的 seq_before(2, 0xFFFFFFFF) 断言写反,回绕处 2 是在 0xFFFFFFFF **之后**⑥527 的 E7j 首版拿 0xFFFFFFFF 当「远离 SND.UNA」的例子,实际它距 SND.UNA 仅 11,两个判据都收,改用距十万的那个 ACK⑦528 的 mean_tries_single_shot 默认 N=2.5 得 81920 而文档用 N=2 得 65536,断言必须显式指定 N⑧528 的 E3h 用文档取整后的 4 Mbit/s 去比 0.5 只得 0.4578,改用 4.37 Mbit/s 才对上⑨530 的 mpint 对负数用了 bit_length 导致 -1 被算成两字节,改为「最小能容纳 v 的字节数」判定,-1 是 0xff 而 -129 是 0xff7f⑩530 的 E4b 首版把需要 encrypt 型密钥的算法只放进客户端列表,条件①就先失败了,补进服务端列表并把 host_key_caps 从单值改成能力集合(ssh-rsa 同时具备 sign 与 encrypt,ssh-dss 与 ed25519 只有 sign)⑪531 的 E3b 把重试后保存的 history 误认成上一次 MD5 的右 64 位,实际是**本次** MD5 的右 64 位。行数审计:新增 32 个文件,源文件 1 处超限(go/sshtrans.go 311 行)当场拆出 wire.go(63 行)后 252 行,终态全 ≤252;README 全 ≤176。commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 经 urllib 单次 POST 未重试，发送前已确认 schedule.md 本轮行是 pending) |
| 2026-09-21 18:54 | 44 | M=09-语言学习/Golang **第四批 5 demo**（大类严格轮转第 8 位 → 09-语言学习，`sub_pos[09]=1` → 索引表第 44 项 Golang；上轮 08-安全。开局 shell date=2026-09-21 18:00:52，18:00 槽 +52 秒含会话启动，无 slot_mismatch；`last_run 2026-09-21 16:52` 距 62 分钟 > 45 通过保险。该子类原有 15 个 demo，本轮全部避开已有主题。ID 532-536）：**532 定时器四叉堆**（runtime/time.go 实读：timerHeapN=4 且 parent=(i-1)/4、timerWhen 存时刻快照而 t.when 是真实值、Heaped/Modified/Zombie 三状态位、Stop 只打标记且 pending 取清零前的值、Reset 让 zombie 复活、cleanHead 先摘堆尾再处理堆顶、updateHeap 只有 siftDown 所以改早必须靠 adjust、ticker 的 next=when+period*(1+delay/period) 跳过 missed tick、一次性定时器触发后瞬时置 zombie 再被 deleteMin 摘除；实测四叉堆上浮次数约为二叉堆 0.57 倍，Py 100 断言）· **533 netpoll 与网络轮询器**（runtime/netpoll.go 与 netpoll_epoll.go 实读：rg/wg 二值信号量的四态、**gopark 之后的代码只在被唤醒后执行**故「仍是 G 指针」不等于数据损坏、unblock 的 ioready 决定置 pdReady 还是 pdNil 所以超时唤醒后是 pdNil、delta 只在换出 G 指针时减 1、pdNil 且非 ioready 时是 no-op、deadline 靠 rseq 比对失效、WakeSig 的 CAS(0,1) 去重；Py 72 断言）· **534 栈增长与连续栈**（runtime/stack.go 与 proc.go 实读：stackMin=2048、fixedStack 上取整到 2 的幂、stackGuard=800×multiplier+stackSystem+128 三平台实测、newstack 翻倍后再按 needed 与 used 比较可能继续翻倍且判据是严格小于、1e9 与 2.5e8 两个十进制上限、三个哨兵值且 stackPreempt 走抢占分支、收缩要求 used 严格小于四分之一且下限是 fixedStack 故 windows 的 8192 永不收缩；Py 64 断言）· **535 字符串与 UTF-8 rune**（unicode/utf8 实读：first[256] 高 4 位是 acceptRanges 下标低 4 位是长度、0xED/0xF4/0xE0/0xF0 四个特殊下标分别拦代理区与超上限与两类 overlong、非法与截断返回 (RuneError, 1) 而只有空输入是 size=0、RuneLen 的代理区判断夹在 rune2Max 与 rune3Max 之间、RuneCountInString 把每个非法字节算 1 个 rune；自检把全部 1114112 个合法码点与 Python codec 逐字节对拍；Py 88 断言）· **536 方法集与嵌入提升**（Go 规范 §Method sets 与 §Selectors 与 §Struct types 实读：T 的方法集是 *T 的子集、接口方法集是类型集方法集的交集、嵌入 T 与嵌入 *T 的提升差异（嵌入 *T 时连值类型都能调到指针接收者方法）、选择器取深度最浅且唯一否则非法且字段与方法共用名字空间、定义型指针 Q=*T2 只对字段开后门故 q.x 合法而 q.M0() 非法；官方 T0/T1/T2 示例逐条复现；Py 47 断言） | S1=索引同步（09-语言学习/Golang/README 新增「第四批」5 行已完成清单与 6 条新读来源；09-语言学习/README 的 Golang 计数 15→20 并补一段第四批说明）+ S3=归档（completed 100 加 5 后 rotate 保留 100 行即 ID 437..536；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 531→536 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 与 curl 带 User-Agent 直抓原文后本地解析，不消耗检索额度：golang/go master 的 src/runtime/time.go 44KB、netpoll.go 21KB、netpoll_epoll.go 5KB、stack.go 46KB、runtime2.go 57KB、proc.go 243KB、unicode/utf8/utf8.go 17KB、internal/abi/stack.go、cmd/internal/objabi/stack.go、internal/runtime/sys/consts.go；go.dev/ref/spec 全文 341KB HTML（抽 §Method sets 与 §Selectors 与 §Struct types 的官方示例）、go.dev/blog/strings 17KB 文本、go.dev/doc/go1.3 与 go1.14 与 go1.23 三份 release notes、pkg.go.dev/time。质量：Python 自检 5 份**全部实跑 371 断言全绿**（100 加 72 加 64 加 88 加 47）；Go 12 个文件无本机工具链（which go gcc clang rustc 全空）走人工审查 + bracket_check 全 BALANCED + go_sanity --spec check=2 全过 + go_crossref 无重名无未定义调用。**开发期实跑修 bug 与断言错 6 处**：①532 首版 27 条断言失败，真因是模型漏了 timerHeaped —— 官方不在 addHeap 里置位而是在调用方 maybeAdd() 里（time.go:718），补上后 Stop/modify/run 全通 ②532 的 E8 我断言「改晚后堆顶仍是 25」，实际 updateHeap 会把 25 下沉到下标 1、堆顶变 20，是**期望值算错不是代码错** ③532 的 E11 同理：unlockAndRun 置 zombie 后立刻 updateHeap→deleteMin 摘掉，净变化是 0，改断言为「记录 zombie 峰值」才测到那个瞬时状态 ④533 首版把 gopark 之后的代码当顺序执行，导致「信号量还是 G 指针」被判为 corrupted polldesc —— 官方语义是**被唤醒后才执行**，改为仍在阻塞则返回 false ⑤533 的 deadline 溢出用例在 Python 里恒不触发（Python 无 int64 回绕），加 to_int64 显式折回有符号区间才对上官方 ⑥534 的 E10「栈底地址确实换了」恒假，因为新地址公式用了 moves 而首次搬动 moves=0，改成 (moves+1)。行数审计：新增 23 个文件，源文件 2 处超限当场拆（定时器 heap.go 200 加 timers.go 205、netpoll netpoll.go 232 加 deadline.go 123），终态全 ≤232；5 篇 README 183 加 186 加 185 加 185 加 187 全 ≤200。commit 消息时间串全部用 $(date '+%Y-%m-%d %H:%M') 命令替换生成 | notify: ok |
| 2026-09-21 22:46 | 47 | M=11-性能分析/03-基准测试方法论 **第三批 5 demo**（大类严格轮转第 11 位 → 11-性能分析，`top_pos=10`，`sub_pos[11]=2` → 索引表第 47 项 03-基准测试方法论；该子类原有 9 个 demo，本轮全部落在 README 的 5 条待研究项上。开局 shell date=2026-09-21 22:00:55，22:00 槽 +55 秒含会话启动，无 slot_mismatch；`last_run 2026-09-21 20:01` 距 120 分钟通过保险。ID 541-545）：**541 JMH -prof gc 与 Go -benchmem 分配口径**（`GCProfiler.java` 与 `src/testing/benchmark.go` 实读；norm 列只在 allocated 非零时发射而 Go 恒打印 0 B/op、进程级 vs 计时窗口、`gc.count` 用 SUM 而 `alloc.rate` 用 AVG 故聚合后不可相除（两轮 count=2、norm=6.5 而加权真值 3.636）、`gc.time` 条件发射、负差钳 0、per-thread 跳过当前线程并漏掉短命线程、churn 是净回收且只在 c 大于 0 时累加、JMH 无对象数口径；Py 40 断言）· **542 预热充分性程序化判定**（pyperf `_worker.py`/`_utils.py`/`_runner.py` 与 JMH `Defaults.java`/`Warmup.java` 实读；样本对半分、离群只查最大值且 iqr 不含首值、`mean_diff` 区间非对称负 0.5 到正 0.10、离散度用 MAD 不用 stdev、采样总数恒为 nwarmup 加 40、MAX_WARMUP_VALUES=300、纯噪声时判据自身抖动把 nwarmup 推到 24、JMH 固定 5 次实测欠预热 216%；Py 35 断言）· **543 趋势存储与分片**（treeherder `etl/perf.py`/`perf/models.py` 与 catapult `graph_data.py` 实读；signature_hash 把键值混进同一个桶排序故键值互换碰撞、`json.dumps` 不排列表顺序故调用侧必须先 sorted、summary 仅在 suite 带 value 时建、路径即主键且父子由前缀推导、Row 主键含 revision 故同 revision 覆盖、LastAddedRevision 拆实体避写热点；Py 54 断言）· **544 容器与虚拟化下的 benchtime 标定**（`internal/runtime/cgroup/cgroup.go`+`testing/benchmark.go`+`runtime/proc.go`+Linux `proc.rst` 实读；CFS 配额的量化闭式使前 Q 的 CPU 时间免费、配额边界是悬崖（51ms 的活墙钟 101ms）、限流不改 N 却把 ns/op 放大约 P/Q 倍（N 都是 1000000 而 ns/op 1000→1950）、缩短 benchtime 到 10ms 可绕开、steal 乘性无量化且串联叠加（2166）、手动设 GOMAXPROCS 永久关掉容器自适应；Py 45 断言）· **545 参数化基准多维公平比较**（hyperfine `range_step.rs`/`relative_speed.rs`/`benchmark_result.rs` 与 JMH `Param.java` 实读；全局最快作参考会把各维度混成一个数（10 = 4 乘 2.5）、分组后组内结论一致、`size_hint` 公式加的是绝对值 1 故整数步长低估小数步长高估且在 100000 上限处放过 100001 个值、误差传播假定协方差为 0、mean 为 0 的三分支、JMH 走外积且 Enum 是唯一有隐式默认值的类型；Py 37 断言） | S1=索引同步（03-基准测试方法论/README 已完成表 +5 行、待研究勾掉本轮 5 项并新增 5 项、新增「2026-09-21 第三批 5 demo 新增来源」11 条链接；11-性能分析/README 待研究 +1 行、参考资料 +1 行）+ S3=归档（completed 100 加 9 后 rotate 保留 100 行即 ID 446..545，其中 537-540 是本轮**补登 20:00 槽未归档的 4 个协议逆向 demo**；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 536→545 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 curl 与 urllib 带 User-Agent 直抓原文后本地解析，不消耗检索额度：openjdk/jmh 的 `GCProfiler.java`、`Defaults.java`、`Warmup.java`、`Param.java`（jsDelivr 镜像）；golang/go 的 `src/testing/benchmark.go`、`src/internal/runtime/cgroup/cgroup.go`、`src/runtime/proc.go`；psf/pyperf 的 `_runner.py`、`_worker.py`、`_utils.py`（默认分支是 main 不是 master，首批按 master 抓全部 404）；mozilla/treeherder 的 `perf/models.py` 与 `etl/perf.py`；catapult-project/catapult 的 `dashboard/dashboard/models/graph_data.py`；Linux 的 `Documentation/filesystems/proc.rst`；sharkdp/hyperfine 的 `range_step.rs`、`relative_speed.rs`、`benchmark_result.rs`（先用 `data.jsdelivr.com` 的 flat 列表反查真实路径，直接猜 `parameter_range.rs` 会得到 88 字节的 not-found 页）。**本轮 raw.githubusercontent 大面积 WinError 10054 与 SSL 握手超时**，全部改走 `cdn.jsdelivr.net/gh/<repo>@<branch>/<path>` 后成功；`torvalds/linux` 在 jsDelivr 上报 Package size exceeded，故内核源码只取到 `proc.rst`，`kernel/sched/fair.c` 未取到 —— 涉及 CFS 配额的**数值一律不硬编码**，period 与 quota 全部作为参数注入并在 README 标注为发行版相关；`openjdk/jdk` 的分层编译阈值（`Tier3InvocationThreshold` 等）三条通道均未取到（`globals.hpp` 里只有 `TieredCompilation`），故预热 demo 的 JVM 侧只用 JMH Defaults 常量、不编造阈值。质量：Python 自检 5 份**全部实跑 211 断言全绿**（40 加 35 加 54 加 45 加 37）；Go 5 个文件无本机工具链（which go gcc clang rustc 全空）走人工审查 + bracket_check 全 BALANCED + go_sanity --spec check=2 全过 + go_crossref 无重名无未定义调用。**开发期实跑修 bug 与断言错 5 处**：①542 的 E8 首版期望「已稳态的曲线只需 1 次暖机」，实测给出 24——真因是 MAD 判据本身在 20 个样本上不稳，纯噪声也会把 nwarmup 推大，改断言为「无噪声时才是 1」并把这个副作用写进 README ②543 的 E3a 首版断言「列表顺序无关」是**错的**，官方 `json.dumps(sort_keys=True)` 只排 dict 的键、列表保持原序，顺序不同 hash 就不同；真正的顺序无关来自调用侧那次 `sorted` ③545 的 E6 首版用 `relative_speeds([ref, zero])` 测「mean 为 0 的分支」，但 `fastest_of` 会把 mean 为 0 的当成最快、于是它自己成了参考，必须显式传 reference 才测得到 INFINITY 分支 ④545 的 E10a 首版把字典序写反了（`size` 小于 `threads`，故列序是 size 在前）⑤545 的 `result.mean / reference.mean` 在 Rust 的 f64 下给 inf 而 Python 直接抛 `ZeroDivisionError`，加 `_fdiv` 显式模拟 IEEE 语义。行数审计：新增 20 个文件，源文件 0 处超限（最大 go/warmup.go 255、python/main.py 293），5 篇 README 136/118/142/148/123 全 ≤200；父目录 03-基准测试方法论/README 从约 100 行涨到 166 行仍 ≤200。commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成。**遗留**：20:00 槽的 4 个协议逆向 demo（537-540）只补登了 `completed.md`，`10-逆向工程/03-协议逆向/README.md` 与 `10-逆向工程/README.md` 的索引同步未做（本轮副任务已用满 2 个） | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 单次 POST 未重试，发送前已确认本轮行是 pending) |
| 2026-09-22 00:01 | 5 | M=01-游戏开发/06-AI 游戏AI **首批 5 demo**（大类严格轮转第 1 位 → 01-游戏开发，`top_pos=0`，`sub_pos[01]=5` → 索引表第 5 项 06-AI 游戏AI；上轮 11-性能分析。开局 shell date=2026-09-22 00:00:50，00:00 槽 +50 秒含会话启动，无 slot_mismatch；`last_run 2026-09-21 22:46` 距 75 分钟 > 45 通过保险。该子类此前只有 1 个 A-star demo，本轮 5 个全部新建子目录。ID 546-550）：**546 行为树执行语义**（BehaviorTree.CPP master 12 份头文件与 cpp 实读；Sequence 与 Fallback 靠 RUNNING 索引停在原地而 ReactiveSequence 每 tick 从头重评估、halt 只作用于 RUNNING 的孩子、Parallel 默认 failure_threshold_ 为 1 使三子节点组在首个孩子失败后立刻判负而后两个孩子一次都不会被 tick、阈值为负时语义翻转且 SKIPPED 算成功票、Inverter 只翻 SUCCESS 与 FAILURE；Py 54 断言）· **547 GOAP 反向规划**（ReGoap 的 ReGoapNode.cs 与 AStar.cs 与 ReGoapPlanner.cs 实读；从 Goal 反向展开前置条件、节点存「还需满足的世界状态差」而非动作序列、Expand 用的是父节点的 Goal、启发式取缺失差异条目数、默认 PlanningEarlyExit 为 false 故得代价 3 的 GetAxe 然后 ChopWood 而非早期退出下代价 5 的 BuyWood、重复状态检测用 break 故入队数随动作声明顺序变化、空计划与无计划不可区分、SelectNextGoal 加权随机且零权重仍以 0.001 参与；Py 29 断言）· **548 Utility AI 评分与选择**（big-brain 的 scorers.rs 与 measures.rs 与 evaluators.rs 与 pickers.rs 实读；SigmoidEvaluator 默认 k 为负 0.5 时并不是 S 形曲线、Singularity 使 f(0.25) 被 clamp 成 0 而第二个奇异点在输入 1.25 处不可达、two_over_dx 公式写的是 ya 属官方潜在笔误、WeightedProduct 折叠初值为 0 故恒等于 0、FirstToScore 用大于等于而 HighestToScore 用严格大于；Py 58 断言）· **549 跳点搜索 JPS**（Harabor 与 Grastien AAAI 2011 全文实读；式 1 直线与式 2 对角邻域剪枝、只在存在 forced neighbour 时保留、jump 沿方向递归且对角先递归两个直线分量、堆里存跳点而非格子；代价与 A\* 完全相同保最优而展开数从 14 降到 1 与 29 降到 1；Py 30 断言）· **550 层级状态机 HFSM**（W3C SCXML 附录 D 全文实读；配置集不含 scxml 容器、isDescendant 排除相等、后代优先于祖先且并行分支互不相关的并发源各自独立、内部转移取 LCCA 而外部转移取真祖先故会先退出源状态自身、历史值必须在 onexit 之前统一记录完否则得到空历史；Py 39 断言） | S3=索引同步（06-AI/README.md 重写：经典模型表加状态列、子领域 5 条链接、已完成 5 行表、跨 demo 共性要点、待研究勾掉 BehaviorTree 与 Utility AI 并新增 7 条、参考资料 5 组实读链接；01-游戏开发/README.md 的 06-AI 状态 待研究 改 进行中 并补 5 行已完成清单） | +5 | 545→550 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 curl 与 urllib 带 User-Agent 直抓原文后本地解析，不消耗检索额度：BehaviorTree.CPP master 的 basic_types.h 与 controls 下 sequence/fallback/reactive_sequence/parallel 四个 h 与 cpp、decorators 下 inverter 与 repeat、src/control_node.cpp 与 src/tree_node.cpp；luxkun/ReGoap 的 ReGoapNode.cs、AStar.cs、ReGoapPlanner.cs、ReGoapState.cs、ReGoapCondition.cs（raw.githubusercontent 连接重置后改走 cdn.jsdelivr.net 成功）；zkat/big-brain 的 scorers.rs、measures.rs、evaluators.rs、pickers.rs、choices.rs、thinker.rs（经 GitHub API 反查仓库后抓取）；AAAI 2011 原文 PDF 取自 users.cecs.anu.edu.au 并用 pypdf 抽文本；W3C SCXML Recommendation 全文 298KB（urllib 得 403，改 curl 后成功）。**未取到的来源一律不硬编码默认值**：cgf-ai.com 的 Orkin GDC2006 GOAP 讲稿与 alumni.media.mit.edu 的 jorkin 草稿三条通道全失败、gameaipro.com 返回 403、icaps12 版 JPS 论文 404、Harel 状态charts 原论文连接重置，故 GOAP 侧只用开源实现语义、效用理论侧只用 big-brain 源码。质量：Python 自检 5 份**全部实跑 210 断言全绿**（54 加 29 加 58 加 30 加 39）；Go 22 个文件无本机工具链（which go gcc clang rustc 全空）走人工审查 + bracket_check 全 BALANCED + go_sanity --spec check=2 全过 + go_crossref 无重名无未定义调用。**开发期实跑修 bug 与断言错 12 处**：①547 的 E4-2 首版拿初始 goal 去判 IsGoal 恒假，真因是 Expand 用的是父节点的 Goal，改用 kid.goal 并补 E4-2b ②547 的 E7-1 期望空实际 GetAxe 仍被展开，改为断言 TradeAxe 不在而 GetAxe 在 ③547 的 E11-2 权重手算错（零权重不是 0 而是 0.001，总和 5.001），拆成 roll 0.0001 与 0.0005 两个用例 ④548 的 E3-5 首版期望 f(0.75) 为 1.0，实际第二个奇异点在 1.25 已越界，改期望为 0.875 并补 E3-5b ⑤548 的 E10-4 是伪断言（is None or 等于 flee），改成显式计算 zeroed 后两条真断言 ⑥549 的 E10-2 首版断言展开数大于 50，实际 15×15 上 A\* 只展开 14，放宽为 exp2 小于等于 2 且小于 A\* 并补 E10-3 ⑦549 的格式化串含中文与百分号触发 ValueError，改用 format ⑧550 的 E1-1 首版配置集含 scxml 容器，规范里容器不计入配置，给 _enter 加跳过分支并同步 12 条配置断言 ⑨550 的 E2-2 把 is_descendant 写成了包含相等导致父状态被自己退出，改为严格后代 ⑩550 的 E4-5 历史值是空列表，真因是历史记录与退出合并成一个循环，按规范拆成两个循环 ⑪550 的 h2 历史伪状态未注册成 State 触发 KeyError，补注册并让 is_atomic 与 is_compound 认识 history 类型 ⑫550 的 E6 与 E9 内部外部转移与并行冲突用例构造错了，重建 m6 机器并改为断言 exit 为 s1a 与 s1、entry 为 s1 与 s1b。行数审计：新增 34 个文件，源文件 3 处超限当场拆（bt.go 583 行拆 8 个、goap.go 412 行拆 5 个、hfsm.go 488 行拆 5 个），终态全 ≤282；5 篇 README 179/162/160/140/176 全 ≤200。commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 经 urllib 单次 POST 未重试，发送前已确认 schedule.md 本轮行为 pending) |
| 2026-09-22 02:00 | 10 | M=02-Web开发/03-数据库 **首批 5 demo**（大类严格轮转第 2 位 → 02-Web开发，`top_pos=1`，`sub_pos[02]=2` → 索引表第 10 项 03-数据库；该子类原有 6 个 demo，本轮全部新建目录。开局 shell date=2026-09-22 02:00:49，02:00 槽 +49 秒含会话启动，无 slot_mismatch；`last_run 2026-09-22 01:05` 距 55 分钟 > 45 通过保险。ID 551-555）：**551 ORM 会话与工作单元**（SQLAlchemy `orm/identity.py` 与 `orm/unitofwork.py` 与 `orm/persistence.py` 实读；身份映射是弱引用的、同键活对象 add 抛 InvalidRequestError 而 replace 不抛、官方 FAQ 明说它不是查询缓存；脏检查只比已提交快照故改回原值不发 UPDATE；flush 三层顺序：saves 早于 deletes、表间走 `_sorted_tables` 拓扑序而 DELETE 取逆序、**表内 UPDATE 先于 INSERT**；Py 30 断言）· **552 批量写入与 Upsert**（PG18《INSERT》与 SQLite《UPSERT》实读；唯一索引推断「不考虑顺序且列集合完全相同」而非部分索引也能被带谓词的 target 命中、DO UPDATE 必须给 target 而 DO NOTHING 可省略、excluded 反映 BEFORE INSERT 触发器、WHERE 最后求值且不满足时行不更新但仍被锁且 RETURNING 不返回、同批两行命中同一行报 cardinality violation 而 DO NOTHING 不受限、SQLite 的 DO UPDATE 冲突处理恒为 ABORT 且 REPLACE 是先删后插、`INSERT ... SELECT` 后接 ON CONFLICT 需补 WHERE 消歧义；Py 35 断言）· **553 JSONB 半结构化与索引**（PG18《8.14 JSON Types》与《65.4 GIN Indexes》实读；json 存原文副本而 jsonb 规范化、jsonb 在输入函数里就拒 `\u0000` 与非法代理对与超范围数字、包含越级不算且数组顺序无关而重复只算一次、数组包含顶层标量是唯一例外且不可反向、存在运算符只匹配顶层、jsonb_ops 键值分开并支持存在运算符而 jsonb_path_ops 只支持包含与 jsonpath、fastupdate 的 pending list 超限才合并且查询必须同时扫 pending、GIN 是 lossy 必须 recheck；Py 43 断言）· **554 行级安全与多租户**（PG18《5.9 Row Security Policies》与源码 `rowsecurity.c` 实读；USING 管已存在的行而 WITH CHECK 管被写出的新行且只写 USING 时后者隐含相同、违反 WITH CHECK 是整句报错、启用却无策略即 default-deny 而未启用时策略不起作用、superuser 与 BYPASSRLS 与表属主默认绕过故多租户必须 FORCE、permissive 取 OR 而 restrictive 取 AND、只有 restrictive 时先全放行再收紧、参照完整性检查总是绕过 RLS 故唯一冲突会泄露存在性；Py 33 断言）· **555 列式存储与 Parquet**（`apache/parquet-format` 的 README.md 与 Encodings.md 与 LogicalTypes.md 实读；max def 数 optional 与 repeated 而 max rep 数 repeated、required 字段的列照样有 NULL、完整复现 Dremel Figure 3 的 Country 列 (r,d) 序列 (0,3) (2,2) (1,1) (1,3) (0,1)、r 记最深的那个新开出现的 repeated 层级且 r=0 是文档边界、数据页 rep → def → values 三段无填充而 NULL 不占 values、RLE 混合编码的 header 最低位是标志位且 bit-packed 恒为 8 个一组且位打包是 LSB-first（官方示例 bit width 3 打包 0 到 7 得 0x88 0xC6 0xFA 逐字节对拍）、varint 是标准 ULEB-128；Py 52 断言） | S3=索引同步（03-数据库/README 子领域加列式存储入口、已完成表 5 行、待研究勾掉列式存储与 OLAP 并新增 4 条、参考资料新增「2026-09-22 新增」10 条并把旧来源归入「早期来源」；02-Web开发/README 已完成表 5 行）+ 归档（completed 100 加 5 后 rotate 保留 100 行即 ID 456..555；schedule 本轮行）与 STATE 二节三节滚动 | +5 | 550→555 | 权威来源：**0 WebSearch / 0 WebFetch / 0 agentic_search** —— 全部 urllib 与 curl 带 User-Agent 直抓原文后本地解析，不消耗检索额度：docs.sqlalchemy.org 的 session_basics 与 session_state_management 全文；jsDelivr 上的 sqlalchemy 源码 `lib/sqlalchemy/orm/persistence.py` 61KB、`identity.py` 9KB、`unitofwork.py` 27KB；gorm.io/docs/session.html；postgresql.org 的 sql-insert.html 21KB、datatype-json.html 30KB、gin.html 22KB、ddl-rowsecurity.html 16KB；sqlite.org/lang_upsert.html 10KB；jsDelivr 上的 postgres 源码 `src/backend/rewrite/rowsecurity.c` 31KB；jsDelivr 上的 apache/parquet-format 的 README.md 15KB、Encodings.md 20KB、LogicalTypes.md 45KB。**dev.mysql.com 本轮返回 “Technical Difficulties”**（urllib 403 与 curl 三通道均失败），故 MySQL 的 ON DUPLICATE KEY UPDATE 与 JSON 两处只在 README 作文献著录、不参与断言。**PostgreSQL numeric 的精确量级上限不做逐位判定**，jsonb 侧只判 IEEE 双精度溢出并在代码注释里写明口径。质量：Python 自检 5 份**全部实跑 193 断言全绿**（30 加 35 加 43 加 33 加 52）；Go 9 个文件无本机工具链（which go gcc clang rustc 全空）走人工审查 + bracket_check 全 BALANCED + go_sanity 全过 + go_crossref 无重名无未定义调用。**开发期实跑修 bug 与断言错 9 处**：①551 首版 IdentityMap 强持有 state 导致「对象 GC 后键不存在」的断言失败，改成 InstanceState 用 weakref 并把 obj 做成 property（期间 Edit 把属性插进了 __init__ 中间，其余赋值被吞掉触发 AttributeError，回读才发现）②551 的 B2 期望值忘了清空上一轮 flush 留下的 sql 列表，是**断言算错不是代码错**③551 的 D3 原断言「查询后语句数增加」是伪断言（模型压根不发 SELECT），改成「清掉库里的行后查询返回空而按主键取仍命中」才真正测到「不是查询缓存」④552 的 D2 首版把 affected 记账放在 DO NOTHING 之前，于是 DO NOTHING 也报 cardinality violation —— 官方只有 DO UPDATE 是 deterministic，记账必须挪到分支之后⑤553 的 A4/A5 首版在**解码后**的字符串上找 `\u0000`，而官方是在**输入函数**里对原始文本检查，永远查不到；改对原始文本检查⑥553 的 A6 用 float 的 inf 与 `1e131072`（本身也是 inf）比较恒假，改成判 IsInf/NaN 并把口径写进注释⑦553 的 D4 pending limit 设成 4 而单行实际抽出 5 个条目（K.tags 加两个 V 加 K.n 加 V1），首轮就触发清理，是**期望值算错**，改成 8 ⑧553 的 `_has_lone_surrogate` 校验完高代理后只跳 6 字节，于是合法代理对的低代理被当成孤立代理，改成跳 12 字节⑨555 的 B6 首版断言「Code 是 required 就没有 NULL」，实际**祖先缺失时 required 列照样写 NULL**（d=1），是模型对而断言错；同时装配函数首版对 `r=k` 的语义写反（在第 k 层又新建了一次出现），改为「k 小于 r 则沿用当前出现」后 C1-C8 全通。行数审计：新增 23 个文件，源文件 7 处超限当场拆（Go 侧 jsonb 拆 gin、upsert 拆 sqlite、uow 拆 identity、columnar 拆 levels 与 encoding；Python 侧 uow 拆 identity、parquet 拆 encoding），终态全 ≤297；5 篇 README 141/137/151/149/184 全 ≤200。commit 消息时间串全部用 `$(date '+%Y-%m-%d %H:%M')` 命令替换生成 | notify: ok(HTTP 200，errcode=0 errmsg=ok；UTF-8 payload 单次 POST 未重试，发送前已确认 schedule.md 本轮行是 pending，发送成功后才回写) |
