| 164 | `10-逆向工程/01-二进制逆向/栈溢出与ROP/` | 栈溢出与 ROP（Aleph1 Phrack 49/14 + Eli Bendersky x86-64 栈帧 + Shacham CCS 2007 + Intel SDM CET：进程布局 Stack/Heap/bss/data/Text 与栈向低地址生长；System V AMD64 栈帧 = [第 7+ 个参数][返回地址][SFP][局部变量][red zone]，前 6 个整型参数走 rdi/rsi/rdx/rcx/r8/r9，call 压 8 字节返回地址；red zone 128 字节是 AMD64 独有、叶子函数可省 prologue，一旦发生调用即被踩；溢出经 buffer→SFP→RET 顺次覆盖把 ret 的弹出目标改成攻击者值——改的不是指令而是「下一条指令在哪」；NX/DEP 只挡代码注入不挡代码重用（rip 指向已在可执行页里的现成代码）；ASLR 挡硬编码地址但相对偏移不变、一次泄露即可重定位；**ROP 模型 `ret` 即 `pop rip`**：被劫持函数的 ret 弹第一个 gadget 地址、gadget 尾部 ret 再弹下一个，rsp 就是程序计数器；x86 密集变长指令 + 无对齐要求 ⇒ 从 0xC3 向前回溯任意字节偏移都能拼出合法序列，非对齐 gadget 让可用 gadget 取之不尽；变体 ret2libc / ret2plt+泄露 / SROP（单条 `syscall; ret` 伪造 signal frame 一次设全寄存器）/ JOP / Blind ROP；CET 两道锁 SHSTK（ret 比对影子栈，伪造链从未被 call 压入故每次 ret 都失败）与 IBT（间接跳转目标须以 ENDBR64 `F3 0F 1E FA` 开头，非 CET CPU 上是无害 REP NOP）；SHSTK 上线后攻击面转向 data-only 攻击与堆上函数指针；坑：rsp%16==0 栈对齐否则 libc 序言 movaps 崩（修法插一个 ret 微调 8 字节）、`-fstack-protector` 只对有 char 数组的函数插桩） | C / Python / Go | 2026-09-14 |
| 165 | `10-逆向工程/01-二进制逆向/Mach-O文件解析/` | Mach-O 文件格式解析（OSX ABI Mach-O File Format Reference + Apple `mach-o/loader.h`：mach_header_64 固定 32 字节（magic=0xFEEDFACF，MH_CIGAM_64 为字节序反转）+ cputype + filetype + ncmds + sizeofcmds + flags + reserved；load_command 通用头只有 cmd+cmdsize，**64 位下 cmdsize 必须 8 字节对齐**（32 位为 4），按 cmdsize 步进时忽略对齐即越界；segment_command_64 固定 72 字节（cmd/cmdsize + segname[16] + vmaddr + vmsize + fileoff + filesize + maxprot + initprot + nsects + flags），cmdsize = 72 + nsects×80；section_64 80 字节（sectname[16] + segname[16] + addr + size + offset + align 2 的幂 + reloff + nreloc + flags：低 8 位节类型 S_REGULAR/S_ZEROFILL/S_CSTRING_LITERALS/S_LAZY_SYMBOL_POINTERS/S_SYMBOL_STUBS、高 24 位属性 S_ATTR_PURE_INSTRUCTIONS）；段名全大写、节名全小写皆双下划线前缀；__PAGEZERO 是 MH_EXECUTE 首段，vmsize 很大而 filesize=0 让解引用 NULL 立刻崩；__TEXT 代码+只读、__DATA 可写 COW、__LINKEDIT 供 dyld；节编号**从 1 开始且跨段连续**（与 ELF 从 0 起相反）；dylib_command 的 union lc_str 存**相对本 load command 起点**的偏移，LC_LOAD_DYLIB 按出现顺序构成有序列表（即 MH_TWOLEVEL 下的库序号 ordinal）、兼容版本须 ≤ 声明值；固定 16 字节名不保证有终止符须强制作终止；未实现 LC_DYLD_CHAINED_FIXUPS/LC_CODE_SIGNATURE/LC_MAIN，大端仅识别不翻转） | Python / Go | 2026-09-14 |
| 166 | `10-逆向工程/01-二进制逆向/控制流图与结构化反编译/` | 控制流图恢复与结构化反编译（Nystrom Cornell §2.1 + Cifuentes LNCS 1994 + Stitt & Vahid TODAES 2007：基本块 = 只能从第一条指令进入、只能从最后一条离开的直线段；leader 算法（第一条指令 ∪ 所有跳转目标 ∪ 跳转的下一条）等价于「跳转目标开块、跳转指令结束块」；补虚拟 entry/exit，块不可达=死代码、exit 不可达=死循环；支配 = 所有 entry→N 路径都过 M，支配关系自反/传递/反对称故每个可达节点有唯一 idom 构成以 entry 为根的支配树，朴素迭代 O(V²)、Lengauer–Tarjan O(E·α(E,V))，迭代版核心是「相交即上溯」；回边 = (n→h) 且 h 支配 n（DFS 祖先只是必要条件），自然循环 = {h} ∪ {不经过 h 能到 n 的节点}，循环头 = 被回边指向的支配者并支配循环体内所有块，循环外加空 pre-header 便于 LICM；可归约 ⟺ 所有回退边都是回边 ⟺ 循环只有唯一入口，其余前向边构成 DAG，结构化语言产生的 CFG 必可归约而 goto/优化器（jump threading）可能造出不可归约；Cifuentes 通用结构集（2 路/n 路条件、前测/后测/无限/多出口循环）+ follow 节点 = 从两条分支出发都能到达的**第一个**共同节点（H..follow 折叠成结构节点后递归）+ 工程准则「结构化失败时优先选多出口循环而非多入口循环」；另一路线区间分析法逐区间折叠，折叠顺序天然决定循环嵌套层级；边界：通用 CFG 结构化是 NP-完全、反编译整体不可判定；坑：忽略 follow 的「第一个汇合点」语义会让 else 被吞进 if 体、用 DFS 回边代替支配性回边会误判循环） | Python / Go | 2026-09-14 |
| 167 | `11-性能分析/01-系统级剖析/eBPF动态追踪/` | eBPF 动态追踪（ebpf.io/what-is-ebpf + docs.kernel.org BPF ISA 规范 + Kprobes 文档：11 个 64 位寄存器 r0-r10，r10 只读且指向 512 字节栈顶故栈访问是负偏移 [-512,-1]；指令定长 8 字节（opcode 1B + regs 1B（dst 低 4 位 / src 高 4 位）+ off 2B + imm 4B）；8 个指令类 LD/LDX/ST/STX/ALU/JMP/JMP32/ALU64，其中 LD 与 ST 类的第 2 字节是 mode（<<5，如 MEM=0x60）与 size（<<3，B=1/H=2/W=4/DW=8）而**不是** code/source——本 demo 编码自检即在此踩坑（ldxdw 期望 0x79 算成 0x39）；call 指令 0x85 用 imm 编码 helper（非标准编码）；验证器 = 对每条路径做寄存器类型格（Uninit/Scalar/PtrToStack/PtrToMapValue）的抽象解释，工作队列按 (pc, 状态) 去重，模拟量未知时按上界保守推进，**有界循环必须能证出上界否则拒载**，栈越界读与未初始化寄存器使用直接拒；验证器是"安全性工具不是安全工具"——它只保证不崩内核、不保证逻辑正确；JIT 把 BPF 译成原生码，r10→rbp、BPF 调用按 C ABI；kprobe 机制：把目标指令首字节换成 int3(0xCC)，触发 int3 异常后内核把它换回单步执行再重新埋入（与 ptrace 软件断点同构），kretprobe 支持 maxactive 实例池并在入口替换返回地址到 trampoline；bpf 侧项目按 hook 类型分：kprobe/kretprobe/uprobe/tracepoint/perf_event/XDP/tc/cgroup/LSM；程序 100 万条指令上限、bounded loop 自 5.3 起、tail call 最多 33 层；Python 侧 mini_bpf.py 用真实编码复刻指令编码 + 验证器 + 迷你 VM，自检 9 个 opcode 常量并跑通 count_events=100） | C / Python / Go | 2026-09-14 |
| 168 | `11-性能分析/01-系统级剖析/ftrace内核追踪/` | ftrace 内核追踪（docs.kernel.org/trace/ftrace.html 全文 + Kprobes 文档：编译期在每个可跟踪函数入口埋一条 nop（原 mcount/`__fentry__` 调用点），**未启用追踪时它真的只是一条 nop** 故开销几乎为零；启用时做 text patching 把 nop 改写成跳转到 trampoline 的调用，`set_ftrace_filter` 命中哪些函数就只改写哪些调用点，`dyn_ftrace_total_info` 报告当前转为 nop 的数量、`enabled_functions` 的 M 标记表示该调用点曾被 ip-modify/direct-call 改过且永不清除；13 种 tracer：function / function_graph / blk / hwlat（硬件延迟，靠 hwlat_detector/width 与 /window 关中断打点）/ irqsoff / preemptoff / preemptirqsoff / wakeup / wakeup_rt / wakeup_dl / mmiotrace / branch（likely/unlikely 分支预测正确性）/ nop（关全部）；function_graph 输出格式：入口行以 ` {` 结尾、出口行是单独 `}`，**出口行上的 duration 才是该函数的 inclusive 耗时**，叶子函数只有一行 `func();` 其 inclusive == self；overhead 标记阈值 `$`>1s `@`>100ms `*`>10ms `#`>1000µs `!`>100µs `+`>10µs；trace 与 trace_pipe 的语义差别是踩坑高发点——trace 是静态快照**不是消费者**（O_TRUNC 打开即清空 buffer）追踪中读可能不一致，trace_pipe **是消费者**读完即消费、无新数据时阻塞，二进制版走 trace_pipe_raw + splice()；per_cpu/cpuN/stats 的 overrun 才是"有没有丢事件"的权威字段；error_log 只保留最近 8 条命令错误；function_profile_enabled=1 打开函数直方图（调用次数+耗时）读 trace_stat/function<cpu>，function-fork 让 set_ftrace_pid 里任务 fork 的子进程自动纳入跟踪；坑：filter 与 notrace 同时命中时不跟踪、写 current_tracer 会清空 buffer 与 snapshot、available_filter_functions 支持写行号比写名字快得多；Python 侧 funcgraph_parser.py 用 `{`/`}` 花括号配对而非缩进宽度还原调用树（缩进会随内核版本变化），inclusive 减子树得 self time，并验证 Σself == Σroot_inclusive == 12802.100 us） | C / Python / Go | 2026-09-14 |
| 169 | `11-性能分析/01-系统级剖析/PMU硬件计数器/` | PMU 硬件计数器与 perf_event_open（man7 perf_event_open(2) 全文：glibc 无 wrapper 只能走 syscall(2)；pid/cpu 组合语义表（0/-1 调用进程任 CPU、0/≥0 仅该 CPU、>0指定进程、-1/≥0 该 CPU 上所有进程需 CAP_PERFMON 或 perf_event_paranoid<1、-1/-1 非法）；struct perf_event_attr 的 size 用 sizeof 填以做前向/后向兼容（PERF_ATTR_SIZE_VER0=64 → VER5=112 逐步长大）；PERF_FORMAT_GROUP 一次读整组 + TOTAL_TIME_ENABLED/RUNNING 做缩放 + ID/LOST；字段语义：sample_period>0 即采样事件、freq 按频率采样内核每 tick 调周期、pinned 仅硬件仅 group leader 抢不到槽位进 error 态 read 返回 0、exclusive 常导致永不运行（NMI watchdog 也在用计数器）、inherit 对 GROUP 无效；硬件缓存事件编码 config = id | (op<<8) | (result<<16)，id 取 L1D/L1I/LL/DTLB/ITLB/BPU/NODE、op 取 READ/WRITE/PREFETCH、result 取 ACCESS/MISS，故"L1D 读未命中"= L1D|(READ<<8)|(MISS<<16)、未命中率用 MISS/ACCESS；**多路复用与缩放是本 demo 核心**：事件数超过计数器槽位时只有一部分时间真的在计数器上跑，等价的整数版缩放 count = (count/running)*enabled + (rem*enabled)/running，**以 time_enabled != time_running 判断是否发生多路复用**、差距越大估算误差通常越大；mmap 必须 1+2^n 页，首页是 perf_event_mmap_page 元数据页含 index/offset/pmc_width（rdpmc 需符号扩展）/cap_user_rdpmc/cap_user_time*/time_shift/mult/offset/zero/data_head/data_tail（data_head 不回绕、读后需 rmb()）；rdpmc 快路径用 seqlock 自旋（lock 变了就重读整块）；precise_ip 0-3 控制 skid，配合 PERF_SAMPLE_REGS_INTR 在 Intel 上拿 PEBS 退休时的寄存器快照；组读 struct read_format 按 nr+2 个 u64 解析、缓冲区不足返回 ENOSPC；Python 侧 pmc_multiplex.py 用非可通约的突发负载周期（BURST_PERIOD=3300）+ 每事件不同相位模拟组原子轮转，实测缩放估算平均误差 1.70%） | C / Python / Go | 2026-09-14 |
| 170 | `11-性能分析/01-系统级剖析/LoadAverage与PSI/` | LoadAverage 与 PSI（Brendan Gregg《Linux Load Averages: Solving the Mystery》+ docs.kernel.org/accounting/psi.html：loadavg 数的是 active = nr_running + nr_uninterruptible（可运行 + 不可中断睡眠 D 状态），**D 状态是 1993 年 Linux 0.99.14 补丁加进去的**，作者 Matthias Urlichs 的理由是"只数可运行进程会把快 swap 换成慢 swap 时负载反而下降"，与直觉相反；副作用是 0.99.14 只有 13 条代码路径会置 TASK_UNINTERRUPTIBLE，到 4.12 已有近 400 条且包含部分锁原语，故 loadavg 偏高可能是 CPU/磁盘/锁三者之一、必须再查别的指标；三个数**不是算术平均**而是以 5 秒为周期的定点 EMA，FIXED_1=2048、EXP_1=1884/EXP_5=2014/EXP_15=2037，对应 α = 1 - EXP/FIXED_1 = 0.080078/0.016602/0.005371，与理论 1-e^(-5/T) 的 0.079956/0.016529/0.005540 有定点截断误差；直接后果是空闲机器上启动 100% CPU 线程"1 分钟平均"在 60 秒时只到约 0.62（1-e^(-60/60)=0.6321）；/proc/loadavg 五字段 = 1min/5min/15min/running:total/最近分配的 PID；**PSI 相对 loadavg 的关键优势是能区分"忙"与"卡"**：some = 至少部分任务停顿的时间占比，full = 所有非 idle 任务同时停顿（此时 CPU 周期真的在浪费、长时间即 thrashing），CPU full 在系统级**未定义**但自 5.13 起报告并填 0；累计总量用 total 微秒字段做增量才能算任意自定义窗口的停顿占比、也才能抓到不影响均值的瞬时尖峰；PSI trigger 格式 `<some|full> <停顿量 us> <窗口 us>`（如 some 150000 1000000 = 任意 1 秒窗口内累计停顿超过 150 ms 就叫醒），写入后对同一 fd 用 poll/select/epoll 等 POLLPRI，注销即 close；约束：窗口 500 ms~10 s、非特权用户窗口须为 2 s 整数倍、同一 fd 只能一个 trigger 再写返回 EBUSY、进入 stall 才激活且至少保持一个窗口、通知每窗口最多一次、事件源消失 poll 返回 POLLERR；挂 cgroup v2 后每个 cgroup 有 cpu/memory/io.pressure 格式与 /proc/pressure 完全相同，**loadavg 只有系统级全局值无法归因到容器**；Python 侧 loadavg_calc.py 复刻定点 EMA，D 状态场景下 load1 峰值 1.90 而 nr_running=0） | C / Python / Go | 2026-09-14 |
| 171 | `11-性能分析/01-系统级剖析/页缺失与内存剖析/` | 页缺失类型与内存剖析（docs.kernel.org/filesystems/proc.html + Valgrind Massif 手册：minor fault 只在内核页表里建立映射、不需要磁盘 I/O（匿名页首次触碰、COW 的读端、栈增长），major fault 必须从磁盘读入（文件页首次读、swap in），两者差在"要不要等 I/O"而不差在"是否第一次访问"；口径来源 getrusage 的 ru_minflt/ru_majflt、/proc/self/stat 第 10/12 字段、/proc/self/statm、/proc/vmstat 的 pgfault/pgmajfault，perf 侧对应 PERF_COUNT_SW_PAGE_FAULTS_MIN/_MAJ 与 PAGE_FAULTS；COW 让 fork 后父子的每个可写页在首次写时各产生一次 minor fault（本 demo 实测），MADV_DONTNEED 与 posix_fadvise(DONTNEED) 让"下次访问重新 fault"、MAP_POPULATE 则把 fault 提前到 mmap 时从而把 major 变 minor；**PSS/RSS 口径是本 demo 的第二个重点**：RSS 把共享页在每个进程里各算一次会重复计数，PSS 按共享者均摊——单个 1000 私有 + 1000 共享被两个进程映射时 PSS = 1000 + 1000/2 = 1500；**私有/共享的判定依据是映射数而非 MAP_SHARED 标志**（一个 MAP_SHARED 文件只被一个进程映射时照样算私有），故 smaps_rollup 与逐段累加会有差异；smaps 各字段含义（Size/Rss/Pss/Shared_Clean/Shared_Dirty/Private_*/Referenced/Anonymous/Locked/AnonHugePages/THPeligible、VmFlags 的 rd wr mr mw me ac sd 等），THP 会让 PSS 变粗（2 MB 大页按整页均摊）；statm 的 trs/drs/lrs/dt 四个字段自 Linux 2.6 起恒为 0 属历史遗留、不可依赖，VmHWM 是峰值 RSS 用于查内存泄漏、MemAvailable 是对 MemFree 的可分配修正、CommitLimit = swaptotal + overcommit_ratio% × MemTotal；堆剖析用 Valgrind Massif：默认按指令数采样并对间隔**倍增抽稀**（decimation）以控制开销，三种快照类型（`:` 正常/`@` 详细/`#` peak），**峰值只在发生释放之后才可能被记录**故默认有约 1% 欠估（--peak-inaccuracy），`--pages-as-heap=yes` 把 mmap 页也算作堆、--time-unit=B 改口径；分配树的不变量是"父节点 inclusive ≥ 子节点之和、差值即父自身分配"；Python 侧 smaps_pss.py 解析 smaps 并实测 PSS 均摊与 ΣRSS(19456) vs ΣPSS(15360) 的重复计数差异） | C / Python / Go | 2026-09-14 |
| 172 | `05-AI与机器学习/05-经典机器学习/朴素贝叶斯/` | 朴素贝叶斯三变体(scikit-learn §1.9 + `_joint_log_likelihood` 源码:P(y|x) ∝ P(y)·∏P(xi|y),条件独立假设把复杂度降到 O(n·d);GaussianNB P(xi|y)=1/√(2πσ²)·exp(−(xi−μ)²/(2σ²)),方差加 `var_smoothing × 各列极差的最大值` 的 ε;MultinomialNB θ̂yi=(Nyi+α)/(Ny+αn),α=1 为 Laplace、0<α<1 为 Lidstone、α=0 退化为 MLE;BernoulliNB 对**未出现**的词显式乘 (1−θ̂),显式惩罚缺席;对数域先算对数联合再 logsumexp 归一(max-shift 防上溢,等价 sklearn 的 _logsumexp);**未平滑的 −inf 灾难**:ham 类从未见过 win ⇒ logθ=−inf ⇒ P(ham|x)=0、整类后验被钉死(实测 joint_ll=[−inf, −2.5337]、P(spam)=1.000000 vs 平滑后 0.781250);**0×(−inf)=NaN 陷阱**:多项式模型里计数为 0 的维度不能乘 log 概率,必须跳过零项;sklearn 原文 «naive Bayes is a decent classifier but a bad estimator»;Bernoulli vs Multinomial 在短文档上的口径差(0.749310 vs 0.781250) | Python / Go | 2026-09-14 |
| 173 | `05-AI与机器学习/05-经典机器学习/支持向量机/` | SMO 求解 SVM 对偶(Platt 1998 + Fan/Chen/Lin 2005 WSS1:原问题 min ½‖w‖² s.t. yᵢ(wᵀxᵢ+b)≥1,对偶 max Σαᵢ − ½ΣΣαᵢαⱼyᵢyⱼK(xᵢ,xⱼ) s.t. 0≤αᵢ≤C、Σαᵢyᵢ=0;KKT 三档互补松弛 α=0 非支持向量 / 0<α<C 在间隔上 y·f=1 / α=C 在间隔内;箱约束下 αj 的可行区间 [L,H] 由 yᵀα=0 这条斜线与 [0,C]² 相交得到(同类 αi+αj=const、异类 αi−αj=const);解析牛顿步 η=2Kij−Kii−Kjj≤0(核矩阵 PSD 故必然),η≥0 必须跳过;WSS1 最大违反对用 I_up/I_low 与 m(α)≤M(α) 的间隔判据,**比 Platt 的启发式遍历可靠**:实测遍历启发式在第 13 轮误判收敛(w 只更新一半、支持向量上 y·f=1.38),换成 WSS1 后 KKT 违例清零;b 的区间精化 L=max_{I_up}(y−g) ≤ b ≤ min_{I_low}(y−g)=U 取中点(原先用 `α<=1e-6` 当“下界集”是错的);核技巧 linear/poly/rbf/sigmoid 四个工厂;实测线性可分 w=[+0.2738,+0.2707] b=−0.0340 margin=5.1951、2/40 支持向量、C 从 0.01→100 时 SV 数 64→24;XOR 上线性核得到 w≡[0,0] 是真实的退化最优解(不是 bug),rbf(γ=0.5) 准确率 1.0000;**修掉的符号 bug**:αj 更新原为 `aj + yⱼ(Ei−Ej)/η` 是往山上走,线搜索验证当前点已是可行段上的最小点,改成减号才收敛 | Python / C | 2026-09-14 |
| 174 | `05-AI与机器学习/05-经典机器学习/随机森林/` | 随机森林(Breiman 2001 原论文 + sklearn §1.11:bootstrap 有放回抽 N 个样本建树,每结点再从 `max_features` 个随机特征里挑最优切分;袋外估计 OOB = 用**没抽到该样本**的树投票(约 1−(1−1/N)^N → 1−1/e ≈ 0.632 的样本落在袋外),故无需额外留出集 —— 代价是比留出集**略微偏悲观**且无偏;margin 函数 mg(X,Y)=av_k I(h_k(X)=Y) − max_{j≠Y} av_k I(h_k(X)=j),PE* = P(mg<0),泛化误差界 PE* ≤ ρ̄(1−s²)/s²(s 为 strength、ρ̄ 为平均相关系数)说明“单树越强 + 树间越不相关”越好;随机特征子集正是为了压低 ρ̄;切分搜索用**单次排序 + 前缀计数扫描**(原 O(n²) 的逐阈值列表推导改为 sorted 后增量 Counter);OOB 查表预算 `oob_trees[i]` 避免每次 O(T·n);排列重要性(打乱某列后准确率下降)vs 基尼重要性(不纯度下降),前者可以画出误差棒;实测 test 0.9000 / OOB 0.9667、strength 0.7954、树数 1→300 时 OOB 误差 0.2105→0.0333、`bootstrap=False` 时 OOB 为 nan(袋子恒等于全集);f0/f1 排列重要性 0.1856/0.1589 vs 噪声列 f2/f3 0.0078/0.0000 | Python / C | 2026-09-14 |
| 175 | `05-AI与机器学习/05-经典机器学习/提升算法/` | AdaBoost.SAMME 与梯度提升(sklearn `_weight_boosting.py` 源码 + 《1.11.7/1.11.1》+ Friedman 2001/2002:Bagging 靠并行平均降**方差**、Boosting 靠串行纠错降**偏差**;初始 wᵢ=1/N,每轮算加权错误率 err=Σwᵢ·1(h≠y)/Σwᵢ,α = lr·(log((1−err)/err) + **log(K−1)**),权重更新 `wᵢ ← wᵢ·exp(α·1(错))` 后**必须归一化**否则连续 exp 溢出(sklearn 用 `exp(log(w)+α·incorrect·(w>0))` 数值更稳);`log(K−1)` 的作用:把“比随机猜好”的判据从 err<1/2 统一成 **err < 1−1/K**,否则多分类在 1/K<err<1/2 这段会把比随机差的学习器当好的用;两道早停闸 `err≤0 → α=1.0 并停`、`err ≥ 1−1/K → 丢弃该学习器`(否则 err→0 时 log 爆炸);加权投票“预测对投 +α、错投 −α/(K−1)”、概率用 SAMME.R eq.(15)(K=2 用 [−f,f]/2、K>2 用 softmax(f/(K−1)));GBDT 是**函数空间的梯度下降**:加性模型 F_m=F_{m−1}+ν·h_m,一阶泰勒 ⇒ h_m 每轮拟合当前模型的负梯度(最小二乘下即残差),F₀=ȳ,子学习器**始终是回归器**;shrinkage ν 与 n_estimators 强交互、subsample 为随机梯度提升;坑:XOR 上树桩 AdaBoost 只有 0.59(数据与弱学习器不匹配,不是算法错),换成同心圆后 0.79→0.975;实测 GBDT lr=0.1/#300 MSE 0.000092、lr=0.01/#800 0.000329(缩 10 倍需约 10 倍轮数)、subsample=0.5 训练 MSE 0.000272 | Python | 2026-09-14 |
| 176 | `05-AI与机器学习/05-经典机器学习/模型评估/` | 模型评估三件套(拆为 `_synth.py` 共享数据 + `model_eval.py` + `roc_auc.py`,均 ≤300 行):① **切数据的折**:KFold 测试折 i 取 [n·i/k, n·(i+1)/k),docstring 例 X=[a,b,c,d]/n_splits=2 → 测试 [2 3] 再 [0 1](复现一致);**KFold 默认 shuffle=False** 直接切连续块,实测 600 条极不平衡(正例率 6.67%)且按特征排序的数据上普通 K 折各折正例数 [1,6,10,12,11](首折 recall = 0/(0+0) 无定义、折间极差 0.0917)而分层折恒为 [8,8,8,8,8](极差 0.0000);LOO/GroupKFold/TimeSeriesSplit 的语义边界;② **算分**:混淆矩阵按 sklearn 定义“entry i,j = actually in group i, predicted j”即**行 = 真实类**(搞反会让 precision/recall 对调);precision=tp/(tp+fp)、recall=tp/(tp+fn)、F_β=(1+β²)tp/((1+β²)tp+fp+β²fn);micro(汇总 tp/fp/fn)/macro(单类等权)/weighted(按 support)三口径在二分类下**并不相等**(实测 0.8977/0.9011/0.9000),类越不平衡差距越大;③ **排序**:ROC 的 TPR=TP/(TP+FN)、FPR=FP/(FP+TN),AUC=∫₀¹TPR d(FPR);AUC 三种**恒等**算法全部实现并交叉验证 —— 梯形积分(0.9809253247)、秩和/Mann-Whitney U(Σpos 秩 − n_pos(n_pos+1)/2)/(n_pos·n_neg) 且**并列须取平均秩**、Bamber 定义直接数有序对,三者差 0.00e+00(Gini=2·AUC−1=0.961851 只是线性重标定、与 AUC 信息等价);多分类 OvR macro=(1/(c(c−1)))ΣjΣ_{k>j}(AUC(j|k)+AUC(k|j))、micro 展平后 TPR=ΣTPc/Σ(TPc+FNc);实测 K=3 macro 0.931895 / micro 0.932511、K=2 退化检验 0.922575=per[0];5 折 CV accuracy 0.8465±0.0463、AUC 0.9346±0.0187(单点划分没有误差棒) | Python | 2026-09-14 |
| 177 | `06-DevOps/05-IaC与配置管理/Terraform状态锁与远程后端/` | Terraform 远程后端与 state 锁（HashiCorp 官方 s3 backend 文档 + AWS S3 条件写语义：state 的 source of truth 存远端对象存储，S3 后端对象键 = `<key>`（default workspace）或 `<workspace_key_prefix>/<workspace>/<key>`（默认前缀 `env:`），锁对象为 `<key>.tflock`；**锁的底层原语是条件写 / compare-and-swap**——S3 用 `If-None-Match: *`（PutObject 仅在对象不存在时才成功），DynamoDB 用 `attribute_not_exists(LockID)`（ConditionExpression + ConditionalCheckFailedException），两者是同一语义的两种载体，所以 `use_lockfile` 与 `dynamodb_table` 可互相替代；acquire 即"写入一个不存在过的锁对象"，失败者拿到 412/`ConditionalCheckFailed` 而非覆盖；**锁内容里的 `ID` 是 nonce**：释放与 `force_unlock` 都必须携带创建时写入的同一个 `ID`，否则拒绝——否则 A 的锁会被 B 用同一个 key 误删；`force_unlock` 是 advisory 删除（引擎不校验持有者是否真的死了），`is_stale` 按 timeout 判定过期锁以便自动清理；本 demo 用内存 `ObjectStore.put_if_absent` 复现 CAS 原语并在其上实现 acquire/release/force_unlock/is_stale，6 组场景实测：二次加锁被拒、正常释放、workspace 键隔离（`env:/prod/terraform.tfstate` 与根键互不干扰）、错误 nonce 拒绝、正确 nonce 成功、过期锁可清理；文件：C 300 行（条件写 + `ID=..;Operation=..` 扁平记录 parse/encode）/ Python 231 行 / Go 224 行） | C / Python / Go | 2026-09-15 |
| 178 | `06-DevOps/05-IaC与配置管理/Ansible变量优先级/` | Ansible 变量优先级 22 级（Ansible 官方 playbooks_variables 文档 "Understanding variable precedence" 原文：22 级完整有序列表，从 ① command line `-e` ② playbook 内 `vars`……一直到 ㉒ role 默认 `defaults/main.yml`；总原则是 **last definition wins**，但 inventory 组层级上另有一条 **more specific wins**——子组变量覆盖父组变量，`all` 是最泛的组；关键机制：把"更具体"编码成**同层内的更大排序键**而不是依赖加载顺序，用小数偏移 `level = INVENTORY_BASE + depth * 0.1`（`INVENTORY_BASE = 3`）实现，从而与遍历顺序无关；`hash_behavior` 默认 `replace` 整键替换、设为 `merge` 时 dict 递归合并；role 变量（`roles/x/vars/main.yml`）与任务级 `vars` 分层；本 demo 覆盖 22 级全序、三层 inventory 嵌套（`all` → `web` → `web_east`）、last-write-wins 同层比较、role vars、dict 合并 vs 替换；文件：Python 201 行（`PRECEDENCE` 表为官方 22 级原文字面量）/ Go 251 行） | Python / Go | 2026-09-15 |
| 179 | `06-DevOps/05-IaC与配置管理/Ansible-Vault格式/` | Ansible-Vault 文件格式（Ansible 官方 vault 文档 + needham/ansible-vault 参考实现：header 形如 `$ANSIBLE_VAULT;1.1;AES256` 或 1.2 带 vault-id `$ANSIBLE_VAULT;1.2;AES256;dev`；**密钥派生**用 PBKDF2-HMAC-SHA256、10000 轮、16 字节随机 salt，产出 **80 字节**密钥材料按 **32 / 32 / 16** 切分为 `aes_key / hmac_key / iv`；密文用 **AES-256-CTR**（逐块 counter 加密，无 padding 但需真实 padding 补齐语义）；**HMAC 对密文计算**（encrypt-then-MAC）并前置随 salt 一起写进 body，校验用常量时间比较；body 是 `hexlify` 后的十六进制文本，按 **80 列硬换行** 形成装甲；明文体先做 **RFC 5652 PKCS#7 padding**（补到 16 的整数倍，恰好整块时补一整块 16 字节）；**先验签再解密**的顺序不可颠倒，否则会对着攻击者可控输入跑解密；AES 从零实现（S-box 用 p/q 递推程序化生成避免手抄 256 常数、GF(2⁸) `_xtime`/`_mul`、Nk=8/Nr=14/60 words 密钥扩展、列主序状态、ShiftRows/MixColumns/AddRoundKey），单测锚定 **FIPS-197 附录 C.3** 官方向量 `8ea2b7ca516745bfeafc49904b496089`；实测通过：加解密往返、篡改任一字节被 HMAC 拒绝、错密码被拒、同一明文 4 次加密得到 4 个不同载荷（salt 随机）、padding 边界 15→16 / 16→32 / 17→32；文件：Python `aes.py` 180 行 + `vault_format.py` 201 行 / Go 300 行（用 `crypto/aes` + `cipher.NewCTR`，手写 `pbkdf2SHA256` 与 PKCS#7）） | Python / Go | 2026-09-15 |
| 180 | `06-DevOps/05-IaC与配置管理/Pulumi资源注册与Preview/` | Pulumi 资源注册与 Preview（Pulumi 官方 how-pulumi-works 文档：三组件分工——**language host** 跑用户程序并在其中执行 SDK、**deployment engine** 维护期望状态快照并比对出 plan、**resource provider** 才是真正调云 API 的那层；与 Terraform 的本质差异是 **程序是真跑的**，所以"预览"靠的是引擎把 provider 调用截流为无副作用的登记；**URN** = `urn:pulumi:<stack>::<project>::<parentType$>type::<name>`，它是资源在 state 里的身份，被删资源能重新纳管的关键就是 URN 稳定（改名 = 新建 + 删除）；**Output 的 unknown 在 preview 阶段向上传播**——`unknown` 参与任何派生计算其结果仍是 `unknown`，这正是 CLI 显示 "(known after apply)" 的机制来源；资源按依赖做 **Kahn 分层**决定 apply 顺序；plan diff 四态 `create / update / replace / delete`，`replace` 由 provider 的 `forces_replace` 触发，`deleteBeforeReplace` 决定先删后建还是先建后删；`name` 省略时引擎按 `<type>-<hex>` 自动生成；本 demo 覆盖 URN 文法、preview 中 unknown 传播、依赖双层、up 自动命名、改名建+删、tags 原地更新、replace 先建后删、重复 URN 报错；文件：Python 300 行 / Go 拆 `types.go` 82 + `engine.go` 229 + driver 105 行（按 OPTIMIZATION §1.1 单文件 ≤300 行拆分）） | Python / Go | 2026-09-15 |
| 181 | `06-DevOps/05-IaC与配置管理/GitOps调和与漂移检测/` | GitOps 调和与漂移检测（OpenGitOps v1.0.0 四原则 + Argo CD automated sync 文档 + Kubernetes Server-Side Apply 文档：四原则 = declarative / versioned & immutable / pulled automatically / continuously reconciled，核心推论是"集群状态由 Git 单向驱动，人工改动会被视为漂移"；Argo CD 语义——**每一对 (revision SHA, 渲染参数) 只成功同步一次**，同步失败后**不自动重试**（需人工 rollback 或改 spec 触发新 pair），`selfHeal` 与 `prune` 默认关闭，`allowEmpty` 防误删，调和周期默认 120 s 并叠加 **60 s jitter**（避免所有实例同刻打满 Git/集群 API），实测 8 次间隔落在 120~176 s、与文档给出的抖动上界一致；**live drift 的判据**为 `bool(stale) and not git_moved`——Git 自身前进了不算 live drift，孤儿资源也不算，只有"Git 没动而集群偏了"才是漂移；门控顺序按优先级串行：无 pending → 未开 automated → 有 live drift 且未开 selfHeal → 已同步且未开 selfHeal → 该 pair 已失败 → 仅孤儿且 prune 关 → 否则执行 sync；**Server-Side Apply 的 `managedFields`** 记录每个字段的所有者，同一字段被两个控制器写即冲突，仅 `force=True` 才能夺取所有权，写空值会释放该字段所有权（交还）；9 个场景实测全部符合预期（自动同步 / 手动 spec 不自动 / drift 不自愈 / selfHeal 自愈 / 失败不重试 / prune 关保孤儿 / 开 prune 清理 / SSA 共享所有权 / SSA 冲突与 force）；开发期修掉两个真 bug：`sync()` 原本在"仅孤儿 + prune 关"路径返回 `OutOfSync`，导致该 pair 被计入 failed 并在后续被"不重试"规则永久拦住 → 改为返回 `Synced`（操作成功）而由 `status()` 单独报告应用状态；`live_drift` 原写成 `pending and not git_moved` 会把孤儿误判成漂移 → 改为 `bool(stale) and not git_moved`；文件：Python 拆 `model.py` 134 + `gitops_reconcile.py` 247 行 / Go 拆 `model.go` 156 + 主 276 行） | Python / Go | 2026-09-15 |
| 182 | `09-语言学习/Golang/interface与itab/` | Go interface 底层机制(research.swtch.com/interfaces 原文 + go.dev/ref/spec Interface types/Method sets 章:接口值=双字对(tab,data),两指针对程序隐式;itable=元数据+只含接口要求方法的 fun 函数指针列表,对应(接口类型,具体类型)配对,配对太多无法预计算故运行时生成;生成算法=两个方法表排序后同步遍历 O(ni+nt),生成后进全局缓存只算一次;调用等价 s.tab->fun[0](s.data),方法查找前移到赋值点,调用点仅"几次内存读取+一次间接跳转";两种优化=空接口去 itable 第一字直指类型描述(eface 雏形)+单字值内联不堆分配;方法集三条规则:T 只含值接收者、*T 含两者、指针接收者方法致值类型不实现接口(须 &T 赋值);实现=纯结构化判定 type set 成员,无 implements 关键字;type assertion=s.tab->type 指针比较+解引用 data;comma-ok 接口到接口断言=运行时询问方法集;typed-nil 陷阱:规范示例 x=v 后 x 值为 (*T)(nil) 动态类型 *T,接口第一字非空则整体非 nil,返回具体 nil 指针的函数会被误判 err!=nil;汇编证据 8g -S:MOVL 20(BX),BX 取 fun[0]) | Go(207 行;itable 生成/缓存/方法集/断言/type switch/typed-nil 六节演示) | 2026-09-15 |
| 183 | `09-语言学习/Golang/GPM调度模型/` | Go 调度器 G/M/P 三层模型(Ardan Labs Scheduling In Go Part II + pkg.go.dev/runtime:G=goroutine 执行路径、M=machine OS 线程、P=processor 每虚拟核心一个、M 必须绑 P 才能执行 Go 代码;LRQ 每 P 一个、GRQ 存未分配 G;schedule() 顺序=1/61 概率先查 GRQ->本地 LRQ->偷其他 P 的一半->再查 GRQ->poll network;work stealing 内部称 spinning the M,防 M 进 waiting 并均衡负载;channel/mutex 阻塞=G 重新排队 M 不闲着,同一 M+Core 全程复用,"Go has turned IO/Blocking work into CPU-bound work at the OS level";同步阻塞系统调用(文件 I/O/cgo)=M1 带阻塞 G 与 P 解绑、调度器调 M2 服务 P、G1 回 LRQ 后 M1 挂起备用 placed on the side for future use;异步网络调用=G 移交 network poller 无需新 M;协作式调度安全点=函数调用,1.11- 紧凑循环卡调度,非协作抢占提案接受后落地为 1.14 信号式异步抢占(runtime 文档 asyncpreemptoff 佐证);GOMAXPROCS 控制 P 数,n<1 只查询,默认 min(逻辑CPU,亲和掩码,cgroup 配额) 且不下于 2,阻塞系统调用线程不计入;Goexit 只终止自己先跑 defer 且 recover 返回 nil;切换成本对比 goroutine ~200ns/2.4k 指令 vs OS 线程 ~1000ns+/12k 指令) | Go(129 行)+ Python 模拟(229 行;4 场景断言全过:偷一半/1/61 规则/channel 阻塞 G 让位 M 复用/M-P 解绑与新 M 接管) | 2026-09-15 |
| 184 | `09-语言学习/Golang/GC三色标记与写屏障/` | Go GC 并发三色标记与混合写屏障(proposal 17503 + go.dev/blog/ismmkeynote + gc-guide:延迟演进 1.4 STW 300-400ms->1.5 并发 GC 30-40ms->1.6 4-5ms->1.8 混合写屏障 sub-millisecond,2018 SLO 500µs,均为 Twitter 生产实测;1.7 Dijkstra 插入屏障只拦堆写->栈只能 permagrey->已扫栈保守退灰->周期末 STW 重扫全部灰栈最坏 10s-100s ms;1.8 混合写屏障伪代码 writePointer(slot,ptr){shade(*slot) 无条件 Yuasa 删除侧; if current stack is grey: shade(ptr) Dijkstra 插入侧; *slot=ptr},两 shade 分工=删除侧防"唯一指针从堆挪进栈"、插入侧防"栈里白指针装进黑堆对象",栈变黑后 shade(ptr) 不再必要;效果=栈一次扫描永久黑,重扫/rescan list/栈屏障全删,初实验最坏 STW<50µs(3 个数量级);不变式降为弱三色(Pirinen 98:黑指白可,白须被灰经白色链保护),sound+bounded+complete 形式化证明;代价=浮动垃圾(标记期任一时刻从根可达对象活过本轮);前置=对象分配即黑;GOGC 目标堆=live+(live+roots)*GOGC/100(1.18+ 计根,此前 live*(1+GOGC/100)),官方示例 live 8MiB+根 2MiB,GOGC=100 总足迹 18MiB;GOGC 翻倍=堆开销翻倍 GC CPU 约减半;最小堆 4MiB;Pacer 反馈循环+mark assist(分配大户被拉去标记,gcAssistAlloc>5% 即分配跑赢 GC);GOMEMLIMIT Go1.19 软限制+防颠簸 GC CPU 上限 50% 最坏减速 2x;不分代理由=年轻对象在栈上生生死死,分代需常开写屏障与 ROC 方案同被否;runtime.GC() 只排队 finalizers 不等待执行) | Go(117 行)+ Python 模拟(207 行;3 场景断言全过:混合屏障 soundness 藏匿序列被拦截/浮动垃圾/无屏障反例可达对象被误回收) | 2026-09-15 |
| 185 | `09-语言学习/Golang/context包/` | context 包(go.dev/blog/context Sameer Ajmani 2014 原文:Context 接口四方法 Done()<-chan struct{} 关闭即广播取消/Err() Done 关闭后返回原因 Canceled 或 DeadlineExceeded/Deadline()/Value();没有 Cancel 方法的原因=收信号方不是发信号方,Done 是 receive-only 同理,取消能力 CancelFunc 单独交给派生者、子不能取消父;Background 是树根永不取消;WithCancel=parent.Done 关闭或 cancel() 调用则新 Done 关闭;WithTimeout 新 Deadline=min(now+timeout,parent deadline),timer 未触发时 cancel 释放资源故必 defer cancel;父取消传播到所有后代、子取消向上隔离;select 模式=工作 channel 与 ctx.Done() 竞争,取消后仍 <-c 等工作 goroutine 返回防泄漏(官方 httpDo 骨架);WithValue 只放请求范围数据,官方 userip 模式=包内未导出类型 type key int 做键防冲突,Value 沿树向上查找;Google 实践=call path 上每个函数第一参数 ctx;Deadline 先判断值不值得开工) | Go(167 行;接口/取消树传播/select 竞争/无 Cancel 设计/Value/首参惯例六节演示) | 2026-09-15 |
| 186 | `09-语言学习/Golang/type-parameter泛型/` | Go 1.18 泛型(go.dev/blog/intro-generics Griesemer&Taylor 原文 + spec Interface types 产生式:三大支柱=类型参数+接口即类型集+类型推断;类型参数列表方括号 [T Number];实例化两步=替换类型实参+校验满足约束,成功后即普通函数可当值传递(fmin:=GMin[float64]);约束必是接口,type set 视图优势=可显式往集合加类型,竖线并集;constraints.Ordered 真身=Integer|Float|~string 而非裸类型并集;~T=底层类型为 T 的全部类型(~int 含 type MyInt int),约束场景几乎总写 ~;函数体内允许操作=类型集内全部类型支持的操作;约束写法三步简化 [S interface{~[]E},E interface{}]->[S ~[]E,E interface{}]->[S ~[]E,E any],any=空接口预声明别名;union/~ 接口只能用作约束;函数实参推断只对出现在参数列表的类型参数有效,Make[T any]()T 推不出必须显式;约束类型推断=从 S 的约束 ~[]E 反推 E=切片元素类型,官方 Scale 例子=单参数版返回 []E 丢具名类型 Point 致下游 .String() 编译失败,双参数 [S ~[]E,E] 修复;设计哲学"宁可不推断也不推断错";泛型类型 Tree[T] 接收者携带类型参数,使用前必须实例化;any 不保证可比较,需 == 应收紧约束) | Go(187 行;GMin/类型集/实参推断/约束推断 Scale/Tree[T]/写法简化六节演示) | 2026-09-15 |
| 187 | `07-数据存储/05-图数据库/VarLengthPath/` | 量化路径模式({min,max} 展开为固定长度并集 + 相邻节点模式须匹配同一节点 + 组变量外部绑定为列表(m 不含起点) + 关系唯一性防环内无限展开 + 内联谓词遍历时剪掉整棵子树 vs 先枚举再过滤 — Neo4j Cypher Manual Patterns/Variable-length paths + MATCH) | Python(170 行) + Go(172 行) | 2026-09-15 |
| 188 | `07-数据存储/05-图数据库/BoltProtocol/` | Bolt 协议三层(握手 60 60 B0 17 + 4 版本大端协商 0=断连 / chunked 2B 长度头+65535 上限+00 00 边界 / PackStream marker byte TINY_INT 单字节+F0-FF 负数+8x 字符串+9x 列表+Ax 字典+Bx 结构+tag;size 按 UTF-8 字节数;接收方未收全不得消费缓冲 — Bolt Protocol 官方文档 PackStream + Handshake) | Python(264 行) + Go(170+187 行拆分) | 2026-09-15 |
| 189 | `07-数据存储/05-图数据库/Neo4jLocks/` | 并发控制(READ_COMMITTED 读不加锁 → 100 并发 +1 最坏为 1;SET 右侧直接依赖被读属性才自动写锁否则哑属性技巧;实体级锁:增删关系锁两端节点;等待图 tx→entity→持有者 投影找环,阻塞时等待边必须保留;交叉顺序死锁 vs 固定顺序免疫 — Operations Manual Concurrent data access) | Python(204 行) + Go(273 行) | 2026-09-15 |
| 190 | `07-数据存储/05-图数据库/CypherPipeline/` | 执行计划火山模型(算子二叉树根 ProduceResults 自底向上流;二元两类:Apply 每左行带绑定重启 RHS vs Join/CartesianProduct 两侧完整输入;默认惰性一产出即推,Sort/聚合 Eager 全量缓冲 O(行数);Estimated Rows 统计估算可能错 vs PROFILE 实测 Rows/DB Hits — Cypher Manual Execution plans) | Python(121+238 行拆分) + Go(144+266 行拆分) | 2026-09-15 |
| 191 | `07-数据存储/05-图数据库/MergeSemantics/` | MERGE 语义(find-or-create 全属性精确匹配,属性集不一致即创建"相似"节点需唯一约束兜底;查询内绑定复用 Location 例子;无向先双向匹配再左→右创建;并发 MERGE 关系=两端排他锁+锁后二次 MATCH 吸收竞态,不依赖唯一约束 — Cypher Manual MERGE) | Python(178 行) + Go(236 行) | 2026-09-15 |
| 192 | `10-逆向工程/03-协议逆向/报文序列对齐/` | 协议报文序列对齐（MIT OCW 6.096 Lecture 5 + ScienceDirect Protein Sequencing + MCB112 w04：NW 全局对齐 F(0,j)=-j*d 初始化 + 三分量递推 max(DIAG/LEFT/UP) + 从 F(M,N) 回溯；SW 局部对齐三处修改=初始化全 0/递推加 0 候选/回溯起点改全矩阵 argmax 且走到 0 停；Beddoe 2004 生物信息学协议分析思路=同类型报文互相对齐，不变段=常量字段、可变段=变长字段；consensus star alignment=全部对第一条+插入列合并；打分 match3/mismatch-1/gap-4（mismatch 远低于 gap：变长字段宁可错位对齐也不开洞）；仿射 gap W=γ+δ×(l-1)（Gotoh 1982，典型 -12/-1）线性 gap 会把长 indel 拆碎、本 demo 未实现只留说明；实测 5 条合成报文切出 magic/ver 常量+type/len 低字节变量+len 高字节恒 0+payload 变量+CRC 常量；坑：回溯并列须固定优先级否则不可复现、先聚类后对齐否则常量段被稀释、SW 分数矩阵允许负 mismatch 否则退化为近似 LCS） | Python / Go | 2026-09-15 |
| 193 | `10-逆向工程/03-协议逆向/信息熵字段边界检测/` | 信息熵字段边界检测（BinaryInferno NDSS 2023 论文 + 其源码文档 DeepWiki：香农熵 H=-Σp·log2p；**熵差>1bit 边界规则作用于纵向熵剖面**（跨报文按列统计字节分布的熵，相邻列差超 1 bit 即信息量翻倍置边界）；列分类 const(熵0)/low(<2bit 枚举)/high(随机)；序列启发式 sequenceHeur=列取值跨报文单调递增概率→计数器/序列号标签；横向滑窗熵只适合长流且窗口熵上限 log2(w)（w=64 明文头 4.3bit vs 随机体 ~6bit，边界精度 ±w/2）；探测器集成=多探测器→SIGMA 假设→黑板合成→冲突消解，本 demo 简化为三探测器投票；评测 precision 0.69/recall 0.73/FP 0.04 优于 AWRE/FieldHunter/Nemesys/Netplier/Netzob；实测 24 条合成报文列熵曲线准确切出 magic(0)+ver+type 枚举 1.6bit+seq 列(高熵但 P(monotonic)=1.0)+nonce/body 高熵+CRC，尾部 GAP 占位列熵被压低=变长字段位移污染的活例；坑：变长字段先分桶或先对齐、熵分不出加密与压缩、乱序抓包会把计数器列错判成随机） | Python / Go | 2026-09-15 |
| 194 | `10-逆向工程/03-协议逆向/协议状态机恢复/` | 协议状态机恢复 Veritas P-PSM（ACNS 2011 原文：状态消息=标识协议状态的消息、格式消息=格式里最高频字符串即 keyword；四阶段=报文分析(K-S 检验定消息单元最优数)→状态消息推断(PAM 聚类，medoid 即状态消息)→状态机推断(逐流标注+转移计数)→P-PSM 输出；一阶马尔可夫 P(s'|s)=C/ΣC；评测 SMTP 86%/PPLIVE 100%/XUNLEI 90% 流分类、平均 92% 流刻画；本 demo 实现骨架：首 token+方向做状态标签→逐流转移计数→p>=0.05 且 count>=3 剪枝→流覆盖率回测；实测 30 条合成 SMTP 流 97% 覆盖率、剪出 15 条边含 RCPT 自环(p=0.54)与 550 错误分支；状态合并示例 C:BODY/C:. → C:DATA_BODY 自环；坑：方向必须进状态标签否则请求应答合并退化、逐流统计禁止跨流否则产生 S:221→C:EHLO 伪边、medoid≠众数、剪枝后必须回测流覆盖率） | Python / Go | 2026-09-15 |
| 195 | `10-逆向工程/03-协议逆向/JA3与JA4指纹/` | JA3/JA4 TLS 握手指纹（salesforce/ja3 + FoxIO-LLC/ja4 README + Trueguard/NETCAP 字段级解析：JA3=SSLVersion,Cipher,Extension,EllipticCurve,PointFormat 五字段十进制、字段内-字段间,、wire 顺序、GREASE 忽略、MD5 全长 32 字符；JA4=a_b_c 三段式，a 段 10 字符=t/q+版本(supported_versions 最高值而非 legacy 字段!)+d/i(SNI 有无)+套件数2+扩展数2(剔 GREASE，SNI/ALPN 仍计数)+ALPN 前 2 字符，b 段=排序后套件 %04x 逗号串 SHA-256 截断 12，c 段=排序后扩展(剔 GREASE+0000+0010)+_+wire 顺序签名算法再截断 12；GREASE 判定 (v&0x0f0f)==0x0a0a 位运算；空列表输出 000000000000；排序抗 Chromium 扩展顺序随机化与 cipher stunting，三段可局部匹配 JA4_ac 跨 b 段漂移追踪(GreyNoise 案例)；**官方向量端到端回环验证**：自建 ClientHello 构造→解析→指纹，JA4 复现 t13d1516h2_8daaf6152771_e5627efa2ab1、JA3 复现 769,47-53-...串→ada70206e40642a3e4461f35503241d5，GREASE 注入后指纹不变；坑：握手头是 1+3=4 字节非 5、扩展计数≠进哈希数(16 计数 14 进哈希)、签名算法不排序、ALPN 取首 2 字符是 JA4 而 JA4S 才是首尾字符） | Python / Go | 2026-09-15 |
| 196 | `10-逆向工程/03-协议逆向/Polyglot污点启发式/` | Polyglot 污点分析启发式轨迹驱动复刻（CCS'07 原文全文精读：shadowing=程序处理输入的方式泄露消息格式；字段格式五属性 start/length/boundary(Fixed|Direction|Separator)/type(方向字段?)/keywords；token 表=行 token 列位置、双哈希表 tokens-at-position+token-series 记录全部污点比较；分隔符三步=①token 表②单字节分隔符取连续位置段 series>=③③扩展为多字节(前驱/后继恒定且被比较过、出现>=4 次、上限 4 字节)；关键字=只为真的比较按位置升序拼串、无真比较或撞分隔符断开；方向字段=length/pointer/counter 三类，检测=间接内存访问的目标地址由污点计算→参与地址计算的连续字节即方向字段、最小被访问位置=target 末尾、多次使用只记第一次；定长合并=同一指令使用的连续污点位置并成一个字段(方向字段例外)；实测 HTTP GET 轨迹切出分隔符 

(0x0a 扩展而来)+空格、关键字 GET/HTTP/1.1/Host:，二进制报文切出方向字段[4,5]→target 止于 11+定长 magic(0,1)/crc(11,14)；坑：分隔符扩展必须查"整个分隔符起点的前一字节"否则对同一邻居反复前插、为假比较不能拼串否则 switch 所有分支常量错误合并、格式宽松性(HTTP 空格/Tab 皆可)是论文自述局限、只恢复扁平结构无嵌套） | Python | 2026-09-15 |
| 197 | `11-性能分析/03-基准测试方法论/benchstat统计显著比较/` | benchstat 非参数比较：中位数置信区间（order-statistic 精确区间 + bootstrap 交叉验证，实测覆盖率 0.992 vs 名义 0.95）+ Mann-Whitney U 检验（`U1 = T1 - n1(n1+1)/2` 子集和等价刻画、精确分布仅无并列 n<=50 / 有并列 n<=25、并列校正 `t=Sum(tj^3-tj)`、连续性校正 -0.5、`U1==U2` 则直接 p=1 的离散特例）+ geomean 比例语义（n 个中一个变 2 倍 = 2^(1/n)；假阳性率实测 0.055 vs 名义 0.05） | Python / Go | 2026-09-15 |
| 198 | `11-性能分析/03-基准测试方法论/JMH-perfnorm硬件归因/` | JMH `-prof perfnorm` 复刻：22 条候选事件与逐事件探测、增量（非累计）计数解析（3 列 / >=4 列两种布局）、归一化 `s/(maxTime-minTime)`、**丢最后 2 样本 + 跳过首样本**、`1000*ops/timeMs` 速率换算、CPI/IPC 派生与 `:u` 回退；实测裁剪漏掉会让 cycles/op 高估 30%~37.5%，近似零判定阈值 1e-3；并记录 PMU 是采样估计（实测分数差 4.26x、绝对误差差 21.5x） | Python / Go | 2026-09-15 |
| 199 | `11-性能分析/03-基准测试方法论/统计检验的选择/` | 中位数+分位数 vs t 检验的选择律：Welch-Satterthwaite 自由度近似（t 分布尾概率用不完全 Beta 函数 `P(|T|>|t|) = I_{nu/(nu+t^2)}(nu/2, 1/2)`）、ARE 渐近相对效率（n=10 功效比 0.894 / n=150 为 0.972）、重尾下功效塌陷（Cauchy 均值散布 n=10->1000 为 6.31->30.42，即「加样本不改善均值」）；实测 t 显著而 U 不显著的分歧案例（p=0.0036 vs 0.091） | Python / Go | 2026-09-15 |
| 200 | `11-性能分析/03-基准测试方法论/CI性能回归门禁/` | 双判据门禁（幅度 >=10% + Welch t，df>30 用 1.96 否则查表插值）、Skia/Jetpack 式 step fitting（WIDTH=5、THRESHOLD=25、z>2.0）与朴素差分对比（纯噪声 sigma=6.0 下 59 次比较中 10 次朴素差分假阳性、step fit 报 0）、噪声地板与 CoV 门禁、Bonferroni 与 Benjamini-Hochberg（200 个 benchmark 未校正 13 个假阳性）；修正流传伪码中「相对 delta 除绝对 stderr」的量纲错误 | Python / Go | 2026-09-15 |
| 201 | `11-性能分析/03-基准测试方法论/主动基准测试/` | Gregg Active Benchmarking 7 条 problem checklist 程序化判定（扰动观测、资源争用、网络上限、单线程夹紧 `whileTrue`/`onSpinWait`、测错目标、热降频、结果不可信，其中 6 条可程序化）；含 C 语言 `/proc`+`/sys` 纯文件接口采样器（`read_cpu_times`/`cpu_busy`/`read_task_info`/`read_freq_mhz`/`read_cgroup_quota_cores`） | Python / Go / C | 2026-09-15 |
| 202 | `05-AI与机器学习/07-特征工程与降维/PCA的SVD与协方差路线/` | PCA 两条路线的数值对比:协方差路线物化 p×p 矩阵并解特征分解,官方 `covariance_eigh` 明确警告「条件数翻倍」—— 实测 σ=1e-8 时协方差路线相对误差 2.10e-02、λ_min 实测 -1.518e-19(负值),σ<=1e-10 直接出 nan,而 SVD 路线在 κ=1e12 下最大相对误差仅 4.20e-06(精度预算 eps·κ vs eps·κ²/2);PCA 只中心化不缩放 —— 未标准化时首主成分 = +0.000013·f0 减 1.000000·f1,f1 独占 100.0000%,标准化后 49.97%/50.03%;whiten 后各分量方差归一 [1.0,1.0,1.0];p=4096 时协方差矩阵 128 MiB(故官方 auto 只在小 p 走该 solver);官方文档锚点 explained_variance_ratio_ [6.30061, 0.54980] 与 [0.9924, 0.0076] 复现 | Python(273 行)+ Go(linalg 185 + pca 115 + main 106)+ C(linalg 225 + main 98 + linalg.h 28) | 2026-09-15 |
| 203 | `05-AI与机器学习/07-特征工程与降维/随机化SVD/` | 随机化截断 SVD(Halko, Martinsson, Tropp 2009):n_oversamples 0→20 使 σ 最大相对误差 1.710e-01→1.405e-13、子空间 sinθ 8.688e-01→5.497e-07;谱衰减慢时 n_iter 才是关键(0→7 对应 1.679e-01→7.866e-11);谱衰减快且 n_components>=有效秩时 n_iter=0 已够(2.767e-07,官方 n_iter=0 or 1 should even work fine);power_iteration_normalizer 对比 —— none 在 n_iter>=5 时数值崩(σ 误差 8.123e-01)而 LU 1.239e-10 / QR 1.057e-14(三者 ‖QᵀQ 减 I‖_F 恒 ~1e-16,即正交性不是判别标准,子空间精度才是);开发期踩坑:Gram-Schmidt 必须「正交化一列就立刻归一化」,否则 ‖QᵀQ 减 I‖_F 高达 4.5 | Python(278 行)+ Go(linalg 273 + main 174) | 2026-09-15 |
| 204 | `05-AI与机器学习/07-特征工程与降维/t-SNE与UMAP/` | t-SNE 与 mini-UMAP 的可视化幻觉量化(自写困惑度二分 + 模糊单纯集/交叉熵 SGD):簇大小不可读 —— 40 vs 160 点(真实比 4.00)嵌入 RMS 半径比仅 1.75;纯随机噪声在低困惑度下凭空成团 —— 抱团点占比 0.107/0.040/0.000/0.000 随 P=2/5/30/100;簇间距离可信度强依赖 P —— 真实质心距离 [22.4, 111.8, 89.4],t-SNE P=2 相关系数 +0.055、P=30 +0.933、P=100 +0.994,mini-UMAP +0.608;早停的形态是簇尺度剧烈摆动(簇半径 28.13→4.63→5.57→15.69→1.59)而 KL 仍从 2.948 降到 0.376,即「没收敛的图不能读」;诚实声明 3 点相关系数只是量级趋势、mini-UMAP 不可用于判定 UMAP 优于 t-SNE | Python(tsne 127 + umap_lite 134 + main 199)+ Go(tsne 189 + main 151) | 2026-09-15 |
| 205 | `05-AI与机器学习/07-特征工程与降维/Target编码与泄漏/` | 目标编码的泄漏机理与三种正规做法:官方锚点复现 target_mean_=44.3、smooth=1.0 得 [20.9, 80.8, 43.2](docstring 显示 21 为取整,公式值 20.93)、smooth=5000 得 [44.1, 44.4, 44.3]、smooth='auto' 等价 m=0.0831;1500 类**纯噪声**特征(类别与标签独立)上的泄漏量化:A 训练集内全量编码 训练 0.937 测试 0.712、B 全量数据编码 0.911/0.911(连测试分都变成谎言)、C K 折交叉拟合 0.799/0.787、D 常数编码对照基线 0.799/0.787、E CatBoost 有序统计 0.799/0.787 —— C/D/E 落回基线恰说明它们确实没偷看;smooth 扫掠:smooth=0 时稀有类别取自身单样本均值(0.5/0.0,把噪声当信号),smooth→1e6 时全部塌到全局均值 0.367 | Python(254 行)+ Go(encoding 239 + main 166) | 2026-09-15 |
| 206 | `05-AI与机器学习/07-特征工程与降维/Pipeline与ColumnTransformer防泄漏/` | Pipeline/ColumnTransformer 契约的最小实现与泄漏量化:特征选择泄漏随样本量稀释 —— n=100 时泄漏式 CV 0.627 vs 正确式 0.520(+0.107),n=200 +0.000,n=400 +0.050;最刺眼的是**零信号**行(coef=0)泄漏式仍报 0.573 vs 正确式 0.490(抛硬币上限 0.5,这 +0.083 分纯属凭空变出);留出集 8 次重复均值 0.535±0.146 vs 0.482±0.092(+0.053,且 2/8 次符号相反 ⇒ 单次实验分辨不出);ColumnTransformer remainder 三语义实测 'drop' 8 列 / 'passthrough' 9 列(末位原样 +15.00)/ StandardScaler 9 列(末位 -0.51 用训练集 μ/σ),未见类别「深圳」在仅训练拟合表下退化为全 0 且列宽恒定;嵌套 CV 外层 0.615 vs 在外层验证集上调 k 0.664(+0.049)vs 固定 k=25 0.602(内层 12 次选 k 分布 {25:4, 100:8});开发期修 3 处实现 bug:OneHot 只取 row[0] 致多列类别块静默丢列、ColumnTransformer 存了变换输出而非变换器、行拼接用 sum() 而非 extend | Python(main 299 + pipeline_lite 213)+ Go(ml 255 + main 170) | 2026-09-15 |
| 207 | `09-语言学习/Rust/所有权与借用/` | 所有权与借用检查器模型(E0382/E0499/E0502/E0596/E0106 五大报错抽成可运行期判定的规则;数据流查 move-后-使用 + 借用区间两两相交查 E0499/E0502 + 透过 & 写查 E0596 + 无入参可借查 E0106):NLL 与词法作用域**双模型对比** —— 同一片段(r1=&s; r2=&s; 用 r1/r2; let r3=&mut s)在词法模型下 r1 区间 `[=====]` 与 r3 相交判 E0502,NLL 下收缩到 `[==]` 后区间不相交判合法;从未被使用的引用区间为空、不参与冲突;Rust 侧 7 个夹具(5 非法 + 2 合法)全 PASS、31 条抽象语句、11 次借用;真实编译通过的 nll_live_example() 是本二进制能跑起来即证明 | Rust(main 300 行 + 5 个 compile_fail 源文件各自含 rustc 原文报错) + Python(borrow_checker 212 + run_cases 131) | 2026-09-15 |
| 208 | `09-语言学习/Rust/Copy与Drop/` | Move/Copy/Clone 与 drop 时机(Rust Reference *Destructors* 四条顺序规则逐条断言):变量逆声明序 `c,b,a`;同时离开多层作用域由内向外 `inner,outer-2,outer-1`;struct 字段**声明顺序** `a,b,c` 与数组**首→尾** `0,1,2`,恰好与变量的逆序相反;模式内绑定逆声明序 `pat-last,pat-first`;覆盖赋值旧值立即 drop(日志里旧值出现在新值之前,即早于作用域结束);move 只 drop 一次;`mem::forget` + 部分 move 后仅 drop `partial-0`(被抑制的 2 个对象记入泄漏清单);函数参数最后 drop(Reference 示例顺序 3 2 0 1);Copy 与 Drop 互斥 = E0184("`Copy` may not be implemented for this type; the type has a destructor") | Rust(main 254 行 + compile_fail E0184) + Python(drop_sim 280 行,作用域机 + scope 嵌套示意图 + forget 泄漏计数) | 2026-09-15 |
| 209 | `09-语言学习/Rust/trait与分发/` | trait 三身份(接口 + 约束 + 分发开关):静态分发用 `std::any::type_name::<T>()` 打印出单态化实体 `NewsArticle` / `SocialPost` 两份,调用点 **0 次间接跳转**;动态分发 `&dyn Summary` 断言 `size_of::<&dyn Summary>() == 2 × size_of::<&NewsArticle>()`(**16 字节胖指针** = 数据指针 + vtable 指针),异构 `Vec<Box<dyn Summary>>` 长度 2 分别走默认实现与覆盖实现两条路径;`impl Trait` 参数位置允许不同类型、泛型 `<T: Summary>` 强制同型、返回位只能单一类型(多类型须 `Box<dyn>`,否则 E0308);孤儿规则/coherence 四种组合(仅「外部 trait + 外部类型」被拒,E0117);blanket impl `impl<T: Display> ToString for T` 使自定义类型白拿 `to_string()`;`Pair<T>` 条件实现只在 `T: Display + PartialOrd` 时提供 `cmp_display` | Rust(main 211 行 + 2 个 compile_fail) + Python(dispatch 189 行,单态化实体计数 vs vtable 计数表 + coherence 校验器 + opaque 返回检查) | 2026-09-15 |
| 210 | `09-语言学习/Rust/智能指针/` | Box/Rc/RefCell/Weak 四个逃生舱的运行期行为全部断言化:Box 递归类型 —— 无间接层报 E0072(无限大小)+ E0391,加 Box 后 `size_of::<List>() = 16`(i32(4)+指针(8)=12 对齐到 16),64 KiB 数组 move 前后**堆地址相同**(只搬指针);deref coercion 三情形,`&MyBox<String> → &String → &str` 编译期插入 2 次 `Deref::deref`;Rc 计数轨迹 `[1,2,3,2]` 与 Book Listing 15-19 一致;RefCell 运行期借用判定(已有 2 个 Ref 时 try_borrow_mut 失败、持 RefMut 时 try_borrow/try_borrow_mut 均失败、双重 borrow_mut 触发 `already borrowed: BorrowMutError` 被 catch_unwind 拦下);`Rc<RefCell<i32>>` 复现 Book 输出 `Cons(RefCell { value: 15 }, Nil)` 等三行;引用环 strong 计数 2→1 **永不归零**(upgrade 仍成功 ⇒ 泄漏但内存安全);Weak 破环后 branch 被释放而 weak 计数 1 不阻止释放、`leaf.parent.upgrade() = None` | Rust(main 228 行 + compile_fail Box 陷阱) + Python(smart_ptr 259 行,伪堆 + strong/weak 计数 + 环检测 + BorrowMutError 语义) | 2026-09-15 |
| 211 | `09-语言学习/Rust/生命周期/` | 生命周期标注与三条省略规则的判定引擎(规则 1 每引用参数各自一个生命周期 / 规则 2 恰好一个输入→赋给所有输出 / 规则 3 多输入但有 &self 仅方法→输出取 self):7 个签名里只有 **2 个**必须显式标注(自由函数 + 多引用入参的 `longest`、无入参却返回引用的 `make_static`),`first_word` 走规则 2、`announce_and_return_part` 走规则 3、输出不含引用的两个无需标注;返回值生命周期取两输入**较短者** —— 区间 `r:[==]` ⊂ `x:[===]` 判合法,把使用点移出 short 作用域即 E0597(与 E0106「签名无法表达」是两回事);结构体持引用 `ImportantExcerpt<'a> { part: &'a str }` 与 `impl<'a>` 复述参数;`'static` 与常量提升(`&None` → `&'static Option<i32>`)生存区间均为全程序 | Rust(main 131 行 + 2 个 compile_fail) + Python(lifetime_solver 223 行,elision 引擎 + 存活区间包含检查 + 规则命中计数) | 2026-09-15 |
| 212 | `01-游戏开发/01-服务端/游戏服务端架构/AOI兴趣管理/` | 九宫格 AOI 兴趣管理(3×3 邻域集合差:old−new→Leave / new−old→Enter / old∩new→Move;1 实体/格时须把 eid 自身从接收列表剔除,否则自发 Move;广播量实测 35,880,000→300,224 = 降 **99.16%**,每实体每步 299.000→2.502 条;滞回门限把换格抖动从 800 次压到 0;自检拆成「集合势断言」+「消息路由精确事件序」两段 — 来源见目录 README 参考资料) | C(270 行) + Python(280 行) + Go(253 行) | 2026-09-15 |
| 213 | `01-游戏开发/01-服务端/游戏服务端架构/帧同步/` | 帧同步 / 确定性锁步(通信回合 + 2 回合前瞻:延迟 1~2 回合 setup_delay=0 且 0 次停顿,延迟 3 回合 setup_delay=1 且 1 次停顿;丢包按「不确定即视为丢失」重传,通信回合 13→15;定点数(Q16.16)两种执行顺序均不失同步,浮点第 2 回合即分歧(5.435714285714286 vs …285,8.882e-16);随机数调用次数不一致(extra_rng)第 1 回合就失同步;trunc −32768 vs floor −32767 显式对照 — 来源见目录 README 参考资料) | C(289 行) + Python(283 行) + Go(188+203 行) | 2026-09-15 |
| 214 | `01-游戏开发/01-服务端/游戏服务端架构/客户端预测与回滚/` | 客户端预测与回滚(Valve Source 模型:tick 15ms、快照 ~20/s、用户命令 ~30/s;回放式和解最大误差 20.00 且 **0 次丢失**,快照式和解 15.00 但有 1 次丢失;无阻挡物对照组客户端领先 +7.50 而快照式落后 −15.00;平滑把单帧最大跳变压到 10.55(降 53%)残留 0.25%;插值延迟 30.0 单位;延迟补偿回溯 190ms → 57.0 单位,朴素判定 miss vs 回溯后 hit — 来源见目录 README 参考资料) | C(284 行) + Python(241 行) + Go(275 行) | 2026-09-15 |
| 215 | `01-游戏开发/01-服务端/游戏服务端架构/网关/` | 游戏网关 / Gateway(连接层+路由层:14 个消息类型→7 个服务,未知类型显式拒绝并计数;实例选择 uid 取模 vs 一致性哈希 —— 扩容 8→9 重映射 **88.88% vs 11.85%**(理论下限 11.11%),负载最大偏差 0.00% vs 25.72%(512 虚拟节点/节点;ketama 默认 160 点为 43.92%,1024 点 23.52% 显示收益递减);会话表存网关 id 支撑 push 回程,无会话/网关已下线/连接已漂移三种情况都返回失败;TTL 300s + 心跳 30s,6 分钟后未刷新的 3 个被回收;gw1 下线断开 10 连接并作废 10 会话,重连后模数 4→3 落点必变 — 来源见目录 README 参考资料) | C(267+102+28 行) + Python(311 行) + Go(215+148+131 行) | 2026-09-15 |
| 216 | `01-游戏开发/01-服务端/游戏服务端架构/匹配系统/` | 匹配系统(Elo 400 分标尺:E(同分)=0.500、+400→0.909、−400→0.091,K=32 时爆冷 +29.09;FIDE 官方 K=40/20/10 分档与 K×n≤700 上限、1400 下限、2200 初始上限、2025-10-01 起 400 分封顶;TrueSkill:新玩家 mu=25 / sigma=8.333 / 展示分 0,双方展示分之和 +6.83≠0 即**非零和**,爆冷时新手动 7.53 分而老手仅 0.43 分,连胜 12 局后 sigma 8.333→4.281,sigma 永不归零靠 tau 动量;匹配质量 0.480(展示分差 25)竟高于 0.051(展示分差 1),同 mu 下新手 0.447 vs 老手 0.902;窗口扩张:FIFO \|Δ真技能\| 6.16 等待 0.00 → 紧窗口 3.27 等待 1.31 tick,前半程 4.11 → 后半程 2.42;队伍平衡贪心 299.9 → 局部搜索 33.5 → 2^n 精确 0.1 — MSR TrueSkill 项目页 + FIDE Rating Regulations) | C(88+40+190+40+190 行) + Python(91+279 行) + Go(81+247+160 行) | 2026-09-15 |
| 217 | `02-Web开发/01-前端框架/React/Concurrent/` | React 18 并发渲染 / Lane 位掩码模型(31 位 lane 取代过期时间:Sync=bit0 / InputContinuous=bit2 / Default=bit4 / TransitionLanes=bit6..21 共 16 位 / Idle=bit29 / Offscreen=bit30;`lanes & -lanes` 取最高优先级 lane、`31 - clz32(lane)` 转索引;两级优先级换算 lanes→EventPriority→Scheduler;laneToTimeout 为 Sync=0 / InputContinuous=250 / Idle·Offscreen=NoTimestamp / 其余 5000ms;饥饿防护 markStarvedLanesAsExpired 从高位扫描,expirationTimes = 首次关注时刻 + timeout(1000 时刻观察到 → 6000 过期,5999 未过期 / 6001 过期);5ms 帧预算时间切片(多片累计 60ms,浮点容差 1e-6);transition 被更高优先级中断则丢弃在跑工作;Suspense 边界用「抛出自身」向上传播挂起 — react.dev v18 并发渲染博客 + useTransition / hydrateRoot 官方文档) | JS(296+164 行) + TS(295 行) | 2026-09-15 |
| 218 | `02-Web开发/01-前端框架/Vue/编译器优化/` | Compiler-Informed VDOM(静态提升产出 `_hoisted_1` 与 `-1 /* CACHED */`;PatchFlags 位掩码 TEXT=1 / CLASS=2 / STYLE=4 / PROPS=8 … DEV_ROOT_FRAGMENT=2048,另有 CACHED=-1 / BAIL=-2;Block Tree 用 dynamicChildren 只收集带 flag 的节点,运行期不再做结构 diff;连续静态兄弟 ≥5(STATIC_CONDENSE_THRESHOLD)压缩为单个 createStaticVNode 一次 innerHTML 挂载;事件缓存 `_cache[0] || (_cache[0] = handler)`;v-if 编成 createCommentVNode(v-if,true) 占位;根节点必须显式 openBlock() 才成为 block。实测 dynamicChildren=3(p/span/button,flags 1,5,32)、静态节点挂载 5 次、优化版与全量版最终 DOM 完全一致而节点访问 6→4、prop 比较 6→2 — vuejs.org rendering-mechanism + vuejs/core patchFlags.ts 原文) | JS(300+224+200 行) + TS(238 行) | 2026-09-15 |
| 219 | `02-Web开发/01-前端框架/Signals/` | TC39 Signals 提案(Stage 1)push-pull 实现(Computed 四状态 ~clean~/~checked~/~computing~/~dirty~;Watcher 三状态 waiting/watching/pending;State.set 同步 notify + 拓扑顺序求值保证 glitch-free —— 改一次 a 后 Δb=Δc=Δd 均为 1 且 D 只见到 10,而 push 基线 D 跑 2 次且看到中间值 7;lossy 语义 10→30;equals 短路;错误缓存(两次 get 抛同一对象);动态依赖;环检测;frozen 期间读写抛错;AggregateError 聚合;`directCheckedToDirty` 对照:提案字面语义读到陈旧值 11,本实现 12 — tc39/proposal-signals + push-pull 算法推导文) | JS(299+271 行) + TS(279 行) | 2026-09-15 |
| 220 | `02-Web开发/01-前端框架/SSR-Hydration/` | React 18 流式 SSR + 选择性水合(renderToPipeableStream 的 shell 在 5ms 先吐骨架,非流式要等 300ms 才有首字节,Δ295;boundary 用 `<!--$S:id-->` 标记,chunk 300ms 到达、301ms 结束,回调顺序 onShellReady/onBoundaryReady/onAllReady = 5/300/301;水合按 boundary 排队:shell 5→35、文档序 shell>b1>b2、空闲时前移到最近到达时刻、用户交互可抢跑尚未到达的顺序外 boundary(jumps=1、interrupted=1、代价 +10ms),无数据则不能抢跑;TTI 220 vs 350,Δ130;mismatch 三态 match/suppressed/fatal/recoverable + 两遍渲染;流式错误处理 —— 状态码已发出 200,只能由 Error Boundary 替换区域并用 Suspense+Error 成对兜底,爬虫走 onAllReady — react.dev hydrateRoot / renderToReadableStream + patterns.dev 流式 SSR) | JS(189+199 行) + TS(217 行) | 2026-09-15 |
| 221 | `02-Web开发/01-前端框架/构建工具/Turbopack/` | turbo-tasks 增量计算引擎(value cell Vc + 函数级记忆化:task 身份 = 函数名 + 参数 ArgIds;读时依赖跟踪 —— 只有 read 到的 cell 才成为依赖,读 4 个 cell 的 graph task 在叶子改动后失效闭包只含 4 个任务而兄弟模块不在内;内容相等短路分「引用相同」与「结构相等」两级,任务重跑但输出相等时保留旧值对象;需求驱动懒打包 —— 只构建 admin 入口时 index/c 分支从未被创建,新增 import 才按需补跑;聚合图把 200 个模块的 diagnostics 收集从 200 次节点访问降到 1 次;文件系统缓存 serialize/deserialize 后重建 0 次执行。实测:冷构建 10 次任务 / 无改动 0 次 / 注释改动 1 次(朴素基线 4 次)/ 改字面量 3 次且最贵的 bundle 零重跑 — nextjs.org Inside Turbopack + turbo_tasks rustdoc + turbo.build/pack) | JS(299+157 行) + TS(300 行) | 2026-09-15 |
| 222 | `03-系统编程/01-网络编程/传输层/TCP拥塞控制/` | TCP 拥塞控制 Reno(RFC 5681) vs CUBIC(RFC 9438):慢启动 cwnd += min(N,SMSS);拥塞避免 AIMD 加性增 cwnd += SMSS²/cwnd(RTT 一轮 +1 MSS);两种丢包信号 —— 3 个重复 ACK 走快重传/快恢复(ssthresh = max(FlightSize/2, 2·SMSS)、cwnd = ssthresh + 3·SMSS 再「放气」回 ssthresh),RTO 超时则 cwnd = 1·SMSS 重进慢启动。CUBIC 只替换拥塞避免阶段的窗口函数(RFC 9438 明文「不改 Fast Retransmit / Fast Recovery」):W_cubic(t) = C·(t−K)³ + W_max、K = ∛((W_max − cwnd_epoch)/C),C = 0.4(SHOULD)、β_cubic = 0.7(SHOULD,注意不是 Reno 的 0.5)、α_cubic = 3(1−β)/(1+β) ≈ 0.5294 用于 W_est 的 Reno-friendly 追平;三段(凹/凸/Reno-friendly)+ 两条护栏;快收敛 W_max = cwnd·(1+β)/2(新流不吃掉旧流的余量)。实测:平均窗口 Reno 298.9 段 → 3.49 Mbit/s vs CUBIC 351.7 段 → 4.11 Mbit/s(+17.7%);回到 W_max=640 的恢复时间 Reno 320 RTT vs CUBIC 8 RTT(≈40×);区域分布 凹 209 / 凸 137 / 快重传 48。12 项断言 | C(261+187 行) + Python(187+247 行) + Go(182+192 行) | 2026-09-15 |
| 223 | `03-系统编程/01-网络编程/零拷贝/sendfile-splice/` | 零拷贝发送路径:read+write(4 次数据搬运 / 2 次 CPU 拷贝 / 4 次用户↔内核上下文切换)→ mmap+write(省 1 次 CPU 拷贝,切换仍 4 次)→ sendfile(2 次切换 / 1 次系统调用 / 无 SG-DMA 时 1 次 CPU 拷贝,有 SG-DMA 时为 0)→ splice(0 次 CPU 拷贝,但需要管道、每块数据 2 次调用)。sendfile 的限制清单:in_fd 不能是 socket、out_fd 自 Linux 2.6.33 起可为任意 fd、单次调用最多 0x7ffff000 字节(超出 EOVERFLOW)、非法参数 EINVAL / 不可寻址 ESPIPE、遇 EINVAL/ENOSYS 要回退 read+write;`TCP_CORK` 与 headers+file 的配合。本机用户态拷贝微基准 ≈64 GB/s,而「只收集引用」≈212–279 GB/s —— 说明省掉的那次 memcpy 随字节数线性增长,而传描述符是 O(1)。10 项断言 | C(168+155 行) + Python(154+173 行) + Go(216 行) | 2026-09-15 |
| 224 | `03-系统编程/01-网络编程/连接管理/backlog与SYN队列/` | TCP 建连的两条队列:Linux 2.2 起 `listen()` 的 backlog 约束的是**全连接(accept)队列**而非半连接队列;全连接队列上限 = min(backlog, net.core.somaxconn)(**静默取小**,`listen(fd,1000000)` 不报错);半连接(SYN)队列上限 = net.ipv4.tcp_max_syn_backlog(每监听器);全连接队列满时 `tcp_abort_on_overflow=0`(默认)静默忽略客户端最终 ACK → 客户端重发 ACK 自愈,`=1` 直接回 RST(快失败、失去自愈);半连接队列满时 `tcp_syncookies=1`(默认)改用 SYN Cookie 且「上限检查不再严格适用」,man 文档对它的措辞极严(「seriously violate TCP protocol」「MUST NOT be used to help highly loaded servers」);somaxconn 默认 4096(Linux 5.4 起,更早 128);单个 SYN_RECV 约 304 字节。实测:accept 间隔 1 → 60/60 建连成功 vs 间隔 50 → 仅 6;abort_on_overflow 0 → 最终 accept 27 个且自愈 18 个 vs 1 → 12 个且自愈 0;syncookies 关 → 处理 SYN 2280 丢 2216 vs 开 → 处理 400 丢 0 发 384 个 cookie;somaxconn 128 → 队列钉在 128、溢出 1632 vs 4096 → 峰值 400、溢出 0。10 项断言 | C(271+171 行) + Python(183+189 行) + Go(177+196 行) | 2026-09-15 |
| 225 | `03-系统编程/01-网络编程/IO模型/scatter-gather/` | scatter-gather IO:readv 分散读 / writev 聚集写(iovec 按**数组顺序**处理 —— 必须先填满 iov[0] 才轮到 iov[1]);IOV_MAX = 1024(现代 Linux,2.0 时代为 16),iovcnt 超限或 iov_len 之和溢出 ssize_t 均 EINVAL;writev 写出的数据是**单一数据块**、不与其他进程输出交错(原子性);短读/短写不是错误,调用方必须循环;TCP_CORK 组包(200 ms 上限到点自动发送、被 TCP_NODELAY 覆盖、Linux 3.14 起 tcp_autocorking 默认开启会**自动**合并小写,所以两种写法的包数只能断言单调关系而不能断言严格相等)。实测:小响应(头 180 B + 体 512 B)时两次 write = 2 个包 vs memcpy 合并 = 1 个 vs writev = 1 个 vs TCP_CORK = 1 个(但 3 次系统调用);大响应(体 1 MiB)时四种写法包数趋同(720/719/719/719)—— 此时 writev 的价值是**省掉用户态 1 MiB memcpy** 而非减少包数;微基准 合并到用户态缓冲 7.83 GB/s vs 只收集 iovec 引用 34.70 GB/s(≈4.4×)。10 项断言 | C(179+153 行) + Python(205+225 行) + Go(263 行) | 2026-09-15 |
| 226 | `03-系统编程/01-网络编程/进程间通信/SCM-RIGHTS-fd传递/` | SCM_RIGHTS 传文件描述符:传的**不是 fd 编号**,而是「对 open file description 的引用」,语义等价于把该 fd `dup` 进目标进程的 fd 表 —— 接收方偏移量、状态、锁全部共享(实测发送方读走 5 字节后传 fd,接收方刚拿到时 `lseek(SEEK_CUR)` 就是 5、接着读到 `-WORL`;按路径 re-open 的那份从 0 读到 `HELLO`);`SCM_MAX_FD` = 253(Linux 2.6.38 前 255),超过 `sendmsg` 报 EINVAL;流式 socket 传控制数据**必须夹带 ≥1 字节真实数据**(否则 EINVAL),接收侧也必须在同一个 recvmsg 里读到 ≥1 字节;控制缓冲按 `CMSG_SPACE(4n)` 而非 `CMSG_LEN` 预留 —— 对齐粒度 8 字节而每个 fd 只占 4 字节,故 CMSG_SPACE(4)=CMSG_SPACE(8)=24 字节,**1 个 fd 和 2 个 fd 占的缓冲一样大**(24 B 能装 2 个;按 sizeof(int)=4 或 CMSG_LEN=20 预留都一个都收不到且不报错);缓冲过小 → 置 MSG_CTRUNC 且**多余的 fd 在接收进程里被自动关闭**(实测 3 个 fd 只给 24 B → 收 2 个、关 1 个,refcount [2,2,2]→[2,2,1];若发送方已先 close 则 [1,1,1]→[1,1,0] 即 OFD 当场释放);接收方 RLIMIT_NOFILE 不足同样自动关闭且 recvmsg **不报错**(man 未规定置标志);发送方在途(in-flight)fd 超 RLIMIT_NOFILE → `ETOOMANYREFS`(Linux 4.5 起诊断,除非有 CAP_SYS_RESOURCE),4.5 之前可「发完即 close」绕过记账无限在途;控制数据是字节流的**屏障**(man 的 4/1/4 字节例子:第 1 次 recvmsg(20) 拿到 5 字节 + fd,第 2 次才拿到后 4 字节);`MSG_CMSG_CLOEXEC`(Linux 2.6.23 起)让收到的 fd **原子**带上 FD_CLOEXEC,消除「recvmsg 之后、fcntl 之前」的 exec 泄漏窗口。34 项断言 | C(229+227+179+38 行) + Python(282+259+80+55 行) + Go(256+96+294 行) | 2026-09-15 |
| 227 | `04-移动开发/01-iOS/UI框架/AutoLayout约束求解/` | AutoLayout 的求解内核 Cassowary:约束是**线性等式/不等式**(`x - y >= 8`),解空间是一个凸多面体,目标是「满足 required 硬约束的前提下,把 strong/medium/weak 各档软约束的加权误差和降到最小」—— 所以它是**线性规划**,不是牛顿法/迭代松弛。单纯形(SIMPLEX)按 Badio & Borning 1992 的论文形式:引入**误差变量** ±e 把不等式转等式(约束 `expr OP 0` 变成 `expr - e⁺ + e⁻ OP 0`),e 的系数进目标函数并按其 strength 加权;required 约束不产生误差变量、只允许「被违反则不可行」。**双单纯形(DUAL SIMPLEX)**用于增量场景:新增约束后当前基解不再可行但保持对偶可行,通过 pivot 把基解「抬」回可行域,复杂度远低于从头再解(增量 addConstraint 平均 ≈ O(n) 次 pivot)。**strength 是可加符号**:required = 1001000·1000000(内部表示成 1001·1000000),strong = 1000000,medium = 1000,weak = 1;同一档内多条软约束共同竞争,越界多少由权重决定 —— 这就是「两个视图都想要 100pt 但父容器只有 150pt」时窗口会**按权重比例**各让 25pt 的原因。冲突时 `required` 违反会 raise `UnsatisfiableConstraint`。实测:Cassowary 增量求解 9 个约束的 pivot 次数远低于「每加一条重新构造」;强度对比实验(两条 weak 对一条 strong)证明 strong 一动不动、weak 平摊让步。23 项断言 | Swift(194+146 行) + ObjC(273+144 行) + Python(248+216 行) | 2026-09-15 |
| 228 | `04-移动开发/01-iOS/UI框架/SwiftUI状态管理/` | SwiftUI 的重算粒度由 **Observation 依赖追踪**决定(Swift 5.9 / macOS 14 起替代 ObservableObject):`@Observable` 宏把每个存储属性改写为 `access`/`withMutation` 一对调用,读取时把「当前属性 → 当前 observer」写进注册表,写入时只通知**真正读过该属性**的 observer。关键差异是**追踪是「按属性」不是「按对象」**:`ObservableObject` + `@Published` 只有 0/1 两态,任一属性变 → 整个视图重算;`@Observable` 下视图 body 读了 `a` 没读 `b`,`b` 变化**不会**触发该视图重算。`withObservationTracking { ... }` 的闭包只在**首次求值**期间记录依赖(第二次求值不再记录),而且**每个注册只通知一次**(one-shot),要持续观察必须在 onChange 回调里重新注册 —— 这正是 SwiftUI 内部每个 body 求值都重新开一次 tracking 的原因。所有权语义:`@State` 由视图持有(值类型,存在 view 的 storage 里,`$` 投影出 Binding);`@StateObject` / `@ObservedObject` / `@Environment` 区分「谁创建、谁持有、谁注入」;`@Bindable` 在 @Observable 时代替 @ObservedObject 的投影能力。实测(模型化注册表)按对象追踪下「改 1 个字段 = 100 次重算」,按属性追踪下同一场景 = 1 次重算;惰性规则验证:构造时未读的属性不产生注册。17 项断言 | Swift(235 行) + ObjC(298 行) + Python(286 行) | 2026-09-15 |
| 229 | `04-移动开发/01-iOS/并发编程/Swift-Concurrency/` | Swift 结构化并发与 actor:①**job**(SE-0304 的定义:task 与 child task 统一抽象为 job,父 job 必须等所有子 job 结束才能结束,这是「结构化」的全部含义)+ **task tree**(不是调用树,是 job 树);②**actor 可重入**(SE-0306 明文:actor 的方法是**挂起点之间**互斥,不是整个方法互斥,`await` 期间允许其他消息进入)—— 所以 `if balance >= amount { await ...; balance -= amount }` 这种**跨 await 的检查-使用**是经典错,必须在同一同步段里完成判断与扣减,或改用「判断-挂起-再校验」二段式;③**exclusive executor**:actor 不是靠锁,是靠自己持有一个串行 executor,job 只在该 executor 上跑,所以 actor 内部根本不会并发;④**协作式取消**:`Task.cancel()` 只置标志 + 向下传播到子 job,不抢占、不抛异常,代码必须自己在每个检查点 `try Task.checkCancellation()`;`withTaskGroup` / `async let` 失败时的隐式取消(首个子任务抛错 → 组内其余子任务被取消);⑤**优先级升级(priority escalation)**:高优先级任务 await 低优先级 task 时,后者会被**临时**提升,避免优先级反转。实测:虚拟时钟事件环里 actor 的 10 个同步段(5 job × 挂起前后各 1)严格不重叠,而 `reentrant=False` 的对照实现会把跨 await 的临界区拉长成死锁;取消传播验证「父取消后子任务在自己下一个检查点才停,已完成的子任务不受影响」。断言:Python 26 + Swift 18;ObjC 侧另附 GCD 真死锁复现(`dispatch_sync` 进当前队列 → 主线程卡死,`dispatch_queue_set_specific` / `dispatch_get_specific` 检测法) | Python(291+300 行) + Swift(158+236 行) + ObjC(72+131+279 行) | 2026-09-15 |
| 230 | `04-移动开发/01-iOS/启动优化/dyld-pre-main/` | App 启动前 dyld 做的四件事(WWDC 2016 Session 406):①**Load dylibs**:读每个 Mach-O 的 `LC_LOAD_DYLIB`,递归加载(苹果官方口径「每个库约 几百 µs ~ 1ms 的固定成本」,所以「少依赖、合并动态库」是第一优化);②**Rebase & Bind**:Rebase 修正镜像内指针(ASLR 滑动后的自指针,`LC_DYLD_INFO` 里的 rebase opcode,成本 ∝ 指针条数),Bind 填写对外部符号的引用(成本 ∝ **外部符号引用条数**,所以「用 `-fvisibility=hidden` 少导出」有效);③**ObjC setup**:注册类/协议/分类,注册 `selector` 名字 —— 这一步是 `+load` 之前,也是「类越多启动越慢」的来源;④**Initializers**:跑 `+load`(ObjC)和 C/C++ 的 `__attribute__((constructor))`。`+load` 与 `+initialize` 的硬保证完全不同:`+load` 由 dyld 在 ObjC setup 后**直接按 IMP 调用**,不走 `objc_msgSend`,因此**不继承、父类先于子类、类先于分类**;`+initialize` 是**懒**的、在第一次给该类发消息前、走 `objc_msgSend`、且**会被子类继承**(子类没实现时父类的方法会被调用两次),所以正确写法里必须 `if (self == [Parent class])` 判一次。Swift 侧:全局常量与 `static let` 都是**懒**初始化(编译成 `dispatch_once` 语义),苹果因此在官方文章里说 "startup time in Swift scales cleanly with no global initializers" —— 把昂贵对象从「`+load` 里建」改成「Swift 全局懒建」通常就是直接收益。实测:ObjC 用 C 数组记录 pre-main 事件序,断言 `+load` 顺序为 父类 → 子类 → 父类分类、不实现 `+load` 的子类不出现;`+initialize` 断言「子类未实现时父类被调用两次且在子类首次发消息时触发」;Python 成本模型里 dylib 数从 8 → 2 时 initializers 成本降幅 > 50%(含每库固定开销 + 符号绑定 + 类注册三段)。24 项断言 | Python(299 行) + Swift(120+99 行) + ObjC(190 行) | 2026-09-15 |
| 231 | `04-移动开发/01-iOS/渲染/CoreAnimation管线/` | Core Animation 的一帧:①**Event**(触摸/定时器)→ ②**Commit Transaction**(`CATransaction` 在 runloop observer 里 commit,内含三段:**Layout** 调 `layoutSubviews`/`updateConstraints` 产出 frame → **Display** 调 `drawInContext:` 产出 bitmap → **Prepare** 把图层的可动画属性打包成 `CA::Render::Layer` 树)→ ③**render server** 进程(`backboardd`/`RenderServer`,消费 IPC 编码的树、做动画插值、按脏矩形重新合成)→ ④**GPU**(合成 + 光栅化)→ ⑤**VSYNC 回条**。**deadline = 下一帧 VSYNC**(60Hz 16.67ms / 120Hz ProMotion 8.33ms),错过就叫 **hitch**,分两类:commit hitch(主线程段超时,表现为「手指滑动时掉帧」)和 render hitch(render server / GPU 超时,表现为「动画仍流畅但内容更新卡」)。**脏标记合并**是三层各自的:多次改 frame 只触发一次 Layout(同一次 commit 内合并)、`setNeedsDisplay` 只让该层 Display、而只改 frame/opacity/transform 这类**可动画属性**连 Display 都不需要(render server 直接改)。**离屏渲染**的触发条件是「不能在同一次光栅化里画完」:圆角+`masksToBounds`、阴影无 `shadowPath`、`shouldRasterize`、`groupOpacity`、mask —— 每次都要额外一次 pass + 一次上下文切换,实测把 30 层圆角+裁剪的列表打开 `shouldRasterize` 后,静态帧从 42 次 draw 降到 1 次,但**一旦内容每次都在变**(每帧重新光栅化 + 失效成本),净收益变负(`C_RASTERIZE_ONCE + C_INVALIDATE` > 每次重绘的省额),这就是「shouldRasterize 只在内容稳定时用」的量化依据。29 项断言 | Python(298 行) + Swift(110+128 行) + ObjC(182 行) | 2026-09-15 |
| 232 | `05-AI与机器学习/01-深度学习/LayerNorm/` | LayerNorm / RMSNorm 的解析反向与不变量(Ba 2016 arXiv:1607.06450 / Zhang & Sennrich 2019 arXiv:1910.07467 / Xiong 2020 arXiv:2002.04745):①**归一化维度决定一切** —— 统计量取自**单个样本的特征维**(per-sample over feature axis),与 batch 完全无关;实测同 batch 内换样本不影响彼此输出(`max\|Δy\| = 0`)、换 batch 组成不影响单样本输出,这正是它取代 BatchNorm 用于变长序列的原因(BN 在 batch=1 或序列长度悬殊时统计量失真)。②**不变量**:LN 对输入平移+缩放都不变(平移被均值吸收、缩放被标准差吸收),RMSNorm 只对缩放不变、**对平移敏感**(不 re-center);实测 eps=0 时 LN 平移不变残差 4.44e-16 / RMSNorm 缩放不变残差 2.22e-16,而 eps>0 会留下 ~2e-05 的「假不变量」残差 —— 因为 eps 破坏了严格齐次性。③**解析反向三路**:dx 同时来自「除标准差路径 + 减均值路径 + 反向进均值的路径」,RMSNorm 为 `dx_j = γ_j·dy_j/r − x_j·Σ_i(γ_i·dy_i·x_i)/(H·r³)` —— 关键坑是 γ 必须在**分子求和内部**而非乘在 x_j 上,写错位置实测相对误差 8.99e-02 vs 正确实现 2.05e-10。④**成本差**:LN 两次归约(均值+方差)vs RMSNorm 一次(均方),实测 LN 116~192 ms vs RMSNorm 109~125 ms(H=4096、20000 次前向;比值随机器负载波动故不作硬判据)。⑤**Pre-LN vs Post-LN 的梯度剖面**:Post-LN 把归一化放在残差相加**之后**,深堆叠时靠后的块梯度远大于靠前的块(首块/末块比 < 1,实测 0.28~0.89,需 warmup 救);Pre-LN 放在分支**内**,首块梯度更大(2.61~4.41);两者量级差 3.9e6,这就是 Post-LN 必须配 warmup、Pre-LN 可省 warmup 的直接原因。22 项 Python 断言全绿 | Python(286+123 行) + Go(265 行) | 2026-09-15 |
| 233 | `05-AI与机器学习/01-深度学习/Softmax交叉熵/` | Softmax 与交叉熵的数值稳定与梯度化简:①**max-shift**:softmax 对任意常数平移不变,故 `logits − max(logits)` 不改变数学结果却能避开 exp 溢出 —— 实测 200 组量级 ~1000 的大 logits,朴素版 80 组出 inf/nan,shift 版**全部有限**;log-sum-exp 同理 `log Σexp(x) = m + log Σexp(x−m)`。②**梯度化简**:`∂ℓ/∂x_k = p_k − y_k`,softmax 与交叉熵连用时的经典抵消,实测解析梯度 vs 中心差分最大相对误差 3.9e-10。③**class weight 下 mean 的除数**:加权交叉熵的平均**除数是 Σw_{y_n}(权重之和)而不是 N**,否则 loss 量级随权重漂移。④**label smoothing**:one-hot 换成 ε/C 的均匀分布后 loss 下界从 0 抬到 `ε·log C`(实测严格 ≥)、梯度不再零和,作用即抑制过度自信。⑤**log_softmax vs log(softmax)**:先 softmax 再取 log 会在 p 极小时下溢到 −inf,log_softmax 直接在 log 域给出有限正确值。27 项 Python 断言全绿 | Python(209+125 行) + Go(211 行) | 2026-09-15 |
| 234 | `05-AI与机器学习/01-深度学习/残差连接/` | 残差连接与恒等映射(He 2015 arXiv:1512.03385 / He 2016 arXiv:1603.05027):①**退化问题**:加深网络后**训练误差反而变高**(不是过拟合,是优化变难),这是 ResNet 的出发点 —— 本 demo 实测 32 层 plain 初始损失 7.62 → 训练后 9.36(变差),同深度 residual 4.34 → 1.50(变好)。②**梯度里的那个「1」**:`∂ε/∂x_l = ∂ε/∂x_L · ∏(1 + ∂F(x_i)/∂x_i)`,即使 ∏∂F 项全为 0,连乘每一项都带常数 1,所以梯度不消失 —— 恒等映射给梯度开了一条无衰减通道;实测首末块梯度范数比 plain 93.9 vs residual 3.3。③**shortcut 消融**:把恒等 shortcut 换成带参投影 `0.9·h·W` 或缩放 `0.9·h` 会重新引入连乘衰减,实测末损失 恒等 1.50 < 缩放 3.50;带参 shortcut 在较大 lr 下发散(本 demo 最终用无参、条件数好的 `0.9·h[:, ::-1]` 倒序投影作对照)。④**实验设计坑**:为使对比只归因于「那条直连」,plain 与 residual 在每个单元分支入口都放同一个逐样本 LayerNorm —— 否则不加归一化的 residual 在 depth=32 直接发散(实测 `‖x_L‖/‖x_0‖ = 4.4e+02`、初始损失 2.9e+06),结论被非目标因素污染。20 项 Python 断言全绿 | Python(201+80 行) + Go(299 行) | 2026-09-15 |
| 235 | `05-AI与机器学习/01-深度学习/学习率调度/` | 学习率调度与梯度裁剪:①**Transformer 的 inverse-sqrt**(Vaswani 2017 公式 3):`lr = d_model^{-0.5}·min(step^{-0.5}, step·warmup^{-1.5})` 是**两分支取 min** —— warmup 段线性增长、之后按 1/√step 衰减,两支恰好相交于 `step = warmup`,实测峰值 6.9877e-04。②**warmup 的作用**:初期参数远离最优点、梯度方向不可信,大 lr 会把参数推向坏区域 —— 实测同一 tiny net,峰值 2e-3 下**无 warmup 发散(nan)、带 150 步 warmup 收敛 2.8187**;峰值 1e-3 下两者都收敛但 warmup 更低(2.4571 vs 2.6854)。③**SGDR 余弦退火 + warm restart**(Loshchilov & Hutter 2017 arXiv:1608.03983):`η_t = η_min + 0.5(η_max−η_min)(1+cos(πT_cur/T_i))`,关键在**周期倍增** `T_i = T_0·T_mult^i` —— 必须先累加周期起点(`while step >= start + T: start += T; T *= mult`),用 `step % len` 在倍增时会算错,实测重启点恰为 0/10/30/70。④**AdamW 解耦权重衰减**(arXiv:1711.05101):原版 Adam 把 L2 加进梯度会被 `1/√v` 的自适应缩放吃掉,导致不同参数衰减强度不等;AdamW 改为直接乘 `(1−lr·λ)`,实测归一化衰减强度比 ≈ 1.000001,而 Adam+L2 达 1.098。⑤**梯度裁剪**(Pascanu 2012 arXiv:1211.5063):按**全局范数**裁剪是等比缩放、方向严格不变(实测裁剪前后 cos = 1),按元素裁剪会改方向(实测偏 11.59°),两者只在梯度接近各向同性时等价。30 项 Python 断言全绿 | Python(211+111 行) + Go(178 行) | 2026-09-15 |
| 236 | `05-AI与机器学习/01-深度学习/混合精度/` | 混合精度训练(Micikevicius 2017 arXiv:1710.03740 + IEEE-754):①**FP16 可表示范围**:min subnormal 2^-24 ≈ 5.96e-08、max 65504,指数范围比 FP32 小 ~30 个数量级,而**尾数只有 10 位** —— FP16 便宜在带宽与吞吐不是精度。②**梯度归零**:小于 min subnormal 一半的梯度直接下溢成 0,阈值 2^-25,实测 2 万个对数均匀梯度里 **20.75% 静默变 0**(loss scaling 即把它们整体左移出危险区)。③**loss scaling**:乘 S 上移,但 S 太大会**溢出到 inf**,且有效精度上限被尾数钉死 —— 实测 S ≥ 2^18 后精度饱和在 2^-11 不再改善。④**FP32 master weights**:纯 FP16 下 `lr·grad` 常小于当前权重的一个 ulp(1.0 附近 ulp = 2^-10 ≈ 9.77e-04,而典型 lr 是 1e-4~1e-7),更新被**四舍五入吃掉** —— 实测 20000 步 FP16 副本纹丝不动,FP32 master 累计下降 2e-3(1000 步时 1.19e-04,解析值 1.00e-04,差值来自 FP32 自身 2^-24 的亚 ulp 累积);单步更新比 ulp **小 3~4 个数量级**。⑤**动态缩放**:连续若干步不溢出就 ×2、一溢出就 ÷2 并跳过该步(实测日志里 scale 的倍增/减半与步数吻合)。⑥**FP32 累加**:FP16 直接累加 10000 项误差 1.06e-2,FP32 累加为 0 —— 这也是 tensor core 内部按 FP32 累加的原因。32 项 Python 断言全绿 | Python(192+105 行) + Go(197 行) | 2026-09-15 |
| 237 | `06-DevOps/01-容器化/Docker层缓存失效/` | BuildKit 构建缓存的失效规则:①**缓存键是链式的** —— 每一步的键 = H(父层键 + 本指令摘要),父层一变后面所有键都跟着变,失效自动级联、无法跳步复用(实测改一行源码:好行序 4 hit/2 miss、坏行序 2 hit/3 miss)。②**多数指令只比指令文本**,只有 `ADD`/`COPY`/`RUN --mount=type=bind` 才 "calculate a cache checksum from file metadata"。③**mtime 被显式排除** —— "If only the mtime of the copied files have changed, the cache is not invalidated",实测平移 30 天 mtime 后 6 步全命中;而 `chmod` 动了权限位就从 COPY 起级联重建。④**普通 RUN 不看文件系统** —— "just the command string itself is used to find a match",故 `RUN apt-get update` 永远命中、一周后重建包还是旧的,只有命令串变了才重建。⑤**构建密钥内容不参与缓存**但 id/挂载路径参与(内容轮换仍命中、改名则失效),要强制失效须另传 `ARG CACHEBUST`。⑥`WORKDIR` 尊重 `SOURCE_DATE_EPOCH`,改它会让 WORKDIR 及后续全部失效。⑦**文档版本分歧**:旧版页写 "the modification time **and size** file metadata",现行页改为 "cache checksum from file metadata" 且明确 mtime 不参与 —— README 以现行页为准并显式记录该差异。18 项 Python 断言全绿 | Python(284 行) + Go(234 行) | 2026-09-16 |
| 238 | `06-DevOps/01-容器化/多阶段构建/` | 多阶段构建与镜像瘦身:①**只有 target 阶段的文件系统成为最终镜像** —— "The Go SDK and any intermediate artifacts are left behind",实测单阶段 814 MB vs 多阶段 14 MB,差值正是被留下的工具链基础层 + 源码树。②**`COPY --from` 只搬指定路径**,源与目标各写各的(源 `/out/app` → 目标 `/app`),引用不存在的源路径直接构建失败。③**`FROM <stage>` 与 `COPY --from=<stage>` 取同样的参数**(官方博客 "Inheriting from a stage"),故 `FROM builder AS build1` 会继承整个文件系统。④**BuildKit 只构建 target 依赖的阶段**,legacy builder "processes all stages of a Dockerfile leading up to the selected --target" —— 官方例子 `--target stage2` 时 BuildKit 处理 base+stage2、跳过 stage1,实测 3 个阶段 vs 4 个。⑤**攻击面**:运行时镜像里没有编译器,应用被攻破也无法在容器内重新编译 payload。⑥`--cache-to` 的 `mode=max` "exports every layer even those of intermediate steps",所以"构建阶段不进镜像"说的是**不被 manifest 引用**,不等于缓存里没有(源码树可能被一起导出)。⑦`--from` 里不能含构建参数(阶段依赖须在构建开始前确定)。20 项 Python 断言全绿 | Python(279 行) + Go(285 行) | 2026-09-16 |
| 239 | `06-DevOps/01-容器化/UserNamespace-UID映射/` | user namespace 的 UID/GID 映射:①**行格式 `ID-inside-ns ID-outside-ns size`,第二字段含义随读者变化** —— 读它的进程与 pid 同命名空间时表示"父命名空间的起始 ID",不同命名空间时表示"读它的进程所在命名空间的起始 ID"。②**只能写一次**(再次写入 EPERM);须换行结尾、须偏移 0、总字节 < 一页、至少一行,否则 EINVAL;行数上限 4.16+ 为 340、更早为 5。③**行数上限与页大小互相挤压** —— 341 行先被页大小拦下(4323 B > 4096 B),340 行只有紧凑到每行 ≤12 字节(实测 3184 B)才塞得进一页。④**无特权写入的三重限制**:只能一行、size 必须为 1、必须把自身在父 ns 的有效 UID 映射进去,且写入者与创建者 euid 相同。⑤**gid_map 需先 `setgroups("deny")`**(永久禁用 setgroups),否则 EPERM。⑥**Linux 5.12 起映射到父 ns 的 UID 0 需 CAP_SETFCAP**(补"缺该能力的 UID 0 进程可造出带命名空间文件能力的二进制"这一漏洞)。⑦**未映射 ID 一律折算成 overflow 65534** —— 官方 rootless 数字 testuser:231072:65536 下容器 0→宿主 231072、65535→296607、65536→65534;**唯一例外**是读 uid_map 第二字段显示 4294967295 而非 65534(因 `(uid_t)-1` 另有含义)。⑧嵌套三层逐层翻译(ns2 999 → ns1 999 → 宿主 232071),上限 32 层超出 EUSERS。⑨文件属主未映射时 setuid 位被静默忽略(等价 MS_NOSUID)。39 项 Python 断言全绿 | C(287 行) + Python(294 行) + Go(226 行) | 2026-09-16 |
| 240 | `06-DevOps/01-容器化/Rootless容器/` | rootless 容器的前置、能力降级与诊断:①**守护进程与容器都在 user namespace 内**,与 userns-remap 的区别是"守护进程本身也没有 root";官方原文 "does not use binaries with SETUID bits or file capabilities, except newuidmap and newgidmap"。②**前置硬条件 subuid/subgid ≥ 65536**(官方示例 testuser:231072:65536),缺 newuidmap / `unprivileged_userns_clone=0` / `max_user_namespaces` 过小 / `$XDG_RUNTIME_DIR` 未设置都会阻断。③**存储驱动白名单与内核门槛**:overlay2 需 kernel ≥ 5.11、fuse-overlayfs 需 ≥ 4.18 且已安装、btrfs 次之、vfs 兜底 —— 实测 5.10 掉到 fuse-overlayfs、4.14 掉到 vfs。④**cgroup 只在 cgroup v2 + systemd 下可用**,且默认只委派 `memory pids`,于是 `--memory`/`--pids-limit` 生效而 `--cpus` **被静默忽略**;写 `Delegate=cpu cpuset io memory pids` 后才生效(委派 cpuset 需 systemd ≥ 244)。⑤**特权端口 `<1024`** 需给 rootlesskit 加 CAP_NET_BIND_SERVICE 或 `ip_unprivileged_port_start=0`。⑥**不支持清单**:AppArmor / Checkpoint / Overlay network / SCTP ports。⑦**四条官方报错原文→根因**映射(operation not permitted → userns_clone=0;no space left on device → max_user_namespaces 太小;No subuid ranges found → subuid 未配置;could not get XDG_RUNTIME_DIR → 未设置)。⑧网络在 RootlessKit 的 netns 内(`docker inspect` 的 IP 宿主不可直达、端口转发默认丢源 IP);`--cap-add` 只作用于容器自身 userns。43 项 Python 断言全绿 | Python(295 行) + Go(225 行) | 2026-09-16 |
| 241 | `06-DevOps/01-容器化/Cgroup-eBPF附加/` | cgroup v2 层级与 eBPF 附加:①**`cgroup.subtree_control` 四条规则** —— 只能启用 `cgroup.controllers` 里列出的、一次写多个**全有或全无**、同名控制器**最后一个生效**(`+cpu -cpu` 净效果是不启用)、子仍启用时父不能禁用(EBUSY)。②**no internal process** —— 含进程的 cgroup 不能启用域控制器(实测 3 个进程时 `+memory` 报 EBUSY,迁走进程后才成功),故加限制必须"先建子节点、迁进程、再启用";根 cgroup 豁免。③**在父上启用会为子节点创建 `cpu.`/`memory.` 前缀接口文件** —— 控制器接口文件属于父而非自身。④**ELF section 名决定 (程序类型, attach 类型)**:`cgroup_skb/ingress`→CGROUP_SKB+INET_INGRESS、`cgroup/connect4`→CGROUP_SOCK_ADDR+INET4_CONNECT、`cgroup/dev`→CGROUP_DEVICE、`cgroup/sysctl`→CGROUP_SYSCTL,且 `cgroup/skb` 与 `cgroup_skb/ingress` attach 类型相同;`xdp`/`tc`/`kprobe` 等非 cgroup 类不能挂到 cgroup。⑤**同一 attach type 上是一条有序程序链**,可单独 detach。⑥**BPF token 把能力检查从 init userns 搬到 BPF FS 所属 userns**("subsequent BPF operations performed with BPF token would be performing capabilities checks within that user namespace"),无 token 时 CAP_BPF/CAP_PERFMON/CAP_NET_ADMIN/CAP_SYS_ADMIN 必须在 init userns 检查;四项委派集合 cmds/maps/progs/attachs 逐项判定。⑦**token 不足以单独授权** —— 进程仍须在该 userns 内具备能力组合(TC/XDP 需 CAP_BPF+CAP_NET_ADMIN、tracing 需 CAP_BPF+CAP_PERFMON),token 跨 userns 无效,`BPF_TOKEN_CREATE` 自身需 `ns_capable(CAP_BPF)`。⑧C 侧还原 `struct bpf_insn` 的 8 字节布局与两个 4 位寄存器域,汇编 LD_MAP_FD→MOV→CALL→EXIT 并给出小端字节流与编解码往返一致。37 项 Python 断言全绿 | C(223 行) + Python(216+159 行) + Go(300 行) | 2026-09-16 |
| 242 | `07-数据存储/01-关系型/ReplicationBinlog/` | 主从复制与 binlog:①**SBR 记 SQL 语句、RBR 记行事件(WriteRows/UpdateRows/DeleteRows,8.0 默认)**,官方已弃用 binlog_format、未来 ROW 成唯一格式;STATEMENT 遇 UUID()/NOW() 等非确定性函数有主从不一致风险(MIXED 自动切 ROW)。②**三线程链路**:主库 dump 线程推 binlog → 从库 IO 线程写 relay log(落盘后才 ACK)→ SQL 线程重放。③**GTID 幂等**:格式 `source_id:transaction_id`,同一 GTID 在同一服务器 commit 后被忽略 → 重放天然防重复;auto-position 免手工指定 binlog 位点。④**半同步三要点**:等的是「≥1 从库把事件写入 relay log 并刷盘」而非从库执行完成;等待点 **AFTER_SYNC**(binlog 落盘后、引擎 commit 前,8.0 默认,ACK 的事务必已持久化)vs **AFTER_COMMIT**(commit 后才等,存在幻读窗口);**超时(默认 10s)静默降级异步**,从库追上再恢复 —— 必须监控 Rpl_semi_sync_master_status,否则丢失窗口悄然打开。⑤官方警告:半同步 failover 后旧主库可能含未 ACK 事务,不应复用为 source。Python 5 断言组全绿(格式对照/三线程/GTID 去重/超时降级+ACK 阻塞验证) | Python(155 行) + Go(123 行) | 2026-09-16 |
| 243 | `07-数据存储/01-关系型/LogicalDecodingCDC/` | 逻辑复制与 CDC:①**逻辑解码流水线**:SQL 变更 → 物理 WAL(LSN 递增)→ walsender+output plugin 解码为行级事件流(begin/insert/update/delete/commit)→ 消费者;**消费方式三选一**(流复制协议 / pg_logical_slot_get_changes / 自定义 output writer)。②**复制槽持留 WAL**:`confirmed_flush_lsn` 之前的 WAL 才能回收 —— 消费者停止 ACK 会把 pg_wal 撑爆(PG 文档 47.2.2 警告,孤儿槽是磁盘膨胀头号来源)。③**REPLICA IDENTITY**:无 PK 表的 UPDATE/DELETE 拿不到完整旧元组(before=null)→ 下游无法精确对账,建议 REPLICA IDENTITY FULL。④**Debezium 事件结构**:op(c/u/d/r-snapshot)+ before/after + source(lsn/slot);MySQL 走 binlog、PG 走逻辑流;**offset 存 connector 侧,重启断点续传**;部署三形态 Kafka Connect/Server/嵌入式 Engine;先全量 snapshot(op=r)再无缝转增量。⑤实现踩坑:事件必须自带**事件级 LSN** 而非批次尾 LSN(否则批内后续事件被误判已消费)—— 这正是实跑断言暴露的真 bug。Python 7 断言全绿 | Python(192 行) + Go(82 行) | 2026-09-16 |
| 244 | `07-数据存储/01-关系型/JoinAlgorithms/` | Join 算法五种:①**成本表(CMU 15-445 讲义数字 M=1000,m=100k,N=500,B=100,0.1ms/IO)**:朴素嵌套 M+m·N≈1.4h、块嵌套 M+⌈M/(B-2)⌉·N、索引嵌套 M+m·C、sort-merge M+N+sort≈0.75s、**hash join 3(M+N)≈0.45s**;实测 B=100 下块嵌套(6500)优于 SMJ(7500)。②**哈希连接**:build 小表 + probe 大表,只需外表(或其分区)进内存;内表可无限大。③**GRACE 分区哈希连接**:两表同一 h1 分区 → 跨分区必不匹配 → 逐对分区内存 join;分区 2(M+N)+probe (M+N)=总 3(M+N);单分区仍超内存换 h2 递归分区;单键重复超多用块嵌套兜底;hybrid=热分区留内存(讲义:难正确实现);N 页表约需 √N 缓冲(加 fudge factor f>1)。④**sort-merge**:外部归并排序成本 2P(1+⌈log_{B-1}⌈P/B⌉⌉);双指针 + **重复键回溯**(内表指针回重复段起点枚举全部组合,漏回溯=漏配对);讲义选型结论:hash 几乎总快,但 skew/已序/结果需排序时 SMJ 优。⑤TUM 现代硬件视角:build 易并行(各自 chunk 写共享表,锁竞争低)、probe 多读者零同步。5 种算法结果一致性 + 成本模型 + GRACE 分区性质 + probe 计数全断言 | Python(143 行) + Go(96 行) | 2026-09-16 |
| 245 | `07-数据存储/01-关系型/MaterializedView/` | 物化视图与增量刷新:①**与视图/表的差异(PG 官方)**:查询时直接读持久化数据、不能直接 DML、**可建索引**;定义(查询)像视图一样存进系统目录,解析器眼中就是 relation;pg_class.relispopulated 标记填充状态。②**官方性能示例**:file_fdw 外表查询 188ms → 物化视图 index-only scan 0.117ms(1600×);fdw 无索引而物化视图可建 gist/trgm(1431ms→198ms)。③**全量 REFRESH**:重算+整体替换,期间独占锁**阻塞所有 SELECT**;④**CONCURRENTLY**:前置条件是**物化视图上的唯一索引**(缺则直接报错,断言复现);流程=临时版本重算 → 按唯一键 diff(删/插/改)→ 原子切换 → 读全程不阻塞;代价是重算+diff。⑤刷新失败会让 relispopulated=false,查询直接报错;REFRESH 后统计信息不自动更新,老 ANALYZE 会误导优化器。Python 5 断言全绿(创建即物化/全量阻塞/无唯一索引报错/diff 三态/再刷新) | Python(127 行) + Go(79 行) | 2026-09-16 |
| 246 | `07-数据存储/01-关系型/OnlineDDL/` | 在线 DDL 与 gh-ost:①**三算法矩阵(MySQL 8.0 手册)**:INSTANT 只改数据字典(秒级;加列 8.0.12+ 默认、8.0.29+ 任意位置)、INPLACE 引擎内就地(可重建可不重建,多数允许 LOCK=NONE)、COPY 服务器层临时表全量复制(共享锁只读);选型 INSTANT→INPLACE→COPY;显式 ALGORITHM 不被支持时**直接报错**不静默降级。②**INPLACE 优于 COPY 三官方理由**(即使同样重建表):无服务器层临时表→无额外 undo/redo、二级索引条目预排序、不用 change buffer。③**MDL 独占只出现在 prepare 开始与 commit 收尾两端**;执行期并发 DML 记入 **row log**、commit 阶段应用;长事务会卡住 commit 独占 → 后续查询全体排队(MDL 雪崩)。④**INSTANT 行版本**:每次瞬时加/删列 +1,**上限 64**(ERROR 4092),重建型 ALTER/OPTIMIZE 清零。⑤**gh-ost**:ghost 表 + **订阅 binlog 流**(区别于 pt-osc 触发器)异步回放 copy 期间的变更 → 原子 cut-over 换名;可真暂停/在从库先行验证(--test-on-replica)/throttle 让主库完全回原生负载;依赖 ROW 格式(SBR 不可用)。Python 5 断言全绿(INSTANT 元数据+版本上限/row log 应用/COPY 重命名/支持矩阵优先级/gh-ost 三步) | Python(151 行) + Go(106 行) | 2026-09-16 |
| 247 | `08-安全/01-密码学/哈希/SHA-3-Keccak/` | SHA-3/Keccak-f[1600] 海绵函数(FIPS 202:状态 5×5×64bit=1600,24 轮 θ 列奇偶 D[x]=C[x-1]⊕rot1(C[x+1]) / ρ+π 走位 B[y,2x+3y]=rot(A[x,y],r[x,y]) / χ 唯一非线性 A=B⊕(¬B[x+1]∧B[x+2]) / ι LFSR 轮常数破对称;pad10*1+域分离后缀 SHA3=0x06 SHAKE=0x1F,末块剩 1 字节时 0x06|0x80=0x86 合并;rate/capacity 分档 SHA3-256=136/512 SHAKE128=168/256;5 demo:NIST 官方向量空串+abc / SHAKE128/256 / 跨块 200-1000B hashlib 对照 / 雪崩 132/256 位 / padding 块边界;**坑:RC 常量表抄录错位靠官方 LFSR 生成式重推导捕获**) | C(185 行) + Python(165 行 含 hashlib 对照全 PASS) + Go(218 行 RC 同 LFSR 自产) | 2026-09-16 |
| 248 | `08-安全/01-密码学/密钥派生/HKDF/` | HKDF Extract-and-Expand(RFC 5869:Extract PRK=HMAC(salt,IKM) 浓缩不均匀熵,salt 为 HMAC key、IKM 为消息;Expand T(i)=HMAC(PRK,T(i-1)|info|i) 计数器单字节,OKM 前 L 字节 L≤255*HashLen;缺省 salt=HashLen 个 0x00 非"空";info 域分离实现多密钥隔离,TLS 1.3 用它搭秘密树;5 demo:A.1 基本向量 / A.2 长 IKM+salt L=82 跨 3 块 / A.3 空 salt+info / A.7 缺省 salt==零字节(SHA-1) / info 域分离+IKM 雪崩+L 边界;**坑:Extract 参数顺序(salt 作 key)最易写反**) | C(203 行 自含 SHA-256+HMAC) + Python(118 行 RFC A.1-A.3/A.7 全 PASS) + Go(127 行) | 2026-09-16 |
| 249 | `08-安全/01-密码学/签名/ECDSA-P256/` | ECDSA + RFC 6979 确定性签名(P-256 参数 SEC2 v2 §2.4.2,seed 可验证随机;仿射点加 λ=(y2-y1)/(x2-x1) 倍点 λ=(3x²+a)/(2y),double-and-add;签名 s=k⁻¹(e+r·x) mod n 验证 u1·G+u2·Q;RFC 6979 HMAC-DRBG:bx=int2octets(x)‖bits2octets(h1 mod n),V=0x01×32 K=0x00×32 两轮 K/V 更新,T=HMAC(K,V) 逐候选,k 与 n 比较而非取模防偏置;5 demo:曲线阶自检 n·G=∞ / A.2.5 sample+test 的 k/r/s 逐字段 / 验证+篡改拒绝 / 确定性+私钥雪崩 / 20 组 round-trip;**坑:bits2octets=h1 mod n 非 h1 原值;仿射点加 r 与 p1 别名先写后读**) | C(ecdsa.c 209 行+bn256.c 206 行 手写 4×64 limb 大数+移位-比较-相减模归约+二元扩展欧几里得求逆,向量运行时 hex 解析) + Python(150 行 A.2.5 全 PASS) + Go(270 行 math/big) | 2026-09-16 |
| 250 | `08-安全/01-密码学/消息认证/HOTP-TOTP/` | HOTP/TOTP 一次性口令(RFC 4226:HOTP(K,C)=Truncate(HMAC-SHA-1(K,C)) mod 10^Digit,动态截断 offset=HS[19]&0xf 取 4 字节首字节&0x7f 成 31bit,位置由数据自选偏置更小;RFC 6238:T=floor((unix-T0)/X) X=30s 代计数器,SHA256/512 密钥 32/64B;验证端 ±1 步 resync 窗口+限速防在线穷举;5 demo:附录 D count 0-9 的 HMAC+HOTP / 附录 B 6 时间点×3 算法 / §5.4 截断细节 offset=0xa→872921 / 步进边界+窗口 / 密钥雪崩+失败锁定;**坑:计数器 8 字节大端,SHA512 组密钥是 64 字节非 20**) | C(239 行 自含 SHA-1+HMAC) + Python(110 行 向量全 PASS) + Go(136 行) | 2026-09-16 |
| 251 | `08-安全/01-密码学/密码派生/PBKDF2/` | PBKDF2 口令密钥派生(RFC 8018 §5.2:F(P,S,c,i) U1=PRF(P,S‖INT(i)) Uj=PRF(P,U(j-1)) F=U1⊕…⊕Uc,INT(i) 大端 4 字节块号;XOR 而非串联使时空折中无法跳步;c 迭代放大猜口令成本,盐唯一随机防彩虹表;WPA2/加密 zip 底座,OWASP 2023 建议 SHA-256 600k 次;5 demo:RFC 6070 c=1/c=2 / c=4096+hashlib 对照 / dkLen=25 跨块 / NUL 字节口令盐陷阱 / 确定性+迭代敏感+盐隔离+多块拼接;**坑:与 HKDF 计数器位数易混(PBKDF2 4 字节 vs HKDF 1 字节),C 语言层 NUL 截断**) | C(163 行 自含 SHA-1) + Python(85 行 RFC 6070 全 6 组向量 PASS) + Go(88 行) | 2026-09-16 |
| 252 | `09-语言学习/Python/异常链与异常组/` | 异常链(PEP 3134)与异常组(PEP 654):__context__ 隐式链四条语义(线程异常上下文初始 None/抛出时挂 context/抛后置自身/退出 except 或 return,continue,yield,break 时重置 None);__cause__ 显式链 raise...from 且隐含 __suppress_context__=True(PEP 409/415,PEP 3134 原文仅列为开放问题);from None 保留 context 但抑制显示;回溯两种消息("direct cause" vs "During handling",遍历 cause 优先,最新异常显示在最后);ExceptionGroup 构造(两仅位置参数/BaseExceptionGroup 全 Exception 叶子自动升级/混入 Base 报 TypeError/可嵌套成树);subgroup 无匹配返回 None 非空组、split 平凡分割另一侧 None、空嵌套组剔除、条件命中内部节点整棵子树纳入、derive 不重写丢子类类型且 __cause__/__context__/__traceback__ 按引用共享;except* 对不断缩小的 unhandled 组调 split,每子句至多执行一次拿到保留嵌套结构的子组,未匹配叶子自动 reraise,裸异常捕获时包装为 ExceptionGroup('',[原异常]),顺序敏感(子类检查与 except 相同),子句内禁 continue/break/return;**坑:except* ExceptionGroup 是运行期 TypeError("Use except instead")非 SyntaxError(实测仅真有异常要匹配时触发);自定义组重写 __new__ 后仍需 __init__ 吞参否则 "takes no keyword arguments"**) | Python(287 行;12 项断言全实跑通过) | 2026-09-16 |
| 253 | `09-语言学习/Python/typing与Protocol/` | typing 运行时行为(PEP 544 + PEP 695):Protocol 结构化子类型三原则(实现全部成员即子类型/protocol 间同规则/可变属性强制不变——x:float 协变接受 x:int 会被 arg.x=0.42 写坏);@runtime_checkable 仅做成员存在性检查(实测签名完全错误的 __len__(self,extra) 仍通过 isinstance),未装饰则 isinstance/issubclass 直接 TypeError;issubclass 仅 non-data protocol(含 x:int 的 data protocol 报 "non-method members don't support");protocol 默认实现仅显式继承可用(隐式结构子类型不在 MRO、super() 不可达);PEP 695:type 软关键字语句生成 TypeAliasType(__name__/__value__/__type_params__)递归别名免引号(惰性求值存代码对象首次访问才求值),class C[T: bound] 的 Generic 隐式入 MRO 与 __orig_bases__(实测 (Generic[T],)),def f[T](x:T) 的 __type_params__,新式参数 __infer_variance__ 恒 True 旧式默认 False;类型参数覆盖层作用域:对基类列表/注解可见、对参数默认值不可见(实测 NameError)、不进 globals/类命名空间;**坑:data protocol 的 issubclass 报错文本是 "non-method members" 非想当然的 "only non-data";新旧语法同一声明混用属静态检查器错误,运行期不拦**) | Python(223 行;10 项断言全实跑通过) | 2026-09-16 |
| 254 | `09-语言学习/Python/slots与内存布局/` | __slots__ 与对象内存布局(datamodel §3.3.2.4 原文精读):类级为每个名字建描述符+实例定长槽位,阻止 __dict__/__weakref__ 自动创建(实测 2 属性普通实例 ≈344B vs slots 48B,100 万实例 344MB→48MB);未列出属性赋值 AttributeError,加 '__dict__' 可恢复动态;父类无 slots 则子类实例 __dict__ 穿透回来,子类不声明 slots 也重新拿到 __dict__(子类只写新增名);'__weakref__' 需显式声明否则 weakref.ref 报 TypeError;类属性默认值会覆盖 slot 描述符导致 slot 失效(无 __dict__ 可存,赋值报错)→ 默认值放 __init__;多继承只允许一个非空 slot 布局否则 "multiple bases have instance lay-out conflict";同名 slot 子类遮蔽基类(基类变量不可访问);变长内建类型(int/bytes/tuple)+非空 slots 报 TypeError;__class__ 赋值仅相同 slots 允许;迭代器赋 __slots__ 描述符照建但自身变空迭代器(实测 list()==[]),dict 赋值键即 slot 名值作 docstring;dataclass(slots=True) 返回新类、查字段用 fields() 勿读 __slots__、frozen 走 object.__setattr__ 绕过、FrozenInstanceError 是 AttributeError 子类;**坑:slots 不预置默认值,未赋值直接读就是 AttributeError;写 "assert s2.a==0 or True" 这类废话断言仍会触发求值炸掉**) | Python(288 行;10 项断言全实跑通过) | 2026-09-16 |
| 255 | `09-语言学习/Python/字符串与编码/` | PEP 393 柔性字符串表示与编码:按 maxchar 动态选 1/2/4 字节三档 kind(ASCII/latin-1=1B、BMP=2B、非 BMP=4B),一个字符拉满整串("a"*999+"😀" 全串 4B);compact 布局数据内联紧跟对象头(实测 ASCII 每 +1 字符恰好 +1B、ASCII 头 42B vs latin-1 头 60B),纯 ASCII 时 data 与 utf8 指针同内存(仅 latin-1 不够);拼接按结果 maxchar 重算 kind 不降档;UTF-8 变长 1/2/3/4B(é=C3A9、中=E4B8AD、😀=F09F9880);latin-1 编不了 BMP 字符;孤立代理 strict 报 UnicodeEncodeError/surrogatepass 放行 ED A0 80;解码 strict 带 start/reason,replace→U+FFFD、ignore 丢弃;比较/len/索引按码点区域无关(Z<a、z<é,len("😀")==1,索引 O(1) 是等宽存储收益);驻留:标识符与编译期常量折叠同对象,非标识符动态串不自动驻留但 sys.intern 后共享;Django 实测 Unicode 存储 65378540B→2216807B(降 65%,35713/36000 个是 ASCII),代价是遍历基准慢 1%-30%;**坑:推字符宽要用 getsizeof 斜率差分(不同 kind 结构头不同,直接减空串基数会混入头差);"hello"+"!" 常量折叠成同一常量,验证驻留须经变量运行期构造;截断 UTF-8 序列报错位置是首字节位置 0 非坏字节位置(3.13 实测)**) | Python(153 行;7 项断言全实跑通过) | 2026-09-16 |
| 256 | `09-语言学习/Python/decimal与fractions/` | 数值精度模型:decimal 三元组(符号+系数+指数)Decimal((0,(3,1,4),-2))==3.14,尾零保留显著性(1.30+1.20=2.50 但 ==2.5,compare_total 按表示比较才区分),教科书式乘法 1.30*1.20=1.5600;构造看输入运算看上下文(Decimal("3.1415926535") 保留 11 位,prec=6 加法才舍入),默认 prec=28+ROUND_HALF_EVEN(2.5→2、3.5→4 银行家舍入),8 种舍入模式,localcontext 临时切精度一元+舍回;精度不足破坏结合律(Knuth 实测 prec=8 时 (u+v)+w=9.5111111 而 u+(v+w)=10,prec=20 恢复);Decimal(3.14) 无损暴露二进制真值 3.140000000000000124344978758017532527446746826171875→永远用字符串构造;NaN ==恒False、序比较触发 InvalidOperation(默认在 trap)、compare() 返 NaN;Decimal+float 算术 TypeError 但比较允许;% 符号随被除数、// 向零截断(int 相反)而 x==(x//y)*y+x%y 恒成立;Fraction 自动约分分母恒正(16,-10)→-8/5,字符串解析含 '7e-6'/'-3/7'/'1.414213';Fraction(1.1) 是 float 真值 2476979795053773/2251799813685248≠11/10,limit_denominator 恢复(3.1415926535897932→355/113、cos(pi/3)→1/2),Fraction(Decimal('1.1'))==11/10;混算语义与 Decimal 相反:F(1,3)+0.5 合法返回 float;round 平局取偶 F(7,2)→4、F(5,2)→2;**坑:上下文 ROUND_UP=远离零非四舍五入;连分数逼近误差~1/F(n)²,20 步仅 3.7e-9 到 1e-12 需 30 步;写 "nan==nan is False" 链式比较炸断言(开发期 3 处断言自身错全是实跑暴露)**) | Python(196 行;11 项断言全实跑通过) | 2026-09-16 |
| 257 | `10-逆向工程/01-二进制逆向/ARM64指令集与调用约定/` | A64 定长 32 位编码 + AAPCS64:组标识位 [28:24]/[28:23] 分派、sf/op/S/shift/Rm/imm6/Rn/Rd 位域逐字段还原、别名折叠(`mov`/`ret` 是伪指令)、SP 与 XZR 共用编号 31 靠位域区分、逻辑组 S 变体其实是 opc=11 而无独立 S 位、ORR Xd,XZR,Xm → MOV 的别名条件是 rn==31;覆盖分支(寄存器/立即/条件/比较)/ADR-ADRP/加减(立即数与移位寄存器)/逻辑移位寄存器/宽立即数搬移/载入存储(无符号偏移、imm9 变址、pair)/NOP;AAPCS64 寄存器角色表 x0-x7 传参、x8 间接结果、x9-x15 caller-saved、x16/x17 IP0-IP1(veneer 可破坏)、x18 PR、x19-x28 callee-saved、x29 FP、x30 LR;SP mod 16 = 0;帧记录 = 两个 64 位值(低地址存前一帧指针、高地址存 LR)且链以地址 0 终止;NZCV 不跨调用保留;C++ this 占 X0;结构体返回走 X8;序言形态分类 stp-frame-pair / stp-pair-only / str-lr-preindex / leaf;**坑:pair 的 idx 位 [24:23] 在 0xA9BF7BFD 上是 0b11(pre-index)而非 0b10(纯偏移),写错会让全部位域断言看着很绿而语义反了;str_lr 形态序列首条必须是序言那一条;`sx(0x1FFFF,19)` 是正数(19 位里最高位为 0),拿它断言 == -1 必挂;`decode_bytes` 末条是 `ret x30` 而非别名形式,断言要写 `alias or text`** | Python(397 行)+ C(235 行,无工具链走人工审查 + 结构校验);75 项断言实跑通过(prologue_shape 44 / aapcs64_check 31) | 2026-09-16 |
| 258 | `10-逆向工程/01-二进制逆向/Ghidra反编译流水线/` | Ghidra 官方反编译流水线:Main Work Flow 15 阶段逐条标注本 demo 覆盖情况;主简化循环六步(构造 SSA / 死代码消除 / 类型传播 / 项重写 / CFG 调整 / 控制流结构恢复);SLEIGH 汇编 → p-code(Op/Block/Func 三结构,PUT/GET/WrTmp/STEXit 与临时变量 tN);**Heritage**(支配树与支配边界、phi 放置、支配树 DFS 重命名、增量式 SSA 只重放 heritaged 区段);SIDE_EFFECT 集合决定死代码删除边界;INDIRECT 别名标注;INT_SUB 规范化为加补码、07 阶段再转回;**坑:最小 SSA 必须配合活跃性检查,否则会在 B1 插进无人使用的 phi(实测 4 个 vs 期望 2 个);phi 的入边是「边上的使用」,校验时要按「定义支配对应前驱」判定而不是「支配本块」,否则 verify_ssa 全体误报;Op.__slots__ 漏了 src_name 会在 rename() 抛 AttributeError;断言取 ops[-1] 是 CBRANCH,INT_SUB 在 ops[-2];fmt() 不认 ("neg", v) 标记则断言读不出意图;Python 读写一律 newline="" 否则写入时引入 CRLF** | Python(440 行)+ Go(519 行,无工具链走人工审查 + 结构校验);21 项断言实跑通过 | 2026-09-16 |
| 259 | `10-逆向工程/01-二进制逆向/Ghidra-headless自动化/` | analyzeHeadless 自动化:命令行骨架与组合约束(-import 与 -process 互斥、脚本名必须带扩展名、-max-cpu 负数归一为 1、含路径分隔符的通配符必须加引号);**HeadlessContinuationOption 4×4 官方合并表**逐格抄录 + 用规则而非查表实现 merge();preScript / postScript 对照;两种模式(-import / -process)的处置差异(-deleteProject 只在 import 模式有意义、-readOnly 与 delete 语义冲突);`-okToDelete` 安全门;`getScriptArgs()` 与 askXxx 的消费顺序(参数被 ask 先吃掉,而 ask 取完会让后续 ask 抛 IndexOutOfBoundsException —— 这就是「GUI 能跑、headless 炸掉」的常见原因);.properties 的 key 格式;HeadlessScript 与 GhidraScript 的差异;**坑:把脚本参数当作脚本名去校验(报「脚本名必须带扩展名: arg1」),修法是 _pairs() 支持 (name, [args]) 元组形式且只对脚本名校验;元组参数被切分错(("S.java", ["a","b"]) 被展开成 ["a","b"]);max-cpu 断言里不该出现 -2** | Python(408 行)+ Go(268 行,无工具链走人工审查 + 结构校验);33 项断言实跑通过 | 2026-09-16 |
| 260 | `10-逆向工程/01-二进制逆向/二进制差分与函数匹配/` | BinDiff 原理复刻:官方 signature 三元组(基本块数/块间边数/调用数,极弱 —— 两档强指纹跑完后它会把「被删的函数」与「新增的函数」错配成 (1,0,0)↔(1,0,0),这正是 confidence 存在的理由);prime signature(助记符→唯一小素数全乘、顺序无关、抗立即数改动;product 口径 64 条指令即撑满 uint64,Go 版直接断言回溢到 0 而 sum 口径不溢出;manual 的 product 与源码走读的 sum 口径分歧如实标注);MD index(graph_util.h 精确公式:sqrt(w0)*in_deg(src)+sqrt(w1)*out_deg(src)+sqrt(w2)*in_deg(tgt)+sqrt(w3)*out_deg(tgt)+sqrt(w4)*level(src)+sqrt(w5)*level(tgt) 取倒数,kDefaultWeightsNode={2,3,5,7,0,0};顶点值 = 所有关联边之和且**求和前先排序**因浮点加法不可交换;**由默认权重直接推出:拓扑层系数为 0 ⇒ top-down 与 bottom-up 结果完全一致、relaxed 与默认权重等价** —— 把层系数调成 11/13 两个方向才分道扬镳);分阶段匹配(签名须在两侧都唯一、只在剩余集合上施加、已匹配数单调不减)+ drill down(worker 与 twin 指纹全同、两侧都歧义,靠「已匹配函数的唯一 callee」解开,无调用关系可依托的 twin 仍留在未匹配集);confidence(官方原文:均值经 sigmoid 压扁,断言两条行为性质)+ similarity(函数 25/15/10/**50**、二进制 35/25/10/10/**20**,均再乘 confidence,且只统计非库函数);13 个官方函数匹配算法清单按 match quality 降序、edge matching「更强但可能超慢 → 性能问题先关它」、address sequence 的两条额外约束;**坑:把块字典当邻接表传给 md_of_functions,`for m in graph.get(v, [])` 退化成遍历字典键把块名当边,孤立函数 MD index 不再是 0(断言当场失败);匹配元组是 (B 函数名, 算法, 置信度),下标写反一次挂 4 条断言;指纹算法必须能「弃权」(不引用字符串的函数返回 None 并在流水线里过滤),否则空集合彼此相等会互相配上 —— 官方 string references 原文即「至少引用 1 个才参与」** | Python(664 行)+ Go(587 行,无工具链走人工审查 + 结构校验);44 项断言实跑通过(流水线 33 / 指标 11) | 2026-09-16 |
| 261 | `10-逆向工程/01-二进制逆向/Ghidra脚本化批量重命名/` | Ghidra 脚本化批量改名:类层次 FlatProgramAPI ← GhidraScript ← YourScript 与脚本元数据注释(//@category 等);FlatProgramAPI 类注释「NO METHODS SHOULD EVER BE REMOVED ... Changing this class will break user scripts」正是 createSymbol 自 7.4 起被弃用却仍保留的原因;createLabel 三重载(含 namespace 版)、makePrimary、**makeUnique 口径(重名时把地址拼到名字后)**;`SourceType` 优先级链 **USER_DEFINED > IMPORTED > ANALYSIS > DEFAULT**(官方 isHigherPriorityThan 原文)与「Symbol source determines priority - user symbols won't be overwritten by analysis」;命名规则(首字符须字母/下划线、无空格、区分大小写、同命名空间内唯一)与自动生成名前缀 FUN_/DAT_/LAB_/SUB_;官方 BatchRename.java 的模式化改名流程(Select → Apply Pattern → **Preview changes** → Apply)抽象成**计划/预演/事务三段式**:build_plan 是纯函数(只挑自动名、模板 re.sub + {addr} 占位、名没变不进计划 ⇒ 幂等)、resolve_conflicts 按 makeUnique 拼地址、dry-run 逐字段不变、apply 在单个事务里执行且任一条非法名整批回滚;**坑:askXxx() 会先从 getScriptArgs() 取值,非交互脚本必须按固定顺序解析;默认名前缀判断区分大小写 ⇒ 小写 sub_ 被视为用户命名,首次批量改名后同规则拿不到候选(需显式关掉 only_default);Go 的 ReplaceAllString 用 $1 且引用不存在的捕获组会静默展开成空串(产出 impl_ 这种丢一半信息的名字),Python re.sub 用 \1 且直接抛 invalid group reference;{addr} 占位符必须在正则替换之后再做,否则地址里的十六进制字符会被正则二次匹配;改名要连带更新 Function 对象,否则 getFunctionAt(addr).getName() 还是旧名;同地址创建同名 label 官方是幂等的,跨地址重名才要处理** | Python(572 行)+ Go(537 行,无工具链走人工审查 + 结构校验);44 项断言实跑通过(符号表 20 / 批量 24) | 2026-09-16 |
