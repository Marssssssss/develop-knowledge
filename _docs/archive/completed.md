| 011 | `01-游戏开发/08-动画/IK算法/FABRIK/` | FABRIK 启发式 IK(位置空间反向/正向两阶段 + 不可达目标退化) | C / Python / Go | 2026-09-11 |
| 012 | `02-Web开发/01-前端框架/Vue/reactive/` | Vue 3 Proxy 响应式最小实现(reactive/ref/effect/track/trigger + lazy 嵌套代理 + cleanup + effect 栈) | TypeScript / JavaScript | 2026-09-11 |
| 013 | `02-Web开发/02-后端/Node.js/Event-Loop/` | Node.js Event Loop(6 阶段 + nextTick/Promise 微任务 + libuv 1.45.0 行为变化 + I/O 中 setImmediate 必早于 setTimeout) | JavaScript / TypeScript | 2026-09-11 |
| 014 | `02-Web开发/03-数据库/B+树索引/` | B+ 树索引(M-way + 叶子兄弟链 + copy-up/push-up 分裂 + borrow/merge 重平衡 + bulk-load O(N)) | C / Python / Go | 2026-09-11 |
| 015 | `02-Web开发/04-API设计/WebSocket/握手协议/` | WebSocket 握手协议 RFC 6455 §4(HTTP Upgrade + SHA-1+GUID → Sec-WebSocket-Accept + 101 Switching Protocols + 子协议协商) | C / Python / Go | 2026-09-11 |
| 016 | `03-系统编程/01-网络编程/Socket基础/Nagle算法/` | Nagle 算法 vs `TCP_NODELAY`(RFC 896"inhibit sending when unacknowledged data exists"+Linux tcp(7) man page `tcpi_segs_out` 段计数实测;100 次 1 字节 send 在 Nagle on → ~8 段 vs `TCP_NODELAY=1` → 100 段) | C / Python / Go | 2026-09-11 |
| 021 | `03-系统编程/04-文件系统/mmap内存映射/` | mmap(2) 文件/匿名内存映射(MAP_SHARED 共享 page cache + msync 持久化;MAP_PRIVATE 写时复制 CoW 不影响源文件;MAP_ANONYMOUS|MAP_SHARED 父子进程 IPC;ftruncate 后访问越界 → SIGBUS 边界;offset 必须页对齐、length 页向上取整;fd 可立即 close 不影响映射) | C / Python / Go | 2026-09-11 |
| 022 | `03-系统编程/04-文件系统/ext4-Journaling/` | ext4 Journaling / JBD2(预写日志:journal_header_t 12 字节 magic=0xC03B3998 + blocktype + sequence;5 类块 descriptor/commit/jsb v1+2/revoke;descriptor 含 blocknr 标签描述 data block 最终落盘位置;recovery 重放已 commit 事务丢弃 incomplete;JBD2 大端 vs ext4 小端;JBD2_FLAG_ESCAPE 处理 data 前 4B 与 magic 碰撞) | C / Python / Go | 2026-09-11 |
| 023 | `03-系统编程/04-文件系统/Page-Cache/` | Linux page cache + writeback(`posix_fadvise` 6 advice: NORMAL/RANDOM/SEQUENTIAL/WILLNEED/DONTNEED/NOREUSE;`sync_file_range` 精细 writeback 3 flag 组合;dirty page 触发条件:`vm.dirty_background_ratio` 10% / `dirty_ratio` 20% / `dirty_expire_centisecs` 30s / 显式 fsync / umount;/proc/meminfo Cached/Dirty 状态观察;Linux-only demo,Windows 直接 exit 提示) | C / Python / Go | 2026-09-11 |
| 024 | `04-移动开发/01-iOS/内存管理/ARC/` | Swift / Objective-C ARC 内存管理(strong retain 默认 / weak 可选自动置 nil / unowned 非可选不自动置 nil / Person-Apartment + Customer-CreditCard + HTMLElement 闭包 3 例;引用计数仅适用类实例,struct/enum 不归 ARC 管;Objective-C 对应 `__weak` / `__unsafe_unretained` / Block `__weak typeof(self)` 捕获列表;CFGetRetainCount 验证 retain/release 等价行为) | Swift / Objective-C | 2026-09-11 |
| 025 | `04-移动开发/01-iOS/并发编程/GCD同步原语/` | GCD DispatchQueue / DispatchSemaphore / DispatchGroup / DispatchWorkItemFlags.barrier(主队列 vs 全局并发 vs 自定义;QoS 5 等级;.barrier 在并发队列独占但全局并发队列无效;dispatch_semaphore 限并发上限 3 时 10×200ms 任务实测 ≈ 0.67s;DispatchGroup enter/leave 配对 + notify;main.sync 死锁警告) | Swift / Objective-C | 2026-09-11 |
| 026 | `04-移动开发/01-iOS/事件循环/RunLoop/` | RunLoop modes / sources / timers / observers(每线程一实例 lazy 创建;CFRunLoopObserver 监听 kCFRunLoopActivity 6 个位 entry/beforeTimers/beforeSources/beforeWaiting/afterWaiting/exit;5 个标准 mode default/connection/modal/eventTracking/commonModes;Timer scheduledTimer 必须 invalidate 否则 RunLoop↔Timer↔Closure↔Self 循环;ScrollView 滚动切到 eventTracking mode 使 default mode 的 timer 暂停 → 注册到 commonModes 解决) | Swift / Objective-C | 2026-09-11 |
| 027 | `04-移动开发/02-Android/并发编程/Handler消息机制/` | Android Handler / Looper / MessageQueue 异步消息机制(Handler(Looper) post/sendMessage/postDelayed + HandlerThread 自带 Looper 的工作线程 + nativePollOnce epoll 阻塞唤醒 + Message.sPool 对象池复用 + onNewIntent 与 setIntent 配合 + 5 个 demo:post Runnable 跨线程 / sendMessage 四元组 / HandlerThread 串行 / postDelayed 定时 / 内存泄漏静态内部类 + WeakReference 修复) | Kotlin / Java | 2026-09-11 |
| 028 | `04-移动开发/02-Android/组件生命周期/Activity启动模式/` | Android Activity launchMode 5 种(standard 默认创建新实例 + singleTop 栈顶复用 onNewIntent + singleTask 同 affinity 唯一并清空上方 + singleInstance 全设备独占任务 + singleInstancePerTask API 31+ 文档型)+ Intent flags 运行时优先级(NEW_TASK / SINGLE_TOP / CLEAR_TOP)+ onNewIntent 必须 setIntent(intent) 更新 getIntent() + taskAffinity 自定义任务分组) | Kotlin / Java | 2026-09-11 |
| 029 | `04-移动开发/02-Android/UI框架/Compose重组/` | Jetpack Compose 重组机制(mutableStateOf 自动重组 + by 委托 setValue + remember(key1, key2) 缓存昂贵计算 + derivedStateOf 仅在结果变化时触发下游重组避免滚动每帧重组 + Modifier.offset{} / drawBehind{} lambda 版本跳过 Composition 阶段直接 Layout/Draw + Backwards write 反模式无限重组循环 + Snapshot 系统 MVCC 多版本快照追踪 + Slot Table 增量更新) | Kotlin | 2026-09-11 |
| 030 | `04-移动开发/03-跨平台/React-Native/Bridge-vs-JSI/` | React Native New Architecture(Bridge 异步 JSON 队列 → JSI 同步 C++ 接口直接函数调用;Fabric C++ 渲染管线 + React 18 并发渲染 + 跨平台共享 C++ 渲染核心;TurboModules 懒加载 on first access + Codegen 编译期 TS spec → C++/Java/ObjC 胶水类型校验;2 个 JS 文件 + 2 个 TS 文件纯 Node.js 模拟 legacy bridge async/JSON/queue/JIT 全特性 + JSI HostObject 同步直接调用 + lazy module 实例化 + 新旧架构 5 维度对比表) | TypeScript / JavaScript | 2026-09-11 |
| 031 | `04-移动开发/03-跨平台/Flutter/三棵树/` | Flutter 三棵树(Widget 不可变配置声明 + 跨 frame 比对复用 + build() 返回新实例;Element 跨帧持久实例 create/mount/update/activate/deactivate/unmount 6 阶段生命周期 + ComponentElement vs RenderObjectElement 两类;RenderObject layout/paint 可变 + performLayout() 接收父 BoxConstraints + 5 类 RenderBox/FixedBox/ExpandedBox/Container/Row 模拟;Box Constraint 模型自顶向下约束 + 自底向上 size O(n) DFS + tight/loose/bounded 三类约束传播;build→layout→paint→composite 4 阶段渲染管线 vsync 信号触发 + Impeller 默认渲染器 + Skia WASM web fallback;5 个独立 Dart demo 覆盖三棵树 + 约束模型 + 完整管线) | Dart | 2026-09-11 |
| 032 | `04-移动开发/03-跨平台/Kotlin-Multiplatform/expect-actual/` | KMP expect/actual 声明合并机制(expect 函数 randomUUID + 属性 platformTag + 类 AtomicRef typealias + 枚举 LogLevel + 函数 writeLogMessage 5 种声明类型全部覆盖;commonMain 仅声明无实现、androidMain 走 java.util.UUID + AtomicReference typealias + android.util.Log、iosMain 走 platform.Foundation.NSUUID + synchronized AtomicRef + NSLog;commonMain/iosMain intermediate source set 层级 + iosArm64Main/iosX64Main/iosSimulatorArm64Main 平台 source set 共享 iosMain;Main.kt 不感知平台差异直接调用 expect 声明) | Kotlin | 2026-09-11 |
| 033 | `05-AI与机器学习/01-深度学习/反向传播/` | 反向传播链式法则(d2l.ai §5.3 公式 5.3.1~14:1-hidden-layer MLP 前向 z=W¹x, h=φ(z), o=W²h, L=ℓ(o,y), s=λ/2·(‖W¹‖²+‖W²‖²), J=L+s;反向 ∂J/∂W¹=(∂J/∂z)·xᵀ+λW¹, ∂J/∂W²=(∂J/∂o)·hᵀ+λW², ∂J/∂z=(W²ᵀ·∂J/∂o)⊙φ'(z);中间变量必须缓存以供反向使用;中心差分数值梯度 max err < 1e-7;mini-batch SGD 200 步 acc 0.25→1.00 验证学习能力) | Python | 2026-09-11 |
| 034 | `05-AI与机器学习/01-深度学习/卷积/` | 2D 卷积 im2col + col2im 完整 forward/backward(arXiv 2408.12561 §Preliminaries Eq 3/4/5:X (N,C,H,W) → im2col → cols (C·kh·kw, N·Hout·Wout),W 展平为 W_col (Cin·kh·kw, Cout),Y_col = W_col·cols = GEMM;反向 dW = cols·dY_colᵀ, db = ΣdY, dX = W_col·dY_col 后 col2im 累加回输入;零拷贝视图用 np.lib.stride_tricks;col2im 必须 += 而非 = 因同一输入像素被多个输出窗口共享;中心差分 dW max err ≈ 1e-12) | Python | 2026-09-11 |
| 035 | `05-AI与机器学习/01-深度学习/注意力/` | Scaled Dot-Product + Multi-Head Self-Attention(Vaswani et al. 2017 *Attention Is All You Need* §3.2 公式 1: Attention(Q,K,V)=softmax(QKᵀ/√d_k)V;公式 2: MultiHead=Concat(head₁..head_h)·W^O;公式 3: head_i=Attention(Q·W_i^Q, K·W_i^K, V·W_i^V);缩放因子 √d_k 抵消点积方差 d_k 增长防止 softmax 饱和;h=8, d_k=d_v=d_model/h=64 基础模型;causal mask 把上三角设 -∞ 保证 decoder 位置 i 只见 0..i;三种使用方式 encoder-decoder/encoder self/decoder self attention) | Python | 2026-09-11 |
| 036 | `05-AI与机器学习/02-强化学习/Q-Learning/` | Q-Learning 表格方法(Watkins & Dayan 1992;off-policy Bellman 更新 Q(s,a)←Q(s,a)+α[r+γ max_a' Q(s',a')-Q(s,a)];epsilon-greedy 探索 π_e=(1-ε) argmax Q + ε uniform;自纠正性:高估 Q 值被探索后自动下调;γ=0 时无法传播目标奖励;4x4 GridWorld 1000 ep 成功率 89%;5 demo: basic 收敛/epsilon 对比/gamma 对比/自纠正性/学习曲线) | Python | 2026-09-12 |
| 037 | `05-AI与机器学习/02-强化学习/REINFORCE/` | REINFORCE 策略梯度(Williams 1992;策略梯度定理 ∇J=E[Σ∇log π(a_t|s_t)·A(s_t,a_t)];对数导数技巧将期望梯度转化为对数概率梯度;softmax 线性策略 ∂log π(a|s)/∂θ[s][a']=I(a'=a)-π(a'|s);rewards-to-go G_t=Σγ^{k-t}r_k 因果性只用 t 以后奖励;baseline V(s) 不改变期望但降低方差 E[∇log π·G]=E[∇log π·(G-b(s))];5 demo: 无 baseline/有 baseline/方差对比/折扣因子影响/学习曲线) | Python | 2026-09-12 |
| 038 | `05-AI与机器学习/02-强化学习/PPO/` | PPO 近端策略优化(Schulman et al. 2017 arXiv:1707.06347;裁剪替代目标 L^clip=E[min(r_t·A_t, clip(r_t,1-ε,1+ε)·A_t)] 取悲观下界;策略比率 r_t=π_θ/π_{θ_old};多 epoch 复用同一 batch vs REINFORCE 只用一次;GAE 优势 Â_t=Σ(γλ)^l δ_{t+l} 平衡偏差与方差;价值函数 MSE L^critic=E[||V_ϕ(s_t)-R̂_t||²];ε=0.2 默认值;5 demo: basic 训练/clip 参数对比/策略比率分布/优势估计/价值损失追踪) | Python | 2026-09-12 |
| 039 | `05-AI与机器学习/03-LLM/BPE分词/` | BPE 分词器(Gage 1994 压缩算法 → Sennrich et al. ACL 2016 arXiv:1508.07909 引入 NMT 子词分段;训练 = 词频表上迭代合并最高频相邻符号对,`</w>` 词尾标记区分词内/词边界;编码 = 按 merge 规则学习顺序 rank 贪心应用而非按新文本频率,保证切分唯一确定;经典语料 low/lower/newest/widest 第一轮 (e,s) 9 次 → es;未见词 lowest → low + est</w> 泛化;byte-level BPE(GPT-2)以 256 字节为基础词表理论 OOV 为零;训练朴素 O(N·V) 优先队列 O(N log V);3 demo: 经典训练/未见词编码/词表-序列长度权衡) | Python | 2026-09-12 |
| 040 | `05-AI与机器学习/03-LLM/KV-Cache/` | KV Cache 与自回归解码 + PagedAttention(Kwon et al. SOSP 2023 arXiv:2309.06180 + vLLM 官方博客;历史 token K/V 不随新 token 变化 → 缓存后每步投影 O(n²d)→O(nd),QK 点积 O(n) 仍在省的是 K/V 重复投影;两版输出逐元素一致 1e-9 容差 + 乘加计数对比;显存公式 2×seq_len×layers×n_kv_heads×head_dim×dtype_bytes,LLaMA-13B(MHA 40×40×128) 2048 token ≈1.68GB 与官方 1.7GB 一致,Llama-3-8B GQA(32×8×128)同长度仅 0.27GB;PagedAttention = OS 虚拟内存分页:块=页/token=字节/序列=进程/块表=页表,逻辑连续物理不连续按需分配,浪费仅最后一块尾部 <4% vs 预分配 60-80%;并行采样 CoW 共享省 55%;3 demo: 一致性与计算量/显存账单/分页模拟) | Python | 2026-09-12 |
| 041 | `05-AI与机器学习/03-LLM/采样策略/` | 解码采样策略 temperature/top-k/top-p(HF 官方博客 How to generate + Inference for PROs + GenerationConfig 文档;自回归 P(w_1:T|W_0)=∏P(w_t|w_1:t-1,W_0);temperature p=softmax(z/T) T<1 更尖 T→0 退化为 greedy(HF 官方确认等价);top-k 固定 k 候选(常用 5-50)尖峰分布仍带长尾/平坦分布过早截断;top-p nucleus 按概率降序累积到质量 p 为止候选数动态,尖峰留 1-2 个平坦留全部;截断后必须重归一化;min_tokens_to_keep=1;softmax 减 max 防溢出;贪心 GPT-2 复读现象是采样策略存在理由;4 demo: 温度扫描与熵/top-k 行为/top-p 动态候选/20000 次采样频率近似概率) | Python | 2026-09-12 |
| 042 | `05-AI与机器学习/04-计算机视觉/Sobel边缘检测/` | Sobel 边缘检测(OpenCV 2.4 Sobel Derivatives 官方教程出自 Learning OpenCV;Gx=[[-1,0,1],[-2,0,2],[-1,0,1]] 检测垂直边缘/Gy 转置检测水平边缘;梯度幅值 G=sqrt(Gx²+Gy²) 工程近似 |Gx|+|Gy|;方向 θ=atan2(Gy,Gx) 垂直边缘走向;Sobel=高斯平滑+微分联合对噪声鲁棒;OpenCV filter2D 执行相关而非卷积,Gx 不对称两者差一符号;CV_8U 截断陷阱:负梯度直接存 uint8 全变 0 白→黑边缘消失,须 CV_16S/CV_64F→abs→uint8;ksize=3 精度不足时 Scharr [-3,0,3] 更准;4 demo: 垂直边仅 Gx 响应/水平边仅 Gy/方块四边+左右边缘 Gx 符号相反/CV_8U 截断陷阱) | Python | 2026-09-12 |
| 043 | `05-AI与机器学习/04-计算机视觉/Hough直线检测/` | Hough 变换直线检测(Hough 1962 专利 + Duda & Hart 1972 极坐标化;ρ=x·cosθ+y·sinθ 绕开 y=mx+b 垂线斜率无穷大;图像点↔参数空间正弦曲线对偶,共线点曲线交汇成峰;累加器行=ρ(1px,上限对角线长,须允许负 ρ)列=θ(1°,半平面 [0°,180°));threshold 语义=最少共线点数即最小直线长度;进 Hough 前必须 Canny/阈值二值化;量化误差使峰散落相邻格必须做参数空间 NMS;OpenCV 教程 100x100 水平线 (ρ=50,θ=90°) 完整算例;概率 Hough 随机采样降计算量 + minLineLength/maxLineGap;4 demo: 水平线 (ρ=6,θ=90°)/对角线 y=x → (ρ=0,θ=135°)/方块轮廓 4 峰/断线缺口 4 点仍检出) | Python | 2026-09-12 |
| 044 | `05-AI与机器学习/04-计算机视觉/IoU与NMS/` | IoU 与非极大值抑制 NMS(torchvision.ops 官方文档:nms "iteratively removes lower scoring boxes which have an IoU greater than iou_threshold with another (higher scoring) box",严格大于;框格式 (x1,y1,x2,y2);贪心:按分数降序保留最高分并抑制 IoU>thr 的剩余框;IoU=交/并,union=area_a+area_b−inter 容斥,union>0 防零面积除零;batched_nms offset 技巧:每类框加 idx×(max_coord+1) 偏移使异类框几何永不相交,一次全局 NMS 等价逐类;逐类 NMS 不能省否则低分类别被吃掉;平票 CPU/GPU 保留哪个不保证一致(文档明示);典型 thr 0.45-0.5 密集场景调 0.6;4 demo: torchvision 文档标准例 5 框保留[0,3]/阈值敏感性/逐类 NMS vs offset 技巧等价性/退化情形(相同框/内含框/零面积/平票)) | Python | 2026-09-12 |

---

> 写入规则:每 demo 完成后 append 一行(用现有 ID 续号)。监控:agent 巡查时若行数 > 100 → 触发 rotate(见 STATE.md §4)。
| 045 | `06-DevOps/01-容器化/Namespace隔离/` | Linux namespaces(man7 namespaces(7):8 类 ns 表 + unshare/clone/setns 三 API;unshare(2):CLONE_NEW* flags 表、EINVAL/EPERM 条件、uid_map"新ns起始ID 父ns起始ID 长度"三字段格式、setgroups=deny 前置、NEWUSER 授予新 ns 全套 capabilities 且同调用免 CAP_SYS_ADMIN;pid_namespaces(7):CLONE_NEWPID 只影响后续子进程、PID ns 单向不可回退、新 ns 须重挂 procfs;3 demo: UTS 父子主机名隔离/user ns 无特权全套 caps/PID ns 子进程 PID=1+新 procfs;坑:多线程进程 unshare(NEWUSER) EINVAL、挂 procfs 前须 MS_REC|MS_PRIVATE 断传播、Go 无 fork 用 exec 自我复制+SYS_SETHOSTNAME 直调) | C / Python / Go | 2026-09-12 |
| 046 | `06-DevOps/01-容器化/Cgroups-v2/` | cgroup v2(docs.kernel.org admin-guide/cgroup-v2:单一层级 mount -t cgroup2、subtree_control 写"+cpu +memory"为子代启用、cgroup.procs 写 PID 即迁移、/proc/pid/cgroup 格式"0::$PATH"、内部进程约束(非根组有进程不可启用 domain 控制器)、层级收紧;cpu 控制器:cpu.max"$MAX $PERIOD"带宽配额默认"max 100000"、cpu.weight 1-10000 默认 100、cpu.stat 的 usage_usec/nr_periods/nr_throttled/throttled_usec、内核 cfs_bandwidth 按周期发放配额耗尽进 throttled 队列;memory 控制器:current 含匿名页+page cache+内核结构+TCP 缓冲、min/low 保护、high 软限节流不杀、max 硬限先回收后 OOM kill、events 计数 oom_kill;3 demo: 建组自迁移观察 current/16MiB max 触发 OOM kill/50% 配额忙循环限流;坑:有进程的组 rmdir EBUSY 须先迁回 root、malloc 不触碰不记账、控制器写在父组 subtree_control) | C / Python / Go | 2026-09-12 |
| 047 | `06-DevOps/01-容器化/OCI镜像格式/` | OCI image-spec(specs.opencontainers.org:config.md 的 DiffID=sha256(未压缩 tar)≠blob digest=sha256(压缩后内容)、ChainID 递归 ChainID(L0)=DiffID(L0) 且 ChainID(前缀|Ln)=Digest(前缀+" "+DiffID(Ln)) 空格参与哈希、ImageID=SHA256(config JSON) 内容寻址、rootfs.diff_ids 自底向上;manifest.md:schemaVersion 必须为 2 兼容 Docker、layers[0]=base 栈序、descriptor{mediaType,digest,size}、层媒体类型 tar/+gzip/+zstd;layer.md:.wh.<name> 白出只删父层条目、.wh..wh..opq 隐藏父层目录全部子项、目标已存在语义=unlink+重建、白出标记应用后必须隐藏;demo 从零构建 3 层镜像(base/app 含 whiteout/cleanup 含 opaque)→写 image-layout(blobs/sha256+index.json+oci-layout)→重读校验 digest/DiffID/ChainID→按层序解包比对文件树;坑:DiffID 与 blob digest 混淆、ChainID 空格、gzip mtime 不定则 digest 不可复现、config JSON 序列化字节参与 ImageID) | Python / Go | 2026-09-12 |
| 048 | `06-DevOps/02-CI-CD/DAG流水线调度/` | CI 流水线 DAG 调度(GitLab docs needs 语义: stage barrier 全部完成才进下一 stage vs DAG 前置一完成立即启动、needs:[] 立即运行、扇出/扇入/菱形、optional 可选依赖;GitHub Actions docs: jobs 默认并行+needs 数组依赖、失败则依赖者跳过除非 if:always();Kahn 分层拓扑:入度计数+队列、出队数<节点数即环、O(V+E);关键路径 ef[j]=dur[j]+max(ef[need]);GitLab 博客 A1min/B5min/C只依赖A算例 barrier 8min vs DAG 4min;4 demo: 菱形分层+关键路径/环检测创建即失败/barrier vs DAG/失败传播 SKIPPED;坑:同一层输出顺序不唯一须排序、环是创建时错误非运行时、wall-clock 节省≠计算量节省) | C / Python / Go | 2026-09-12 |
| 049 | `06-DevOps/02-CI-CD/内容寻址缓存/` | CI 构建缓存(actions/cache 官方: key/restore-keys/path 三输入、key≤512 字符、精确命中 cache-hit=true、miss 时 job 成功后自动新建、缓存创建后不可修改只能新 key、restore-keys 有序前缀逐个尝试取最近创建的部分匹配、单仓库 5GB 上限按最近访问淘汰+一周未访问也淘汰、敏感数据禁入缓存因 PR 可读;caching-strategies.md: 锁文件哈希作 key 是最常见策略、OS 隔离 key、run_id/commit SHA 短生命周期缓存;demo 实现 sha256 键派生(hashFiles 语义,C 版手写 FIPS 180-4 SHA-256 已对照 hashlib 验证)+精确/部分/未命中三级查找+LRU+TTL 淘汰;坑: cache-hit 是字符串'true'非布尔、restore-keys 前缀从长到短、部分命中仍要执行构建只是打底) | C / Python / Go | 2026-09-12 |
| 050 | `06-DevOps/02-CI-CD/蓝绿与金丝雀发布/` | 部署策略(Argo Rollouts 官方: Recreate 先删旧再启新两版本从不共存但必然停机、RollingUpdate 逐实例替换无停机但共存、BlueGreen 双 RS 并存只切 activeService selector 回滚瞬时因旧 RS 未缩容、Canary setWeight 步进+AnalysisRun 指标门控自动晋升/回滚即渐进式交付;官方选型表: 蓝绿无需流量管理器/兼容队列 worker/爆炸半径大,金丝雀需流量管理器做细粒度/爆炸半径低、无管理器时权重粒度=副本粒度;4 demo 用统一 LCG 模拟 1000 请求: recreate 10% 停机窗口 vs rolling 0 错误、蓝绿坏版本检测后瞬时切回零额外错误、金丝雀 20% 错误率 10% 步即中止、好版本三步晋升;坑: 数据库 schema 须双向兼容否则切回失败、分析门控要时间窗口聚合防低流量扰动) | C / Python / Go | 2026-09-12 |
| 051 | `06-DevOps/03-监控与可观测性/Prometheus指标模型/` | Prometheus 数据模型与 exposition 文本格式 0.0.4(官方 exposition_formats: 行导向 \\n 分行、# HELP 文档串只转义 \\\\ 与 \\n、# TYPE 必须 counter/gauge/histogram/summary/untyped 且出现在首个样本之前否则 untyped、样本 EBNF name{labels} value [ts]、标签值仅 \\\\ \\" \\n 三种转义、value 按 Go ParseFloat 且 NaN/+Inf/-Inf 合法、timestamp int64 毫秒、同指标行须连续成组且 name+labels 唯一否则 undefined;histogram 展开约定 x_bucket{le} 累积桶+x_sum+x_count、必含 le="+Inf" 且值==_count、桶按上界升序;内容协商: text 0.0.4 为兜底、OpenMetrics 1.0 含 UNIT/INFO/# EOF、OM 2.0 CompositeValue 单行+st@ 起始时间戳;demo 解析官方完整示例并校验三条不变式+转义 round-trip+无 TYPE 默认 untyped+NaN/+Inf;坑: 毫秒(text) vs 秒(OpenMetrics)、从右往左切 value/ts 因标签值可含空格花括号) | C / Python / Go | 2026-09-12 |
| 052 | `06-DevOps/03-监控与可观测性/分布式追踪与TraceContext/` | W3C Trace Context(W3C REC trace-context-1: traceparent=version-trace_id-parent_id-trace_flags 四段 ABNF、trace_id 32 小写 hex 全链路不变、parent_id 16 hex 每跳换成本跳 span_id、trace_id/parent_id 全零非法 MUST ignore、version ff 禁止、version>00 布局匹配则按 v0 解释、flags 位域须掩码 (flags&0x01)==0x01 禁止 ==1 判等防 0x03 漏判、flags 是建议非强制;tracestate 厂商键值最多 32 项最左为 traceparent 书写方;OTel: Trace=Span 的 DAG、Span{name,trace_id,span_id,parent_id,start/end,attributes,status}、Context+Propagation 两子概念、TracerProvider/Tracer/Exporter 三层;demo 模拟 client→A→B→C 逐跳传播还原 Span 树+5 类非法头拒绝+位掩码+非法上游静默忽略另起新 trace;坑: 大写 hex 非法(HEXDIGLC)、非法≠报错而是静默重开、span 命名低基数 get_account 合理 get_account/42 反模式) | C / Python / Go | 2026-09-12 |
| 053 | `06-DevOps/03-监控与可观测性/直方图分位数估算/` | histogram_quantile 插值算法(官方 functions 页: φ<0→-Inf φ>1→+Inf φ=NaN→NaN、桶数<2 或缺 +Inf→NaN、桶内均匀分布假设线性插值 rank=φ×total、最高桶特例返回次高桶上界不外插、最低桶上界>0 时下界假设 0、非单调累积计数先忽略相对差<1e-12 浮点噪声再强制抬升并打 annotation;practices/histograms 220ms 尖峰算例: 桶{0.1,0.2,0.3,0.45} 全落 (0.2,0.3] p95=0.2+0.1×0.95=0.295 误差 75ms 且逼近 SLO 错觉、+100ms 后跳 (0.3,0.45] 估算不连续;native histogram factor 1.1 同场景误差仅 ≤19ms 指数桶+指数插值;summary 分位数 avg 统计无意义 BAD! 须 sum by (le) 后 quantile;坑: 桶边界按 SLO 密集布、rate() 先于聚合、histogram_quantile(0/1) 得估算 min/max) | C / Python / Go | 2026-09-12 |
| 054 | `06-DevOps/04-Kubernetes/Pod生命周期与重启策略/` | Pod 5 态 phase(官方 lifecycle 表格) + restartPolicy 三值 Always/OnFailure/Never + 决策表(退出码 0 → 仅 Always 重启;非 0 → Always 与 OnFailure 重启;Never 从不重启)+ 指数退避 10s→20s→40s→80s→160s→300s(上限)+ 10min 连续运行重置 + Sidecar 容器无视 Pod-level 强制 Always;4 demo: basic Pending→Running→Succeeded/三策略 6 例对比 + Always/OnFailure/Never × exit 0/1/连续崩溃 7 次累计退避 790s/Pod-level Never 但 sidecar 仍 Always | C / Python / Go | 2026-09-12 |
| 055 | `06-DevOps/04-Kubernetes/kube-proxy-IPVS负载均衡/` | kube-proxy 4 种模式(iptables/ipvs/nftables/kernelspace)+ IPVS 三步拓扑(kube-ipvs0 dummy interface + Service IP 绑定 + ipvsadm virtual server)+ NAT 模式唯一支持端口映射 + 11 调度算法 rr/wrr/lc/wlc/lblc/lblcr/sh/dh/sed/nq/mh(sed 公式 (C+1)/U + nq 优先空 server fallback sed + Maglev LUT 137 质数 × M 后端);4 demo: rr 100 req 三后端各 33-34 次/11 算法全跑同 src 演示 sh/dh 同 dst 命中/Maglev LUT 简化版/wrr 4:2:1 权重 57%/29%/14% + session affinity 180min sticky;ipvs 已弃用 v1.40 默认禁用 v1.43 完全移除(内核 IPVS API 与 K8s Service 边缘场景不匹配) | C / Python / Go | 2026-09-12 |
| 056 | `06-DevOps/04-Kubernetes/Controller-Reconciler模式/` | controller-runtime 缓存模型(官方博客 TL;DR: r.Get/r.List 走本地 Indexer map[ns/name] 不是 apiserver;cache miss fallback APIReader;r.Update 直连 apiserver 绕开 cache 防脑裂;watch 事件流 API Server→Reflector→DeltaFIFO→handler→workqueue key 去重→Reconcile;Reconcile 必须幂等 因最终一致;Result{RequeueAfter} 优于 time.Sleep)+ 乐观并发 resourceVersion 不匹配 409 Conflict;4 demo: 3 Deployment list+watch warm-up + 5 events dedup → 1 enqueue + 写后 stale read 演示 watch event 回流才一致 + 并发写 409 retry 重入队 | C / Python / Go | 2026-09-12 |
| 057 | `07-数据存储/01-关系型/MVCC/` | MVCC 多版本并发控制(InnoDB 9.7 文档:DB_TRX_ID 6B+DB_ROLL_PTR 7B+DB_ROW_ID 6B 三个隐藏字段;undo log 分 insert/update 两类;insert undo 仅 commit 时可丢弃,update undo 须等所有 ReadView 不再需要才可 purge;purge 线程清理死行;ReadView 4 条规则: trx_id == creator / trx_id < low_water / trx_id >= high_water / trx_id in active_ids;PostgreSQL xmin/xmax 对照;5 demo: 基本 snapshot 读 vs current 读/RR 复用 snapshot/RC 每 SELECT 新 snapshot/undo 链反向查找历史版本/双写冲突简化语义) | C / Python / Go | 2026-09-12 |
| 058 | `07-数据存储/01-关系型/WAL/` | WAL 预写日志 + ARIES 恢复(Derby Logging & Recovery:changes always take the system forward 即 rollback 也写 CLR redo-only;Repeating History 在 redo 阶段重放所有 Update+CLR 含未 commit tx;no force 配合 steal 让 commit 时不必落盘 data page;pageLSN 防无谓 redo;fuzzy checkpoint 三阶段 begin→end_checkpoint→master record 持久 LSN;LogRecord 字段 lsn/prev_lsn/trans_id/type/pid/offset/before/after/undo_next_lsn;CLR undoNextLSN 跳过被补偿记录;3 阶段恢复 Analysis→Redo→Undo 中 Analysis 重建 tx_table 与 dirty_page_table,redo 从 min(recLSN) 顺扫 pageLSN 判定跳过,undo 反向扫 loser tx lastLSN 写 CLR;5 demo: basic WAL force on commit/Steal+No-Force/fuzzy checkpoint/3 阶段模拟恢复 + loser undo/同 tx LSN 链) | C / Python / Go | 2026-09-12 |
| 059 | `07-数据存储/01-关系型/2PL/` | 两阶段锁 2PL + 死锁检测(CMU 15-445 L16:S/X-lock 兼容性矩阵,Growing 阶段只能 acquire 不允许 release;Shrinking 反之;Strict 2PL X-lock 持有到 commit 防 cascading abort;SS2PL=Rigorous 2PL 所有锁持有到 commit;CMU 层次 Universal ⊇ View Serializable ⊇ Conflict Serializable ⊇ SS2PL ⊇ Serial;waits-for graph DFS 找环选 youngest victim = max id;4 隔离级别异常矩阵 dirty/non-repeatable/phantom;FIFO 反饥饿 S 申请者需等前面所有 X;5 demo:X 互斥/S-S 共存 S-X 互斥/Strict 2PL phase 切换/死锁 T1↔T2 循环检测/youngest victim abort;Vlad Mihalcea 例: write skew 在 2PL 防住 但在 PG/MySQL MVCC 上需 SERIALIZABLE/SSI) | C / Python / Go | 2026-09-12 |
| 060 | `07-数据存储/02-NoSQL/Dynamo风格/` | Dynamo 风格 4 大机制(SOSP 2007 DeCandia:一致性哈希+256 vnode + preference_list=N顺时针不同节点;sloppy quorum N=3 R=W=2 默认;Vector Clock dominates/sibling 两套语义读时 reconcile;hinted handoff 转交下一个健康节点+hint 标记 intended_for,owner 恢复后投递;paper §4 Summary of techniques 表;Cornell 课件 cart add X/Y 冲突例子) | C / Python / Go | 2026-09-12 |
| 061 | `07-数据存储/02-NoSQL/Cassandra一致性/` | Cassandra 可调一致性 8 级(DataStax 官方分类表 ANY/ONE/TWO/THREE/QUORUM/LOCAL_QUORUM/EACH_QUORUM/ALL;W+R>RF 强一致性公式 4 组合;sloppy quorum 失败节点 hint 转交;Read Repair 10% chance 同步;LWT Paxos 4 阶段 ballot 单调递增 4-5x 普通写;Anti-Entropy Merkle tree sha256 keys 范围最小差异流式同步;cassandra.yaml hinted_handoff_enabled/max_hint_window_in_ms=10800000 3h/read_repair_chance=0.1/gc_grace_seconds=864000 10d;5 demo:CL 决策/W+R>RF 表/写读+repair/LWT insert-if-not-exists/Merkle 对账) | C / Python / Go | 2026-09-12 |
| 062 | `07-数据存储/02-NoSQL/MongoDB写关注/` | MongoDB 副本集 + writeConcern/readConcern(MongoDB 5.0+ 隐式公式:arbiters>0 且 data_bearing ≤ majority 时退回 w=1 即 P-S-A trap;OpObserver 钩子 + oplog.rs capped collection 5% 磁盘空间;tailable cursor 拉 oplog;commit_point = min(lastApplied);readConcern 5 级 local/available/majority/linearizable/snapshot;linearizable 仅 primary+snapshot 仅事务;j=true 隐含 fsync journal;P-S-S-S-A 五节点 majority=3 w=majority 隐式;5 demo:隐式公式/P-S-A trap/w:1 vs w:majority vs w:3 超时/5 级 RC/failover 回滚) | C / Python / Go | 2026-09-12 |
| 063 | `07-数据存储/03-缓存/RedisPersistence/` | Redis 持久化 RDB+AOF+Fork CoW(Redis official Persistence/Glossary/Learn 全文:save 60 1000 触发 BGSAVE → fork 子进程 → rdbSaveRio 遍历 dict → rename(2) 原子替换 dump.rdb;AOF 三种 fsync 策略 always/everysec/no,everysec 后台线程每秒 fdatasync,丢 ≤ 1s;multi-part AOF Redis 7.0+ base+increment;AOF rewrite fork 子进程按当前内存生成最小命令集 100 INCR→1 SET,父进程同时写旧 AOF + 新 incr,manifest 原子切换;CoW fork 页表 + 写时复制 大实例 fork 阻塞毫秒-秒级,INFO latest_fork_usec 监控;三语言 C/Python/Go 演示 fork+exec/CoW+AOF RESP 协议编码,Windows 直接 exit) | C / Python / Go | 2026-09-12 |
| 064 | `07-数据存储/03-缓存/RedisCluster/` | Redis Cluster(Redis Cluster Spec 英文/中文 + Scalability Learn 全文:HASH_SLOT=CRC16(key)&16383,CRC16/XMODEM 多项式 1021 init 0000 输出 0x31C3;16384 槽非 65536 因 bitmap 16384/8=2KB 友好;Hash Tag {x} 同槽强约束 `MGET {user1000}.following,followers`;Node ID 160-bit SHA-1-like 重启不变;Gossip ping 通用头部+ 1/10 节点信息,每秒随机 ping 几节点;故障转移 replica currentEpoch+1+多数票+configEpoch=max+1;Replica 排名延迟 500+random(500)+rank*1000ms;MOVED vs ASK 重定向)(C/Python/Go ×3;CRC16 表与 spec 附录 A 完全一致;gossip 1/10 sampling 与节点 < 10 时取 1 算法还原;failover demo 简化全票通过) | C / Python / Go | 2026-09-12 |
| 065 | `07-数据存储/03-缓存/LRUEviction/` | 缓存淘汰 LRU(Memcached 精确 LRU 双向链表+hash 头插 O(1) 移链 + 尾删 + LRU Crawler 后台线程 100ms 扫尾+TTL,segmented LRU 1.5+ HOT/WARM/COLD 三段 vs Redis 近似 LRU 24-bit lru_ts + maxmemory-samples=5 + 候选池复用次旧候选,maxmemory-samples=10 命中率 < 1% 误差;slab class 80 个 48B-1MB 大小 1.25x 倍增,eviction 只在 slab 内隔离;LFU Redis 4.0+ lfu-log-factor=10+lfu-decay-time=1 分钟 衰减抗 scan pollution;maxmemory-policy 6 种 noeviction/allkeys-lru/volatile-lru/allkeys-lfu/volatile-lfu/allkeys-random/volatile-random;Kablamo 实测 48 线程 segmented LRU)(C/Python/Go ×3;demo 100 insert 36 hit test,精确 LRU 0 hit 印证 36 个最旧 key 全淘汰,近似 LRU 随机采样留 ~3-5 个最旧未访问) | C / Python / Go | 2026-09-12 |
| 066 | `07-数据存储/04-搜索引擎/BM25/` | BM25 评分算法(Lucene 9.x / ES 5+ 默认相关性,score(q,d) = Σ IDF(q) · (tf·(k1+1))/(tf + k1·(1-b+b·|D|/avgdl)),IDF(q) = ln(1 + (N-n+0.5)/(n+0.5)),k1=1.2/b=0.75 默认,半饱和点 k1=1.2 时 tf≈1.2 BM25=0.5·max;norm=1-b+b·|D|/avgdl 长度归一化 + Lucene 8 长度归一化从 docCount/avgdoclen 切换到 norms/avgdl + BM25F/BM25+ 等变体;Python+Go 公式复刻 ES 文档示例 4 词"fox"查询 BM25 = TFIDF/0.86 等算例;Python 纯 stdlib 174 行 + README 172 行) | Python / Go | 2026-09-12 |
| 067 | `07-数据存储/04-搜索引擎/HNSW/` | HNSW 近似最近邻(Malkov & Yashunin arXiv 1603.09320v4 2018,Algorithm 1 INSERT 层采样 l=floor(-log(u)·mL)/mL=1/log M,mL=0.3825;Algorithm 2 SEARCH-LAYER 贪心 W/C 候选集合 ef 参数;Algorithm 5 K-NN-SEARCH 自顶向下 ef=efSearch efConstruction=200,M=4/M0=8/efConstruction=8/efSearch=16;mL=1/log(M) 层分布,高层稀疏"高速公路"意象;Python+Go 实现,Python 226 行完整模拟 + Go 298 行贴近上限;2D 20 点 query recall@3 ≈ brute-force ground truth) | Python / Go | 2026-09-12 |
| 068 | `07-数据存储/04-搜索引擎/段合并/` | Lucene 段合并与删除回收(TieredMergePolicy score ∝ size·(1 + reclaim_deletes_weight·del_ratio),reclaim_deletes_weight=2.0;floor_segment=2MB,max_merged_segment=5GB;segments_per_tier=10;store.throttle.type 默认 merge 默认 20 MB/s 节流,索引速度上限;forceMerge(1) 仅适用 ILM cold phase;ES 8.19 data stream lifecycle settings merge_factor=16/floor=100MB;Apache Lucene 10 release highlights logical partitioning;80% 删除 demo 小段 score 1+2·0.8=2.6 倍印证 reclaim_deletes_weight 优先级) | Python / Go | 2026-09-12 |
| 069 | `08-安全/01-密码学/对称加密/AES-GCM/` | AES-GCM 认证加密(NIST SP 800-38D:§7.1/§7.2 AEAD 流程,J0 = IV || 0x00000001 96-bit IV,H = E_K(0^128) 子密钥,C = GCTR_K(J0+1, P) §6.5 CTR,S = GHASH_H(A||C||lenA·8||lenC·8) §6.4 通用哈希,T = GCTR_K(J0, S)[:t] 16B tag;GF(2^128) 乘法 x^128+x^7+x^2+x+1 0xe1<<120 约简;FIPS 197 AES-128 SubBytes/ShiftRows/MixColumns/AddRoundKey 10 轮;GCM Python 自实现 AES-128+GHASH+GCTR,Go stdlib crypto/cipher.NewGCM;NIST SP 800-38D Test Case 1/2/3/4 全部命中 + 篡改密文/AAD/tag 检测) | Python(从零) + Go(stdlib) | 2026-09-12 |
| 070 | `08-安全/01-密码学/非对称加密/RSA-PSS/` | RSA-PSS 概率签名(RFC 8017 §8.1 + §9.1:RSASSA-PSS-SIGN(K,M) 步骤 1 EMSA-PSS-ENCODE M' = 8B零 || mHash || salt,MGF1(H, emLen-hLen-1) 掩码,maskedDB = DB ⊕ dbMask 顶部 emBits 清零,EM = maskedDB || H || 0xbc,RSASP1 m^d mod n,S = I2OSP(s,k);emBits = modBits-1;salt = hLen 32B SHA-256;verify 步骤 EMSA-PSS-VERIFY 严格检查 0xbc trailer/高位零/PS 全 0 + 0x01/常数时间比较 H;RSASSA-PKCS1-v1_5 vs PSS 对比;FIPS 186-5 §5.6 RSA-2048+ e=65537 合规;Python 1024-bit 自实现 RSA 密钥生成 + Miller-Rabin + EMSA-PSS + 签名验签,Go stdlib crypto/rsa SignPSS/VerifyPSS) | Python(从零) + Go(stdlib) | 2026-09-12 |
| 071 | `08-安全/01-密码学/签名/Ed25519/` | Ed25519 签名(RFC 8032:扭曲爱德华兹曲线 -x²+y² = 1+d·x²y² a=-1 d=-121665/121666 mod 2^255-19;基点 B_y = 4/5 mod p,B_x 由 y 恢复 (u/v)^((p+3)/8) Tonelli-Shanks p≡5 mod8, sign 取 x & 1 == 0;§5.1.5 KeyGen SHA-512(seed)[:32] clamp 清 bit 0/1/2 置 bit 254 清 bit 255 + mod L + [a]B encode A 32B;§5.1.6 Sign r = SHA-512(prefix || M) mod L,R = [r]B,s = (r + SHA-512(R || A || M) · a) mod L 确定性;§5.1.7 Verify k = SHA-512(R || A || M) mod L,[s]B = R + [k]A 简化公式,完整 [8][s]B = [8]R + [8][k]A cofactor;Python 自实现 Curve25519 twisted Edwards 扩展坐标点加 + Montgomery ladder scalar mult,Python RFC §7.1 Test 1/2 全部命中,Go stdlib crypto/ed25519) | Python(从零) + Go(stdlib) | 2026-09-12 |
| 072 | `08-安全/01-Web安全/XSS与CSP/` | XSS 与 CSP(OWASP XSS Prevention Cheat Sheet + MDN CSP header:HTML 5 种上下文编码 body/attr/JS/URL/CSS,`&<>"'` HTML Entity + JS Unicode + URL `%HH`;XSS 三类型 Reflected(URL+服务端反射)/Stored(评论持久化)/DOM-based(location.hash→innerHTML);textContent vs innerHTML sink 对比;textContent 自动 HTML Entity 防 XSS,CSP 阻断 inline + nonce 允许;CSP nonce 每响应生成随机值 + `'nonce-<val>'` 放 script-src + 无 nonce 脚本被阻断;`'unsafe-inline'`/`'unsafe-eval'`/`'strict-dynamic'` + `'self'`/`'none'` 源表达式;fetch 指令集 + fallback 链 default-src → 所有;Reporting `report-uri`/`report-to`;WAF 黑名单绕过 `<scr<script>ipt>alert(1)</script>` + Unicode + 大小写 + `<svg onload=>` + `javascript:` scheme;Python+JavaScript 双语 demo 完全,纯 stdlib + 浏览器解析字符串) | Python + JavaScript | 2026-09-12 |
| 073 | `08-安全/01-Web安全/CSRF-Token/` | CSRF Token 同步器令牌(OWASP CSRF Prevention Cheat Sheet:Synchronizer Token Pattern 服务端生成 CSPRNG token + 表单隐藏域 + POST 验证比对,secrets.token_urlsafe(32) ≥ 256-bit 熵;Signed Double Submit Cookie HMAC(secret, session_id + random) 签名版服务端无状态;SameSite Strict/Lax/None 三态,Chrome 80+ 默认 Lax,Lax 不阻挡顶级 GET;Fetch Metadata Sec-Fetch-Site = cross-site + unsafe method → 主策略拒绝;Origin/Referer 兜底精确字符串匹配(尾 / + 防 example.org.attacker.com 前缀绕过);__Host- 前缀强制 Secure + Path=/ + 无 Domain;5 demo + JS 浏览器模拟 fetch + Preflight;Python stdlib + Node.js URL API) | Python + JavaScript | 2026-09-12 |
| 074 | `08-安全/01-Web安全/SQL注入与预编译/` | SQL 注入与参数化查询(OWASP SQL Injection Prevention Cheat Sheet 四选项:Prepared Statements 首选 + Stored Procedures(SP 内不再 EXECUTE IMMEDIATE) + Allow-list 输入验证(表名/列名无法绑定) + Escaping 强烈反对;SQL 数据/代码边界,跨数据库占位符 `?`/`$n`/`@x`/`:name`;Hibernate HQL 命名参数 + Django ORM 自动 + bobby-tables.com 跨语言示例;UNION 攻击 + Boolean/Time-based Blind SQLi + Second-order SQLi;Second-order 即使入库时转义,再次读出拼接仍触发;跨语言占位符对照 JDBC/psycopg2/database/sql/mysql2/PDO/.NET/Hibernate;Allow-list 表名 switch-case;Least Privilege db_datareader/writer ≠ db_owner;6 demo sqlite3 prepared statements + Go database/sql 参数化) | Python + Go | 2026-09-12 |
| 075 | `08-安全/01-Web安全/JWT验证/` | JWT 验证 RFC 7519(三段 base64url `header.payload.signature`,HS256 HMAC-SHA256;base64url RFC 4648 §5 URL-safe + 无 padding;Registered Claims iss/sub/aud/exp/nbf/iat/jti + kid + typ;`alg=none` 历史漏洞 CVE-2015-9235 服务端必须白名单 alg;RFC 8725 §3.1 BCP Algorithm Confusion 攻击 header.alg=HS256 用公钥当 HMAC secret 防御:服务端按业务决定算法 + 检查 alg 与密钥类型匹配;RFC 7519 §A.1 HS256 示例向量完整复刻 secret 字节级;HMAC 常量时间比较 hmac.compare_digest;JWT 不加密(base64url 可解码)——机密数据用 JWE RFC 7516;jti 黑名单防 replay SETEX jti <exp-now>;Python+Go 双语 demo,RFC 7519 向量 100% 一致) | Python + Go | 2026-09-12 |
| 076 | `08-安全/01-Web安全/Same-Origin与CORS/` | 同源策略与 CORS(RFC 6454 三元组 scheme+host+port,任一不同即跨源,大小写不敏感但端口敏感;MDN CORS-safelisted headers Accept/Accept-Language/Content-Language/Content-Type/Range,POST Content-Type 三 MIME form-urlencoded/multipart/form-data/text/plain 不需 preflight;非简单请求 PUT/DELETE/JSON Content-Type/Custom header 触发 OPTIONS preflight;Server: ACAO 精确单源(非 credentials 时可用 *) + ACAM + ACAH + Max-Age + Vary: Origin 防 CDN 串味;Credentialed Request credentials: include 必须具体 ACAO,* 浏览器强制拒绝 + ACAC true;Origin 反射反射漏洞;5 demo + JS 浏览器模拟 fetch;Python stdlib + Node.js URL) | Python + JavaScript | 2026-09-12 |
| 077 | `08-安全/02-网络安全/TLS-1.3握手/` | TLS 1.3 握手协议 (RFC 8446:1-RTT ClientHello X25519 key_share → ECDHE g^ir → HKDF-Extract SKEYSEED/HKDF-Expand-Label handshake_secret → client/server_handshake_traffic_secret/handshake key/iv → 服务端用 AEAD AES-128-GCM 加密 EncryptedExtensions/Certificate/CertificateVerify/Finished → CertificateVerify 对 transcript 签名防 downgrade attack → 双方验证 Finished 后派生 master_secret → client/server_application_traffic_secret → 1RTT 后双方用 app traffic key 加解密 HTTP;对比 TLS 1.2 2-RTT 证书明文;5 大 REMOVED RSA key exchange/CBC/RC4/MD5-SHA1/Compression/Renegotiation/Session ID;0-RTT PSK NewSessionTicket 防 replay(限幂等);5 cipher suites TLS_AES_128_GCM_SHA256 etc) | C + Python + Go | 2026-09-12 |
| 078 | `08-安全/02-网络安全/SYN-Cookie/` | SYN Cookie 防 SYN Flood (Bernstein/Schenk 1996 + RFC 4987 §3.1:服务端**不分配 TCB**,只有收到 ACK(ack = ISN+1)时才校验;32 bit cookie = top 5 bit t(慢时戳 time()>>6 mod 32) + mid 3 bit MSS code(从 8 个预定义 MSS_TABLE 中最近匹配) + low 24 bit s = HMAC-SHA1(server_secret, src_ip:port | dst_ip:port | t | m_code) 低 24 bit;验证:t_cooked 与 t_now 差 ≤ 4(±256 秒容忍)、HMAC 匹配、用 m_code 反查 MSS_TABLE;Linux 内核 `tcp_syncookies` sysctl 三态 0/1(backlog 满时)/2(始终);3 bit MSS 限 8 预设、丢 TCP 选项 window scale/SACK(攻击期);C/Python/Go 三实现+10 个不同 src 同一秒 SYN 各得独立 hash) | C + Python + Go | 2026-09-12 |
| 079 | `08-安全/02-网络安全/XDP包过滤/` | eBPF/XDP 高速包过滤 (Linux 4.8+:SEC("xdp") BPF 程序 attach 到网卡驱动 RX 早期 hook,NAPI poll 内执行,XDP_DROP/XDP_PASS/XDP_TX/XDP_REDIRECT 5 action;26Mpps/core 单核丢包性能;XDP 程序必须做 `if (ptr + 1 > data_end) return XDP_PASS` bounds check,verifier 静态校验无越界/无死循环;`BPF_MAP_TYPE_HASH` blacklist_map (key=src_ip, value=hit_count);3 模式 Native(默认最早期,驱动原生支持,最快)/ Generic(fallback,慢)/ Offloaded(网卡硬件);AF_XDP 4 ring FILL/RX/TX/COMPLETION + UMEM 零拷贝用户态 socket;`bpftool` 管理 map 与 prog;工业应用 Cloudflare L4Drop/Facebook Katran/Cilium KubeProxy;用户态 controller Python / C libbpf-pseudo / Go cilium/ebpf 模拟 3 语言等值算法) | C(BPF 用户态 loader 骨架)+ Python(用户态 controller)+ Go(用户态 controller) | 2026-09-12 |
| 080 | `08-安全/02-网络安全/IKEv2-ESP/` | IKEv2 密钥协商 (RFC 7296 + ESP RFC 4303:IKE_SA_INIT(2 消息 DH X25519 + Nonce + SA 提议 → SKEYSEED = prf(Ni | Nr, g^ir) → SK_ai/ar/ei/er/pi/pr/d 共 7 把密钥 prf+ 派生) → IKE_AUTH(身份 + 证书 + AUTH prf(PSK,"Key Pad") 或签名 + 第一个 Child SA);Child SA keymat = prf+(SK_d, Ni | Nr, k) → sk_ei_child/sk_ai_child/sk_er_child/sk_ar_child(AES-128-GCM 16+16+16+16=64 B);ESP 隧道格式 新 IP 头(20+B)|SPI(32)|SeqNo(32)|IV(12)|AES-GCM CT|ESP Trl+Pad|ICV(16);Anti-replay 滑动窗口 ±32;NAT-T RFC 3948(UDP 4500 封装);MOBIKE RFC 4555(移动切换保 SA);RFC 9370 多 KE 抗量子;Prf+ RFC 7296 §2.13 链式扩展 T(i) = prf(K, T(i-1) | seed | i);CertificateVerify-style AUTH 抵抗 downgrade 比 TLS 1.3 弱(其靠 SPI+Crypto Agility 防御);强大工程 Openswan/strongSwan/LibreSwan/Racoon2) | C + Python + Go | 2026-09-12 |
| 081 | `08-安全/02-网络安全/DNSSEC链式信任/` | DNSSEC 链式信任 (RFC 4033/4034/4035:Kaminsky 攻击 CVE-2008-1447 触发 2010 根 zone signed;4 RR 类型 DNSKEY(flags 256 ZSK / 257 KSK_SEP=1,RFC 4034 §2 wire format flags:protocol:algorithm:raw_pub + RDLENGTH)/RRSIG(RFC 4034 §3 type_covered:algorithm:labels:orig_ttl:inception:expiration:key_tag:signer_name:signature)/DS(RFC 4034 §5 key_tag:algorithm:digest_type:SHA-256 digest)/NSEC3(RFC 5155 hash 防 zone walking);Chain of Trust `DNSKEY->[DS->DNSKEY]*->RRset` 根 trust anchor(RFC 5011 自动更新)→.TLD KSK→子域 DS→子域 KSK→子域 ZSK→目标 RRset;每层 DNSKEY RRset 被上一层 KSK 签、目标 RRset 被本层 ZSK 签;4 状态 Secure/Insecure(无 DS)/Bogus(签名错)/Indeterminate(无 trust anchor);RFC 4035 canonical sort 防 RRset ordering;现代算法 ECDSA P-256(13)/Ed25519(15)/Ed448(16);Alg 5 RSASHA1/7 RSASHA1-NSEC3-SHA1 遭弃用;Clock skew 容忍 ±5 min;Ed25519 demo 自实现 RFC §7.1 Test 2 公钥 3d4017c3... 命中) | C + Python + Go | 2026-09-12 |
| 082 | `09-语言学习/Python/生成器/` | Python 生成器与 yield(docs.python.org/3/reference/expressions.html §6.2.10 完整机制:yield 表达式挂起/恢复语义保留全部局部状态 + send/throw/close 三方法 + close 后 yield 抛 RuntimeError + 生成器协议 __iter__/__next__ 自动满足 + StopIteration 耗尽 + PEP 380 yield from 子迭代器自动委派 send/throw/close + subgenerator return value 经 StopIteration.value 传出 + PEP 525 async def + yield 异步生成器 __anext__/asend/aclose 全部 awaitable + 耗尽抛 StopAsyncIteration;Python 159 行纯 stdlib py_compile 干净;官方 echo() 经典示例展示 send(2)→2 / throw(ValueError)→exception / close()→GeneratorExit 触发 finally) | Python | 2026-09-13 |
| 083 | `09-语言学习/Python/上下文管理器/` | Python 上下文管理器(contextlib 官方库完整 API:__enter__/__exit__ 协议 + PEP 343 with 引入 + __exit__ 返回 True 抑制异常/None 传播 + @contextmanager 装饰器基于生成器 yield=__enter__/finally=__exit__ + 单次性消费 + 3.10+ 可作装饰器 + ExitStack 动态 enter_context/push/callback 编程式组合多上下文 + suppress 优雅吞异常 3.12+ 支持 BaseExceptionGroup + closing 给不原生支持协议的对象加尾巴 + aclosing 异步版 3.10 + nullcontext 占位 + ContextDecorator 既当上下文管理器又当装饰器 + AsyncExitStack + @asynccontextmanager 异步版;Python 236 行纯 stdlib py_compile 干净 + main() 全 6 节演示) | Python | 2026-09-13 |
| 084 | `09-语言学习/Python/描述符/` | Python 描述符协议(docs.python.org/3/howto/descriptor.html 完整机制:__get__/__set__/__delete__ 三个魔术方法 + 数据描述符(定义 __set__ 或 __delete__)优先级高于实例字典 + 非数据描述符(只 __get__)可被实例字典覆盖 + 优先级链 data → instance dict → non-data → class var → __getattr__ + property 用描述符实现同时定义 __get__+__set__ 验证 property 是数据描述符 + 函数是非数据描述符 Function.__get__ 返回 MethodType 绑定 self + @staticmethod 不绑定 + @classmethod 把 cls 作首参 + PEP 487 __set_name__ 描述符自动获知属性名 type.__new__ 自动调 + 完整 Validator 抽象基类 OneOf/Number 校验 + ORM Field 描述符代理 _data 字典) | Python | 2026-09-13 |
| 085 | `09-语言学习/Python/协程与asyncio/` | Python 协程与 asyncio(docs.python.org/3/library/asyncio-task.html 完整机制:async def 单纯调用不执行只生成 coroutine 对象 + 三种 awaitable coroutine/Task/Future + asyncio.run 顶层入口 + asyncio.create_task 自动调度必须持强引用防 GC 提前回收 + TaskGroup 3.11+ 结构化并发强安全保证 + 失败传播 ExceptionGroup + gather 按入参顺序收集首个异常立即抛 vs wait 返回 (done,pending) 不取消子任务 vs as_completed 按完成顺序产出 + asyncio.timeout 3.11+ 上下文管理器统一抛 TimeoutError 取代 asyncio.TimeoutError + wait_for 函数式超时 + asyncio.to_thread 阻塞 IO 包装线程池 + asyncio.run_coroutine_threadsafe 跨线程调度) | Python | 2026-09-13 |
| 086 | `09-语言学习/Python/元类/` | Python 元类与 __init_subclass__(PEP 3115 Python 3.0 引入 metaclass= 语法 + PEP 487 Python 3.6 引入 __init_subclass__/__set_name__;type 三参数动态创建类等价 class 定义;类创建三步流程 __prepare__ 返回命名空间 → 执行类体 → metaclass.__new__/__init__;metaclass 查找顺序 子类显式 > 基类元类 > type 且 metaclass= 不可继承;元类三个钩子 __prepare__/__new__/__init__ 签名;OrderedClass PEP 3115 原版示例 member_table 自定义 dict __setitem__ 记录 key 首次插入顺序 + __prepare__ 返回 member_table + __new__ 收尾;__init_subclass__ 隐式 classmethod + PEP 487 插件注册 PluginBase + 参数注入 Configurable label=primary;__set_name__ 自动获知属性名;元类 vs __init_subclass__ vs 类装饰器 三方取舍;官方推荐 80% 场景用 __init_subclass__ 替代) | Python | 2026-09-13 |
| 087 | `10-逆向工程/01-二进制逆向/ELF文件解析/` | ELF 文件解析(man7.org elf(5):Elf64_Ehdr 64B @0 字段偏移表 e_ident 16B(魔数 \x7fELF/EI_CLASS=2/EI_DATA=1)+e_type ET_EXEC=2/ET_DYN=3+e_machine EM_X86_64=62+e_entry+e_shoff;程序头 56B(p_flags 位置是 64 位独有布局)vs 节头 64B 双目录:段=加载视图/节=链接视图,strip 删 .symtab 但不删节头表;Elf64_Shdr 64B sh_name 是 .shstrtab(由 e_shstrndx 定位)内偏移的字符串表间接寻址模式;.text PROGBITS+EXECINSTR/.bss NOBITS 不占文件空间/.symtab+.strtab strip 删除对象/.dynsym+.dynstr 保留;.rela.plt 遵循 .relaNAME 命名约定由 DT_JMPREL/DT_PLTRELSZ 定位;Elf64_Sym 24B st_info 打包 bind>>4+type&0xF,STT_FILE 符号可恢复编译单元构成;e_shnum>=0xff00 溢出转存真实值存节头首项 sh_size 的 Linux 扩展未实现;链接视图 vs 加载视图 sh_offset 文件偏移 vs sh_addr 虚拟地址) | Python(struct.unpack_from 逐字段小端解码) + C(memcpy 结构体布局直读, Linux 可自解析) | 2026-09-13 |
| 088 | `10-逆向工程/01-二进制逆向/PE文件解析/` | PE 文件解析(Microsoft Learn PE Format 官方规范:DOS 头 e_magic='MZ'@0 + e_lfanew@0x3C 单点枢纽易被加壳器篡改 → NT 签名 'PE\0\0' → COFF 文件头 20B(Machine 0x8664=AMD64/NumberOfSections/SizeOfOptionalHeader)→ 可选头 Magic 0x10b PE32/0x20b PE32+ 三差异:ImageBase 4B@28→8B@24/BaseOfData 删除/数据目录 +96→+112 → 节表每项 40B(判断代码节看 Characteristics EXECUTE 0x20000000 而非节名);RVA→文件偏移换算公式 file = RVA - sec.VA + sec.PointerToRawData 是一切目录解析公共前置,FileAlignment 512 文件世界 vs SectionAlignment 4K 内存世界错开;导入表数据目录#1 IMAGE_IMPORT_DESCRIPTOR 20B OriginalFirstThunk→ILT 名字表(最高位=1 序号导入)双表结构因加载器改写 IAT 后名字丢失;IBT 绑定/延迟导入#13 未解析;ASLR 下 ImageBase 只是首选地址 DllCharacteristics 0x0040 DYNAMIC_BASE;imphash 家族归类出发点) | Python(PE32/PE32+ 双格式单文件 rva_to_offset 集中换算) | 2026-09-13 |
| 089 | `10-逆向工程/01-二进制逆向/x64调用约定/` | x86-64 System V 调用约定(psABI AMD64 Supplement Figure 3.4:INTEGER 类前 6 参 rdi rsi rdx rcx r8 r9/SSE 类并行 xmm0-7 两序列独立计数互不挤占/寄存器耗尽右到左压栈/返回 rax+rdx 128 位/MEMORY 类大结构隐藏首参 rdi 传缓冲区地址;rbx rbp r12-r15 callee-saved vs 其余 caller-saved;call 前 rsp%16==0 对齐(SSE 需要)call 压 8B 返回地址后入口 rsp%16==8;红色区 rsp-128 叶子函数免 sub rsp;可变参数 %al=SSE 寄存器用量上界 0-8 是 variadic 调用指纹;结构体拆 8B 分片可同占 INTEGER+SSE 两个不相邻寄存器;Windows x64 完全不同 rcx rdx r8 r9+影子空间 32B;逆向应用:mov rdi/rsi 连续装填=前 2 参/call 前 push=参数>=7/push rbx r12 开场=有跨调用存活变量) | C(内联汇编入口抓拍 rdi-r9 实证 + rsp 对齐探针 + r12 保存验证)+ Python(psABI 参数分类算法模拟器 INTEGER/SSE 独立迭代器直译并行计数语义) | 2026-09-13 |
| 090 | `10-逆向工程/01-二进制逆向/GOT与PLT延迟绑定/` | GOT 与 PLT 延迟绑定(Ian Lance Taylor Linkers part 4 airs.com/archives/41:共享库加载地址运行时定+代码段必须只读位置独立→把"跳到哪"从代码搬到可写 .got.plt 数据,代码只写死 jmp *GOT槽;PLT 三段式 jmp *offset(%ebx)/push $index/jmp PLT[0] + PLT[0] push 4(%ebx) link_map/jmp *8(%ebx) _dl_runtime_resolve;首次调用 5 步:GOT 槽初始值指回本 PLT 条目第二条指令→push 索引→PLT0→解析器用 (link_map,index) 查 .rela.plt JMP_SLOT→查 .dynsym→回填 GOT 永久生效;elf(5) GLOB_DAT 非惰性启动全解析 vs JMP_SLOT 惰性、DT_JMPREL/DT_PLTRELSZ/DT_BIND_NOW 动态标签;LD_BIND_NOW 关闭惰性;逆向视角:call func@plt 不等于已调用/frida LD_PRELOAD 劫持发生在 GOT 回填后/GOT overwrite 攻击与 Full RELRO mprotect 只读防御/__libc_start_main@plt 交叉引用定位 main;x86-64 改 RIP 相对寻址无 %ebx;.got vs .got.plt 两张表;.plt.sec IBT/endbr64 差异;现代发行版默认 Full RELRO+BIND_NOW 惰性被事实禁用) | Python(状态机模拟器 5 步 trace + resolved 计数量化惰性收益)+ C(plt_subject.c 供 objdump -d -j .plt / -R grep JUMP_SLOT 观察) | 2026-09-13 |
| 091 | `10-逆向工程/01-二进制逆向/YARA静态特征匹配/` | YARA 静态特征匹配(YARA 官方文档 writing-rules 全文 + 官网 silent_banker 示例:rule 四段式 meta/strings/condition;文本修饰符 nocase 不能与 xor 同用/wide 只是 ASCII 交错 \x00 非真 UTF-16/xor 单字节密钥展开 255 变体 xor(0x01-0xff) 可限范围/fullword 前后非字母数字;十六进制半字节通配 ?? 与 A? + 跳变 [4-6]/[6] 固定/[10-] 无界 YARA 4.3 加 ~ 非;条件 #x==6 计数/@x[1] 首现偏移索引从 1 起越界 NaN/$x at 100/in (0..100)/filesize<200KB KB=1024 仅文件扫描有效进程扫描永假/2 of ($a,$b,$c) any all none of 集合量词;for any section in pe.sections 迭代;pe.entry_point 取代已弃用 entrypoint;imphash 家族归类;引擎三层:模式展开层 xor 255 倍→匹配层 Aho-Corasick 类单遍多模式近似 O(n)→求值层布尔组合;跳变区间过大指数分支是性能杀手) | Python(mini_yara.py 教学引擎复刻匹配语义 + fake_backdoor.yara 可被真实 yara 命令执行的等价规则) | 2026-09-13 |
| 092 | `11-性能分析/01-系统级剖析/采样剖析原理/` | 采样剖析原理(man7 getitimer(2):ITIMER_PROF 按用户+系统 CPU 时间递减到期投递 SIGPROF(不在 CPU 上不计时)vs ITIMER_VIRTUAL 只计用户态 SIGVTALRM vs ITIMER_REAL 墙钟 SIGALRM;itimerval{it_interval 周期/it_value 首次,双 0=拆除};fork 不继承/execve 保留/同类信号只能 pending 一个重负载会丢;POSIX.1-2008 标 obsolete 推荐 timer_create(CLOCK_PROCESS_CPUTIME_ID);采样数=时间占比的统计估计;Gregg CPU Flame Graphs:定时中断抓 PC/整栈,99Hz 奇数频率避免与周期活动步调一致;采样数可超流逝时间(多线程并行);栈回溯依赖帧指针 -fomit-frame-pointer 破坏栈;样本数 N 时 1% 以下热点不可信) | C(setitimer+SIGPROF+backtrace 折叠记账,符号化延迟到 addr2xl 事后)+ Python(signal.setitimer(ITIMER_PROF)+sys._current_frames 逐线程帧折叠)+ Go(ticker+runtime.Stack(all) 折叠全 goroutine 栈) | 2026-09-13 |
| 093 | `11-性能分析/01-系统级剖析/火焰图生成/` | 火焰图生成(flamegraphs.html+cpuflamegraphs.html+flamegraph.pl 源码:folded 格式单行栈分号分隔+行尾计数;三步采集→折叠→渲染;宽=占比平=栈深;x 轴按帧名字母序排序最大化合并非时间;widthpertime=(imagewidth-2*xpad)/timemax,x1=pad+stime*wpt;minwidth 0.1px 剪枝+depthmax 定画布高;暖色 r=205+50v3/g=230v1/b=55v2,名字校验和 srand 种子保证同名同色跨图一致 vs --hash 前部字符权重高 vs --random;根帧强制占满 timemax;帧内文字按宽算字符数≥3 才绘制超长截断加..;& 转义必须最先否则二次转义;sort @Data vs --flamechart 时间序不合并;icicle 翻转/sunburst 径向;Chrome flame chart 混称辨析;采样数>流逝时间属正常;LBR 栈 8/16/32 帧回溯不到公共根无法合并不合火焰图) | Python(folded→SVG 生成器复现官方渲染:惰性匹配解析+字母排序+剪枝+暖色+转义+悬停/缩放 JS)+ Go(folded 解析+前缀树+终端 ASCII 火焰图验证宽∝计数深=栈深) | 2026-09-13 |
| 094 | `11-性能分析/01-系统级剖析/perf_events剖析/` | perf_events 剖析(Gregg perf 页:三种插桩模式 counting 开销最低→sampling 次之→trace 最高,先 stat 后 record;perf stat 输出解读 IPC>1 好但警惕自旋/frontend cycles idle 取指停滞/backend idle 乱序引擎可并行 3-4 uops 故 IPC>1/SCPI 停滞即延迟;perf record -F 99 定时采样(99 避开步调一致)vs -c 10000 事件计数阈值由处理器实现达到阈值才中断内核;软件事件默认采样周期 context-switches sample_freq=4000 是子集采样,-c 1 全量;skew:PMC 溢出到捕获 IP 有延迟,:p 修饰符 PEBS/IBS 精确采样;栈采集三方案帧指针 -fno-omit-frame-pointer/DWARF libunwind/LBR 8-16-32 帧上限云环境禁用;dd 实测 strace 慢 62x vs perf stat 2.5x;PMC 固定硬件资源同编程少数计数器;JIT 需 /tmp/perf-PID.map) | C(rdtsc 周期计数 vs 墙钟+PMC 溢出阈值采样模型 -c 1/100/10000 对比)+ Python(perf stat 输出解析器:四段式行解析+IPC/SCPI/miss 率派生+hardware/software/tracepoint/raw-pmc/hw-cache 事件分类) | 2026-09-13 |
| 095 | `11-性能分析/01-系统级剖析/strace与ptrace/` | strace 与 ptrace(man7 ptrace(2) 全文:TRACEME 后 raise(SIGSTOP) 或 ATTACH 发 SIGSTOP 必须 waitpid 等到停止/SEIZE 3.4+ 附加不停止;PTRACE_SYSCALL 让 tracee 在 syscall 进出处各停一次,enter/exit-stop 形态相同 tracer 必须自己配对,enter 后用 CONT 重启则无 exit-stop;PTRACE_O_TRACESYSGOOD 陷阱报 SIGTRAP|0x80 可靠区分;GETREGS x86-64 调用号 orig_rax 参数 rdi/rsi/rdx/r10/r8/r9 返回 rax;PEEK 返回 -1 歧义先清 errno;waitpid __WALL;seccomp EVENT stop 可致 exit 无 enter 误判;Yama ptrace_scope 0-3 默认 1 只许祖先防 GPG-agent 窃取;new usernamespace 削弱 Yama;strace -p 抑制 SIGSTOP 致 epoll_wait 杂散 EINTR;execve 后 TID 重置用 TRACEEXEC;tracee 对象是线程非进程;Gregg dd 实测 strace 62x vs perf stat 2.5x) | C(迷你 strace:TRACEME+SETOPTIONS+SYSCALL 循环+GETREGS 读写+enter/exit 配对+-c 式汇总)+ Python(ptrace 开销模型:dd 3.53s→218.9s 反推每 syscall 追踪税+外推不同 syscall 频率+strace -c 汇总解析) | 2026-09-13 |
| 096 | `11-性能分析/01-系统级剖析/Off-CPU分析/` | Off-CPU 分析(Gregg offcpuflamegraphs 全文:off-CPU=线程不在 CPU 上运行的时间,同步路径上直接成比例影响性能而 on-CPU 剖析完全不可见;插桩点 finish_task_switch() 切换结束处处于下一个线程上下文,算时长同时直接抓当前内核栈+用户栈无需保存检索,栈不变性=醒来抓的栈与睡去时相同(期间未运行);offcputime 伪代码 sleeptime[prev]=ts/delta 结算/totaltime[pid,execname,user stack,kernel stack]+=delta;-f folded -d 内核栈与用户栈插"-"分隔帧 --state=2 只留 TASK_UNINTERRUPTIBLE 剔除非自愿切换;线程池时间膨胀:多线程同路径聚合等待和>墙钟,mysqld 案例 io_handler_thread 列宽超 30s;空闲塔淹没信号用 grep do_command 过滤;坑:栈只解释等在哪不解释为何久需 wakeup/off-wake/chain graphs 逐级增强;文件读 -fomit-frame-pointer 破坏栈/JIT 语言只剩地址;工具矩阵 offcputime/fileiostacks/biostacks(键 request_id 入队时测因完成异步)/wakeuptime(记唤醒者栈锁竞争利器)/offwaketime(--分隔向下 off-CPU 向上倒序唤醒栈);开销警告:调度器事件每秒百万级 bcc 内核态汇总需 Linux 4.8+;40%→70% 问题覆盖率) | Python(上下文切换记账模拟器:finish_task_switch 记账直译+双核 owner+idle 兜底+线程池时间膨胀与 --state=2 过滤肉眼可见)+ Go(goroutine off-CPU 记账:park 记时刻/唤醒结算 delta 聚合 folded+对照 pprof block profile 是 runtime 内建版) | 2026-09-13 |
| 097 | `05-AI与机器学习/05-经典机器学习/线性回归/` | 线性回归(OLS normal equation `w=(XᵀX)⁻¹Xᵀy`+ Ridge closed form `(XᵀX+αI)⁻¹Xᵀy` + 梯度下降 + 多重共线性下 OLS 退化 Ridge 收缩 ||w|| 18.24→0.69 + α sweep 系数范数单调下降 + train/test R² 0.9996/0.9993 + 复杂度 O(n·p²) 实证 p=20/p=2 ≈ 30x) | Python(纯 stdlib 矩阵求逆 + Gauss-Jordan + 5 demo) | 2026-09-13 |
| 098 | `05-AI与机器学习/05-经典机器学习/逻辑回归/` | 逻辑回归(sigmoid σ(z)=1/(1+exp(-z)) 数值稳定分两支 + 交叉熵 + L2 正则化 + α=1/(n·C) + 6 solver lbfgs/liblinear/newton-cg/sag/saga + 数值梯度 vs 解析梯度 max err < 1e-7 + OvR 多分类 + 决策边界 20x20 ASCII 概率映射 0-9) | Python(纯 stdlib GD + 二元 LR + OvR + 数值梯度验证) | 2026-09-13 |
| 099 | `05-AI与机器学习/05-经典机器学习/决策树/` | 决策树 CART(Breiman 1984 二叉树 + Gini=1-Σp² / Entropy=-Σp log p + 贪心穷举 (feature, threshold) 相邻唯一值中点 + 预剪枝 max_depth/min_samples_leaf + ID3/C4.5/C5.0/CART 对比 + 10% 标签噪声演示过拟合 + Grid 调优 depth×split×leaf) | Python(纯 stdlib 递归 CART + 5 demo 全跑通) | 2026-09-13 |
| 100 | `05-AI与机器学习/05-经典机器学习/K近邻/` | KNN(brute force O(n·d) + KD-Tree 中位数切分 + 递归回溯 + 球/超平面剪枝 + 距离度量 Minkowski p=1/2/3 + uniform/distance 加权投票 + 算法 brute/kd_tree/ball_tree/auto 4 选 + 平票警告 sklearn docs 明示 + k 偏差-方差权衡 k=1→k=N) | Python(纯 stdlib 两种实现 brute + KD-Tree,KD-Tree 17x 加速 + 答案一致率 100%) | 2026-09-13 |
| 101 | `05-AI与机器学习/05-经典机器学习/K均值聚类/` | K-Means(Lloyd 三步 ① init ② E-step 指派最近 ③ M-step 中心均值 + k-means++ 概率 ∝ D(x)²/Σ D(x²) 选远处点 Arthur & Vassilvitskii 2007 + inertia=Σmin||xᵢ-μⱼ||² + n_init=10 random/1 k-means++ 'auto' 1.4+ + elbow k=3 拐点 + silhouette 轮廓系数 [-1,1] + 假设演示球形 silhouette=0.67 vs 同心圆 0.29 失败案例) | Python(纯 stdlib Lloyd + k-means++ + silhouette + 5 demo) | 2026-09-13 |
| 102 | `06-DevOps/05-IaC与配置管理/HCL-解析/` | HCL (HashiCorp Configuration Language) 最小解析器(github.com/hashicorp/hcl/blob/v2.12.0/hclsyntax/spec.md:Body 顶层 + Attribute 定义 + Block 嵌套 + 三种注释 + 三种 Literal 字面量 + Collection Value tuple/object + Template Expr;本 demo 覆盖 attribute/block/字符串字面量/嵌套 object) | C(208 行手写 lexer + 递归下降)+ Python(281 行 regex 字典式)+ Go(277 行 bufio.Scanner + 递归下降) | 2026-09-13 |
| 103 | `06-DevOps/05-IaC与配置管理/资源依赖图/` | Terraform 资源依赖图(docs.hashicorp.com/terraform/internals/v1.15.x/graph:8 步建图 + Kahn 拓扑分层 + 默认并行 10 + -parallelism flag;apply 正向、destroy 沿反向;depends_on 显式 vs attribute ref 隐式;4 demo:菱形+显式+环+parallelism=10 切片) | C(254 行)+ Python(232 行)+ Go(236 行) | 2026-09-13 |
| 104 | `06-DevOps/05-IaC与配置管理/幂等模块/` | Ansible 幂等模块(docs.ansible.com/ansible/devel/playbooks_intro.html:"Most Ansible modules check whether the desired final state has already been achieved and exit without performing any actions..." 原文 idempotency 定义;--check mode 当 drift 测试;check-then-act 模式 + file/package/service 3 module + result {changed,failed,msg,diff} 字典 + 3 遍执行后 2 遍 0 changed) | Python(290 行)+ Go(294 行)+ JS Node.js(229 行) | 2026-09-13 |
| 105 | `06-DevOps/05-IaC与配置管理/模板引擎/` | 模板引擎 (Jinja2 Template Designer Documentation 全文:jinja.palletsprojects.com/en/2.10.x/templates/;三种 delimiter {{ }} {% %} {# #};foo.bar 先 getattr 后 getitem;|filter 链;Jinja2 → Go text/template 对照 {{ .Var }} {{ range }} {{ if }} {{ $x }} FuncMap) | C(195 行极简无 for)+ Python(299 行 miniJinja 自实现)+ Go(106 行 stdlib text/template) | 2026-09-13 |
| 106 | `06-DevOps/05-IaC与配置管理/State-diff/` | Terraform State diff & Plan 生成(developer.hashicorp.com/terraform/tutorials/cli/state-cli + /state/resource-drift:state v4 JSON schema version=4 + resources[mode/type/name/instances[attributes]];三类 Action +/-/~;drift detection refresh 阶段默认开启;4 demo:create-only / drift / partial-destroy / noop) | C(299 行手写 JSON)+ Python(268 行)+ Go(294 行 encoding/json) | 2026-09-13 |
| 107 | `09-语言学习/Golang/goroutine与channel/` | Goroutine + Channel + Select(go.dev/ref/spec Channel types + Concurrency chapter + Go Blog "Share Memory By Communicating":goroutine 2 KB 栈由 GPM 调度 + channel 是 typed conduit FIFO 顺序保证 + close(ch) 充当广播信号 + select 多 case 就绪时随机选择避免饥饿 + nil channel 永远不就绪可用于动态屏蔽 + comma-ok v,ok:=<-ch + range channel 配合 close 自动退出) | Go(154 行,gogoroutine 模块,涵盖 9 示例:goroutine+WaitGroup/unbuffered 同步握手/buffered 异步队列/close 广播/comma-ok 接收/range channel 自动退出/select 多路复用+timeout+ctx 取消/nil channel 屏蔽/fan-out 4 worker 共享 jobs) | 2026-09-13 |
| 108 | `09-语言学习/Golang/slice底层机制/` | Slice 三字头与 growslice(go.dev/ref/spec Slice types:ptr+len+cap 三字头零值 nil + make 等价 new([cap]T)[0:len] + append 触发 growslice Go 1.18+ 1.25x+192 算法小对象向上对齐 + 共享底层数组陷阱 + 三索引 s[lo:hi:max] 切断 max 之后共享) | Go(108 行,goslice 模块,涵盖 9 示例:三字头 unsafe 观察/make vs new+slice/append cap 够不分配 + 不够 growslice 3→6→12→24/共享底层数组陷阱/三索引切片防意外覆盖/copy 内置函数/nil slice vs empty slice/string-byte 互转/reflect.SliceHeader 构造) | 2026-09-13 |
| 109 | `09-语言学习/Golang/error处理/` | Error 接口与错误包装链(pkg.go.dev/errors + Go Blog "Working with Errors in Go 1.13":error 接口单方法 Error() string + 哨兵 var ErrFoo=errors.New + 自定义 type 携带上下文 + fmt.Errorf %w 包装链 + errors.Is 沿链比对 sentinel + errors.As 提取特定类型 + errors.Unwrap 一层 + Go 1.20+ errors.Join 实现 Unwrap() []error) | Go(121 行,goerror 模块,涵盖 10 示例:errors.New/sentinel/自定义 NotFoundError 类型携带 Resource+ID/fmt.Errorf %w 三层包装链/errors.Is 沿链/errors.As 提取 NotFoundError/ValidationError Unwrap/errors.Join 多 err 合并/%v vs %w/strconv.NumError 实战提取 Func/Num/Err) | 2026-09-13 |
| 110 | `09-语言学习/Golang/defer与panic-recover/` | Defer/panic/recover(go.dev/ref/spec Defer statements + Handling panics:defer 参数立即求值快照 + LIFO 逆序 + 命名返回值可被 defer 修改 + panic 触发源显式/运行时检测/向已关闭 channel 发送 + recover 必须直接 defer 内调用才生效 + Go 1.14+ 开放编码 defer 1ns vs 旧链表 30ns + runtime.Goexit 不 panic 仍执行 defer) | Go(164 行,godefer 模块,涵盖 9 示例:defer LIFO/参数立即求值快照/defer 改命名返回值 double(5)=12/panic+recover 标准模板/资源清理 defer 即使 panic 也执行/recover 硬性规则 funcWrapper 失败 vs 匿名 defer 成功/panic 栈展开/runtime.Goexit/panic 值任意类型) | 2026-09-13 |
| 111 | `09-语言学习/Golang/map底层实现/` | Map hmap+bmap+渐进式扩容(runtime/map.go 源码 + Go Under The Hood 5.4 Swiss Table:Go 1.23 之前 hmap{B,hash0,buckets,oldbuckets,nevacuate} + bmap 编译期展开 tophash[8]+keys[8]+values[8]+overflow 拉链法 8 元素/桶 + tophash 哈希高 8 位过滤 + 装载因子 6.5 触发翻倍扩容或等量扩容 + 渐进式搬迁 spread across 写操作 + Go 1.24+ Swiss Table 目录化 open addressing) | Go(181 行,gomap 模块,涵盖 10 示例:reflect 估算 hmap.B 随装载因子增长/迭代顺序随机化演示 3 轮/nil map vs empty map 区别/元素不可寻址/并发读写 fatal error 加 sync.RWMutex 后 OK/sync.Map 读多写少/map 赋值浅拷贝 vs 深拷贝/delete 不缩容/map 与 JSON nil→null/{}→{}/演示 map header size) | 2026-09-13 |
| 112 | `07-数据存储/05-图数据库/PropertyGraphModel/` | 属性图模型 LPG(节点+关系+标签+属性 + 按 (type, direction) 分桶的双向链表,neighbors_out 沿 next_out O(d) 遍历 — Neo4j Getting Started "Graph database concepts" + Developer Guides "What is a Graph Database") | Python(161 行) + Go(149 行) | 2026-09-13 |
| 113 | `07-数据存储/05-图数据库/CypherParser/` | Cypher 只读子集递归下降解析器(MATCH / WHERE / RETURN / ORDER BY / SKIP / LIMIT + 单节点 + 单关系 + 单节点最小执行器 — Neo4j Cypher Refcard 4.0 + Cheat Sheet 5.x) | Python(300 行) + Go(298 行) | 2026-09-13 |
| 114 | `07-数据存储/05-图数据库/GraphTraversal/` | 图遍历 BFS + DFS + Dijkstra(邻接表 + 队列/栈/最小堆;BFS 求无权图最短跳数;Dijkstra O((V+E)logV);7 节点 Wikipedia 经典图验证 — Wikipedia "Dijkstra's algorithm" + Czech Technical University Programming for Engineers L8 + TIRA MOOC) | C(143 行) + Python(112 行) + Go(155 行) | 2026-09-13 |
| 115 | `07-数据存储/05-图数据库/PageRank/` | PageRank 幂迭代(d=0.85 阻尼 + teleport (1-d)/N + 悬挂节点贡献 d × sum(PR) / N 平摊;收敛判据 max\|Δ\|<1e-6;13 轮经典 4 节点示例收敛 sum=1.0 — Wikipedia "PageRank" + Greif/Callut SIAM Review 2010) | Python(88 行) + Go(127 行) | 2026-09-13 |
| 116 | `07-数据存储/05-图数据库/Neo4jStorage/` | Neo4j 存储层定长记录 + 双向链表(15 B NodeRecord + 34 B RelRecord 字节级;每条关系同时挂 src.out + dst.in 两条双向链表;NULL=0xFFFFFFFF 避免与合法 rid=0 冲突;O(1) 定位 + O(d) 邻接遍历 — Neo4j KB "Understanding Neo4j's data on disk" + neo4j-contrib Glossary + arXiv 1910.09017) | C(127 行) + Python(195 行) | 2026-09-13 |
