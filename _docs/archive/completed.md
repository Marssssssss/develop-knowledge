| 491 | `11-性能分析/02-应用级剖析/Go-bLoop基准循环/` | Go 1.24 `for b.Loop()` 与 b.N 风格的差异（release notes 与 src/testing/benchmark.go 实读）：基准函数每个 -count **只执行一次**，昂贵的 setup 与 cleanup 各做一次且都不计入（首次调用 ResetTimer、返回 false 时 StopTimer），而 b.N 风格下基准函数被反复调用、setup 每轮重做并落在计时区间内；循环内 b.N 被刻意置 0，结束后才等于总迭代数；计时器被关掉时 runtime 用 1<<63 把 loop.i 打毒，逼快路径失效后在慢路径 Fatal；predictN 先乘后除（避免快速基准丢掉数量级）再乘 1.2，然后被 100×last 与 1e9 上下夹住、且至少比上次多一个；保活是编译期变换，要求循环条件**字面**写成 b.Loop() 且语句在花括号内 | Python(265 行 27 断言实跑) + Go(168 行人工审查) | 2026-09-21 |
| 492 | `01-游戏开发/05-物理/碰撞检测/SAT与EPA/` | 分离轴定理与 EPA 的可执行模型（dyn4j 官方 SAT/EPA 教程与源码实读）：凸多边形只需测**边法线**方向，圆与多边形要额外加一条「圆心到最近顶点」的轴 —— 少了这条会把擦过顶点的圆误判为相交；投影重叠量的**符号取决于轴朝向**，故要做 α̃ 归一化；**包含（一个凸包完全在另一个内部）时 SAT 不会给出分离轴**，必须做包含修正，实测不做修正时按深度 1.0 推开后仍相交、修正后深度变 2.0 才真正分开；EPA 从 GJK 的单纯形出发反复取最近边并加支撑点，官方三份逐步数值被精确复算（0.6324555320336759 与 0.9370425713316364，法线 0.6246950475544243 与 -0.7808688094430304）；**三重积在真实多边形数据上会退化并抛 ZeroDivisionError**，加绕序安全分支后两个版本在非退化输入上结果一致 | Python(311 加 196，70 项断言实跑全绿) | 2026-09-21 |
| 493 | `01-游戏开发/05-物理/碰撞检测/BroadPhase与空间分割/` | 宽相四路交叉验证（Box2D v3 源码与 pbrt BVH 章节实读）：均匀网格按 AABB 铺进可能相交的格子、同一对只登记一次；Box2D v3 的 `b2DynamicTree` 用**自由链表加代理 id** 而非指针，叶子按 `aabb margin = b2_aabbMargin * (lower 与 upper 的距离之和)` 外扩（大物体被夹到 0.05、静态体用 speculativeDistance 0.02），fat AABB 的用途是**只在明显移出时才重新插入**；AABB 相交判据是**闭区间**（贴着算相交、1e-7 的缝才算不相交）；SAH-BVH 按「 traversal 代价加 isect 代价 × 面积比」选分割；四路实现在 6 组随机场景下两两一致。另记两个实测坑：**rebuild 会新建叶子对象，外部持有的旧叶子收不到 moved 标记**；**叶子内多个图元没有兄弟可交叉，必须枚举叶子内部两两配对** | Python(157 加 263 加 176 加 181，90 项断言实跑全绿) | 2026-09-21 |
| 494 | `01-游戏开发/05-物理/刚体与柔体/PBD与XPBD约束求解/` | PBD 与 XPBD 的对拍模型（Müller 2006 与 Macklin 2016 实读）：PBD 把位置投影回约束流形，距离约束的式 10 与 11 就是按逆质量分配修正量，**沿梯度方向才能保证动量守恒**（否则引入 ghost force）；`k` 不是线性量，残差是 `(1-k)^n` 而非 `1-nk`；XPBD 引入柔度 α 与 `α̃ = α/Δt²`，式 18 的 `Δλ = (-C - α̃λ)/(∇C M⁻¹∇Cᵀ + α̃)` 在 `α=0` 时**逐位退化**为 PBD 的缩放因子 s。主结论实测：同一载荷下 PBD(k=0.2) 的稳态伸长随迭代数一路下降（0.0667 → 0.0081 → 0.00019 → 2.2e-6，永远不收敛），而 **XPBD(α=1e-5) 在 1/5/20/40 次迭代下给出完全相同的 0.000600000000**，且伸长量与 α 严格成正比（α 放大 10 倍 → 伸长放大 10.000000 倍）；Gauss-Seidel 一轮就能把扰动传到四粒子链尾而 Jacobi 一轮纹丝不动；全局阻尼只吃偏离刚体运动的部分 | Python(245 加 234，56 项断言实跑全绿) | 2026-09-21 |
| 495 | `01-游戏开发/05-物理/刚体与柔体/布料模拟/` | 三角网格布的两套弯曲模型（Ten Minute Physics 第 14 期官方 JS 与 Müller 2006 第 4 章实读）：官方的「秘密」是**不用二面角**，直接在两个相邻三角形的**对角顶点**之间加一根距离约束，静长取初始距离，从而复用同一段 XPBD 求解代码（`alpha = compliance/dt/dt`，`s = -C/(w+alpha)`，且**不累加 λ**）；默认 `bendingCompliance=1.0` 而 `stretchingCompliance=0.0`（布不可伸长）；顶点质量 = 每个相邻三角形质量的 1/3 之和；主循环 15 个子步、地面是 `preSolve` 里的**位置投影**。本 demo 同时实现 PBD 2006 的二面角 `acos(n1·n2) - φ0` 并对照。三个实测坑：①官方 `findTriNeighbors` 用「排序后两两扫」配对，**跨偶数与奇数边界的重复边会被整体漏配**（3×3 网格 8 条共享边只配出 1 条，拉伸约束重复登记成 23 条而唯一边只有 16 条），改成字典归桶即可 ②二面角投影必须四个顶点**共享同一个缩放因子** `s = C/Σ w_j‖∇C‖²`，逐顶点各做一次完整牛顿步会从 0.615 过冲到 1.670 ③**静止二面角是 π 不是 0**（相邻三角形在公共边两侧，n1·n2 = -1） | Python(113 加 88 加 171 加 222，93 项断言实跑全绿) | 2026-09-21 |
| 496 | `01-游戏开发/05-物理/刚体与柔体/接触与冲量求解/` | Box2D v3.1 接触求解核心的复刻（types.c / constants.h / solver.c-h / contact_solver.c / physics_world.c 实读）：`b2MakeSoft(hertz, zeta, h)` 给出 biasRate 与 massScale 与 impulseScale，且**任意参数下 massScale 加 impulseScale 恒等于 1**，hertz 为 0 即软约束关闭；**静态接触用的是两倍 contactHertz**（静态体没有质量可退让，推得更硬）；间距 `s>0` 走**推测性接触**（目标 `vn = -s/h`，实测 1 m/s 下落距 0.01 m 时解完恰为 -0.6 m/s），`s<=0` 走重叠偏置并被 `-contactSpeed` 夹住（重叠放大 1000 倍推开速度只从 0.008 涨到恰好 3.0）；**velocityBias 不乘 massScale**；累加冲量必须 clamp 到非负；摩擦上界用**本轮 relax 更新之后**的法向冲量（不是 push 阶段的，实测差 0.0057）；恢复系数要求「确实在挤压且撞得够快（< -1 m/s）」且**只在 relax 阶段**生效，实现为偏置下限。有效质量 `1/(wA+wB+iA(rA×n)²+iB(rB×n)²)` 的一个坑：**锚点偏移平行于法线时 r×n = 0**，有效质量纹丝不动 | Python(255 加 226，74 项断言实跑全绿) | 2026-09-21 |
| 497 | `02-Web开发/02-后端/优雅关闭与连接排空/` | Go net/http Shutdown 与 Close 的分野（src/net/http/server.go 实读）：Shutdown 置 inShutdown 后关监听、并发跑 onShutdown 钩子、再轮询 closeIdleConns，**只关 idle 与「超 5 秒仍未读到首包的 StateNew」**（Issue 22682），活跃连接始终留活口，故不停发请求时 Shutdown 只能由 ctx 到期收场；Close 则对 activeConn 无差别 rwc.Close。轮询间隔 1ms 起、每次翻倍并夹到 500ms，附 rand.IntN(base/10) 的 10% 抖动；**首次 closeIdleConns 发生在任何等待之前**；unixSec 为 0 的「刚建好」连接不算 quiescent。doKeepAlives 由 shuttingDown 一票否决，关机后再 SetKeepAlivesEnabled(true) 也无效；trackListener 在关机中返回 false 才让 Serve 报 ErrServerClosed。Kubernetes 侧：preStop 超时只给**一次性 +2 秒**延期，EndpointSlice 先置 ready=false 但不立即摘除 | Python(262 加 213，34 项断言实跑全绿) / Go(270 行人工审查) | 2026-09-21 |
| 498 | `02-Web开发/02-后端/中间件与责任链/` | Django 洋葱与 Express 责任链的对照（官方文档实读）：请求阶段按 MIDDLEWARE 正序、响应阶段逆序；**某层短路则内层与 view 完全看不到该请求**，响应只回穿请求进来时经过的那些层（这是「加了缓存中间件后安全头消失」的根因）；process_view 正序且返回响应即跳过 view 但响应中间件照常；**process_exception 逆序**，命中即让上层同类钩子不再被调用，故「最外层做统一异常转换」不可靠；process_template_response 在响应阶段逆序；__init__ 抛 MiddlewareNotUsed 即启动期摘除。Express 侧**靠形参个数（arity 4）识别错误处理器**，三参函数即使命名为 err 也被当普通中间件在错误路径上整体跳过；next('route') 只在 METHOD 加载的栈里生效，USE 栈里不跳路由 | Python(223 加 217，28 项断言实跑全绿) / Go(270 行人工审查) | 2026-09-21 |
| 499 | `02-Web开发/02-后端/HTTP缓存与条件请求/` | RFC 9111 与 RFC 9110 §13 的可执行模型：response_is_fresh 是**严格大于**，故 age 恰好等于 lifetime 时已 stale；freshness_lifetime 取第一个匹配，**s-maxage 只对共享缓存生效**（同一响应共享 60s 而私有 600s），Expires 减的是 **Date 而非本地时钟**；有显式过期时**禁止**启发式，启发式典型取 Last-Modified 间隔的 10%；Age 有 apparent / corrected / 保守版三种口径，保守版取 max 后可与非保守版差两个数量级；must-revalidate 与 no-cache 下即使有 max-stale 也禁止出 stale；**Vary 匹配中「缺席只能对缺席」**，Vary: * 恒不匹配，重排不属于许可的归一化；前置条件六步优先级里 If-Match 与 If-Unmodified-Since **只对 origin 有意义**，故同一请求经缓存与直连源站结果不同（304 对 412） | Python(262 加 64 加 210，56 项断言实跑全绿) / Go(165 加 188 行人工审查) | 2026-09-21 |
| 500 | `02-Web开发/02-后端/HTTP2帧与流多路复用/` | RFC 9113 帧层与 RFC 7541 HPACK：帧头固定 9 字节且 **Length 不含帧头**，R 保留位接收时必须忽略，Stream ID 是 31 位、取 0 表示整条连接；MAX_FRAME_SIZE 合法区间 [2^14, 2^24-1]，越界是 PROTOCOL_ERROR 而超限是 FRAME_SIZE_ERROR；SETTINGS 六参数初始值（HEADER_TABLE_SIZE 4096 / ENABLE_PUSH 1 / MAX_CONCURRENT_STREAMS 无限 / INITIAL_WINDOW_SIZE 65535 / MAX_FRAME_SIZE 16384 / MAX_HEADER_LIST_SIZE 无限）。流控复现官方 -44 KB 例：先发 60 KB 后流窗口剩 4095，收到 INITIAL_WINDOW_SIZE=16384 时按**新旧初始值之差**调整得 -45056；**SETTINGS 改不了连接窗口**。HPACK 整数表示拿附录 C.1 逐字节对拍（10→0x0A、1337→1F 9A 0A、42→0x2A），附录 C.3.1 的 20 字节序列解出 4 条头字段，动态表条目大小 = 名字加值加 32，官方标注 s=57 | Python(282 加 191，52 项断言实跑全绿) / Go(192 加 186 行人工审查) | 2026-09-21 |
| 501 | `02-Web开发/02-后端/JWT与JWS会话认证/` | RFC 7515 紧凑序列化与 RFC 7519 注册声明校验：签名输入是 **ASCII(header.payload)**，即第二个句点之前那段，算的是编码后的两段而非原始 JSON；base64url 是标准 base64 换 -_ 且**无 padding**。用附录 A.1 的官方字节序列（header 含 CRLF 与行首空格，共 30 字节）重算 HMAC-SHA256，结果与官方签名 dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk 逐字符相同。声明语义：**exp 是严格小于**（now 等于 exp 即已过期）而 **nbf 是大于等于**；aud 支持单值字符串与数组两种形态，处理方若不能在 aud 中认出自己则 MUST be rejected；alg=none 的**无保护 JWS 是规范一等公民**（附录 A.5，签名段为空、紧凑表示以句点结尾），只能靠校验侧硬编码的算法白名单拦截 | Python(151 加 141，37 项断言实跑全绿) / Go(213 行人工审查) | 2026-09-21 |
| 502 | `03-系统编程/02-进程与线程/futex机制/` | futex 原语（futex(2) 加 futex(7) 加 FUTEX_WAIT(2const) 加 include/uapi/linux/futex.h 加 kernel/futex/waitwake.c 实读）：FUTEX_WAIT 是「原子比较-并-阻塞」，值不符立即 EAGAIN，对照组证明不带比较的朴素等待会睡死；FUTEX_WAKE 返回实际唤醒数，且返回 0 也可能是假唤醒必须回环复判；FUTEX_WAIT 的 timeout 是相对值其余操作是绝对值；FUTEX_OP 位编码里 op 字段内核只取低 3 位而最高位 0x80000000 是 OPARG_SHIFT 令 oparg 变成 1 左移 oparg，oparg 另需 sign_extend32(11) 故 -1 编成 0xFFF 能原样解回；WAKE_OP 无条件唤醒 uaddr1 上的等待者且比较只看 uaddr2 的旧值；PI futex 的取值策略是 0 加 TID 加 FUTEX_WAITERS 位，非持有者 unlock 得 EPERM，持有者猝死由内核置 FUTEX_OWNER_DIED 并把锁交给下一位，优先级继承沿锁链传递（H 等 L 而 L 又等 M 时中间优先级被抬到 10 压过无关的 7） | Python(futex_model.py 加 futex_pi.py，77 断言实跑) / C(futex_mutex.c 人工审查) / Go(futex_model.go 人工审查) | 2026-09-21 |
| 503 | `03-系统编程/02-进程与线程/自旋锁与信号量/` | 自旋锁与 POSIX 信号量（IEEE Std 1003.1-2017 pthread_spin_lock 加 sem_overview(7) 加 sem_wait(3) 实读）：TAS 不排队，逆序调度下获取序列就是调度序列且插队 6 次，ticket 锁严格按到达顺序放行且零插队；持有者递归上锁在 Issue 7 起是 undefined 而非返回 EDEADLK，trylock 被持有时是 shall fail 的 EBUSY，二者都不返回 EINTR；单核不可抢占时自旋者占着 CPU 令持有者永远出不来（500 tick 全空转），有轮转后 3 tick 的活被 5 tick 空转白白垫掉；POSIX APPLICATION USAGE 点名的优先级反转实测把 3 tick 的临界区拖成 103 tick；信号量值永不为负、EINTR 时值保持不变、命名信号量最长 NAME_MAX-4 即 251 字符且落在 /dev/shm/sem.somename 并具内核持久性，unlink 只解名字 | Python(52 断言实跑) / C(spinlock_sem.c 人工审查) / Go(spinlock_sem.go 人工审查) | 2026-09-21 |
| 504 | `03-系统编程/02-进程与线程/内存序与原子操作/` | 六档 memory_order 的 litmus 穷举模型（cppreference std::memory_order 加 C11 n1570 §5.1.2.4 与 §7.17 加 Go 官方内存模型文档实读）：message passing 下「读到 flag 却看不到 data」这一结果在 release-acquire 被排除、全 relaxed 与只写 release 不写 acquire 时都出现；store buffering 下 release-acquire 挡不住双方都读到旧值，只有 seq_cst 或插入 seq_cst 栅栏才排除；release sequence 靠 relaxed 的**读改写**接力（读到 release 写时新写继承其发布集，换成普通写立刻切断）；fetch_add 的返回值集合恒为 0 与 1，原子性与内存序无关；另断言 CoRR 不会出现先新后旧 | Python(memorder_model.py 穷举所有交错与传播，18 断言实跑) / C(memorder_demo.c 人工审查) / Go(memorder_go.go 人工审查) | 2026-09-21 |
| 505 | `03-系统编程/02-协程/GoGMP调度器/` | Go GMP 调度骨架（src/runtime/runtime2.go 加 src/runtime/proc.go 加 design/24543-non-cooperative-preemption.md 实读）：并行度由 P 的数量即 GOMAXPROCS 决定，M 进系统调用时把 P 摘下 handoff 给新 M 而并行度不变；本地 runq 容量 256 加一个 runnext，runnext 继承当前时间片且被顶下去的进队尾，没有 sysmon 时运行时干脆禁用 runnext 防饥饿；本地满则搬 129 个进全局，全局每 61 个 schedtick 才查一次且批量恒为 len(runq)/2 即 128；窃取取靠头的一半（7 个取 4）并先跑这批里最新的那个；sysmon 休眠 20us 起步、idle 大于 50 后翻倍、封顶 10ms（第 60 次撞顶，真实值本该 10240）；forcePreemptNS 为 10ms，协作式安全点只在函数调用处故紧循环一个都没有，异步抢占可停任意点但绕过 unsafe.Pointer 转 uintptr 与 write barrier 两处 unsafe-point | Python(55 断言实跑) / Go(gmp_model.go 人工审查) | 2026-09-21 |
| 506 | `03-系统编程/02-协程/C++20协程/` | C++20 无栈协程状态机（cppreference Coroutines 实读）：协程状态是堆上分配的对象，装 promise 加参数副本加挂起点加局部变量，final_suspend 为 suspend_always 时不 destroy 就是泄漏，改为 suspend_never 则自行销毁但之后 resume 是 UB；initial_suspend 决定惰性还是立即启动（惰性下构造完一行都没跑）；awaiter 三件套中 await_ready 为真时 await_suspend 一次都不调用而 await_resume 照常调用，await_suspend 返回 void 或 true 挂起、返回 false 立即恢复、返回句柄是对称转移；对称转移 64 节的栈深恒为 1 而写成在 await_suspend 里 resume 下一个则栈深 64；进入 await_suspend 时协程已完全挂起故句柄可交给别的线程；按引用传入的参数保持为引用且对端销毁后即悬垂 | Python(34 断言实跑) / C++(coroutine_demo.cpp 人工审查) | 2026-09-21 |
| 507 | `04-移动开发/02-Android/UI框架/ComposeNavigation类型安全路由/` | 类型安全导航（androidx navigation serialization 与 NavGraphNavigator 实读）：route 的基准串就是 serializer.descriptor.serialName；PATH 还是 QUERY 只由 computeParamType 这一个判据决定（CollectionNavType 或 isElementOptional 走 query，其余走 path，多值参数重复同一 key 而不是拼逗号）；RouteEncoder 产出的 argMap 值恒为字符串列表，标量也被包成单元素列表，typeMap 查不到就抛 Cannot find NavType；RouteDecoder 对 store 里没有的元素是**跳过**而不是填 null，只有「在 store 里但取到 null 且字段非可空」才抛 Unexpected null value，这是两种完全不同的失败；导航到 NavGraph 时压栈的是 startDestination 而非图自身，且 startDestination 必须是**直接子节点**（findNode 的 searchParents=false），startRoute 带字面值时会先精确后模式匹配，解析出的参数先写、外部传入后写故外部优先，最后做必填校验 | Python(31 断言实跑) / Kotlin(人工审查) | 2026-09-21 |
| 508 | `04-移动开发/02-Android/NDK与JNI/JNI互调机制/` | JNI 互调（Oracle JNI 规范 Chapter 2 与 OpenJDK jni.h 实读）：符号名 = Java_ 加 mangled 全限定类名 加 下划线 加 mangled 方法名，仅重载追加双下划线与**参数签名**（既不含括号也不含返回类型，故 (D)I 与 (D)Z 在符号层同名）；转义表 下划线1 等于下划线、下划线2 等于分号、下划线3 等于左方括号、下划线0XXXX 用四位小写十六进制；链接时先找短名再找长名，且只有与同库内另一个 native 方法重名才必须用长名；JNIEnv 只在当前线程有效，传给 native 的对象全是局部引用且随方法返回整表失效，全局引用是强引用会阻止 GC 而弱全局不阻止；PushLocalFrame 与 PopLocalFrame 释放整帧只保留 result；JNI_OnLoad 返回 VM 不支持的版本即加载失败 | Python(50 断言实跑) / Kotlin(人工审查) / C(人工审查) | 2026-09-21 |
| 509 | `04-移动开发/02-Android/网络编程/Retrofit动态代理与适配器/` | Retrofit 动态代理（square/retrofit 主干实读）：create 先 validateServiceInterface，非接口报错、接口带类型参数报错且用 ArrayDeque **递归检查所有父接口**（报错信息区分是不是父接口），validateEagerly 时预解析非 default 非 static 非 synthetic 的方法；InvocationHandler 三分支（Object 上的方法走自身、default 方法走 invokeDefaultMethod、其余走 loadServiceMethod 且带缓存）；RequestFactory 的规则全部是硬错误消息（HTTP method 必填与唯一、编码注解只用于带 body 的方法、@Body 与非 body 方法互斥、表单至少一个 @Field、@Path 不能晚于 @Query、URL 的 query 段不能有替换块、@Headers 必须有冒号等），带 body 的方法只有 POST 加 PUT 加 PATCH；suspend 函数先解包 Response 再包装成 Call 并强制补 SkipCallbackExecutor 注解，落三种实现 CallAdapted 加 SuspendForResponse 加 SuspendForBody；nextCallAdapter 的起点是 indexOf(skipPast) 加 1，skipPast 不在列表时 indexOf 返回负一故从头找而不是空集；Platform 按 java.vm.name 分支，Dalvik 上回调投递到主线程 | Python(59 断言实跑) / Kotlin(人工审查) | 2026-09-21 |
| 510 | `04-移动开发/02-Android/数据持久化/Room连接池与ORM映射/` | Room 连接管理（androidx room3-runtime 与 AOSP SQLiteConnectionPool 实读）：ConnectionPoolConfiguration 只两种实现且要求读写数都大于 0，WAL 默认 4 读 1 写；选池四路分支（driver 自带池走 Passthrough、内存库**强制单连接**否则每个连接各是一个库、SingleConnection 与 MultipleConnection 各自建池）；打开流程用文件锁加 isInitializing 双保险，在回调里再用到数据库会立即抛 Recursive database initialization；tryOpenInitialConnection 是失败后忽略、退避 500 毫秒重试、仅当 SQLiteException 且非内存库且开了 allowDataLossOnRecovery 才删库重开；configureDatabase 的顺序是 busy_timeout 加 journal_mode 加 synchronous 加版本迁移，非 WAL 时 journal_mode 是 **TRUNCATE** 而不是 DELETE、synchronous 随之变 FULL，迁移跑在 BEGIN EXCLUSIVE TRANSACTION 里成功 END 失败 ROLLBACK；**非首个连接只做 busy_timeout 加 synchronous 加 onOpen，不再设 journal_mode 也不做迁移**；框架层池的主连接唯一、非主连接超限即关闭、等待队列按优先级降序、忙等待阈值 30 秒只打日志不抛异常 | Python(59 断言实跑) / Kotlin(人工审查) | 2026-09-21 |
| 511 | `04-移动开发/02-Android/UI框架/Compose快照与LazyColumn复用/` | Compose 快照与惰性列表（androidx compose runtime 与 foundation 实读）：状态是带 snapshotId 的 StateRecord 链，读某个快照即取 id 不超过它的最新一条；碰撞写的唯一定义是「自本快照 take 之后有别的状态写在别的快照里被 apply 了」，空修改的 apply 永不失败；innerApplyLocked 对每个被改状态分三支（merge 返回空则整个 apply 失败、返回本快照写入则本地胜出、返回全局最新则本地作废并把该状态移出 modified），而 previous 的 id 恰为 PreexistingSnapshotId 等于 1 时直接跳过冲突判定，这是首次写永远成功的由来；官方注释明确**不保证可串行化**；LazyColumn 侧，空数据集重置滚动、索引越界钳到最后一项并把 offset 归零、间隔只加在非末项、beyondBoundsItemCount 与 pinnedItems 追加测量项；滚动锚点用 key 而不是位置找回，且整段包在 Snapshot.withoutReadObservation 里否则测量自身会登记依赖引发自激重组 | Python(46 断言实跑) / Kotlin(人工审查) | 2026-09-21 |
| 512 | `05-AI与机器学习/07-特征工程与降维/特征选择的稳定性/` | 特征选择稳定性(Nogueira/Sechidis/Brown, JMLR 18(174) 与 Meinshausen-Bühlmann 稳定性选择原文实读):判据不是"跑两次看重合"而是对重抽样求期望;Φ̂ 取每列入选频率的方差除以零模型期望方差再取补,零模型下期望恰为 0 故无需再减基准;Kuncheva IC 做了机会校正但只在"每次固定选 k 个"时有定义,Jaccard 与 Dice 无校正故 H₀ 下的期望随 k 与 d 漂移,三者不可互替;稳定性选择按入选频率筛变量并给出误选数上界 PFER;两篇原文的重抽样口径不同(bootstrap 有放回 n 个 vs subsample 无放回「n/2」个)必须在 README 显式标注。实测:同一数据同一选择器下 k 从 3 增到 30,Jaccard 0.4644 → 0.3510 → 0.4176 会**非单调**回弹,而 Φ̂ 0.5861 → 0.4126 → 0.1727 单调下降(Φ̂ 才是跨 k 可比的那个);常量 k 下逐对 Kuncheva 平均与 Φ̂ 之差为 -2.082e-17(即该设定下两者等价);纯噪声 d=60 k=8 M=80 重复 12 次,阈值 0.75 时稳定性选择 V=0.42 而上界 2.13、阈值 0.90 时 V=0.08 上界 1.33 —— 上界不是紧的;4 列互为副本时 F 值全等 31.028359257590 且每次选出的集合恒为 [2, 3] | Python(52 断言实跑) / Go(人工审查) | 2026-09-21 |
| 513 | `05-AI与机器学习/07-特征工程与降维/幂变换与分位数变换/` | 幂变换与分位数变换(Box-Cox 1964 与 Yeo-Johnson 2000 原式,加 scipy stats/morestats.py 源码实读):Box-Cox 只接受严格正数且 λ=0 时退化为 log,Yeo-Johnson 四分支让负数走 2−λ 镜像;λ 的估计目标是剖面对数似然,scipy 的 *_llf 就是它(丢掉与 λ 无关的常数);**两种 λ 口径必须分清** —— boxcox() 走 MLE 而 boxcox_normmax() 默认走 pearsonr,数值不同;morestats 里 BOUNDS_THRESHOLD = 1e-07 决定 λ 是否被判为 0 或 2(**实测确认该常量就是 1e-07**);分位数变换先估经验 CDF 映到均匀再用目标分布的逆 CDF 映出去,越界值夹到端点。实测:分位数-uniform 的 KS 距离 0.8645(nq=200),分位数-normal 只有 0.8334,参照的精确正态得分(Blom) 0.8612 —— normal 输出偏低的原因是地标位置取 i/(n_quantiles−1) 且两端被夹到 ±5.1993 而非 Blom 位置,nq 越小台阶越粗压得越多(0.8334 → 0.7842);10/10/10 三台阶数据被压成三个值 0.000000 / 0.500000 / 1.000000 且台阶内极差全为 0,quantiles_ 只留 3 个不同地标值 —— 平台在分位数上被压平 | Python(58 断言实跑) / Go(人工审查) | 2026-09-21 |
| 514 | `05-AI与机器学习/07-特征工程与降维/分箱与样条/` | 分箱与样条(sklearn 1.9.1 的 preprocessing/_discretization.py 与 _data.py 与 polynomial.py 源码实读):KBinsDiscretizer 三种 strategy(uniform/quantile/kmeans)与三种编码(onehot 返回稀疏、onehot-dense 返回稠密);SplineTransformer 的基个数 n_splines = n_knots + degree − 1,单位基由 Cox-de Boor 递推给出,五个 extrapolation 模式决定节点外行为,periodic 专治 12/31 到 1/1 的跳变。实测:periodic 时 base[-1]−base[0] 就是周期,1 / −0.5 / 0.5 / 1.5 同余于 0.5 故三行**完全相同**(周期性特征不会在年末年初跳变);periodic 的 n_features_out_ = n_knots − 1(4 结给 3 个特征、5 结给 4 个);knots=quantile 用 np.nanpercentile **默认 linear**,与 KBinsDiscretizer 默认的 averaged_inverted_cdf 不是同一口径,且 base 结允许重复(实测出现 4 个 0.0000);knots=uniform 的端外结按等距外推 | Python(257 断言实跑) / Go(人工审查) | 2026-09-21 |
| 515 | `05-AI与机器学习/07-特征工程与降维/稀疏矩阵降维路线/` | 稀疏矩阵降维路线(sklearn 1.9.1 的 tsvd.py 与 utils/extmath.py 与 decomposition/ipca.py 源码实读):TruncatedSVD **不中心化**,故第一主成分就是数据的均值方向(实测与均值方向的余弦 0.9999999785),代价是不中心化的秩-(k+1) 近似约当于中心化的秩-k;randomized 路线用 Halko 2009 的范围查找器加幂迭代,flip_sign=False 之后手动 svd_flip(u_based_decision=False),X_transformed = X 乘以 components_ 的转置而**不是** U 乘以 Sigma;IncrementalPCA 的 batch_size_ = 5 乘以 n_features(**1.9.1 已从旧的 10 改成 5**),partial_fit 把「S·V 与中心化批次与均值修正」叠起来再 SVD,均值修正项 = sqrt((seen/total)·n_batch) 乘以 (last_mean − batch_mean),均值与方差用 Chan-Golub-LeVeque 的两遍校正公式 | Python(163 断言实跑) / Go(人工审查 + 转写对拍) | 2026-09-21 |
| 516 | `05-AI与机器学习/07-特征工程与降维/时间序列因果特征/` | 时间序列因果特征(pandas 3.0.6 的 rolling 与 window 官方文档、sklearn 1.9.1 的 model_selection/_split.py 源码与 TimeSeriesSplit API 实读):滚动窗口默认右闭故 rolling_mean(x,5)[i] **含 x[i] 本身**,center=True 只把窗口末端右移 (window−1)//2 故偶数窗偏左,closed 四取值各挪不超过 1 格,std 默认 ddof=1 且 window=1 时是缺失而非 0;TimeSeriesSplit 的 gap 是"从每个训练集末端剔除、留在测试集之前的样本数",**完全不移动测试集**故只治标签侧越界;n_splits 不小于 2 的校验在父类 _BaseKFold 的 __init__ 里属构造期。核心方法:把"因果/错位/泄漏"三档用**支持集**(扰动 x[j] 看第 i 行变不变)机械切开 —— 判据是"第 t 行不得依赖 x[j] 且 j 大于 t",故含当期但在 y_t = x_{t+1} 设定下已知的"错位"档未来下标数是 0 即**不是泄漏**,只有 center 与 shift(-1) 是真泄漏。实测:泄漏版在时序 CV 下照样拿 0.8285(切分治不了行内泄漏);随机 KFold 均值 0.5276 对比时序 CV 0.3633;落差最小的恰是泄漏最重的(0.0638 对比因果 0.1755)故"落差小即可信"是错的判据;gap 从 0 到 3 把"标签伸进测试期的训练行数"从 3 降到 0 而 CV R² 几乎不动;E6 探针实测错位档次 0/2838、泄漏 center 264/3102(= 3 种子 × 88,恰为每点贡献 t=j−1 与 t=j−2 两个未来位置) | Python(246 断言实跑) / Go(人工审查 + 3304 项转写对拍) | 2026-09-21 |
| 517 | `06-DevOps/06-SRE与可靠性工程/SLI窗口与聚合口径/` | SLI 窗口与聚合口径(Prometheus 源码 extrapolatedRate 与 OpenSLO v1 规范实读):区间向量是**左开右闭**的,落在左边界上的样本不算;`rate` 与 `increase` 是同一条 factor 只差一次除法,**外推**使整数计数器也能给出非整数结果(实测 2.5);外推阈值 = 平均间隔 × 1.1,超过就退化为半间隔,且 counter 还会被**零点钳制**再压一次(闭式:钳制生效时 increase = 裸差 + 首点值,实测 45 加 10 = 55 而非 67.5);`irate` 只看最后两点且不外推。OpenSLO 侧:rolling 窗口长度恒定、calendar 长度随日历变(1M 在 2 月 28 天、7 月 31 天),同一段 2 小时故障 rolling 30d 要到 4/20 才恢复而 calendar 1M 在 4/1 就清零;`rawType` 写反会让 0.997 的 SLI 被读成 0.003 | Python(64 断言实跑) / Go(人工审查) | 2026-09-21 |
| 518 | `06-DevOps/06-SRE与可靠性工程/错误预算与燃烧率/` | 错误预算与燃烧率(Sloth internal/alert/window.go 与 OpenSLO v1 实读):预算 = 1 减 SLO,99.9% 加 30d = 43.2 分钟;燃烧率 = 错误率除以预算率,**与 SLO 周期无关但阈值与 SLO 有关**(同一个 14.4 在 99% 下阈值 0.144、99.99% 下 0.00144);`getBurnRateFactor` 把「预算百分比 + 长窗」翻译成燃烧率,30d 得 14.4 / 6 / 3 / 1,**28d 整体按 28/30 缩放**(13.44 / 5.6 / 2.8 / 28 分之 30);烧光时间有两条独立推导且必须合上。OpenSLO 三种 budgetingMethod 的差别是**加权方式**:同一份数据(11 个 1/2 切片加 1 个 1000/1000)给出 0.989237 / 0.083333 / 0.541667;三者收敛的唯一条件是切片比值只取 0 或 1,**不是流量均匀** —— 同样 99% 摊到每个切片会让 Timeslices 跳到 1.0 | Python(79 断言实跑) / Go(人工审查) | 2026-09-21 |
| 519 | `06-DevOps/06-SRE与可靠性工程/多窗口多燃烧率告警/` | 多窗口多燃烧率告警(Sloth alert_rules_v1 模板与 google-30d/28d YAML 实读):表达式是「(短窗 and 长窗) or (短窗 and 长窗)」—— 短窗与长窗是 **and**(只破一个不告警),quick 与 slow 是 **or**,右侧阈值是「燃烧率 × 错误预算率」。短窗的作用不是更灵敏而是**叫停**:10 分钟全站故障后,MWMB 在第 13 分钟停止而「只看 1h 长窗」的静态阈值要拖到第 68 分钟(差 55 分钟);负控是把短窗拉长到与长窗同宽,MWMB 立刻退化回 68。慢燃侧:持续 0.12% 错误率(燃烧率 1.2、25 天烧光预算)在静态 1% 阈值下**完全漏报**,只有 ticket_slow 档抓得住。分界点可手算:10 分钟故障要 r 大于 0.0864 才 page,且比较是严格大于,恰好等于阈值时不告警 | Python(67 断言实跑) / Go(人工审查) | 2026-09-21 |
| 520 | `06-DevOps/06-SRE与可靠性工程/退避与抖动/` | 退避与抖动(AWS 官方模拟器 aws-arch-backoff-simulator 源码与同名博客实读):四种策略逐行转写,`expo(n) = min(cap, 2^n × base)`,AWS 实际取 base=5ms、cap=2000ms;**纯指数退避一点工都不省** —— 100 客户端竞争下工作量与完全不退避同为 5050(闭式 N(N+1)/2),耗时却长 94 倍(186530ms 对 1980ms);加 jitter 后降到 454 / 387 / 367。Decorrelated Jitter 是**有状态**的,n 被完全忽略(同种子同状态下 backoff(0) 与 backoff(99) 相等),且下界恒为 base 故**非单调**(30 次里有 7 次下降)。模型必须做时间槽离散化,否则连续随机永不相等、四种策略会给出同一个数字 199 | Python(61 断言实跑) / Go(人工审查) | 2026-09-21 |
| 521 | `06-DevOps/06-SRE与可靠性工程/过载保护与自适应并发/` | 过载保护与自适应并发(Envoy 官方文档实读):梯度控制器 `gradient = (minRTT + B) / sampleRTT`、`B = minRTT × buffer_pct`、`limit_new = gradient × limit_old + headroom`,headroom **不可配置**且等于 sqrt(limit);**buffer 是百分数不是小数**,按字面实现会让 B 变成 minRTT 的 10 倍、梯度恒大于 1、并发放大到天文数字。梯度小于 1 时有稳态闭式 `L = 1/(1−g)²`(实测 g=0.916667 时闭式 144 与 400 步迭代吻合到 6.9e−06);headroom 恒为正故**过冲后阻尼收敛**(峰值 140.47 高出稳态 140.149 约 0.33),不是单调爬升。minRTT 要连续 5 个窗口踩到下限(缺省并发 3)才重算,期间会有 503,jitter=0 会让 20 个 host 全部同时进入测量窗口。Overload Manager 侧:threshold 是**严格大于**的方波,scaled 是三段斜坡;cgroup 没配内存 limit 时压力恒为 0 | Python(68 断言实跑) / Go(人工审查) | 2026-09-21 |
| 522 | `07-数据存储/01-关系型/ShardingVitess/` | 分片与路由键空间（Vitess 官方《Sharding》文档与 `go/vt/vtgate/vindexes` 四个 vindex 源码逐行转写）：键范围是**含起点不含终点**的区间，空端点代表最小/最大值；键**左对齐**使右侧 0 可省，故 `0x80` 是 64 位键空间的正中值（实测 `0x8000` 与 `0x80` 同值，落在 `80-` 而非 `-80`）；完整分区必须首尾各有一个空端且首尾相接，不等分的五分片 `-80 / 80-c0 / c0-dc00 / dc00-dc80 / dc80-` 也合法。核心对照：`numeric` 是**位模式**映射（`BigEndian.PutUint64`），10000 个连续 id **全部**落 `-80` 一个分片；`reverse_bits` 把最低位搬到最高位，**按 id 奇偶精确对半 5000/5000**（闭式：`first_byte = reverse(low_byte)`，最高位即 id 的最低位）。`Cost()` 官方给 binary/numeric 0、hash/reverse_bits 1；`ReverseBits` 的 var 块没有 `Sequential` 故无 `RangeMap`，`BETWEEN` 只能退化为 scatter（实测 4 分片 fan-out 从 1 跳到 4）。resharding 时 `keyspace id` 不变、只有边界动：`80-c0` 切成 `80-a0` 加 `a0-c0`，`0x8000000000000123` 落到 `80-a0`、`0xa000000000000001` 落到 `a0-c0`。非 2 的幂分片数直接报错而非编造（官方示例全按二进制位等分） | Python(73 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-21 |
| 523 | `07-数据存储/01-关系型/ExplainAnalyze/` | EXPLAIN ANALYZE 读数口径（PostgreSQL 18 官方文档《Using EXPLAIN》14.1 实读）：`actual rows` 与 `actual time` 都是**每次执行的平均值**，要乘 `loops` 才是总量——文档原例内层 `rows=1.00 loops=10`，总行数 10、总耗时 0.030 ms（不是 0.003）。cost 算式逐条复原：`seq_scan_cost(345, 10000) = 345×1.0 + 10000×0.01 = 445`，加 `WHERE` 后是 `445 + 10000×cpu_operator_cost(0.0025) = 470`，**cost 不降反升**。`Rows Removed by Filter` 只在至少拒绝 1 行时出现（实测 removed=0 时该行不渲染）。三个「像 bug 却不是」的陷阱：BitmapAnd/BitmapOr 的 `actual rows` **恒为 0**（即使真的产出了 40 行）；`LIMIT` 下子节点估算按跑到底显示 10 行而实测 2 行，**不是估算错误**；merge join 外层重复键让内层被回溯重扫，实测 100 行的关系被报成 300 行。估算误差口径是 `est / (rows × loops)`，实测低估时 1/7000、高估时 1000 倍；估算或实测为 0 时不做比值 | Python(33 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 524 | `07-数据存储/01-关系型/ParallelQuery/` | 并行查询 worker 数（逐行转写 `allpaths.c` 的 `compute_parallel_worker`）：公式是「阈值三倍三倍涨」的 while 循环，`workers` 从 1 起、`threshold` 从 `min_parallel_table_scan_size` 起，每满足 `pages >= threshold×3` 就 `workers++` 且 `threshold×=3`；实测堆 1024 页给 1、3072 页给 2、9216 页给 3、100000 页给 5，索引侧阈值 64 页同样算法给 7，两者**取小**。BASEREL 有下界门槛：1023 页直接返回 0，但继承子表/分区（非 BASEREL）不受限仍给 1。表级 `parallel_workers` reloption **优先于一切**且跳过下界门槛，只再被 `max_workers` 截断。反直觉结论：**默认配置下 worker 数几乎恒为 2**，因为 `max_parallel_workers_per_gather=2` 在 24MB 以上就把公式结果全压平。代价模型 `Gather = 1000(setup) + 并行部分/(workers+1) + rows×0.1`，216018 的串行代价在 2 worker 下是 73006；leader 不参与时分母变成 workers。不可并行的四条硬约束（文档 15.2）：写数据、可挂起（CURSOR / PLpgSQL FOR）、`PARALLEL UNSAFE` 函数（**用户自定义函数默认 UNSAFE**）、并行内嵌套 | Python(38 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 525 | `07-数据存储/01-关系型/AutovacuumXID/` | autovacuum 阈值与事务 ID 回绕（逐行转写 `autovacuum.c` 的 `relation_needs_vacanalyze` 与 `varsup.c` 的 `SetTransactionIdLimit`）：`vacthresh = 50 + 0.2×N`（超过 1 亿被 `autovacuum_vacuum_max_threshold` 封顶，实测 10 亿行表停在 1 亿）、`anlthresh = 50 + 0.1×N`、`vacinsthresh = 1000 + 0.2×N×未冻结比例`；判据是**严格大于**（250 不触发、251 触发）。insert 阈值的分母是 `pcnt_unfrozen = 1 - relallfrozen/relpages`（`relallfrozen` 先被 clamp 到 `relpages`），全冻结表退化成纯 1000。防回绕是**独立通路**：`age(relfrozenxid) > 2e8` 直接 `dovacuum=true`，**即使 autovacuum 关闭也照跑**（实测 av_enabled=false 时强制仍为 true，而普通死元组触发被跳过）。XID 四线：`wrap = oldest + (MaxTransactionId>>1)` 即 +2147483647、`stop = wrap - 300万`、`warn = wrap - 1亿`、`vac = oldest + 2亿`；剩余百分比分母是 `MaxTransactionId/2`，warn 点约 4.66%、stop 点约 0.14%。静态表强制 vacuum 间隔 ≈ `2e8 - 5e7 = 1.5e8` 个事务 | Python(48 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 526 | `07-数据存储/01-关系型/PartitionPruning/` | 声明式分区与分区裁剪（PostgreSQL 18 官方文档 5.12 与源码 `partprune.c` 实读）：RANGE 边界**含下界不含上界**，文档原例「1 到 10 与 10 到 20 两个分区，值 10 属于第二个」；因此 `BETWEEN 1 AND 10` 是闭区间反而**两个分区都留**。裁剪由**分区键的隐式约束**驱动、与索引无关。三阶段：计划期裁掉的分区根本不出现在计划里；执行期初始化裁掉的只留 `Subplans Removed: N` 且**仍会被加锁**；执行期逐次裁剪要看 `loops` 差异，全没跑的子计划显示 `(never executed)`（实测外值全在第一个分区时第二个 loops=0）。方式差异：HASH 等值能精确定位（每个值恰好落 1 个分区）但**区间完全裁不掉**（`BETWEEN` 命中全部 4 片）；LIST 的 **DEFAULT 分区永远排除不掉**（`city='BJ'` 结果是 east 加 other）。`IS NULL` 走 `MATCH_NULLNESS`，只有无下界范围分区或含 NULL 的 LIST/DEFAULT 存活；`x>15 AND x<5` 触发 `MATCH_CONTRADICT` 全裁 | Python(52 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 527 | `08-安全/02-网络安全/TCP盲注入与RFC5961/` | TCP 盲重置与盲注入(RFC 5961 与 Linux tcp_input.c 实读):RST 从「窗口内即有效」收紧成「必须精确等于 RCV.NXT」三分支,窗内非精确只发 challenge ACK、窗外静默丢弃;**SYN 恰恰相反,不管序列号落在哪都发 challenge**(RFC 5961 明文 irrespective of the sequence number)。内核额外放宽两条:seq 等于 rcv_nxt 减 1 且状态属 CLOSE_WAIT 加 LAST_ACK 加 CLOSING 三态时直接 reset、seq 等于最右 SACK 块的 end_seq 时直接 reset。数据注入侧把 ACK 判据从 SND.UNA 减 2 的 31 次方减 1 收紧到 SND.UNA 减 MAX.SND.WND,可接受取值从 2147483648 降到 65537。challenge ACK 限速是两级:先 per-socket 的 invalid_ratelimit(严格小于),再 per-netns 的 challenge_ack_limit,而后者**每秒复位成区间 half 到 limit 加 half 减 1 里的随机值**,故单秒上限是 limit 加 half 减 1 而不是 limit(limit=1000 时可达 1499)。难度实测:窗口 32768 从 65536 包抬到 2 的 31 次方,数据注入恒为 RST 的两倍故 RFC 定为 MAY。另记录 RFC 5961 §3.2 第 1 条把窗外写成 SEQ 小于等于 RCV.NXT、与第 2 条在 SEQ 等于 RCV.NXT 处重叠的文本不自洽 | Python(68 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-21 |
| 528 | `08-安全/02-网络安全/DNS缓存投毒与RFC5452/` | DNS 伪造应答的组合难度(RFC 5452 §7.2 实读):P_s 等于 D 乘 F 除以 N 乘 P 乘 I,P_cs 等于 1 减 (1 减 P_s) 的 A 次方,A 等于 T 除以 TTL;代入 D=1 加 W=0.1 加 N=2.5 加 P=1 加 I=65536 后化简分母恰为 **1638400**。文档给出的数字全部复现:TTL=3600 配 R=7000 pps 在 24 小时与 7 天分别得 9.77% 与 51.3%,TTL=60 在 24 分钟、3 小时、9 小时得 9.77%、53.7%、90.1%;且 TTL=3600 配 24h 与 TTL=60 配 24min 的 A 同为 24 故概率恒等 —— **降 TTL 反而加速被投毒**。三处文档口径不一致记录不改正:§8.1 的「7 秒 / 116 小时」按 N=1 才对得上(取 §7 默认的 2.5 则是 16.2 秒 / 288 小时);§7 的「4 Mbit/s 达 50%」是线性近似,用 §7.2 精确公式反解需 5.89 Mbit/s;§8 的「285 Gb/s」等于 4.45 Mbit/s 乘 64000,与单次窗口的 416 Mbit/s 差两个数量级故不是从它推出来的 | Python(44 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 529 | `08-安全/02-网络安全/证书透明度Merkle审计/` | 证书透明度 Merkle 审计(RFC 9162 §2.1 实读):MTH 用 0x00 与 0x01 做叶子与节点的域分隔,k 取**严格小于 n** 的最大 2 的幂。用 §2.1.5 的 7 叶树做向量,四个包含性证明与三个一致性证明全部与文档逐字节一致;验证端靠 fn 与 sn 双游标,fn 等于 sn 这个额外条件专门处理非满树最右路径上「没有兄弟」的节点;一致性验证时若 first 是 2 的幂要先补 first_hash,这正是 PROOF(4, D7) 只有一个节点却能通过的缘由。SUBPROOF 的 m 大于 k 分支强制把 b 置为 false,否则最内层不会提交整棵子树哈希。栈式树头算法(§2.1.2)与递归定义在 n=0..20 全部一致;结构编码里 NodeHash 是 1 字节长度前缀而 sth_extensions 是 2 字节,两者位数不同 | Python(675 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-21 |
| 530 | `08-安全/02-网络安全/SSH传输层与密钥交换/` | SSH 传输层(RFC 4253 与 RFC 8308 实读):二进制包的 padding 要满足对齐单位为 max(cipher block, 8)(**流密码也必须按 8 对齐**)、padding 在 4 到 255、整包不小于 16 三条,最小 padding 有闭式且可用「再减一块就小于 4」判定最小性。**KEX 与 cipher 的选法不同**:KEX 取客户端列表里第一个同时满足「服务端也支持」加「主机密钥具备所需能力」的算法,故 rsa2048-sha256(需加密型密钥)在 ed25519 下被跳过而在 ssh-rsa 下被选中;cipher 只看名字。六把密钥由 HASH(K 加 H 加字母加 session_id) 派生,超长时链式扩展且**扩展是追加式**(64 字节版的前 32 字节等于 32 字节版);session_id 是首次 KEX 的 H 且终身不变,而密钥随每次 KEX 的 H 变。RFC 8308 的 ext-info-c 与 ext-info-s 名字故意不同以保证永不匹配成 KEX 方法,加进列表不影响协商结果;被协商成 KEX 方法时双方必须断开 | Python(401 断言实跑) / Go(人工审查 + 三项静态检查,拆 wire.go 后全过) | 2026-09-21 |
| 531 | `08-安全/02-网络安全/IPv6隐私地址与SLAAC/` | IPv6 临时地址(RFC 4941 与 RFC 8981 实读):RFC 4941 取 MD5 的**左** 64 位并把 bit 6 清成 0(U/L 位表示本地),冲突时把 history 换成 MD5 的右 64 位重来,右 64 位同时是下一轮的 history;RFC 8981 改用带密钥 PRF(RID 加六个输入)且**从最低有效位开始取**,不再动 U/L 位(RFC 7136 说 IID 里没有特殊位),冲突靠 DAD_Counter 加 1。生命周期里 **valid 取 min(前缀, TEMP_VALID_LIFETIME) 而 preferred 取 min(前缀, TEMP_PREFERRED_LIFETIME 减 DESYNC_FACTOR)** —— 只有 preferred 减;是否创建用的是**严格大于** REGEN_ADVANCE(默认 5 秒),恰好等于时不创建。默认参数:2 天 / 1 天 / 重试 3 次 / REGEN_ADVANCE 等于 2 加 3 乘 1 乘 1000 除以 1000 等于 5 秒 / MAX_DESYNC_FACTOR 等于 0.4 乘 86400 等于 34560 秒。特例:Preferred Lifetime 为 0 的 RA 让地址 deprecated 但不得触发新建 | Python(65 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-21 |
| 532 | `09-语言学习/Golang/定时器与四叉堆/` | 定时器四叉堆(runtime/time.go 实读):per-P 堆是**四叉**的(timerHeapN=4,parent=(i-1)/4、孩子 4i+1..4i+4),85 个节点刚好填满 4 层;堆里存的是 timerWhen 的**时刻快照**而非 t.when,Reset 走 modify 只置 Modified 位、快照延后到 updateHeap 或 adjust 才同步;Stop 只置 Zombie 并从不清堆(pending 取的是清零**之前**的 t.when>0),被 Reset 会复活(zombies 减回去);cleanHead 先零成本摘堆尾 zombie 再处理堆顶,updateHeap 里只有 siftDown(0) 所以「改早」必须靠 adjust 扫全堆后 initHeap 重建;ticker 重排是 next = when + period*(1+delay/period) 的整数除法,when=10 period=5 now=27 得 30 而非 15,错过的拍不补发;一次性定时器触发时瞬时置 zombie 后立刻被 deleteMin 摘除(净变化 0)。实测同数据下四叉堆上浮次数约为二叉堆的 0.57 倍 | Python(100 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-21 |
| 533 | `09-语言学习/Golang/netpoll与网络轮询器/` | netpoll 的 rg/wg 二值信号量(runtime/netpoll.go 实读):取值只有 pdNil/pdReady/pdWait/G 指针四种,同一方向二次等待直接 throw double wait;netpollblock 三步 CAS 后 gopark,而 **gopark 之后的代码只在被唤醒后才执行**,所以「信号量仍是 G 指针」= 还没人唤醒而非数据损坏;unblock 的 ioready 决定是否置 pdReady,因此**超时唤醒后是 pdNil 不是 pdReady**、netpollblock 返回 false 由上层查错误码;delta 只在换出的是 G 指针时减 1,换出的是 pdWait(对方尚未 commit)则不动;pdNil 且非 ioready 时什么都不做(超时不凭空造通知);deadline 每次修改都 rseq++ 让旧 timer 事件按 seq 比对失效;netpollBreak 用 WakeSig 的 CAS(0,1) 去重;int64 溢出取 1<<63-1(Python 无回绕,模型里必须显式模拟) | Python(72 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 534 | `09-语言学习/Golang/栈增长与连续栈/` | goroutine 连续栈(runtime/stack.go 与 proc.go 实读):stackMin=2048、fixedStack 把 stackMin+stackSystem **上取整到 2 的幂**(linux 2048、windows 8192、plan9 4096)、stackGuard = 800×multiplier + stackSystem + 128(linux 928 / windows 5024);newstack 先翻倍,再按 needed = funcMaxSPDelta + stackGuard 与 used = hi-sp 比较,**不够就继续翻倍**(不是「一定翻一倍」),判据是严格小于所以恰好相等时不翻倍;上限 maxstacksize 是十进制 1e9(64 位)/2.5e8(32 位),ceiling 是 2 倍;stackguard0 还有 stackPreempt/stackFork/stackForceMove 三个哨兵值(0xfffffade 等),等于 stackPreempt 时走的是**抢占分支**不是增长分支;收缩只在 GC 时发生且要求 used(= hi-sp 再加 stackNosplit) 严格小于 avail 的四分之一,下限是 fixedStack 所以 windows 下 8192 的栈永不收缩 | Python(64 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 535 | `09-语言学习/Golang/字符串与UTF8rune/` | UTF-8 查表解码(unicode/utf8 实读):first[256] 的高 4 位是 acceptRanges 下标、低 4 位是序列长度,0xED 走 acceptRanges[2]={0x80,0x9F} 拦代理区、0xF4 走 acceptRanges[4]={0x80,0x8F} 拦超上限、0xE0 走 [1]={0xA0,0xBF} 与 0xF0 走 [3]={0x90,0xBF} 拦 overlong —— **合法性主要由第二个字节决定**;非法与截断一律返回 (RuneError, 1),只有空输入是 size=0(对比 Python 直接抛 UnicodeDecodeError);RuneLen 的代理区判断夹在 rune2Max 与 rune3Max 之间,U+D800 返回 -1 而不是 3;越界或代理区的 EncodeRune 写 EF BF BD;RuneCountInString 官方实现就是 for range,所以每个非法字节各算 1 个 rune。自检把全部 1114112 个合法码点与 Python codec 逐字节对拍 | Python(88 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 536 | `09-语言学习/Golang/方法集与嵌入提升/` | 方法集与选择器(Go 规范 §Method sets 与 §Selectors 实读):T 的方法集只含值接收者方法、*T 含两种,故 **T 的方法集是 *T 的子集**;接口方法集是类型集方法集的**交集**;嵌入 T 时 S 只拿到接收者为 T 的方法而 *S 两种都拿,嵌入 *T 时 **S 与 *S 两种都拿**;选择器取**深度最浅且唯一**的 f,最浅层有多个即非法(字段与方法共用名字空间),自己声明的会遮蔽提升的;定义型指针 Q=*T2 只对**字段**开后门,所以 q.x 合法而 q.M0() 非法(规范把它列在 invalid 之下)。官方 T0/T1/T2 示例实测:T2 方法集 = {M0, M1},*T2 = {M0, M1, M2};t.z 深度 0、t.y 与 t.x 深度 1 | Python(47 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 537 | `10-逆向工程/03-协议逆向/Prospex会话级状态机/` | Prospex（IEEE S&P 2009）会话级协议规范抽取：消息类型聚类用三组特征（direction 与 keywords、funcs 与 syscalls、fileops）各占三分之一且组内等权，所以**只改方向**的两条报文距离恰好是六分之一；聚类用 PAM（BUILD 阶段贪心挑代表点、SWAP 阶段反复尝试用非代表点换掉代表点直到代价不再下降），k 由**泛化 Dunn 指数**定，分子取 single-linkage 的簇间分离度、分母取基于 RNG（Relative Neighborhood Graph，两点成边当且仅当不存在第三点使两段距离都更小）的簇内直径，比原始 Dunn 指数抗离群点；状态机推断把会话写成消息类型序列，用 APTA 加前置条件标注造带标签状态树，再交 Exbar 求最小一致 DFA，产物可导出成 Peach fuzzer 的 XML | Python / Go | 2026-09-21 |
| 538 | `10-逆向工程/03-协议逆向/CRC参数反解/` | CRC 是六参数族（width、poly、init、refin、refout、xorout），本 demo 与 RevEng 官方目录全部 113 个模型的 check 值逐一对拍。**最容易写反的一条：寄存器永远左移**，refin 反射的是输入字节与 init，不是把寄存器改成右移——按右移写 CRC-32 会算出 0xFC891918 而正确值是 0xCBF43926，差得毫无规律；反解第一步用**等长差**消掉 init 与 xorout（CRC 对 init 是线性的，等长两条报文的 crc 异或与两者都无关、只由 poly 决定）；参数解**不唯一**，所以断言只能落在函数等价上而不能落在具体参数上 | Python(656 断言实跑) / Go | 2026-09-21 |
| 539 | `10-逆向工程/03-协议逆向/原子语义检测器/` | BinPRE（CCS 2024）Table 2 的可执行版：五个语义类型（Static / Integer / Group / Bytes / String）加六个语义功能（Command / Length / Delim / Checksum / Filename / Aligned），每个原子检测器只看两样东西——字段的指令算子序列 I(f) 与取值 V(f)。**functional operation 定义为「算子不属于 mov 系列」，所以 cmp 也算**，这条口径必须分清：Aligned 要求不涉及任何 functional 操作故有 cmp 就不是 Aligned，而 Static 只要求没有额外的 functional 操作故有 cmp 仍然是 Static | Python(42 断言实跑) / Go | 2026-09-21 |
| 540 | `10-逆向工程/03-协议逆向/语义精化与类型约束/` | BinPRE §3.5 语义精化的三段流水线：① Format-based clustering（Algorithm 2）遍历所有字段位置当候选 command field，用 NW_f（在**字段边界序列**而非字节序列上跑 Needleman-Wunsch）给每种分法打分，取最高分的那个位置；② Entropy-based type refinement（§3.5.2）算簇内每个字段的 Shannon 熵并取**中位数**作判据，Static 的熵必须小于中位数、Bytes 必须大于中位数，否则该推断被撤掉，类型未知的字段取簇内熵最接近者的类型；③ Type-based function refinement（§3.5.3）用 Table 3 的「功能到类型」约束清掉不合理的功能推断（例如 Length 却不是 Integer） | Python(55 断言实跑) | 2026-09-21 |
| 541 | `11-性能分析/03-基准测试方法论/JMH-gc与benchmem分配口径/` | JMH `-prof gc` 与 Go `-benchmem` 的分配口径对拍（GCProfiler.java 与 testing/benchmark.go 实读）：JMH 的 `gc.alloc.rate.norm` 是**浮点**且**只在 allocated 不等于 0 时发射**——零分配时整列消失而不是显示 0，Go 侧一律打印 `0 B/op`（整数截断，亚字节分配报 0）；JMH 优先用 `getTotalThreadAllocatedBytes` 的**进程级**口径，回退 per-thread 时**刻意跳过当前线程**且漏掉两次快照之间创建又死亡的线程，Go 只算 `StartTimer` 到 `StopTimer` 的窗口故停表后的分配完全不计；`gc.count` 用 SUM 而 `alloc.rate` 用 AVG ⇒ 两轮后 count=2、norm=6.5 而加权真值是 3.636，**聚合后的两个数字不可相除**；`gc.time` 只在次数或时间变化时才发射；churn 是净回收量且只在 c 大于 0 时累加，默认关闭、churnWait 默认 500ms；JMH **没有对象数**口径 | Python(40 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-21 |
| 542 | `11-性能分析/03-基准测试方法论/预热充分性判定/` | 预热充分性的程序化判定（pyperf `_worker.py` 实读）：把 nwarmup 之后的值**对半分**成 sample1 与 sample2，五条不等式全过才算预热够了——离群检验**只查最大值**且计算 iqr 时**刻意不含首值本身**（所有值相等时 iqr=0，首值只高 0.0001 也算离群）、`mean_diff` 区间**非对称**为负 0.5 到正 0.10（慢 11% 被拦而快 40% 却被 mean 这条放行，最终由 q1/MAD 拦下）、离散度用 **MAD** 而非 stdev（给 20 个数里某个加 500，MAD 不变而 stdev 放大 77 倍）；校准循环只补算缺失的值故采样总数恒为 nwarmup 加 40，`MAX_WARMUP_VALUES=300` 到顶即放弃；纯噪声无升温时判据自身抖动会把 nwarmup 推到 24；对照组 JMH 只有固定 `WARMUP_ITERATIONS=5`，同一条升温曲线上**欠预热 216%** 而校准值 17 次只差 2.1% | Python(35 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 543 | `11-性能分析/03-基准测试方法论/趋势存储与分片/` | 基准趋势的两套存储模型对拍（treeherder 与 catapult 实读）：Perfherder 用 40 字符 SHA-1 的 `signature_hash`，算法是 `sha1("".join(sorted(keys + str_values)))`——**键与值被放进同一个桶一起排序**，于是取值在属性间对调（suite 与 test 互换）或键当值、值当键（`{"a":"1"}` 与 `{"1":"a"}`）都会碰撞，只适合做主键不适合做校验；非字符串 value 走 `json.dumps(sort_keys=True)` 但**列表顺序不被排序**，官方在调用侧补了 `sorted`，少这一次就会把同一组选项裂成两个系列；summary 只在 suite 带 value 时才建，subtest 属性里注入 `parent_signature`。Catapult 侧 **TestMetadata 的主键就是完整路径**，`bot` 只在路径恰好 3 段时非空、`parent_test` 在少于 4 段时为 None 否则取去掉末段，**父子关系由路径前缀推导没有外键**；`Row` 主键含 `revision = key.integer_id()` 故同一 revision 重复上报是覆盖而非报错，X 轴必须是单调整数；`LastAddedRevision` 被单独拆成实体以避免 datastore 写热点 | Python(54 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 544 | `11-性能分析/03-基准测试方法论/容器与虚拟化下的标定/` | 容器与虚拟化对 `-benchtime` 标定的影响（cgroup.go、testing/benchmark.go、proc.go、proc.rst 实读）：CFS 带宽下跑完 R 的 CPU 工作所需墙钟是 `(k-1)P + (R-(k-1)Q)`，k 为 R/Q 上取整——**前 Q 的 CPU 时间是免费的**，于是 50ms 以内完全不受影响、51ms 的活墙钟跳到 101ms（配额边界是悬崖）、1s 时吞吐降到 0.513、足够长时趋近 Q/P。**关键结论是限流不改标定出的 N 却把 ns/op 放大约 P/Q 倍**（实测 N 都是 1000000 而 ns/op 从 1000 变 1950），因为 `predictN` 只看墙钟而 `min(n, 100*last)` 与 `max(n, last+1)` 两道钳制把收敛点钉住了；缩短 benchtime 到 10ms 可绕开偏差（ns/op 回到真值 1000）。`/proc/stat` 的 steal 定义只有一句 involuntary wait，它是**乘性且无量化**的（短任务也照吃），与 cgroup 配额**串联叠加**（50% 限流加 10% steal 得 2166）。另：`sysmonUpdateGOMAXPROCS` 里手动设过 GOMAXPROCS 就永久关掉容器自适应 | Python(45 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 545 | `11-性能分析/03-基准测试方法论/参数化基准的多维比较/` | 参数化基准的多维公平比较（hyperfine 三个源文件与 JMH Param.java 实读）：hyperfine 默认以 `fastest_of`（**全局最快**）为参考，比值恒 ≥ 1，在多维扫描下这个比值会把各维度混成一个数——实测 1 线程 large 的 10 倍等于 size 的 4 倍乘 threads 的 2.5 倍，无法分离；公平做法是**固定其它维度后在组内各自选参考**，分组后 large 恒为 small 的 4 倍且两组一致。`RangeStep` 的三条拒绝是 end 小于 start、step 为 0、规模超 `MAX_PARAMETERS=100_000`；`size_hint` 用 `(end - start + 1) / step`——**加的是绝对值 1 不是一个 step**，于是整数步长低估（0..10 step 3 报 3 实际 4）、小数步长高估（0..1 step 0.1 报 20 实际 11），低估的代价是 0..300000 step 3 的 size_hint 恰好 100000 通过检查却实际产生 100001 个值。相对速度的 σ 走误差传播且源码注明**假定协方差为 0**（同机相邻两次并不独立，故偏乐观）；`mean == 0` 时非参考给 INFINITY、参考自己给 1.0。JMH 侧多参数走**外积**（2 乘 3 得 6），`@Param` 只能放 `@State` 的非 final 字段且在任何 Setup 之前注入，**Enum 是唯一有隐式默认值**的类型（取全部枚举常量） | Python(37 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-21 |
| 546 | `01-游戏开发/06-AI/行为树/执行语义/` | BehaviorTree.CPP 控制节点与装饰器的逐 tick 执行语义（master 分支 12 份头文件与 cpp 实读）：Sequence 与 Fallback 靠 RUNNING 索引停在原地而 ReactiveSequence 每个 tick 从头重评估、halt 只作用于 RUNNING 的孩子且 SKIPPED 不进 halt 列表；**Parallel 默认 failure_threshold_ 为 1**，判负式是「子节点总数减已失败数小于所需成功数」，于是三子节点组在第一个孩子失败后立刻判负，第二第三个孩子**一次都不会被 tick**（这是默认参数下最反直觉的行为）；阈值取负值时语义翻转为「至少 N 个才算成功」，且 SKIPPED 会被计成一张成功票；Inverter 只翻 SUCCESS 与 FAILURE，RUNNING 与 SKIPPED 原样透传；Repeat 的 NUM_CYCLES 在成功一轮后自增并与入参比较，失败则立刻返回并重置计数 | Python(54 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-22 |
| 547 | `01-游戏开发/06-AI/GOAP规划/` | ReGoap 的**反向**目标导向规划（ReGoapNode.cs 与 AStar.cs 与 ReGoapPlanner.cs 实读）：搜索从 Goal 出发反向展开动作的前置条件，节点里存的是「还需满足的世界状态差」而不是已执行的动作序列，到 IsGoal 时沿父链回溯出正序动作表；Expand 使用的是**父节点的** Goal 而非初始目标；启发式取「缺失差异」的条目数；`ReGoapPlannerSettings` 默认 PlanningEarlyExit 为 false、MaxIterations 1000、MaxNodesToExpand 10000、WeightedRandomMinimumWeight 0.001，故默认解是代价 3 的 GetAxe 然后 ChopWood 而早期退出下先拿到代价 5 的 BuyWood；重复状态检测用 break 而非 continue 故入队数随动作声明顺序变化；空计划与无计划在 API 上不可区分；SelectNextGoal 用加权随机，权重为零的目标仍以 0.001 参与抽取 | Python(29 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-22 |
| 548 | `01-游戏开发/06-AI/效用系统/` | big-brain 的 Scorer 到 Measure 到 Picker 三段效用决策（scorers.rs 与 measures.rs 与 evaluators.rs 与 pickers.rs 实读）：Score 约束在 0 到 1 之间且 set 越界会 panic；all_or_nothing 见零即零、sum_of_scorers 求和后封顶、product_of_scorers 用补偿公式抵消项数影响；**SigmoidEvaluator 默认 k 为负 0.5 时并不是 S 形曲线**，f(0)=1、f(0.25)=0（除以零得正负无穷后被 clamp）、f(0.5)=0.5、f(0.75)=0.875，第二个奇异点在输入 1.25 处已超出截断范围故不可达；two_over_dx 的公式里写的是 ya 而非 yb 属官方潜在笔误但公有构造器走不到；WeightedProduct 折叠初值为 0 故输出恒为 0；FirstToScore 用大于等于而 HighestToScore 用严格大于；Thinker 每帧重算以支持改变主意并提供 otherwise 兜底 | Python(58 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-22 |
| 549 | `01-游戏开发/06-AI/寻路算法/跳点搜索JPS/` | 跳点搜索的在线图剪枝（Harabor 与 Grastien AAAI 2011 全文实读）：先按父节点方向做邻域剪枝（式 1 直线与式 2 对角），只在存在 forced neighbour 时保留否则剪掉；jump 沿方向递归直到撞墙、到达目标或发现跳点，对角方向先递归两个直线分量；identify_successors 只把跳点入堆，堆里存的是跳点而非格子。路径代价与 A\* **完全相同**（保最优），展开节点数却大幅下降——15×15 开阔图 1 对 14、30×30 开阔图 1 对 29、30×30 百分之十五随机障碍 48 对 78、12×12 带缝墙 2 对 9、40×40 百分之二十障碍 81 对 123；代价口径用八方向 octile 距离 | Python(30 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-22 |
| 550 | `01-游戏开发/06-AI/状态机/层级状态机HFSM/` | SCXML 附录 D 的 microstep 解释算法（W3C Recommendation 全文实读）：配置集是活跃原子状态的集合且**不含 scxml 容器本身**；isDescendant **排除相等**故父状态不会被自己退出；选择转移时按文档序排序且后代优先于祖先、并行分支中互不相关的并发源各自独立；转移域对内部转移取 LCCA 而对外部转移取 LCCA 的真祖先，于是外部转移会先退出源状态自身再重新进入；**历史值必须在执行 onexit 之前统一记录完**（与退出合并成单个循环会得到空历史），深历史收集全部活跃后代而浅历史只收集直接子状态；entryOrder 按文档序、exitOrder 为其逆序 | Python(39 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-22 |
| 551 | `02-Web开发/03-数据库/SQL与ORM/ORM会话与工作单元/` | ORM 会话与工作单元(SQLAlchemy 2.0 官方源码实读):身份映射是**弱引用**的(对象被 GC 后 `key in identity_map` 直接为 False,官方连「GC 把键带走」都用 try/except KeyError 兜住),同键已有另一个活对象时 `add()` 抛 InvalidRequestError 而同一个 state 重复登记返回 False,`replace()` 静默换掉不抛;**它不是查询缓存**(官方 FAQ 原话 Yeee…no,select 永远打库,只有按主键 get 才查身份映射)。脏检查只比**已提交快照**,所以改回原值不产生 UPDATE。flush 三层顺序:①整体 saves 早于 deletes(`unitofwork.py` 的 `dependencies.add((saves, deletes))`);②表间走 `_sorted_tables` 拓扑序,DELETE 取它的**逆序**;③**表内 UPDATE 先于 INSERT**(`_save_obj` 里先 `_emit_update_statements` 后 `_emit_insert_statements`),与直觉相反 | Python(30 断言实跑) / Go(人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-22 |
| 552 | `02-Web开发/03-数据库/SQL与ORM/批量写入与Upsert/` | PG ON CONFLICT 与 SQLite UPSERT(PG18 与 SQLite 官方文档实读):conflict_target 的推断规则是「**不考虑顺序**且列集合**完全相同**」的唯一索引,非唯一索引不能当 arbiter,而**非部分索引也能被带 index_predicate 的 target 推断命中**;DO UPDATE **必须**给 target 而 DO NOTHING 可省略(省略则对所有唯一约束生效);`excluded` 反映 BEFORE INSERT 触发器后的拟插入值;WHERE **最后**求值,不满足时行不更新但**仍被锁**且 RETURNING 不返回;DO UPDATE 是 deterministic,同批两行命中同一行报 **cardinality violation** 而 DO NOTHING 不受限;分区表不允许把冲突行改到另一个分区。SQLite 侧:无 target 时任一唯一约束触发、DO UPDATE 只作用于冲突行、**DO UPDATE 的冲突处理恒为 ABORT**(哪怕外层写 INSERT OR REPLACE)、`INSERT ... SELECT` 后紧跟 ON CONFLICT 有解析歧义必须补 WHERE,而 **REPLACE 是先删后插**会触发 DELETE 触发器与外键 ON DELETE | Python(35 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-22 |
| 553 | `02-Web开发/03-数据库/NoSQL/JSONB半结构化与索引/` | jsonb 与 GIN(PG18 官方文档实读):json 存**原文副本**(保空白/键序/重复键)而 jsonb 全部规范化(键排序、重复键只留最后一个、数字按 numeric 打印),jsonb 在**输入函数里**就拒绝 `\u0000` 与非法代理对、拒绝超范围数字。包含 `@>` 的三条规则:数组元素顺序无关且重复只算一次、**越级不算**(`[1,2,[1,3]]` 不包含 `[1,3]` 但包含 `[[1,3]]`;对象同理)、唯一例外是数组可包含顶层标量且**不可反向**。存在 `?` 只匹配**顶层**键或元素。GIN 两操作符类:jsonb_ops 键与值各自成条目并支持 `?`,jsonb_path_ops 把路径加值哈希成一条、只支持 `@>` 与 jsonpath 的两个运算符;fastupdate 的 pending list 超过 `gin_pending_list_limit` 才合并进主结构,**查询必须同时扫 pending 否则会漏**,且 GIN 是 lossy 的必须 recheck | Python(43 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-22 |
| 554 | `02-Web开发/03-数据库/SQL与ORM/行级安全与多租户/` | 行级安全(PG18 文档 + `rowsecurity.c` 实读):`USING` 管**已存在的行**、`WITH CHECK` 管**被写出的新行**,只写 USING 时 WITH CHECK 隐含相同,INSERT 只看 WITH CHECK 而 UPDATE 两者都要;违反 WITH CHECK 是**整句报错**不是少改一行。启用 RLS 却无策略 = **default-deny**(源码注释 all access is prohibited),反之未 ENABLE 时建再多策略也不生效。superuser 与 BYPASSRLS 一律绕过,**表属主默认也绕过**(多租户头号漏洞,必须 ALTER TABLE ... FORCE ROW LEVEL SECURITY)。多策略组合:permissive 之间取 **OR**(源码 `makeBoolExpr(OR_EXPR, ...)`)、restrictive 再 **AND** 上去,只有 restrictive 时基础是全放行再收紧。参照完整性检查**总是绕过** RLS,故唯一冲突会泄露存在性(官方点名的 covert channel) | Python(33 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-22 |
| 555 | `02-Web开发/03-数据库/列式存储与Parquet/` | Parquet 嵌套列式编码(parquet-format 官方规范实读):max definition level = 路径上 optional 与 repeated 节点个数(required 不计),max repetition level = repeated 节点个数;**required 字段的列里照样会有 NULL**(祖先缺失时,d=1)。完整复现 Dremel Figure 3:`Name.Language.Country` 的 (r,d) 序列是 (0,3) (2,2) (1,1) (1,3) (0,1);r 记的是**最深的那个新开出现的 repeated 层级**且 r=0 同时是文档边界,装配时 `r=k` 表示保留前 k−1 层。数据页三段 rep → def → values **背靠背无填充**,非嵌套不写 rep、required 不写 def、NULL 只体现在 def 上不占 values。RLE/Bit-Packing 混合编码:header 最低位是标志位(1 为 bit-packed、0 为 RLE)、bit-packed 总是 **8 个一组**(header 存长度除 8)、位打包是 **LSB-first**(官方示例 bit width 3 打包 0 到 7 得 0x88 0xC6 0xFA,逐字节对拍通过)、varint 是标准 ULEB-128 | Python(52 断言实跑) / Go(人工审查 + 三项静态检查) | 2026-09-22 |
| 556 | `03-系统编程/03-内存管理/垃圾回收/mark-sweep-free-list/` | 标记-清除与空闲链表合并（dlmalloc 设计说明与 malloc-2.8.6.c 实读）：边界标记让「向前遍历」与「相邻合并」都退化为一次加法，新版本在使用中 chunk 上省略 trailer 只在空闲 chunk 上保留；128 个近似对数 bin，小于 512 字节的每箱只装一种尺寸、间隔 8 字节，bin 内按尺寸升序同尺寸最旧优先；算法总类目是 best-first with coalescing，wilderness 被当成比所有块都大因而只在没有其它候选时才用；延迟合并会造成假性 OOM（三块共 15 word 空闲，9 word 的请求却失败），强制 coalesce 后同样请求成功；mmap 阈值默认 1MB 且仅当 arena 装不下时才启用，独立映射块释放后即使地址相邻也永不并入 arena；余数小于最小 chunk 时不分割而是整块交付；最小可分配 chunk 32 位 16 字节、64 位 24 字节；清扫必须扫过整个堆，环形垃圾 mark-sweep 回收 2 个而同形状下引用计数回收 0 个 | Python(main.py 加 selfcheck_ms.py，60 断言实跑) / Go(main.go 加 marksweep.go 人工审查) | 2026-09-22 |
| 557 | `03-系统编程/03-内存管理/分配器/分级分配器对比/` | 三家现代分配器的尺寸分级对照（jemalloc.3 man page、TCMalloc Design Doc、mimalloc types.h 实读）：jemalloc 每次翻倍 4 档、间隔恒为 2 的 k 次方除以 4，最坏内部碎片是 (S-1)/5S 随档位趋近 20%，超过 20% 的只有 16/32/48/64 这最小四档，small 与 large 的分界是 4 倍页大小即 16 KiB；tcmalloc 的静态最大容量等于相邻两档数组起点之差除以指针大小，运行期容量被它夹住，档位耗尽可从同 CPU 的其它档偷槽位且总槽位守恒，总缓存随 CPU 数线性增长；mimalloc 三级页 64 KiB / 512 KiB / 4 MiB，对象上限 (页减 4 KiB) 除以 6 得 10 KiB 与 86698 字节，MI_BIN_HUGE 为 73；jemalloc 的 dirty 衰减默认 10 秒、muzzy 默认关闭，衰减曲线两端速率为零中点为峰值。另记录两处官方口径冲突：官方 Table 1 在 8 到 16 KiB 区间缺失递推规则应产生的 10/12/14 KiB 三档，以及 mimalloc 只给 12.5% 比例而纯递推需要 98 步与官方 73 档不符，均只记录不反推 | Python(sizeclass.py 加 main.py 加 selfcheck_alloc.py，65 断言实跑) / Go(sizeclass.go 加 main.go 人工审查) | 2026-09-22 |
| 558 | `03-系统编程/03-内存管理/垃圾回收/zgc-colored-pointers/` | ZGC 着色指针与并发压缩（OpenJDK zAddress.hpp 实读，对照 Shenandoah）：zpointer 的 16 位元数据布局 RRRRMMmmFFrr0000，Reserved 在 shift 0 占 4 位、Remembered 在 shift 4 占 2 位、Marked 在 shift 6 占 6 位、Remapped 在 shift 12 占 4 位；三层屏障掩码是 Load 0xF000 包含于 Mark 0xFFC0 包含于 Store 等于 All 0xFFF0，remembered 位只有 store 屏障看而 reserved 四位不参与任何屏障；元数据占满低 16 位故地址位恒从 bit16 开始、堆基址必须 64 KiB 对齐；load shift table 对 Null 取 24、对 Remapped00/01/10/11 取 13/14/15/16，非法组合恒为 0，重叠零位依次为 3/2/1/0；RemappedOldMask 与 RemappedYoungMask 各在两个四位图案间交替，取交后 remap 位按 00 到 01 到 11 到 10 再回 00 四步循环；并发重定位后字段颜色过期，load barrier 走慢路径查转发表并把修正后的指针写回字段实现自愈，第二次 load 不再进慢路径；Shenandoah 的 Brooks pointer 把转发词放在对象头里，LRB 每次 load 都要多读一次内存恒为 1 比 1，而 ZGC 在颜色一致时零慢路径 | Python(zpointer.py 加 main.py 加 selfcheck_zgc.py，63 断言实跑) / Go(zpointer.go 加 main.go 人工审查) | 2026-09-22 |
| 559 | `03-系统编程/03-内存管理/分配器/percpu-numa/` | per-CPU 分配与 NUMA 感知分配（mbind(2)、numa(7)、set_mempolicy(2)、this_cpu_ops、jemalloc.3 实读）：this_cpu_read/write/add 自带抢占与中断保护而 __this_cpu_ 系列要求调用方自行保证，无保护的 RMW 若在中途被抢占并迁移会把更新写到新 CPU 的副本上，初值 5 加 1 得到总数 11 而非正确的 6；共享计数器 12 次写入产生 11 次 cacheline 失效而 per-CPU 计数器零失效且汇总值相同；jemalloc 的 percpu_arena 按线程当前所在 CPU 绑定 arena，percpu 下线程迁移则 arena 随之改变，phycpu 下同核两个超线程共享一个 arena 而线程在同核内迁移不变；NUMA 七种策略里 MPOL_DEFAULT 与 MPOL_LOCAL 落在触发分配的 CPU 所属节点，MPOL_BIND 是严格策略且取有足够空闲的最近节点而非最小节点号（Linux 2.6.26 的语义变更），nodemask 内无空闲时报 ENOMEM 而 MPOL_PREFERRED 会回退；MPOL_MF_STRICT 遇已就位不符的页返回 EIO，MPOL_MF_MOVE 只搬本进程独占的页，MPOL_MF_MOVE_ALL 需要 CAP_SYS_NICE；四种 EINVAL 组合被逐条断言 | Python(main.py 加 selfcheck_numa.py，49 断言实跑) / Go(numa.go 加 main.go 人工审查) | 2026-09-22 |
| 560 | `03-系统编程/03-内存管理/虚拟内存/多级页表与TLB/` | 多级页表、TLB 与缺页中断（内核 mm.rst 与 5level-paging.rst、userfaultfd(2)、madvise(2)、mincore(2) 实读）：4 KiB 页配每级 9 位，4 级 48 位地址 5 级 57 位，遍历代价等于级数加一；规范地址要求高位是符号扩展，0x0000800000000000 与 0x0100000000000000 分别是 4 级与 5 级的非规范空洞起点；内核布局原文数值包括 4 级用户空间止于 00007ffffffff000 后接 4 kB guard hole、直接映射 ffff888000000000 64 TB、vmemmap ffffea0000000000 1 TB、内核文本 ffffffff80000000 512 MB 映射到物理地址 0；5 级把上限抬到 128 PiB 虚拟与 4 PiB 物理，分别是 4 级的 512 倍与 64 倍，用户空间从 128 TB 扩到 64 PB 正好 512 倍，但仍默认不分配 47 位以上地址；TLB 同样项数换 2 MiB 巨页覆盖量放大 512 倍，mprotect 降权若不 shootdown 会被陈旧条目放行走写；缺页分类中匿名页首次访问与 COW 都是 minor，前者零页按需后者只清 PTE 写位不动 VMA 权限，VMA 只读的写与 PROT_NONE 的访问才是 SIGSEGV；userfaultfd 四种注册模式里 WP 与 RWP 互斥而 MISSING 与 WP 可共存 | Python(main.py 加 selfcheck_paging.py，62 断言实跑) / Go(paging.go 加 main.go 人工审查) | 2026-09-22 |
| 561 | `04-移动开发/03-跨平台/React-Native/Yoga布局算法/` | Yoga 布局引擎（facebook/yoga@main 实读）：`roundValueToPixelGrid` 的 fractial 先按 fmod 保号、负数补 1 再判，**0.5 一律进位**（2.5→3，与 Python 内建 round 相反）；**宽度是两条绝对边各自取整后再相减**，故 100 三等分得 33/34/33；`NodeType::Text` 的 textRounding 让位置 forceFloor、宽度在有小数时 forceCeil，**文本只会变宽不会截字**；`inexactEquals` 硬编码 epsilon 0.0001 且两个 NaN 相等。`canUseCachedMeasurement` 一条否决（lastComputed 为负）+ 三条放行（同一约束 / StretchFit 且尺寸相同 / MaxContent→FitContent 仍装得下 / 两次 FitContent 且收紧后仍装得下），psf 非 0 时先取整再比。`calculateFlexLine` 的 gap 只对非首项计、grow 合计在 (0,1) 抬到 1 而 shrink 合计**恒为负所以永远抬不起来**（注释与实现打架，只记录）。两遍弹性分配：规范要循环到收敛而 Yoga 固定两遍，第一遍用原始总量还是**滚动总量**由 `Errata::FlexFirstPassUsesRunningTotals` 决定，**默认 errata=24 走修复前的滚动总量**（basis 20×3/max 25·35 下得 [25,35,40] 而非 [25,35,37.5]）；`YGErrataClassic = All & ~1` | Python(main.py 加 selfcheck_yoga.py，60 断言实跑) / Go(yoga.go 加 main.go 人工审查 + bracket_check + go_sanity + go_crossref) | 2026-09-22 |
| 562 | `04-移动开发/03-跨平台/Flutter/事件循环与Timer/` | Dart 事件循环（dart-lang/sdk@main 实读）：微任务是**单链表**不是队列，只有「从空变非空且不在 loop 中」才排一次 immediate，因此 loop 中新增的微任务在同一轮排空（源码注释给的饥饿例子：递归 scheduleMicrotask 会让 Timer 永远不执行）；优先级回调**插在上一个优先级项之后**而非队首，且 `_microtaskLoop` 每轮开头清 `_lastPriorityCallback`。非零延迟 Timer 放二叉小顶堆（初值 7、扩容 2n+1、`(i-1)~/2`、同时刻按 `_id` FIFO、id 按 `0x1fffffff` 回绕）；**零延迟 Timer 不进堆**而是独立 FIFO 链表，且每个都单独发一条 `_ZERO_EVENT=1` 消息（超时是 `_TIMEOUT_EVENT=null`），`_queueFromTimeoutEvent` 在有零延迟时**不看当前时刻**只看「是否早于零延迟」。`_runTimers` 是快照式派发（回调里新建的 Timer 要等下一次事件），每个回调后跑一次 `_runPendingImmediateCallback`，周期计时器按 `missedTicks` 补齐 tick | Python(main.py 加 selfcheck_loop.py，35 断言实跑) / Go(loop.go 加 main.go 人工审查 + 三项静态检查) | 2026-09-22 |
| 563 | `04-移动开发/03-跨平台/React-Native/Hermes值与字节码/` | Hermes 的 64 位值表示与 HBC 格式（facebook/hermes@main 实读）：NaN-boxing 借**高 16 位**，数据位 48 位，标签区间 0xfff9..0xffff，0xfff8 是规范 quiet NaN，故 `isDouble()` 判据是 `raw < 0xFFF9000000000000`（严格小于）；7 个 Tag 由 `SignExtend32<8>` 得 -7..-1，`Object` 同时是 `Last`；前四个标签各向右借一位成 4 位 ETag（ETag=2×Tag 或 2×Tag+1），**ETag 编码时 bit47 被占用，可用数据位实为 47 位**（val ≥ 2^47 会把 undefined 写成 null）；指针上限 48 位（x86-64/ARM64 用户态地址高位恒 0），`isPointer` 阈值 0xFFFD000000000000 且是大于等于；`isNaN` 先掩掉符号位故负 NaN 也算 NaN；原生指针不参与 NaN-boxing，要求调用方保证其位模式是合法 double。HBC：`MAGIC = 0x1F1903C103BC1FC6` 正是古希腊语 Ἑρμῆ 的四个 UTF-16BE 码元，`DELTA_MAGIC = ~MAGIC`，文件头 8+4+20+20×4+1+19 = **128 字节且 static_assert 是 32 的整数倍**，`SmallStringTableEntry` 的 1+23+8 位域用 `length == 255` 判溢出，`SmallFuncHeader` 溢出时把 32 位偏移拆成两个 16 位位域 | Python(main.py 加 selfcheck_hermes.py，71 断言实跑) / Go(hermes.go 加 main.go 人工审查 + 三项静态检查) | 2026-09-22 |
| 564 | `04-移动开发/03-跨平台/Compose-Multiplatform/快照系统/` | Compose 快照系统（JetBrains/compose-multiplatform-core@master 实读）：`SnapshotIdSet` 是 upperSet/lowerSet 两个 Long 覆盖 [lowerBound, lowerBound+127] 加下界以下的有序数组，**窗口之上恒为 0**，`set` 越界时整体下移窗口并把原 lowerSet 逐位移进数组；不可变且**无变化时返回同一实例**，刻意不实现 equals；窗口内 O(1)，下界以下 get 是 O(log N)、set 是 O(N)。可见性三条件 `candidate != 0 && candidate <= current && !invalid.get(candidate)`，`readable` 取合法的最大 id；`invalid` = 取快照时仍开着的快照（**要去掉全局自己**，否则全局看不到自己）。`innerApplyLocked` 用三条记录判碰撞：applied / current（下一个快照会看到的）/ previous（**假如本快照没改过**会看到的，`start = invalid ∪ {snapshotId} ∪ previousIds`），`current != previous` 才问 `mergeRecords`，而默认实现返回 null ⇒ **任何碰撞都失败**；`previous.snapshotId == PreexistingSnapshotId` 时直接采用。全局 id 也来自 nextSnapshotId（首个 = 2，预置 id = 1）；源码注释明说该算法**不保证可串行化** | Python(main.py 加 snapshot_ids.py 加 selfcheck_snapshot.py，56 断言实跑) / Go(snapshot.go 加 main.go 人工审查 + 三项静态检查) | 2026-09-22 |
| 565 | `04-移动开发/03-跨平台/WebView容器/IPC与能力ACL/` | Tauri 2 的 IPC 与 capability ACL（tauri-apps/tauri@dev 实读）：命令键归一化分三类——`APP_ACL_KEY="__app-acl__"` 裸名、`core:X` 剥前缀成 `plugin:X\|cmd`、普通插件 `plugin:<key>\|cmd`；解析权限时 `commands.allow/deny` 都为空的走 **global scope**（只贡献作用域不授权命令）。`resolve_access` 三段：**deny 优先且只看 origin**（不看 window/webview），命中即整体拒绝；然后 origin 必须匹配；最后 window 与 webview 是**或**关系。`Origin::matches` 里 Local 只匹配 Local、Remote 交给 `urlpattern`，**本地来源吃不到只给远程 URL 的能力**；`RemoteUrlPattern` 的规则由源码单测钉死——`http://*` 命中任意主机但不换 scheme、`http://*.tauri.app` **不命中裸域名**、`http://localhost/*` 命中路径、`*://localhost` 任意 scheme。IPC 请求头的 `Tauri-Callback` / `Tauri-Error` 必须是数字字符串（源码单测用 12378123 / 6243），响应头 `Tauri-Response` 取 ok / error | Python(main.py 加 selfcheck_acl.py，40 断言实跑) / Go(acl.go 加 main.go 人工审查 + 三项静态检查) | 2026-09-22 |
| 566 | `05-AI与机器学习/08-概率图模型/01-变量消除与消元顺序/` | 变量消除的消元顺序启发式（pgmpy@dev 的 `EliminationOrder.py` 与 `ExactInference.py` 实读）：`fill_in_edges` 只数**当前道德图里还缺的边**（已存在的不重复计数），四种启发式只是对「缺边数」的不同加权 —— MinFill 取缺边数最少、MinNeighbors 取邻居数最少、**MinWeight 的权重是消去该变量时新团的势函数定义域大小而非度数**、WeightedMinFill 再乘对数项；Kjærulff 的 H1 到 H6 是同一套 `S/E/M/C` 参数的不同取值，`_Kjaerulff` 里拆成 mass 与 cost 两个分量。`induced_width` = 消元过程中**最大团大小减一**（pgmpy 源码注释明说同一个 factor 只考虑一次）。弦图判定走 MCS，官方 doctest 里 `weightedminfill` 在 Asia 网上给出 c d l s g 的顺序。**启发式给的是极小而非最小**，README 明确标注不保证最优 | Python(main.py 281 行加 selfcheck_elim.py 199 行，41 断言实跑全绿) / Go(elimorder.go 290 加 cliques.go 129 加 main.go 47，人工审查加 bracket/sanity/crossref 全 OK) | 2026-09-22 |
| 567 | `05-AI与机器学习/08-概率图模型/02-连接树与三角化/` | 连接树与三角化（networkx 的 `algorithms/chordal.py` 与 pgmpy 的 `JunctionTree` 实读）：MCS-M 是「反向最大势搜索加填充」，`complete_to_chordal_graph` 因此得到的是**最小三角化**而非任意三角化；树上结点是极大团、边上标签是分离集，`add_edge` 直接拒绝成环、`check_model` 要求连通。`chordal_graph_treewidth` = 最大团大小减一，官方 doctest 钉死 `barbell_graph(4, 6)` 的树宽是 3。Lauritzen-Spiegelhalter 校准是两趟：collect 沿 BFS 逆序、distribute 沿 BFS 正序；`_update_beliefs` 的公式是 `sigma` 先边缘化到分离集、`beta_j *= sigma / mu`、`mu ← sigma` —— **不除掉旧消息就把同一个因子算了第二遍**，自检里专门留了 `divide_out=False` 的负控 | Python(main.py 211 加 pgmodel.py 227 加 selfcheck_jt.py 143，40 断言实跑全绿) / Go(chordal/factor/graph/jt/main 五文件，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 568 | `05-AI与机器学习/08-概率图模型/03-循环信念传播/` | 循环信念传播与收敛判据（Ihler, Fisher & Willsky, JMLR 2005 全文实读）：度量取跨比的 `d(e) = max log(e_max/e_min)`，势强度 `d(psi)²` 按 Eq (7) 在四个配置上取跨比；**Theorem 8** 给出压缩映射的充分条件（与图的度数、环的势强度有关），**Theorem 10（Simon 条件）** 与 **Theorem 11**（`g'(0) < 1` 的导数判据）并不互相蕴含 —— 自检里造出一个 `theorem11_derivative ≈ 1.9999 > 1` 而 `simon ≈ 15.2` 的实例。单环图必收敛到**唯一**不动点但与初值无关的是不动点本身不是精度；树上是精确的；含双三角的对称图会出现**周期 2 振荡**，判据是连续快照满足 `m_i == m_{i-2}` 且 `m_i != m_{i-1}`。消息下标方向写反会让「单环必收敛」与实现自相矛盾 | Python(main.py 254 加 selfcheck_bp.py 169，54 断言实跑全绿) / Go(loopybp.go 264 加 mrf.go 73 加 main.go 42，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 569 | `05-AI与机器学习/08-概率图模型/04-HMM三算法/` | HMM 三个算法（Jurafsky & Martin《SLP3》附录 A 与 hmmlearn@main 的 `src/hmmlearn/base.py` 实读）：Eq A.7 钉死 `P(3 1 3 给定 hot hot cold) = .4 × .2 × .1 = 0.008`，而 Eq A.8 的联合还要乘转移概率故必更小；Fig A.2 冰激凌 HMM 的 `A` 矩阵排布有歧义，故**所有定量断言改由枚举 `N^T` 条路径独立验证**，不依赖这一处读数。缩放前向里 `c_t` 是除数所以 `log P = +Σ log c_t`，而 hmmlearn 的 `scaling_factors` 记的是倒数故写成 `-Σ log c_t`，**两边都对只是记法相反**；`backward_scaled` 必须复用同一组 `c_t`（`beta_hat_{T-1} = ∏ c_t`，递推再除以 `c_{t+1}`）。`_decode_map` 是逐点取后验最大，**不等于 Viterbi**：自检构造出两者不同的例子并额外断言 Viterbi 路径的联合概率更高。hmmlearn 明确 `if n_samples == 1: return`，即**长度为 1 的样本不更新转移矩阵** | Python(main.py 239 加 selfcheck_hmm.py 164，63 断言实跑全绿) / Go(hmm.go 196 加 decode.go 197 加 main.go 31，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 570 | `05-AI与机器学习/08-概率图模型/05-变分推断与平均场/` | 变分推断与平均场（Blei, Kucukelbir & McAuliffe, arXiv:1601.00670 全文实读，公式编号沿用该文）：Eq (13) 的 ELBO 与 Eq (14) 的分解 `log p(x) = KL(q 对 p) + ELBO(q)` 是本 demo 的第一条断言（随机 `q` 上容差 `1e-12`），它比「ELBO 单调上升」强得多 —— 后者只要实现基本正确就会过。CAVI 即 Algorithm 1；**Eq (40) 的指数族捷径**说完全条件属指数族时 `nu_j = E[eta_j]`，自检里枚举 `2^{n-1}` 项按定义求期望来验证。两个算例都刻意选成真后验可解析以量化代价：离散 Ising 上 `q` 的协方差**恒为 0**（强排斥算例里 `q` 给 `(1,1)` 的概率是真值的 20 倍以上）；二元高斯上均值无偏但 `var_j = 1/Lambda_jj` **严格小于**真边缘方差 `(Lambda^{-1})_jj`，且 `KL(q* 对 p) = -½ ln(1 - rho²)` 有闭式。KL 的方向只在**强耦合**算例上断言不同：温和模型两侧数值恰好接近（0.0456 对 0.0454），拿巧合当证据会得出错误结论。解耦时平均场必须完全精确（负控） | Python(main.py 237 加 selfcheck_vi.py 193，66 断言实跑全绿) / Go(vi.go 206 加 gauss.go 167 加 main.go 49，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 571 | `06-DevOps/07-DevSecOps/01-SAST语义规则引擎/` | Semgrep 通用匹配引擎（semgrep/semgrep@develop 的 `src/matching/Matching_generic.ml`、`SubAST_generic.ml`、`Normalize_generic.ml`、`Match_patterns.ml`、`Pattern_vs_code.ml` 与 `semgrep-interfaces/rule_schema_v1.yaml` 实读）：`m_list_with_dots` 把 `...` 当作「任意长度子序列」，与 `m_list_in_any_order`（`<... x ...>`）是两条独立的匹配路径；`MetavarEllipsis` 绑定到的片段在后续匹配中必须**内容一致**，`check_and_add_metavar_binding` 先查已有绑定再 unify，不一致即整体失败。`equal_ast_binded_code` 只比对**已绑定**的 metavar，未绑定的不参与。`full_module_names` 把 Python 的 `import x.y` 归一化成全模块名，故 `foo.bar` 能匹配 `import foo.bar` 的调用点。规则 schema 里 `pattern` 与 `patterns` 互斥、`metavariable-regex` 的 `regex` 是必填。**转写坑**：`match_args` 必须把 `cfg` 带进闭包（用默认 `MatchConfig` 会让严格模式断言静默变假绿） | Python(astmodel.py 264 加 main.py 186 加 selfcheck_rule.py 242，49 断言实跑全绿) / Go(matching.go 加 main.go，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 572 | `06-DevOps/07-DevSecOps/02-SBOM格式与漏洞关联/` | PURL 规范化与 CycloneDX 与 SPDX 的漏洞关联（`package-url/purl-spec` 的 Clause 5 与 how-to-build、`CycloneDX/specification` 的 bom-1.6 与 bom-1.7 schema、`spdx/spdx-spec` v2.3 实读）：PURL 七段里 **type 强制小写而 namespace 与 name 保留原大小写**（只有 PyPI 等少数 type 有额外归一化规则），qualifier 的键**排序后**再参与比较，路径段**逐段**百分号编码（整串编码会连分隔符一起吃掉）。CycloneDX 的 VEX 状态机按 `affects` 的 `ref` 指向 bom 里的组件，`vers` 判定是「版本区间是否与被影响区间相交」的**子集**语义而不是等价比较。SPDX 侧 `dependsOn` 与 `contains` 的传递闭包口径不同，转译时 CDC 的 `dependencies` 只能映到前者。跨格式主键用「PURL 规范化串」而非组件名加版本 | Python(purl.py 161 加 main.py 255 加 selfcheck_sbom.py 223，46 断言实跑全绿) / Go(purl.go 180 加 sbom.go 212 加 main.go 54，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 573 | `06-DevOps/07-DevSecOps/03-制品签名与来源证明/` | DSSE 信封与 Sigstore 制品签名（`secure-systems-lab/dsse` 的 `protocol.md` 与 `envelope.md`、`sigstore/cosign` 的 `SIGNATURE_SPEC.md` 与 `BUNDLE_SPEC.md`、`sigstore/protobuf-specs` 的 bundle 与 common 与 rekor proto 实读）：PAE 的格式是 `DSSEv1` 加若干长度前缀段，官方测试向量用 `payloadType` 为 `http://example.com/HelloWorld` 与 payload 为 `hello world`，**中间的分隔符是一个空格而 payload 自身也含空格**，数空格会数错（本 demo 改用 `pae("t", b"x")` 数分隔符）。纯标准库实现 RFC 6979 确定性 nonce 加 P-256，签名结果与官方向量的 r 与 s **逐字节一致**，`d·G` 与官方给的 X 与 Y 一致。cosign 的镜像签名 tag 是 `sha256-<digest>.sig`；bundle 的 `integratedTime` 只在信任根内才可信，`canonicalized_body` 必须与验证用 body 一致 | Python(p256.py 163 加 main.py 273 加 selfcheck_dsse.py 292，63 断言实跑全绿) / Go(p256.go 227 加 dsse.go 223 加 main.go 88，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 574 | `06-DevOps/07-DevSecOps/04-密钥扫描算法/` | detect-secrets 与 gitleaks 的密钥扫描口径对比（`Yelp/detect-secrets` 的 `plugins/high_entropy_strings.py`、`plugins/keyword.py`、`plugins/base.py` 与 `gitleaks/gitleaks` 的 `config/rule.go`、`detect/detect.go`、`detect/utils.go`、`cmd/generate/config/main.go`、`gitleaks.toml` 实读）：Shannon 熵公式两边相同，但**字符集不同**——detect-secrets 的 Base64 只认 `A-Za-z0-9+/=` 与 Hex 只认 `0-9a-fA-F`，gitleaks 交给规则各自的正则，故同一字符串一边报一边不报。Base64 阈值 4.5、**Hex 阈值 3.0 且带 `1.2 / log2(len)` 的惩罚项**（越长越容易被判为密钥）；gitleaks 用规则里的 `entropy` 字段直接比，没有长度修正。`KeywordDetector` 的 deny 列表分「跟着密钥」与「跟着值」两类，且**引号必须是紧跟的**；C 家族规则里 secret 落在第 6 个捕获组（源码写 `(\5)` 是因为前面还有隐式组），靠实测 `regex.groups` 确认而不是数括号 | Python(detect_secrets.py 289 加 gitleaks.py 167 加 main.py 59 加 selfcheck_secret.py 269，66 断言实跑全绿) / Go(secret.go 212 加 main.go 59，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 575 | `06-DevOps/07-DevSecOps/05-策略即代码Rego/` | OPA 编译期 safety 检查与 body 重排（`open-policy-agent/opa@main` 的 `v1/ast/compile.go`、`v1/ast/unify.go`、`v1/ast/compile_test.go`、`v1/ast/visit.go`、`v1/ast/policy.go` 实读）：`safe` 初值是 `bodyVars ∩ globals`，而 `SafetyCheckVisitorParams` 只设了 `SkipRefCallHead` **不是 `SkipRefHead`**，所以 `input` 与 `data` 能进入 bodyVars —— 这是整个分析的起点。不动点迭代每趟按原始顺序扫，**一变安全就立刻追加并把产出并入 safe**（故同趟后面的表达式可见），某一趟一条都没加进去即剩余真 unsafe。`outputVarsForExpr` 各分支：取反恒空产出、`with` 的 target 未全安全则恒空、`every` 只产出 domain 的下标变量而 key 与 value 是**声明变量**、`and` 与 `or` 不给外围贡献绑定。`outputVarsForExprEq` 是「term 变量并上 safe，**`:=` 再删掉 LHS 整体**（issue #3546），然后 Unify，最后才减 safe」，`input.a[i] == 1` 产出 `{i}` 而 `input.a[i] := 1` 产出空。`Args(terms)` 是**类型转换不是切片**，误当切片会丢掉调用的输出项。`checkBodySafety` 报错时返回**原始** body；`safetyErrorSlice` 只要同批还有非 LHS 的 unsafe 变量就压掉 `:=` 的 LHS。用官方 `TestOutputVarsForNode` 的 31 条向量逐条对拍 | Python(terms.py 177 加 exprs.py 164 加 safety.py 219 加 main.py 107 加 selfcheck_rego.py 240，67 断言实跑全绿) / Go(terms.go 253 加 exprs.go 156 加 safety.go 278 加 main.go 262，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 576 | `07-数据存储/02-NoSQL/ScyllaDB分片感知与shard-per-core/` | ScyllaDB token 到 shard 的映射（`scylladb/scylladb@master` 的 `dht/token.cc`、`dht/token.hh`、`dht/token-sharding.hh`、`dht/i_partitioner.cc`、`dht/murmur3_partitioner.cc`、`dht/fixed_shard.cc`、`dht/fixed_shard.hh` 实读）：`zero_based_shard_of` 是官方自称的 master function，先把 token 按 msb 左移丢弃高位再做 128 位乘法右移 64 位，等价于 floor(0.token 乘 shards)；逆函数 `init_zero_based_shard_start` 因除法向下取整必须逐格加一修正（shards=3 时 floor(2 的 64 次方/3) 乘回去只有 2 的 64 次方减 1，仍归 shard 0）。`shard_of` 对 minimum 与 maximum 两个哨兵是硬编码的 0 与 shards 减 1，不走哈希；`token_for_next_shard` 只在目标 shard 严格在后时才给边界，否则返回 maximum（**不回绕**），spans 大于 1 也一律判溢出。tablet 迁移期 `shard_for_writes` 返回两个 shard 而 `shard_for_reads` 空值语义是 0 不是空。fixed_shard 的 token 布局是高 16 位 shard 加低 48 位 hash，顶层位留 0 规避 bias 故 shard 实际只有 15 位 | Python(sharding.py 179 加 selfcheck_sharding.py 115 加 main.py 101，49 断言实跑全绿) / Go(sharding.go 202 加 main.go 101，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 577 | `07-数据存储/02-NoSQL/DynamoDB自适应容量与热分区/` | DynamoDB 分区吞吐与自适应容量（AWS 官方开发者指南四篇实读：bp-partition-key-design、HowItWorks.Partitions、bp-partition-key-uniform-load、bp-partition-key-sharding）：每个分区设计上限 3000 RCU 与 1000 WCU，1 RCU 是 4KB 强一致读或两次最终一致读，1 WCU 是 1KB 写，官方例子里 20KB 的 item 一次强一致读消耗 5 RCU 使单分区并发降到 600。Adaptive capacity 官方只给了一句「适用于 on-demand 与 provisioned」，未公布算法，本 demo 选的读法是冷分区余量借给热分区但**冲不过单分区硬顶**，并用 adaptive 开关成对对比验证。写分片官方给了两种：随机后缀 1 到 200，以及「码点乘积 mod 200 加 1」的计算后缀（可反算故 GetItem 直接命中），代价是读一天要扇出 200 次 Query | Python(capacity.py 100 加 selfcheck_capacity.py 109 加 main.py 60，41 断言实跑全绿) / Go(capacity.go 136 加 main.go 45，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 578 | `07-数据存储/02-NoSQL/MongoDB分片集群与chunk均衡/` | MongoDB 分片集群的 chunk 与均衡器（mongodb.com 官方文档 markdown 版实读：《Sharded Cluster Balancer》与《Troubleshoot Stuck Chunk Migrations》）：均衡阈值不是「相等」而是「差值小于 3 倍 range size」，默认 128MB 时要差到 384MB 才动手，所以差 383MB 官方就叫均衡；并发迁移数是 floor(n/2) 因为一次迁移占两个 shard。range 迁移是 7 步，第 6 步更新 config 元数据前后都要求多数派确认并会短暂暂停读写，第 7 步删除源端副本是**异步**的（不等它完成就开始下一次迁移）。jumbo chunk 是超过 range size 且无法自动分裂，分 divisible（多值，可 splitAt）与 indivisible（单值，只能 refine shard key 或 reshard）两种，单调 shard key 是典型成因 | Python(chunking.py 130 加 selfcheck_chunking.py 102 加 main.py 65，43 断言实跑全绿) / Go(chunking.go 135 加 main.go 69，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 579 | `07-数据存储/02-NoSQL/Cassandra二级索引与SAI/` | Cassandra SAI 的两层索引结构（`apache/cassandra@trunk` 的 `index/sai/README.md`、`index/sai/disk/RowMapping.java`、`index/sai/disk/v1/V1OnDiskFormat.java` 与 bbtree 目录实读）：SAI 是列级的**本地**索引，全局查询建立在 range read 之上且结果**只保证 token 序**。组件分 per-SSTable 与 per-column 两层，完成标记也分 GROUP 与 COLUMN 两级；`RowMapping` 只在 OperationType 为 FLUSH 时建真映射，其余返回 DUMMY 且 get 恒为负一；`hasClustering` 决定用 Wide 还是 Skinny 的 PrimaryKeyMap。核心陷阱是 **rowID 是 SSTable 局部的**，跨 SSTable 合并 postings 必须带 SSTable 标识，本 demo 用正确实现与错误实现的成对用例验证（同一 rowID 1 在两个 SSTable 指向不同行，错误实现会张冠李戴）。字符串走 byte-ordered trie、数值走 block-oriented balanced tree，索引更新与 mutation 同步且**不需要 read-before-write** | Python(sai.py 149 加 selfcheck_sai.py 123 加 main.py 83，35 断言实跑全绿) / Go(sai.go 147 加 main.go 78，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 580 | `07-数据存储/02-NoSQL/HBase Region与HFile/` | HBase region 分裂策略与 HFile 版本（`apache/hbase@master` 的 `IncreasingToUpperBoundRegionSplitPolicy.java`、`ConstantSizeRegionSplitPolicy.java` 与官方 book 实读）：默认策略的阈值是 min(maxFileSize, initialSize 乘 region 数的三次方)，initialSize 优先取 `hbase.increasing.policy.initial.size`，否则是 2 倍 memstore flush（默认 128MB 故 256MB），于是曲线是 256MB 到 2048MB 到 6912MB 再顶到 10GB 天花板；region 数为 0 或超过 100 时直接退回最大文件大小，源码注释写明是为**防数值溢出**，且计数只数本节点上同表的主副本。阈值还带随机抖动：jitterRate 等于 (random 减 0.5) 乘 0.25 落在正负 0.125，默认 10GB 时两个 region 的实阈值可差 2.5GB，用来错峰分裂。HFile 默认 v3，官方明确已不能写早于 v3 的 HFile 但仍可读 v2 | Python(regions.py 108 加 selfcheck_regions.py 116 加 main.py 64，43 断言实跑全绿) / Go(regions.go 92 加 main.go 51，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 581 | `08-安全/03-应用安全与供应链/CVSSv4评分与宏向量/` | CVSS v4.0 的宏向量等价类与查表插值（FIRST 官方 `cvss-v4-calculator` 的 `cvss_lookup.js` 270 条表、`cvss_score.js`、`max_composed.js`、`max_severity.js`、`metrics.js` 与规范 §8 实读）：六个等价类 EQ1–EQ6 把向量压成宏向量后只查 270 条表，未命中按「严重性距离 / 深度」沿最浅方向插值；`m()` 的归一化是 `E:X→A`、`CR/IR/AR:X→H`、`M` 前缀只在被修改时替换基本值。无影响（所有 Impact 指标为 None）直接短路到 0.0，qualScore 阈值 4.0/7.0/9.0 得 Medium/High/Critical。用官方 JS 参考实现在 node 下跑 690 个随机向量 + 26 个官方策展向量对拍，**零偏差** | Python(cvssdata.py 133 加 cvss.py 270 加 selfcheck_cvss.py 259 加 main.py 62，796 断言实跑全绿) / Go(cvssdata.go 151 加 eq.go 145 加 cvss.go 200 加 main.go 65，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 582 | `08-安全/03-应用安全与供应链/TUF更新框架/` | The Update Framework 四角色与委托搜索（theupdateframework/specification 的 `tuf-spec.md` 实读）：root/targets/snapshot/timestamp 四角色各带签名阈值，阈值按**唯一 KEYID** 计数故同一把钥匙签两次只算一次；回滚攻击靠 snapshot 的版本单调与 timestamp 的频繁重签，冻结攻击靠 timestamp 过期，混搭攻击靠 root 与 targets 的版本一致性。中间 root 的过期**不重要**（规范 5.3.6 只在最后一份上检查），故链式更新要区分 final 与非 final。委托是可终止的（terminating 为 true 时未命中即停），搜索按**前序 DFS** 且父委托先于子委托；一致快照下元数据文件名是 `<version>.<rolename>.json` 而目标文件是 `<hash>.<filename>` | Python(tuf.py 245 加 selfcheck_tuf.py 249 加 selfcheck_deleg.py 119 加 main.py 131，61 断言实跑全绿) / Go(tuf.go 254 加 walk.go 101 加 main.go 140，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 583 | `08-安全/03-应用安全与供应链/in-toto布局与链路验证/` | in-toto 布局与链路验证（in-toto/specification 实读）：layout 声明 step 与 inspection，每个 step 由授权签名者产出一个 link，link 记录实测的 materials 与 products。7 条工件规则（CREATE/DELETE/MODIFY/MATCH/ALLOW/REQUIRE/DISALLOW）是**按序消费**的防火墙——规则一旦吃掉某个工件就不再参与后续匹配，末尾有隐式 `ALLOW *`。MATCH 的 `IN <src>` 与 `WITH <dst>` 两侧各自剥前缀后再逐条比，源里的每个工件都必须在目的侧找到对应。子布局对父布局就是一个「虚拟 link」，其 materials/products 是子布局自身的展开；link 文件名取签名 keyid 的前 6 字节 | Python(rules.py 192 加 intoto.py 177 加 selfcheck_intoto.py 273 加 main.py 94，56 断言实跑全绿) / Go(rules.go 248 加 intoto.go 236 加 main.go 117，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 584 | `08-安全/03-应用安全与供应链/可复现构建/` | 可复现构建的时间、路径与环境归一（reproducible-builds.org 的 SOURCE_DATE_EPOCH 规范与 `/docs/timestamps` 与 `/docs/build-path` 实读）：`SOURCE_DATE_EPOCH` 是**上界**不是直接采用，构建产物里晚于它的时间戳要被压到它；ZIP 时间戳下界是 1980，更早的会被抬上来；构建路径用 `-ffile-prefix-map` 映射掉。locale 影响排序与格式化、TZ 影响时间渲染、umask 影响产物权限位——**umask 无法靠归一化解掉**，只能靠构建环境统一，本 demo 用控制组成对验证。C 参考实现对 `SOURCE_DATE_EPOCH` 的解析不做 strip，故尾随换属于**非法值** | Python(repro.py 166 加 selfcheck_repro.py 201 加 main.py 81，44 断言实跑全绿) / Go(repro.go 174 加 main.go 74，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 585 | `08-安全/03-应用安全与供应链/覆盖率引导模糊测试/` | AFL 的边覆盖位图、命中数分桶与语料库裁剪（google/AFL 的 `docs/technical_details.txt` 与 `config.h` 与 `afl-fuzz.c` 实读）：槽位是 `cur ^ prev` 且 `prev = cur >> 1`，**右移才是打破 A→B 与 B→A 对称的那一句**（XOR 本身对称，块 1 是碰撞反例因为 `1>>1 == 0`）。命中数经 `count_class_lookup8` 压成 8 个 2 的幂桶，故可当位掩码用：`virgin[i] & cur` 非零即新桶，`virgin[i] == 0xff` 即全新的边，`has_new_bits` 返回 0/1/2 三态。裁剪是贪心集合覆盖，score 正比于延迟乘大小，通常压到原来的五分之一到十分之一；非 favored 条目按 99%/95%/75% 跳过。内置 trimmer 按变长块删、校验和不变就提交，平均只省 5–20%，刻意不追求彻底 | Python(afl.py 237 加 selfcheck_afl.py 200 加 main.py 100，64 断言实跑全绿) / Go(afl.go 135 加 fuzz.go 180 加 main.go 132，人工审查加三项静态检查全 OK) | 2026-09-22 |
| 586 | `09-语言学习/Rust/方法解析与自动解引用/` | 方法调用的候选接收者列表与三级查找顺序（Reference `expressions/method-call-expr` 实读）：`a.f()` 先在接收者类型上反复解引用构造候选列表，每个 `T` 后面紧跟 `&T` 与 `&mut T`，末尾再补一次未定长强制转换，官方 `Box<[i32;2]>` 的 9 项例子被逐条断言（含「未定长只在最后一步」与二级解引用链 `Box<String>`→`String`→`str`）。查找按列表**顺序**而非签名合适度，所以 trait 的 `&self` 能抢在 struct 的 `&mut self` 之前；同一候选类型上固有方法又压过 trait 方法，两者胜负靠「有无 trait bound」的成对对照钉死。可变性与 unsafe 只在**选中之后**检查（E0596 与 E0133），查找期不会因为不可变而排除候选；歧义 E0034 只在同层多个命中时触发，完全限定语法对 `dyn` 上的固有方法失效（官方 WARNING：there is no way to call the inherent method）；数组 `IntoIterator` 在 2018 与 2021 两版下结果不同 | Python(resolve.py 195 加 selfcheck_resolve.py 163 加 main.py 57，39 断言实跑全绿) / Rust(main.rs 加 4 个 compile_fail 用例，无工具链走人工审查加 find_unbalanced 全 BALANCED) | 2026-09-22 |
| 587 | `09-语言学习/Rust/型变与PhantomData/` | 子类型化只发生在生命周期上（Reference `subtyping` 加 Nomicon `phantom-data` 实读）：struct 的型变由字段逐个合并，一致则保留、冲突即 invariant，官方 `Variance` 结构体的五个参数逐项断言，并用「去掉 `w: *mut U` 后 U 变回协变」与「把 `fn(&'c ()) -> &'c ()` 换成纯协变用法后 'c 变回协变」两组成对对照钉死；**不在 struct 里时每个位置各自计算**，故同一个 `'long` 可以一部分收缩到 `'short` 一部分保持，而同样字段放进 struct 就真的变成不变。PhantomData 的九种写法不是照抄表格，而是用型变代数 `compose` 与 `join` **算**出来再去和 Nomicon 的表比对，两个来源完全吻合；`&mut T` 在 T 上不变是安全前提（`e0597` 复现），`Cell<T>` 同理 | Python(variance.py 225 加 selfcheck_variance.py 181 加 main.py 76，61 断言实跑全绿) / Rust(main.rs 加 3 个 compile_fail 用例) | 2026-09-22 |
| 588 | `09-语言学习/Rust/trait高级形态/` | 关联类型与泛型参数的分水岭是「同一类型能实现几次」（The Book ch20-02 加 Reference `items/traits` 与 `items/associated-items` 实读）：泛型参数版可以对同一类型实现多次而关联类型版只能一份，多份时调用方必须标注（E0119）。supertrait 是「实现我之前必须先实现谁」，闭包可传递且自己是自己的 supertrait 属错误，不兼容会**传染**给子 trait。dyn 兼容（原 object safety）是一张六条逐条否决的清单：关联常量与 GAT 被否（均 E0038）、无 self 接收者被否、方法带泛型参数被否，但加一句 `where Self: Sized` 就能救回来（成对对照）；按值 `self` 隐含 Sized 反而是合法的；可分派接收者集合共 10 种；`dyn Trait` 漏写参数或关联类型即 E0191 | Python(traits.py 175 加 selfcheck_traits.py 149 加 main.py 92，44 断言实跑全绿) / Rust(main.rs 加 4 个 compile_fail 用例) | 2026-09-22 |
| 589 | `09-语言学习/Rust/声明宏与卫生性/` | `macro_rules!` 是一台逐 token、**不向前看**的小解析器（Reference `macros-by-example` 实读）：先把输入按 15 个片段分类符切成 AST 片段，片段后面能跟什么由跟随集规定（expr 只能跟 `=>` 加 `,` 加 `;`；组的**左定界符**算作前一个 token，故 `$i:expr [..]` 违规而 `$i:expr ,` 合法；`pat` 与 `pat_param` 在竖线上的差别也是一处成对对照）。重复匹配里零次迭代照样绑定（空列表），转录按层级取元变量。卫生性是 mixed-site：**局部变量与标签在定义处查找、其它符号在调用处查找**，因此宏里定义的局部不在调用间共享（E0425），`$crate` 指定义宏的 crate 但**不绕过可见性**（E0603）；宏名作用域分 textual（只看调用点之前、取最近一个）与 path-based 两种 | Python(macro_lex.py 220 加 macro_match.py 198 加 macro_trans.py 126 加 hygiene.py 107 加 selfcheck_macros.py 222 加 main.py 96，51 断言实跑全绿) / Rust(main.rs 加 4 个 compile_fail 用例) | 2026-09-22 |
| 590 | `09-语言学习/Rust/过程宏与TokenStream/` | 过程宏是「在编译期跑一段 Rust 程序，把 token 流变成另一段 token 流」（Reference `procedural-macros` 实读）：只有函数式加 derive 加属性三种形态，必须住在 `proc-macro` crate 里（因此定义它的 crate 自己**不能**用、且必须在 crate 根），**完全不卫生** —— 生成的代码就像被就地写在调用处解析，与声明宏的 mixed-site 成对对比。反直觉的是两套 token 定义不同：生命周期加复合运算符加 `-1` 在过程宏侧是**一个** token，跨界要双向转换，往返一致但 `-1` 回到声明宏侧会摊成两个 token。doc 注释就是 `#[doc = r".."]` 属性；属性宏输入按官方四例切成 attr 与 item（括号加 `=>` 加花括号三种写法）；报错走 panic 或 `compile_error!`，而**死循环不会被捕获** | Python(proc_macro.py 272 加 selfcheck_procmacro.py 166 加 main.py 105，50 断言实跑全绿) / Rust(workspace 加 mymacro proc-macro crate 加 main.rs 加 3 个 compile_fail 用例) | 2026-09-22 |
