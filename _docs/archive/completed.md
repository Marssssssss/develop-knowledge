| 232 | `05-AI与机器学习/01-深度学习/LayerNorm/` | LayerNorm / RMSNorm 的解析反向与不变量(Ba 2016 arXiv:1607.06450 / Zhang & Sennrich 2019 arXiv:1910.07467 / Xiong 2020 arXiv:2002.04745):①**归一化维度决定一切** —— 统计量取自**单个样本的特征维**(per-sample over feature axis),与 batch 完全无关;实测同 batch 内换样本不影响彼此输出(`max\|Δy\| = 0`)、换 batch 组成不影响单样本输出,这正是它取代 BatchNorm 用于变长序列的原因(BN 在 batch=1 或序列长度悬殊时统计量失真)。②**不变量**:LN 对输入平移+缩放都不变(平移被均值吸收、缩放被标准差吸收),RMSNorm 只对缩放不变、**对平移敏感**(不 re-center);实测 eps=0 时 LN 平移不变残差 4.44e-16 / RMSNorm 缩放不变残差 2.22e-16,而 eps>0 会留下 ~2e-05 的「假不变量」残差 —— 因为 eps 破坏了严格齐次性。③**解析反向三路**:dx 同时来自「除标准差路径 + 减均值路径 + 反向进均值的路径」,RMSNorm 为 `dx_j = γ_j·dy_j/r − x_j·Σ_i(γ_i·dy_i·x_i)/(H·r³)` —— 关键坑是 γ 必须在**分子求和内部**而非乘在 x_j 上,写错位置实测相对误差 8.99e-02 vs 正确实现 2.05e-10。④**成本差**:LN 两次归约(均值+方差)vs RMSNorm 一次(均方),实测 LN 116~192 ms vs RMSNorm 109~125 ms(H=4096、20000 次前向;比值随机器负载波动故不作硬判据)。⑤**Pre-LN vs Post-LN 的梯度剖面**:Post-LN 把归一化放在残差相加**之后**,深堆叠时靠后的块梯度远大于靠前的块(首块/末块比 < 1,实测 0.28~0.89,需 warmup 救);Pre-LN 放在分支**内**,首块梯度更大(2.61~4.41);两者量级差 3.9e6,这就是 Post-LN 必须配 warmup、Pre-LN 可省 warmup 的直接原因。22 项 Python 断言全绿 | Python(286+123 行) + Go(265 行) | 2026-09-15 |
| 233 | `05-AI与机器学习/01-深度学习/Softmax交叉熵/` | Softmax 与交叉熵的数值稳定与梯度化简:①**max-shift**:softmax 对任意常数平移不变,故 `logits − max(logits)` 不改变数学结果却能避开 exp 溢出 —— 实测 200 组量级 ~1000 的大 logits,朴素版 80 组出 inf/nan,shift 版**全部有限**;log-sum-exp 同理 `log Σexp(x) = m + log Σexp(x−m)`。②**梯度化简**:`∂ℓ/∂x_k = p_k − y_k`,softmax 与交叉熵连用时的经典抵消,实测解析梯度 vs 中心差分最大相对误差 3.9e-10。③**class weight 下 mean 的除数**:加权交叉熵的平均**除数是 Σw_{y_n}(权重之和)而不是 N**,否则 loss 量级随权重漂移。④**label smoothing**:one-hot 换成 ε/C 的均匀分布后 loss 下界从 0 抬到 `ε·log C`(实测严格 ≥)、梯度不再零和,作用即抑制过度自信。⑤**log_softmax vs log(softmax)**:先 softmax 再取 log 会在 p 极小时下溢到 −inf,log_softmax 直接在 log 域给出有限正确值。27 项 Python 断言全绿 | Python(209+125 行) + Go(211 行) | 2026-09-15 |
| 234 | `05-AI与机器学习/01-深度学习/残差连接/` | 残差连接与恒等映射(He 2015 arXiv:1512.03385 / He 2016 arXiv:1603.05027):①**退化问题**:加深网络后**训练误差反而变高**(不是过拟合,是优化变难),这是 ResNet 的出发点 —— 本 demo 实测 32 层 plain 初始损失 7.62 → 训练后 9.36(变差),同深度 residual 4.34 → 1.50(变好)。②**梯度里的那个「1」**:`∂ε/∂x_l = ∂ε/∂x_L · ∏(1 + ∂F(x_i)/∂x_i)`,即使 ∏∂F 项全为 0,连乘每一项都带常数 1,所以梯度不消失 —— 恒等映射给梯度开了一条无衰减通道;实测首末块梯度范数比 plain 93.9 vs residual 3.3。③**shortcut 消融**:把恒等 shortcut 换成带参投影 `0.9·h·W` 或缩放 `0.9·h` 会重新引入连乘衰减,实测末损失 恒等 1.50 < 缩放 3.50;带参 shortcut 在较大 lr 下发散(本 demo 最终用无参、条件数好的 `0.9·h[:, ::-1]` 倒序投影作对照)。④**实验设计坑**:为使对比只归因于「那条直连」,plain 与 residual 在每个单元分支入口都放同一个逐样本 LayerNorm —— 否则不加归一化的 residual 在 depth=32 直接发散(实测 `‖x_L‖/‖x_0‖ = 4.4e+02`、初始损失 2.9e+06),结论被非目标因素污染。20 项 Python 断言全绿 | Python(201+80 行) + Go(299 行) | 2026-09-15 |
| 235 | `05-AI与机器学习/01-深度学习/学习率调度/` | 学习率调度与梯度裁剪:①**Transformer 的 inverse-sqrt**(Vaswani 2017 公式 3):`lr = d_model^{-0.5}·min(step^{-0.5}, step·warmup^{-1.5})` 是**两分支取 min** —— warmup 段线性增长、之后按 1/√step 衰减,两支恰好相交于 `step = warmup`,实测峰值 6.9877e-04。②**warmup 的作用**:初期参数远离最优点、梯度方向不可信,大 lr 会把参数推向坏区域 —— 实测同一 tiny net,峰值 2e-3 下**无 warmup 发散(nan)、带 150 步 warmup 收敛 2.8187**;峰值 1e-3 下两者都收敛但 warmup 更低(2.4571 vs 2.6854)。③**SGDR 余弦退火 + warm restart**(Loshchilov & Hutter 2017 arXiv:1608.03983):`η_t = η_min + 0.5(η_max−η_min)(1+cos(πT_cur/T_i))`,关键在**周期倍增** `T_i = T_0·T_mult^i` —— 必须先累加周期起点(`while step >= start + T: start += T; T *= mult`),用 `step % len` 在倍增时会算错,实测重启点恰为 0/10/30/70。④**AdamW 解耦权重衰减**(arXiv:1711.05101):原版 Adam 把 L2 加进梯度会被 `1/√v` 的自适应缩放吃掉,导致不同参数衰减强度不等;AdamW 改为直接乘 `(1−lr·λ)`,实测归一化衰减强度比 ≈ 1.000001,而 Adam+L2 达 1.098。⑤**梯度裁剪**(Pascanu 2012 arXiv:1211.5063):按**全局范数**裁剪是等比缩放、方向严格不变(实测裁剪前后 cos = 1),按元素裁剪会改方向(实测偏 11.59°),两者只在梯度接近各向同性时等价。30 项 Python 断言全绿 | Python(211+111 行) + Go(178 行) | 2026-09-15 |
| 236 | `05-AI与机器学习/01-深度学习/混合精度/` | 混合精度训练(Micikevicius 2017 arXiv:1710.03740 + IEEE-754):①**FP16 可表示范围**:min subnormal 2^-24 ≈ 5.96e-08、max 65504,指数范围比 FP32 小 ~30 个数量级,而**尾数只有 10 位** —— FP16 便宜在带宽与吞吐不是精度。②**梯度归零**:小于 min subnormal 一半的梯度直接下溢成 0,阈值 2^-25,实测 2 万个对数均匀梯度里 **20.75% 静默变 0**(loss scaling 即把它们整体左移出危险区)。③**loss scaling**:乘 S 上移,但 S 太大会**溢出到 inf**,且有效精度上限被尾数钉死 —— 实测 S ≥ 2^18 后精度饱和在 2^-11 不再改善。④**FP32 master weights**:纯 FP16 下 `lr·grad` 常小于当前权重的一个 ulp(1.0 附近 ulp = 2^-10 ≈ 9.77e-04,而典型 lr 是 1e-4~1e-7),更新被**四舍五入吃掉** —— 实测 20000 步 FP16 副本纹丝不动,FP32 master 累计下降 2e-3(1000 步时 1.19e-04,解析值 1.00e-04,差值来自 FP32 自身 2^-24 的亚 ulp 累积);单步更新比 ulp **小 3~4 个数量级**。⑤**动态缩放**:连续若干步不溢出就 ×2、一溢出就 ÷2 并跳过该步(实测日志里 scale 的倍增/减半与步数吻合)。⑥**FP32 累加**:FP16 直接累加 10000 项误差 1.06e-2,FP32 累加为 0 —— 这也是 tensor core 内部按 FP32 累加的原因。32 项 Python 断言全绿 | Python(192+105 行) + Go(197 行) | 2026-09-15 |
| 237 | `06-DevOps/01-容器化/Docker层缓存失效/` | BuildKit 构建缓存的失效规则:①**缓存键是链式的** —— 每一步的键 = H(父层键 + 本指令摘要),父层一变后面所有键都跟着变,失效自动级联、无法跳步复用(实测改一行源码:好行序 4 hit/2 miss、坏行序 2 hit/3 miss)。②**多数指令只比指令文本**,只有 `ADD`/`COPY`/`RUN --mount=type=bind` 才 "calculate a cache checksum from file metadata"。③**mtime 被显式排除** —— "If only the mtime of the copied files have changed, the cache is not invalidated",实测平移 30 天 mtime 后 6 步全命中;而 `chmod` 动了权限位就从 COPY 起级联重建。④**普通 RUN 不看文件系统** —— "just the command string itself is used to find a match",故 `RUN apt-get update` 永远命中、一周后重建包还是旧的,只有命令串变了才重建。⑤**构建密钥内容不参与缓存**但 id/挂载路径参与(内容轮换仍命中、改名则失效),要强制失效须另传 `ARG CACHEBUST`。⑥`WORKDIR` 尊重 `SOURCE_DATE_EPOCH`,改它会让 WORKDIR 及后续全部失效。⑦**文档版本分歧**:旧版页写 "the modification time **and size** file metadata",现行页改为 "cache checksum from file metadata" 且明确 mtime 不参与 —— README 以现行页为准并显式记录该差异。18 项 Python 断言全绿 | Python(284 行) + Go(234 行) | 2026-09-16 |
| 238 | `06-DevOps/01-容器化/多阶段构建/` | 多阶段构建与镜像瘦身:①**只有 target 阶段的文件系统成为最终镜像** —— "The Go SDK and any intermediate artifacts are left behind",实测单阶段 814 MB vs 多阶段 14 MB,差值正是被留下的工具链基础层 + 源码树。②**`COPY --from` 只搬指定路径**,源与目标各写各的(源 `/out/app` → 目标 `/app`),引用不存在的源路径直接构建失败。③**`FROM <stage>` 与 `COPY --from=<stage>` 取同样的参数**(官方博客 "Inheriting from a stage"),故 `FROM builder AS build1` 会继承整个文件系统。④**BuildKit 只构建 target 依赖的阶段**,legacy builder "processes all stages of a Dockerfile leading up to the selected --target" —— 官方例子 `--target stage2` 时 BuildKit 处理 base+stage2、跳过 stage1,实测 3 个阶段 vs 4 个。⑤**攻击面**:运行时镜像里没有编译器,应用被攻破也无法在容器内重新编译 payload。⑥`--cache-to` 的 `mode=max` "exports every layer even those of intermediate steps",所以"构建阶段不进镜像"说的是**不被 manifest 引用**,不等于缓存里没有(源码树可能被一起导出)。⑦`--from` 里不能含构建参数(阶段依赖须在构建开始前确定)。20 项 Python 断言全绿 | Python(279 行) + Go(285 行) | 2026-09-16 |
| 239 | `06-DevOps/01-容器化/UserNamespace-UID映射/` | user namespace 的 UID/GID 映射:①**行格式 `ID-inside-ns ID-outside-ns size`,第二字段含义随读者变化** —— 读它的进程与 pid 同命名空间时表示"父命名空间的起始 ID",不同命名空间时表示"读它的进程所在命名空间的起始 ID"。②**只能写一次**(再次写入 EPERM);须换行结尾、须偏移 0、总字节 < 一页、至少一行,否则 EINVAL;行数上限 4.16+ 为 340、更早为 5。③**行数上限与页大小互相挤压** —— 341 行先被页大小拦下(4323 B > 4096 B),340 行只有紧凑到每行 ≤12 字节(实测 3184 B)才塞得进一页。④**无特权写入的三重限制**:只能一行、size 必须为 1、必须把自身在父 ns 的有效 UID 映射进去,且写入者与创建者 euid 相同。⑤**gid_map 需先 `setgroups("deny")`**(永久禁用 setgroups),否则 EPERM。⑥**Linux 5.12 起映射到父 ns 的 UID 0 需 CAP_SETFCAP**(补"缺该能力的 UID 0 进程可造出带命名空间文件能力的二进制"这一漏洞)。⑦**未映射 ID 一律折算成 overflow 65534** —— 官方 rootless 数字 testuser:231072:65536 下容器 0→宿主 231072、65535→296607、65536→65534;**唯一例外**是读 uid_map 第二字段显示 4294967295 而非 65534(因 `(uid_t)-1` 另有含义)。⑧嵌套三层逐层翻译(ns2 999 → ns1 999 → 宿主 232071),上限 32 层超出 EUSERS。⑨文件属主未映射时 setuid 位被静默忽略(等价 MS_NOSUID)。39 项 Python 断言全绿 | C(287 行) + Python(294 行) + Go(226 行) | 2026-09-16 |
| 240 | `06-DevOps/01-容器化/Rootless容器/` | rootless 容器的前置、能力降级与诊断:①**守护进程与容器都在 user namespace 内**,与 userns-remap 的区别是"守护进程本身也没有 root";官方原文 "does not use binaries with SETUID bits or file capabilities, except newuidmap and newgidmap"。②**前置硬条件 subuid/subgid ≥ 65536**(官方示例 testuser:231072:65536),缺 newuidmap / `unprivileged_userns_clone=0` / `max_user_namespaces` 过小 / `$XDG_RUNTIME_DIR` 未设置都会阻断。③**存储驱动白名单与内核门槛**:overlay2 需 kernel ≥ 5.11、fuse-overlayfs 需 ≥ 4.18 且已安装、btrfs 次之、vfs 兜底 —— 实测 5.10 掉到 fuse-overlayfs、4.14 掉到 vfs。④**cgroup 只在 cgroup v2 + systemd 下可用**,且默认只委派 `memory pids`,于是 `--memory`/`--pids-limit` 生效而 `--cpus` **被静默忽略**;写 `Delegate=cpu cpuset io memory pids` 后才生效(委派 cpuset 需 systemd ≥ 244)。⑤**特权端口 `<1024`** 需给 rootlesskit 加 CAP_NET_BIND_SERVICE 或 `ip_unprivileged_port_start=0`。⑥**不支持清单**:AppArmor / Checkpoint / Overlay network / SCTP ports。⑦**四条官方报错原文→根因**映射(operation not permitted → userns_clone=0;no space left on device → max_user_namespaces 太小;No subuid ranges found → subuid 未配置;could not get XDG_RUNTIME_DIR → 未设置)。⑧网络在 RootlessKit 的 netns 内(`docker inspect` 的 IP 宿主不可直达、端口转发默认丢源 IP);`--cap-add` 只作用于容器自身 userns。43 项 Python 断言全绿 | Python(295 行) + Go(225 行) | 2026-09-16 |
| 241 | `06-DevOps/01-容器化/Cgroup-eBPF附加/` | cgroup v2 层级与 eBPF 附加:①**`cgroup.subtree_control` 四条规则** —— 只能启用 `cgroup.controllers` 里列出的、一次写多个**全有或全无**、同名控制器**最后一个生效**(`+cpu -cpu` 净效果是不启用)、子仍启用时父不能禁用(EBUSY)。②**no internal process** —— 含进程的 cgroup 不能启用域控制器(实测 3 个进程时 `+memory` 报 EBUSY,迁走进程后才成功),故加限制必须"先建子节点、迁进程、再启用";根 cgroup 豁免。③**在父上启用会为子节点创建 `cpu.`/`memory.` 前缀接口文件** —— 控制器接口文件属于父而非自身。④**ELF section 名决定 (程序类型, attach 类型)**:`cgroup_skb/ingress`→CGROUP_SKB+INET_INGRESS、`cgroup/connect4`→CGROUP_SOCK_ADDR+INET4_CONNECT、`cgroup/dev`→CGROUP_DEVICE、`cgroup/sysctl`→CGROUP_SYSCTL,且 `cgroup/skb` 与 `cgroup_skb/ingress` attach 类型相同;`xdp`/`tc`/`kprobe` 等非 cgroup 类不能挂到 cgroup。⑤**同一 attach type 上是一条有序程序链**,可单独 detach。⑥**BPF token 把能力检查从 init userns 搬到 BPF FS 所属 userns**("subsequent BPF operations performed with BPF token would be performing capabilities checks within that user namespace"),无 token 时 CAP_BPF/CAP_PERFMON/CAP_NET_ADMIN/CAP_SYS_ADMIN 必须在 init userns 检查;四项委派集合 cmds/maps/progs/attachs 逐项判定。⑦**token 不足以单独授权** —— 进程仍须在该 userns 内具备能力组合(TC/XDP 需 CAP_BPF+CAP_NET_ADMIN、tracing 需 CAP_BPF+CAP_PERFMON),token 跨 userns 无效,`BPF_TOKEN_CREATE` 自身需 `ns_capable(CAP_BPF)`。⑧C 侧还原 `struct bpf_insn` 的 8 字节布局与两个 4 位寄存器域,汇编 LD_MAP_FD→MOV→CALL→EXIT 并给出小端字节流与编解码往返一致。37 项 Python 断言全绿 | C(223 行) + Python(216+159 行) + Go(300 行) | 2026-09-16 |
| 242 | `07-数据存储/01-关系型/ReplicationBinlog/` | 主从复制与 binlog:①**SBR 记 SQL 语句、RBR 记行事件(WriteRows/UpdateRows/DeleteRows,8.0 默认)**,官方已弃用 binlog_format、未来 ROW 成唯一格式;STATEMENT 遇 UUID()/NOW() 等非确定性函数有主从不一致风险(MIXED 自动切 ROW)。②**三线程链路**:主库 dump 线程推 binlog → 从库 IO 线程写 relay log(落盘后才 ACK)→ SQL 线程重放。③**GTID 幂等**:格式 `source_id:transaction_id`,同一 GTID 在同一服务器 commit 后被忽略 → 重放天然防重复;auto-position 免手工指定 binlog 位点。④**半同步三要点**:等的是「≥1 从库把事件写入 relay log 并刷盘」而非从库执行完成;等待点 **AFTER_SYNC**(binlog 落盘后、引擎 commit 前,8.0 默认,ACK 的事务必已持久化)vs **AFTER_COMMIT**(commit 后才等,存在幻读窗口);**超时(默认 10s)静默降级异步**,从库追上再恢复 —— 必须监控 Rpl_semi_sync_master_status,否则丢失窗口悄然打开。⑤官方警告:半同步 failover 后旧主库可能含未 ACK 事务,不应复用为 source。Python 5 断言组全绿(格式对照/三线程/GTID 去重/超时降级+ACK 阻塞验证) | Python(155 行) + Go(123 行) | 2026-09-16 |
| 243 | `07-数据存储/01-关系型/LogicalDecodingCDC/` | 逻辑复制与 CDC:①**逻辑解码流水线**:SQL 变更 → 物理 WAL(LSN 递增)→ walsender+output plugin 解码为行级事件流(begin/insert/update/delete/commit)→ 消费者;**消费方式三选一**(流复制协议 / pg_logical_slot_get_changes / 自定义 output writer)。②**复制槽持留 WAL**:`confirmed_flush_lsn` 之前的 WAL 才能回收 —— 消费者停止 ACK 会把 pg_wal 撑爆(PG 文档 47.2.2 警告,孤儿槽是磁盘膨胀头号来源)。③**REPLICA IDENTITY**:无 PK 表的 UPDATE/DELETE 拿不到完整旧元组(before=null)→ 下游无法精确对账,建议 REPLICA IDENTITY FULL。④**Debezium 事件结构**:op(c/u/d/r-snapshot)+ before/after + source(lsn/slot);MySQL 走 binlog、PG 走逻辑流;**offset 存 connector 侧,重启断点续传**;部署三形态 Kafka Connect/Server/嵌入式 Engine;先全量 snapshot(op=r)再无缝转增量。⑤实现踩坑:事件必须自带**事件级 LSN** 而非批次尾 LSN(否则批内后续事件被误判已消费)—— 这正是实跑断言暴露的真 bug。Python 7 断言全绿 | Python(192 行) + Go(82 行) | 2026-09-16 |
| 244 | `07-数据存储/01-关系型/JoinAlgorithms/` | Join 算法五种:①**成本表(CMU 15-445 讲义数字 M=1000,m=100k,N=500,B=100,0.1ms/IO)**:朴素嵌套 M+m·N≈1.4h、块嵌套 M+⌈M/(B-2)⌉·N、索引嵌套 M+m·C、sort-merge M+N+sort≈0.75s、**hash join 3(M+N)≈0.45s**;实测 B=100 下块嵌套(6500)优于 SMJ(7500)。②**哈希连接**:build 小表 + probe 大表,只需外表(或其分区)进内存;内表可无限大。③**GRACE 分区哈希连接**:两表同一 h1 分区 → 跨分区必不匹配 → 逐对分区内存 join;分区 2(M+N)+probe (M+N)=总 3(M+N);单分区仍超内存换 h2 递归分区;单键重复超多用块嵌套兜底;hybrid=热分区留内存(讲义:难正确实现);N 页表约需 √N 缓冲(加 fudge factor f>1)。④**sort-merge**:外部归并排序成本 2P(1+⌈log_{B-1}⌈P/B⌉⌉);双指针 + **重复键回溯**(内表指针回重复段起点枚举全部组合,漏回溯=漏配对);讲义选型结论:hash 几乎总快,但 skew/已序/结果需排序时 SMJ 优。⑤TUM 现代硬件视角:build 易并行(各自 chunk 写共享表,锁竞争低)、probe 多读者零同步。5 种算法结果一致性 + 成本模型 + GRACE 分区性质 + probe 计数全断言 | Python(143 行) + Go(96 行) | 2026-09-16 |
| 245 | `07-数据存储/01-关系型/MaterializedView/` | 物化视图与增量刷新:①**与视图/表的差异(PG 官方)**:查询时直接读持久化数据、不能直接 DML、**可建索引**;定义(查询)像视图一样存进系统目录,解析器眼中就是 relation;pg_class.relispopulated 标记填充状态。②**官方性能示例**:file_fdw 外表查询 188ms → 物化视图 index-only scan 0.117ms(1600×);fdw 无索引而物化视图可建 gist/trgm(1431ms→198ms)。③**全量 REFRESH**:重算+整体替换,期间独占锁**阻塞所有 SELECT**;④**CONCURRENTLY**:前置条件是**物化视图上的唯一索引**(缺则直接报错,断言复现);流程=临时版本重算 → 按唯一键 diff(删/插/改)→ 原子切换 → 读全程不阻塞;代价是重算+diff。⑤刷新失败会让 relispopulated=false,查询直接报错;REFRESH 后统计信息不自动更新,老 ANALYZE 会误导优化器。Python 5 断言全绿(创建即物化/全量阻塞/无唯一索引报错/diff 三态/再刷新) | Python(127 行) + Go(79 行) | 2026-09-16 |
| 246 | `07-数据存储/01-关系型/OnlineDDL/` | 在线 DDL 与 gh-ost:①**三算法矩阵(MySQL 8.0 手册)**:INSTANT 只改数据字典(秒级;加列 8.0.12+ 默认、8.0.29+ 任意位置)、INPLACE 引擎内就地(可重建可不重建,多数允许 LOCK=NONE)、COPY 服务器层临时表全量复制(共享锁只读);选型 INSTANT→INPLACE→COPY;显式 ALGORITHM 不被支持时**直接报错**不静默降级。②**INPLACE 优于 COPY 三官方理由**(即使同样重建表):无服务器层临时表→无额外 undo/redo、二级索引条目预排序、不用 change buffer。③**MDL 独占只出现在 prepare 开始与 commit 收尾两端**;执行期并发 DML 记入 **row log**、commit 阶段应用;长事务会卡住 commit 独占 → 后续查询全体排队(MDL 雪崩)。④**INSTANT 行版本**:每次瞬时加/删列 +1,**上限 64**(ERROR 4092),重建型 ALTER/OPTIMIZE 清零。⑤**gh-ost**:ghost 表 + **订阅 binlog 流**(区别于 pt-osc 触发器)异步回放 copy 期间的变更 → 原子 cut-over 换名;可真暂停/在从库先行验证(--test-on-replica)/throttle 让主库完全回原生负载;依赖 ROW 格式(SBR 不可用)。Python 5 断言全绿(INSTANT 元数据+版本上限/row log 应用/COPY 重命名/支持矩阵优先级/gh-ost 三步) | Python(151 行) + Go(106 行) | 2026-09-16 |
| 247 | `08-安全/01-密码学/哈希/SHA-3-Keccak/` | SHA-3/Keccak-f[1600] 海绵函数(FIPS 202:状态 5×5×64bit=1600,24 轮 θ 列奇偶 D[x]=C[x-1]⊕rot1(C[x+1]) / ρ+π 走位 B[y,2x+3y]=rot(A[x,y],r[x,y]) / χ 唯一非线性 A=B⊕(¬B[x+1]∧B[x+2]) / ι LFSR 轮常数破对称;pad10*1+域分离后缀 SHA3=0x06 SHAKE=0x1F,末块剩 1 字节时 0x06|0x80=0x86 合并;rate/capacity 分档 SHA3-256=136/512 SHAKE128=168/256;5 demo:NIST 官方向量空串+abc / SHAKE128/256 / 跨块 200-1000B hashlib 对照 / 雪崩 132/256 位 / padding 块边界;**坑:RC 常量表抄录错位靠官方 LFSR 生成式重推导捕获**) | C(185 行) + Python(165 行 含 hashlib 对照全 PASS) + Go(218 行 RC 同 LFSR 自产) | 2026-09-16 |
| 248 | `08-安全/01-密码学/密钥派生/HKDF/` | HKDF Extract-and-Expand(RFC 5869:Extract PRK=HMAC(salt,IKM) 浓缩不均匀熵,salt 为 HMAC key、IKM 为消息;Expand T(i)=HMAC(PRK,T(i-1)|info|i) 计数器单字节,OKM 前 L 字节 L≤255*HashLen;缺省 salt=HashLen 个 0x00 非"空";info 域分离实现多密钥隔离,TLS 1.3 用它搭秘密树;5 demo:A.1 基本向量 / A.2 长 IKM+salt L=82 跨 3 块 / A.3 空 salt+info / A.7 缺省 salt==零字节(SHA-1) / info 域分离+IKM 雪崩+L 边界;**坑:Extract 参数顺序(salt 作 key)最易写反**) | C(203 行 自含 SHA-256+HMAC) + Python(118 行 RFC A.1-A.3/A.7 全 PASS) + Go(127 行) | 2026-09-16 |
| 249 | `08-安全/01-密码学/签名/ECDSA-P256/` | ECDSA + RFC 6979 确定性签名(P-256 参数 SEC2 v2 §2.4.2,seed 可验证随机;仿射点加 λ=(y2-y1)/(x2-x1) 倍点 λ=(3x²+a)/(2y),double-and-add;签名 s=k⁻¹(e+r·x) mod n 验证 u1·G+u2·Q;RFC 6979 HMAC-DRBG:bx=int2octets(x)‖bits2octets(h1 mod n),V=0x01×32 K=0x00×32 两轮 K/V 更新,T=HMAC(K,V) 逐候选,k 与 n 比较而非取模防偏置;5 demo:曲线阶自检 n·G=∞ / A.2.5 sample+test 的 k/r/s 逐字段 / 验证+篡改拒绝 / 确定性+私钥雪崩 / 20 组 round-trip;**坑:bits2octets=h1 mod n 非 h1 原值;仿射点加 r 与 p1 别名先写后读**) | C(ecdsa.c 209 行+bn256.c 206 行 手写 4×64 limb 大数+移位-比较-相减模归约+二元扩展欧几里得求逆,向量运行时 hex 解析) + Python(150 行 A.2.5 全 PASS) + Go(270 行 math/big) | 2026-09-16 |
| 250 | `08-安全/01-密码学/消息认证/HOTP-TOTP/` | HOTP/TOTP 一次性口令(RFC 4226:HOTP(K,C)=Truncate(HMAC-SHA-1(K,C)) mod 10^Digit,动态截断 offset=HS[19]&0xf 取 4 字节首字节&0x7f 成 31bit,位置由数据自选偏置更小;RFC 6238:T=floor((unix-T0)/X) X=30s 代计数器,SHA256/512 密钥 32/64B;验证端 ±1 步 resync 窗口+限速防在线穷举;5 demo:附录 D count 0-9 的 HMAC+HOTP / 附录 B 6 时间点×3 算法 / §5.4 截断细节 offset=0xa→872921 / 步进边界+窗口 / 密钥雪崩+失败锁定;**坑:计数器 8 字节大端,SHA512 组密钥是 64 字节非 20**) | C(239 行 自含 SHA-1+HMAC) + Python(110 行 向量全 PASS) + Go(136 行) | 2026-09-16 |
| 251 | `08-安全/01-密码学/密码派生/PBKDF2/` | PBKDF2 口令密钥派生(RFC 8018 §5.2:F(P,S,c,i) U1=PRF(P,S‖INT(i)) Uj=PRF(P,U(j-1)) F=U1⊕…⊕Uc,INT(i) 大端 4 字节块号;XOR 而非串联使时空折中无法跳步;c 迭代放大猜口令成本,盐唯一随机防彩虹表;WPA2/加密 zip 底座,OWASP 2023 建议 SHA-256 600k 次;5 demo:RFC 6070 c=1/c=2 / c=4096+hashlib 对照 / dkLen=25 跨块 / NUL 字节口令盐陷阱 / 确定性+迭代敏感+盐隔离+多块拼接;**坑:与 HKDF 计数器位数易混(PBKDF2 4 字节 vs HKDF 1 字节),C 语言层 NUL 截断**) | C(163 行 自含 SHA-1) + Python(85 行 RFC 6070 全 6 组向量 PASS) + Go(88 行) | 2026-09-16 |
| 252 | `09-语言学习/Python/异常链与异常组/` | 异常链(PEP 3134)与异常组(PEP 654):__context__ 隐式链四条语义(线程异常上下文初始 None/抛出时挂 context/抛后置自身/退出 except 或 return,continue,yield,break 时重置 None);__cause__ 显式链 raise...from 且隐含 __suppress_context__=True(PEP 409/415,PEP 3134 原文仅列为开放问题);from None 保留 context 但抑制显示;回溯两种消息("direct cause" vs "During handling",遍历 cause 优先,最新异常显示在最后);ExceptionGroup 构造(两仅位置参数/BaseExceptionGroup 全 Exception 叶子自动升级/混入 Base 报 TypeError/可嵌套成树);subgroup 无匹配返回 None 非空组、split 平凡分割另一侧 None、空嵌套组剔除、条件命中内部节点整棵子树纳入、derive 不重写丢子类类型且 __cause__/__context__/__traceback__ 按引用共享;except* 对不断缩小的 unhandled 组调 split,每子句至多执行一次拿到保留嵌套结构的子组,未匹配叶子自动 reraise,裸异常捕获时包装为 ExceptionGroup('',[原异常]),顺序敏感(子类检查与 except 相同),子句内禁 continue/break/return;**坑:except* ExceptionGroup 是运行期 TypeError("Use except instead")非 SyntaxError(实测仅真有异常要匹配时触发);自定义组重写 __new__ 后仍需 __init__ 吞参否则 "takes no keyword arguments"**) | Python(287 行;12 项断言全实跑通过) | 2026-09-16 |
| 253 | `09-语言学习/Python/typing与Protocol/` | typing 运行时行为(PEP 544 + PEP 695):Protocol 结构化子类型三原则(实现全部成员即子类型/protocol 间同规则/可变属性强制不变——x:float 协变接受 x:int 会被 arg.x=0.42 写坏);@runtime_checkable 仅做成员存在性检查(实测签名完全错误的 __len__(self,extra) 仍通过 isinstance),未装饰则 isinstance/issubclass 直接 TypeError;issubclass 仅 non-data protocol(含 x:int 的 data protocol 报 "non-method members don't support");protocol 默认实现仅显式继承可用(隐式结构子类型不在 MRO、super() 不可达);PEP 695:type 软关键字语句生成 TypeAliasType(__name__/__value__/__type_params__)递归别名免引号(惰性求值存代码对象首次访问才求值),class C[T: bound] 的 Generic 隐式入 MRO 与 __orig_bases__(实测 (Generic[T],)),def f[T](x:T) 的 __type_params__,新式参数 __infer_variance__ 恒 True 旧式默认 False;类型参数覆盖层作用域:对基类列表/注解可见、对参数默认值不可见(实测 NameError)、不进 globals/类命名空间;**坑:data protocol 的 issubclass 报错文本是 "non-method members" 非想当然的 "only non-data";新旧语法同一声明混用属静态检查器错误,运行期不拦**) | Python(223 行;10 项断言全实跑通过) | 2026-09-16 |
| 254 | `09-语言学习/Python/slots与内存布局/` | __slots__ 与对象内存布局(datamodel §3.3.2.4 原文精读):类级为每个名字建描述符+实例定长槽位,阻止 __dict__/__weakref__ 自动创建(实测 2 属性普通实例 ≈344B vs slots 48B,100 万实例 344MB→48MB);未列出属性赋值 AttributeError,加 '__dict__' 可恢复动态;父类无 slots 则子类实例 __dict__ 穿透回来,子类不声明 slots 也重新拿到 __dict__(子类只写新增名);'__weakref__' 需显式声明否则 weakref.ref 报 TypeError;类属性默认值会覆盖 slot 描述符导致 slot 失效(无 __dict__ 可存,赋值报错)→ 默认值放 __init__;多继承只允许一个非空 slot 布局否则 "multiple bases have instance lay-out conflict";同名 slot 子类遮蔽基类(基类变量不可访问);变长内建类型(int/bytes/tuple)+非空 slots 报 TypeError;__class__ 赋值仅相同 slots 允许;迭代器赋 __slots__ 描述符照建但自身变空迭代器(实测 list()==[]),dict 赋值键即 slot 名值作 docstring;dataclass(slots=True) 返回新类、查字段用 fields() 勿读 __slots__、frozen 走 object.__setattr__ 绕过、FrozenInstanceError 是 AttributeError 子类;**坑:slots 不预置默认值,未赋值直接读就是 AttributeError;写 "assert s2.a==0 or True" 这类废话断言仍会触发求值炸掉**) | Python(288 行;10 项断言全实跑通过) | 2026-09-16 |
| 255 | `09-语言学习/Python/字符串与编码/` | PEP 393 柔性字符串表示与编码:按 maxchar 动态选 1/2/4 字节三档 kind(ASCII/latin-1=1B、BMP=2B、非 BMP=4B),一个字符拉满整串("a"*999+"😀" 全串 4B);compact 布局数据内联紧跟对象头(实测 ASCII 每 +1 字符恰好 +1B、ASCII 头 42B vs latin-1 头 60B),纯 ASCII 时 data 与 utf8 指针同内存(仅 latin-1 不够);拼接按结果 maxchar 重算 kind 不降档;UTF-8 变长 1/2/3/4B(é=C3A9、中=E4B8AD、😀=F09F9880);latin-1 编不了 BMP 字符;孤立代理 strict 报 UnicodeEncodeError/surrogatepass 放行 ED A0 80;解码 strict 带 start/reason,replace→U+FFFD、ignore 丢弃;比较/len/索引按码点区域无关(Z<a、z<é,len("😀")==1,索引 O(1) 是等宽存储收益);驻留:标识符与编译期常量折叠同对象,非标识符动态串不自动驻留但 sys.intern 后共享;Django 实测 Unicode 存储 65378540B→2216807B(降 65%,35713/36000 个是 ASCII),代价是遍历基准慢 1%-30%;**坑:推字符宽要用 getsizeof 斜率差分(不同 kind 结构头不同,直接减空串基数会混入头差);"hello"+"!" 常量折叠成同一常量,验证驻留须经变量运行期构造;截断 UTF-8 序列报错位置是首字节位置 0 非坏字节位置(3.13 实测)**) | Python(153 行;7 项断言全实跑通过) | 2026-09-16 |
| 256 | `09-语言学习/Python/decimal与fractions/` | 数值精度模型:decimal 三元组(符号+系数+指数)Decimal((0,(3,1,4),-2))==3.14,尾零保留显著性(1.30+1.20=2.50 但 ==2.5,compare_total 按表示比较才区分),教科书式乘法 1.30*1.20=1.5600;构造看输入运算看上下文(Decimal("3.1415926535") 保留 11 位,prec=6 加法才舍入),默认 prec=28+ROUND_HALF_EVEN(2.5→2、3.5→4 银行家舍入),8 种舍入模式,localcontext 临时切精度一元+舍回;精度不足破坏结合律(Knuth 实测 prec=8 时 (u+v)+w=9.5111111 而 u+(v+w)=10,prec=20 恢复);Decimal(3.14) 无损暴露二进制真值 3.140000000000000124344978758017532527446746826171875→永远用字符串构造;NaN ==恒False、序比较触发 InvalidOperation(默认在 trap)、compare() 返 NaN;Decimal+float 算术 TypeError 但比较允许;% 符号随被除数、// 向零截断(int 相反)而 x==(x//y)*y+x%y 恒成立;Fraction 自动约分分母恒正(16,-10)→-8/5,字符串解析含 '7e-6'/'-3/7'/'1.414213';Fraction(1.1) 是 float 真值 2476979795053773/2251799813685248≠11/10,limit_denominator 恢复(3.1415926535897932→355/113、cos(pi/3)→1/2),Fraction(Decimal('1.1'))==11/10;混算语义与 Decimal 相反:F(1,3)+0.5 合法返回 float;round 平局取偶 F(7,2)→4、F(5,2)→2;**坑:上下文 ROUND_UP=远离零非四舍五入;连分数逼近误差~1/F(n)²,20 步仅 3.7e-9 到 1e-12 需 30 步;写 "nan==nan is False" 链式比较炸断言(开发期 3 处断言自身错全是实跑暴露)**) | Python(196 行;11 项断言全实跑通过) | 2026-09-16 |
| 257 | `10-逆向工程/01-二进制逆向/ARM64指令集与调用约定/` | A64 定长 32 位编码 + AAPCS64:组标识位 [28:24]/[28:23] 分派、sf/op/S/shift/Rm/imm6/Rn/Rd 位域逐字段还原、别名折叠(`mov`/`ret` 是伪指令)、SP 与 XZR 共用编号 31 靠位域区分、逻辑组 S 变体其实是 opc=11 而无独立 S 位、ORR Xd,XZR,Xm → MOV 的别名条件是 rn==31;覆盖分支(寄存器/立即/条件/比较)/ADR-ADRP/加减(立即数与移位寄存器)/逻辑移位寄存器/宽立即数搬移/载入存储(无符号偏移、imm9 变址、pair)/NOP;AAPCS64 寄存器角色表 x0-x7 传参、x8 间接结果、x9-x15 caller-saved、x16/x17 IP0-IP1(veneer 可破坏)、x18 PR、x19-x28 callee-saved、x29 FP、x30 LR;SP mod 16 = 0;帧记录 = 两个 64 位值(低地址存前一帧指针、高地址存 LR)且链以地址 0 终止;NZCV 不跨调用保留;C++ this 占 X0;结构体返回走 X8;序言形态分类 stp-frame-pair / stp-pair-only / str-lr-preindex / leaf;**坑:pair 的 idx 位 [24:23] 在 0xA9BF7BFD 上是 0b11(pre-index)而非 0b10(纯偏移),写错会让全部位域断言看着很绿而语义反了;str_lr 形态序列首条必须是序言那一条;`sx(0x1FFFF,19)` 是正数(19 位里最高位为 0),拿它断言 == -1 必挂;`decode_bytes` 末条是 `ret x30` 而非别名形式,断言要写 `alias or text`** | Python(397 行)+ C(235 行,无工具链走人工审查 + 结构校验);75 项断言实跑通过(prologue_shape 44 / aapcs64_check 31) | 2026-09-16 |
| 258 | `10-逆向工程/01-二进制逆向/Ghidra反编译流水线/` | Ghidra 官方反编译流水线:Main Work Flow 15 阶段逐条标注本 demo 覆盖情况;主简化循环六步(构造 SSA / 死代码消除 / 类型传播 / 项重写 / CFG 调整 / 控制流结构恢复);SLEIGH 汇编 → p-code(Op/Block/Func 三结构,PUT/GET/WrTmp/STEXit 与临时变量 tN);**Heritage**(支配树与支配边界、phi 放置、支配树 DFS 重命名、增量式 SSA 只重放 heritaged 区段);SIDE_EFFECT 集合决定死代码删除边界;INDIRECT 别名标注;INT_SUB 规范化为加补码、07 阶段再转回;**坑:最小 SSA 必须配合活跃性检查,否则会在 B1 插进无人使用的 phi(实测 4 个 vs 期望 2 个);phi 的入边是「边上的使用」,校验时要按「定义支配对应前驱」判定而不是「支配本块」,否则 verify_ssa 全体误报;Op.__slots__ 漏了 src_name 会在 rename() 抛 AttributeError;断言取 ops[-1] 是 CBRANCH,INT_SUB 在 ops[-2];fmt() 不认 ("neg", v) 标记则断言读不出意图;Python 读写一律 newline="" 否则写入时引入 CRLF** | Python(440 行)+ Go(519 行,无工具链走人工审查 + 结构校验);21 项断言实跑通过 | 2026-09-16 |
| 259 | `10-逆向工程/01-二进制逆向/Ghidra-headless自动化/` | analyzeHeadless 自动化:命令行骨架与组合约束(-import 与 -process 互斥、脚本名必须带扩展名、-max-cpu 负数归一为 1、含路径分隔符的通配符必须加引号);**HeadlessContinuationOption 4×4 官方合并表**逐格抄录 + 用规则而非查表实现 merge();preScript / postScript 对照;两种模式(-import / -process)的处置差异(-deleteProject 只在 import 模式有意义、-readOnly 与 delete 语义冲突);`-okToDelete` 安全门;`getScriptArgs()` 与 askXxx 的消费顺序(参数被 ask 先吃掉,而 ask 取完会让后续 ask 抛 IndexOutOfBoundsException —— 这就是「GUI 能跑、headless 炸掉」的常见原因);.properties 的 key 格式;HeadlessScript 与 GhidraScript 的差异;**坑:把脚本参数当作脚本名去校验(报「脚本名必须带扩展名: arg1」),修法是 _pairs() 支持 (name, [args]) 元组形式且只对脚本名校验;元组参数被切分错(("S.java", ["a","b"]) 被展开成 ["a","b"]);max-cpu 断言里不该出现 -2** | Python(408 行)+ Go(268 行,无工具链走人工审查 + 结构校验);33 项断言实跑通过 | 2026-09-16 |
| 260 | `10-逆向工程/01-二进制逆向/二进制差分与函数匹配/` | BinDiff 原理复刻:官方 signature 三元组(基本块数/块间边数/调用数,极弱 —— 两档强指纹跑完后它会把「被删的函数」与「新增的函数」错配成 (1,0,0)↔(1,0,0),这正是 confidence 存在的理由);prime signature(助记符→唯一小素数全乘、顺序无关、抗立即数改动;product 口径 64 条指令即撑满 uint64,Go 版直接断言回溢到 0 而 sum 口径不溢出;manual 的 product 与源码走读的 sum 口径分歧如实标注);MD index(graph_util.h 精确公式:sqrt(w0)*in_deg(src)+sqrt(w1)*out_deg(src)+sqrt(w2)*in_deg(tgt)+sqrt(w3)*out_deg(tgt)+sqrt(w4)*level(src)+sqrt(w5)*level(tgt) 取倒数,kDefaultWeightsNode={2,3,5,7,0,0};顶点值 = 所有关联边之和且**求和前先排序**因浮点加法不可交换;**由默认权重直接推出:拓扑层系数为 0 ⇒ top-down 与 bottom-up 结果完全一致、relaxed 与默认权重等价** —— 把层系数调成 11/13 两个方向才分道扬镳);分阶段匹配(签名须在两侧都唯一、只在剩余集合上施加、已匹配数单调不减)+ drill down(worker 与 twin 指纹全同、两侧都歧义,靠「已匹配函数的唯一 callee」解开,无调用关系可依托的 twin 仍留在未匹配集);confidence(官方原文:均值经 sigmoid 压扁,断言两条行为性质)+ similarity(函数 25/15/10/**50**、二进制 35/25/10/10/**20**,均再乘 confidence,且只统计非库函数);13 个官方函数匹配算法清单按 match quality 降序、edge matching「更强但可能超慢 → 性能问题先关它」、address sequence 的两条额外约束;**坑:把块字典当邻接表传给 md_of_functions,`for m in graph.get(v, [])` 退化成遍历字典键把块名当边,孤立函数 MD index 不再是 0(断言当场失败);匹配元组是 (B 函数名, 算法, 置信度),下标写反一次挂 4 条断言;指纹算法必须能「弃权」(不引用字符串的函数返回 None 并在流水线里过滤),否则空集合彼此相等会互相配上 —— 官方 string references 原文即「至少引用 1 个才参与」** | Python(664 行)+ Go(587 行,无工具链走人工审查 + 结构校验);44 项断言实跑通过(流水线 33 / 指标 11) | 2026-09-16 |
| 261 | `10-逆向工程/01-二进制逆向/Ghidra脚本化批量重命名/` | Ghidra 脚本化批量改名:类层次 FlatProgramAPI ← GhidraScript ← YourScript 与脚本元数据注释(//@category 等);FlatProgramAPI 类注释「NO METHODS SHOULD EVER BE REMOVED ... Changing this class will break user scripts」正是 createSymbol 自 7.4 起被弃用却仍保留的原因;createLabel 三重载(含 namespace 版)、makePrimary、**makeUnique 口径(重名时把地址拼到名字后)**;`SourceType` 优先级链 **USER_DEFINED > IMPORTED > ANALYSIS > DEFAULT**(官方 isHigherPriorityThan 原文)与「Symbol source determines priority - user symbols won't be overwritten by analysis」;命名规则(首字符须字母/下划线、无空格、区分大小写、同命名空间内唯一)与自动生成名前缀 FUN_/DAT_/LAB_/SUB_;官方 BatchRename.java 的模式化改名流程(Select → Apply Pattern → **Preview changes** → Apply)抽象成**计划/预演/事务三段式**:build_plan 是纯函数(只挑自动名、模板 re.sub + {addr} 占位、名没变不进计划 ⇒ 幂等)、resolve_conflicts 按 makeUnique 拼地址、dry-run 逐字段不变、apply 在单个事务里执行且任一条非法名整批回滚;**坑:askXxx() 会先从 getScriptArgs() 取值,非交互脚本必须按固定顺序解析;默认名前缀判断区分大小写 ⇒ 小写 sub_ 被视为用户命名,首次批量改名后同规则拿不到候选(需显式关掉 only_default);Go 的 ReplaceAllString 用 $1 且引用不存在的捕获组会静默展开成空串(产出 impl_ 这种丢一半信息的名字),Python re.sub 用 \1 且直接抛 invalid group reference;{addr} 占位符必须在正则替换之后再做,否则地址里的十六进制字符会被正则二次匹配;改名要连带更新 Function 对象,否则 getFunctionAt(addr).getName() 还是旧名;同地址创建同名 label 官方是幂等的,跨地址重名才要处理** | Python(572 行)+ Go(537 行,无工具链走人工审查 + 结构校验);44 项断言实跑通过(符号表 20 / 批量 24) | 2026-09-16 |
| 262 | `11-性能分析/01-系统级剖析/bpftrace前端DSL/` | bpftrace 前端 DSL 词法/语法/引擎（bpftrace language.md + man 页 + one-liners 教程：探针短写别名表 t/k/kr/u/ur/p/i/s/h/w/it/rt/f/fr → tracepoint/kprobe/kretprobe/uprobe/uretprobe/profile/interval/software/hardware/watchpoint/iter/rawtracepoint/fentry/fexit，`k:func` 的冒号即别名分隔；表达式优先级爬升六级 `||` < `&&` < `== !=` < `< > <= >=` < `+ -` < `* / %`，一元 `!` 与括号最高；map/@ 与 map[key] 两种引用形态、聚合函数 count/sum/min/max/avg/hist/lhist 在赋值右侧出现即聚合；直方图桶边界是 2 的幂但**桶 0 是 `[0,1]` 而非 `[1,2]`**（官方 runqlat 输出首行为 `[0, 1)`），桶号 i≥1 才等于区间左端点，故 `hist_index(v)` = `0 if v<2 else v.bit_length()-1`；条宽按最大值归一到 52 列 `'%10s -> %-11s: %-9d|%-40s|'` 同族口径；`delete(@map, key)` 的 key 是**独立实参**、map 名不带方括号（写成 `@map[key]` 是错的），另有 `clear(@map)` 清值保键、`zero()` 置零保键；块结构 `probe /predicate/ { stmts }`，谓词为假整块跳过；Engine 为事件驱动、按块维护 scratch 作用域栈，`synthetic_events()` 造 3 类事件重放；30 项断言实跑通过） | C / Python / Go | 2026-09-16 |
| 263 | `11-性能分析/01-系统级剖析/USE方法检查清单/` | USE 方法落地检查清单（Gregg usemethod.html + use-linux.html + kernel iostats：USE = 对**每一个**资源逐项检查 Utilization 使用率 / Saturation 饱和度 / Errors 错误，先列全资源再填指标，避免"盯着 CPU 忘记网卡"；物理资源 10 类（CPU、内存、网络接口、存储设备与控制器、互联总线、I/O 通道、…）与软件资源 4 类（互斥锁、线程池、进程/文件描述符表、连接池）共 37 格；阈值口径——利用率 ≥70% 记 CAUTION、≥100% 记 BOTTLENECK；饱和度的判据是队列长度而非使用率（0 → OK，非零 → QUEUED）；**CPU 使用率必须扣除 idle 与 iowait**（iowait 不是 CPU 在忙，是 CPU 在等），故 `util = 100 × (total - idle - iowait) / total`；饱和 = running 线程数 − CPU 核数（正数才是排队）；设备利用率用 `avgqu-sz` 与 `%util` 双看，`%util = iops / DEV_MAX_IOPS × 100`（对 SSD 是上界估计，NVMe 队列深度大时 `%util` 会长期 100% 却未饱和）；网卡 `util = bps / NIC_MAX_BPS`、饱和度看 `tx_queue` 与 `drops`；**瓶颈格 = 前一项的饱和**（CPU 队列长往往因磁盘 IOPS 打满导致 D 状态堆积，要在表里交叉指认）；38 项断言实跑通过） | C / Python / Go | 2026-09-16 |
| 264 | `11-性能分析/01-系统级剖析/容器性能归因/` | 容器性能归因（cgroup v2 内核文档 + Gregg DockerCon 2017 + PSI 文档：cgroup v2 的两条约束 —— **No Internal Process Constraint**（只有叶子 cgroup 能放进程，中间层只能放子 cgroup）与 **Top-down Constraint**（父 cgroup 的控制器必须先于子 cgroup 开启，且父的限额对子天然生效），故容器被限流时必须判断是**自身限额 / 祖先限额 / 宿主机争抢**三种之一；`cpu.stat` 七个字段中前 3 个（usage_usec / user_usec / system_usec）是**层级累计含所有后代**、后 5 个是 CFS 带宽（nr_periods / nr_throttled / throttled_usec / nr_bursts / burst_usec）**不含后代**，而 `cpu.stat.local`（新增）里的 `throttled_usec` 才是含祖先继承的"本层到底被限了多久"，两者口径不同不可直接比；限额换算 —— `cpu.max` 形如 `200000 100000`（quota/period，`max` 表示不限），或旧版 `cpu.shares` 口径下**容器可用 CPU = 100% × shares / 全部 busy shares**（而非 / 全部已分配 shares），故同一 shares 在不同忙闲比下限额不同，最小保证 = 100% × shares / 全部已分配；`memory.events` 的 `oom_kill` 与 `max` 计数区分"撞限额回收"与"OOM 杀进程"，`memory.max` 为 `max` 时 OOM 来自宿主机而非自身限额；诊断函数按 VERDICT 常量输出（CAP_THROTTLED / ANCESTOR_THROTTLED / HOST_CONTENDED / APP_QUEUED / CPU_OK / HIGH_THROTTLED / NEAR_MAX / OOM_KILLED）；32 项断言实跑通过） | C / Python / Go | 2026-09-16 |
| 265 | `11-性能分析/01-系统级剖析/runqlat与调度延迟/` | 运行队列延迟与调度统计（Gregg runqlat + kernel sched-stats + one-liners：runqlat 测的是**「可运行 → 真正拿到 CPU」的等待时长**，不是"在 CPU 上待了多久"（后者是 cpudist）；`sched_wakeup` → 下一条 `sched_switch` 的配对以 **tid 为键**，且 `sched_switch` 的 `next` 字段才是开始运行的那个人（`prev` 是被换下的）——配对取错字段会把延迟算反，孤儿 switch（找不到对应 wakeup）**不得记成 `now - 0`**；直方图桶边界是 2 的幂、桶 0 是 `[0,1)`，官方样例 `233 * 40 // 809 == 11` 个星号即条宽归一为 40 的算法；`/proc/schedstat` 的 `cpu<N>` 行有 9 个字段（1 `yld_count`、2 `array_exp` 遗留恒 0、3 `sched_count`、4 `sched_goidle`、5 `ttwu_count`、6 `ttwu_local`、7 `rq_cpu_time`、8 `run_delay`、9 `pcount`），`domain<N>` 行 45 个；**run_delay 占比 = 100 × Δrun_delay / Δrq_cpu_time**，只要先做差分再相除（直接除累计值会被开机时长稀释）；`/proc/<pid>/schedstat` 只有 3 个字段（在 CPU 上 ns、在运行队列等 ns、时间片次数）；**`kernel.sched_schedstats=0` 时 run_delay 字段存在但恒为 0，必须区分「未统计」与「0% 等待」**（返回 None 而非 0.0）；M/M/1 平均排队等待 `Wq = ρ/(1-ρ)`：ρ=0.5→1 倍服务时间、0.9→9 倍、0.98→49 倍，即饱和度涨 8.9% 等待涨 5.4 倍，ρ→1 发散 —— 这就是"CPU 到 90% 就崩"的算术原因；38 项断言实跑通过（含浮点容差，因 IEEE754 下 `0.9/0.1 == 8.999999999999998`） | C / Python / Go | 2026-09-16 |
| 266 | `11-性能分析/01-系统级剖析/中断与软中断剖析/` | 中断、软中断与网卡收包（kernel proc.html + irq-affinity + ia64/irq-redir + Red Hat 调优指南 §4.3：`/proc/interrupts` 每行 = `标签:` + N 个 per-CPU 计数 + 控制器类型 + 驱动/设备名列表（驱动列表是**逗号连接的单个 token**，不能按空格切）；**无编号的架构向量（`NMI:`/`LOC:`/`RES:`/`TLB:`）不是 IRQ**，判定依据必须是"标签能否当整数读"而**不能**维护白名单（新向量会漏）；`/proc/softirqs` 名字固定且由内核预定义：HI / TIMER / NET_TX / NET_RX / BLOCK / IRQ_POLL / TASKLET / SCHED / HRTIMER / RCU；**口径差异**：`/proc/stat` 的 `intr` 总数**大于**各字段之和（无编号向量只计入总数），而 `softirq` 总数**恰等于**各字段之和；`/proc/net/softnet_stat` 是**十六进制且无表头** —— 第 1 列已处理包数、第 2 列因 backlog 满而丢弃、第 3 列 `time_squeeze`（预算耗尽被迫退出），第 4 列起跨内核版本不稳定，不可依赖；三分归因：dropped > squeeze → RAISE_BACKLOG（`net.core.netdev_max_backlog`），仅 squeeze → RAISE_BUDGET（`net.core.netdev_budget` 默认 300），两者皆无 → REAL_WORK（确实是流量大）；`/proc/irq/<N>/smp_affinity` 是十六进制 CPU 位掩码、`default_smp_affinity` 为 `0xffffffff`，**不允许全 0**（会无 CPU 可投递），**逗号分组是低位组在前**（`ffffffff,ffffffff` = 64 核，`00000000,ffffffff` = 32-63 号核；要用掩码表达"仅 32-63 核"需补 32 个零），`smp_affinity_list` 才接受 `0-3`/`0,3`/`1024-1031` 人类写法；多队列网卡每个队列独立 IRQ（`eth0-TxRx-0`/`-1`），故绑核要逐队列设；53 项断言实跑通过） | C / Python / Go | 2026-09-16 |
| 267 | `01-游戏开发/01-服务端/网络编程/协议设计/FlatBuffers零拷贝/` | FlatBuffers 线格式与零拷贝访问(flatcc binary-format.md + flatbuffers.dev white paper/annotation + table.h 源码:**vtable = int16 数组[自身长度, 表长度, 字段0..n 的 table 内偏移]**,条目 0 = 缺失/默认,查字段先比 vtable[0] 越界即默认 —— 前后向兼容的基石;**table 首字段是 int32 soffset,vtable = table地址 − soffset**,flatc 放 table 前(正值)与 flatcc 放末尾(负值)都合法;uoffset 恒正加在自身存储地址上,back-to-front 构建下引用永远向前、缓冲区是 DAG 不可能有环;string = 长度(元素数)+内容+\0;vector 长度是**元素个数**不是字节数;缺省值字段不占存储(vtable 条目 0);同布局表共享 vtable;2GB 上限(soffset 有符号)+最小 8 字节;table 内字段顺序格式不规定只要求对齐;白皮书动机 = 移动端内存带宽,与 Protobuf「先 parse 成对象再访问」的本质差异;**逐字节复现 flatcc 教学缓冲区 44 字节(root 8 + NOOB 标识 + table 12 + string 10 + pad 2 + vtable 12),soffset e8ffffff 负值路径**;零拷贝用 memoryview/切片直接落在原缓冲区上;**坑:构建器忘了给 soffset 自己留 4 字节占位,回填时覆盖 say 的 uoffset 槽 —— 逐字节断言当场抓住**) | Python(16 断言,含参考字节逐字节比对/缺省不存储/前向兼容读旧数据/vtable 共享/vector 读取)+ Go(205 行,同组断言) | 2026-09-17 |
| 268 | `01-游戏开发/01-服务端/网络编程/协议设计/协议压缩Snappy/` | Snappy 块格式完整实现 + zstd 选型对比(google/snappy format_description.txt 官方规范:**preamble 小端 varint 未压缩长度,64→0x40、2097150→FE FF 7F;tag 低 2 位定类型 00 literal/01 copy1/10 copy2/11 copy4;literal 高 6 位存 len-1,len>60 用 60..63 档后跟 1~4 字节小端;copy1 长度 4..11 且 offset 11 位(高 3 位挤在 tag bits[5..7]、低 8 位在后续字节),copy2/copy4 长度 1..64;RLE 语义允许 len > offset 逐字节回引,xababab = literal "xab" + copy(2,4) 规范原文示例逐字节复现;官方压缩器 32KB 块不跨块但解码器不应依赖;无熵编码后端无成帧层**;zstd README 官方 Silesia 基准:zstd -1 比值 2.896(510/1550 MB/s) vs snappy 2.089(520/1500) vs lz4 2.101(675/3850),小数据需字典训练模式;gRPC grpc-encoding 官方枚举含 snappy 且压缩上下文不跨消息;解码器四守卫:offset=0/回引越界/截断/声明长度不符;**坑:扫 copy4 不能裸扫字节流里的 0x03,须按流结构逐元素解析,否则 literal 数据里的普通字节被误当 tag;LCG 低 8 位周期 256,300 字节内必现 4 字节重复,「纯随机」数据要换固定种子 random.randbytes**) | Python(26 断言:varint 规范数值/规范示例字节串/copy4 70000 长回引/四守卫)+ Go(269 行,解码器+编码器+守卫) | 2026-09-17 |
| 269 | `01-游戏开发/01-服务端/游戏服务端架构/心跳与断线重连/` | 应用层可靠性:ack 位图/心跳超时/断线重连(Glenn Fiedler Gaffer On Games 两篇:**TCP 可靠有序流丢一个包就队头阻塞,重发的旧包到达时早已没用,所以 FPS 全用 UDP 自建「通知式」可靠性;包头 [proto][seq][ack][ack_bits],每包恒定携带 33 个 ack(ack=最高已收 + 32 位位图覆盖其前 32 个),冗余发送对抗 ack 本身丢包,100% 丢包持续 >1 秒才会漏 ack;永不重发同一序号,丢的数据由应用层组新包携带 —— 重发消息必须带消息号去重;丢包是推断的:1 秒未 ack 即判丢;RTT 用 EMA α=10%;序号回绕用差值-半量程技巧((s1-s2)&0xFFFF < 0x8000 则比大小否则反向);心跳:稳态流量即保活无需 keep-alive,5 秒无包即超时回收槽位**;实测:回程丢 20% ack 包送达集仍完整、RTT EMA 收敛 37ms、断线重连后 20 条消息恰好一次投递;**坑:模拟交换必须逐包交错应答,先发完 100 个再统一回 ack 时位图只盖得住最后 33 个 —— 33-ack 窗口语义被断言当场暴露**) | Python(23 断言)+ Go(222 行,回绕/位图/冗余 ack/丢包推断/重连去重) | 2026-09-17 |
| 270 | `01-游戏开发/01-服务端/游戏服务端架构/跨服RPC/` | 跨服 RPC:自研实现 + gRPC over HTTP/2 逐项对照(grpc/grpc PROTOCOL-HTTP2.md 官方规范:**一次调用占一个 HTTP/2 stream,stream-id ≈ 自研 req_id,仅单连接内有效;Length-Prefixed-Message = 1 字节压缩标志 + 4 字节大端消息长,压缩上下文不跨消息;:path = /包名.服务名/方法名,:method 恒 POST,content-type 必须 application/grpc 前缀否则 HTTP 415;grpc-status 在 trailer 里,OK(0) 也必须发,HTTP 状态恒 200;grpc-timeout 最多 8 位数字+单位字符(H/M/S/m/u/n);PING 双方可发内容须精确回显,服务器 PING 超时→未完成调用全 CANCELLED、客户端→UNAVAILABLE;GOAWAY 优雅下线;REFUSED_STREAM→UNAVAILABLE 可重试;规范明言不假设幂等、无去重机制**;自研帧 [4B 大端帧长][类型][req_id][路径长][路径][载荷],RESP 带 1 字节状态码;实测:三并发乱序响应按 req_id 正确关联、110ms 判 TIMEOUT、晚到响应静默丢弃、业务错误按状态码传播、PUSH 无配对直达) | Python(12 断言)+ Go(300 行,chan 模拟进程间信道) | 2026-09-17 |
| 271 | `01-游戏开发/01-服务端/游戏服务端架构/全局服务器/` | 全局服务器与平台服进程组(CSDN MMO 架构简述 + gamedev.net MMO Server Layout + 游戏服务端编程实践:**平台服进程分工 = flserver 登录/dbaccessserver/roleregserver 角色名唯一/rolechangeserver 转区/globalserver 全区信息与排行/idipserver 运营邮件,普通服 = gatewayserver 桥/sceneserver/recordserver;三层拓扑 Auth/Character/Zone,换图由 zone 告诉客户端下一个 zone 地址**;GlobalServer 内核:login 发 token 按 (load,id) 选轻区;zone 心跳 10s 超时摘除且**其在线玩家一并从在线表除名**(消息自动转 offline_mail);**epoch 机制防旧实例**:重启 epoch+1,迟到心跳按 epoch 拒绝;全区 Top-N 榜插入后截断;rolereg 名字全区唯一且本人重复幂等;**两阶段转区状态机 free→locked→inflight→ack→free**:锁定期拒绝重复请求、stage2 源区立即除名杜绝双在线、卡死超时回滚可重发;实测 25 断言含摘除/epoch 拒绝/回滚全路径) | Python(25 断言)+ Go(300 行,同组断言子集) | 2026-09-17 |
| 272 | `02-Web开发/02-后端/Node.js/Libuv线程池/` | Libuv 线程池与 UV_THREADPOOL_SIZE(libuv 官方文档 + Node CLI 文档:默认 4/上限 1024/全局共享所有事件循环/首次使用时预分配;使用线程池的 API = 全部 fs(除 watcher 与显式同步)/异步 crypto(pbkdf2/scrypt/randomBytes/randomFill/generateKeyPair)/dns.lookup/全部 zlib(除同步);**跨 API 传染**——官方"一个慢 API 拖累其它看似无关的线程池 API";进程内 process.env 运行时设置不保证生效(线程池在运行时初始化阶段早于用户代码创建)。实测(16 核):8 并发 pbkdf2(100 万迭代)pool=1/4/8 = 931/370/280ms 近似线性加速;pool=1 时慢 pbkdf2(577ms)占池后 dns.lookup 553ms / fs.readFile 550ms 全被阻塞,pool=2 立即恢复 7/4ms;子进程内设 UV_THREADPOOL_SIZE=8 实测 ≈ pool=4 基线 → 未生效;网络 I/O 走 epoll/IOCP 不占线程池。**坑:探针里 readFile 若在 lookup 完成后才发出,慢任务已结束测不到阻塞——两个探针必须并发发出**) | JS(9 断言,6 子进程实测)+ TS(同组,--experimental-strip-types 实跑) | 2026-09-17 |
| 273 | `02-Web开发/02-后端/Node.js/Stream背压/` | Stream 背压机制(stream.html 原文精读经 curl 下载本地提取:write() 返回 true 当且仅当 **admit chunk 后**缓冲 < hwm,false 后应停手等 'drain',期间继续 write 仍会缓冲并返回 false;**hwm 是阈值不是限制**(官方原文),对象模式按个数、字节模式按字节、字符串模式按 UTF-16 码元;默认 16384B/16 个;pipe() "flow of data will be automatically managed so that the destination Writable is not overwhelmed";Three states:unpipe/背压把 readableFlowing 置 false,且此状态下**补挂 'data' 监听不会恢复流动**。实测:10 写入 hwm=4 返回序列 1110000000、恰 1 次 drain;无背压 200 条缓冲峰值 200 vs 有背压峰值 3;pipe 100 条 hwm=4 触发 25 次 'pause' 事件;hwm=8B 连写 3×8B 缓冲 24B 证明阈值非限制;对象模式 3 个 1KB 对象第 3 个即 false。**坑:interval 采样 isPaused() 是时序运气(流动窗口在单个宏任务内切换,采样器全程只见暂停态),观测流状态要用 'pause' 事件而非采样**) | JS(14 断言)+ TS(同组实跑) | 2026-09-17 |
| 274 | `02-Web开发/02-后端/Node.js/Cluster与WorkerThreads/` | Cluster 与 Worker Threads(cluster.md + worker_threads.md 官方:两种连接分发——RR(非 Windows 默认)vs 主进程发监听句柄给 worker 直连,后者官方警告"实践中分布极不均衡,曾观察到 8 进程中 70%+ 连接集中在 2 个";**Windows 默认 SCHED_NONE 实测 2 worker 共享端口 8 个串行请求 100% 落同一 worker**;disconnect() 优雅(关服等 'close' 再断 IPC,只管 server 连接)vs kill()(默认 SIGTERM 不等优雅断开,别名 destroy),区分标志 exitedAfterDisconnect;worker 全灭即拒新连接,fork 可重建恢复;worker_threads 与 cluster/child_process 本质差异 = **可共享内存**(transfer ArrayBuffer / share SharedArrayBuffer);结构化克隆保留环引用/Map/Set/RegExp 但**原型不保留**(类实例变普通对象、Buffer 收到端是普通 Uint8Array、URL 直接 DataCloneError);transferList 转移后发送端 byteLength=0;SAB 不能进 transferList 双端共享;terminate() 后 exitCode=1;'exit' 是 Worker 实例最后一个事件。**坑①:terminate() 的 Promise 在 'exit' 发出后才 resolve,之后再 once('exit') 永远等不到,且挂起 promise 不阻止进程退出——事件循环排干后静默退出 0 无任何报错,直接用 await terminate() 返回值;②:worker 停止后 w.resourceLimits 变空对象须运行中读;③:TS 的 import * as cluster 在 ESM 下拿不到 EventEmitter 方法要默认导入;strip-only 模式不支持构造器参数属性**) | JS(18 断言:cluster 7/克隆 5/worker 6)+ TS(同组实跑) | 2026-09-17 |
| 275 | `02-Web开发/02-后端/Node.js/async_hooks与AsyncLocalStorage/` | async_hooks 与 AsyncLocalStorage(async_hooks.md + async_context.md 官方:init 在资源构造未完成时即触发;before 对 handle 类 0-N 次、request 类恰 1 次;destroy 依赖 GC 可能永不触发(init 里存 resource 引用 = 内存泄漏);executionAsyncId 回答"何时"、triggerAsyncId 回答"为何";**Promise 默认不分配 asyncId,安装任意 hook 即启用追踪**且 before/after 只跑链式 Promise;hook 回调不得是 async 函数、回调内 console.log 会无限递归;ALS:run() 内 store 对回调及其创建的异步操作可见、抛错后 run() 重抛且上下文退出;exit() 相反(抛错后重新进入);**enterWith 会泄漏到同轮后续事件处理器(官方明示优先 run)**;disable() 后 getStore 恒 undefined 且**必须 disable 实例才能被 GC**。实测:Timeout 恰好 init→before→after 各 1 次;PROMISE 因果链 10←6(top)、11←10(then)、12←11;ALS 六项语义全验证;3 个交错异步请求(延迟 30/10/20ms)交错序 1,2,3→2,2,2→3,3,3→1,1,1 各 4 条日志归属零串扰;createHook+Map 复刻迷你 ALS 跨 setTimeout 链传播互不串扰(故意不做 destroy 清理,README 注明)。**坑:als.run(s, asyncCb) 的 Promise 必须 return 出去给调用方 await,否则断言跑在半截数据上(实测日志只剩 start)**) | JS(16 断言)+ TS(同组实跑) | 2026-09-17 |
| 276 | `02-Web开发/02-后端/Node.js/HTTP-Agent连接复用/` | HTTP Agent 连接池与 keep-alive(http.md Agent 节官方:Agent 管连接持久化复用,对同 host:port 维护待处理请求队列,队列空后 socket 销毁或入池取决于 keepAlive;maxSockets 默认 Infinity per origin,超出进 pending 队列;maxFreeSockets 256;scheduling 默认 lifo(v15.6.0 从 fifo 改来);**HTTP/1.1 keep-alive 响应按连接内顺序配对、无逐请求归属**;agentKeepAliveTimeoutBuffer 默认 1000ms 从 server 的 keep-alive hint 里扣除保证 agent 先关;池中 socket setKeepAlive(true)+unref 再复用时 ref;用完要 destroy();**Node 19 起 globalAgent 默认 keepAlive=true(v22.22.2 实测确认),而 new Agent() 选项默认仍是 false**;agent.getName() 返回 host:port:localAddress[:family] 作为复用键)。实测:keepAlive+maxSockets=1 下 5 串行请求只建 1 条 TCP 连接且 reusedSocket 序列 01111;无 keepAlive 5 请求 5 连接;4 并发×60ms 延迟 maxSockets=1 排队串行 252ms vs maxSockets=4 并行 80ms;响应后 socket 入 freeSockets 池→server keepAliveTimeout=2000 实际 ~3s 关空闲连接(含宽限)→池项移除→下一请求重建连接。**坑①:server keepAliveTimeout 设太小(100ms)时 hint−buffer 已是过去时刻,agent 直接销毁 socket 根本不进池;②:server 关闭空闲连接的实际时刻 ≈ 超时值+1s,测试等待窗口要留足**) | JS(9 断言,真实本地 server)+ TS(同组实跑) | 2026-09-17 |
| 277 | `03-系统编程/02-进程与线程/fork与僵尸进程/` | fork 与僵尸进程(man7 fork(2)+wait(2):fork 用 COW 页实现、只付页表+task 结构代价;子进程只保留调用线程、多线程 fork 后到 execve 前只能调 async-signal-safe;fd 复制指向同一 open file description 故共享偏移;僵尸=已终止未 wait、内核保留 PID/终止状态/资源使用三样最小信息、占进程表槽、表满无法建新进程;清除唯有一次成功 wait,父先死被 init(1)/subreaper 收养自动收尸;SIGCHLD 显式 SIG_IGN/SA_NOCLDWAIT→不变僵尸但 wait 阻塞到全部终止后 ECHILD(Linux 2.6 起符合 POSIX、2.4 不符);WEXITSTATUS=退出参数低 8 位故 _exit(300)→44、_exit(-1)→255;waitpid(WNOHANG) 三态 0/pid/-1;stdio 全缓冲被 fork 复制→子 exit(3) 刷继承缓冲致输出双份、_exit(2) 不刷;C 四场景实测(文件级缓冲复现双份/僵尸窗口 ps 可见 Z/信号致死 WTERMSIG=SIGKILL/SIG_IGN→ECHILD),Python 用纯数据结构模拟进程表 R→Z→REAPED 状态机+位编码宏+孤儿收养+WNOHANG 乱序轮询,Go 无 fork 原语走 os/exec 自 exec 演示 Run 内置收尸与退出码 8 位截断) | C / Python / Go | 2026-09-17 |
| 278 | `03-系统编程/02-进程与线程/生产者消费者/` | 生产者-消费者(man7 pthread_cond_wait(3)+go.dev/ref/mem+pkg/sync:条件变量必须配互斥锁否则"准备等待"与"发信号"竞态;cond_wait 原子地解锁+挂起、被唤醒后重新加锁才返回;**返回时谓词不保证成立→必须 while 循环**(signal 只保证唤醒≥1、无人等待则信号不累积);signal 若都在持锁后发,则"条件在加锁与挂起间被置真又漏掉"不可能发生;有界环形缓冲 count 独立维护避免 head==tail 空/满二义;Go channel 三条 happens-before:第 k 次发送→对应第 k 次接收完成、容量 C 的第 k 次接收→第 k+C 次发送完成、close→零值接收,故数据经 channel 传递无需锁,close 即广播(官方 Broadcast↔close 对照);Python 版含**虚假唤醒确定性证明**:wait() 直接返回的 SpuriousCond 下 if 写法必 IndexError、while 写法安全;真实多线程+混沌线程注入 notify_all 不动数据,40 项恰好消费一次零丢失) | C / Python / Go | 2026-09-17 |
| 279 | `03-系统编程/02-进程与线程/读者写者锁/` | 读者-写者锁(Open Group pthread_rwlock_rdlock 规范+pkg/sync:读者放行两档——TPS+SCHED_FIFO/RR 下仅当无写者持锁且无不低于己的写者排队;**无 TPS 时"无写者持锁但有写者排队"是否放行由实现自定义**=偏好策略自由度;同线程可持多个并发读锁须配对解锁,读锁总数上限实现定义超出 EAGAIN;tryrdlock 拿不到永不阻塞返回 EBUSY;已持写锁再 rdlock 可能死锁 EDEADLK(may fail);不返回 EINTR;Go sync.RWMutex 官方明文写者偏好——有 goroutine 调 Lock 且读者持锁时后续 RLock 阻塞到该写者获得并释放,故**禁止递归读锁**、RLock 不能升级 Lock、Lock 不能降级 RLock;Python 双策略锁只差一个谓词(readers-pref: while(writer);writers-pref: while(writer||waiting_writers)),确定性编排验证:读者偏好下写者排队时 R2 直接进入(读流不断则写者饿死)、写者偏好下 R2 排在写者后;真实线程不变量:读者重叠 max=4、写者段内零读者零并发写者;C 版递归 rdlock×3 配对解锁+tryrdlock/trywrlock EBUSY) | C / Python / Go | 2026-09-17 |
| 280 | `03-系统编程/02-协程/最小有栈协程/` | 最小有栈协程 ucontext(man7 makecontext(3):四件套=getcontext 快照→uc_stack 挂自备栈→uc_link 定后继→makecontext 绑入口;swapcontext 原子地存当前进 oucp 激活 ucp,再被切回时"看起来"返回 0;函数 return→激活 uc_link,NULL 则线程退出;**变参只能是 int**,指针仅 int/指针等宽机器碰巧可行、标准为 UB(glibc 2.8 起 x86-64 开口子);uc_stack 语义同 sigaltstack 不必管栈生长方向;HISTORY:SUSv2/POSIX.1-2001 有→POSIX.1-2008 移除(可移植性问题,官方建议改 pthread);C 版调度器模式:非末轮 swap 回调度器、末轮 return 走 uc_link,trace=9 切换点断言;Python 复刻协议状态机(int-only 拒非 int、uc_link=NULL→thread-exit、切换轨迹逐项断言)+无栈对照实验:**helper 里的 yield 不经 yield from 不传导到外层消费者**(无栈挂起点必须词法可见);Go 版对照:goroutine 深度 10 万递归安然(栈按需增长)vs 固定 64KiB 栈同深度必溢出+从 3 层嵌套深处让出再唤醒(有栈自由度)) | C / Python / Go | 2026-09-17 |
| 281 | `03-系统编程/02-进程与线程/CFS调度器/` | CFS 完全公平调度器(docs.kernel.org sched-design-CFS+sched-eevdf:80% 设计=模拟"理想精确多任务 CPU"每任务 1/nr_running 等速;真实 CPU 一次一个→引入 vruntime=实际运行时间按权重归一 p->se.vruntime 纳秒精度;**永远挑红黑树最左(vruntime 最小=执行最少)**,任务越跑越靠右,每个任务都能在确定时间内上 CPU;切换加 granularity 粒度防过度调度打爆缓存;rq->cfs.min_vruntime 单调递增跟踪队列最小 vruntime,**用于放置新激活实体尽量放左侧**;纳秒记账不依赖 jiffies/HZ、无旧式 time slice、唯一中心参数 base_slice_ns;策略 SCHED_NORMAL/BATCH/IDLE,调度类=sched_class 钩子(enqueue/dequeue/pick_next_task/task_tick);组调度 CONFIG_FAIR_GROUP_SCHED 的 cpu.shares;**6.6 起 EEVDF 取代 CFS**:lag(正=被欠/负=超额)定义资格、最早虚拟截止时间 VD 选取、睡眠任务 deferred dequeue+VRT 衰减防投机——CFS 文档 §2/3 的最左+min_vruntime 已不匹配现行代码但 vruntime 归一思想被继承;模拟断言:等权重 504/496 平分(误差≤1 粒度)、权重 1024:512→份额 664:336≈2:1 且 vruntime 收敛、迟到者 clamp 到 min_vruntime 后 E=L+500ms、睡眠者裸唤醒独占 1000ms vs clamp 后 1 粒度内让出、min_vruntime 全程单调) | C / Python / Go | 2026-09-17 |
| 282 | `04-移动开发/02-Android/网络编程/OkHttp拦截器与连接池/` | OkHttp 拦截器链与连接池(interceptors.md + connections.md + ConnectionPool.kt 官方:两层链——**应用拦截器**只关心最终请求/响应一次、不感知重定向与重试,`networkInterceptors` 在 **Connect 之后**故能看到真实网络请求;内置七层执行序 ①自定义应用 → ②RetryAndFollowUp(重定向/重试,`isRecoverable` 决定能否重试)→ ③Bridge(补 Host/Content-Type/Accept-Encoding gzip;自动解压后**从响应头删掉 Content-Encoding 与 Content-Length**)→ ④Cache(命中即短路**不建连接**)→ ⑤Connect(取/建连接)→ ⑥自定义网络 → ⑦CallServer(真正读写 socket);`proceed()` 可以**调用多次**(重试/重定向)也可以**不调用**(短路),这是链式拦截器与单一责任中间件的本质区别;连接池 `DEFAULT_MAX_IDLE=5` 空闲连接、`keepAliveDuration = 5 MINUTES`、`ConnectionPool.kt` 用 `cleanupRunnable` 周期回收;三级模型 **Address(host+port+代理+协议+cert)多对一 Route→Connection**,`Address.equals` 含 TLS 配置故换证书不会复用;连接池命中后 `StreamAllocation` 把流绑到连接,共享连接时**必须等当前流结束**才能复用池里那条;Happy Eyeballs 由 `RouteSelector` 双栈间快速回退(IPv6 失败即并行试 IPv4)。Python 复刻链 + 池的语义,22 断言全过(含**短路场景**:缓存预置后首层短路 → `server_calls == 0`;以及同一 `POST` 两次 proceed 触发服务器两次、`GET` 第二次被 Cache 拦截只调一次服务器的对照)) | Python(22 断言)/Kotlin/Java | 2026-09-17 |
| 283 | `04-移动开发/02-Android/并发编程/协程上下文与Flow/` | 协程上下文与 Flow(kotlinlang.org coroutines-overview/basics/coroutine-context-and-dispatchers/coroutines-flow 官方四篇全文:`CoroutineContext` 是**带键的元素索引集**,`+` 运算符右侧覆盖同键元素(`Job` 也照覆盖,所以 `launch(coroutineContext + Job())` 会**丢掉父子关系**);`Dispatchers.Default` 并行度 = max(2, CPU 核数) 而 `Dispatchers.IO` 是 `limitedParallelism` 默认 64,`Dispatchers.Unconfined` **在首个挂起点前跑在调用者线程**、之后交由恢复它的线程;结构化并发:父等所有子结束才算完成、**子失败取消父及其所有兄弟**、`CancellationException` 不视为失败、`coroutineScope` 会重抛而 `supervisorScope` 不传;冷流 `flow{}` **每次 collect 从头重跑**、热流 `StateFlow`/`SharedFlow` 共享且可 `replay`;`flowOn` 只改变**上游**上下文,下游仍在收集者上下文 —— 上游不得直接改外部状态(上下文保存性);`launch` 里 `emit` 必须换上下文否则违反上下文保存 → 实测抛异常。Python 复刻最小协程运行时(Job 树 + 状态机 + 调度器继承 + 结构化取消)21 断言 + Flow 模型(冷流/热流/`flowOn` 边界/上下文保存性违约)18 断言,共 39 断言全过;**开发期实跑抓到真 bug**:原 `_cancel` 只递归取消**子节点**不取消节点自身 → 父协程在子失败后仍显示 `COMPLETED`(存活兄弟跑到 t=50 把父"复活"),改为取消自身+整棵子树后才得到 `parent CANCELLED@5`) | Python(39 断言)/Kotlin | 2026-09-17 |
| 284 | `04-移动开发/02-Android/并发编程/WorkManager约束与重试/` | WorkManager 约束与重试(androidx Constraints.kt + Worker.kt + WorkRequest.kt 官方源码:`Constraints` 各字段**全部 AND** 且**默认全 false**(不设约束 = `Constraints.NONE`,但 `setRequiresDeviceIdle(true)` 后 `hasRequiresDeviceIdle()` 才是 true);退避 `DEFAULT_BACKOFF_DELAY_MILLIS = 30_000`、`MIN_BACKOFF_MILLIS = 10_000`、`MAX_BACKOFF_MILLIS = 5 小时`,`BackoffPolicy.EXPONENTIAL` 下第 n 次重试延迟按倍率增长并由上下限 clamp;`Worker.doWork()` 每个 Worker 实例**只被调用一次**,单次执行窗口 10 分钟(超时进 `Result.retry()` 或停止);`Result.failure()` 会让**下游依赖任务一并被阻断**(不是重试),`Result.retry()` 才走退避;`CoroutineWorker` 用 `Data.Builder()` 传参且 `Result.success(Data)` 的数据会传给下游;唯一任务 `enqueueUniqueWork` 三策略 —— **KEEP**(已有则整个新请求丢弃)、**REPLACE**(取消已有后入队新请求)、**APPEND**(串行追加,后者等前者终态);`ExistingWorkPolicy.APPEND` 与 `APPEND_OR_REPLACE` 的区别在前者一遇失败则后续全部不跑。Python 复刻约束判定 + 退避计算 + 执行窗口 + 唯一任务队列,40 断言全过(含 599s 允许 / 601s 超窗、REPLACE 后 `len(requests)==2` 且首个被 CANCELLED) | Python(40 断言)/Kotlin/Java | 2026-09-17 |
| 285 | `04-移动开发/02-Android/组件生命周期/Fragment生命周期与ViewModel/` | Fragment 生命周期与 ViewModel 作用域(androidx Fragment.java + ViewModel.kt/java 官方源码:Fragment `mState` **9 态** `INITIALIZING=-1 / ATTACHED=0 / CREATED=1 / VIEW_CREATED=2 / AWAITING_EXIT_EFFECTS=3 / ACTIVITY_CREATED=4 / STARTED=5 / AWAITING_ENTER_EFFECTS=6 / RESUMED=7`,两个 `AWAITING_*` 是"**状态已改、副作用回调尚未跑**"的中间拍点,由 `FragmentStateManager` 逐级推进时出现,业务代码不应依赖;**视图生命周期 ⊥ Fragment 生命周期** —— 进返回栈时 `onDestroyView` 会跑而 `onCreate`/`onDestroy` 不跑,回来时 `onCreateView` **重跑**、`onCreate` 只跑一次;`getViewLifecycleOwner()` 在 `onCreateView` 之前或 `onDestroyView` 之后调用**必抛 `IllegalStateException`**,故不能缓存在字段里;ViewModel 作用域由 `ViewModelStoreOwner` 决定 —— `by viewModels()` = Fragment 自己的 store,`by activityViewModels()` = 宿主 store(**只有 Activity 真正结束时才 `onCleared`**,配置变更保留 store 不触发);**清理顺序关键**:`ViewModel.clear()` 先遍历 `mBagOfTags` 逐个 `closeWithRuntimeException()` **再**调 `onCleared()`,而 `viewModelScope` 是懒创建并经 `putCloseable()` 存进 bag —— 所以 `viewModelScope` **先于 `onCleared()` 被取消**。Python 复刻状态机 + 作用域 + 清理顺序,33 断言全过) | Python(33 断言)/Kotlin | 2026-09-17 |
| 286 | `04-移动开发/02-Android/组件生命周期/Service三种形态/` | Service 三种形态(AOSP Service.java 类级 javadoc 全文:`Service` **不是进程也不是线程**,`onCreate`/`onStartCommand`/`onDestroy` 都在**主线程**,耗时工作须自开线程;三形态并存判定 —— "the system will keep them running as long as **either it is started or there are one or more connections to it with the `BIND_AUTO_CREATE` flag**";多次 `startService` **不嵌套**但产生多次 `onStartCommand`(startId 递增);**`stopSelf(int startId)` 只在 startId 等于最近一次 start 时才真正停止**,官方原文警告 *"If you call this function with the most-recently received ID before you have called it for previously received IDs, the service will be immediately stopped anyway"*(乱序必须先调最近 ID 会立即停止),`stopSelfResult(int)` 是同语义带 boolean 返回;`onStartCommand` 返回值四档 `START_STICKY_COMPATIBILITY=0 / START_STICKY=1 / START_NOT_STICKY=2 / START_REDELIVER_INTENT=3` —— STICKY 重建且无 pending 时**收 null intent**(必须判空)、NOT_STICKY 不重建、REDELIVER 重建并重投递最后一条 Intent(**直到用对应 startId 调 stopSelf(int)**),`START_FLAG_REDELIVERY=0x0001`/`START_FLAG_RETRY=0x0002`;`startForeground(int, Notification)` 的 id **不得为 0**,且它**不会让服务变成「已启动」**(名字误导),`setForeground(boolean)` 官方已是 **no-op**(实现只剩一行 `Log.w("setForeground: ignoring old API call")`);API 28+ 需 `FOREGROUND_SERVICE` 权限、API 29+ `type` 必须是 manifest `foregroundServiceType` 的**子集**、API 34 起未声明 type 抛 `MissingForegroundServiceTypeException`/声明成 NONE 抛 `InvalidForegroundServiceTypeException`/无权限抛 `SecurityException`、API 31+ targeting S 禁止**后台**启动前台服务(`ForegroundServiceStartNotAllowedException`,但**不影响 sticky 前台服务的重启**);进程优先级四档(回调中=前台进程 / 已启动=可杀但高于不可见 / 有绑定=不低于最重要客户端 / 前台服务=低内存非候选);`FLAG_GRANT_*_URI_PERMISSION` 的授权保持到服务为该 start 命令(或更晚的)调 `stopSelf(int)` 或完全停止 —— **必须正确回传 startId 的第二个理由**。Python 复刻状态机 + restart_plan,35 断言全过;**开发期实跑抓到设计 bug**:原 `dispatch()` 一次抽干整个队列,导致"stopSelf(1) 因最新是 2 而 no-op"的场景永不触发,改成**一次投递一条**并新增 `StopWithDeliveredId`/`StopWithLatestId` 两个子类对照官方警告的两种顺序) | Python(35 断言)/Kotlin/Java | 2026-09-17 |
| 287 | `05-AI与机器学习/02-强化学习/DQN经验回放与目标网络/` | DQN 经验回放与目标网络(Mnih 2013 arXiv:1312.5602 §3-§5:回放语义"randomize over the data"打破样本时序相关从而降低更新方差、目标网络冻结自举参数、半梯度 target 依赖权重、RMSProp/batch32/ε 1→0.1 线性退火/100 万帧内存;致命三要素 = 函数逼近+自举+off-policy;终止转移不自举 `y = r`;纯 stdlib 双层 MLP 复刻 5 种子消融:full 成功率 1.000/同号率 0.525、no_target 0.825/0.563、no_replay 0.800/**0.732**(0.732→0.525 即"breaks these correlations…reduces the variance of the updates"的量化);开发期两处真 bug:环境终点格未实现为吸收态、`_hidden_grad` 忽略调用方传入的 lr) | Python(12 断言) | 2026-09-17 |
| 288 | `05-AI与机器学习/02-强化学习/DoubleQLearning与最大化偏差/` | Double Q-learning 与最大化偏差(NIPS 2010 全文 + arXiv:1509.06461:max 作用在带噪估计上 ⇒ Jensen 不等式给出上偏,均匀误差闭式 E[max]=σ(m−1)/(m+1)(m=2→1/3、m→∞→1)、正态误差按 √(2 ln m) 增长;**双估计量 Lemma 1** `E[μ^B_{a*}] ≤ max_i E[X_i]` 且**等号成立当且仅当 P(a*∉M)=0**(实测 σ=0.2 时 0.000 不低估、σ=2.0 时 0.616 严格低估,逐行一致);Theorem 1 下界 `max Q ≥ V* + √(C/(m−1))` 代数分解 + 紧性构造(ε=(0.5,0.5,0.5,0.5,−2.0),4000 随机可行点残差 2.84e-14);Algorithm 1 抛硬币更新、argmax 用一张表取值用另一张、ε-greedy 用两表均值;网格世界 200 运行×10000 环境步:Q-learning 起点 max Q 抬到 +7.74/+14.45(真值 0.3627 的 20~40 倍)而 Double Q 策略回报 +0.188≈最优 +0.2;坑:"learning steps"≠"回合"(按回合实现要算 200 万环境步、7 分半没跑完,改环境步后 7 秒)、数值积分求 E[max] 漏负半轴项把 1/√π=0.5642 算成 0.6810、Baird 反例谱半径恒为 1.0000 做不出对比遂改交付论文自身实验) | Python(21 断言) | 2026-09-17 |
| 289 | `05-AI与机器学习/02-强化学习/资格迹TDLambda/` | 资格迹 TD(λ)(S&B 1st ed. §7.1-§7.4+§7.8 在线全本:n 步回报 R^(n) 与 TD(0)/MC 两端、"中间 n 误差最小"、λ-回报(7.3)几何权重和为 1、**前向视图非因果故不能直接实现**、资格迹(7.5)与 TD 误差(7.6)驱动的增量更新(7.7)、离线更新下前后向等价而在线只是近似;累积迹 vs 替换迹(Eq. 7.16)与 Singh & Sutton 动作值变体(Eq. 7.17);19 状态随机游走实测 E1 n 步 RMS n=1 0.1258/n=4 **0.0386**/n=16 0.0800/n=∞ 0.2791(比 TD(0) 好 3.3 倍)、E2 前向后向 λ=0 与 λ=1 精确相等 0.00e+00 中间最大差 2.11e-15、E3 最优 λ=0.8 RMS 0.0315、E4 替换迹 0.0393 < 累积迹 0.0602 且累积迹峰值可达 2.466~5.08 而替换迹恒 ≤1;坑:λ-回报截断尾项重复乘 (1−λ) 会静默吞尾(误差 9.20e-02)、浮点等值断言必须给容差、λ 与 γ 分工不同) | Python(10 断言) | 2026-09-17 |
| 290 | `05-AI与机器学习/02-强化学习/ActorCritic与GAE/` | Actor-Critic 与 GAE(arXiv:1506.02438 ar5iv 全文 + Spinning Up policy optimization 篇:S&B 1st ed. §6.6 actor-critic 原始形式;策略梯度里"动作之前的奖励"零均值非零方差 → reward-to-go 再减基线,EGLP 引理 E[∇logπ·b(s)]=0 保证无偏;k 步优势 Â^(k) 与两端 Â^(1)=δ / Â^(∞)=reward-to-go−V;**GAE = (1−λ)Σλ^{k−1}Â^(k) = Σ(γλ)^l δ_{t+l}** 且等价于后向递推 Â_t=δ_t+γλÂ_{t+1}(O(1) 内存);走廊 MDP 用高斯消元解 Bellman 得**精确** Vπ/Aπ 当靶子:E1 两条独立实现最大差 3.47e-17、E2 真实 Vπ 作基线偏差/标准误全程 ≤0.90 且 MSE 0.01850→0.09317(λ=1 是 5.0 倍,无偏时 MSE 全由方差决定)、E3 欠拟合 V̂ 作基线偏差/标准误 8.23→3.86(放大 4.3 倍)且**小 λ 偏差反而更小**(常值偏移被 δ 里的 −V(s_t)+γV(s_{t+1}) 抵消,说明"λ 小→偏差大"取决于误差结构)、E4 策略梯度批间方差稀疏回报 3.260→2.824 而稠密噪声奖励 td_residual 1.979e+01 « full 1.210e+02(约 6 倍),**"减基线一定降方差"是错的**;坑:按分组均值统计偏差会造出假偏差(必须固定 (s0,a0) 分层)) | Python(11 断言) | 2026-09-17 |
| 291 | `05-AI与机器学习/02-强化学习/SAC最大熵与温度自调节/` | SAC 最大熵与温度自调节(arXiv:1812.05905 ar5iv 全文为主依据 + arXiv:1801.01290 初版:最大熵目标 J=ΣE[r+αH],α "determines the relative importance of the entropy term versus the reward" 且 α→0 退回常规目标;软 Bellman backup V(s)=α·logΣexp(Q/α) 即软最大,由 V=E_a[Q−αlogπ] 对 π 求导置零得 Boltzmann 策略 π=exp((Q−V)/α);温度自动调节把熵写成**约束**(平均熵 ≥ H̄)再取对偶,对偶变量即 α 故可做梯度下降(Algorithm 2 的 `Adjust temperature` 一行),本 demo 用负反馈 α←max(1e-6, α−lr(H−H̄)),三个相差两个数量级的起点(0.05/1.0/4.0)**全部收敛到 α*=0.39009** 且 |H−H̄|=0.00000;5×5 网格精确软策略迭代:α→0 误差**线性**趋零(gap 比值恰 10.000)但**熵非单调**(α≈0.7 见顶 1.3617 后回落饱和于 1.3306 < ln4=1.3863,故"熵随 α 单调递增"这条断言是错的,改分层断言);E4 log-sum-exp 必须减最大值(朴素写法 OverflowError,稳定版 1001.3863=1000+log4);坑:温度更新符号写反是正反馈发散且不报错(只能靠多起点是否收敛同一点判断)、奖励量级与熵预算的量纲问题(goal_reward 1.0→20 才稳定)、目标熵口径(论文连续控制用微分熵 H̄=−dim(A),离散香农熵必须 ≥0,本 demo 取 0.8318 不可与论文数值直接比较)) | Python(14 断言) | 2026-09-17 |
| 292 | `06-DevOps/02-CI-CD/GitHubActions工作流语义/` | GitHub Actions 触发过滤与表达式语义(GitHub 官方 workflow-syntax + expressions 两页实读):`on` 过滤器按"模式→正则"翻译,`*` **不跨** `/`、`**` 跨多级、`?` 与 `+` 是**量词**而非通配符;`paths`/`branches` 列表的**顺序即语义** —— 从前往后逐条判定、**最后命中者胜出**(`!` 取反),从不命中才是 false(与"整表求或"的直觉相反);`paths` 与 `paths-ignore` 互斥;每次运行最多 1000 个提交/3000 个文件;`matrix` 的 `include` 是**两阶段**并入(先与已有组合合并、不能则新建组合),`exclude` 在 axis 展开后剔除;表达式语言:`${{ }}` 内 `==` 是**宽松相等**且带一张明确的类型转换表(字符串↔数字↔布尔↔null;`NaN` 参与任何关系运算恒假;`&&`/`||` **返回操作数本身**而非布尔);开发期两处真 bug:①`expand_matrix` 的 include 并入判据对新建组合(`origins[i] is None`)直接 `TypeError`;②jq/过滤器实现里"正向列表顺序语义"最初按"任意命中即为真"写(靠断言纠正);Python 110 断言全绿,Go 55 断言(无工具链,人工审查 + 结构自检) | Python / Go(110 / 55 断言) | 2026-09-17 |
| 293 | `06-DevOps/02-CI-CD/GitLabCI规则与父子流水线/` | GitLab CI `rules` 与父子流水线(GitLab 官方 ci/yaml + job_rules + downstream_pipelines 三页实读):`rules` **首次匹配即生效**,该 rule 的 `when`/`allow_failure` 随之携带;**`rules` 里的 `when: manual` 默认 `allow_failure=false`**(与 job 级 `manual` 默认 `true` 相反),无人点则堵住流水线;rule 内 `if` 的**正则字面量里变量不展开**(`/^release/` 是正则而非变量);`rules:changes` 在**非推送类**流水线(定时/手动/web)恒为真;`include` 合并顺序决定覆盖关系,且有 **150 个上限(含嵌套与重复)** 与 **30 秒**总预算;父子流水线**最多两层**(子流水线不能再触发孙),单父最多 1000 条下游、每个子流水线最多 3 个配置文件;子流水线里 `$CI_PIPELINE_SOURCE` **恒为** `parent_pipeline`;开发期修 bug 4 处:`_tokenize` 把 `/^release/` 当 `$` 前缀变量查表(正则字面量必须原样返回)、比较链 `A == B == C` 未拒绝(官方为创建期错误)、`fnmatch` 不支持 `**`(自实现 `glob_to_regex`, `**/`→`(?:.*/)?`)、断言文案与实际错误串不符;Python 67 断言全绿 | Python / Go(67 / 50 断言) | 2026-09-17 |
| 294 | `06-DevOps/02-CI-CD/Jenkins声明式流水线/` | Jenkins 声明式 Pipeline 语义(Jenkins 官方 pipeline syntax 页实读):`post` 十个条件按**官方固定顺序**执行(`cleanup` 恒最后,与声明顺序无关);**顶层 agent 的 timeout 不计任务分配时间,stage 级 agent 的 timeout 计入**(同一份耗时在两种 scope 下判定不同);stage 内求值时点固定为 `beforeOptions > beforeInput > beforeAgent > options > input > agent > when`;stage 体**四选一**(steps/stages/parallel/matrix)且 `parallel` 与 `matrix` **不可嵌套**;`matrix` 是**两段式**求值(`axis`/`exclude` 在阶段 1 静态展开、每个 cell 的 `when`/`environment` 等在阶段 2 运行时求值);`matrix` 至少需要 1 个 axis、cell 名是 axis 值拼接;开发期两处**断言自身**写错(未把 `unstable` 写进 declared 列表;用含 `failure` 的数据去断言 `failFast` 不触发——与断言名自相矛盾),再次印证"断言出错率高于实现";Python 69 断言全绿,`GOFLAGS` 无关 | Python / Go(69 / 62 断言) | 2026-09-17 |
| 295 | `06-DevOps/02-CI-CD/ArgoCD同步与漂移检测/` | Argo CD 自动同步与漂移检测(官方 auto_sync + diffing 两页实读):`syncPolicy.automated.enabled` **三态**(块缺失=未开、`enabled: null` **等同开启**、只有显式 `false` 才关);**只有 `OutOfSync` 才尝试自动同步**(`Synced`/`Unknown` 都不动);**每个 `(commit SHA1, 应用参数)` 组合只尝试一次**(成功过且未开 `selfHeal` 不再试、**失败过则即便 selfHeal 也不再重试**);`selfHeal` 打开后等待 self-heal timeout(**默认 5 秒**)再试;调和周期 `timeout.reconciliation` **默认 120s + 60s 抖动 = 上限 3 分钟**;`prune` **默认关闭**(Git 删了 live 还在),`allowEmpty` 再防"目标集为空清空应用";开启自动同步的应用**不能 rollback**;diff 定制:`ignoreDifferences` 四元组 `group/kind/name/namespace` 是**与**关系且 **core 组的 group 是空串而非 `v1`**(写 `v1` 规则静默失效)、`jsonPointers` 走 RFC6902 转义、`jqPathExpressions` 的**数组必须显式写 `[]`**(省略时路径落空)、`managedFieldsManagers` 按字段所有权忽略(HPA 拥有 `/spec/replicas`)、`core/Quantity` 规整(`100m` 与 `0.1` 同量,不规整即假漂移)、`ignoreResourceStatusField` 默认 `all`;开发期修 bug:①jq 通配原本只在**末尾**生效,链式 `.webhooks[]?.clientConfig.caBundle` 整个落空(改为通配可在任意层、后半段分发到每个元素);②`ignore_differences` 的 `kind` 缺省写死 `Deployment`,断言里没传 `kind` 导致忽略规则不生效;**同概念多处各写一份实现**的风险再次出现(`parse_jq_path` 的通配位置信息丢失);Python 77 断言全绿 | Python / Go(77 / 71 断言) | 2026-09-17 |
| 296 | `06-DevOps/02-CI-CD/制品晋升与语义化版本/` | 制品晋升与语义化版本(SemVer 2.0.0 规范原文 `semver.md` + OCI Distribution Spec `spec.md` + OCI Image Spec `descriptor.md` 三份实读):SemVer 优先级是**全序**——官方例子链 `1.0.0-alpha < 1.0.0-alpha.1 < 1.0.0-alpha.beta < 1.0.0-beta < 1.0.0-beta.2 < 1.0.0-beta.11 < 1.0.0-rc.1 < 1.0.0` 逐项断言、**数字标识符按数值比**(`beta.2 < beta.11`)、**数字恒低于非数字**(`alpha.1 < alpha.beta`)、**前缀相等时标识符多者更高**(`alpha < alpha.1`)、**构建元数据完全不参与比较**(`1.0.0+a` 与 `1.0.0+b` 连哈希都相同)、`X.Y.Z` 与预发布数字标识符**禁止前导零**、FAQ 明确 **`v1.2.3` 不是语义化版本**;OCI 侧:digest 语法 `algorithm ':' encoded`(algorithm 由 `[a-z0-9]+` 用 `[+._-]` 连接、encoded `[a-zA-Z0-9=_-]+`)、**`sha256` 的 encoded 必须是小写十六进制 `[a-f0-9]{64}`(原文 `[A-F]` MUST NOT be used)**、未注册算法只要语法合规**应当**放行(`multihash+base58:...`)、内容寻址 `ID(C) == D == '<alg>:' + Encode(H(C))`、仓库**必须逐字节**保存 manifest(同一逻辑对象的两种字节表示 = 两个制品)、tag ≤128 字符且**可变**而 digest **不可变**、push 先 blobs 后 manifest 且引用缺失报 `MANIFEST_BLOB_UNKNOWN`(**`subject` 例外**:允许先推 referrer)、`/tags/list` 必须 **ASCIIbetical** 序(故 `1.10.0` 在 `1.9.0` **之前**、`latest` 在最后,不能拿"列表最后一个"当最新版)、`n`/`last` 分页与 `Link: rel="next"`、`n=0` 返回空列表且**不带** Link、referrers API 支持的仓库**不得**返回 404 且客户端应回退 **referrers tag 方案**(算法截断 32 + `-` + encoded 截断 64、非法字符换 `-`,官方表格三行逐字复现);晋升 = **移动 tag 指针**(不重传字节、digest 不变即"制品没被改过")+ 版本闸门(来源 tag 必须是 SemVer、预发布默认不进 prod、通道当前版本不低于待晋升版本即拒降级、同 digest 幂等);开发期修 bug:①`put_manifest` 用 `reference.startswith("sha")` 判 tag/digest,把 **referrers 回退 tag(`sha256-<hex>`)误判成 digest** 而永不写入 tags(改为按是否含冒号判);②"重复晋升是幂等"的断言被前面步骤改动了 `prod` 指向而失败(断言顺序 bug,改为紧随首次晋升之后);③referrers 回退路径返回未解析的字节体(改为解析成 index);Python 108 断言全绿 | Python / Go(108 / 97 断言) | 2026-09-17 |
| 297 | `07-数据存储/02-NoSQL/MongoDB聚合与查询优化/` | MongoDB 聚合管道优化器与 ESR 索引(MongoDB 官方 aggregation-pipeline / aggregation-pipeline-optimization / aggregation-pipeline-limits / ESR guideline 四页实读):**先重排、后合并**是官方强制次序 —— "coalescence occurs *after* any sequence reordering optimization",先把 `$match` 挪到 `$sort` 前才可能出现新的相邻对;R1 投影下推靠**依赖分析**:官方示例的 `$match` 被拆成 4 个单键过滤器各自向左穿过"不影响它"的连续投影阶段(`name` 穿到最前、`maxTime`/`minTime` 被 `$addFields` 计算故只能停在它之后、`avgTime` 依赖 `$project` 计算结果原地不动),前移后 `{name}` 落到首阶段才可能命中索引;coalescence 五条:R5 `$sort`+`$limit`(夹 `$skip` 时 **`limit += skip`**,且中间出现 `$unwind`/`$group` 这类**改变文档数**的阶段就不许合并)、R6 双 `$limit` 取**较小值**、R7 双 `$skip` 取**之和**、R8 `$match`+`$match` 合成 `$and`、R9 `$lookup`+作用于其 `as` 字段的 `$unwind` 折进 `$lookup` 内部;ESR 的机制解释是"**索引键序决定能用的界**"——等值前缀之后的第一个字段才可能承担排序,一旦遇到范围字段其后的键再也不能提供界,故 ERS 只在范围谓词**高选择度**时更优(官方原话 "If your range predicate ... is very selective, then put it before sort fields",不是对错而是取舍);`$in` 的 **201 元素**分界(201 起退化为范围谓词、其后索引字段不再提供排序,官方明确该行为不保证跨版本稳定);1000 阶段上限 / **100 MB 阻塞阶段**(6.0 起 `allowDiskUseByDefault` 决定落盘还是直接报错)/ 16 MiB 单文档;阻塞型阶段清单 `$group $sort $bucket $bucketAuto $setWindowFields $sortByCount`;`$out`/`$merge` 只能末位、`$geoNear` 只能首位且三者不可重复出现;坑:①**`$unset` 也是投影阶段但不能按 `$project` 处理** —— 把 `{x:5}` 前移到 `$unset:[x]` 之前语义会从"匹配不到任何文档"变成"匹配 x==5 的文档",本 demo 取保守口径并在 README 标注官方未给该情形判定细节;②R3(`$redact`+`$match`)官方只说 "sometimes"、无完整条件,本 demo 由官方示例的可观察行为反推(仅前移纯等值过滤器)并标注为自定口径;③R5 漏掉 `$skip` 会多返回文档;④优化规则随版本变化("Optimizations are subject to change between releases")且 `explain` 里的优化后管道**不可手动执行** | Python / Go(67 断言) | 2026-09-17 |
| 298 | `07-数据存储/02-NoSQL/DynamoDB单表设计与容量/` | DynamoDB 单表设计与容量规划(AWS 官方 bp-partition-key-design / GSI / LSI / bp-partition-key-sharding / bp-partition-key-uniform-load / CheatSheet / ServiceQuotas / provisioned pricing 八页实读):容量**完全可算**、没有"大概" —— 读按 **4 KB 台阶**、写按 **1 KB 台阶**,`ceil(bytes/unit) × 一致性倍数`(强一致 1 / 最终一致 0.5 / 事务 2),官方示例逐条复现(10 KB 最终一致读 1.5、强一致 3、事务 6;3 KB 写 3、事务 6;10 KB 事务写 20);**取整口径因 API 而异**是核心陷阱 —— `BatchGetItem` 对**每一条**各自向上取整再相加(官方示例 1.5 KB + 6.5 KB 算成 12 KB 而非 8 KB),`Query`/`Scan` 只对**返回集合总大小**取整一次(合计 40.8 KB → 44 KB),单页 1 MB 靠 `LastEvaluatedKey` 翻页;**单分区硬顶 3000 RCU / 1000 WCU 是物理上限**,官方推导即 20 KB 条目强一致读吃 5 个单位 ⇒ 3000/5 = **600 次/秒**,且按**分区键值**而非按表计,自适应容量只做热点隔离、不抬高它;写分片 `N = ceil(target_WCU/1000)`(官方示例日期键拼 1..200 随机后缀,200000 WCU 正好得 200),代价是**读扇出 N 倍**再合并;LSI/GSI 六维对照(分区键同 vs 可不同、排序键、创建时机 **LSI 必须建表时**且最多 5 个 vs GSI 默认 20 个可后加、吞吐**共用** vs **独立**、LSI 支持强一致读而 GSI **仅最终一致**、GSI **不能回表**、LSI 每分区键值 **10 GB** 项目集合上限);**GSI 写容量不足会反噬主表写**(官方原文要求表与所有 GSI 都有足够写容量,否则主表写被限流),且 GSI 缺排序键时不写索引条目;坑:①**官方文档在 1000 与 1024 之间不自洽**(ServiceQuotas 写 "DynamoDB denotes 1 KB = 1024 bytes" 而读写操作页示例按 1000 进制算 96 KB),本 demo 一律取 **1024** 并在代码注释标注,这个 2.4% 的差会随条目数放大;②**回表按整条条目计费**(官方 "This charge is for reading each entire item from the table, not just the requested attributes"),构造"20 KB 主表条目 + 缺失属性仅 100 B"场景费用是 5.5 而非 1.5;③**LSI 建表后无法补建**;④"项目集合 10 GB 上限"**只对含 LSI 的表成立**;⑤超限后**缩小集合的写仍被允许**否则陷入死锁;⑥10 GB 逻辑用小阈值跑(单条上限 400 KB 意味真要造 10 GB 需 2.6 万条),常量本身另有独立断言 | Python / Go(75 断言) | 2026-09-17 |
| 299 | `07-数据存储/02-NoSQL/Cassandra数据建模与墓碑/` | Cassandra 查询驱动建模与墓碑生命周期(Apache Cassandra 文档源文件实读:`doc/developing/data-modeling/intro.adoc` + `doc/managing/operating/compaction/{tombstones,overview,stcs,lcs,twcs}.adoc`;官网 `cassandra.apache.org` 本机不可达故走 `github.com/apache/cassandra` blob 页精读,README 已标注该口径):**"In Cassandra, data modeling is query-driven"** —— 先列访问模式再为每个模式建表,反范式是常态;主键解析按官方"**首分量哈希成分区键、其余分量是聚簇键**"拆分,解析器必须区分**顶层逗号与括号内逗号**(`(id1,id2),c1,c2` → 分区键 2 + 聚簇键 2,括号内空白要规范化),分区键未全部等值绑定的查询必须扫全集群或加 `ALLOW FILTERING`;分区规模两条尺子(值个数 **100,000** / 磁盘 **100 MB**,官方均写 "below" 故**恰好达线不算超**);**删除即插入** —— "Cassandra treats a deletion as an insertion, and inserts a time-stamped deletion marker called a tombstone",清除需**三条同时**满足:①过 `gc_grace_seconds`(默认 **864000 秒 = 十天**)②本轮压缩**覆盖了所有**含该分区更旧数据的 SSTable ③若开 `only_purge_repaired_tombstones` 还须已跨副本修复 —— `purgeable()` 逐条返回失败原因,以区分"还没到时间"与"这次压缩不够"(两种情况的运维动作完全不同);**三节点僵尸复活最小复现**(一节点宕机 → 删除只写两节点 → 该节点在宽限期内被当修复补数据回集群);fully expired SSTable 与 `sstableexpiredblockers()` 的**阻塞**关系;三种压缩策略的三角权衡:STCS 攒 **4** 个大小相近者(分桶 `[avg×0.5, avg×1.5]`,写放大最低、空间放大最高)、LCS 每层 **10 倍**且 L0 超 **32** 个先走一次 STCS(`lcs_l0_failsafe()` 返回 `stcs_in_l0`/`lcs`)、TWCS 按时间窗口且**最怕乱序写入**(落进已关窗的桶会反复重写);坑:①`PRIMARY KEY (id)` 与 `((id))` 都是单分区键;②**TTL 过期 ≠ 墓碑**(过期单元格靠 `ts + ttl <= now` 判不可见但无墓碑,故 `ExpiredOnly` 需单独识别);③墓碑只遮挡**同分区同聚簇键**下更早的版本,留专门断言防"过度删除"错解;④`gc_grace` 边界是 `<=`(`now - tomb_ts == gc_grace` 仍算宽限期内);⑤LWT 官方要求 "should be kept to the minimum",`lwt_budget()` 每表计数并告警;⑥物化视图 4.0 仍是实验特性,`mv_risk()` 返回风险标记;⑦Go 侧断言函数用**变参** `detail ...string`,规避无工具链时固定实参个数漏网;⑧本 demo 开工前修复了上一轮拆分遗留的 Go 括号失衡(`cassandra_model.go` 的 `check`/`eqStrs` 函数体被切走一半) | Python / Go(68 断言) | 2026-09-17 |
| 300 | `07-数据存储/02-NoSQL/KV存储引擎LSM/` | LSM 存储引擎的 WAL/SSTable 格式与分层 compaction(LevelDB 官方 `doc/log_format.md` / `doc/table_format.md` / `doc/impl.md` + RocksDB wiki `Leveled-Compaction.md` / `RocksDB-Overview.md` / `Write-Stalls.md` 实读):WAL 按 **32 KB 块**切分,**7 字节头部** = CRC32C(4) + 长度(2) + 类型(1),跨块记录切 FULL / FIRST / MIDDLE / LAST 四型,块尾**剩余 ≤ 6 字节**必须补零(7 字节头放不下)——断言实际构造 1000 / 97270 / 8000 三尺寸得 FULL / FIRST+MIDDLE+LAST / FULL 且 `trailer_bytes == 6`,并专门验证"**恰好剩 7 字节**"时写出零长度 FIRST(`len==2, data_len==0, phys_len==7`);SSTable 三层结构(data block + index block + footer),footer **48 字节** = 两个 20 字节 BlockHandle + 8 字节 magic **`0xdb4775248b80fb57`**,filter block base 取 **2 KB**;分层两个口径 —— 静态 `10^L` MB(L1 文件 2 MB)与 **RocksDB 动态层级**(末层锚定在"实际大小"、向上逐层除以 10、低于 base/10 的层直接留空,实测 `{6:276GB, 5:27.6GB, 4:2.76GB, 3:0.276GB, 2:0, 1:0}`,官方称约 90% 数据落在末层),README 明确标注"官方示例 `num_levels=6` 标 L1..L6 而本 demo 把 `num_levels` 视为**含 L0**,故夹具取 7"的**口径差异**;**写停顿三类触发**(memtable 满 / L0 文件数超限 / 待压缩字节超限,顺序即优先级)与 `WriteStopped`;删除标记的可丢条件(更下层无更早版本 + 已过全部 snapshots);写放大 / 空间放大计算与 `level_summary`;坑:①纯函数里硬编码引擎实例导致不可测 → 抽出 `DynamicTargetsFrom(sizes, numLevels, base, multiplier)`;②Go 侧过滤已选表用指针 `!=` 会在重复元素时漏项 → 改 `drop map[*SSTable]bool`;③把假 `None` 条目塞进 `levels[0]` 做分数断言时崩在 `.size_bytes` → 引入 `FakeTable`;④拆分后尾部残留 `List[Entry]` 注解但 import 已删 → `NameError`;⑤`lsm_engine.py` 初版 417 行超 300 上限 → 经 `split_source.py` 多次拆分收成 `lsm_core.py` + `lsm_compact.py`(mixin)+ `lsm_engine.py` 160 行,Go 侧同法拆 6 文件 | Python / Go(64 断言) | 2026-09-17 |
| 301 | `07-数据存储/02-NoSQL/Protobuf编码/` | Protobuf 线格式逐字节实现(protobuf.dev 官方 `programming-guides/encoding/` + `programming-guides/proto3/` 实读):**varint 是 base-128** —— 每字节低 7 位是负载、最高位是续接位,负载按**小端**排(故 `150 → 0x96 0x01`),64 位值最多 **10 字节**;**wire type 六种**(0 VARINT / 1 I64 / 2 LEN / 3 SGROUP / 4 EGROUP / 5 I32),tag = `(field_number << 3) | wire_type`(`field 1` + VARINT → `0x08`);字段号 **1..536,870,911** 且 **19,000..19,999 由 protobuf 实现保留**(`validate_field_number`);官方最小示例 `message Test1 { int32 a = 1; }` 且 `a = 150` → `08 96 01` 逐字节断言,**嵌套消息** → `0a 03 08 96 01`;负数两条路:**intN 走补码恒占 10 字节**(`-2 → feffffffffffffffff01`),而 **sintN 走 ZigZag** `(n << 1) ^ (n >> (bits-1))`(`-500 → 999` 仅两字节),官方 ZigZag 对照表逐行断言;double/float 走 IEEE-754 定点位;LEN 前缀与 **packed 的双向兼容**(编码方用 packed,解码方必须同时接受 packed 与 unpacked,反之亦然);**未知字段按 wire type 跳过**(`skip_field` 含 group 配对);`SGROUP`/`EGROUP` 已废弃但解析器必须能配对跳过;**字段顺序不确定**(官方不得假设序列化输出稳定,同一逻辑消息可有多种字节表示);**Last One Wins** 与**嵌入消息合并**(同字段号重复出现时按 merge 语义递归合并而非覆盖),官方混合示例 `5:{1 2} 4:{"hello"} 5:{3}` 解析为 `nums=[1,2,3]`(packed 与非 packed 混用合并);坑:①Go 里 `uint64(-2)` **是编译错误**("constant -2 overflows uint64")→ 先 `neg := int64(-2)` 再 `uint64(neg)`;②`return DecodeMessage(...)` 多返回值类型不匹配(`map[string]interface{}` ≠ `interface{}`)→ 展开为两条语句并加注释,随后全仓扫描 `return Foo(` 形式确认无同类;③拆分后 doc 注释游离(`DecodeMessage` 的注释留在 `pb_merge.go` 尾、`EncodeMessage` 留在 `pb_group.go` 尾)→ 按精确串补回;④`pb_check.go` 残留未用 `strings` import;⑤`pb_codec.go` 349 行 / `pb_check.go` 331 行超限 → 拆出 `pb_merge.go` / `pb_group.go` / `pb_checkutil.go` | Python / Go(77 断言) | 2026-09-17 |
| 302 | `08-安全/01-Web安全/OAuth2-PKCE与授权码/` | OAuth 2.0 授权码流程 + PKCE(RFC 6749 §4.1 步骤 A–E + RFC 7636 §4.1/§4.2/§4.6 + RFC 9700 §2.1/§2.1.1/§4.2.4/§4.4.2.1 实读):授权请求只出现 `code_challenge = BASE64URL-ENCODE(SHA256(ASCII(code_verifier)))`,verifier(32 字节随机 → base64url → **43 字符**,字符集 `unreserved`、43..128)只在令牌端点出现 —— RFC 7636 **附录 B 向量**(`dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk` → `E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM`)在 Python 与 JS 两侧做**字符串级**断言;`redirect_uri` **精确字符串匹配**(RFC 9700 §2.1,唯一例外是原生 `localhost` 可变端口),大小写差异 / 多余后缀 / `http` scheme(非回环)一律拒绝,**校验失败时不重定向**(RFC 6749 §3.1.2.4 MUST NOT);授权码 **单次使用 + 10 分钟生命周期**(§4.1.2 "maximum 10 minutes is RECOMMENDED",本实现 600 秒闭区间),重放时 SHOULD **撤销**先前基于该码签发的全部令牌;PKCE **降级检测**:授权请求没带 challenge 时令牌端点收到 `code_verifier` 必须 `invalid_grant`(§4.8.2)、公共客户端无 challenge 则 `invalid_request`;`state` **首次使用后失效** + **RFC 9207 `iss`** 比对(不符即中止)→ 挡住 mix-up;`plain` 缺省但 §7.2 明确 SHOULD NOT;坑:①自检脚本里 `flow()` 已消费 state,"state 复用"断言不能再调一次 `callback`(实跑暴露)②`hmac.compare_digest` 防时序侧信道 ③错误响应也必须回填 `state` | Python / JavaScript(32 + 16 断言) | 2026-09-17 |
| 303 | `08-安全/01-Web安全/Cookie安全属性与SameSite/` | 浏览器侧 Cookie 存储模型 + SameSite + HSTS(RFC 6265 §5.1.3/§5.1.4/§5.3/§5.4 全文 + draft-ietf-httpbis-rfc6265bis-20 §4.1.2.7/§4.1.3/§5.4/§5.5/§5.6.7.2/§5.7 + RFC 6797 §6.1/§8 实读):`domain-match` **点边界**(`notexample.com` 不匹配 `example.com`,IP 不做后缀匹配)、`path-match` **不跨词边界**(`/docs` 匹配 `/docs/x` 不匹配 `/docsets`);`default-path` 取到**最右斜杠之前且不含该斜杠**(RFC 6265/6265bis 原文 `...up to, but not including, the right-most "/"`,故 `/docs/Web/HTTP/index.html` → `/docs/Web/HTTP`)—— **与 MDN 表述的 `/docs/Web/HTTP/` 存在口径差异**,README 已标注;`Max-Age` 优先于 `Expires`、**400 天硬截断**(§5.5)、单条 Set-Cookie **4096 字节**、属性值 1024 字节、每域 50 / 总量 3000 淘汰按 last-access;**UA 侧前缀必须大小写不敏感**(§5.4:否则 `__SeCuRe-SID=evil` 与服务端大小写不敏感解析叠加成可用攻击)、`__Host-` 需 Secure + host-only + **显式** `Path=/`;`SameSite=None` 必须配 Secure 否则整条丢弃;**非安全 cookie 不得覆盖同名 Secure cookie**(§5.7 step 21,**路径比较不对称** —— 攻击者仍可在更短路径种同名 cookie,两条同时进入 Cookie 头、路径长者在前,断言覆盖);SameSite 检索语义含 **Lax-allowing-unsafe 2 分钟例外**(§5.6.7.2,只对未显式写 SameSite 者生效);HSTS:`max-age` REQUIRED、指令重复则**整头作废**、未知指令忽略、**非安全传输上的 STS 头完全忽略**、同一响应多个头只取第一个、`max-age=0` 删除策略并连带清掉 includeSubDomains、父子域命中需 `includeSubDomains`、**IP 字面量不得标记**、`<meta http-equiv>` 不理会、§8.3 端口映射(80→443,其他端口保留,无端口不添加);**`preload` 不是 RFC 6797 的指令**(§12.3 讲的是预加载**设施**);坑:①省略 Domain 时未置 `host_only=True`(实跑断言抓出的真 bug)②"非安全覆盖"用例最初写成默认路径 `/` 而规范只拦 path-match 旧路径的情形(断言错)③"多个 STS 头只取第一个"要**同一响应**内传两个值,分两次 `note()` 是两轮响应 | Python / JavaScript(80 + 18 断言) | 2026-09-17 |
| 304 | `08-安全/01-Web安全/SSRF与URL解析歧义/` | SSRF 防护 + WHATWG URL 的 IPv4 主机解析(OWASP SSRF Prevention Cheat Sheet 全文 + WHATWG URL Standard §3.5/§4.4 实读,并用 node **原生 URL 实现**给复刻的解析器当标尺):`ipv4 number parser` 三进制 —— `0x`/`0X` → 16、前导 `0`(长度 ≥2)→ 8、否则 10,**非十进制写法要报 validation error 但不是失败**;`ipv4 parser` 按 `.` 拆段、尾点忽略、前 n-1 段 ≤255、**最后一段必须 < 256^(5-段数)**(1 段可吃满 32 位),故 `127.1` / `2130706433` / `0x7f000001` / `0x7f.1` / `0177.0.0.1` / `0x7f.0x0.0x0.0x1` 全是 `127.0.0.1`(11 条等价写法逐条断言),而 `256.1.1.1` / `1.2.3.4.5` / `1.2.3.65536` 是**非法 URL**;`ends in a number checker` 决定"算不算 IP 写法"(失败则整个 URL 非法,而非回落域名)→ 纯数字主机名 `http://12345/` 合法、`dead.beef` 仍是域名;**解析器分歧** `http://example.com\@evil.com`:WHATWG 把反斜杠当路径分隔符 → `example.com`,CPython `urlsplit` 按最后一个 `@` 切 userinfo → `evil.com`,**不一致即拒绝**(不调和);`is_public()` 先解包 **IPv4-mapped / NAT64 `64:ff9b::/96` / 6to4** 再判,否则 `http://[::ffff:127.0.0.1]/` 会漏(WHATWG 会把它序列化成 `[::ffff:7f00:1]`,字符串黑名单必然失效 —— 与 CPython `IPv6Address.__str__` 保留点分四段形成**口径差异**);Case 1 允许列表命中后**用允许列表条目自行重建 scheme/host/port/path**、Case 2 逐个校验全部 A/AAAA 记录(防 DNS pinning)、**禁用重定向跟随**、`file`/`gopher` 等 scheme 拒绝;坑:①断言把 IPv6 规范串写成 `::ffff:7f00:1`(CPython 实际输出 `::ffff:127.0.0.1`,断言错)②JS 侧"isPublic"最初只匹配 v4 正则导致 mapped 地址漏过(正是 demo 要讲的坑)③主机名按规范降小写后再比对,尾点 FQDN 不等价 | Python / JavaScript(74 + 29 断言) | 2026-09-17 |
| 305 | `08-安全/01-Web安全/访问控制与IDOR/` | 访问控制与对象级授权(OWASP Authorization Cheat Sheet 全文实读):**deny-overrides + 默认拒绝**(默认拒绝给的是显式决定 `default-deny` 而非"中立")、**每请求服务端校验**(客户端自称属性单独存 `untrusted` 仓、决策一律不看)、**对象级校验**是 IDOR 的全部内容("对某类资源有权限 ≠ 对该类每个对象有权限",CWE-639);三层模型叠加 —— RBAC 只认类型级动作(**断言演示它会把"读同事的发票"判为允许 = 横向越权**)、ABAC 管环境(在班/租户,同一角色不同环境结论不同)、ReBAC 管对象(owner / collaborator / **auditor 租户级**);**宽范围访问也必须显式授予** —— admin 角色本身不等于能碰任何对象,需 `relate(uid,"auditor","tenant:t1")` 且仍受租户边界约束;公开资源只读(public 不放行写);**会话级间接引用**(句柄 `h1` 只在签发会话内可解析,别人拿到解析不到,且句柄稳定可复用);**失败安全退出**(策略抛异常 → 拒绝,`guard()` 对外只给 `403`,rule/reason 留在 decision 与审计里,避免 CWE-209)、审计每条决策一行且顺序与调用一致;权限蔓延:角色重配后旧权限立即不残留;坑:①断言"admin 读敏感资源放行"与对象级校验冲突 —— 改为"显式授予 auditor 后才放行"(设计澄清)②`Decision` 默认 `rule` 是 `-` 而断言查 `deny by default`,需在 fallback 里显式给 `default-deny` ③Go 侧 `splitN` 手写实现改用 `strings.SplitN`/`HasSuffix` | Python / Go(41 断言) | 2026-09-17 |
| 306 | `08-安全/01-Web安全/点击劫持与框架嵌入/` | 点击劫持防护(OWASP Clickjacking Defense Cheat Sheet + WHATWG HTML Standard §7.7 + MDN 实读):XFO 处理模型逐步复刻并**逐行断言官方结果表** —— `SAMEORIGIN,SAMEORIGIN`(去重后单值)→ 允许、`SAMEORIGIN,DENY`/`SAMEORIGIN,`(空串也是值)/`SAMEORIGIN,ALLOWALL`/`SAMEORIGIN,INVALID` → 拒绝、`ALLOWALL,INVALID` → 拒绝(`allowall` 与 deny/sameorigin 同属"困惑"集合)、`INVALID,INVALID` → **允许**(等同头被省略),即官方注释 "block any attempts at applying X-Frame-Options which were trying to do something valid, but appear confused";孤立的非法值/`ALLOW-FROM`/单值 `ALLOWALL` 一律放行(fail-open);顶层文档不受约束(step 1);`SAMEORIGIN` 要求**整条祖先链**全部同源(父同源但祖父跨源 → 拒绝,与只看顶层的 `ALLOW-FROM` 形成对照);**CSP `frame-ancestors` 一旦 enforce 存在则无条件忽略 XFO**(CSP 规范原文 "the X-Frame-Options header MUST be ignored"),不论更严更宽 —— "两个都写取交集"是错的,Report-Only 不算 enforce 会回落 XFO;`frame-ancestors` 源列表支持 `'none'`/`'self'`/`*`/host-source/`*.` 通配子域与 scheme 比较;**`<meta http-equiv>` 里的 XFO 与 CSP 一律无效**;frame-buster 绕过场景表(双重 frame 触发跨源导航限制、`<iframe sandbox>`/JS 被禁用则不执行、代理剥头)+ OWASP best-for-now **fail-closed** 写法(`body{display:none}` + `self===top` 才撤销);坑:①断言表最初把 `ALLOWALL,INVALID` 写成"允许"(规范算法把 allowall 列入困惑集合,断言错)②`meta_delivered()` 起初只丢 XFO 没丢 CSP 头 ③断言 label 里用了 ASCII 双引号导致 SyntaxError | Python / JavaScript(46 + 21 断言) | 2026-09-17 |
| 307 | `09-语言学习/Golang/内存分配器/` | Go 内存分配器(runtime `sizeclasses.go` / `msize.go` / `malloc.go` / `mcache.go` / `mcentral.go` 五份 go1.24.0 源码逐段实读):**68 级 size class 全表复核** —— 从官方源码用脚本注入/复核三张表(`class_to_size`×68、`class_to_allocnpages`×68、`size_to_class8`/`size_to_class128`),`roundupsize()` 把小请求折到 class、大请求(>32768-8)按 8 KB 页上取整;**span 与浪费**按官方表逐项验算 —— 每个 span 的 `objects` / tail waste(`npages*8192 % size`)/ max waste(官方公式 `size*npages - pagesize*(npages-1)` 的 bp 口径)全部对上;class 5(48 B)的 span 布局 ASCII 图(class 0 索引偏移 + 每 span 170 个对象 + 尾浪费 9 字节);**tiny allocator** —— `<16 B` 且不含指针的对象共用一个 16 B block,只有"无指针子对象 + 剩余空间 ≥ 新对象尺寸"才复用,否则新开 block;**mcache/mcentral/mheap 三级缓存**的补货路径与计数(本地 3 个对象不满 25% 才补、每次取 `spanClass.npages` 页);`list` 为空时从 `mheap` 切新 span 并按对象数切分,`setSpans`/`map` 的 `spanSet` 位图用于快速定位 | Python / Go(61 断言) | 2026-09-18 |
| 308 | `09-语言学习/Golang/逃逸分析与内联/` | 逃逸分析与内联预算(`cmd/compile/internal/escape/escape.go` + `inline/inl.go` go1.24.0 源码实读):**位置图 + derefs 权重** —— 每个位置带 `derefs` 计数,赋值 `p = &q` 是 −1、`p = q` 是 0、`p = *q` 是 +1、`p = **q` 是 +2(即 `*p` 与 `p[0]` 等价、`**p` 与 `p[0][0]` 等价),两条不变量"图内所有位置 derefs 非负"与"边权由 derefs 差决定"逐条断言;`parameter tag`(`~r0`/`~p`)把形参与返回值的泄漏目标标成 leak 到 caller;`leaks()` 判定与 `-m` 输出的 `moved to heap` 一致;**内联预算表** —— 普通调用/闭包调用 80、`inlCallee` 后的 57、`inlineExtraCallCost` 17、`inlineExtraFormal` 1、大函数 `inlineBigFunctionNodes/MaxCost` 5000/80、`maxImplicitNodes` 800、`maxBodySize` 2000,超预算即拒绝(且"代价恰好等于预算"按 ≤ 处理);`-l` 四档(禁止内联/禁止中层/禁止全部/常规)、`-m -m` 打印内联决策链;结构化闸门(不可内联的形式:含 `recover`、含闭包捕获、函数体过大、含非内联调用) | Python / Go(55 断言) | 2026-09-18 |
| 309 | `09-语言学习/Golang/内存模型与sync原语/` | Go 内存模型与 `sync.Mutex` 双模式(`go.dev/ref/mem` 2022-06-06 版全文 + `internal/sync/mutex.go` go1.24.0 实读):**happens-before = sequenced before ∪ synchronized before 的传递闭包**(Floyd–Warshall 实现,`visible_write` 唯一性即 Requirement 3);三个 Requirement 的形式化定义;规范里 12 条原语保证逐条建模 —— 尤其"goroutine 退出**不保证**同步于任何事件"、"`TryLock` 失败**完全无**同步效果"、"所有 atomic 共享一个 SC 全序"、"无缓冲 channel 的 receive synchronized before 对应 send 完成(缓冲 1 就不保证)";数据竞争 = 同位置 + 至少一个非同步 + HB 不可比,DRF-SC 断言无竞争程序等价于某顺序交错;**Mutex 状态位**(`mutexLocked=1`/`mutexWoken=2`/`mutexStarving=4`/`waiterShift=3`)与**正常模式 vs 饥饿模式**:正常模式新到者可插队(barge)且"失败的等待者回到**队首**"(`queueLifo`)避免无限饥饿,`mutexWoken` 只在已有等待者时被认领;**饥饿模式** 1ms(`starvationThresholdNs = 1e6`)后移交所有权 + 让出时间片,退出条件是"自己是最后一个等待者"或"等待 < 1ms",断言尾延迟对照 | Python / Go(54 断言) | 2026-09-18 |
| 310 | `09-语言学习/Golang/反射三定律/` | Go 反射三定律(Rob Pike 2011 原文 + `reflect/value.go`/`type.go` go1.24.0 实读):**第一定律** interface → 反射对象 —— `TypeOf(i any)`/`ValueOf(i any)` 的入参就是空接口,`ValueOf` 是**复制**故 `SetXxx` 改不到原变量,`ValueOf(nil)` 给 zero Value;接口对永远是 `(值, 具体类型)`,**不能**是 `(值, 接口类型)`;`Kind` 描述底层类型(`MyInt` → `Int`)而 `Type` 给静态类型,getter/setter 一律走**能装下该值的最大类型**(`Int()→int64`、`Uint()→uint64`),写入时按实际类型补码截断(`int8` 的 `SetInt(300) == 44`,且 Go 的 `%` 保留被除数符号故须按位掩码实现);**第二定律** 反射对象 → interface —— `Interface()` 是 `ValueOf` 的逆、且对未导出字段 panic;**第三定律** 可设置性 —— `CanSet()` 一行判定 `flag&(flagAddr|flagRO) == flagAddr`、flag 位表(Kind×5 / StickyRO / EmbedRO / Indir / Addr / Method)、`ValueOf(&x).Elem()` 才可设置、未导出字段**可取址但不可设置**(`CanSet ⇒ CanAddr` 成立、反之不成立)、未导出字段只挡 `Interface`/`Set` 不挡 `Int()`;`Field()` 的掩码**清掉 flagEmbedRO 与 flagMethod**,故未导出嵌入结构体的**导出成员仍可设置**、而该嵌入字段本身不可设置;`ro()` 折叠只在 `Elem()` 的 Interface 分支发生;全部 panic 消息逐条对齐(`on Int Value`/`on zero Value`/`Field index out of range`/`using unaddressable value`),且判定**顺序固定**(先 RO → 再 Addr → 最后 Kind) | Python / Go(103 断言) | 2026-09-18 |
| 311 | `09-语言学习/Golang/unsafe与内存布局/` | unsafe 与内存布局(`unsafe` 包文档 + 规范 *Size and alignment guarantees* + `fieldalignment` 分析器 + `runtime/sizeclasses.go` 实读):**规范的硬尺寸表**(1/2/4/8/16)与**只是下界的对齐保证**(`Alignof ≥ 1`、结构体取字段最大值、数组取元素值)、空 struct/array 尺寸 0 且两个零尺寸变量可同址(故**尾部零尺寸字段占 1 字节**,`struct{int64; struct{}}` 是 **16** 不是 8);`Sizeof` 语义 —— 切片 24、字符串 16、接口 16、map/chan/func/指针各 8,**不含被引用的内存**;`Offsetof` 与累计偏移逐字段对齐;**pointer bytes**(GC 扫描前缀)—— 官方三例 `struct{uint32;string}=16` / `struct{string;*uint32}=24` / `struct{string;uint32}=8` 全部复现,`struct{[100]byte;*int64}` 是 **112**、重排后降到 **8** 而尺寸不变(**两个独立诊断**:尺寸浪费 / pointer bytes);**字段排序五条优先级**(零尺寸最前 → 对齐降序 → 含指针在前 → 尾随非指针字节少者在前 → 尺寸降序);**size class 上取整**(`classSize(40)=48`、`classSize(56)=64`、`>32768` 走大对象)使 `struct{bool;int64;[32]byte;bool}` 的 56 字节被算作 64、诊断文本带 `(allocator size class N)` 后缀(**仅当该侧不是 size class 时**);**false sharing** 警告 —— 最紧凑顺序可能把两个各自被独立 goroutine 更新的字段塞进同一条 64 字节 cache line,填充 56 字节后偏移从 0 变 64;**unsafe.Pointer 的四种特殊操作 + 六种合法模式**做成合法性校验器,文档点名的 `// INVALID:` 反例逐条识别(uintptr 存变量后转回、`&s + Sizeof(s)` 越界一个字节、nil 指针做算术、Syscall 实参未就地转换、reflect header 值先存变量、直接声明 `reflect.StringHeader` 变量),`&^` 取整不豁免越界检查 | Python / Go(99 断言) | 2026-09-18 |
| 312 | `10-逆向工程/02-移动端逆向/Frida-Java层hook/` | Frida Java 层 hook 分发语义(frida.re 官方文档):`Java.perform` 在 App classloader 未就绪时**推迟执行**;`Java.use` 拿类包装、`implementation` 替换后所有 ART 分发先进 hook;替换函数内 `this.onResume()` 走 Frida 保存的**原始 ArtMethod 入口**(不递归);同名方法必须 `.overload(...)` 显式选重载、未选重载不受影响;`$new` 构造/`Java.retain` 跨帧留存/`Java.cast` 裸指针转包装/替换函数抛异常向外传播;Frida 17 起 Java 桥拆成独立包 frida-java-bridge(REPL/frida-trace 仍内置) | JavaScript(46 行 agent) + Python(221 行,12 断言) | 2026-09-18 |
| 313 | `10-逆向工程/02-移动端逆向/Frida-Interceptor-native-hook/` | Interceptor inline hook(官方 JS API 文档):attach 在函数入口 patch 跳转、原指令搬 trampoline;onEnter 的 args 可读写、onLeave 的 `retval.replace()` 改返回值;this 带 returnAddress/context(pc/sp/寄存器)/threadId/depth;`Interceptor.replace`+`NativeFunction` 链回原实现**绕过 hook 不递归**;patch 先入 pending,离开 JS 运行时或 `send()` 才 `flush()`;32 位 ARM Thumb 函数地址 LSB=1;iPhone 5S 官方基准 onEnter-only≈6µs、双回调≈11µs,CModule 快通道零参数编组 | JavaScript(48 行) + Python(212 行,10 断言) | 2026-09-18 |
| 314 | `10-逆向工程/02-移动端逆向/APK签名校验/` | v1/v2/v3 全量(AOSP 官方规格):v1 = MANIFEST.MF 逐条目摘要→.SF 整文件+逐节摘要→.RSA 签名三级保护链;v2 = 签名块(uint64 size×2+magic "APK Sig Block 42"+ID 0x7109871a)插中央目录前,分块摘要两级 Merkle(chunk=sha256(0xa5‖len‖data)、top=sha256(0x5a‖count‖拼接))可并行;**EOCD 的 CD 偏移字段按签名分块偏移取值**;防回滚=.SF 主节 X-Android-APK-Signed: 2;v3(ID 0xf05368c0)=minSDK/maxSDK 内外双写+proof-of-rotation 单链表(最旧证书为根、逐级背书、末级=签名者证书) | Python(231 行,15 断言) + Go(292 行,人工审查) | 2026-09-18 |
| 315 | `10-逆向工程/02-移动端逆向/SSLPinning绕过/` | 三层 pinning 与绕过(MASTG-TECH-0012+objection pinning.ts 392 行全文):动态五路 ①registerClass 空 TrustManager+hook SSLContext.init 换参链回原 init ②OkHttp CertificatePinner.check/check$okhttp 空实现不抛 ③conscrypt TrustManagerImpl.verifyChain 直接返回入参链 ④checkTrustedRecursive 返回空 ArrayList ⑤PhoneGap SSLCertificateChecker.execute 回调 CONNECTION_SECURE;每路包 ClassNotFoundException(类不存在→job.addImplementation 跳过);反检测改 classFactory.tempFileNaming.prefix('frida' 会出现在 /proc/maps);静态路径=smali 换哈希/BKS keytool 注入+重签;frida-multiple-unpinning 监视 SSLPeerUnverifiedException 实例化自动 patch 抛出点、覆盖更广 | Python(231 行,13 断言) + JavaScript(81 行 agent) | 2026-09-18 |
| 316 | `10-逆向工程/02-移动端逆向/Objection与Frida/` | 分层关系(objection 官方 README/Wiki+MASTG-TOOL-0029):frida-core(C+QuickJS 注入)→语言绑定→frida-tools→objection(agent=TypeScript 命令集,`pip3 install objection`);usb 模式(root+frida-server 直连)vs gadget 模式(非 root:patchapk 解码→注入 libfrida-gadget.so+loader→回编→**重签**,原 v1/v2 签名失效);REPL 命令→agent 命令消息→job 模型(addImplementation 只挂成功的 hook);Android 专属:watch class method/keystore 监视提密码/RootBeer 绕过/**deoptimize 强制解释执行提高 hook 可靠性** | Python(148 行,12 断言) | 2026-09-18 |
| 317 | `11-性能分析/02-应用级剖析/pprof在线采集/` | net/http/pprof 在线采集(pkg.go.dev + golang/go pprof.go 源码实读)：init 在 DefaultServeMux 注册 5 条路径(cmdline/profile/symbol/trace + "/debug/pprof/"→Index)，Go 1.22 起 pattern 带 "GET " 前缀(非 GET 由 mux 判 405)；Index 用 strings.CutPrefix 剥前缀，name 非空委托 handler(name)、为空出 HTML 索引页(runtime profiles + 4 特殊端点按名排序附 count 与描述)；handler：Lookup 为 nil → 404 "Unknown profile"；有 seconds → delta 路径(非正整数 400 / 不在 profileSupportsDelta{allocs,block,goroutine,goroutineleak,goroutine,heap,mutex,threadcreate} 400 / 与 debug 并用 400；实现 = collect p0 → sleep → collect p1 → p0.Scale(-1) → profile.Merge，文件名 `<name>-delta`)；gc 参数**仅 heap 生效**(源码 `if name == "heap" && gc > 0`，对其他 profile 静默忽略)；debug≠0 → text/plain 否则 octet-stream+attachment；Profile 端点 seconds ParseInt 基 10、≤0 或解析错默认 30s、StartCPUProfile 已占用 → 500(边采边流式，CPU 非 Profile 对象的根因)；Trace 用 ParseFloat 可带小数默认 1s；Cmdline = os.Args 以 NUL 连接；Symbol 恒先 num_symbols: 1、PC 用 '+' 分隔、ParseUint base-0 识别 0x；**mux 精确匹配语义：四条特殊路径直达各自 handler，若经 Index 委托 handler("cmdline") 会 Lookup 失败 404——这正是四条独立注册存在的意义**；坑：Python 复刻 Symbol 时 int(s,10) 解析不了 "0x401000"，源码是 ParseUint(word, 0, 64) 必须 base-0 | Python(252 行 15 断言) + Go(283 行人工审查+配平) | 2026-09-18 |
| 318 | `11-性能分析/02-应用级剖析/pprof-top解读/` | top/top -cum 解读(Go Blog Profiling Go Programs 实读)：CPU 剖析每秒约 100 次记 goroutine 栈 PC，样本数÷100 ≈ 采集时长(2525 样本≈25s)；五列 = flat 计数、flat%、running%(列表内累计)、cum 计数、cum%；**flat = 该函数为叶子帧(正在运行而非等待被调函数返回)，cum = 出现在栈上任一位置**；经典实例 FindLoops flat 10.6% vs cum 84.1%；-cum 按累计列排序，gosched0/main.main 这类 flat=0 高 cum 的主干入口只有 -cum 能置顶；**栈深截断：每样本只含靠执行侧 100 帧、深递归截掉根侧**(博客 main.main cum 84.9%<100% 的唯一解释——若截叶子侧根帧永远在场)，flat 归因不受影响；nodefraction 过滤 cum/total < 阈值的节点(博客 --nodefraction=0.1 去噪 web 调用图)；--inuse_objects 报分配次数而非字节、内存采样 1-in-524288 是近似值；坑：默认 top 列表含 flat=0 行(如 main.main)，running% 断言别只数前三行 | Python(149 行 10 断言) + Go(241 行人工审查+配平) | 2026-09-18 |
| 319 | `11-性能分析/02-应用级剖析/py-spy采样剖析/` | py-spy 无侵入采样(benfred/py-spy README 实读)：Rust 独立进程经 process_vm_readv(Linux)/vm_read(macOS)/ReadProcessMemory(Windows) 直接读目标内存，不注入代码故"safe for production"；栈重建 = PyInterpreterState → 全部线程 → 沿 PyFrameObject back 链；ABI 逐版本变 → bindgen 生成各版结构；ASLR 对策：带符号 deref interp_head/_PyRuntime，剥离符号则扫 BSS 段找候选；三子命令 dump(单次各线程栈+--locals，定位挂死)/top(实时 %own 叶子/%total 在栈/%GIL)/record(折叠栈→火焰图)；--gil 只采持锁线程(**会漏掉释放 GIL 仍在干活的原生扩展**)；GIL 归属判定 ≤3.6 用 _PyThreadState_Current、≥3.7 从 _PyRuntime 推导；--nonblocking 不暂停目标但读非原子 → 偶发撕裂读(残缺栈叶子侧保留/采样错误)；权限模型：Linux attach 非子进程默认需 root(ptrace_scope)、自建进程不需要，macOS 恒需 root，Docker/K8s 需 SYS_PTRACE 能力位；--subprocesses 自动 attach 子进程(gunicorn 池) | Python(251 行 12 断言) + Go(284 行人工审查+配平) | 2026-09-18 |
| 320 | `11-性能分析/02-应用级剖析/cProfile-tottime-cumtime/` | cProfile 输出解读(docs.python.org/3/library/profile.html 实读)：确定性剖析监控**所有**调用/返回/异常事件并精确计时(解释器自带钩子故无需插桩)，统计性剖析随机采样指令指针；**tottime = 事件间隙记给当前栈顶(不含子函数)，cumtime = primitive 调用闭合时 t_return−t_enter(含子函数)**；恒等式 Σtottime == 总时长；ncalls 递归时印 total/primitive(primitive = 非递归引起的调用，文档示例 3/1)；两个 percall 分母不同：tottime/ncalls 与 cumtime/**primitive**；递归 cumtime 特殊处理"只按 primitive 记账"使递归与迭代实现统计可直接比较(2/1 递归用例 tottime==cumtime==3)；pstats：strip_dirs/sort_stats(TIME=tottime, CUMULATIVE, CALLS)/print_stats(int 行数、float 行数占比、str 正则)；指标用途三句话：计数→bug 与内联点、内部时间→热循环、累计时间→算法选型；C 级函数不产生事件故"显得格外快"；坑：断言第 N 次算错手账(异常用例 A 在 B 进入时被记 1 单位、float 0.5×3 行截断取 1 非四舍五入) | Python(193 行 12 断言) + Go(294 行人工审查+配平) | 2026-09-18 |
| 321 | `11-性能分析/02-应用级剖析/benchmark-ReportAllocs/` | Go benchmark 分配统计(pkg.go.dev/testing 实读)：b.N 机制原文"called multiple times with b.N adjusted until the benchmark function lasts long enough to be timed reliably"→ setup 会被执行多次须 ResetTimer(新代码推荐 b.Loop：只对循环体计时)；ReportAllocs "equivalent to -test.benchmem, but only affects the benchmark function that calls ReportAllocs"；**B/op = r.MemBytes/r.N、allocs/op = r.MemAllocs/r.N 均整数除法**(1000/3=333)；ResetTimer "zeroes elapsed time and memory allocation counters...does not affect whether the timer is running"(清账本不动开关)；StopTimer 期间的分配不入账；SetBytes 是"单次操作处理的字节数"→ 输出 MB/s 吞吐列(**与 B/op 的分配量完全两回事**)；RunParallel "reports ns/op as wall time for the benchmark as a whole, not the sum over each parallel goroutine"且 body 禁用三个计时器方法(全局效果)；输出行 `BenchmarkX-GOMAXPROCS  N  ns/op [+B/op +allocs/op] [+MB/s]`，格式规范 go.dev/design/14313-benchmark-format，A/B 比较用 benchstat；坑：浮点 10×0.1 累加=0.999…9 达标判定须容差；方法名与属性同名(report_allocs)在 Python 里属性覆盖方法 | Python(223 行 10 断言) + Go(277 行人工审查+配平) | 2026-09-18 |
| 322 | `01-游戏开发/02-渲染/图形管线/延迟渲染GBuffer/` | 延迟着色两 pass:geometry pass 以深度测试只留最上层片元,把位置(RGBA16F)/法线(RGBA16F)/反照率+镜面(RGBA8)写进 G-buffer(MRT 单 pass);lighting pass 用屏幕四边形逐像素从 G-buffer 采样重建片元,光照数学与前向完全相同(断言两管线输出逐像素一致)。Light Volume:衰减方程 I/(Kc+Kl·d+Kq·d²) 取 5/256 阈值(8 位 framebuffer 的接近黑)反解二次方程得光源作用半径,渲染背面球体,把光照计算量从 O(pixels×lights) 降到 O(pixels+lights);fragment shader 里 if(dist<radius) 在 GPU 上分支两端都执行、完全无效,必须靠光栅化覆盖范围提前跳像素。G-buffer 带宽 184 bit/px(RGBA16F×2+RGBA8+D24)≈1080p 45.5MiB;单张 32bpp 纹理 1080p 就 8.29MB。实测交叉点:32 盏小体积灯 forward 8192 次光照 vs deferred 2163;单盏大体积亮灯 deferred 512 > forward 256(少光源时 G-buffer 写入是纯开销)。透明物体必须单独前向 pass(glBlitFramebuffer 复制深度)。11 项断言 | Python(249 行) + Go(228 行) | 2026-09-18 |
| 323 | `01-游戏开发/02-渲染/图形管线/ForwardPlus分块光源剔除/` | Forward+(tiled forward):屏幕 16px tile 网格,每 tile 由 4 个过视点的侧平面构成微型视锥 —— 视空间视点在原点 → 平面方程 d=0,法线 = 角射线叉积(TL×BL/BR×TR/TR×TL/BL×BR)再翻转指向 tile 中心射线。点光源按球体判定:任一平面 ρ=N·C<−r 剔除(保守:贴角不进也误保留,只损失性能);近/远平面由 tile 深度归约 [zMin,zMax] 替代(InterlockedMin/Max on asuint 位模式保序);opaque 列表用 [zMin,zMax]、transparent 列表近端放宽为 0(半透明可出现在相机与最浅不透明面之间)。Compute 组织:[numthreads(16,16,1)] 一组一 tile,跨步循环 i=GI;i+=256 分摊光源,先落 groupshared 局部列表再由线程 0 InterlockedAdd 预约全局区段。Clustered:指数深度切片 Z=near·(far/near)^(s/N) 解决 2D tile 深度不连续的过度指派(实测近墙像素 tiled 4 盏 vs clustered 0);分块维度与 deferred/forward 正交(BF3 tiled deferred、DOOM 2016 clustered forward)。规模口径:forward~100 盏/deferred~2500/Forward+ 5000-6000 @1080p。14 项断言 | Python(248 行) + Go(208 行) | 2026-09-18 |
| 324 | `01-游戏开发/02-渲染/图形管线/GPU实例化Instancing/` | 实例化渲染:渲染顶点快、向 GPU 下达指令不快(每次 draw call 前的 bind VAO/纹理/设置 uniform 走慢速 CPU→GPU 总线),万片草叶的瓶颈是数千次 draw call 而非光栅化。glDrawArraysInstanced 多一个 instance count;gl_InstanceID 从 0 起每实例递增(第 43 个实例 ID=42)。两条属性泳道:uniform 数组受 GL_MAX_VERTEX_UNIFORM_COMPONENTS(保证下限 1024)约束,每实例 vec2 → 仅 512 个;instanced array 走顶点属性通道 + glVertexAttribDivisor(attr,1)(divisor=0 每顶点前进/1 每实例前进),只受显存约束。mat4 实例变换占 4 个连续属性位置(每属性最大一个 vec4),第 3 列(location 6)携带平移。实测记账:1000 小行星+1 行星 naive 1001 次 draw call CPU 准备 25.0ms vs 实例化 2 次 0.050ms(500 倍),顶点着色量完全相同(576×1001);100000 实例×576 顶点 ≈57.6M 顶点/帧仍 2 次调用。17 项断言 | Python(190 行) + Go(157 行) | 2026-09-18 |
| 325 | `01-游戏开发/02-渲染/图形管线/TileBasedGPU架构/` | 移动 GPU 图块化渲染:共享内存带宽 4-8 GB/s × 150pJ/byte ≈ 0.6-1.2W(Arm 口径,带宽吃掉整个功耗预算)→ 把 framebuffer 切 16×16 tile,binning 先把三角形按覆盖写进各 tile 的 to-do list(bbox 排他边界 → tile 范围 [⌊x0/t⌉,⌈x1/t⌉),实测 UI 三角形恰好进 4×4 个 tile),逐 tile 在片上 tile memory 光栅化,resolve 后一次写回。外部带宽只剩 loadOp/load 与 storeOp/store 两个阀门:clear 在 tile memory 里免费、loadOp=LOAD 整帧回读使流量近乎翻倍(实测 load 字节=store 字节)、depth 用完即弃 storeOp=DONT_CARE 省一整张附件写回。MSAA 4x:IMR 外部流量严格 ×4,TBR 采样只在片上、resolve 后仍写每像素 1 样本(store 不变),代价转移为 tile memory ×4。Metal=Memoryless/Vulkan=LAZILY_ALLOCATED(允许溢出显存,Metal 不允许);Apple TBDR 可读 render target(programmable blending)做单 pass 延迟渲染,G-buffer 完全不落系统内存。实测 overdraw=8:IMR 8.45MB vs TBR 0.39MB(21.4 倍),且 TBR 对 overdraw 不敏感。14 项断言 | Python(177 行) + Go(167 行) | 2026-09-18 |
| 326 | `01-游戏开发/02-渲染/图形管线/计算着色器执行模型/` | Compute Shader 三层线程组织:Dispatch(gx,gy,gz) 启动组、[numthreads(x,y,z)] 声明每组线程数;四个系统值 SV_GroupID/SV_GroupThreadID/SV_DispatchThreadID(=GroupID×numthreads+GroupThreadID)/SV_GroupIndex(=z·X·Y+y·X+x) —— 文档标准例 Dispatch(5,3,2)+numthreads(10,8,3) 中 GroupThreadID(7,5,0)→DispatchThreadID(27,13,0)/GroupIndex 57,全量枚举 3600 线程验证 DispatchThreadID 无重复、GroupIndex 双射。硬件限制:cs_5_0 每组 ≤1024 线程且 Z≤64(总闸与各维独立:(33,33,1) 总数超/(64,1,65) Z 超)、Dispatch 每维 ≤65535、groupshared 32KB;cs_4_x 更紧(768/16KB/无原子/Z=1)。groupshared 树形归约:每线程写自己值 → 屏障 → 逐级折半(漏屏障读到旧值,屏障是"看到他人写入"的唯一保证);原子 InterlockedAdd vs 非原子丢更新(全线程读旧值 0 再各写各的,只剩最后写)。22 项断言 | Python(220 行) + Go(152 行) | 2026-09-18 |
| 327 | `02-Web开发/03-数据库/SQL与ORM/N+1与预取策略/` | N+1 与预取策略(SQLAlchemy 2.0 官方 relationships/loading 文档实读):N+1 的成因是 `relationship()` 默认 `lazy="select"` —— 访问每个父对象的集合属性时**各发一条 SELECT**,20 个父对象 = 1+20 条。官方四种策略的语义差别不在结果而在**语句数**:`selectin` 用一条 `IN (?,?..)` 一次性取回(官方默认批量 500,本 demo `SELECT_IN_BATCH = 500` 分批)、`subquery` 把父查询当子查询再 JOIN、`joined` 用 LEFT OUTER JOIN 一次取全 + `unique()` 去重、`raiseload` 反向把懒加载变成硬错误(用于"我在生产上绝不允许 N+1")。实测语句数 select=6 / selectin=2 / subquery=2 / joined=1,而**四者返回的行集合逐字节相同** —— 这正是"策略只改语句数不改结果"的可测形式。坑:`joined` + 集合关系不去重会抛 `InvalidRequestError`(官方明写),因为 JOIN 会为每条子行重复父对象;`selectin` 的批次大小不是"越大越好",超过参数上限会退化成多次查询 | Python(297 行 20 断言) + Go(194 行人工审查) | 2026-09-18 |
| 328 | `02-Web开发/03-数据库/SQL与ORM/EXPLAIN执行计划解读/` | EXPLAIN 执行计划解读(PostgreSQL 14.1 官方 using-explain 文档实读):PostgreSQL 的 cost 是**任意单位(arbitrary units)**,由 `postgresql.conf` 的 `seq_page_cost`(默认 1.0)与 `cpu_tuple_cost`(默认 0.01)标定 —— 所以 `seq_scan_cost(pages, rows) = pages*1.0 + rows*0.01`,345 页 10000 行顺序扫描 = 445,数字本身无物理量纲,只有**相对大小**有意义。节点树解析要点:顶层节点打印**没有 `->` 前缀**,子节点才有;每行 `label (cost=startup..total rows=N width=W)` 与 `(actual time=.. rows=N loops=L)` 是**两套口径**(前者估算、后者实测),`actual time` 必须**乘 loops** 才是该节点总耗时(官方原话 "Multiply by the loops value");`loops` 的会计口径是**每子行执行一次**,所以嵌套循环里内层节点的 loops 常等于外层 rows。`Rows Removed by Filter` 是"取回行数"与"最终保留行数"的差 —— 估算与实际在这列上的巨大差通常是统计信息过期而非计划选错。坑:`LIMIT` 会**截断 actual rows 显示**,看到 rows 小于预期不等于估算错误 | Python(232 行 18 断言) + Go(243 行人工审查) | 2026-09-18 |
| 329 | `02-Web开发/03-数据库/SQL与ORM/连接池调优/` | 数据库连接池大小(HikariCP 官方 wiki "About Pool Sizing" 实读):官方经验公式 `connections = core_count * 2 + effective_spindle_count` —— 4 核 + 1 块盘 = 9,注意**超线程不增加 core_count**(官方明写 "you would count 4 cores, not 8"),`effective_spindle_count` 在 SSD 上按官方口径已趋近于 1 甚至可忽略。反直觉推论:池**不是越大越好**,官方给的"小池 + 快查询"压倒性优于"大池 + 排队"的模型说明,过大的池只是把排队从连接池挪到了 CPU/磁盘。另一条硬下界是 **pool-locking**:若一个线程执行期间要同时持有 Tn 个连接(嵌套获取),则池至少得 `Tn * (Cm - 1) + 1`(Cm = 每线程最大并行获取数)—— 少于这个数会发生线程间互相等待的死锁,本 demo 以 `pool_locking_min(3, 4) = 10` 断言。demo 用简化队列模型(cpu=5 / io=20 / 上下文切换税 0.2)复现"吞吐峰值出现在远小于线程数的池容量"这一现象,峰值落在 P=8~12 | Python(191 行 22 断言) + Go(203 行人工审查) | 2026-09-18 |
| 330 | `02-Web开发/03-数据库/SQL与ORM/键集分页/` | 键集分页 vs OFFSET(SQL:2023 Part 2 §4.17.3 + PostgreSQL 14.1 queries-limit 实读):SQL 标准对 OFFSET 的定义是"先排序、再丢弃前 N 行",所以**代价与 N 成正比、与页大小无关** —— `offset_page()` 逐页统计 `rows_examined = page * size + size`,第 1000 页要扫过 9990 行才吐出 10 行,深分页比值 > 100。键集(keyset)分页把游标编码进 WHERE:`WHERE (score, id) < (?, ?) ORDER BY score DESC, id DESC LIMIT n`,行值比较(row value comparison)让数据库能直接**索引定位**到起点,代价 O(log N) 与页码无关。真实差异在**一致性**:翻页期间新插入的行会把 OFFSET 窗口整体后移,导致下一页**重复显示**已看过的行(本 demo 用确定性 (score, id) 构造复现),而 keyset 因为锚定的是"最后一行的值"不会漏也不会重。代价:keyset 无法跳任意页(没有"第 1000 页"按钮)、需要稳定全序(排序键必须唯一,否则要补 tiebreaker),且要求客户端保存游标 | Python(184 行 18 断言) + Go(257 行人工审查) | 2026-09-18 |
| 331 | `02-Web开发/03-数据库/NoSQL/Redis数据结构底层/` | Redis dict 渐进式 rehash 与 zskiplist(redis/redis 源码 `src/dict.c` / `src/t_zset.c` / `src/server.h` 实读):dict 是**双表结构** `ht[0]`/`ht[1]`,rehash 时不是一次搬完,而是每次增删查改搬一个非空桶,**查找要同时看两张表并跳过 `idx < rehashidx` 的旧桶**,新插入**一律进 ht[1]**(保证不出现"搬完又得搬"的回流)。四组触发条件各不相同:正常扩容 1:1(used >= size)、收缩 1:8、`dict_can_resize` 关闭时仍需 4× 强制扩容、以及**空桶数 >= size*10 时的上界保护**(防止稀疏大字典永不收缩)。`pauserehash` 用于安全迭代器:冻结搬运期间新增的键仍进 ht[1] 且可查。zskiplist 是 zset 的有序实现:`P = 0.25`、`MAXLEVEL = 32`,每个节点带 `span` 记账使 `ZRANK` 能 O(log N) 求排名(1-based),`level[0].backward` 提供逆序。紧凑编码阈值:`zset-max-ziplist-entries 128` / `-value 64`、`hash-max-ziplist-entries 512` / `-value 64` 超限即转普通编码。实测 rehash 日志 `[(0,4),(4,8),(8,16),(16,32),(32,64)]`,LCG 层高分布 avg=1.3319、P(≥2)=0.2492、P(≥3)=0.0624 与 1/(1-p) 理论值吻合 | Python(281+195+259 行 47 断言) + Go(218+136+162 行人工审查) | 2026-09-18 |
