# 09 语言学习

> 与 01-08 按"领域/知识点"组织不同,本目录按**编程语言**组织。
> 目的:沉淀每种语言的**语言机制级**知识点(语法糖、运行时特性、标准库惯用法),与"领域原理"互补。

## 为什么单独建这个维度?

- **领域目录**关心"这件事怎么做" —— 比如 IO 多路复用、REST 设计、碰撞检测
- **语言目录**关心"这种语言本身的写法与坑" —— 比如装饰器、GIL、生成器、async/await、错误处理

两者经常交叉:同一知识点(IO 多路复用)在不同语言里有完全不同的写法。领域目录里的 demo 通常已经覆盖了主流语言,但**只触及该知识点的相关 API**;本目录会深入到"语言本身的特性",不受具体场景限制。

## 子目录(每种语言独立)

| 语言 | 状态 | 路径 | 主线(与其他语言的分工) | demo |
| --- | --- | --- | --- | --- |
| Python | ✅ 已开线 | [Python/](./Python/) | 动态类型 + 引用计数 GC + 语法糖(PEP) | 26 |
| Golang | ✅ 已开线 | [Golang/](./Golang/) | GC + CSP 并发(GPM 调度、channel) | 20 |
| Rust | ✅ 已开线 | [Rust/](./Rust/) | **无 GC 的内存安全**:所有权 / 借用检查 / trait 静态分发 | 15 |
| TypeScript | ✅ 已开线 | [TypeScript/](./TypeScript/) | **纯编译期**:结构化类型系统 + 类型级编程(约束在运行时被擦除) | 10 |

> 四门语言的覆盖是有意的对角线:Python 靠**运行期契约**、Go 靠**运行时 GC 与调度器**、
> Rust 靠**编译期证明**、TypeScript 靠**编译期擦除** —— 同一知识点(如并发安全、类型约束)
> 在四种语言里"错在哪、谁来挡、代价是什么"完全不同。
> Rust 共 10 个 demo:首批(2026-09-15)`所有权与借用/`、`Copy与Drop/`、`trait与分发/`、
> `智能指针/`、`生命周期/` —— 从"报错码"到"零成本抽象"逐层讲透编译期证明的边界;
> 第二批(2026-09-19)`闭包与迭代器/`、`无畏并发/`、`async状态机/`、`错误处理惯用法/`、
> `unsafe边界/` —— 五个都是"编译器替你挡到哪一步"的边界题:Fn 三 trait 靠推导、
> 数据竞争靠 Send/Sync、跨 await 状态靠 Pin、错误靠 From 升层,而**死锁与 UB 编译器一律不管**。
> 第三批(2026-09-22 18:00 槽)`方法解析与自动解引用/`、`型变与PhantomData/`、`trait高级形态/`、
> `声明宏与卫生性/`、`过程宏与TokenStream/` —— 主题是「**编译器在你看不见的地方做的选择**」:
> 方法调用的候选接收者顺序(谁先被问到比谁更合适更能决定结果)、型变如何从字段推导
> (struct 里冲突即不变、裸位置却逐位置各算)、dyn 兼容性的六条逐条否决、`macro_rules!`
> 那台「不向前看」的解析器与混合位点卫生性、以及过程宏与声明宏**两套 token 定义**的跨界转换;
> 五份 Python 模型共 245 条断言全部实跑通过,Rust 侧为同语义复刻(无工具链,走静态审查)。
> TypeScript 于 2026-09-15 16:00 槽按类目拓展规则登记(状态 🗺 = 目录与路线图已建、demo 待做)。
> TypeScript 首批 5 个 demo(2026-09-19 22:00 槽):`结构类型/`、`条件类型与分发/`、`映射类型与修饰符/`、
> `类型收窄与守卫/`、`方差与可赋值性/` —— 共同点是把「类型系统的结论」变成**可执行的断言**:
> 每个 demo 都带 `types/*.ts` 编译期 fixture(用 `tsc --strict` 精确比对期望错误码集合)和
> 一个 node 可直跑的主 TS(Node 22 起内置类型擦除),后者专门演示「运行时的原生行为」与
> 「编译期的类型结论」之间的落差 —— 例如谓词撒谎能让编译通过却在运行时抛 `TypeError`,
> 双变让窄参数回调编译通过却在运行时读到 `undefined`。
> TypeScript 第二批 5 个 demo(2026-09-23 22:00 槽):`satisfies与类型注解/`、`模板字面量类型与类型级解析/`、
> `类型实例化深度与上限/`、`声明合并与模块增强/`、`装饰器与元数据反射/` —— 主题是
> 「**类型系统的工程边界**」:`satisfies` 用「只校验不改写」解决注解丢信息的老矛盾、
> 模板字面量类型把字符串变成可计算值(但要付出笛卡尔积的代价)、语言因此必须有护栏
> (实测非尾递归 48 字符放行 / 49 报警、尾递归卡在 `tailCount === 1e3`、联合交叉积卡在 `>= 1e5`)、
> 同名声明按 namespace/type/value 三格分账合并、以及装饰器的两套模型里只有遗留那套带元数据。
> 第二批把手段升级为**负控 + 边界扫描**:改掉条件后必须真的报错,否则原断言就是恒真的。
> Golang 第三批 5 个 demo(2026-09-18):`内存分配器/`、`逃逸分析与内联/`、`内存模型与sync原语/`、
> `反射三定律/`、`unsafe与内存布局/` —— 从"运行时怎么放内存"走到"编译器怎么决定放哪、以及
> 人可以绕过类型系统到什么程度"。
> Python 第四批 5 个 demo(2026-09-20 20:00 槽):`迭代器协议与itertools/`、`MRO与C3线性化/`、
> `参数绑定与调用约定/`、`运算符与特殊方法分派/`、`作用域闭包与单元变量/` —— 五个都是
> 「**解释器替你做的决定**」:迭代器与容器的分野、多继承里谁先被调用(按 CPython `pmerge` 复刻后
> 与 `__mro__` 对拍)、实参怎么填进形参(手写绑定算法与 `inspect` 对拍)、`NotImplemented` 触发的
> 回退链(含「设成 None 是阻断而非促成回退」这一反直觉点)、以及名字在编译期就被判定为局部
> 而引发的 `UnboundLocalError` 与类作用域对推导式不可见的静默陷阱。
> Python 第五批 5 个 demo(2026-09-25 02:00 槽):`pickle与对象序列化/`、`contextvars与异步上下文/`、
> `enum与dataclasses运行时/`、`属性查找全链路/`、`缓冲协议与memoryview/` —— 主题是
> 「**协议与管线的运行时语义**」:pickle 的 `__reduce__` 六元组与 memo 身份保持(实测
> `__getnewargs_ex__` 的 kwargs 在默认 `object.__new__` 下被整条静默丢弃)、contextvars 的
> 快照隔离与 asyncio 两个拷贝时机(Task 创建时/Handle 调度时,对照本机源码)、装饰器与元类的
> 改写时序差(`__init_subclass__` 里看不到 dataclass 字段)、四级属性优先级链的纯 Python 复刻
> 对拍、以及 PEP 3118 的 resize 闸门与只读视图哈希;五份共 103 条断言全部实跑通过
> (含多处 3.12/3.13+ 版本边界标注:DEFAULT_PROTOCOL=4、`1 in Enum` 按值包含、
> 可写 memoryview 哈希抛 ValueError 等)。
> Golang 第四批 5 个 demo(2026-09-21 18:00 槽):`定时器与四叉堆/`、`netpoll与网络轮询器/`、
> `栈增长与连续栈/`、`字符串与UTF8rune/`、`方法集与嵌入提升/` —— 两个「运行时如何调度/唤醒」
> (per-P 四叉定时器堆、`pollDesc` 的 rg/wg 信号量)、两个「内存与编码的基本盘」(连续栈的
> 增长/收缩公式、`first[256]` + `acceptRanges` 的 UTF-8 查表)、一个纯规范层面的
> 「方法集与选择器深度」;五份 Python 模型共 371 条断言全部实跑通过,Go 侧为同语义复刻。
> 后续继续补 Java / C++ / C# 等。

## 目录内约定

每种语言的子目录内部:

```
Python/
├── README.md            # 语言特性总览 + 学习路径 + 与其他语言的关键差异
├── 装饰器/              # 一个语言特性 = 一个 demo
│   ├── README.md
│   ├── basic.py
│   └── with_args.py
├── 生成器/
└── ...
```

单 demo 文件不超过 300 行,优先用标准库。

## 与已有 demo 的边界

- **领域 demo 里同语言的实现**:聚焦在该知识点,**不展开**语言其他特性
- **本目录的 demo**:可以引用领域 demo,但核心目的是讲透**语言本身**

例如:
- `01-游戏开发/01-服务端/网络编程/IO多路复用/select/` 里的 `python/select_echo.py` 只讲怎么用 `selectors` 监听 fd
- `09-语言学习/Python/装饰器/` 讲的是 Python 函数是一等对象 + 闭包 + 语法糖,跟 IO 完全无关

## 进度

由 [`_docs/STATE.md`](../_docs/STATE.md)(主,≤ 5 KB)+ [`archive/`](../_docs/archive/)(历史回溯)统一追踪。