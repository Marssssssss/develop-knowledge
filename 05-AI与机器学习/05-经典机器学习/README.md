# 经典机器学习

> 2026-09-12 类目自动拓展新增（深度学习之前的统计学习基础，`05-AI与机器学习` 下与 01-深度学习互补的维度）。

## 简介

经典（传统）机器学习指深度学习流行之前、以统计学习理论为基础的算法族：决策树、支持向量机、K 近邻、朴素贝叶斯、线性模型、聚类与集成学习。它们在小样本、表格数据、可解释性要求高的场景仍是首选（如风控、医疗、工业），也是理解深度学习（如梯度提升 → 残差思想）的基础。

## 核心知识点（待研究清单）

依据 scikit-learn 官方用户指南归纳：

- [ ] **决策树（CART / ID3 / C4.5）**：递归划分特征空间；分裂准则（基尼不纯度 `H=Σ p(1-p)`、信息熵）；贪心穷举搜索最优 (特征, 阈值)；过拟合与剪枝（`max_depth` / `min_samples_leaf` / 最小代价复杂度剪枝）。sklearn 实现的是优化版 CART（仅二叉树，不支持类别变量）。
- [ ] **K 近邻（KNN）**：无参数惰性学习；距离度量与 k 的偏差-方差权衡；KD 树 / Ball 树加速。
- [ ] **朴素贝叶斯**：条件独立性假设；文本分类基线；拉普拉斯平滑。
- [ ] **支持向量机（SVM）**：最大间隔超平面；核技巧（隐式高维映射）；软间隔与 C 参数；对偶问题与 SMO。
- [ ] **逻辑回归 / 线性模型**：sigmoid + 交叉熵；正则化（L1 稀疏 / L2 岭）；为什么是分类算法却叫"回归"。
- [ ] **K-Means 聚类**：Lloyd 迭代；k 的选择（肘部法 / 轮廓系数）；对初始化敏感（k-means++）。
- [ ] **集成学习**：Bagging（随机森林 = 决策树 + 自助采样 + 随机特征子集）vs Boosting（AdaBoost / GBDT / XGBoost：序列拟合残差）；偏差-方差分解视角。
- [ ] **模型评估**：交叉验证（K 折 / 分层）；混淆矩阵、精确率/召回率/F1、ROC-AUC；过拟合诊断（学习曲线）。

## 已完成 demo

（暂无，按巡检轮次逐步补齐）

## 参考资料

- [scikit-learn User Guide — 1.10 Decision Trees](https://scikit-learn.org/stable/modules/tree.html) — CART/ID3/C4.5/C5.0 算法对比、基尼/熵公式、优缺点与剪枝参数（实际阅读）
