| 297 | `07-数据存储/02-NoSQL/MongoDB聚合与查询优化/` | MongoDB 聚合管道优化器与 ESR 索引(MongoDB 官方 aggregation-pipeline / aggregation-pipeline-optimization / aggregation-pipeline-limits / ESR guideline 四页实读):**先重排、后合并**是官方强制次序 —— "coalescence occurs *after* any sequence reordering optimization",先把 `$match` 挪到 `$sort` 前才可能出现新的相邻对;R1 投影下推靠**依赖分析**:官方示例的 `$match` 被拆成 4 个单键过滤器各自向左穿过"不影响它"的连续投影阶段(`name` 穿到最前、`maxTime`/`minTime` 被 `$addFields` 计算故只能停在它之后、`avgTime` 依赖 `$project` 计算结果原地不动),前移后 `{name}` 落到首阶段才可能命中索引;coalescence 五条:R5 `$sort`+`$limit`(夹 `$skip` 时 **`limit += skip`**,且中间出现 `$unwind`/`$group` 这类**改变文档数**的阶段就不许合并)、R6 双 `$limit` 取**较小值**、R7 双 `$skip` 取**之和**、R8 `$match`+`$match` 合成 `$and`、R9 `$lookup`+作用于其 `as` 字段的 `$unwind` 折进 `$lookup` 内部;ESR 的机制解释是"**索引键序决定能用的界**"——等值前缀之后的第一个字段才可能承担排序,一旦遇到范围字段其后的键再也不能提供界,故 ERS 只在范围谓词**高选择度**时更优(官方原话 "If your range predicate ... is very selective, then put it before sort fields",不是对错而是取舍);`$in` 的 **201 元素**分界(201 起退化为范围谓词、其后索引字段不再提供排序,官方明确该行为不保证跨版本稳定);1000 阶段上限 / **100 MB 阻塞阶段**(6.0 起 `allowDiskUseByDefault` 决定落盘还是直接报错)/ 16 MiB 单文档;阻塞型阶段清单 `$group $sort $bucket $bucketAuto $setWindowFields $sortByCount`;`$out`/`$merge` 只能末位、`$geoNear` 只能首位且三者不可重复出现;坑:①**`$unset` 也是投影阶段但不能按 `$project` 处理** —— 把 `{x:5}` 前移到 `$unset:[x]` 之前语义会从"匹配不到任何文档"变成"匹配 x==5 的文档",本 demo 取保守口径并在 README 标注官方未给该情形判定细节;②R3(`$redact`+`$match`)官方只说 "sometimes"、无完整条件,本 demo 由官方示例的可观察行为反推(仅前移纯等值过滤器)并标注为自定口径;③R5 漏掉 `$skip` 会多返回文档;④优化规则随版本变化("Optimizations are subject to change between releases")且 `explain` 里的优化后管道**不可手动执行** | Python / Go(67 断言) | 2026-09-17 |
| 298 | `07-数据存储/02-NoSQL/DynamoDB单表设计与容量/` | DynamoDB 单表设计与容量规划(AWS 官方 bp-partition-key-design / GSI / LSI / bp-partition-key-sharding / bp-partition-key-uniform-load / CheatSheet / ServiceQuotas / provisioned pricing 八页实读):容量**完全可算**、没有"大概" —— 读按 **4 KB 台阶**、写按 **1 KB 台阶**,`ceil(bytes/unit) × 一致性倍数`(强一致 1 / 最终一致 0.5 / 事务 2),官方示例逐条复现(10 KB 最终一致读 1.5、强一致 3、事务 6;3 KB 写 3、事务 6;10 KB 事务写 20);**取整口径因 API 而异**是核心陷阱 —— `BatchGetItem` 对**每一条**各自向上取整再相加(官方示例 1.5 KB + 6.5 KB 算成 12 KB 而非 8 KB),`Query`/`Scan` 只对**返回集合总大小**取整一次(合计 40.8 KB → 44 KB),单页 1 MB 靠 `LastEvaluatedKey` 翻页;**单分区硬顶 3000 RCU / 1000 WCU 是物理上限**,官方推导即 20 KB 条目强一致读吃 5 个单位 ⇒ 3000/5 = **600 次/秒**,且按**分区键值**而非按表计,自适应容量只做热点隔离、不抬高它;写分片 `N = ceil(target_WCU/1000)`(官方示例日期键拼 1..200 随机后缀,200000 WCU 正好得 200),代价是**读扇出 N 倍**再合并;LSI/GSI 六维对照(分区键同 vs 可不同、排序键、创建时机 **LSI 必须建表时**且最多 5 个 vs GSI 默认 20 个可后加、吞吐**共用** vs **独立**、LSI 支持强一致读而 GSI **仅最终一致**、GSI **不能回表**、LSI 每分区键值 **10 GB** 项目集合上限);**GSI 写容量不足会反噬主表写**(官方原文要求表与所有 GSI 都有足够写容量,否则主表写被限流),且 GSI 缺排序键时不写索引条目;坑:①**官方文档在 1000 与 1024 之间不自洽**(ServiceQuotas 写 "DynamoDB denotes 1 KB = 1024 bytes" 而读写操作页示例按 1000 进制算 96 KB),本 demo 一律取 **1024** 并在代码注释标注,这个 2.4% 的差会随条目数放大;②**回表按整条条目计费**(官方 "This charge is for reading each entire item from the table, not just the requested attributes"),构造"20 KB 主表条目 + 缺失属性仅 100 B"场景费用是 5.5 而非 1.5;③**LSI 建表后无法补建**;④"项目集合 10 GB 上限"**只对含 LSI 的表成立**;⑤超限后**缩小集合的写仍被允许**否则陷入死锁;⑥10 GB 逻辑用小阈值跑(单条上限 400 KB 意味真要造 10 GB 需 2.6 万条),常量本身另有独立断言 | Python / Go(75 断言) | 2026-09-17 |
| 299 | `07-数据存储/02-NoSQL/Cassandra数据建模与墓碑/` | Cassandra 查询驱动建模与墓碑生命周期(Apache Cassandra 文档源文件实读:`doc/developing/data-modeling/intro.adoc` + `doc/managing/operating/compaction/{tombstones,overview,stcs,lcs,twcs}.adoc`;官网 `cassandra.apache.org` 本机不可达故走 `github.com/apache/cassandra` blob 页精读,README 已标注该口径):**"In Cassandra, data modeling is query-driven"** —— 先列访问模式再为每个模式建表,反范式是常态;主键解析按官方"**首分量哈希成分区键、其余分量是聚簇键**"拆分,解析器必须区分**顶层逗号与括号内逗号**(`(id1,id2),c1,c2` → 分区键 2 + 聚簇键 2,括号内空白要规范化),分区键未全部等值绑定的查询必须扫全集群或加 `ALLOW FILTERING`;分区规模两条尺子(值个数 **100,000** / 磁盘 **100 MB**,官方均写 "below" 故**恰好达线不算超**);**删除即插入** —— "Cassandra treats a deletion as an insertion, and inserts a time-stamped deletion marker called a tombstone",清除需**三条同时**满足:①过 `gc_grace_seconds`(默认 **864000 秒 = 十天**)②本轮压缩**覆盖了所有**含该分区更旧数据的 SSTable ③若开 `only_purge_repaired_tombstones` 还须已跨副本修复 —— `purgeable()` 逐条返回失败原因,以区分"还没到时间"与"这次压缩不够"(两种情况的运维动作完全不同);**三节点僵尸复活最小复现**(一节点宕机 → 删除只写两节点 → 该节点在宽限期内被当修复补数据回集群);fully expired SSTable 与 `sstableexpiredblockers()` 的**阻塞**关系;三种压缩策略的三角权衡:STCS 攒 **4** 个大小相近者(分桶 `[avg×0.5, avg×1.5]`,写放大最低、空间放大最高)、LCS 每层 **10 倍**且 L0 超 **32** 个先走一次 STCS(`lcs_l0_failsafe()` 返回 `stcs_in_l0`/`lcs`)、TWCS 按时间窗口且**最怕乱序写入**(落进已关窗的桶会反复重写);坑:①`PRIMARY KEY (id)` 与 `((id))` 都是单分区键;②**TTL 过期 ≠ 墓碑**(过期单元格靠 `ts + ttl <= now` 判不可见但无墓碑,故 `ExpiredOnly` 需单独识别);③墓碑只遮挡**同分区同聚簇键**下更早的版本,留专门断言防"过度删除"错解;④`gc_grace` 边界是 `<=`(`now - tomb_ts == gc_grace` 仍算宽限期内);⑤LWT 官方要求 "should be kept to the minimum",`lwt_budget()` 每表计数并告警;⑥物化视图 4.0 仍是实验特性,`mv_risk()` 返回风险标记;⑦Go 侧断言函数用**变参** `detail ...string`,规避无工具链时固定实参个数漏网;⑧本 demo 开工前修复了上一轮拆分遗留的 Go 括号失衡(`cassandra_model.go` 的 `check`/`eqStrs` 函数体被切走一半) | Python / Go(68 断言) | 2026-09-17 |
| 300 | `07-数据存储/02-NoSQL/KV存储引擎LSM/` | LSM 存储引擎的 WAL/SSTable 格式与分层 compaction(LevelDB 官方 `doc/log_format.md` / `doc/table_format.md` / `doc/impl.md` + RocksDB wiki `Leveled-Compaction.md` / `RocksDB-Overview.md` / `Write-Stalls.md` 实读):WAL 按 **32 KB 块**切分,**7 字节头部** = CRC32C(4) + 长度(2) + 类型(1),跨块记录切 FULL / FIRST / MIDDLE / LAST 四型,块尾**剩余 ≤ 6 字节**必须补零(7 字节头放不下)——断言实际构造 1000 / 97270 / 8000 三尺寸得 FULL / FIRST+MIDDLE+LAST / FULL 且 `trailer_bytes == 6`,并专门验证"**恰好剩 7 字节**"时写出零长度 FIRST(`len==2, data_len==0, phys_len==7`);SSTable 三层结构(data block + index block + footer),footer **48 字节** = 两个 20 字节 BlockHandle + 8 字节 magic **`0xdb4775248b80fb57`**,filter block base 取 **2 KB**;分层两个口径 —— 静态 `10^L` MB(L1 文件 2 MB)与 **RocksDB 动态层级**(末层锚定在"实际大小"、向上逐层除以 10、低于 base/10 的层直接留空,实测 `{6:276GB, 5:27.6GB, 4:2.76GB, 3:0.276GB, 2:0, 1:0}`,官方称约 90% 数据落在末层),README 明确标注"官方示例 `num_levels=6` 标 L1..L6 而本 demo 把 `num_levels` 视为**含 L0**,故夹具取 7"的**口径差异**;**写停顿三类触发**(memtable 满 / L0 文件数超限 / 待压缩字节超限,顺序即优先级)与 `WriteStopped`;删除标记的可丢条件(更下层无更早版本 + 已过全部 snapshots);写放大 / 空间放大计算与 `level_summary`;坑:①纯函数里硬编码引擎实例导致不可测 → 抽出 `DynamicTargetsFrom(sizes, numLevels, base, multiplier)`;②Go 侧过滤已选表用指针 `!=` 会在重复元素时漏项 → 改 `drop map[*SSTable]bool`;③把假 `None` 条目塞进 `levels[0]` 做分数断言时崩在 `.size_bytes` → 引入 `FakeTable`;④拆分后尾部残留 `List[Entry]` 注解但 import 已删 → `NameError`;⑤`lsm_engine.py` 初版 417 行超 300 上限 → 经 `split_source.py` 多次拆分收成 `lsm_core.py` + `lsm_compact.py`(mixin)+ `lsm_engine.py` 160 行,Go 侧同法拆 6 文件 | Python / Go(64 断言) | 2026-09-17 |
| 301 | `07-数据存储/02-NoSQL/Protobuf编码/` | Protobuf 线格式逐字节实现(protobuf.dev 官方 `programming-guides/encoding/` + `programming-guides/proto3/` 实读):**varint 是 base-128** —— 每字节低 7 位是负载、最高位是续接位,负载按**小端**排(故 `150 → 0x96 0x01`),64 位值最多 **10 字节**;**wire type 六种**(0 VARINT / 1 I64 / 2 LEN / 3 SGROUP / 4 EGROUP / 5 I32),tag = `(field_number << 3) | wire_type`(`field 1` + VARINT → `0x08`);字段号 **1..536,870,911** 且 **19,000..19,999 由 protobuf 实现保留**(`validate_field_number`);官方最小示例 `message Test1 { int32 a = 1; }` 且 `a = 150` → `08 96 01` 逐字节断言,**嵌套消息** → `0a 03 08 96 01`;负数两条路:**intN 走补码恒占 10 字节**(`-2 → feffffffffffffffff01`),而 **sintN 走 ZigZag** `(n << 1) ^ (n >> (bits-1))`(`-500 → 999` 仅两字节),官方 ZigZag 对照表逐行断言;double/float 走 IEEE-754 定点位;LEN 前缀与 **packed 的双向兼容**(编码方用 packed,解码方必须同时接受 packed 与 unpacked,反之亦然);**未知字段按 wire type 跳过**(`skip_field` 含 group 配对);`SGROUP`/`EGROUP` 已废弃但解析器必须能配对跳过;**字段顺序不确定**(官方不得假设序列化输出稳定,同一逻辑消息可有多种字节表示);**Last One Wins** 与**嵌入消息合并**(同字段号重复出现时按 merge 语义递归合并而非覆盖),官方混合示例 `5:{1 2} 4:{"hello"} 5:{3}` 解析为 `nums=[1,2,3]`(packed 与非 packed 混用合并);坑:①Go 里 `uint64(-2)` **是编译错误**("constant -2 overflows uint64")→ 先 `neg := int64(-2)` 再 `uint64(neg)`;②`return DecodeMessage(...)` 多返回值类型不匹配(`map[string]interface{}` ≠ `interface{}`)→ 展开为两条语句并加注释,随后全仓扫描 `return Foo(` 形式确认无同类;③拆分后 doc 注释游离(`DecodeMessage` 的注释留在 `pb_merge.go` 尾、`EncodeMessage` 留在 `pb_group.go` 尾)→ 按精确串补回;④`pb_check.go` 残留未用 `strings` import;⑤`pb_codec.go` 349 行 / `pb_check.go` 331 行超限 → 拆出 `pb_merge.go` / `pb_group.go` / `pb_checkutil.go` | Python / Go(77 断言) | 2026-09-17 |
| 302 | `08-安全/01-Web安全/OAuth2-PKCE与授权码/` | OAuth 2.0 授权码流程 + PKCE(RFC 6749 §4.1 步骤 A–E + RFC 7636 §4.1/§4.2/§4.6 + RFC 9700 §2.1/§2.1.1/§4.2.4/§4.4.2.1 实读):授权请求只出现 `code_challenge = BASE64URL-ENCODE(SHA256(ASCII(code_verifier)))`,verifier(32 字节随机 → base64url → **43 字符**,字符集 `unreserved`、43..128)只在令牌端点出现 —— RFC 7636 **附录 B 向量**(`dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk` → `E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM`)在 Python 与 JS 两侧做**字符串级**断言;`redirect_uri` **精确字符串匹配**(RFC 9700 §2.1,唯一例外是原生 `localhost` 可变端口),大小写差异 / 多余后缀 / `http` scheme(非回环)一律拒绝,**校验失败时不重定向**(RFC 6749 §3.1.2.4 MUST NOT);授权码 **单次使用 + 10 分钟生命周期**(§4.1.2 "maximum 10 minutes is RECOMMENDED",本实现 600 秒闭区间),重放时 SHOULD **撤销**先前基于该码签发的全部令牌;PKCE **降级检测**:授权请求没带 challenge 时令牌端点收到 `code_verifier` 必须 `invalid_grant`(§4.8.2)、公共客户端无 challenge 则 `invalid_request`;`state` **首次使用后失效** + **RFC 9207 `iss`** 比对(不符即中止)→ 挡住 mix-up;`plain` 缺省但 §7.2 明确 SHOULD NOT;坑:①自检脚本里 `flow()` 已消费 state,"state 复用"断言不能再调一次 `callback`(实跑暴露)②`hmac.compare_digest` 防时序侧信道 ③错误响应也必须回填 `state` | Python / JavaScript(32 + 16 断言) | 2026-09-17 |
| 303 | `08-安全/01-Web安全/Cookie安全属性与SameSite/` | 浏览器侧 Cookie 存储模型 + SameSite + HSTS(RFC 6265 §5.1.3/§5.1.4/§5.3/§5.4 全文 + draft-ietf-httpbis-rfc6265bis-20 §4.1.2.7/§4.1.3/§5.4/§5.5/§5.6.7.2/§5.7 + RFC 6797 §6.1/§8 实读):`domain-match` **点边界**(`notexample.com` 不匹配 `example.com`,IP 不做后缀匹配)、`path-match` **不跨词边界**(`/docs` 匹配 `/docs/x` 不匹配 `/docsets`);`default-path` 取到**最右斜杠之前且不含该斜杠**(RFC 6265/6265bis 原文 `...up to, but not including, the right-most "/"`,故 `/docs/Web/HTTP/index.html` → `/docs/Web/HTTP`)—— **与 MDN 表述的 `/docs/Web/HTTP/` 存在口径差异**,README 已标注;`Max-Age` 优先于 `Expires`、**400 天硬截断**(§5.5)、单条 Set-Cookie **4096 字节**、属性值 1024 字节、每域 50 / 总量 3000 淘汰按 last-access;**UA 侧前缀必须大小写不敏感**(§5.4:否则 `__SeCuRe-SID=evil` 与服务端大小写不敏感解析叠加成可用攻击)、`__Host-` 需 Secure + host-only + **显式** `Path=/`;`SameSite=None` 必须配 Secure 否则整条丢弃;**非安全 cookie 不得覆盖同名 Secure cookie**(§5.7 step 21,**路径比较不对称** —— 攻击者仍可在更短路径种同名 cookie,两条同时进入 Cookie 头、路径长者在前,断言覆盖);SameSite 检索语义含 **Lax-allowing-unsafe 2 分钟例外**(§5.6.7.2,只对未显式写 SameSite 者生效);HSTS:`max-age` REQUIRED、指令重复则**整头作废**、未知指令忽略、**非安全传输上的 STS 头完全忽略**、同一响应多个头只取第一个、`max-age=0` 删除策略并连带清掉 includeSubDomains、父子域命中需 `includeSubDomains`、**IP 字面量不得标记**、`<meta http-equiv>` 不理会、§8.3 端口映射(80→443,其他端口保留,无端口不添加);**`preload` 不是 RFC 6797 的指令**(§12.3 讲的是预加载**设施**);坑:①省略 Domain 时未置 `host_only=True`(实跑断言抓出的真 bug)②"非安全覆盖"用例最初写成默认路径 `/` 而规范只拦 path-match 旧路径的情形(断言错)③"多个 STS 头只取第一个"要**同一响应**内传两个值,分两次 `note()` 是两轮响应 | Python / JavaScript(80 + 18 断言) | 2026-09-17 |
| 304 | `08-安全/01-Web安全/SSRF与URL解析歧义/` | SSRF 防护 + WHATWG URL 的 IPv4 主机解析(OWASP SSRF Prevention Cheat Sheet 全文 + WHATWG URL Standard §3.5/§4.4 实读,并用 node **原生 URL 实现**给复刻的解析器当标尺):`ipv4 number parser` 三进制 —— `0x`/`0X` → 16、前导 `0`(长度 ≥2)→ 8、否则 10,**非十进制写法要报 validation error 但不是失败**;`ipv4 parser` 按 `.` 拆段、尾点忽略、前 n-1 段 ≤255、**最后一段必须 < 256^(5-段数)**(1 段可吃满 32 位),故 `127.1` / `2130706433` / `0x7f000001` / `0x7f.1` / `0177.0.0.1` / `0x7f.0x0.0x0.0x1` 全是 `127.0.0.1`(11 条等价写法逐条断言),而 `256.1.1.1` / `1.2.3.4.5` / `1.2.3.65536` 是**非法 URL**;`ends in a number checker` 决定"算不算 IP 写法"(失败则整个 URL 非法,而非回落域名)→ 纯数字主机名 `http://12345/` 合法、`dead.beef` 仍是域名;**解析器分歧** `http://example.com\@evil.com`:WHATWG 把反斜杠当路径分隔符 → `example.com`,CPython `urlsplit` 按最后一个 `@` 切 userinfo → `evil.com`,**不一致即拒绝**(不调和);`is_public()` 先解包 **IPv4-mapped / NAT64 `64:ff9b::/96` / 6to4** 再判,否则 `http://[::ffff:127.0.0.1]/` 会漏(WHATWG 会把它序列化成 `[::ffff:7f00:1]`,字符串黑名单必然失效 —— 与 CPython `IPv6Address.__str__` 保留点分四段形成**口径差异**);Case 1 允许列表命中后**用允许列表条目自行重建 scheme/host/port/path**、Case 2 逐个校验全部 A/AAAA 记录(防 DNS pinning)、**禁用重定向跟随**、`file`/`gopher` 等 scheme 拒绝;坑:①断言把 IPv6 规范串写成 `::ffff:7f00:1`(CPython 实际输出 `::ffff:127.0.0.1`,断言错)②JS 侧"isPublic"最初只匹配 v4 正则导致 mapped 地址漏过(正是 demo 要讲的坑)③主机名按规范降小写后再比对,尾点 FQDN 不等价 | Python / JavaScript(74 + 29 断言) | 2026-09-17 |
| 305 | `08-安全/01-Web安全/访问控制与IDOR/` | 访问控制与对象级授权(OWASP Authorization Cheat Sheet 全文实读):**deny-overrides + 默认拒绝**(默认拒绝给的是显式决定 `default-deny` 而非"中立")、**每请求服务端校验**(客户端自称属性单独存 `untrusted` 仓、决策一律不看)、**对象级校验**是 IDOR 的全部内容("对某类资源有权限 ≠ 对该类每个对象有权限",CWE-639);三层模型叠加 —— RBAC 只认类型级动作(**断言演示它会把"读同事的发票"判为允许 = 横向越权**)、ABAC 管环境(在班/租户,同一角色不同环境结论不同)、ReBAC 管对象(owner / collaborator / **auditor 租户级**);**宽范围访问也必须显式授予** —— admin 角色本身不等于能碰任何对象,需 `relate(uid,"auditor","tenant:t1")` 且仍受租户边界约束;公开资源只读(public 不放行写);**会话级间接引用**(句柄 `h1` 只在签发会话内可解析,别人拿到解析不到,且句柄稳定可复用);**失败安全退出**(策略抛异常 → 拒绝,`guard()` 对外只给 `403`,rule/reason 留在 decision 与审计里,避免 CWE-209)、审计每条决策一行且顺序与调用一致;权限蔓延:角色重配后旧权限立即不残留;坑:①断言"admin 读敏感资源放行"与对象级校验冲突 —— 改为"显式授予 auditor 后才放行"(设计澄清)②`Decision` 默认 `rule` 是 `-` 而断言查 `deny by default`,需在 fallback 里显式给 `default-deny` ③Go 侧 `splitN` 手写实现改用 `strings.SplitN`/`HasSuffix` | Python / Go(41 断言) | 2026-09-17 |
| 306 | `08-安全/01-Web安全/点击劫持与框架嵌入/` | 点击劫持防护(OWASP Clickjacking Defense Cheat Sheet + WHATWG HTML Standard §7.7 + MDN 实读):XFO 处理模型逐步复刻并**逐行断言官方结果表** —— `SAMEORIGIN,SAMEORIGIN`(去重后单值)→ 允许、`SAMEORIGIN,DENY`/`SAMEORIGIN,`(空串也是值)/`SAMEORIGIN,ALLOWALL`/`SAMEORIGIN,INVALID` → 拒绝、`ALLOWALL,INVALID` → 拒绝(`allowall` 与 deny/sameorigin 同属"困惑"集合)、`INVALID,INVALID` → **允许**(等同头被省略),即官方注释 "block any attempts at applying X-Frame-Options which were trying to do something valid, but appear confused";孤立的非法值/`ALLOW-FROM`/单值 `ALLOWALL` 一律放行(fail-open);顶层文档不受约束(step 1);`SAMEORIGIN` 要求**整条祖先链**全部同源(父同源但祖父跨源 → 拒绝,与只看顶层的 `ALLOW-FROM` 形成对照);**CSP `frame-ancestors` 一旦 enforce 存在则无条件忽略 XFO**(CSP 规范原文 "the X-Frame-Options header MUST be ignored"),不论更严更宽 —— "两个都写取交集"是错的,Report-Only 不算 enforce 会回落 XFO;`frame-ancestors` 源列表支持 `'none'`/`'self'`/`*`/host-source/`*.` 通配子域与 scheme 比较;**`<meta http-equiv>` 里的 XFO 与 CSP 一律无效**;frame-buster 绕过场景表(双重 frame 触发跨源导航限制、`<iframe sandbox>`/JS 被禁用则不执行、代理剥头)+ OWASP best-for-now **fail-closed** 写法(`body{display:none}` + `self===top` 才撤销);坑:①断言表最初把 `ALLOWALL,INVALID` 写成"允许"(规范算法把 allowall 列入困惑集合,断言错)②`meta_delivered()` 起初只丢 XFO 没丢 CSP 头 ③断言 label 里用了 ASCII 双引号导致 SyntaxError | Python / JavaScript(46 + 21 断言) | 2026-09-17 |
| 307 | `09-语言学习/Golang/内存分配器/` | Go 内存分配器(runtime `sizeclasses.go` / `msize.go` / `malloc.go` / `mcache.go` / `mcentral.go` 五份 go1.24.0 源码逐段实读):**68 级 size class 全表复核** —— 从官方源码用脚本注入/复核三张表(`class_to_size`×68、`class_to_allocnpages`×68、`size_to_class8`/`size_to_class128`),`roundupsize()` 把小请求折到 class、大请求(>32768-8)按 8 KB 页上取整;**span 与浪费**按官方表逐项验算 —— 每个 span 的 `objects` / tail waste(`npages*8192 % size`)/ max waste(官方公式 `size*npages - pagesize*(npages-1)` 的 bp 口径)全部对上;class 5(48 B)的 span 布局 ASCII 图(class 0 索引偏移 + 每 span 170 个对象 + 尾浪费 9 字节);**tiny allocator** —— `<16 B` 且不含指针的对象共用一个 16 B block,只有"无指针子对象 + 剩余空间 ≥ 新对象尺寸"才复用,否则新开 block;**mcache/mcentral/mheap 三级缓存**的补货路径与计数(本地 3 个对象不满 25% 才补、每次取 `spanClass.npages` 页);`list` 为空时从 `mheap` 切新 span 并按对象数切分,`setSpans`/`map` 的 `spanSet` 位图用于快速定位 | Python / Go(61 断言) | 2026-09-18 |
| 308 | `09-语言学习/Golang/逃逸分析与内联/` | 逃逸分析与内联预算(`cmd/compile/internal/escape/escape.go` + `inline/inl.go` go1.24.0 源码实读):**位置图 + derefs 权重** —— 每个位置带 `derefs` 计数,赋值 `p = &q` 是 −1、`p = q` 是 0、`p = *q` 是 +1、`p = **q` 是 +2(即 `*p` 与 `p[0]` 等价、`**p` 与 `p[0][0]` 等价),两条不变量"图内所有位置 derefs 非负"与"边权由 derefs 差决定"逐条断言;`parameter tag`(`~r0`/`~p`)把形参与返回值的泄漏目标标成 leak 到 caller;`leaks()` 判定与 `-m` 输出的 `moved to heap` 一致;**内联预算表** —— 普通调用/闭包调用 80、`inlCallee` 后的 57、`inlineExtraCallCost` 17、`inlineExtraFormal` 1、大函数 `inlineBigFunctionNodes/MaxCost` 5000/80、`maxImplicitNodes` 800、`maxBodySize` 2000,超预算即拒绝(且"代价恰好等于预算"按 ≤ 处理);`-l` 四档(禁止内联/禁止中层/禁止全部/常规)、`-m -m` 打印内联决策链;结构化闸门(不可内联的形式:含 `recover`、含闭包捕获、函数体过大、含非内联调用) | Python / Go(55 断言) | 2026-09-18 |
| 309 | `09-语言学习/Golang/内存模型与sync原语/` | Go 内存模型与 `sync.Mutex` 双模式(`go.dev/ref/mem` 2022-06-06 版全文 + `internal/sync/mutex.go` go1.24.0 实读):**happens-before = sequenced before ∪ synchronized before 的传递闭包**(Floyd–Warshall 实现,`visible_write` 唯一性即 Requirement 3);三个 Requirement 的形式化定义;规范里 12 条原语保证逐条建模 —— 尤其"goroutine 退出**不保证**同步于任何事件"、"`TryLock` 失败**完全无**同步效果"、"所有 atomic 共享一个 SC 全序"、"无缓冲 channel 的 receive synchronized before 对应 send 完成(缓冲 1 就不保证)";数据竞争 = 同位置 + 至少一个非同步 + HB 不可比,DRF-SC 断言无竞争程序等价于某顺序交错;**Mutex 状态位**(`mutexLocked=1`/`mutexWoken=2`/`mutexStarving=4`/`waiterShift=3`)与**正常模式 vs 饥饿模式**:正常模式新到者可插队(barge)且"失败的等待者回到**队首**"(`queueLifo`)避免无限饥饿,`mutexWoken` 只在已有等待者时被认领;**饥饿模式** 1ms(`starvationThresholdNs = 1e6`)后移交所有权 + 让出时间片,退出条件是"自己是最后一个等待者"或"等待 < 1ms",断言尾延迟对照 | Python / Go(54 断言) | 2026-09-18 |
| 310 | `09-语言学习/Golang/反射三定律/` | Go 反射三定律(Rob Pike 2011 原文 + `reflect/value.go`/`type.go` go1.24.0 实读):**第一定律** interface → 反射对象 —— `TypeOf(i any)`/`ValueOf(i any)` 的入参就是空接口,`ValueOf` 是**复制**故 `SetXxx` 改不到原变量,`ValueOf(nil)` 给 zero Value;接口对永远是 `(值, 具体类型)`,**不能**是 `(值, 接口类型)`;`Kind` 描述底层类型(`MyInt` → `Int`)而 `Type` 给静态类型,getter/setter 一律走**能装下该值的最大类型**(`Int()→int64`、`Uint()→uint64`),写入时按实际类型补码截断(`int8` 的 `SetInt(300) == 44`,且 Go 的 `%` 保留被除数符号故须按位掩码实现);**第二定律** 反射对象 → interface —— `Interface()` 是 `ValueOf` 的逆、且对未导出字段 panic;**第三定律** 可设置性 —— `CanSet()` 一行判定 `flag&(flagAddr|flagRO) == flagAddr`、flag 位表(Kind×5 / StickyRO / EmbedRO / Indir / Addr / Method)、`ValueOf(&x).Elem()` 才可设置、未导出字段**可取址但不可设置**(`CanSet ⇒ CanAddr` 成立、反之不成立)、未导出字段只挡 `Interface`/`Set` 不挡 `Int()`;`Field()` 的掩码**清掉 flagEmbedRO 与 flagMethod**,故未导出嵌入结构体的**导出成员仍可设置**、而该嵌入字段本身不可设置;`ro()` 折叠只在 `Elem()` 的 Interface 分支发生;全部 panic 消息逐条对齐(`on Int Value`/`on zero Value`/`Field index out of range`/`using unaddressable value`),且判定**顺序固定**(先 RO → 再 Addr → 最后 Kind) | Python / Go(103 断言) | 2026-09-18 |
| 311 | `09-语言学习/Golang/unsafe与内存布局/` | unsafe 与内存布局(`unsafe` 包文档 + 规范 *Size and alignment guarantees* + `fieldalignment` 分析器 + `runtime/sizeclasses.go` 实读):**规范的硬尺寸表**(1/2/4/8/16)与**只是下界的对齐保证**(`Alignof ≥ 1`、结构体取字段最大值、数组取元素值)、空 struct/array 尺寸 0 且两个零尺寸变量可同址(故**尾部零尺寸字段占 1 字节**,`struct{int64; struct{}}` 是 **16** 不是 8);`Sizeof` 语义 —— 切片 24、字符串 16、接口 16、map/chan/func/指针各 8,**不含被引用的内存**;`Offsetof` 与累计偏移逐字段对齐;**pointer bytes**(GC 扫描前缀)—— 官方三例 `struct{uint32;string}=16` / `struct{string;*uint32}=24` / `struct{string;uint32}=8` 全部复现,`struct{[100]byte;*int64}` 是 **112**、重排后降到 **8** 而尺寸不变(**两个独立诊断**:尺寸浪费 / pointer bytes);**字段排序五条优先级**(零尺寸最前 → 对齐降序 → 含指针在前 → 尾随非指针字节少者在前 → 尺寸降序);**size class 上取整**(`classSize(40)=48`、`classSize(56)=64`、`>32768` 走大对象)使 `struct{bool;int64;[32]byte;bool}` 的 56 字节被算作 64、诊断文本带 `(allocator size class N)` 后缀(**仅当该侧不是 size class 时**);**false sharing** 警告 —— 最紧凑顺序可能把两个各自被独立 goroutine 更新的字段塞进同一条 64 字节 cache line,填充 56 字节后偏移从 0 变 64;**unsafe.Pointer 的四种特殊操作 + 六种合法模式**做成合法性校验器,文档点名的 `// INVALID:` 反例逐条识别(uintptr 存变量后转回、`&s + Sizeof(s)` 越界一个字节、nil 指针做算术、Syscall 实参未就地转换、reflect header 值先存变量、直接声明 `reflect.StringHeader` 变量),`&^` 取整不豁免越界检查 | Python / Go(99 断言) | 2026-09-18 |
| 312 | `10-逆向工程/02-移动端逆向/Frida-Java层hook/` | Frida Java 层 hook 分发语义(frida.re 官方文档):`Java.perform` 在 App classloader 未就绪时**推迟执行**;`Java.use` 拿类包装、`implementation` 替换后所有 ART 分发先进 hook;替换函数内 `this.onResume()` 走 Frida 保存的**原始 ArtMethod 入口**(不递归);同名方法必须 `.overload(...)` 显式选重载、未选重载不受影响;`$new` 构造/`Java.retain` 跨帧留存/`Java.cast` 裸指针转包装/替换函数抛异常向外传播;Frida 17 起 Java 桥拆成独立包 frida-java-bridge(REPL/frida-trace 仍内置) | JavaScript(46 行 agent) + Python(221 行,12 断言) | 2026-09-18 |
| 313 | `10-逆向工程/02-移动端逆向/Frida-Interceptor-native-hook/` | Interceptor inline hook(官方 JS API 文档):attach 在函数入口 patch 跳转、原指令搬 trampoline;onEnter 的 args 可读写、onLeave 的 `retval.replace()` 改返回值;this 带 returnAddress/context(pc/sp/寄存器)/threadId/depth;`Interceptor.replace`+`NativeFunction` 链回原实现**绕过 hook 不递归**;patch 先入 pending,离开 JS 运行时或 `send()` 才 `flush()`;32 位 ARM Thumb 函数地址 LSB=1;iPhone 5S 官方基准 onEnter-only≈6µs、双回调≈11µs,CModule 快通道零参数编组 | JavaScript(48 行) + Python(212 行,10 断言) | 2026-09-18 |
| 314 | `10-逆向工程/02-移动端逆向/APK签名校验/` | v1/v2/v3 全量(AOSP 官方规格):v1 = MANIFEST.MF 逐条目摘要→.SF 整文件+逐节摘要→.RSA 签名三级保护链;v2 = 签名块(uint64 size×2+magic "APK Sig Block 42"+ID 0x7109871a)插中央目录前,分块摘要两级 Merkle(chunk=sha256(0xa5‖len‖data)、top=sha256(0x5a‖count‖拼接))可并行;**EOCD 的 CD 偏移字段按签名分块偏移取值**;防回滚=.SF 主节 X-Android-APK-Signed: 2;v3(ID 0xf05368c0)=minSDK/maxSDK 内外双写+proof-of-rotation 单链表(最旧证书为根、逐级背书、末级=签名者证书) | Python(231 行,15 断言) + Go(292 行,人工审查) | 2026-09-18 |
| 315 | `10-逆向工程/02-移动端逆向/SSLPinning绕过/` | 三层 pinning 与绕过(MASTG-TECH-0012+objection pinning.ts 392 行全文):动态五路 ①registerClass 空 TrustManager+hook SSLContext.init 换参链回原 init ②OkHttp CertificatePinner.check/check$okhttp 空实现不抛 ③conscrypt TrustManagerImpl.verifyChain 直接返回入参链 ④checkTrustedRecursive 返回空 ArrayList ⑤PhoneGap SSLCertificateChecker.execute 回调 CONNECTION_SECURE;每路包 ClassNotFoundException(类不存在→job.addImplementation 跳过);反检测改 classFactory.tempFileNaming.prefix('frida' 会出现在 /proc/maps);静态路径=smali 换哈希/BKS keytool 注入+重签;frida-multiple-unpinning 监视 SSLPeerUnverifiedException 实例化自动 patch 抛出点、覆盖更广 | Python(231 行,13 断言) + JavaScript(81 行 agent) | 2026-09-18 |
| 316 | `10-逆向工程/02-移动端逆向/Objection与Frida/` | 分层关系(objection 官方 README/Wiki+MASTG-TOOL-0029):frida-core(C+QuickJS 注入)→语言绑定→frida-tools→objection(agent=TypeScript 命令集,`pip3 install objection`);usb 模式(root+frida-server 直连)vs gadget 模式(非 root:patchapk 解码→注入 libfrida-gadget.so+loader→回编→**重签**,原 v1/v2 签名失效);REPL 命令→agent 命令消息→job 模型(addImplementation 只挂成功的 hook);Android 专属:watch class method/keystore 监视提密码/RootBeer 绕过/**deoptimize 强制解释执行提高 hook 可靠性** | Python(148 行,12 断言) | 2026-09-18 |
| 317 | `11-性能分析/02-应用级剖析/pprof在线采集/` | net/http/pprof 在线采集(pkg.go.dev + golang/go pprof.go 源码实读)：init 在 DefaultServeMux 注册 5 条路径(cmdline/profile/symbol/trace + "/debug/pprof/"→Index)，Go 1.22 起 pattern 带 "GET " 前缀(非 GET 由 mux 判 405)；Index 用 strings.CutPrefix 剥前缀，name 非空委托 handler(name)、为空出 HTML 索引页(runtime profiles + 4 特殊端点按名排序附 count 与描述)；handler：Lookup 为 nil → 404 "Unknown profile"；有 seconds → delta 路径(非正整数 400 / 不在 profileSupportsDelta{allocs,block,goroutine,goroutineleak,goroutine,heap,mutex,threadcreate} 400 / 与 debug 并用 400；实现 = collect p0 → sleep → collect p1 → p0.Scale(-1) → profile.Merge，文件名 `<name>-delta`)；gc 参数**仅 heap 生效**(源码 `if name == "heap" && gc > 0`，对其他 profile 静默忽略)；debug≠0 → text/plain 否则 octet-stream+attachment；Profile 端点 seconds ParseInt 基 10、≤0 或解析错默认 30s、StartCPUProfile 已占用 → 500(边采边流式，CPU 非 Profile 对象的根因)；Trace 用 ParseFloat 可带小数默认 1s；Cmdline = os.Args 以 NUL 连接；Symbol 恒先 num_symbols: 1、PC 用 '+' 分隔、ParseUint base-0 识别 0x；**mux 精确匹配语义：四条特殊路径直达各自 handler，若经 Index 委托 handler("cmdline") 会 Lookup 失败 404——这正是四条独立注册存在的意义**；坑：Python 复刻 Symbol 时 int(s,10) 解析不了 "0x401000"，源码是 ParseUint(word, 0, 64) 必须 base-0 | Python(252 行 15 断言) + Go(283 行人工审查+配平) | 2026-09-18 |
| 318 | `11-性能分析/02-应用级剖析/pprof-top解读/` | top/top -cum 解读(Go Blog Profiling Go Programs 实读)：CPU 剖析每秒约 100 次记 goroutine 栈 PC，样本数÷100 ≈ 采集时长(2525 样本≈25s)；五列 = flat 计数、flat%、running%(列表内累计)、cum 计数、cum%；**flat = 该函数为叶子帧(正在运行而非等待被调函数返回)，cum = 出现在栈上任一位置**；经典实例 FindLoops flat 10.6% vs cum 84.1%；-cum 按累计列排序，gosched0/main.main 这类 flat=0 高 cum 的主干入口只有 -cum 能置顶；**栈深截断：每样本只含靠执行侧 100 帧、深递归截掉根侧**(博客 main.main cum 84.9%<100% 的唯一解释——若截叶子侧根帧永远在场)，flat 归因不受影响；nodefraction 过滤 cum/total < 阈值的节点(博客 --nodefraction=0.1 去噪 web 调用图)；--inuse_objects 报分配次数而非字节、内存采样 1-in-524288 是近似值；坑：默认 top 列表含 flat=0 行(如 main.main)，running% 断言别只数前三行 | Python(149 行 10 断言) + Go(241 行人工审查+配平) | 2026-09-18 |
| 319 | `11-性能分析/02-应用级剖析/py-spy采样剖析/` | py-spy 无侵入采样(benfred/py-spy README 实读)：Rust 独立进程经 process_vm_readv(Linux)/vm_read(macOS)/ReadProcessMemory(Windows) 直接读目标内存，不注入代码故"safe for production"；栈重建 = PyInterpreterState → 全部线程 → 沿 PyFrameObject back 链；ABI 逐版本变 → bindgen 生成各版结构；ASLR 对策：带符号 deref interp_head/_PyRuntime，剥离符号则扫 BSS 段找候选；三子命令 dump(单次各线程栈+--locals，定位挂死)/top(实时 %own 叶子/%total 在栈/%GIL)/record(折叠栈→火焰图)；--gil 只采持锁线程(**会漏掉释放 GIL 仍在干活的原生扩展**)；GIL 归属判定 ≤3.6 用 _PyThreadState_Current、≥3.7 从 _PyRuntime 推导；--nonblocking 不暂停目标但读非原子 → 偶发撕裂读(残缺栈叶子侧保留/采样错误)；权限模型：Linux attach 非子进程默认需 root(ptrace_scope)、自建进程不需要，macOS 恒需 root，Docker/K8s 需 SYS_PTRACE 能力位；--subprocesses 自动 attach 子进程(gunicorn 池) | Python(251 行 12 断言) + Go(284 行人工审查+配平) | 2026-09-18 |
| 320 | `11-性能分析/02-应用级剖析/cProfile-tottime-cumtime/` | cProfile 输出解读(docs.python.org/3/library/profile.html 实读)：确定性剖析监控**所有**调用/返回/异常事件并精确计时(解释器自带钩子故无需插桩)，统计性剖析随机采样指令指针；**tottime = 事件间隙记给当前栈顶(不含子函数)，cumtime = primitive 调用闭合时 t_return−t_enter(含子函数)**；恒等式 Σtottime == 总时长；ncalls 递归时印 total/primitive(primitive = 非递归引起的调用，文档示例 3/1)；两个 percall 分母不同：tottime/ncalls 与 cumtime/**primitive**；递归 cumtime 特殊处理"只按 primitive 记账"使递归与迭代实现统计可直接比较(2/1 递归用例 tottime==cumtime==3)；pstats：strip_dirs/sort_stats(TIME=tottime, CUMULATIVE, CALLS)/print_stats(int 行数、float 行数占比、str 正则)；指标用途三句话：计数→bug 与内联点、内部时间→热循环、累计时间→算法选型；C 级函数不产生事件故"显得格外快"；坑：断言第 N 次算错手账(异常用例 A 在 B 进入时被记 1 单位、float 0.5×3 行截断取 1 非四舍五入) | Python(193 行 12 断言) + Go(294 行人工审查+配平) | 2026-09-18 |
| 321 | `11-性能分析/02-应用级剖析/benchmark-ReportAllocs/` | Go benchmark 分配统计(pkg.go.dev/testing 实读)：b.N 机制原文"called multiple times with b.N adjusted until the benchmark function lasts long enough to be timed reliably"→ setup 会被执行多次须 ResetTimer(新代码推荐 b.Loop：只对循环体计时)；ReportAllocs "equivalent to -test.benchmem, but only affects the benchmark function that calls ReportAllocs"；**B/op = r.MemBytes/r.N、allocs/op = r.MemAllocs/r.N 均整数除法**(1000/3=333)；ResetTimer "zeroes elapsed time and memory allocation counters...does not affect whether the timer is running"(清账本不动开关)；StopTimer 期间的分配不入账；SetBytes 是"单次操作处理的字节数"→ 输出 MB/s 吞吐列(**与 B/op 的分配量完全两回事**)；RunParallel "reports ns/op as wall time for the benchmark as a whole, not the sum over each parallel goroutine"且 body 禁用三个计时器方法(全局效果)；输出行 `BenchmarkX-GOMAXPROCS  N  ns/op [+B/op +allocs/op] [+MB/s]`，格式规范 go.dev/design/14313-benchmark-format，A/B 比较用 benchstat；坑：浮点 10×0.1 累加=0.999…9 达标判定须容差；方法名与属性同名(report_allocs)在 Python 里属性覆盖方法 | Python(223 行 10 断言) + Go(277 行人工审查+配平) | 2026-09-18 |
| 322 | `01-游戏开发/02-渲染/图形管线/延迟渲染GBuffer/` | 延迟着色两 pass:geometry pass 以深度测试只留最上层片元,把位置(RGBA16F)/法线(RGBA16F)/反照率+镜面(RGBA8)写进 G-buffer(MRT 单 pass);lighting pass 用屏幕四边形逐像素从 G-buffer 采样重建片元,光照数学与前向完全相同(断言两管线输出逐像素一致)。Light Volume:衰减方程 I/(Kc+Kl·d+Kq·d²) 取 5/256 阈值(8 位 framebuffer 的接近黑)反解二次方程得光源作用半径,渲染背面球体,把光照计算量从 O(pixels×lights) 降到 O(pixels+lights);fragment shader 里 if(dist<radius) 在 GPU 上分支两端都执行、完全无效,必须靠光栅化覆盖范围提前跳像素。G-buffer 带宽 184 bit/px(RGBA16F×2+RGBA8+D24)≈1080p 45.5MiB;单张 32bpp 纹理 1080p 就 8.29MB。实测交叉点:32 盏小体积灯 forward 8192 次光照 vs deferred 2163;单盏大体积亮灯 deferred 512 > forward 256(少光源时 G-buffer 写入是纯开销)。透明物体必须单独前向 pass(glBlitFramebuffer 复制深度)。11 项断言 | Python(249 行) + Go(228 行) | 2026-09-18 |
| 323 | `01-游戏开发/02-渲染/图形管线/ForwardPlus分块光源剔除/` | Forward+(tiled forward):屏幕 16px tile 网格,每 tile 由 4 个过视点的侧平面构成微型视锥 —— 视空间视点在原点 → 平面方程 d=0,法线 = 角射线叉积(TL×BL/BR×TR/TR×TL/BL×BR)再翻转指向 tile 中心射线。点光源按球体判定:任一平面 ρ=N·C<−r 剔除(保守:贴角不进也误保留,只损失性能);近/远平面由 tile 深度归约 [zMin,zMax] 替代(InterlockedMin/Max on asuint 位模式保序);opaque 列表用 [zMin,zMax]、transparent 列表近端放宽为 0(半透明可出现在相机与最浅不透明面之间)。Compute 组织:[numthreads(16,16,1)] 一组一 tile,跨步循环 i=GI;i+=256 分摊光源,先落 groupshared 局部列表再由线程 0 InterlockedAdd 预约全局区段。Clustered:指数深度切片 Z=near·(far/near)^(s/N) 解决 2D tile 深度不连续的过度指派(实测近墙像素 tiled 4 盏 vs clustered 0);分块维度与 deferred/forward 正交(BF3 tiled deferred、DOOM 2016 clustered forward)。规模口径:forward~100 盏/deferred~2500/Forward+ 5000-6000 @1080p。14 项断言 | Python(248 行) + Go(208 行) | 2026-09-18 |
| 324 | `01-游戏开发/02-渲染/图形管线/GPU实例化Instancing/` | 实例化渲染:渲染顶点快、向 GPU 下达指令不快(每次 draw call 前的 bind VAO/纹理/设置 uniform 走慢速 CPU→GPU 总线),万片草叶的瓶颈是数千次 draw call 而非光栅化。glDrawArraysInstanced 多一个 instance count;gl_InstanceID 从 0 起每实例递增(第 43 个实例 ID=42)。两条属性泳道:uniform 数组受 GL_MAX_VERTEX_UNIFORM_COMPONENTS(保证下限 1024)约束,每实例 vec2 → 仅 512 个;instanced array 走顶点属性通道 + glVertexAttribDivisor(attr,1)(divisor=0 每顶点前进/1 每实例前进),只受显存约束。mat4 实例变换占 4 个连续属性位置(每属性最大一个 vec4),第 3 列(location 6)携带平移。实测记账:1000 小行星+1 行星 naive 1001 次 draw call CPU 准备 25.0ms vs 实例化 2 次 0.050ms(500 倍),顶点着色量完全相同(576×1001);100000 实例×576 顶点 ≈57.6M 顶点/帧仍 2 次调用。17 项断言 | Python(190 行) + Go(157 行) | 2026-09-18 |
| 325 | `01-游戏开发/02-渲染/图形管线/TileBasedGPU架构/` | 移动 GPU 图块化渲染:共享内存带宽 4-8 GB/s × 150pJ/byte ≈ 0.6-1.2W(Arm 口径,带宽吃掉整个功耗预算)→ 把 framebuffer 切 16×16 tile,binning 先把三角形按覆盖写进各 tile 的 to-do list(bbox 排他边界 → tile 范围 [⌊x0/t⌉,⌈x1/t⌉),实测 UI 三角形恰好进 4×4 个 tile),逐 tile 在片上 tile memory 光栅化,resolve 后一次写回。外部带宽只剩 loadOp/load 与 storeOp/store 两个阀门:clear 在 tile memory 里免费、loadOp=LOAD 整帧回读使流量近乎翻倍(实测 load 字节=store 字节)、depth 用完即弃 storeOp=DONT_CARE 省一整张附件写回。MSAA 4x:IMR 外部流量严格 ×4,TBR 采样只在片上、resolve 后仍写每像素 1 样本(store 不变),代价转移为 tile memory ×4。Metal=Memoryless/Vulkan=LAZILY_ALLOCATED(允许溢出显存,Metal 不允许);Apple TBDR 可读 render target(programmable blending)做单 pass 延迟渲染,G-buffer 完全不落系统内存。实测 overdraw=8:IMR 8.45MB vs TBR 0.39MB(21.4 倍),且 TBR 对 overdraw 不敏感。14 项断言 | Python(177 行) + Go(167 行) | 2026-09-18 |
| 326 | `01-游戏开发/02-渲染/图形管线/计算着色器执行模型/` | Compute Shader 三层线程组织:Dispatch(gx,gy,gz) 启动组、[numthreads(x,y,z)] 声明每组线程数;四个系统值 SV_GroupID/SV_GroupThreadID/SV_DispatchThreadID(=GroupID×numthreads+GroupThreadID)/SV_GroupIndex(=z·X·Y+y·X+x) —— 文档标准例 Dispatch(5,3,2)+numthreads(10,8,3) 中 GroupThreadID(7,5,0)→DispatchThreadID(27,13,0)/GroupIndex 57,全量枚举 3600 线程验证 DispatchThreadID 无重复、GroupIndex 双射。硬件限制:cs_5_0 每组 ≤1024 线程且 Z≤64(总闸与各维独立:(33,33,1) 总数超/(64,1,65) Z 超)、Dispatch 每维 ≤65535、groupshared 32KB;cs_4_x 更紧(768/16KB/无原子/Z=1)。groupshared 树形归约:每线程写自己值 → 屏障 → 逐级折半(漏屏障读到旧值,屏障是"看到他人写入"的唯一保证);原子 InterlockedAdd vs 非原子丢更新(全线程读旧值 0 再各写各的,只剩最后写)。22 项断言 | Python(220 行) + Go(152 行) | 2026-09-18 |
| 327 | `02-Web开发/03-数据库/SQL与ORM/N+1与预取策略/` | N+1 与预取策略(SQLAlchemy 2.0 官方 relationships/loading 文档实读):N+1 的成因是 `relationship()` 默认 `lazy="select"` —— 访问每个父对象的集合属性时**各发一条 SELECT**,20 个父对象 = 1+20 条。官方四种策略的语义差别不在结果而在**语句数**:`selectin` 用一条 `IN (?,?..)` 一次性取回(官方默认批量 500,本 demo `SELECT_IN_BATCH = 500` 分批)、`subquery` 把父查询当子查询再 JOIN、`joined` 用 LEFT OUTER JOIN 一次取全 + `unique()` 去重、`raiseload` 反向把懒加载变成硬错误(用于"我在生产上绝不允许 N+1")。实测语句数 select=6 / selectin=2 / subquery=2 / joined=1,而**四者返回的行集合逐字节相同** —— 这正是"策略只改语句数不改结果"的可测形式。坑:`joined` + 集合关系不去重会抛 `InvalidRequestError`(官方明写),因为 JOIN 会为每条子行重复父对象;`selectin` 的批次大小不是"越大越好",超过参数上限会退化成多次查询 | Python(297 行 20 断言) + Go(194 行人工审查) | 2026-09-18 |
| 328 | `02-Web开发/03-数据库/SQL与ORM/EXPLAIN执行计划解读/` | EXPLAIN 执行计划解读(PostgreSQL 14.1 官方 using-explain 文档实读):PostgreSQL 的 cost 是**任意单位(arbitrary units)**,由 `postgresql.conf` 的 `seq_page_cost`(默认 1.0)与 `cpu_tuple_cost`(默认 0.01)标定 —— 所以 `seq_scan_cost(pages, rows) = pages*1.0 + rows*0.01`,345 页 10000 行顺序扫描 = 445,数字本身无物理量纲,只有**相对大小**有意义。节点树解析要点:顶层节点打印**没有 `->` 前缀**,子节点才有;每行 `label (cost=startup..total rows=N width=W)` 与 `(actual time=.. rows=N loops=L)` 是**两套口径**(前者估算、后者实测),`actual time` 必须**乘 loops** 才是该节点总耗时(官方原话 "Multiply by the loops value");`loops` 的会计口径是**每子行执行一次**,所以嵌套循环里内层节点的 loops 常等于外层 rows。`Rows Removed by Filter` 是"取回行数"与"最终保留行数"的差 —— 估算与实际在这列上的巨大差通常是统计信息过期而非计划选错。坑:`LIMIT` 会**截断 actual rows 显示**,看到 rows 小于预期不等于估算错误 | Python(232 行 18 断言) + Go(243 行人工审查) | 2026-09-18 |
| 329 | `02-Web开发/03-数据库/SQL与ORM/连接池调优/` | 数据库连接池大小(HikariCP 官方 wiki "About Pool Sizing" 实读):官方经验公式 `connections = core_count * 2 + effective_spindle_count` —— 4 核 + 1 块盘 = 9,注意**超线程不增加 core_count**(官方明写 "you would count 4 cores, not 8"),`effective_spindle_count` 在 SSD 上按官方口径已趋近于 1 甚至可忽略。反直觉推论:池**不是越大越好**,官方给的"小池 + 快查询"压倒性优于"大池 + 排队"的模型说明,过大的池只是把排队从连接池挪到了 CPU/磁盘。另一条硬下界是 **pool-locking**:若一个线程执行期间要同时持有 Tn 个连接(嵌套获取),则池至少得 `Tn * (Cm - 1) + 1`(Cm = 每线程最大并行获取数)—— 少于这个数会发生线程间互相等待的死锁,本 demo 以 `pool_locking_min(3, 4) = 10` 断言。demo 用简化队列模型(cpu=5 / io=20 / 上下文切换税 0.2)复现"吞吐峰值出现在远小于线程数的池容量"这一现象,峰值落在 P=8~12 | Python(191 行 22 断言) + Go(203 行人工审查) | 2026-09-18 |
| 330 | `02-Web开发/03-数据库/SQL与ORM/键集分页/` | 键集分页 vs OFFSET(SQL:2023 Part 2 §4.17.3 + PostgreSQL 14.1 queries-limit 实读):SQL 标准对 OFFSET 的定义是"先排序、再丢弃前 N 行",所以**代价与 N 成正比、与页大小无关** —— `offset_page()` 逐页统计 `rows_examined = page * size + size`,第 1000 页要扫过 9990 行才吐出 10 行,深分页比值 > 100。键集(keyset)分页把游标编码进 WHERE:`WHERE (score, id) < (?, ?) ORDER BY score DESC, id DESC LIMIT n`,行值比较(row value comparison)让数据库能直接**索引定位**到起点,代价 O(log N) 与页码无关。真实差异在**一致性**:翻页期间新插入的行会把 OFFSET 窗口整体后移,导致下一页**重复显示**已看过的行(本 demo 用确定性 (score, id) 构造复现),而 keyset 因为锚定的是"最后一行的值"不会漏也不会重。代价:keyset 无法跳任意页(没有"第 1000 页"按钮)、需要稳定全序(排序键必须唯一,否则要补 tiebreaker),且要求客户端保存游标 | Python(184 行 18 断言) + Go(257 行人工审查) | 2026-09-18 |
| 331 | `02-Web开发/03-数据库/NoSQL/Redis数据结构底层/` | Redis dict 渐进式 rehash 与 zskiplist(redis/redis 源码 `src/dict.c` / `src/t_zset.c` / `src/server.h` 实读):dict 是**双表结构** `ht[0]`/`ht[1]`,rehash 时不是一次搬完,而是每次增删查改搬一个非空桶,**查找要同时看两张表并跳过 `idx < rehashidx` 的旧桶**,新插入**一律进 ht[1]**(保证不出现"搬完又得搬"的回流)。四组触发条件各不相同:正常扩容 1:1(used >= size)、收缩 1:8、`dict_can_resize` 关闭时仍需 4× 强制扩容、以及**空桶数 >= size*10 时的上界保护**(防止稀疏大字典永不收缩)。`pauserehash` 用于安全迭代器:冻结搬运期间新增的键仍进 ht[1] 且可查。zskiplist 是 zset 的有序实现:`P = 0.25`、`MAXLEVEL = 32`,每个节点带 `span` 记账使 `ZRANK` 能 O(log N) 求排名(1-based),`level[0].backward` 提供逆序。紧凑编码阈值:`zset-max-ziplist-entries 128` / `-value 64`、`hash-max-ziplist-entries 512` / `-value 64` 超限即转普通编码。实测 rehash 日志 `[(0,4),(4,8),(8,16),(16,32),(32,64)]`,LCG 层高分布 avg=1.3319、P(≥2)=0.2492、P(≥3)=0.0624 与 1/(1-p) 理论值吻合 | Python(281+195+259 行 47 断言) + Go(218+136+162 行人工审查) | 2026-09-18 |
| 332 | `03-系统编程/03-内存管理/分配器/mmap-brk-虚拟内存/` | brk/sbrk 与 mmap 两条分配路径与虚拟内存记账(Linux man-pages 6.19 的 malloc.3 / mmap.2 / brk.2 / mallopt.3 实读):内核侧三套分配器 —— malloc 走 brk 推高 program break、大块走 mmap 私有匿名映射、内核自己用 slab/slub 管对象缓存;brk 的语义是"设新 break 并原样返回旧 break"(sbrk 才是增量,且**返回旧 break 不等于分配成功** —— 失败也返回旧值,只能靠 errno 区分),读未触碰的匿名页是 0 填充(COW 零页);mmap 约束极硬:`addr` 必须页对齐、文件映射 `offset` 必须是页大小的整数倍(巨页映射更要求 offset 是底层巨页大小的整数倍)否则 EINVAL,`length` 不必对齐(内核向上取整到页),失败返回 `MAP_FAILED = (void*)-1` 而非 NULL(**必须与 MAP_FAILED 比较,判 NULL 永远不成立**);`MAP_FIXED` 的语义是**只丢弃重叠部分**、不整条映射(本 demo 初版按"整条丢弃"写,导致"范围外的页不受影响"断言失败 —— 手册措辞就是 the overlapped part,这类粒度写粗的错误不报错、只让后续断言莫名失败);mallopt 侧:默认 mmap 阈值 128 KiB,当**被释放**的块大于当前阈值且不超过 `DEFAULT_MMAP_THRESHOLD_MAX`(64 位下 32 MiB)时阈值**上调到该块大小**,trim 阈值同步被动态调整为动态阈值的 **2 倍**(顶到 32 MiB 即停),而**只要用 mallopt 设过 M_TRIM_THRESHOLD / M_TOP_PAD / M_MMAP_THRESHOLD / M_MMAP_MAX 中任意一个,动态调整即被禁用**;Linux 4.7 起 `RLIMIT_DATA` 同样约束 mmap 分配;另有 M_ARENA_MAX 与 M_PERTURB(free 时按位填充,能暴露 use-after-free)。demo 用页粒度模型复现"brk 不可局部回收 vs munmap 立即归还内核"以及阈值的动态爬升路径 | Python(224 行 63 断言) + Go(281 行人工审查) + C(mmap_brk.c 243 行人工审查) | 2026-09-18 |
| 333 | `03-系统编程/03-内存管理/分配器/ptmalloc2/` | glibc ptmalloc2 的常量公式与查找顺序(glibc 官方 wiki MallocInternals + `malloc/malloc.c` master 与 2.35 源码实读):64 位下 chunk 头 16 字节,`prev_size` 与 `size` 复用(只有前一个 chunk 空闲且本块不在 mmap 路径时 `prev_size` 才有效),`size` 低 3 位是标志 PREV_INUSE / IS_MMAPPED / NON_MAIN_ARENA,最小 chunk = 4×sizeof(void*) = 32 字节;`request2size` **不是"向上取整到 16"** —— 要先加 `SIZE_SZ` 再对齐,故 `malloc(64)` 得到的 chunk size 是 80(64+8 升到 16 的倍数),把它当成 64 会让所有以尺寸为键的断言(fastbin / tcache idx)整体错位(demo 开发期正因此挂 3 处断言);四类 bin:fastbin(单链表 + LIFO,尺寸 16~80 字节共 10 个)、smallbin(< 1024,双向、FIFO、精确尺寸)、largebin(≥1024,按尺寸序,`fd_nextsize` 提供跳跃指针)、unsorted(就是第 1 号普通 bin、双向、`free` 的默认落点,"给一次被快速复用的机会");`bin_index` 的源码注释自带 slop("There is actually a little bit of slop ... for the sake of speed"),断言应直接对公式求值而不是照抄注释里的区间描述;tcache(2.26 引入)在 malloc/free 的**最前**被查,每线程每尺寸一条 payload 链表加 `counts` 数组,**只做精确匹配、不做尺寸放宽**(这是它与 unsorted / largebin 的本质区别 —— 实现写成"找不到精确的就用一个更大的"会凭空造出内部碎片,而断言往往还能过,因为它只是更浪费而不是更错),master 为 `TCACHE_MAX_BINS=76` / `FILL_COUNT=16`,2.35 起改为 64 / 7;fastbin 的访问可用原子操作完成故**不必加锁**;arena 方面 main arena 用 sbrk 扩展,检测到锁竞争后由 mmap 创建附加 arena 与 heap,数量上限 = 8 × CPU 核数(`M_ARENA_MAX` 可覆盖),线程用 TLS 记住自己上次用的 arena;`free` 之后 RSS 不下降是正常的,想让内存真回去要么靠堆顶修剪(trim 阈值默认 128 KiB)、要么 `malloc_trim(0)`、要么把大块配成 mmap 路径。demo 逐条复刻 `request2size` / `bin_index` / `csize2tidx` 并把 malloc/free 的查找顺序还原成可观测的步骤日志 | Python(pm_model 98 + pm_arena 202 + main 231 行 83 断言) + Go(pm_model 90 + pm_arena 277 + main 277 行人工审查) | 2026-09-18 |
| 334 | `03-系统编程/03-内存管理/垃圾回收/gc-cheney-copying/` | Cheney 半空间复制式 GC(Baker/Cheney 经典算法 + Memory Management Reference 术语页实读):堆分成 from-space / to-space 两半,分配只在 from-space 的 free 指针上推进;收集时先把根可达对象复制到 to-space,再用 `scan` / `alloc` 两个指针 —— **scan 追 alloc,中间那段就是队列**,于是整个 BFS 不需要递归栈也不需要显式队列(这是 Cheney 相对简单复制算法的全部卖点);每个被复制的对象都在旧地址留下**转发指针**(指向 to-space 新地址),再次遇到同一对象时直接取转发地址,**命中转发指针时 alloc 不能前进** —— 若把命中也算成一次复制,to-space 会凭空多出副本,而"对象首尾相接"的断言还能过(多出来的副本也是连续的),属于典型的静默错误;收集结束后 from-space 整体作废、两半互换。复制顺序严格等于 **BFS 层序**(demo 用 A→{B,C}, B→D, C→D, D→E 断言得到复制序 A,B,C,D,E),因此遍历赋值表达式 `x = y + z` 时两个操作数会被搬成相邻地址 —— 这正是复制式 GC 提高局部性的来源。代价:物理内存有效利用率 = 存活量 / (2×堆),**恒等于存活率的一半**,而对照的标记-清除只要 1× 堆加标记位(按平均 8 字/对象算约堆的 1/64);另一代价是成本 ∝ 存活量(全存活时最贵),标记-清除则 ∝ 堆大小(存活越少越亏) | Python(cheney 119 + main 271 行 59 断言) + Go(cheney 165 + main 200 + demos 177 行人工审查) | 2026-09-18 |
| 335 | `03-系统编程/03-内存管理/垃圾回收/gc-mark-compact/` | 标记-压缩式 GC(Memory Management Reference 对 mark-compact 的定义 + GHC `rts/sm/Compact.c` + V8 `src/heap/mark-compact.h` 实读):术语页把 mark-compact 定义为 "a number of sequential passes"、回收后留下 "a single contiguous block of memory"、目的是 "avoid external fragmentation and to increase locality of reference" —— 三条正好对应两阶段(标记 + 压缩)与两个收益;压缩的难点全在**移动对象之后如何修正引用**,对策是转发表(forwarding table):标记阶段顺带算出每个存活对象的新地址,而**更新引用必须在搬对象之前完成**(否则旧地址已被覆写)。保序(order-preserving,Lisp2)压缩把存活对象按原地址序滑动到堆底,空闲区被挤成"一整块"(demo 断言空闲段个数 == 1),代价是搬运量大、收益是保持原相对顺序从而改善引用局部性;实测同一引用序 A→E→D→C→B 下,保序布局的地址跳跃总量 20、层序布局只有 10(**恰好 2 倍**)—— 说明"保序一定更好"是错的,层序在小图里跳跃更少,保序的价值在长程与时间局部性而非单次测量。GHC 的 `Compact.c` 用**线程化(threading)**免去转发表:把指向同一对象的字段逐个链进该对象的 info 槽(`*field, **field = **field, field`),链尾放原 info 内容,大小写在**指向槽的指针**上(tag 1 = 未标记 / 2 = 已标记),搬完后沿链回头把每个字段改成新地址并顺带还原信息、统计更新数;V8 则把整个流程显式状态机化:`CollectorState{IDLE, PREPARE_GC, MARK_LIVE_OBJECTS, SWEEP_SPACES, ENCODE_FORWARDING_ADDRESSES, UPDATE_POINTERS, RELOCATE_OBJECTS}`,并断言"指针编码只在 UPDATE_POINTERS 阶段生效"(`are_map_pointers_encoded() == (state_ == UPDATE_POINTERS)`),顺序即"先编码转发地址 → 再改引用 → 最后搬对象"(demo5 用真实状态名复现该顺序) | Python(compact 209 + main 288 行 52 断言) + Go(compact 274 + main 235 + demos 248 行人工审查) | 2026-09-18 |
| 336 | `03-系统编程/03-内存管理/垃圾回收/gc-generational/` | 分代式 GC 与 CPython 三代回收(弱分代假说 + Oracle V8 调优指南 MaxTenuringThreshold + CPython `Python/gc.c` / `Modules/gcmodule.c` / `Include/internal/pycore_runtime_init.h` 实读):弱分代假说 —— 绝大多数对象很快死亡,所以只频繁收集新生代(eden + 两块 survivor 交替充当 from/to,复制式 minor),老年代只在容量超限时做 major;晋升(tenuring)按对象在 minor 中存活的次数,超过 `MaxTenuringThreshold` 才进老年代,**survivor 的存活率还会动态调整该阈值**;跨代引用是分代的命门 —— 老对象指向新对象时不能只扫新生代(会漏),对策是**写屏障 + 记忆集/脏卡**:任何写入 owner.field = young 的位置都被记进记忆集(卡表按页标记),minor 时把记忆集当额外根扫描。本 demo 两条关键实测:① 扫描成本 —— 长期存活集(2000 字)建好后跑 4000 字 churn,**分代路径只扫 4008 字、老年代那 2000 字一次没碰,基线(关掉分代)扫 21708 字,5.4 倍**;② 基线对照只能关掉"分代"这一层 —— 初版基线用新生代容量(12 字)作"堆满"判据,导致它疯狂做全量收集(82 次)且比值仅 1.35 倍,**对比不公平**,改成两边共用同一套堆预算(young + old 容量)后才得到真实的 5.4 倍(基线被非目标因素放大,省下的倍数是假的)。另一处易漏细节:晋升本身会"新造"一条老→新引用,故 major 之后必须重建记忆集(`_rebuild_remembered`)。CPython 侧是三代加计数阈值(不是 Java 那种空间分代):`record_allocation` 在净分配数超 `threshold0` 时触发,`gc_select_generation` 逐代**累加计数**判断该收哪一代,一旦某代存活对象超过 **25%**(LONG_LIVED_RATIO)就升级为长期存活并**跳过更年轻的代直接收最老代**(issue #4074 的启发式,用于避免反复无效收集长生命周期容器);默认阈值**版本差异经实测纠正**:`gc.get_threshold()` 在 3.13.14 与 3.14.6 上均返回 **(2000, 10, 10)**,源码 `pycore_runtime_init.h` 3.12 为 {700}、3.13+ 为 {2000} —— 凡把默认值写成 700 的资料都停留在 3.12 之前(该组真实性断言只有 Python 版,Go 无 `gc.get_threshold` 对应物,已在代码里留 `[info]` 说明) | Python(gen_model 218 + cpy_gc 142 + harness 26 + demos 122 + main 209 行 79 断言) + Go(gen_model 177 + gen_collect 166 + cpy_gc 150 + main 126 + demos 150 + demos2 166 行人工审查) | 2026-09-18 |
| 337 | `04-移动开发/03-跨平台/Flutter/平台通道/` | Flutter 平台通道与 StandardMethodCodec 二进制格式(docs.flutter.dev 平台通道页 + flutter 源码 `packages/flutter/lib/src/services/message_codecs.dart` 实读):编解码的**类型判定字节**是全部语义的锚 —— 0=null / 1=true / 2=false / 3=int32 / 4=int64 / 5=largeInt / 6=float64 / 7=string,8..14 覆盖 TypedData 各宽度、List、Map。长度用 **expanding 格式**:0..253 单字节、254 则后跟 uint16、255 则后跟 uint32(**注意 254/255 是转义标记而不是长度本身**,把 254 当长度 254 会让后续所有字段整体错位)。`float64` **必须 8 字节对齐**,编码器要补 `(-offset) % 8` 个 0 字节(**补的是"到下一个 8 的倍数还有几个",不是 8 减余数** —— 本 demo 首版即写成 `8 - offset % 8` 导致 offset 恰为 8 倍数时多补一整组);TypedData 的元素区按**元素宽度**对齐而非一律 8 字节(Int32List 4 字节对齐),这是与 float64 不同的第二套对齐规则。`MethodCodec` 在此之上加两层信封:调用侧 `[方法名字符串, 参数]`,应答侧首字节 **0 = 成功、非零 = 错误**(判定是"非零"而非"等于 1" —— 官方实现用 `if (reply[0] != 0)`),错误信封再带 code/message/details,若还有第 4 个值则是 stacktrace;乱序或截断的字节流一律抛同一个 `FormatException('Message corrupted')`(官方不为不同损坏原因分错误类型,所以**无法从异常区分"读到越界"与"类型字节非法"**)。补充:`EventChannel` 走的是同一种 codec 但方向是 native→Dart 的持续流;后台 isolate 需 `BackgroundIsolateBinaryMessenger` 才能收发(普通 `ServicesBinding` 的 messenger 只在 root isolate 有效)。75 项 Python 断言全绿 | Dart(117 行) + Python(298+75+248 行) | 2026-09-18 |
| 338 | `04-移动开发/03-跨平台/Flutter/dart-ffi/` | dart:ffi 的 ABI 相关整数类型(dart.dev/interop/c-interop + api.dart.dev 的 `AbiSpecificInteger` / `Long` / `Size` / `UintPtr` 实读):`dart:ffi` 把整数类型分成**三类** —— 定宽(`Int8..Int64` / `Uint8..Uint64`,任何 ABI 下宽度固定)、**仅作标记**的抽象类型(`AbiSpecificInteger` 的子类如 `Long` / `Size` / `UintPtr`,不能实例化、只用于 `@Long()` 这样的标注)、以及可实例化且宽度随 ABI 变化的映射类型(`Long` 对应 `int`、`Size` 对应 `int`)。关键点在 **LP64 与 LLP64 的分野**:`Long` 表示 C 的 `long`,在 linuxX64 / macosArm64 / androidArm64 / iosArm64 等 LP64 平台是 64 位,但在 **windowsX64 / windowsArm64 这类 LLP64 平台上只有 32 位** —— 同为 64 位平台,`long` 的宽度并不一致(指针 8 字节而 `long` 仍 4 字节)。因此 `@Long()` 标注的结构体字段**不能用一个固定的 native 类型去承接**,跨平台写法必须用 `Long` 标注让编译器按目标 ABI 取宽度;demo 用 `LONG_BITS`(20 项)与 `UINTPTR_BITS`(22 项)两张表建了 ABI→宽度映射,并在两表**交集**(20 个共同 ABI)上做断言 —— 本机抓到的 api.dart.dev 快照是 2.19.5,不含 `androidRiscv64` / `fuchsiaRiscv64`,故只对交集做全量断言并把该口径差异显式写进 README 与代码注释(不编造未见过的 ABI 宽度)。另演示 `naive_struct_stride` 错口径:把字段宽度按"按对齐上取整"与"直接累加"两种算法算出的 stride 差异(32 位 `long` 上 3 个字段各吃 4 字节额外填充 = 12 字节),以及 `calloc`/`free` 与 `@Native` 两步绑定的写法;`sizeof(long)` / `sizeof(uintptr_t)` 由 C 侧导出供真机比对。37 项 Python 断言全绿 | Dart(111 行) + C(65 行) + Python(108+140 行) | 2026-09-18 |
| 339 | `04-移动开发/03-跨平台/Kotlin-Multiplatform/内存模型/` | Kotlin/Native 内存模型的新旧交替(kotlinlang.org 的 native-memory-manager / native-migration-guide / native-arc-integration 三篇实读):**旧模型的核心是"冻结(freeze)"** —— 跨线程共享的对象必须冻结,冻结后不可变,违者 `InvalidMutabilityException`;`@SharedImmutable` 声明全局常量、`AtomicReference` 用于共享可变值。冻结的代价是**引用环永不释放**(环上对象互相持有、引用计数永不为零),这是旧模型最被诟病的泄漏形态。**新模型(1.7.20 默认、1.9.20 后旧模型完全移除)**改为**共享堆 + 追踪式 GC**:收集器是 CMS(并发标记清除),**不分代**、多线程并行标记,于是对象可以自由跨线程传递、`AtomicReference` 的引用环能被回收。两处易漏的行为变更:①**全局属性从急切初始化改为惰性初始化** —— 旧模型下顶层 `val` 在首次访问某属性时会初始化**同一文件内的全部**全局,新模型改为按需逐个初始化(官方另有 `@EagerInitialization` 恢复急切语义,但该注解必须配 `-Xproperty-lazy-initialization` 关闭或按官方说明使用),本 demo 用 `GlobalInit(eager, eager_marked)` 把两种语义都建模并断言访问一个属性后其余属性的初始化状态;②与 **Swift/ObjC 的 ARC 集成**是另一套规则 —— 从 Kotlin 回调进 ObjC 时 `deinit` 不保证在原线程执行(故 ObjC 侧释放逻辑跨线程安全性需自行保证),跨语言边界传递的长生命周期引用要用 **stable refs**(`createStablePointer` 类机制)显式钉住,否则被 GC 回收后 ObjC 侧拿到悬垂指针;Kotlin 侧调 ObjC 返回的 autoreleased 对象自行 `autoreleasepool`。demo 实测 stable refs 峰值 **1000 vs 1**(不做稳定化时同一对象被反复跨边界传递会不断复制/重建)。34 项 Python 断言全绿 | Kotlin(79+92 行) + Python(160+176 行) | 2026-09-18 |
| 340 | `04-移动开发/03-跨平台/Compose-Multiplatform/重组与SlotTable/` | SlotTable 的数据结构(androidx `compose/runtime/SlotTable.kt` 注释源码 + commit `a038886` 实读):Compose 运行时把组合产生的树与状态压成**两个扁平数组** —— `groups` 是整数数组,每个 group 占固定 `Group_Fields_Size` 个元素(头字段含键、group 大小、父/子锚点等),`slots` 是配套的槽数组。`slots` 用 **gap buffer** 实现:逻辑顺序上的连续槽在物理数组里被一个"间隙"切开,插入时若间隙不够就搬动元素;本 demo 的 `GapBuffer.move_gap_to(position)` 以**逻辑位置**(间隙前的有效元素数)为参数并在边界 `0..size` 上都合法,搬动计数为 `m`(间隙本身不算搬动 —— 首版按 `2m` 计会把成本算成两倍)。最反直觉的一条:**写模式下 `dataAnchor` 是锚点而不是下标** —— 因为 `slots` 里可能存在间隙,物理下标会随间隙移动而改变,所以读写过程中持有的引用必须是"锚",到真正访问时才解析成当下有效的下标;本 demo 的 `SlotTable` 用 `anchors` 映射维护"锚 → 物理下标",并在 `move_slots_gap_to` 里同步前移存活锚点的下标、在 `remove_group` 里**重映射父指针与锚点**(少了这一步,删掉当前打开中的 group 会留下悬空锚点,`verify_well_formed` 立刻判不自洽)。另外两条:①`groupSize` / `skipToGroupEnd` 让运行时能**整段跳过**一组原语而不逐个走,这是"跳过未变化子树"的成本来源;②四类 group —— Restart(可重组的函数调用点,`replaceableGroup` 的前身形态)、Replaceable(可被替换的 key 组)、Movable(可移动)、Node(附着到 UI 树)。回归点取自 commit `a038886`:当 `slots` **恰好占满(间隙为 0)** 时删除 group 会漏做"更新父锚点",本 demo 的 D 段专门以"满表 + 删除"作为回归用例。54 项 Python 断言全绿 | Kotlin(150 行) + Python(231+224 行) | 2026-09-18 |
| 341 | `04-移动开发/03-跨平台/WebView容器/Tauri-vs-Electron/` | Tauri 与 Electron 的架构分野(v2.tauri.app/concept/architecture + tauri-apps/tauri README + tauri-apps/wry + Electron 官方 4 篇文档实读):**分歧的根只有一条 —— 渲染引擎从哪来**。Electron **自带 Chromium**,安装包必然携带整个浏览器运行时,好处是 5 个平台只有 1 个引擎、行为完全一致;Tauri 用**系统 WebView**(Windows 用 WebView2 / macOS 用 WKWebView / Linux 用 WebKitGTK / Android 用系统 WebView),安装包不携带运行时(体积与内存显著更小),代价是**5 个平台至少 4 个引擎** —— 引擎碎片化直接变成兼容性成本,且客户端无法保证系统 WebView 版本(需运行时探测 + 降级)。**一次调用的跳数**是这一分野的直接结果:Electron 是 **5 段**(renderer JS → preload → IPC → main 进程 → Node API),Tauri 是 **3 段**(WebView JS → `invoke` → Rust core),少掉的正是"前端与原生之间的代理层" —— 本 demo 用 `TAURI_CALL_HOPS` / `ELECTRON_CALL_HOPS` 两组常量把这个差异固化成可断言的结构。权限模型也走了两条路:Electron 靠 **preload + `contextBridge` 手工裁剪**(把要暴露的能力一个个手工包出来,漏包就是能力泄漏),Tauri 靠 **capability 声明式**(在配置里声明允许哪些命令与哪些路径,权限是"默认拒绝"的白名单)。序列化上 Electron 的 IPC 走 **Structured Clone Algorithm**,因此不可传函数/DOM 节点/原型链,且大对象是**深拷贝**(负载大小与频率直接决定开销);Tauri 侧同为结构化序列化但也有等价的负载限制。沙箱:Electron 20 起 renderer **默认开启沙箱**,而 `nodeIntegration: true` 会**关掉沙箱**(二者互斥,官方明写),这是"图省事直连 Node"最常见的性能与安全代价。本 demo 还断言了"能力差集"的具体形态:unsandboxed 比 sandboxed 多出的恰好是 `fs` / `path` / `child_process` 三项,`node:` 前缀形式的条目不计入该差集(首版把它算进去导致集合包含关系不成立)。52 项 Python 断言全绿 | TypeScript(95 行) + JavaScript(120 行) + Python(98+125 行) | 2026-09-18 |
| 342 | `05-AI与机器学习/04-计算机视觉/Canny边缘检测/` | Canny 五阶段流水线(OpenCV 4.x `tutorial_py_canny` + OpenCV 3.4 `tutorial_canny_detector` + Stanford EE368 讲义 实读):三条设计判据 —— 低错误率(不漏不误)、良好定位(检出边与真实边距离最小)、单一响应(一条边只一个响应);五阶段 = 5x5 高斯去噪 → Sobel 求 Gx/Gy 与幅值/方向 → 非极大值抑制 → 双阈值 → 滞后连通。3.4 教程给的除数 159 的 5x5 整数高斯核实测是对 **σ≈1.4** 连续核的定点近似(逐元素比 25 个系数,最大相对偏差 **7.16%**),且两核都满足「2D 核 = 1D 核外积」即**可分离**(本 demo 的外积之差为 0,可分离后每像素由 25 乘加降到 10)。梯度方向量化到 0/45/90/135 四档,每档对应固定邻居偏移(注意梯度方向**垂直于**边缘),NMS 沿该方向与两邻居比较。滞后三情形(OpenCV 原例):A 幅值超过上限 → 确定边;C 低于上限但高于下限且**与 A 连通** → 保留得到完整曲线;B 高于下限但**与确定边不连** → 丢弃(哪怕与 C 同区)。等价说法是「对 weak 做连通域标记,只保留含 strong 像素的连通域」—— 这正是「弱边能被带出、孤立噪声不会」的机制来源;连通域用**显式栈**而非递归,不会爆栈。`cv.Canny` 默认 `apertureSize=3`、`L2gradient=false`(即**默认用 \|Gx\|+\|Gy\| 近似而非 sqrt(Gx²+Gy²)**,45° 边上两者比值恰为 √2≈1.4142);`high:low` 建议落 2:1~3:1(教程示例 `cv.Canny(img,100,200)`)。四个坑:①**理想阶跃的两个梯度峰严格相等** —— 0→200 竖直阶跃平滑后 \|G\| 在等距两列上都是 **434.1353**,平局规则必须写死:用 `>=` 留两列(边缘 2 px 宽)、用 `>` 一列都不留(**边缘整条消失**),本 demo 取「先出现的邻居必须严格更小、后出现的允许相等」保证每行恰 1 个边缘像素,该坑只在合成图/仿真暴露但一暴露就是整幅图级错误;②**平坦区有 1e-14 量级浮点残差** —— 同一幅图平坦区 \|Gx\| 因累加顺序不同可得 0.0 与 **2.842e-14**,L1 形式累加后更明显,若用 `m == 0.0` 判「无梯度」NMS 会在纯平坦区输出**一整排伪边**(自检 D 段单独断言),规避办法是给 NMS 加相对峰值的地板(本 demo 用 `peak * 1e-9`);③判据是 `>=` 不是 `>`,故 `low = high = 0` 会把 8x8 **常量图**判成 **64/64** 全边 —— 阈值必须 > 0;④`high`/`low` 是**绝对阈值、跨图不可移植**(对比度变化使幅值整体缩放);按比例相对 max 取或中位数启发式(`low≈0.67·median`、`high≈1.33·median`)是**工程惯例不是 Canny 原文口径**,README 与代码注释双处标注。边界处理本 demo 用 clamp 复制,OpenCV 默认 `BORDER_DEFAULT`(reflect_101),差异只影响最外 1~2 像素;未平滑 0→200 阶跃的 `\|Gx\|` 恰为 `4·(high−low) = 800`,故 high=300 能检出、high=500 检不出。47 项 Python 断言全绿 | Python(210+183 行) + Go(280+150 行) | 2026-09-18 |
| 343 | `05-AI与机器学习/04-计算机视觉/Harris角点检测/` | 结构张量与响应函数(OpenCV 4.x `tutorial_py_features_harris` + Harris & Stephens 1988《A Combined Corner and Edge Detector》实读):把「窗口沿任意方向平移 (u,v),窗口内强度变化都很大则该窗口中心是角点」的几何直觉写成二次型 `E(u,v) ≈ [u v] M [u v]ᵀ`,`M = Σ w(x,y) [[Ix², IxIy], [IxIy, Iy²]]` 是 2×2 对称**结构张量**(窗函数 w 按教程原文可为矩形窗或高斯窗);响应 `R = det(M) − k·trace(M)² = λ1λ2 − k(λ1+λ2)²` **只由特征值决定、无需真解特征值**;判别:λ1、λ2 都小 → 平坦(\|R\| 小)、一大一小 → 边缘(**R < 0**)、都大且接近 → 角点(R 大且 > 0)。`k` 经验值 0.04(教程示例 `cv.cornerHarris(gray,2,3,0.04)`)、区间 0.04~0.06。四条定量结论:①**R < 0 有闭式边界** —— 令 r = λ1/λ2 ≥ 1 则 `R = λ2²(r − k(r+1)²)`,`R > 0` 等价 `r < (1 − 2k + sqrt(1 − 4k)) / (2k)`,k=0.04 得上界 **≈22.9564**、k=0.06 降到 **≈14.5982**、k=0.02 升到 **≈47.9792**;实测 r=22 的结构 k=0.04 下仍是角点(**2.360**)、k=0.06 下已被判边缘(**−6.460**)—— 即 **k 越大 = 对边缘抑制越强 = 角点更纯但更易漏**,这是「k 不是精度参数而是敏感度旋钮」的定量含义;②结构张量**必为半正定**(λmin ≥ 0 恒成立,等价 Cauchy–Schwarz `Sxx·Syy − Sxy² ≥ 0`),整幅棋盘图上实测 min λ2 = 0(理想阶梯边缘恰秩 1);这条性质决定 **Shi-Tomasi 的 `R = min(λ1,λ2)` 永远不会为负、不能用「R > 0」当判据**、必须显式给阈值(λ=(100,1) 的强边缘 Harris 给 **−308.04** 而 λmin 给 **1.0**);③响应随对比度的 **4 次方**缩放(图像乘 c → Ix 乘 c → M 乘 c² → det 与 tr² 都乘 c⁴),实测 c=2 时比值**恰为 16.000000000000**,故阈值必须**相对 max 取**:固定阈值 `1.2×原图 max` 在原图检出 **0** 个、对比度 ×2 的图检出 **36** 个,改 `0.01×max` 相对阈值后两图给出**同一个角点集合**;④Harris **尺度不敏感**(窗口是固定像素尺寸,同一图案放大 4× 后同 blockSize 下 argmax 从 (8,8) 移到 (42,33)、簇数由 1 变 3)但**旋转不变来自特征值在正交变换下的不变性(精确)**,自检在 1 px 宽 L 角上验证 90° 旋转后逐像素差为 0 —— 两个性质不可混为一谈,要尺度不变须往上走 SIFT 尺度空间(见本类目 344)。最大坑:**理想角点周围是一整块「响应平台」而非单点** —— 33×33 理想棋盘角点在矩形窗下 R 在 4×4 = **16 个像素上完全相同**(实测 2.69746e10 重复 16 次),朴素严格比较 NMS 一个也删不掉(16 点全当角点输出)、换成 `>=` 又连带删掉真实峰;OpenCV 教程其实明写此事 ——「There may be a bunch of pixels at a corner, **we take their centroid**」并给出 `connectedComponentsWithStats` 取质心的做法,本 demo 的 `cluster_candidates` 实测把 16 个候选并成 1 个簇,**但质心是 (15.5, 15.5),比边界所在像素 (16,16) 偏半个像素,属「取质心」近似的系统偏差**。另三条工程要点:`cv.cornerHarris` 要求 src 先 `cvtColor` 转灰度再 `np.float32`(传 uint8 会因截断/溢出得到「看起来很弱」的响应,`0.01*max` 阈值随之失效);`ksize` **必须奇数**且 `blockSize`(窗口多大)与 `ksize`(梯度用多大孔径)是两个独立概念;教程落地点是 dst 先 `dilate` 再按 `0.01*dst.max()` 阈值化,高精度需求再上 `cornerSubPix` 亚像素精化。49 项 Python 断言全绿 | Python(221+211 行) + Go(277+148 行) | 2026-09-18 |
| 344 | `05-AI与机器学习/04-计算机视觉/SIFT尺度空间/` | SIFT 完整流水线(本机下载 Lowe IJCV 2004 全文按节精读 + OpenCV 4.x `cv::SIFT` 类参考 实读;纯标准库 Python + Go 双实现,自检 **62** 条):①**为什么必须是高斯** —— Koenderink(1984)与 Lindeberg(1994)证明要使尺度空间满足**因果性**(粗尺度不引入细尺度结构)且线性、平移不变,平滑核**只能**是高斯,故 `L(x,y,σ) = G(x,y,σ) * I(x,y)`;核截断到 **3σ**(约丢 0.3% 能量)、核长 `2·ceil(3σ)+1`(σ=1.6 时为 **11**)、核和归一化到 1。②**DoG 近似 LoG** —— 热方程给 `∂G/∂σ = σ∇²G`,对 σ 求导用差分替代得 `D = L(kσ) − L(σ) ≈ (k−1)σ²∇²G * I`,右端正是**尺度归一化** LoG(乘 σ² 才让不同尺度响应可比);关键在因子 `(k−1)` **与尺度无关**、只整体缩放幅度不改极值位置,故可不额外算 LoG 就做尺度检测;自检 B 段用解析高斯量化近似质量,k=1.02/1.10/1.26(=2^(1/3))/1.40 对应相关系数 **0.999878/0.997219/0.984315/0.968154**、幅度比 **0.9913/0.9558/0.8861/0.8362**,即 k→1 时两者**同时单调趋近 1**,论文取 s=3 得 k=2^(1/3)≈1.2599 仍有 0.984。③**数据结构:必须生成 s+3 张模糊图才能凑出 s+2 张 DoG** —— 每 octave 尺度序列 `σ,σk,…,σk^(s+2)`,由 `σ·k^s = 2σ` 推出张数关系;降采样取第 s 张(σ 恰为 2σ),隔点重采样后有效 σ 回到 σ,故下一 octave 精度不变而计算量降到 1/4;实测 σ0=1.6000→σ3=3.2000、k³=2、6 张模糊→5 张 DoG、尺寸 32→16→8→4。④**26 邻居极值** —— 同层 8 邻居 + 上下层各 9 邻居,这使**首尾两层天然不可用**(s 只能取 1..n−2),也解释了为何要多生成 3 张;负峰同样检出,平坦区无极值。⑤**亚像素定位与两级剔除** —— 二阶 Taylor 展开并令导数为零:`x̂ = −H⁻¹(∂D/∂x)`、`D(x̂) = D + ½(∂D/∂x)ᵀx̂`;**偏移任一维 > 0.5 时真极值更靠近相邻采样点、须换点重做插值**(判据是 **strictly > 0.5**,恰为 0.5 时不换点);`\|D(x̂)\| < 0.03` 低对比度丢弃(像素值域 [0,1]);实测真偏移 (0.3,0.2,0.2) 恢复为 (0.2862,0.1897,0.1897)、最大误差 0.0138 < 0.03,幅度 0.02 剔除 0.05 保留。边缘剔除用 2×2 Hessian,令 α、β 为主曲率、r = α/β,则 `Tr(H)²/Det(H) = (r+1)²/r` **只依赖 r、无需显式求特征值**,该式在 r=1 取最小 4 并随 r 增大,论文取 r=10 → 阈值 **12.1**、只有 < 12.1 才保留;实测对称隆起恰 4.000000000000、细长隆起 σy=6 得 **16.516** 剔除而 σy=4 得 **8.631** 保留,`Det ≤ 0`(曲率异号)直接剔除。⑥**主方向 36 bin + 80% 判据** —— 绕关键点统计梯度方向直方图(每 bin 10°、样本按**梯度幅值加权**、再乘 σ = 1.5×关键点尺度的圆形高斯窗),取最高峰,**达到最高峰 80% 的局部峰各自再生成一个关键点**(同位置同尺度不同方向,论文实测约 **15%** 的点被赋多方向但显著提升匹配稳定性),最后对峰附近 3 个直方图值**拟合抛物线**做亚 bin 插值(自检证实可**精确**还原真抛物线 0.1 个 bin 的偏移);实测 30° 落 bin 3、次峰 85% → 2 个关键点而 79% → 1 个。⑦**128 维描述子与光照不变** —— 16×16 采样按主方向旋转对齐、分 4×4 子块、每块 8 方向 bin 得 4×4×8 = **128** 维,子块内**三线性插值**(每维权重 1−d)避免边界突变,窗口高斯权重 σ 取窗口宽度**一半**以压低受配准误差影响最大的外围梯度;光照不变是**两步**:先**单位化**抵消对比度线性缩放(亮度平移本就不影响梯度),再把每分量**截到 0.2** 后**重新单位化**抑制非线性光照/饱和 —— 语义常被误解之处在于**截断发生在最终归一化之前**故「每分量 ≤ 0.2」**不是**描述子的最终性质,自检 H 段用两根非零分量的极限例子钉住:截断前两分量比 1.111111 被抹平为 1.000000、而重归一化后最大分量达 **0.7071**。⑧**匹配用最近邻/次近邻距离比** `d₁/d₂ > 0.8` 拒绝(论文称可剔约 **90%** 错误匹配而只丢不到 **5%** 正确匹配);自检 G 段另钉不变性:整体幅值 ×7 后描述子最大差异 **1.11e-16**、位置+梯度+关键点角同时旋转 90° 后取值集合最大差异 **2.78e-17**。参数口径坑(README 专门一节):论文 σ=1.6 / contrastThreshold=0.03 / r=10 与 OpenCV 默认值**不能直接对齐** —— OpenCV `SIFT::create` 默认 `contrastThreshold=0.04` 且文档明写它在应用时**会除以 nOctaveLayers**,要等价论文的 0.03 必须传 **0.09**;`edgeThreshold` 方向是**反的**(越大留下特征越多,它是 r 的上界而非剔除比例);octave 数论文经验值 4 而 OpenCV **按分辨率自动决定**(小图硬写 4 会让末层只剩几像素);别把 DoG 当 LoG 用(k=2^(1/3) 时约 11% 系统性幅度偏差,下游需绝对幅度须自己补 `(k−1)`)。复杂度:金字塔构建总卷积量 ≈ `N²(1+1/4+1/16+…)·(s+3) ≈ 8N²`(s=3)**与原图同阶**,可分离卷积使每张图两趟一维卷积各 `O(N²·(6σ+1))`;极值检测 `4/3·N²·(s+2)` 量级;内存驻留 ≈ `(s+3)·4/3·N²` 个浮点。适用边界:模糊输入(σ 从「假设已 σ=0.5 模糊」往上叠,输入本身已糊则细节层被吃掉)、重复纹理(128 维描述子大量近邻并列,比率判据全拒——是特性非 bug)、大视角变化(约 < 60° 稳健,超过需 ASIFT 类扩展) | Python(234+139+230 行) + Go(212+74+71+235+130+138+221 行,同 main 包 7 文件) | 2026-09-18 |
| 345 | `05-AI与机器学习/04-计算机视觉/YOLO检测头/` | Anchor-based 检测头(YOLOv3 arXiv:1804.02767 §2.1 + YOLO9000 arXiv:1612.08242 §2.2 Dimension Clusters 实读;纯标准库 Python + Go 双实现,自检 **45** 条):①**头部输出布局** —— 某尺度特征图 S×S、每格 B 个锚框时张量为 `S×S×B×(4+1+C)`,COCO 下 C=80、B=3,13×13 层即 **13×13×3×85 = 43095** 个数;85 维里真正与「框在哪」有关的只有 4 维,这是单阶段检测器分类与定位耦合、正负样本极度不均的根源。②**中心用 sigmoid 把预测绑在格子里** —— 论文式 `bx = σ(tx) + cx`、`by = σ(ty) + cy`;**σ 把偏移压进 (0,1)**,故无论 tx 多大预测中心都落在**该格内部**(自检用 tx=±20 验证);收益是训练稳定(偏移不会跑出格子、无需额外归一化)与尺度无关(同一 tx 在不同尺度层都表示「格内相对位置」)。**代价是量化误差**:中心只能落格内、最大误差为半个 stride,输入 416 时三尺度 stride 32/16/8 对应 **16/8/4 px**;真实中心 (191,245) 在 13×13 层被量化到格 (5,7) 中心 (176,240)、误差 (15,5) —— 一个 16 px 误差预算对小目标等于灾难,**这正是必须多尺度预测的原因(不是「顺便涨点」,而是细目标在粗层根本无法定位)**。③**尺寸用指数缩放锚框** —— `bw = pw·e^tw`,`tw=0` 时 `bw = pw`、`tw=1` 时放大 **e≈2.718** 倍(实测 116 → 315.320692);取指数而非线性是因为框尺寸是**乘性**的量,让「放大一点」在不同尺度上含义一致;编码是解码逆运算 `tw = ln(bw/pw)`、中心部分 `tx = logit(cx/stride − gx)`,encode→decode 往返误差 < 1e-9。④**多尺度预测数** —— 13²+26²+52² 三层合 `3·3549 = 10647` 个框,只留 13×13 层则 507 个、**相差 21 倍**;粗层感受野大负责大目标,细层负责小目标,层间通过上采样+通道拼接把语义传下去。⑤**正/负/忽略三态分配** —— 论文两句规则:(a)与 GT 的 **IoU 最大**的锚框负责该目标(正样本),(b)**非最优但 IoU 超过 0.5** 的锚框**被忽略**(既不奖励也不惩罚);自检用 GT 形状 (100,100) 与锚框 (100,100)/(80,90)/(50,200)(形状 IoU 1.0/0.72/0.3333)钉住三态:负责 = 0、忽略 = {1}、负样本 = {2};判据是 **strictly > 0.5**,形状 (10,10) 与 (10,20) 的 IoU 恰为 0.5 **不**进忽略集,写 `>=` 就会在边界分错样本(自检专门有这一条);注意论文/Darknet 里这条规则比较的是**网络预测框**与 GT,本 demo 无权重退化为比较**锚框形状**(判据形式相同、比较对象不同,README 与代码注释均标注)。⑥**为什么丢弃 softmax** —— softmax 蕴含类别互斥(Σp=1)但真实标签是**重叠**的(一张图里的人既属 Person 也属 Woman),YOLOv3 因此换成**每类各一个 logistic 分类器**;同一组 logits `[8,6]` 实测:softmax 给 (0.880797, 0.119203) 和为 1、第二名被压到 0.119,独立 logistic 给 (0.999665, 0.997527) 两类都接近 1;用交叉熵衡量更直观 —— 同一 logits、同一目标类 1 时 softmax 交叉熵 **2.126928**(把第二名判成错)而独立 logistic 的 BCE 只有 **0.002811**。⑦**k-means 选锚框 `d = 1 − IOU`** —— 不用欧氏距离的理由:同样差 10 px,对大框只是噪声、对小框是灾难,欧氏距离把两者等同看待会使聚类偏向大框,而 `1 − IOU` 天然是「形状相似度」;合成数据三个已知尺寸簇 (40,40)/(120,60)/(200,240) 各 100 框 ±12% 抖动,实测 avg IOU:k=1 **0.3021**、k=2 **0.6181**、k=3 **0.8889**、k=5 0.9067、k=9 0.9308、**oracle(直接用三真尺寸)0.8879**、RPN 手工 9 个(3 尺度×3 宽高比)仅 **0.1354**;三点结论:avg IOU 随 k **单调不减**(k-means 目标函数性质);**k=3 略微超过 oracle** —— 质心是样本均值、比「真值尺寸」更贴合这批抖动样本,所谓 oracle 只是另一个锚框集合并非上界;**手工锚框惨败**因其最小锚框也有 90.5 px 而分布里有 40 px 簇 —— 「锚框必须按数据集分布定」的实证。论文报告的 k=5 得 avg IOU 61.0 与 Faster R-CNN 手工 9 锚框 60.9 相当,本 demo **不下载 VOC 2007 故不复现这两个数值**,只在合成数据上复现结论形式(README 注明)。⑧**损失与「忽略」的必要性** —— objectness 用 BCE、类别用**逐类独立** BCE;同一次预测中忽略那个「自信但 IoU 没到最优」的锚框时损失 **0.210721**,把它当负样本惩罚则暴涨到 **27.8417(放大 132 倍)**(p=0 而 y=1 时 BCE 出现 `−ln(1e-12) ≈ 27.63`),一次这样的样本足以主导整个 batch 梯度 —— 故忽略规则**不是精细化操作而是数值稳定性要求**。零权重下可复现的坑:①**σ 在 float64 下会精确饱和** —— σ(36) 仍 < 1 而 **σ(37) 就是精确的 1.0**(`1 + e^-37` 已落入 1.0 舍入区间),此时中心落在格边界属退化解;负向**不对称**,σ(−37) = 8.53e-17 仍是可表示的数,要到指数下溢(x < −745)才归零 —— 推论:不要假设 σ 输出严格小于 1,也不要假设正负两侧饱和点对称;②每格 3 个锚框 **≠** 3 个正样本(只有 IoU 最大那 1 个负责,其余要么忽略要么为负),正负比极端失衡是单阶段检测器固有特征、需 focal loss 类手段;③`tw`/`th` 是**相对锚框的对数比**不是绝对尺寸,换锚框尺寸就必须重算编码目标 | Python(228+193 行) + Go(221+114+284 行,同 main 包 3 文件) | 2026-09-18 |
| 346 | `05-AI与机器学习/04-计算机视觉/ViT图像分块/` | Patch Embedding 与位置编码(Dosovitskiy 等《An Image is Worth 16x16 Words》arXiv:2010.11929 实读,§3.1 与式 (1)(2)(3)(4) 及 Table 1;纯标准库 Python + Go 双实现,自检 **33** 条):①**分块 `N = H·W / P²`** —— 224×224 配 P=16 得 **14×14 即 N = 196**,每 patch 展平长度 `P²·C = 256·3 = 768` 故线性投影输入维度就是 768;**展平顺序是 `(patch_y, patch_x, channel)` —— 顺序搞错不会报错,只会悄悄训不动**(跨实现迁移权重最易在此出错)。**非整除要小心**:225×225 配 P=16 仍只有 14 列、覆盖 `16 + 13·16 = 224 px`,**最右一列像素被静默丢弃**;步长同理,stride=8(< patch,重叠)使 token 数涨到 **729**、stride=20(> patch)只剩 **121** 个 token 且丢 8 px,**只有 stride = P 才是不重叠且不丢像素的选择**;论文另允许 ViT 直接吃 CNN 特征图 —— ResNet50 在 224 输入下输出 14×14×1024,按 P=1 分块同样得 **196 个 token**(维度 1024),与像素级 P=16 的 token 数天然对齐。②**patch embedding 就是 `stride = patch` 的卷积** —— 论文说的「可训练线性投影」与 `Conv2d(in=3, out=D, kernel_size=P, stride=P)` **完全等价**,只是权重内存排布不同(线性层索引 `(dy·P + dx)·C + c`,卷积核 `[C][P][P]`);自检把同一组权重按两种排布各算一遍得输出**逐元素一致**(最大差异 **2.49e-14**,纯浮点重排)—— 这条等价性使「用卷积实现 patch embedding」成为合法工程优化,也解释了它只需 **589,824 = P²·C·D = 256·3·768** 个参数(无 bias)。③**`[class]` token** —— 论文在 patch 序列**前面**拼一个可学习向量,序列长度变 **N+1 = 197**,分类时只取它的输出 `y = LN(z_L^0)`;它不是卷积式的全局池化,而是**让注意力自己去汇总** —— 自检证实只改动第 4 个 patch 的某个分量,`[class]` 输出就变了(变化 **0.217638**),即**第一层就具备全局感受野**。④**位置编码是置换等变的破除者** —— Transformer 自注意力对 token 顺序是**置换等变**的(重排输入则输出只是同样重排);自检用 6-token、D=4 的确定性小模型验证:不加位置编码时置换前后输出差异 **0.00e+00**(精确为零),加上位置编码后同样重排带来 **0.640767** 的差异 —— 这就是位置编码存在的意义,反过来说 ViT 的**全部**空间信息都压在这 197×768 = **151,296** 个可学习参数里;论文试过 2D-aware 位置编码但「未观察到性能增益」,一个可量化解释是:行优先展平后 1D 下标相邻的 token 中有 **182/195 = 93.33%** 同时是空间**水平**相邻,而空间**垂直**相邻的 token 在 1D 序列里的距离**恒为 W/P = 14** —— 即 1D 顺序已隐含编码绝大部分二维结构,额外的 2D 设计没有多少剩余空间可补。⑤**Pre-LN 编码器** —— 论文式 (2)(3) 为 `z' = MSA(LN(z_{ℓ−1})) + z_{ℓ−1}`、`z_ℓ = MLP(LN(z')) + z'`,**LN 在子层内部(归一化的是子层输入)、残差加的是子层输出而非归一化结果**;自检用「把子层输出置零」验证 —— 把 V 投影置零后 MSA 输出恒为 0、再把 MLP 输出置零则整个块**精确退化为恒等映射**(差异 0.00e+00),说明残差是对 LN 之后的子层输出做加法而 LN 不改变恒等性;同段另钉基础量:LayerNorm 输出均值 0、方差 **0.999992**(eps=1e-5 使其略小于 1,不是精确的 1)、GELU(1) = **0.841192**(tanh 近似)、注意力权重按行 softmax 和为 1 且对整体平移不变。⑥**缺少归纳偏置:平移行为的定量** —— 论文自述代价是「Transformers lack ... translation equivariance and locality」,可在分块层面直接量化:平移 **16 px(= P)** 时序列只是位置重排、内容**逐元素相同**(13/14 的列完全对齐);平移 **1 px** 时**全部 196 个 patch** 的内容都变了(patch 0 差异 0.909091)—— 分块本身只在「整 patch」这一特殊位移下等变,真实世界的连续位移会改变**每一个** token,这是 ViT 需要大规模预训练(或用 CNN 特征混合输入)的直接原因;另一角度是感受野 —— 覆盖 224 px 需 **112 层** 3×3 卷积(感受野 = 1 + 2L)而 ViT 只需 **1 层**全局注意力,代价是注意力的 N² 项。⑦**参数与算力构成** —— 论文 Table 1 给 ViT-Base **86M**,自检逐项拆开:patch embedding **589,824** + 位置编码 **151,296** + 12 层编码器(注意力 `4D²+4D`、MLP `8D²+5D`、两个 LN)**85,054,464** + 末尾 LN 与 1000 类单层头 770,536 = **86,566,120 ≈ 86.57M**(与论文 86M 相对误差 **0.66%**),可见**参数 98.3% 在编码器里**而「图像分块」本身只花 0.59M;算力要盯住 `N²`:单头一层注意力 QKᵀ 是 `N²D = 196²·768 = 29,503,488` 次乘加,**P 从 16 减半到 8 会使 token 数变 4 倍(784)、注意力矩阵面积变 16 倍**(38,416 → 614,656)—— P 的选择本质是「细粒度 vs 二次代价」的取舍。边界:小数据下不如 CNN(论文核心警告)、极高分辨率下注意力代价爆炸、细粒度任务(分割/检测)需更小 P 或特征金字塔;分辨率微调时必须对位置编码做插值否则序列长度对不上 | Python(300+179 行) + Go(239+200+300 行,同 main 包 3 文件) | 2026-09-18 |
| 347 | `06-DevOps/03-监控与可观测性/PromQL范围向量与rate外推/` | PromQL 即时向量与范围向量（prometheus.io querying/basics + functions 实读）：范围向量是 `[5m]` 后缀的产物且**左开右闭**（官方原文 samples coinciding with the left boundary are excluded），故 t=0 的样本被排除；lookback 默认 5m 且判据是**严格小于**（恰好 300s 不返回）；`rate`/`increase` 的外推阈值 `extrapolationThreshold = avgDurationBetweenSamples * 1.1`，任一端距边界超阈值即降级为「最多外推半个平均间隔」—— 实测同一 0.1/s 计数器末样本距右边界 60s 得 0.1000、距 120s 得 0.0700（**低估 30%**）；计数器重置补偿 5,10,2,7 得 raw=(7-5)+10=12 而不补偿只有 2（**差 6 倍**）；零点截断避免把结果外推到负值区；`increase` 是 `rate × 窗口秒数` 的语法糖故记录规则应写 rate；`offset -1w` 会看向未来；选择器必须给出指标名或至少一个不匹配空值的 matcher（故 `{job=~".*"}` 与 `{job!="x"}` 非法而 `{job=~".+"}`、`{job!=""}` 合法）；**坑：外推「超阈值降级」与「零点截断」的先后顺序官方文档未规定**，两个分支结论不同（0.0888888889 vs 0.0944444444，差 6.25%），demo 用 order 参数同时实现两条路径并断言二者确实不同 | C(1 文件 268 行) + Python(4 文件 827 行，51 断言全实跑) + Go(6 文件 1138 行) | 2026-09-18 |
| 348 | `06-DevOps/03-监控与可观测性/TSDB存储与Head块/` | Prometheus TSDB 磁盘布局、崩溃恢复与容量公式（prometheus.io storage 文档实读）：2h **不可变块**（block）一旦写完不再修改，含 `chunks/` + `index` + `meta.json`；`wal/` 每段 128 MB 顺序日志且**至少保留 3 段**；Head 块是当前正在写入的常驻内存块，靠 WAL 重放保证崩溃可恢复；压缩（compaction）把多个 2h 块合并为更大块，tombstone 删除只写墓碑、物理回收靠压缩；容量公式 `needed_disk_space = retention_time_seconds × ingested_samples_per_second × bytes_per_sample`，每样本平均仅 1~2 字节（15d × 10 万样本/s → 129.6 / 194.4 / 259.2 GB 三档）；官方经验「减少序列数比拉长抓取间隔更有效」并被量化：两条路线都让样本总量减半，但序列减半还额外省下一半固定开销（索引项 + chunk 头），1000 序列 × 256 B 开销场景下差额恰为 128,000 字节，且若压缩率随序列内样本数下降（1.5 → 1.6 B）间隔翻倍还要多花 4.32 MB；远程写对照：非 POSIX 文件系统（NFS/EFS）不受支持、备份推荐 snapshot 且须排除 `wal/`、`chunks_head/`、`wbl/`；**坑：Head 常驻内存规模由样本基数决定，只看磁盘占用会误判** | C(1 文件 253 行) + Python(2 文件 503 行，58 断言全实跑) + Go(2 文件 399 行) | 2026-09-18 |
| 349 | `06-DevOps/03-监控与可观测性/OpenTelemetryCollector管线/` | OpenTelemetry Collector 的配置装配语义与数据通路语义（opentelemetry.io 文档实读）：配置 = **六段组件表**（receivers / processors / exporters / connectors / extensions / service）+ service 段；组件 ID 是 `type[/name]` 复合键；装配期做 pipeline 引用校验与 receiver/exporter **两侧 fanout**；connector 连接两条 pipeline 故必须做**依赖图环检测**；`memory_limiter` 的**软/硬限三态**且「**顺序即语义**」（必须排在批处理之前才能按住峰值）；`batch` 的两个触发条件（条数 / 超时）加一个切分上限；exporter helper 的发送队列与退避重试、队满即背压；**坑：processor 的执行顺序直接改变内存峰值与限流行为，不是装饰性的排列** | C(3 文件 520 行) + Python(5 文件 964 行，108 断言全实跑) + Go(7 文件 1193 行) | 2026-09-18 |
| 350 | `06-DevOps/03-监控与可观测性/Alertmanager分组抑制与静默/` | Alertmanager 匹配与路由语义 + 通知节奏（prometheus.io alerting/configuration 与 alerting_rules 实读）：**matcher 没有 IN 操作符**，多值只能写 `service=~"mysql\|cassandra"`，写成逗号分隔会静默失配且配置本身不报错；`=~` 完全锚定故 `team="payments"` 不匹配 `team="payments-eu"`；**缺失标签等价空字符串**故 `env!="prod"` 反而命中没有 env 的告警；路由树「命中即停」、`continue` 默认 false 是「一条告警同时进多个 receiver」的唯一手段，误删后表现为「不再重复打扰」而另一路静默失联；`group_by` 空列表是**全部聚成一组**而非「不聚合」；三时间参数：新组首见 → group_wait(30s) → 首条通知，已有组每 group_interval(5m) 检查一次，无变化则等 repeat_interval(4h)；官方两条易漏细节 —— `group_wait` 结束前已 resolved 则**不发通知**（天然抑制抖动的原理），且 `repeat_interval` 应为 `group_interval` 的倍数否则向上取整；`inhibit_rules` 的 `equal` 语义是**两边都缺该标签也算相等**，把 instance 写进 equal 后无 instance 的聚合告警会互相抑制；静默窗口**左闭右开**；Prometheus 侧状态机 inactive→pending→firing，任一评估为 false 即重置 `for` 计时器（抖动指标永远停在 pending），`keep_firing_for` 默认 0；用 `time() - ALERTS_FOR_STATE` 量真实持续时长 | Python(4 文件 690 行，109 断言全实跑) + Go(5 文件 877 行) | 2026-09-18 |
| 351 | `06-DevOps/03-监控与可观测性/Loki日志存储与LogQL/` | Loki 日志存储与 LogQL 语义（grafana.com 的 Loki overview / components / configuration / enterprise-logs schema / troubleshoot-ingest / bp-configure / query_reference 共 7 页实读）：核心取舍是**只索引标签、不索引正文**，故正文检索是「命中流内线性扫描」而非倒排检索，索引体积只与标签基数相关而与日志量几乎无关；写路径 distributor（校验限额 + 算哈希 + 按副本因子分发，无状态）→ ingester（内存攒 chunk + 刷对象存储，有状态，靠 WAL 崩溃重放），读路径 query-frontend → querier → index-gateway，compactor 单实例；同流时序三规则 —— ts 回退拒行回错 / **ts 与内容都相同则静默忽略**（不计错不计数） / **ts 相同但内容不同被接受**（同纳秒多行合法，不是去重）；chunk 滚动三条件 idle 30m（自**最后一次写入**起算）/ target_size 1572864（按**压缩后**计，官方注释为 1.5 MB 即 1024 进制）/ max_chunk_age 2h，`chunk_encoding` **默认 gzip 而最佳实践推荐 snappy**；**LogQL 同一个 `~` 在两层语义相反** —— 流选择器 `=~`/`!~` 完全锚定、行过滤 `\|~`/`!~` 是搜索语义非锚定；缺失标签 ≡ 空串故 `{env!="prod"}` 与 `{env!~".+"}` 都命中没有 env 的流；解析失败**不丢行**只打 `__error__`（多错误逗号累加），但**指标查询携带错误会整条失败**，故 unwrap 后必须补 `\| __error__ = ""`；**unwrap 成功必须删掉被消费的标签**，否则每个取值裂成独立序列、每序列只剩 1 个样本致 avg/stddev/quantile 退化为恒等运算；日志范围向量（rate / count_over_time / bytes_*）与 unwrap 数值范围向量（sum / avg / stddev / quantile_over_time）函数集不可互换，`count_over_time` 不接受 unwrap 后的数值；stddev/stdvar 用**总体**方差（除 N），quantile_over_time 用**线性插值** rank=φ·(N−1) 故 `[10,20,30,40]` 的 0.5 分位是 **25**；global 策略下 rate 按**健康实例数均摊**而 burst **不均摊** → 扩容降低每实例阈值却不动 burst，即「滚动重启期间 429 与限流同现」的成因；limits 默认值（4 / 6 / global / 30 / 5000 / 256KB / 1024 / 2048 / 5000 / 500）与最佳实践推荐值（标签数 15、snappy）常不一致；tsdb + v13 强制否则**拒绝启动**并抛 CONFIG ERROR、boltdb-shipper 将在 4.0 移除、tsdb 的 index.period 必须 24h、row_shards 自 v10 默认 16、schema 段 `from` 追加在未来而全新安装在过去 | C(5 文件 1039 行，c_sanity --tu 通过) + Python(15 文件 2598 行，209 断言全实跑) + Go(9 文件 1542 行，go_sanity + go_crossref 通过) | 2026-09-18 |
| 352 | `08-安全/02-网络安全/WireGuard握手/` | WireGuard 握手（Noise_IKpsk2；内核 `messages.h` 布局与 TAI64N 口径实读；纯标准库 Python + C(OpenSSL EVP) + Go(`crypto/ecdh`)，自检 **65** 条）：①**WireGuard 与纯 Noise 的第一处差异在初始化** —— Noise 用 `h = HASH(protocol_name)`，WireGuard 改 `ck0 = BLAKE2s("Noise_IKpsk2_25519_ChaChaPoly_BLAKE2s")`（37 字节、无结尾 NUL）、`h0 = BLAKE2s(ck0 ‖ "WireGuard v1 zx2c4 Jason@zx2c4.com")`（34 字节），多出的标识串把「具体实现」也绑进转录哈希，避免同名构造串的不同实现互通；随后 IK 的 pre-message `<- s` 执行 `mix_hash(rs)`（发起方用响应方静态公钥，响应方用自己静态公钥）。②**三状态 `ck/h/k`** —— chaining key 与 handshake hash（转录哈希，**同时充当 AEAD 的 AD**）与 AEAD 密钥；每个 token 都按「MixHash(pub) 再 MixKey(DH)」两步走。③**消息 1 = `e, es, s, ss`** —— 内核 `sizeof` 实测 **148 字节**：`type(4, LE32 低字节为类型、高 3 字节保留零) + sender_index(4) + unencrypted_ephemeral(32) + encrypted_static(48=32+16) + encrypted_timestamp(28=12+16) + mac1(16) + mac2(16)`；`es = DH(e, rs)` 提供响应方认证并抗 KCI，`ss = DH(s, rs)` 在内核用 `precomputed_static_static` 长期缓存以省一次 DH。④**消息 2 = `e, ee, se, psk`** —— `ee` 用双方临时公钥、`se` 用**响应方临时私钥 × 发起方静态公钥**，随后 `MixKeyAndHash(psk)` 同时把 PSK 混进 ck、h 与 k（这正是 `psk2` 的定义位置、落在第二条消息末尾），最后加密**空负载**、其 16 字节 tag 即响应方的密钥确认（`sizeof` = **92 字节**）。⑤**传输阶段与 Split** —— `c1, c2 = Split() = HKDF(ck, 空输入, 2)` 分出发起→响应与响应→发起两个方向；数据消息 `type(4) + key_idx(4) + counter(8) + AEAD`（AD 为空，明文补到 16 字节整数倍）；AEAD nonce 固定为 **4 字节零 ‖ 8 字节小端计数器**，故同一密钥下 nonce 天然不重复，计数器同时驱动 **8192 位滑动窗口**防重放（冗余 64 位以避免移位越界）。⑥**TAI64N 与重放** —— 12 字节（8 字节秒 + 4 字节纳秒）抗握手重放；内核把纳秒**向下对齐到 2^24**，故同一秒内两次取样可能得到相同时间戳 —— 重放判据必须是「不小于已见」，写成「严格大于」会误判。⑦**防 DoS：mac1/mac2 与 cookie** —— `mac1_key = BLAKE2s("mac1----" ‖ 响应方静态公钥)`（32 字节、**未键控**）、`MAC1 = keyed-BLAKE2s(mac1_key, MAC1 之前的全部字节, 16 字节)` 使服务器不做任何 DH 就能拒掉乱扫；负载压力下改回 cookie：`cookie = keyed-BLAKE2s(secret, 源 IP ‖ 源端口, 16)`（secret 每 **120 秒**轮换）、`MAC2 = keyed-BLAKE2s(cookie, MAC2 之前的全部字节, 16)`；cookie 绑的是**源地址**而非消息内容，故无法被搬到别处重放；cookie 本身经 **XChaCha20-Poly1305**（24 字节随机 nonce、密钥 `BLAKE2s("cookie--" ‖ 响应方静态公钥)`、AD 取该消息的 MAC1）回传，只有能算出该 MAC1 的对端才解得开。⑧**五个实跑暴露的坑** —— 两个「静态公钥」别搞混（`se` 用**发起方**静态公钥，响应方解出 `encrypted_static` 才拿到）；PSK 模式里**每个 `e` token 都要额外 `MixKey(e.pub)`**（Noise §9.2），漏掉会使两侧 ck 分叉；HMAC 输入长度必须显式设限，C 版曾写「超长则截断」这类静默截断会让 MAC 在旧实现上意外通过；BLAKE2s 的 keyed 模式**不是**「先哈希密钥再哈希消息」而是把密钥当一个满块喂进去；**XChaCha20 不是「ChaCha20 换个 nonce 长度」** —— 前 16 字节必须经 HChaCha20 派生**子密钥**，直接拿 24 字节 nonce 当输入会得到不同结果 | Python(6 文件，65 断言实跑全绿) + C(1 c + 2 h) + Go(4 文件) | 2026-09-18 |
| 353 | `08-安全/02-网络安全/QUIC包头保护/` | QUIC 头保护与包保护（RFC 9001 §5.4 + RFC 9000 §16/§17.1 + 附录 A.2/A.3，全部用 RFC 原文实例做断言；纯标准库 Python + C + Go，自检 **91** 条）：①**为什么包头也要保护** —— TLS 1.2/1.3 只加密记录层负载、记录头（长度、类型）明文，攻击者可据此做流量分析（按长度指纹识别网页）；QUIC 把**包号与首字节的部分位**也纳入加密范围，代价是接收端解密前必须先知道「包号字段有多长」，而包号长度位本身又被加密了 —— 这个循环只能靠**与头部内容无关的采样规则**打破。②**变长整数（RFC 9000 §16）** —— 2 位前缀决定宽度 `00`→1 字节(6 位值)、`01`→2 字节(14 位)、`10`→4 字节(30 位)、`11`→8 字节(62 位)；**除 Frame Type 外规范不要求最短编码**，`0x40 0x3f` 是合法的 63，解码器不能因为「有更短的编码」就拒绝它（在这里加规范性检查是初学者常犯的错）。③**头部布局与 pn_offset 不固定** —— 长头 `首字节(1) + Version(4) + DCID Len(1) + DCID + SCID Len(1) + SCID + Token Length(i) + Token + Length(i) + 包号 + 负载`，短头 `首字节(1) + DCID + 包号 + 负载`（无长度字段）；**Length 与 Token Length 本身是 varint，它们占几个字节也参与偏移计算**，所以「包号固定在某个偏移」的假设是错的 —— 本 demo 一律按规范顺序逐段解码并与公式 `pn_offset_initial()` 交叉验证。④**头保护两步** —— `mask = header_protection(hp_key, sample)`（**5 字节**）；`packet[0] ^= mask[0] & (长头 ? 0x0f : 0x1f)`（长头只掩低 4 位以保住类型位与固定位，短头掩低 5 位以保住固定位与密钥相位）；`packet[pn_offset .. pn_offset+pn_length) ^= mask[1 .. 1+pn_length)`；**采样固定 `sample_offset = pn_offset + 4`、恒取 16 字节**，一律按包号最长为 4 字节预留，故采样位置与被保护的「包号长度」位无关（包号只有 1 字节时会跳过 3 字节负载）；**长度不足必须整包丢弃**（规范用 MUST），否则会把首字节解成随机包号长度；两套掩码算法：AES 套件 `mask = AES-ECB(hp_key, sample)`，ChaCha20 套件把样本前 4 字节当块计数器（小端）、后 12 字节当 nonce、用 ChaCha20 加密 **5 个零字节**；**负载补齐**要求「包号 + 受保护负载 ≥ 4 + 16」，故 1 字节包号需 3 字节帧、2 字节包号需 2 字节 —— 这正是 RFC 9001 附录示例里长度写成 1182(`0x049E`) 而非 1162 的原因，把示例的选择当成算法要求会立刻失败。⑤**包号编解码** —— 发送端只需「足以表示未确认区间两倍以上」的字节数：`num_unacked = largest_acked is None ? full_pn + 1 : full_pn - largest_acked`、`min_bits = ceil(log2(num_unacked) + 1)`（注意是**实数 log**）、`num_bytes = ceil(min_bits / 8)`；接收端按**半窗口**还原（附录 A.3）：以「最大已处理包号 + 1」为期望值把截断值拼进去，候选低于期望值半窗则加一个窗口、高于则减一个窗口，这样在丢包与乱序下仍能唯一还原。⑥**密钥派生与密钥更新** —— 沿用 TLS 1.3 的 `HKDF-Expand-Label`（标签必须带 `tls13 ` 前缀）派生 `quic key`（AEAD 密钥）/`quic iv`（nonce 基值）/`quic hp`（头保护密钥）三个值实现密钥分离，**nonce = IV 异或左填零的包号**；Initial 级别例外：由固定盐 `38762cf7…7f0a` 加「客户端首个 Initial 包的 DCID」派生；密钥更新只换 `quic ku` 标签推导新 secret，**头保护密钥随之重新派生**，所以更新后旧包既解不开负载也去不掉头保护 —— 解密顺序「先解头保护拿到包号、再解包」不可颠倒 | Python(4 文件，91 断言实跑全绿) + C(1 c + 3 h) + Go(5 文件) | 2026-09-18 |
| 354 | `08-安全/02-网络安全/RPKI前缀源验证/` | RPKI 前缀源验证（RFC 6482 §3/§3.3 + 6488 + 6811 §2 + 8205 + 3779 实读；纯标准库 Python 手写 DER + C + Go，自检 **84** 条）：①**要防的问题** —— 前缀劫持（别家 AS 宣告不属于自己的前缀把流量吸走）、子前缀劫持（宣告更具体的 `203.0.113.0/25` 抢走 `/24` 的部分流量，BGP 最长匹配让它胜出，**这正是 maxLength 存在的理由**）、伪造源（AS_PATH 写成自己不在其中的路径）；**RPKI 只能回答「源头是否被授权」、不验证整条路径**，路径验证是 BGPsec(RFC 8205) 的范围。②**四层规范分工** —— 资源证书 RFC 3779（X.509 v3 扩展，把 IP 块/AS 号绑到证书主体）、签署对象容器 RFC 6488（RPKI 对象的通用 CMS 封装模板）、ROA RFC 6482（「AS + 前缀列表 + 最大长度」的授权声明）、RTR RFC 6810（把 VRP 推给路由器）、验证算法 RFC 6811（路由器侧三态判定）。③**ROA 是 `DEFINITIONS EXPLICIT TAGS` 的 ASN.1** —— `RouteOriginAttestation ::= SEQUENCE { version [0] INTEGER DEFAULT 0, asID ASID, ipAddrBlocks SEQUENCE OF ROAIPAddressFamily }`、`ROAIPAddressFamily ::= SEQUENCE { addressFamily OCTET STRING (SIZE(2..3)), addresses SEQUENCE OF ROAIPAddress }`（`0001` = IPv4、`0002` = IPv6）、`ROAIPAddress ::= SEQUENCE { address IPAddress(BIT STRING), maxLength INTEGER OPTIONAL }`；本 demo 从零写 DER 编解码并用 RFC 6482 §3.3 原文例子（`203.0.113/24` + maxLength 26）逐字节对齐，手推 DER 字节作为断言期望值。④**maxLength 语义精确边界** —— maxLength 26 授权 `203.0.113/24`、`203.0.113.128/25`、`203.0.113.0/25` 以及 `/24` 内**任意** `/26`，但**不授权** `203.0.113.0/27`；**省略 maxLength 时只授权精确前缀那一条**；一条 ROA 里允许出现互相包含的前缀（如同时有 `203.0.113/24 maxLength 26` 与 `203.0.113.0/28 maxLength 28`），判定结果仍然确定。⑤**VRP 与三态验证（RFC 6811 §2）** —— 依赖方验签后把每个 `ROAIPAddress` 展开成一条 **VRP**`(前缀, 最大长度, 源 ASN)`；**Covered** = VRP 前缀不更长且其指定的所有位与路由前缀相同（路由要么相同、要么更具体）；**Matched** = Covered 且路由前缀长度 ≤ maxLength 且源 AS 相等；**NotFound** = 没有任何 VRP 覆盖该路由前缀；**Valid** = 至少一条 Matched；**Invalid** = 至少一条覆盖但无任何一条 Matched；规范伪代码即「遍历所有覆盖项、命中即返回 Valid，遍历完仍无命中但有覆盖则 Invalid」。⑥**两个永不可能 Matched 的边界** —— VRP 的 ASN 可以是 **0**（保留值），源 AS 可能是特殊值 **NONE**（末段是 AS_SET 之类时）；「Invalid」与「无 ROA(NotFound)」**必须分开处理**，运维上一者是「配置与授权不符」、后者是「尚未部署」，混成一个状态会掩盖真实故障 | Python(4 文件，84 断言实跑全绿) + C(1 c + 1 h) + Go(2 文件) | 2026-09-18 |
| 355 | `08-安全/02-网络安全/WPA3-SAE/` | WPA3 SAE / Dragonfly 认证密钥交换（RFC 7664 §2.2/§3.2.2/§3.3/§3.4 逐节对照 + RFC 3526 §3 的 2048-bit MODP Group 逐字符取 p 与 G=2 + RFC 5869 的 KDF-n 构造口径；C + Python + Go 三实现，断言 **Python 66 / C 73 / Go 68**）：①**密码元素 PE** —— 双方用口令各自算出同一个群元素再做 DH：`base = H(max(A,B) ‖ min(A,B) ‖ password ‖ counter)`、`seed = (KDF-(len(p)+64)(base, "Dragonfly Hunting And Pecking") mod (p-1)) + 1`、`temp = seed^((p-1)/q) mod p`；**身份必须先 max/min 排序**再拼接，否则 A 算 `H(bob‖alice‖…)`、B 算 `H(alice‖bob‖…)` 得不同 PE —— 这是 Dragonfly 能做成**对等**（无固定 client/server）的关键，也是抗离线字典的前提；`KDF-n` 里**多取 64 位**(`n = len(p) + 64`)再 mod (p-1) 是为降低模约减偏置。②**`k` 不是「最多试 k 次」而是「至少跑 k 轮」** —— 伪代码循环条件是 `while ((found == 0) \|\| (counter <= k))`，命中后仍继续空转以**掩盖真实迭代数**（抗时序侧信道）；在 2048 位安全素数群（`q = (p-1)/2`）里啄食指数 `(p-1)/q` 恰好等于 **2**，任何非零 seed 的平方都落在子群里，故**第一轮几乎必然命中**、`k` 纯粹是抗侧信道 —— 本 demo 把这条等式写成断言(`PE == seed^2 mod p`)。③**提交交换的代数证明** —— `scalar = (private + mask) mod q`、`Element = PE^(-mask) mod p`，代入即可验证 `Element · PE^scalar = PE^private`；交换后 `ss = (Peer-Element · PE^peer-scalar)^private = (PE^private')^private`，**结构对称故双方必得同一个值** —— 自检用「直接验证这个等式」而不是硬编码魔数；随后 `kck ‖ mk = KDF-(2·len(p))(ss, "Dragonfly Key Derivation")` 各取 `len(p)` 位。④**确认交换的顺序有规范原文** —— `confirm = H(kck ‖ scalar ‖ peer-scalar ‖ Element ‖ Peer-Element ‖ <sender-id>)`；RFC 原文 §3.4：“The order of the scalars and elements are: **scalars before elements**, and **sender's value before recipient's value**”，顺序写反两侧会各自算出不同 confirm 且只在联调时才暴露。⑤**为什么它比旧 4 次握手强** —— 旧 WPA2-PSK 的 4 次握手把口令的哈希当 PMK 直接参与，抓到一次握手就能**离线**穷举口令；SAE 是 PAKE，每次猜测都需**在线交互**，同时天然提供**前向保密**（PMK 由 `H(scalar,...,K)` 导出而非直接用 DH 结果），私有标量必须由**拒绝采样**从 `p-1` 的乘法子群取且双方 `r`/`s` 之和须非零 | C(1 c + 4 h，73 断言) + Python(3 文件，66 断言实跑全绿) + Go(3 文件，68 断言) | 2026-09-18 |
| 356 | `08-安全/02-网络安全/TLS-ECH/` | TLS 1.3 ECH / Encrypted ClientHello（RFC 9849 + HPKE RFC 9180 附录 A.2 官方测试向量 + RFC 8446 §7.1 + RFC 8439 + RFC 7748 实读；C(OpenSSL) + Python(纯标准库) + Go(标准库) 三实现，断言 **Python 81 / C 95 / Go 88**）：①**要解决的问题** —— 明文 SNI 让中间人知道你在访问哪个域名；**ECH 隐藏的是 SNI 而非目标 IP**（IP 仍暴露），故它只挡「按名字分类」这一类观测。②**HPKE 三条易错点** —— **两条 `suite_id` 长度不同**（`"KEM"‖kem_id` = 5 字节 vs `"HPKE"‖kem_id‖kdf_id‖aead_id` = 10 字节，DHKEM(X25519,HKDF-SHA256)=0x0020 / HKDF-SHA256=0x0001 / ChaCha20-Poly1305=0x0003）；`LabeledExtract(salt,label,ikm) = Extract(salt, "HPKE-v1"‖suite_id‖label‖ikm)` 而 `LabeledExpand` 把 `I2OSP(L,2)` 放在**最前**；key schedule 里 `psk_id_hash`/`info_hash` 的盐是空串、而 **`secret` 的盐是 `shared_secret`**，`ksc = mode‖psk_id_hash‖info_hash`、`nonce = base_nonce XOR I2OSP(seq,Nn)`，§7.1.3 的 X25519 直接 `LabeledExpand` 出 sk **不做拒绝采样**，`enc` 是**发送方**临时公钥。③**外层 AAD 用等长零占位** —— `ClientHelloOuterAAD = ClientHelloOuter` 但把 ECH 扩展 payload 换成**等长零**（不含握手头），故密文长度永不随明文变化；`EncodedClientHelloInner = ClientHelloInner(legacy_session_id 清空) ‖ zeros[padding]`。④**ech_outer_extensions(0xfd00) 与四条 MUST abort** —— 把内层重复的扩展压成引用列表；引用缺失(-4)、引用重复(-5)、引用 ECH 自己(-6)、内外层相对顺序不一致(-7) 都必须终止；填充规则有条件分支（有 SNI 补 `max(0, M-D)`、无 SNI 补 `M+9`，最后 `N = 31 - ((L-1) % 32)` 对齐到 32 的倍数）。⑤**接受确认** —— `accept_confirmation = HKDF-Expand-Label(HKDF-Extract(0, ClientHelloInner.random), "ech accept confirmation", transcript_ech_conf, 8)` 写进 **ServerHello.random 末 8 字节**（HRR 版标签换成 `"hrr ech accept confirmation"`），客户端据此确认服务端**真的解开了**内层而不是静默降级；服务端解密失败必须**继续用手上外层信息走完**以抗降级侧信道。⑥**RFC 8446 §7.1 的 `HKDF-Expand-Label` 与 HPKE 的 `LabeledExpand` 编码不同**（前者是 HkdfLabel、带 `tls13 ` 前缀、无 `"HPKE-v1"` 与 suite_id），**两者不能混用**，混用会得到「自洽但对外不通」的密钥。⑦**交付与自检方式** —— 官方向量只认 RFC 9180 附录 A.2（6 组加密 seq 0/1/2/4/255/256 + 3 组 Export + ksc/secret/key/base_nonce/exporter_secret 全项 True）；**无 C/Go 工具链下做算法端口验证**：把 Go 版 `chachaBlock/chacha20XOR/poly1305MAC/aeadMACData/aeadSeal/aeadOpen` 与 `hkdfExtract/hkdfExpand/labeledExtract/labeledExpand/tlsExpandLabel` 逐行移植回 Python 对官方向量比对，因此抓到 Go 版 `poly1305MAC` 的真 bug —— 每块隐含的 `2^(8n)` 位在大端表示下写在大端**最前**（`blk[0] = 1`），误写成 `blk[n] = 1` 时 seal/open **同时错、往返测试照样全过**，只有 A.2 官方向量能暴露 | C(13 文件，95 断言) + Python(8 文件，81 断言实跑全绿) + Go(12 文件，88 断言) | 2026-09-18 |
| 357 | `07-数据存储/03-缓存/HTTP缓存语义/` | HTTP 缓存判定语义（RFC 9111 全文实读：§3/§4.1/§4.2/§4.2.1~4.2.4/§5.2+§5.2.2.x；RFC 5861 §3/§4 实读；纯标准库 Python + Go 双实现，自检 **42+28** 条）：①**新鲜度是严格大于** —— 规范原文 `response_is_fresh = (freshness_lifetime > current_age)`，故 `age == max-age` 的那一刻**已经陈旧**，写成 `>=` 会多复用一个滴答。②**freshness_lifetime 按序取第一个匹配** —— s-maxage（**仅共享缓存**）> max-age > `Expires - Date` > 启发式；私有缓存必须忽略 s-maxage，同一响应给浏览器与 CDN 的寿命可以不同；`Expires - Date` 而非 `Expires - now` 是为了**抵消客户端与源站的时钟偏差**。③**启发式新鲜度 10%** —— 无显式过期时才允许猜，且**有显式过期时 MUST NOT 用**；带 Last-Modified 时规范建议取距今间隔的某个比例、典型 10%；这只是 encouraged 不是强制，故不同 CDN 的启发式寿命可以合法地不一样。④**Age 有两种独立算法** —— `apparent_age = max(0, response_time - date_value)`（**负值必须截断**，否则客户端时钟落后时会把陈旧响应判为新鲜）；`corrected_age_value = age_value + (response_time - request_time)`；保守合成取 `max(apparent, corrected)`，非保守只用后者，两者在时钟分歧时能差出**整条偏移量**（实测 900 秒）。⑤**no-cache ≠ no-store** —— `no-store` 是不许落盘，`no-cache`（无参）是可以存但每次都得回源校验；`no-cache="Set-Cookie"` 这种**带字段名**的限定形式反而允许复用（排除列出的头）。⑥**陈旧复用的四道禁令** —— no-cache 无参 / must-revalidate / proxy-revalidate（仅共享缓存）/ s-maxage；**must-revalidate + 断网必须报错（SHOULD 504）而不是「好心」返回陈旧数据**。⑦**RFC 5861 的两个窗口都是左闭右开** —— `max-age=600, stale-while-revalidate=30`：age 600 进 swr、630 已出窗口；`stale-if-error` 只在**错误**时生效，且规范明确错误 = **500/502/503/504**（429、404 不算）。⑧**Vary 二级键** —— 列出的每个头必须完全一致，**缺失只能匹配缺失**（一端有 Accept-Encoding 一端没有 → 不匹配，用 `dict.get()` 直接比大小会在这条上出错）；**`Vary: *` 永远不匹配**（等于禁用缓存，不是「匹配一切」）。⑨**手写解析器的头号坑** —— `no-cache="Set-Cookie,ETag"` 里的逗号在引号内，朴素 `split(",")` 会把它拆成两个指令并把引号留在结果里，必须做引号感知切分 | Python(215+133 行，42 断言全实跑) + Go(300 行，28 断言) | 2026-09-18 |
| 358 | `07-数据存储/03-缓存/缓存一致性模式/` | 缓存一致性模式（Microsoft Learn《Cache-Aside pattern》实读，含「Update the data store *before* removing the item from the cache」的 Note 与反例；NSDI'13《Scaling Memcache at Facebook》§3.2.1 逐段实读；C + Python + Go 三实现，自检 **33** 条）：①**用交错枚举量化不一致窗口** —— 把「缓存未命中的读」建模为 `读库 → 回填` 两步，写建模为若干步，枚举全部保持内部顺序的交错 `C(m+2, m)` 并逐个判定「缓存要么无该 key、要么值等于库值」。实测：**先删缓存再更新库 4/6**、**先更新库再删缓存 1/6**、延迟双删 1/10、Write-Through（单原子步）1/3 —— 前两者相差 **4 倍**，正是 Azure 那句警告的定量版本。②**「先删缓存」的不一致是持久的** —— 坏交错 `W_DEL → 读旧值 → 回填 → 库才更新` 留下库 v2 / 缓存 v1，**没有任何后续操作能修复它**，只能等 TTL 或下一次写。③**没有一种组合是零窗口** —— 即使写是原子的，只要读的「读库 → 回填」不是原子，就存在 `读旧值 → 原子写 → 回填旧值` 这条交错；**写路径原子化无法消除不一致，根源在读路径**。④**延迟双删不是银弹** —— 收益依赖「第二次删除发生在读回填之后」这一时序假设，本 demo 把第二次删除建模为紧接着的一步（偏乐观）。⑤**Lease 是 64-bit token 且绑定 key** —— 未命中时发 token，回写必须带上；**收到该 key 的 delete 会使在途 token 全部作废**（论文自称类似 load-link/store-conditional）—— 实测带作废 token 的回写被拒、不带 token 的普通回写被接受并立刻污染缓存。⑥**惊群** —— 服务端限制发 token 速率，**每 key 每 10 秒只发一个**，10 秒内的请求收到「稍等」通知；实测 100 个客户端并发读冷 key：无 lease **100 次**打库、有 lease **1 次**。⑦**论文实测 17K/s → 1.3K/s = 13.08 倍**，远小于理想化的 100 倍 —— 差距来自「并非所有请求都恰好同时到达」，写文档时两个数字不能混用。⑧**Stale value** —— 删除后值转存到「最近删除项」结构，`accept_stale` 的客户端可直接拿走旧值；本 demo 中 stale 分支**不消耗 token**、不受 10 秒限流（返回 stale 并不回源） | C(192 行，c_sanity --tu 通过) + Python(194+98 行，33 断言全实跑) + Go(247 行，go_sanity + go_crossref 通过) | 2026-09-18 |
| 359 | `07-数据存储/03-缓存/WTinyLFU准入/` | Window TinyLFU 准入策略（ben-manes/caffeine Wiki《Efficiency》实读，作者本人维护；纯标准库 Python + Go 双实现，自检 **30** 条）：①**LRU 在全表扫描下会彻底失效** —— 实测 100 个热条目预热 5 轮后插入 500 个一次性扫描条目：LRU 热集命中 **0/100**，W-TinyLFU **94/100**；这不是命中率下降而是缓存被彻底换血。②**4-bit Count-Min Sketch 的内存账** —— `ceiling_pow2(capacity)` 个 long × 每个 long 装 16 个 4-bit 计数器 = 16×capacity 个计数器 × 4 bit = **8 字节/条目**（对应 wiki 的 "growing at 8 bytes per cache entry"）；capacity=100 实测 n_longs=128、1024 字节。③**估计量 = 4 个计数器的最小值，且只高估不低估** —— 碰撞只会让某格偏大；方向性很重要：高估让一次性条目**偶尔混入**（500 个里混入 **5 个**、拒绝 **495 个**），低估则会误伤热数据。④**准入判据是严格大于** `freq(candidate) > freq(victim)`，平局 incumbent 胜 —— 这是一次性条目永远进不来的原因。⑤**冷启动陷阱（本 demo 第一版踩到）** —— 若从第一条访问就做准入，冷启动时所有条目频率都是 1、`1 > 1` 为假，**缓存永远装不满**（实测卡在 2 条）；正确做法是**只在缓存已满时才做准入测试**，没装满则窗口 LRU 直接降级进主区。⑥**容量划分** —— 1% 窗口 + 主区 SLRU，实测填满后 window 1 / protected 79 / probation 20。⑦**窗口接住 recency burst** —— 主区填满高频项后访问一个全新条目 X，间隔 gap 次后再访问 X：无窗口**必 miss**；窗口 5% 时 gap=3 **hit**、gap=30 miss（超出窗口容量）。wiki 原话：窗口让「recency bursts which would otherwise be rejected」也能有高命中。⑧**自适应** —— hill climbing 比较窗口与主区命中率，哪边高往哪边挪 ±1%，夹在 [1%, 50%]。⑨**与 ARC/LIRS 的取舍** —— wiki 明说 ARC 需 **2 倍**缓存大小保留被淘汰 key 且有专利、LIRS 需 **3 倍**，而 **W-TinyLFU 不保留被淘汰的 key** | Python(258+124 行，30 断言全实跑) + Go(281+119 行，同 main 包 2 文件) | 2026-09-18 |
| 360 | `07-数据存储/03-缓存/Redis过期与淘汰/` | Redis 键过期与淘汰（redis.io《Key eviction》《EXPIRE》两篇文档实读 + redis/src/{expire,evict,object}.c 与 server.h 源码实读；C + Python + Go 三实现，自检 **64** 条）：①**`GT` 对永久键永远失败、`LT` 永远成功** —— 文档原文 non-volatile 被视为 **infinite TTL**，于是想给永久键加过期只能写 `LT`/`NX`。②**`GT`/`LT` 比的是过期的绝对时间点，不是剩余 TTL** —— 本 demo 第一版写成比较剩余 TTL（`910 > 100` 误判成立），被断言抓出；正确是比 `910` vs `1000`。③**过期 ≠ 删除** —— 非正数超时或过去时间会让键被**删除**，键事件是 **`del` 而非 `expired`**，监听过期事件做清理的业务会漏掉这条路径。④**清 TTL 的判据是「整体替换 vs 就地修改」** —— `DEL/SET/GETSET/*STORE` 清，`INCR/LPUSH/HSET` 保留。⑤**定期删除的参数换算**（expire.c）—— 常量 20 / 1000µs / 25% / 10%，`effort = active_expire_effort - 1`；默认 effort=1 → `keys=20, fast=1000, slow=25, stale=10`；effort=10 → `65, 3250, 43, 1`；方向是 effort 越大扫得越多占 CPU 越多但容忍陈旧越低，且 `acceptable_stale = 10 - effort` **永不为 0**。⑥**LFU 的 24 位 lru 字段** —— 高 16 位是分钟、低 8 位是计数器，`o->lru = (minutes << 8) \| LFU_INIT_VAL`，**`LFU_INIT_VAL = 5`** 给新键保底分。⑦**Morris 概率计数器** —— `p = 1/((counter-5)·lfu_log_factor + 1)`，counter<5 时 baseval 夹到 0 使 p=1；本 demo 用**均值场近似**与**精确马尔可夫链**两种方式复现官方 4×4 对照表，两者相差 **< 0.5**，与官方值最大偏差约 ±5（最大相对误差 16% 出现在最小格子 factor=100/100 hits），因官方表是一次参考运行的典型值而本 demo 算的是期望值。⑧**衰减减的是绝对值不是比例** —— 默认 `lfu-decay-time 1` 每过 1 分钟减 1，故高频键长期不访问也会归零；`0` = 永不衰减。⑨**一个真实的 off-by-one** —— `LFUTimeElapsed` 回绕分支写 `65535 - ldt + now`，而 16 位能表示 65536 个值，真实环绕距离应为 `65536 - ldt + now`，**跨回绕时少算 1 分钟**（ldt=65530/now=5：真实 11、源码 10）。⑩**volatile- 陷阱** —— 文档原文 `volatile-xxx` 在无 TTL 键时 **behave like noeviction**（直接报错，而非退化成 allkeys-*）；且设 expire 本身占内存。⑪**AOF/复制缓冲区不计入 maxmemory**，否则会形成「淘汰 → DEL → 缓冲区变大 → 再淘汰」的反馈环 | C(188 行) + Python(225+167 行，64 断言全实跑) + Go(161+92 行，同 main 包 2 文件) | 2026-09-18 |
| 361 | `07-数据存储/03-缓存/MemcachedSlab分配/` | Memcached Slab 分配器（memcached master 分支 slabs.c / memcached.c / memcached.h / items.c 源码实读；C + Python + Go 三实现，自检 **44** 条）：①**三层尺寸** —— slab 页 **1MB**、最大 chunk **512KB = page/2**、单 item 上限 **1MB**（源码注释 *The famous 1MB upper limit*）；**1MB ≠ 512KB**，超过 512KB 的 item 必须走 `ITEM_CHUNKED` 跨多个 chunk（`slabs_clsid()` 返回 0）。②**`sizeof(item) = 48`** —— 字段 42 字节（三个指针 24 + time/exptime/nbytes 12 + 4 个 1~2 字节字段 6），随后 `union {uint64_t cas; char end;} data[]` 要求 8 字节对齐补齐到 48，柔性数组不计入；起始 chunk = `48 + settings.chunk_size(48)` = **96 字节**。③**建表算法的三个关键点** —— 退出条件是 `size >= slab_chunk_size_max / factor` 而**不是** `MAX_NUMBER_OF_SLAB_CLASSES(64)`，默认参数只建出 **39 个 class**；`power_largest` 被**单独强制设为 512KB** 而不遵循倍率；`perslab = page / size` **向下取整**，每页尾部都有 `< size` 的零头。④**8 字节对齐会放大实际倍率** —— `120 × 1.25 = 150` 被对齐成 **152**；默认参数下 class 1/2/3 = 96/10922、120/8738、152/6898，最后一类 524288/2。⑤**内部碎片就是 factor 的函数** —— 100 字节 → class 2(120) 浪费 20 字节（16.7%）；**97 字节**只因 class 1 是 96 就得跳到 120，浪费 23 字节；最坏（刚好超出 1 字节）趋近 `factor-1 = 25%`，均匀分布下平均约 `(factor-1)/(2·factor) = 10%`。⑥**内存卡死在某个 class** —— 页一旦分配**不会自动归还**；实测 64 页全给 class 2 后连续写 1000 个 class 9 的大 item，**1000 次全部触发淘汰、一个都没存下**，而 class 2 里还有大量空闲 chunk —— 这就是「内存没满却在淘汰」。⑦**slab reassignment 的硬约束** —— 只有**整页 chunk 全部空闲**时才能挪；实测腾空最后一页后可挪且大 item 立刻能存进去，而「最后一页仍有在用 chunk」时 `reassign` 直接失败。⑧**factor 不能一味调小** —— 降到 1.05 碎片率虽降到 ~2.4%，但 class 数从 39 涨到约 190，超过 64 后被截断 | C(111 行，c_sanity --tu 通过) + Python(155+138 行，44 断言全实跑) + Go(209 行，go_sanity + go_crossref 通过) | 2026-09-18 |
| 362 | `08-安全/03-应用安全与供应链/SAST与污点分析/` | 污点分析三档精度阶梯（CodeQL《Analyzing data flow in Python》实读；Py + Go + C 三实现，自检 23 项）：三档抽象各丢弃一类信息 —— flow-insensitive 丢**顺序与分支**故「先读后写」被误报；flow-sensitive 保留顺序但**分支汇合点做并集**故「条件与状态的相关性」丢失（只有 x 被 sanitize 时 ok 才为 True，合并后 x 可能脏且 ok 变不确定，于是第二个 if 上误报），path-sensitive 逐路径枚举 + 只走可行分支才排除它；把 else 分支也改成置 ok=True 打掉相关性后三档都该报。DataFlow 只跟**保值**步骤，TaintTracking 追加**非保值**步骤（字符串拼接），故拼接在 flow 模式漏报、taint 模式命中；sanitize 即 isBarrier。**隐式流**（数据经控制流而非赋值传递）三档**全部漏报** —— 加精度主要是在砍误报、砍不动漏报（报告数 4→3→2，漏报恒为 1）。实现坑：flow-insensitive 必须取**所有赋值的并集（OR）**而不是后写覆盖，否则同一变量有冲突赋值时不动点振荡**永不收敛**（Py 第一版即挂死，靠 faulthandler 定位）；path-sensitive 在 if 分叉后必须**带上后续语句**继续执行，否则挂在 if 之后的 sink 永远走不到 | Python(270+102 行，23 断言全实跑) + Go(288+49 行) + C(262 行) | 2026-09-18 |
| 363 | `08-安全/03-应用安全与供应链/依赖漏洞与SBOM/` | 依赖漏洞两级过滤：版本区间 → 传递闭包 → **可达性剪枝** → SPDX（semver.org 2.0.0 与 SPDX 2.3 规范实读；Py + Go + C 三实现，自检 41 项）：①**SemVer 优先级不是字典序** —— 预发布段纯数字按数值比（beta.2 < beta.11）、数字段优先级**低于**非数字段、段数多者更高；**build metadata（+ 之后）完全不参与比较**。②**`^` 在 0.x 上是陷阱** —— 规范 §4 明写 0.y.z 处于初始开发期、API 不应视为稳定，故 `^0.2.3` 等价于 `>=0.2.3 <0.3.0`（锁到 minor）而非直觉上的「允许兼容升级」。③解析取区间内**最高**版本本身会修掉一部分 CVE —— 本 fixture 中 logfmt 的 CVE 影响 `>=1.0.0 <1.0.2` 而解析结果是 1.0.2，在版本层就被排除；compress 上游只发了 0.2.7 故只能停在受影响版本。④**可达性剪掉「装了但调不到」** —— 从入口 app.main 沿调用图 BFS，compress.Inflate 无任何入边，故 CVE-COMPRESS 虽然版本命中仍被剪掉；最终 3 条版本命中 → 2 条真告警，**两级过滤各自都剪掉了东西**。⑤SBOM 产出 SPDX 2.3 骨架，DESCRIBES / CONTAINS / DEPENDS_ON 三种关系 + PURL externalRefs + SHA256 校验和，其中 DEPENDS_ON 边正是影响范围分析的输入 | Python(254+80 fixture+105 自检，41 断言全实跑) + Go(300+71 行) + C(289 行) | 2026-09-18 |
| 364 | `08-安全/03-应用安全与供应链/供应链完整性/` | SLSA v1.0 Provenance 的生成与**验证**（slsa.dev/spec/v1.0/provenance 与 about 实读；Py + Go + C 三实现，自检 26 项）：`subject` 是 Statement **顶层**字段而非 predicate 内字段；`predicateType` 必须原样用 `https://slsa.dev/provenance/v1`。五条验证规则各对应一个攻击场景 —— ①subject 摘要绑定（构建后换包 → DENY）；②**Consumers MUST accept only specific signer-builder pairs**（GitHub 不能给 Google Cloud Build 签名 → DENY）；③**`builder.id` 是 SLSA Build level 的 sole determiner**，扩展字段 `x_slsaBuildLevel` 自称 L3 不作数（要求 L3 时仍 DENY）；④`externalParameters` 由用户设置**不可信**、必须下游验证且 SHOULD 拒绝未预期字段（注入 entryPoint → DENY），而 `internalParameters` 由可信平台设置**无需验证**；⑤**Consumers MUST ignore unrecognized fields** 故带未知扩展的证明仍应 ALLOW，但单调性原则要求「忽略扩展不得把 DENY 变成 ALLOW」—— 即策略永远不能依赖扩展字段满足强制要求。另记：resolvedDependencies 用于递归分析、其完整性是 best effort，且 **SLSA level 不传递** | Python(234+88 行，26 断言全实跑) + Go(269 行) + C(222 行，签名用 FNV-1a 占位) | 2026-09-18 |
| 365 | `08-安全/03-应用安全与供应链/密钥管理/` | CWE-798 硬编码凭据检测（MITRE CWE-798 实读；Py + Go + C 三实现，自检 31 项）：核心事实是**香农熵上限由字符集大小决定** —— 十六进制 `log2(16) = 4.0` bit，故「阈值 4.5」这个常见教程数字会**系统性漏报**高质量十六进制密钥（40 位 token 实测 3.959）；Base64 上限 6.0。改进一：除以 `log2(观测字符集)` 做**归一化**使跨字符集可比，但归一化熵只对**足够长**的串有意义（`1.2.3` 归一化 0.961、`password123` 0.987），故必须加长度门槛（取 20）。改进二：叠加规则 —— 结构规则（AKIA 前缀 / tok_live_ / PEM 头）救回低熵但有固定形态的凭据、变量名关键字救回 password123、上下文白名单（sha/uuid/digest/version）消掉 git_sha 这类高熵非凭据。三级检测器 TP/FP/FN/TN：**2/1/3/4 → 4/2/1/3 → 5/1/0/4**。量化结论：语料中 `aws_key`（真密钥）熵 **3.6842** 低于 `git_sha`（非密钥）**3.7373**，故**任何裸熵阈值都分不开二者**；阈值扫描（3.0→5.0）显示最高召回 4 条，而零误报约束下召回最多只有 **1** 条 —— 这是判据信息量不足，调参无解 | Python(186+95 行，31 断言全实跑) + Go(230 行) + C(211 行，无正则用子串匹配) | 2026-09-18 |
| 366 | `08-安全/03-应用安全与供应链/CI流水线加固/` | CI 流水线四类攻击面（GitHub《Security hardening for GitHub Actions》实读；Py + Go + C 三实现，自检 30 项）：①**脚本注入** —— `${{ github.event.* }}` 在 YAML 解析前做文本替换，拼进 `run:` 后就是 shell 脚本文本的一部分；标题写成 `a"; curl ... #` 会提前闭合引号使载荷落到**引号外**被执行，判据是按双引号切分后看偶数段；正确写法是走 `env:` 让值进环境变量（**不要**写成 `TITLE='${{ }}'`，单引号同样会被闭合）。②**第三方 action 必须固定到完整 commit SHA** —— 文档原话这是把 action 当不可变版本的**唯一**方式，因为 tag 可被移动或删除。③**特权触发器共享主分支缓存** —— `pull_request_target` / `workflow_run` 若又检出不受信任的 PR 代码即可投毒缓存，叠加仓库写权限与 secrets 可致仓库被接管；job 之间也不隔离。④**令牌与凭据** —— GITHUB_TOKEN 默认只给内容读权限、按 job 提升；用 OIDC 换短期令牌可把泄露后可用窗口从长期 secret 的 **315359880 秒**压到 **780 秒**。六条 lint 规则（R1 注入 / R2 特权检出 / R3 缓存 / R4 权限 / R5 未固定 / R6 长期凭据）在坏流水线上全中、好流水线零告警，且规则正交（只改一项只触发对应一条） | Python(187+101 行，30 断言全实跑) + Go(250 行) + C(211 行) | 2026-09-18 |
| 367 | `09-语言学习/Rust/闭包与迭代器/` | 闭包与迭代器（The Book ch13-01/13-02/13-04 实读；Py 319 行 33 断言 + Rust 100 行 + 4 个 compile_fail）：①**三种捕获方式直接映射三种传参** —— 官方原文 "directly map to the three ways a function can take a parameter"，且由**闭包体怎么用**决定而非变量类型：只读→`&T`、改动→`&mut T`、交出所有权→move，没用到就不捕获 ②**Fn/FnMut/FnOnce 是加法式的**（官方 "in an additive fashion"）Fn ⊂ FnMut ⊂ FnOnce，移出捕获值就只剩 FnOnce，于是进不了约束为 FnMut 的 `sort_by_key`（E0507），而约束为 FnOnce 的 `unwrap_or_else` 三种闭包都能接 ③**`move` 只改捕获方式、不改 trait 集** —— 断言 1.7 固化；它的真实用途是跨线程，因为主线程先结束会 drop 被借用的值 ④**闭包类型由首次调用锁定**（`\|x\| x` 先吃 String 再吃整数即 E0308），这是闭包不能像泛型函数那样单态化的直接后果 ⑤**迭代器惰性**：`v.iter().map(f)` 建完闭包调用次数为 0，rustc 给 `unused Map that must be used` 警告，只有 consuming adapter 才驱动；`sum`/`collect` 拿走迭代器所有权，之后再用是 E0382 ⑥**零成本的两个可计数指标**：官方只给定性说法（"zero-cost abstractions" + 基准 19,620,300 vs 19,234,900 ns/iter）与"allocation-free state machines"，本 demo 翻译成「上游遍历趟数 = 1」与「物化中间容器次数 = 1」，对照分步 collect 的 2 次分配 ⑦`for val in iter` 去糖后 next 次数 = 元素数 + 1 | Python(205+114 行) + Rust(100 行人工审查+配平) | 2026-09-19 |
| 368 | `09-语言学习/Rust/无畏并发/` | 无畏并发（The Book ch16-03/16-04 + std::marker::{Send,Sync} + MutexGuard + RefCell 实读；Py 333 行 31 断言 + Rust 97 行 + 3 个 compile_fail）：①**Send/Sync 是语言内置的 auto trait**，官方精确定义 "`T: Sync` 当且仅当 `&T: Send`"，并给出四条关系（`&T` Send ⟺ `T: Sync`；`&mut T` Send ⟺ `T: Send`；两者 Sync 都 ⟺ `T: Sync`）②**`&mut T` 竟然是 Sync**（官方自称 "somewhat surprising"），因为 `& &mut T` 会退化成只读等价于 `& &T`，故无数据竞争 ③**两张非对称牌**：`RefCell<T>` 是 Send 但非 Sync，于是 `Arc<RefCell<i32>>` 连 Send 都不是（`Arc<T>: Send` 要求 `T: Send + Sync`），这是"共享可变必须换 Mutex"的真正原因；`MutexGuard` 是 `!Send` 但 `T: Sync` 时是 Sync（std 给的 !Send 理由是防止在别的线程被 drop）④**自动推导是全员通过制**（"composed entirely of"），掺一个 `Rc` 字段整体失效 ⑤**Mutex 两条规则被类型接管**：类型是 `Mutex<i32>` 而非 `i32` 所以必须先 lock；`MutexGuard` 的 Deref+Drop 让解锁变 RAII ⑥**拦得住数据竞争、拦不住死锁**：四次加锁动作全都合法且过 borrow check，两线程相反顺序锁 A/B 即死锁，本 demo 用 wait-for 图判环把"官方只警告"变成可断言；另附中毒（持锁 panic 后 `lock()` 返 Err）与不可重入 | Python(216+117 行) + Rust(97 行人工审查+配平) | 2026-09-19 |
| 369 | `09-语言学习/Rust/async状态机/` | async/await 状态机（std::future::Future + async-book 02_execution + std::pin 实读；Py 407 行 31 断言 + Rust 184 行 + 3 个 compile_fail）：①**Future 是惰性的**，std 原文 "Futures alone are inert; they must be actively `poll`ed"，构造后 polls=0、时间不前进；不驱动事件源就永久挂起；官方还把它类比 `epoll(4)` 而非 `poll(2)` ②**真实签名的两处改动都有理由**：`&mut self`→`Pin<&mut Self>` 是为了允许不可移动的 future（自引用 `struct MyFut { a: i32, ptr_to_a: *const i32 }`）；`wake: fn()`→`&mut Context` 是因为纯函数指针 "can't store any data about which Future called wake" ③**Waker 三条契约**：只保留最近一次 Context 里的 Waker（两次 poll 传不同 Context，事件到达时只有后一个被唤醒）、完成后不得再 poll（Join 把已完成子 future 置 None）、poll 要尽快返回 ④**无 Waker 的代价可量化**：同一个 100 tick 定时器，Waker 驱动只需 2 次 poll，盲轮询需要 101 次 ⑤**组合子是无分配状态机**：子 future 内联持有（非装箱）、跑完一圈属性集合不变 ⑥**Pin**：moving 是逐字节拷贝，pinning 保证地址不变；大多数类型自动 Unpin，标准做法是加 `PhantomPinned` 字段摘掉自动 Unpin；本 demo 自引用 future 被"搬"后指针即悬垂 | Python(271+136 行) + Rust(184 行人工审查+配平) | 2026-09-19 |
| 370 | `09-语言学习/Rust/错误处理惯用法/` | 错误处理惯用法（The Book ch09-02 + Rust Reference try propagation + std::error::Error + thiserror/anyhow 实读；Py 362 行 35 断言 + Rust 142 行 + 3 个 compile_fail）：①**`?` 是「提前 return + 调一次 `From::from`」**，Reference 列出五种可作用类型（Result / Option / ControlFlow / Poll<Result> / Poll<Option<Result>>），去糖为 `Try::branch` + `FromResidual::from_residual`；**只有 Result 系会调 From** —— 本 demo 用计数器断言 Option 与 ControlFlow 路径上 `From::from` 一次都不被调 ②缺 From impl 就在 `?` 那一行报 E0277 ③**`Box<dyn Error>` = "any kind of error"**，靠 blanket impl 实现，两个不同下层错误装箱后是同一静态类型且仍能 downcast ④**source 链与 not-both 规则**：std 明令"底层错误要么由 `source()` 返回、要么被 Display 渲染，但**不能两者都做**"，本 demo 把它写成可执行 lint（`source().display() not in display()`）并用反例证明抓得住 ⑤**退出码**：`main() -> Result<(), E>` 时 Ok→0、Err→非 0（兼容 C 约定，机制是 `Termination::report()`）⑥**thiserror 与 anyhow 的分工**：前者是 `std::error::Error` 的 derive（`#[error]` 生成 Display、`#[from]` 生成 From 且**隐含** `#[source]`，官方强调"不出现在公开 API 里"），后者是 trait object 错误类型（`context()` 追加上下文、支持 downcast、Rust ≥1.65 自动 backtrace）；一句话：库用 thiserror 定义"发生了什么"，应用用 anyhow 记录"当时在做什么" | Python(223+139 行) + Rust(142 行人工审查+配平) | 2026-09-19 |
| 371 | `09-语言学习/Rust/unsafe边界/` | unsafe 边界（The Book ch20-01 + std::cell::UnsafeCell + Rustonomicon Transmutes + std::pin 实读；Py 327 行 22 断言 + Rust 152 行 + 4 个 compile_fail）：①**unsafe 只解锁五件事**（官方 "unsafe superpowers"）：解引用裸指针、调 unsafe 函数或方法、访问或修改可变静态变量、实现 unsafe trait、访问 union 字段；断言固化"恰好五项"并逐项验证安全代码里做即 E0133 ②**unsafe 不关借用检查**（官方 "doesn't turn off the borrow checker ... it will still be checked"），本 demo 用两条判据钉死：模型层面 `Ref.write()` 签名里根本没有 `in_unsafe` 参数（结构上无法绕过），代码层面把 `v.push()` 包进 unsafe 仍报 E0502 ③**裸指针创建安全、解引用危险**：官方四条区别（可忽略借用规则、不保证有效、可为 null、无自动清理），E0133 打在 `*r` 上而非创建处；悬垂与空指针解引用被显式建模为 UB ④**UnsafeCell 是内部可变性的核心原语**，只解除 `&T` 的不可变保证，**不**解除 `&mut` 的唯一性（官方 "There is no legal way to obtain aliasing `&mut`, not even with `UnsafeCell<T>`"），且不防数据竞争；`.get()` 返回的就是 `*mut T` ⑤**transmute 唯一编译期检查是尺寸**（nomicon "The only restriction is ... the same size"），尺寸相同后 `&`→`&mut` 永远是 UB、"Do not transmute 3 to bool"、`repr(Rust)` 布局无保证、未标生命周期会产生 unbounded lifetime，全都能编译通过 ⑥**安全抽象把 UB 变成 panic**：`split_at_mut` 只靠一句 `assert!(mid <= len)`，裸实现越界是 UB、安全 API 越界是 panic | Python(176+151 行) + Rust(152 行人工审查+配平) | 2026-09-19 |
| 372 | `10-逆向工程/03-协议逆向/Discoverer递归聚类/` | 协议逆向第二批 demo1：Discoverer 递归聚类（USENIX Security 2007 全文实读；Py 296+24 行 22 断言全实跑 + Go 252+175+64 行）：①**tokenize 的三条硬口径** —— 文本/二值判定按可打印比例、最小文本长度门槛、**UTF-16LE 检测**（高位为零的交替字节），长报文截到 MAX_PREFIX=2048 ②**token 三元组 `(dir, class, value)`** 做初始聚类，`dir ∈ {in, out}` 区分请求与响应 ③**格式推断的两种语义** —— 常量/变量属性 + **长度语义**（该 token 取值是否等于本报文剩余长度）与**偏移语义**（是否为指向某个已知 token 的偏移）④**FD（格式区分符）三判据**：取值足够多样、按取值分组后**最大子簇 ≥ MIN_CLUSTER**、各子簇推断出的格式**互不相同**（若相同则切了也白切）；**再加「常量 token 只能分出 1 个子簇则跳过」是防死循环的关键**，否则会在魔法字 token 上无限递归 ⑤**合并走「基于类型的序列对齐」**：两格式的 token 序列先按类型对齐再做 Needleman-Wunsch，gap 必须成段且落在两侧可容忍范围，§3.4 允许**至多 1 处失配**（本 demo 两个家族的 len 低字节取值集合 `{05,03,08}` 与 `{02}` 不相交，实测失配恰为 1） | Python(296+83+24 行，22 断言全实跑) + Go(252+175+64 行) | 2026-09-19 |
| 373 | `10-逆向工程/03-协议逆向/AutoFormat执行上下文/` | 协议逆向第二批 demo2：AutoFormat 上下文感知执行监视（NDSS 2008 全文实读；Py 215 行 18 断言 + Go 246+102 行）：①**污点传播三条规则** —— `mov` 让目标**继承**源操作数污点且对立即数**解除标记**、算术/逻辑指令把两操作数的污点取**并集**、读内存按地址污点取字节 ②**`<o, c, s, l>` 记录**：offset / 指令内容 / 调用栈 / 字节长度，同一 `(o, s)` 只留一条（去重是字段树能收敛的前提）③**协议字段树（Algorithm 1）** —— **偏移连续 + 同一调用栈** 即合并为同一字段，这是「HTTP 的 Method 与 URI 不会被粘在一起」的直接原因；实现上**循环结束必须再 flush 一次**最后一个 pending 节点，否则末字段凭空消失 ④**并行字段（Algorithm 2）** —— 比较两字段的**执行史共享前缀**，≥ 阈值 h=80% 判为并行；**坑**：逐偏移的执行史长度常为 1，共享前缀比根本到不了 75%，故「h=70 会把 Method/URI 误判为并行」只能作为 `similar()` 的**单元级**断言（用合成执行史构造 3/4=75% 的共享前缀），不能拿真实 trace 去断言 ⑤**顺序字段取先序遍历**且**遇到并行节点不再下钻**，否则候选字段全沉到关键字的子字段里、顶层个数从 4 变 7 | Python(215+126 行，18 断言全实跑) + Go(246+102 行) | 2026-09-19 |
| 374 | `10-逆向工程/03-协议逆向/Tupni记录序列/` | 协议逆向第二批 demo3：Tupni 记录序列与循环合并（CCS 2008 全文实读；Py 243 行 22 断言 + Go 240+80+105 行）：①**chunk = 某条指令操作数里最长的一段连续被污染字节**，`mov` 因不处理操作数被**显式跳过**；权重 w(c) = 访问该 chunk 的指令条数 ②**加权最大 k-Set Packing 贪心**挑选互不重叠的 chunk 集合（NP-hard 故只能贪心），未选中的字节归为 virtual field ③**循环识别走 CFG 单入口环**，只有**迭代相关指令 Ii**（同一指令在不同迭代里访问的输入字节不同，如循环变量驱动的变址取数）才算真的在处理记录序列；**坑**：fixture 里循环条件指令 A 必须**出现在每一轮迭代的开头**而不是孤立的引导块，否则 `I[0]` 为空、断言「B/C/D 都是迭代相关指令」失败 ④**Figure 4 记录边界算法的口径不一致** —— 第 17 行 `ej = s(j+1) - 1` 是闭区间而第 18 行 `en = Offset + sizeof` 是开区间，照抄会得到错位一个字节的区间；本 demo 把它写成显式断言再统一折算成半开区间 ⑤**记录类型靠 `Qi → Q'i`**：把子循环折叠成 virtual EIP 后按子记录类型序列去重，再附三类约束（符号谓词 / 函数依赖 / 跨消息约束）与长度字段判定 | Python(243+133 行，22 断言全实跑) + Go(240+80+105 行) | 2026-09-19 |
| 375 | `10-逆向工程/03-协议逆向/ReFormat加密报文/` | 协议逆向第二批 demo4：ReFormat 加密报文相位划分与数据生命周期（ESORICS 2009 全文实读；Py 116 行 18 断言 + Go 200+88 行）：①**两阶段剖析** —— 先按**算术 + 位运算指令占比**的累计曲线定位「密码学计算集中区」，再在该区里做**逐函数片段**占比找转折点，单看全局占比会把解密淹没在普通业务逻辑里 ②**函数片段 = 同一函数 + 同一运行时栈帧**（同一函数被递归或多处调用时要按栈帧区分，否则不同调用上下文的数据生命周期会互相污染）③**数据生命周期分析** —— 写集 ∩ 读集，且**不同阶段活跃性规则不同**（密码学阶段的输出在阶段结束后即应死亡，若仍活跃说明后面还有一层处理）④**阈值必须落在实测分布之内**：本 demo 把「阈值高过所有解密片段（97%）时无片段达标」写成断言（`phase_profile(tr, 97)[0] is None`），替代了原先形如 `... or True` 的恒真断言 —— **恒真断言等于没测**，这是本 demo 自检里真实存在的两处缺陷之一（另一处是非格式化字符串里混进字面量百分号百分号）⑤ReFormat 自身只做「定位解密完成点」，格式分析仍复用 AutoFormat | Python(116+106 行，18 断言全实跑) + Go(200+88 行) | 2026-09-19 |
| 376 | `10-逆向工程/03-协议逆向/bit级字段切分/` | 协议逆向第二批 demo5：bit 级字段切分与字节粒度的能力边界（RFC 1035 §4.1.1 + Tupni §3.3 + BinPRE §3.2 原文实读；Py 95+111 行 **19 断言** + Go 124+102 行）：①**动机是两句原文** —— Tupni §3.3 "Currently we track input chunks at **byte granularity**; we believe it takes only engineering efforts to refine this to bit granularity"，而 16 年后的 BinPRE（CCS 2024）§3.2 仍是 "It taints protocol message data at the **byte level**"，§3.3 的字段候选也仍是字节序列（Algorithm 1 行 4-5）—— 那句「只需工程投入」并未兑现 ②**判据**：相邻 bit 窗口满足「联合取值基数 < 各自基数之积」→ 统计相关 → 同属一字段；否则独立 → 此处是边界。真值取 RFC 1035 §4.1.1 的 16 位标志字 QR / Opcode / AA / TC / RD / RA / Z / RCODE ③**结果**：位级切出 **9 段**（7 个变量块 + 2 个待定常量段）**vs 字节粒度只有 2 段**；字节 0 的取值基数恰为 48 = 2×3×2×2×2（5 个子字段笛卡尔积）、字节 1 为 10 = 2×5 ④**核心结论是能力边界**：常量段 `[9,13)` 在语义上横跨 **Z（3 位，RFC 规定 Must be zero）+ RCODE 最高位（1 位）** 两个字段，但统计上**完全无法切分** —— 单独开窗基数恒为 1、**换信息论判据同样失效**（Z 段经验熵精确为 0）。纯统计与纯信息论的位级切分对常量位无能为力，突破必须引入非统计先验（规范、结构体布局、移位掩码）⑤**反例固化过度切分**：语料若让 Opcode 取满 0..15（笛卡尔积），`card(bit1..4)=16` 恰等于 2 的 4 次方 → 判为 4 个独立位，**位级切分没有「正确答案」只有相对于语料的答案** ⑥Go 版 `segment()` 的提前返回块漏了右花括号，代码读起来完全正常，只有 `bracket_check.py` 报 UNCLOSED 才暴露 | Python(95+111 行，19 断言全实跑) + Go(124+102 行) | 2026-09-19 |
| 377 | `11-性能分析/03-基准测试方法论/GoBench样本量/` | Go `testing.B` 的迭代标定与样本量（golang/go `src/testing/benchmark.go` master 源码 + pkg.go.dev/testing 逐行实读；Py 分行 + Go 镜像，自检 **9** 组）：①**`-benchtime` 是 `durationOrCountFlag`，没有独立的 `parseBenchTime`** —— `Set` 用 `strings.HasSuffix(s,"x")` 分两态，Nx 走 `strconv.ParseInt`、否则 `time.ParseDuration`；`n<0` 或 `n==0` 报 `invalid count`、`d<=0` 报 `invalid duration`，故 **`0x`/`0s`/`-1x`/`abc` 全部被拒**而不是"跑零次"；默认值写在变量初始化里 `benchTime = durationOrCountFlag{d: 1*time.Second}`。②**`b.N` 不是流传很广的 1,2,5,10,20,50 序列** —— 当前实现 `run1` 先 `runN(1)`，duration 形式再由 `predictN` 动态预测：`n = goalns*prevIters/prevns`（**先乘后除**，源码注释明说先除会让极快基准得到 0/1 从而"hide an order of magnitude"）→ `n += n/5`（多跑 20%）→ `n = min(n,100*last)`（防计时误差暴涨）→ `n = max(n,last+1)` → `n = min(n, maxBenchPredictIters=1e9)`。实测 1µs/op 的序列是 **[1, 100, 10000, 1000000]**（4 轮），1ns/op 因 `100*last` 上限要 **6 轮**才爬到 1e9。③**`1x` 连 `runN` 都不调** —— `launch` 里 `if b.benchTime.n > 1` 才 `runN`，`1x` 直接复用 `run1` 的结果（golang.org/issue/32051），样本量恒为 1。④**`1.2x` 会超调** —— `-benchtime=10s` + 1µs/op 标定出 N=12,000,000、实际墙钟 **12.000s**（比要求多 2 秒）；想严格控制总时长只能用 `Nx`。⑤**`ns/op` 与 `B/op` 都是整数除法直接截断** —— 1501ns/2op 报 750；500KB 摊到 1e6 op 报 **0 B/op**。⑥**每轮 `runN` 前都 `runtime.GC()`**（注释 "Try to get a comparable environment for each run"），发生在 `ResetTimer()` 之前，所以 GC 时间不计入 ns/op。⑦`RunParallel` 的 grain 目标 ~100µs、钳制在 [1,1e4]，`ns/op` 是整体墙钟而非各 goroutine 之和。⑧**样本量只能来自 `-count`** —— benchstat 要求"至少 10 次、理想 20 次"，`-count=1` 时中位数置信区间根本不存在 | Python(248 行，9 组断言实跑全绿) + Go(233 行) | 2026-09-19 |
| 378 | `11-性能分析/03-基准测试方法论/MDE与样本量估算/` | 最小可检测效应与样本量方程（NIST/SEMATECH e-Handbook §7.2.2.2 与 §1.3.5.3 原文实读；Py stats+sample 两文件 + Go 镜像，自检 **9** 组）：①**复现 NIST 两个原例** —— 单侧 α=0.05/β=0.10/δ=σ 且 σ 已知时 `(1.645+1.282)²=8.5638→9`（原文打印 8.567）；σ 未知时以 df=N−1 迭代，df=8 给出 `(1.8595+1.3968)²=10.6039→11`（原文打印 10.6），再迭代 df=10 仍收敛到 11，验证了原文那句 "one iteration is usually sufficient"。②**单样本→两组的 ×2 是推导而非记忆** —— 两组均值差标准误 `σ√(2/n)` 与单样本 `σ/√n` 相等时解出 `n_two=2·n_one`，再用解析功效 `Φ(d√(n/2)−z)+Φ(−d√(n/2)−z)` 验证 n=16 时功效 **0.8074**（目标 0.80），蒙特卡洛另得 0.784。③**把公式翻过来**：`MDE_rel = (z_{1−α/2}+z_{1−β})·CoV·√(2/n)`，于是 benchstat 的 `-count=10` 对应 MDE=**1.253%**（CoV=1% 时）、`-count=20` 对应 **0.886%**，比值恰为 √2；**样本量翻 4 倍 MDE 才减半**。④**取整的方向** —— `ceil` 使实际 MDE 只会**优于**目标（精确解 62.79 → 63 → 反解 0.004992），第一版写成 `back == mde` 直接失败。⑤**`-count=1` 不是不精确而是无解** —— Welch 自由度 = 0，t 分位数根本不存在。⑥benchstat 默认 `assume=nothing` 走 Mann-Whitney U，实测 n=16 时功效 **0.755 < Welch 的 0.784**，故公式给的 n 只是下界 | Python(stats 175 + sample 195 行，9 组断言实跑全绿) + Go(280 行，功效走解析式不依赖 RNG) | 2026-09-19 |
| 379 | `11-性能分析/03-基准测试方法论/CoV噪声地板/` | 变异系数、A/A 噪声地板与降噪（Dropbox Apogee 官方博客 + LLVM Benchmarking tips + benchstat Tips 实读；Py + Go 镜像，自检 **8** 组）：①**CoV 无量纲**故能跨基准比较（整体缩放 7.3 倍，CoV 一字不差），这正是 Apogee 选它做主指标的原因。②**A/A 噪声地板可算** —— `E[‖Δx̄‖]/μ = CoV·√(2/n)·√(2/π)`（`√(2/π)=0.7979` 是 `E‖Z‖`），实测 0.4965% vs 理论 0.5046%；**这个数就是"没改任何代码时你以为看到的回归幅度"**。③**核对 Apogee 那句 "60% → less than 5%，于是能检出 5% 回归"** —— 在 α=0.05/power=0.8 下，CoV=5% 时 `-count=10` 的 MDE 是 **6.26% > 5%**，必须 **每侧 16 次**（MDE 4.95%）原话才成立；官方没写这个前提，属口径差异（记录不改正）。④**降噪与加样本可互换** —— MDE 减半要么样本量 ×4（10→41），要么 CoV 减半（5%→2.5%）。⑤**离群剔除是 O(σ) 的有偏操作** —— "每 5 次丢掉最大值"的理论偏差 `= −E[max(Z₁..Z₅)]·σ/4 = −0.2907σ`；实测 n=2000 得 −0.2714σ、n=20000 得 −0.2860σ，**加样本量救不了它**（此时均值标准误仅 0.007σ）。Go 版不抄常数，用 Simpson 积分现算 `∫x·5Φ(x)⁴φ(x)dx = 1.162964`。⑥**交错 vs 顺序跑的漂移倍数恰为 n** —— 线性漂移下顺序跑贡献 `drift·n`、交错跑 `drift·1`（n=5/10/20 逐一断言到 1e-9）。⑦不降噪（CoV=60%）想检出 5% 需每侧 **2261** 次 | Python(196 行，8 组断言实跑全绿) + Go(176 行，全确定性) | 2026-09-19 |
| 380 | `11-性能分析/03-基准测试方法论/多重比较校正/` | FWER vs FDR 与 family 的定义（benchstat Tips 原文 + statsmodels `multipletests` 文档 + Benjamini-Hochberg(1995)/Benjamini-Yekutieli(2001) 临界常数实读；Py + Go 镜像，自检 **9** 组）：①**benchstat 原话核对** —— 50 个无差异 benchmark 平均报出 **2.52** 个"显著"（期望 2.5），且未校正假阳性数 ≈ `0.05·m`（m=10/50/200 → 0.5/2.5/10）。②**未校正时 FWER = `1−0.95^50` = 0.923**，实测 **0.929** —— 92% 的实验至少有一个假阳性。③**全局零效应下 FDR 与 FWER 相等** —— 因为 `V ≡ R`，被拒的全是假阳性，`V/R = 1`，故 `FDR = P(R>0) = FWER`；这是"m₀=m 时 BH 并不比 Bonferroni 松"的根因（实测 BH 的 FWER 也只有 0.049）。④**有真效应时二者才分道扬镳** —— m=50 含 10 个真效应：BH 的 FDR **0.039** 但 FWER **0.220**，功效 **0.530**；Bonferroni 的 FWER **0.033**、功效 **0.322**。⑤**Holm ⊇ Bonferroni**（300 组随机 p 向量逐一验证），同样控制 FWER，**没有任何理由优先用裸 Bonferroni**；Šidák 的 α_c=0.0010248 略宽于 α/m=0.0010000。⑥**BH 的阈值自适应** —— 10 个真效应时最大被拒原始 p 达 **0.00534**，而 Bonferroni 的判据恒为 p≤0.00100。⑦**BY 的代价是 H_m 倍** —— `H_50 = 4.499205338 ≈ ln50+γ`，`BY校正p = BH校正p × H_m`。⑧**benchstat 自己不做任何校正**，文档只有警告，故 family 由使用者定义 | Python(250 行，9 组断言实跑全绿) + Go(core 152 + family 221 行，自带 splitmix64 不依赖 math/rand) | 2026-09-19 |
| 381 | `11-性能分析/03-基准测试方法论/分配测量与GC/` | `-benchmem` 的 B/op 与 allocs/op：净值语义、整数截断与 GC 摊销（golang/go `benchmark.go` + pkg.go.dev/runtime `MemStats` 字段文档逐条实读；Py + Go 镜像，自检 **9** 组）：①**净值是差值不是绝对值** —— `StartTimer` 采样 `Mallocs`/`TotalAlloc` 作基线，`StopTimer` 累加差值；实测"分配 1000 → **停表期间**分配 5 → 再分配 2000"得到 **3000 而非 3005**。之所以必须取差，是因为官方文档写明 `TotalAlloc ... does not decrease when objects are freed`，且存活对象 `= Mallocs − Frees`。②**`0 B/op` 不等于零分配** —— `AllocedBytesPerOp = MemBytes / N` 是整数除法、直接截断；总分配固定 500KB 时 B/op 随 N 依次为 **500000 / 5000 / 50 / 0**，**`-benchtime` 越大反而越容易看到 0**（稀疏分配被摊没了）。③**每轮 `runN` 前 `runtime.GC()`** 发生在 `ResetTimer()` 之前，故 GC 时间不计入 ns/op，但 `NumGC` 随 `-count` 增长（实测 -count=10 至少 +10），且"堆持续增长触发 GC"的真实服务模式在微基准里被系统性抹掉。④**GC 摊销让 `ns/op` 随 N 非单调** —— 每 op 分配 1KB/存活 5%/单次 STW 2ms 的模型下，N=1e3/1e5/1e6 分别报 **100 / 120 / 108** ns/op：GC 次数按**对数**增长（`next_gc` 每次翻倍）而单次 STW 固定，故 N 再增大摊销反而回落。**同一个含分配的 benchmark 换 `-benchtime` 会得到不同且不单调的 ns/op**。⑤**`GCCPUFraction` 的分母是 GOMAXPROCS 的积分而非墙钟** —— 文档原例 GOMAXPROCS=2 跑 10 秒 → 可用 CPU 20 秒，GC 用 1 秒 → **0.05**；且它 **不含写屏障**，会低估 GC 成本。⑥**`Extra` 优先覆盖内建列** —— `ReportMetric("B/op", 7)` 会让 `AllocedBytesPerOp()` 返回 7。⑦附"把分配当逻辑测"判别：`B/op` 变化 ≥3× 而 `ns/op` 变化 <1.5× | Python(272 行，9 组断言实跑全绿) + Go(core 195 + check 117 行) | 2026-09-19 |
| 382 | `01-游戏开发/03-UI/九宫格缩放/` | 九宫格缩放（9-slice / border-image）：W3C CSS Backgrounds Level 3 §5 四步绘制（§5.2 切片 / §5.3 绘制区 / §5.5 平铺 / §5.6 绘制）逐条代码化。切片三条硬规则：百分比基准上下看高、左右看宽（100x80 图上 25% 30% 12% 20% → 20/30/9.6/20）；偏移超图像尺寸按 100% 解释；左+右 ≥ 图宽则上下边与中间为空。四种平铺实测 src=27 区域 200：stretch 1 块 200、repeat 7 块不缩放首块居中 5.5、round 7 块 28.5714、space 7 块 + 8 个等间隙 1.375（repeat 居中、其余靠左是规范明文）。四角只缩放不平铺，对照整图拉伸会把 27px 圆角放大到 133.33px；中间块默认不画（需 fill）；目标小于四角之和时中间压到 0、四角重叠 | Python(126+222 行) + Go(229 行)，26 项断言 | 2026-09-19 |
| 383 | `01-游戏开发/03-UI/流式布局与弹性尺寸/` | Flexbox 主轴算法：§9.3 分行（用 outer hypothetical main size；首项放不下单独成行；零尺寸项挂到上一行末尾）+ §9.7 九步（grow/shrink 判定、冻结 inflexible 项、initial free space、循环按比例分配、min/max 违约钳制、冻结推进保证终止）。两条反直觉结论：grow 因子之和 < 1 时只分配 initial free space × Σfactors（容器 600 两项各得 200，仍剩 200 空着）；shrink 按 shrink × inner flex base size 加权（base 100/300 容器 200 → 50/150，而非各让 100）。冻结循环实例：A(base200,min150) + B(base200) 容器 100 → 首轮各让 150、A 触 min 冻结、亏空转给 B 压到 0，总宽 150 > 100 溢出是规范允许的结果 | Python(188+117 行) + Go(260 行)，21 项断言 | 2026-09-19 |
| 384 | `01-游戏开发/03-UI/事件分发与冒泡穿透/` | UI 事件分发（WHATWG DOM §2.9 等价实现）：path = target → 祖先链；捕获逆序、冒泡正序，target 项上 eventPhase 恒为 AT_TARGET 故 capture 与 bubble 两种监听都会跑；bubbles=false 只跳过非 target 项（实测冒泡阶段仅剩 B-button@button(at-target)）；在 panel 捕获阶段 stopPropagation 会让 button 一次都收不到；stopImmediatePropagation 额外断掉同节点剩余监听；invoke 时克隆监听表（已 removed 不跑，未到达的祖先上新加的会跑）；once 只触发一次。游戏侧：命中按 depth 从大到小，未消费则穿透到下一层，模态遮罩靠 consumed 标记阻断穿透 | Python(176+175 行) + JavaScript(217 行，node 实跑)，19 项断言 | 2026-09-19 |
| 385 | `01-游戏开发/03-UI/UI合批与Canvas重建/` | UGUI 合批与重建（Unity 官方《Optimizing Unity UI》口径）：rebatch = 按 depth 排序 + 共享 material/texture 贪心成批，50 张图按两种图集交替排 → 50 批，按图集归拢 → 2 批（25 倍）；A-B-A 3 批 vs A-A-B 2 批；Text 用独立字体材质夹中间必断批；Mask 引入 stencil 材质断批；alpha=0 的元素仍进批次、仍参与像素采样（应禁用或删掉 Graphic）。overdraw 量化：8 层全屏 = 8.0，关掉被完全遮住的底板 → 7.0。PerformUpdate 三步：Layout 按层级深度排序（实测 [根, 父, 子]）、Clipping、Graphic 不排序按注册序。Sub-canvas 隔离：100 静态 + 1 动态，单 Canvas 一次 dirty 重算 101 个，拆出 Sub-canvas 后只重算 1 个 | Python(160+150 行) + Go(220 行)，23 项断言 | 2026-09-19 |
| 386 | `01-游戏开发/03-UI/MSDF多通道距离场/` | MSDF（Chlumský 2015 硕士论文）：median-of-three 的 min/max 写法 max(min(a,b), min(max(a,b),c)) 经 6 种排列 + 200 组随机校验等于真中位数且三通道可互换；Figure 3.7 角点象限编码（凸角 median 后恰 1 个内象限 (1,1,0)，凹角恰 3 个内、唯一外部象限 (1,0,0)）；n=3 最小维度的可执行复现（限定通道为平滑形状的 6 种象限模式后穷举：凸角 n=2 可行、凹角 n=1/2 不可行 n=3 可行 → 取 max 得 3）；伪距离沿边所在直线量取、叉积定符号（边 (0,0)→(1,0) 与点 (5,3)：伪 3.000 vs 真实 5.000）且沿边方向严格线性。重建实测（域 ±4，输出 65²）：网格 5² SDF 错 57 像素 vs MSDF 0，9² 17 vs 0，17²/33² 1 vs 0 —— MSDF 两通道本身是线性函数，双线性插值精确，故任意分辨率零误差 | Python(223+143 行) + Go(263 行)，22 项断言 | 2026-09-19 |
| 387 | `02-Web开发/04-API设计/REST/问题详情RFC9457/` | RFC 9457 problem details（obsoletes RFC 7807，全文实读）：§3.1「成员类型不符 MUST be ignored」逐成员复刻 —— 字符串 status、JSON true（Python 里 bool 是 int 子类，不排除就会误判成 number）、404.5 非整数、越界 700 全部被忽略而非报错；§3.1.1 相对 type 按 base URI 解析，规范原文两个资源上的同名相对 type 解析成不同绝对 URI（RFC 3986 §5.2.4 remove_dot_segments 已实现）；§4 扩展名 SHOULD 以 ALPHA 开头、只含 ALPHA/DIGIT/_、长度 ≥3（为能序列化成 XML）；§4.2.1 about:blank 是 type 的缺省值且 title SHOULD 等于状态码推荐短语；附录 B 的 XML 数组靠「只含名为 i 的子元素」判定而非重复同名元素 | Python(260+139 行 44 断言实跑) / Go(276 行人工审查) | 2026-09-19 |
| 388 | `02-Web开发/04-API设计/REST/条件请求与乐观并发/` | RFC 9110 §13 + §8.8.3.2（实读）：强弱比较 Table 3 四行逐条断言，**强比较要求 both are not weak → 服务器给弱 ETag 时 If-Match 永远失败**（想做乐观并发就必须发强 ETag）；If-Match MUST 强比较 / If-None-Match MUST 弱比较；If-None-Match:* 配 PUT 可防两个客户端抢建初始表示；日期判据方向（IMS 是 last_modified<=date→false，IUS 是 →true）；谁屏蔽谁（INM 屏蔽 IMS、If-Match 屏蔽 IUS、非 GET/HEAD 忽略 IMS、无 Range 则忽略 If-Range）；§13.2.2 六步优先级表（If-Match 失败一定是 412，即便同时有会给出 304 的 INM）；§13.2.1 基线响应非 2xx/412 时忽略全部条件（404/301/401 优先）；lost update 场景 200 vs 412 | Python(241+143 行 50 断言实跑) / Go(224 行人工审查) | 2026-09-19 |
| 389 | `02-Web开发/04-API设计/REST/成熟度模型与方法语义/` | RMM（Fowler 2010 全文实读）+ RFC 9110 §9.2：把成熟度做成**可从报文反推**的判据 —— 多资源路径→L1、读取用 safe 方法且用了 200 以外状态码→L2（两条缺一不可，只加状态码仍停 L1）、响应带超媒体控制→L3；safe=GET/HEAD/OPTIONS/TRACE，idempotent=PUT/DELETE+全部 safe，规范只给 GET/HEAD/POST 定义缓存语义；**代理 MUST NOT 自动重试非幂等请求**（比客户端的 SHOULD NOT 更硬），客户端另有两个例外（知道语义幂等 / 能检测原请求从未被应用）；§9.2.1 点名的 page?do=delete 反例做成静态检查 | Python(183+92 行 57 断言实跑) / Go(208 行人工审查) | 2026-09-19 |
| 390 | `02-Web开发/04-API设计/GraphQL/N+1与DataLoader/` | N+1 的数量模型 + DataLoader 语义（官方 README 全文实读）：两层 1+N vs 1+1、三层 1+2N=21 vs 1+1+1=3，且两种写法**返回完全相同的结果集**（DataLoader 只改查询次数不改结果）；batchLoadFn 两条硬约束（返回值等长 + 按索引对齐），后端乱序返回/缺记录时重排与补 null 是 batch 函数的责任；**官方在 load() 时就缓存 promise，所以同一帧内的重复 key 也去重**；cache:false 时 keys 含重复；maxBatchSize 切分；batch:false 等价 maxBatchSize:1；Error 实例作为值会被缓存而 throw/reject 不缓存 | JavaScript(151+166 行 23 断言 node 实跑) + Python(161+98 行 22 断言实跑) | 2026-09-19 |
| 391 | `02-Web开发/04-API设计/幂等性与重试/` | Idempotency-Key（draft-ietf-httpapi-idempotency-key-header-07 全文实读；-08 抓取 404，-07 为当前可用最新）：§2.1 值是 RFC 8941 Item 且 MUST 为 String → 线上形态必须带引号，裸 UUID 非法；§2.6 三态（首次正常处理 / 重试返回先前已完成的结果含错误 / 并发回冲突错误）；§2.7 缺头 400、同键不同 payload 422、并发未完成 409，且 **409 是唯一无需修正即可重试的错误**；§2.4 fingerprint 用规范化 JSON 的 SHA-256（键序无关）；§2.3 过期后同一键视为新键；§5 低熵键导致数据泄漏 → 用「客户端键 + 只有资源知道的客户端属性」复合键隔离，两个客户端传同一键互不可见 | Python(172+143 行 35 断言实跑) / Go(207 行人工审查) | 2026-09-19 |
| 392 | `03-系统编程/04-文件系统/VFS与statx/` | statx(2) 请求掩码不等于返回掩码（stat.h 头注释四规则）：不支持的位被清并编造兼容值、没请求但顺手可得的位会白送、mask 含 STATX__RESERVED 触发 EINVAL、属性位必须与 stx_attributes_mask 相与、AT_STATX_SYNC_TYPE 两个位同时置位非法、stx_blocks 是 512B 单位（稀疏文件小于 size）、符号链接 stx_size 不含结尾 NUL、同一趟调用里不同字段可能来自不同时刻 | Python(290+200 行 36 断言实跑) / Go(201 行) / C(210 行人工审查) | 2026-09-19 |
| 393 | `03-系统编程/04-文件系统/CoW与快照/` | Btrfs CoW：reflink 只加物理块引用计数、往中间写把 extent 切成头中与尾三段（代价正比于被改块数而非文件大小）、defrag 打断 reflink 造成空间放大（3 文件逻辑 24 块物理 8→16 块）、NODATACOW 原地写有 torn write 风险、datacow 与 datasum 与 compress 三角互斥且挂载选项按顺序最后生效、swapfile 必须 NODATACOW 且无洞、send stream 的 CRC32C 初值 0 不取反（等长数据下 init 差异才是常数） | Python(240+103+191 行 48 断言实跑) / Go(268+83 行人工审查) | 2026-09-19 |
| 394 | `03-系统编程/04-文件系统/O_DIRECT与对齐/` | O_DIRECT：缓冲区地址与偏移量与长度三样都要对齐、2.4 按 fs 块 4096 与 2.6 按逻辑块 512 与 6.1 起 statx 查询三个代际、未对齐可能 EINVAL 也可能静默退回 buffered、不支持时 open 直接 EINVAL、O_DIRECT 自身不给 O_SYNC 的元数据保证（glibc 里 O_SYNC 含 O_DSYNC 位，判元数据要剥掉）、私有映射缓冲区上禁止与 fork 并发、混用的代价等于切换次数乘 2 乘重叠量、NFS 只在客户端绕过 page cache | Python(131+132 行 32 断言实跑) / Go(151 行人工审查) | 2026-09-19 |
| 395 | `03-系统编程/04-文件系统/稀疏文件与打洞/` | 稀疏文件：SEEK_HOLE 落在洞中间原样返回 offset、文件末尾是隐式洞、越过 EOF 报 ENXIO、扫描循环必须以 ENXIO 收尾；fallocate 五种 mode——预分配按块向上取整（请求 100 字节可能占 2 块）、PUNCH_HOLE 必须与 KEEP_SIZE 并用否则 EINVAL、ZERO_RANGE 转成 unwritten 只走元数据 IO、COLLAPSE_RANGE 与 INSERT_RANGE 互为逆操作且都有粒度与 EOF 约束；FIEMAP_EXTENT_UNWRITTEN 表示已分配无数据；手册允许的最简退化实现会拷满整个文件 | Python(193+186 行 40 断言实跑) / Go(297 行) / C(125 行真实系统调用，人工审查) | 2026-09-19 |
| 396 | `03-系统编程/04-文件系统/回写与脏页/` | vm.dirty_*（kernel sysctl/vm 实读）：阈值分母是 available 即 free 加 reclaimable 而非总内存（8GiB 机器算出 7GiB，差 12.5%）、bytes 与 ratio 互为 counterpart 写一个另一个读出来是 0、dirty_bytes 最小两页写小了静默忽略、background 阈值必须小于 dirty 阈值否则限流抢先、dirty_expire 与 dirty_writeback 单位都是百分之一秒且后者置 0 禁用定期回写、drop_caches 只丢干净页且 available 不变故阈值不动 | Python(160+147 行 34 断言实跑) / Go(236 行人工审查) | 2026-09-19 |
