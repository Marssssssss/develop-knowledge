# archive/completed.md — 已完成 demo 全本

> 由自动化任务每轮 append。**默认 agent 不读此文件**,当 `STATE.md` 第三节回答不了"做过吗"或"总数"时才 Grep 此处。
> rotate 规则见 `STATE.md §4`:> 100 行 或 > 50 KB → 截断至最近 100 行。

## 迁移说明(2026-09-11 拆分时)

| 来源 | 处理 |
| --- | --- |
| 旧 `_docs/SEARCH_PROGRESS.md` 第二节"已完成 demo 记录"(20 条) | 一次性写入本文档,保留全部原始 detail |
| ID 分配规则 | 001-999 主 demo 用,1000+ 留给"修订/补全"类副任务(未启用) |
| 完成日期 | 保留首次完成日期;补全/修订不增 ID |

## 已完成 demo 列表

| ID | 路径 | 知识点 | 语言 | 完成日期 |
| --- | --- | --- | --- | --- |
| 001 | `01-游戏开发/01-服务端/网络编程/IO多路复用/select/` | IO 多路复用 · `select` | C / Python / Go | 2026-09-11 |
| 002 | `09-语言学习/Python/装饰器/` | Python · 装饰器(基础 + 带参数) | Python | 2026-09-11 |
| 003 | `01-游戏开发/01-服务端/网络编程/IO多路复用/epoll/` | IO 多路复用 · `epoll`(LT/ET) | C / Python / Go | 2026-09-11 |
| 004 | `07-数据存储/04-搜索引擎/elasticsearch/` | Elasticsearch · 倒排索引与 NRT(REST API) | Python / Go | 2026-09-11 |
| 005 | `01-游戏开发/02-渲染/图形管线/深度缓冲/` | Z-Buffer 与 Z-Fighting(1/z 深度模型 + 量化精度) | C / Python / Go | 2026-09-11 |
| 006 | `01-游戏开发/03-UI/文本渲染/SDF/` | SDF 文本渲染(有符号距离场 + 双线性重建 + smoothstep AA) | C / Python / Go | 2026-09-11 |
| 007 | `01-游戏开发/04-游戏引擎/ECS/` | ECS 架构(sparse set 存储 + swap-remove + 最小集合查询) | C / Python / Go | 2026-09-11 |
| 008 | `01-游戏开发/05-物理/碰撞检测/GJK/` | GJK 碰撞检测算法(Minkowski 差 + support 函数 + simplex 演化) | C / Python / Go | 2026-09-11 |
| 009 | `01-游戏开发/06-AI/寻路算法/A-star/` | A* 启发式搜索(f=g+h + 可采纳性 + Dijkstra/A*/Greedy 三模式对比) | C / Python / Go | 2026-09-11 |
| 010 | `01-游戏开发/07-音频/音频压缩/IMA-ADPCM/` | IMA ADPCM(自适应差分 + 4-bit 量化 + 步长双查表 + 块随机访问) | C / Python / Go | 2026-09-11 |
| 011 | `01-游戏开发/08-动画/IK算法/FABRIK/` | FABRIK 启发式 IK(位置空间反向/正向两阶段 + 不可达目标退化) | C / Python / Go | 2026-09-11 |
| 012 | `02-Web开发/01-前端框架/Vue/reactive/` | Vue 3 Proxy 响应式最小实现(reactive/ref/effect/track/trigger + lazy 嵌套代理 + cleanup + effect 栈) | TypeScript / JavaScript | 2026-09-11 |
| 013 | `02-Web开发/02-后端/Node.js/Event-Loop/` | Node.js Event Loop(6 阶段 + nextTick/Promise 微任务 + libuv 1.45.0 行为变化 + I/O 中 setImmediate 必早于 setTimeout) | JavaScript / TypeScript | 2026-09-11 |
| 014 | `02-Web开发/03-数据库/B+树索引/` | B+ 树索引(M-way + 叶子兄弟链 + copy-up/push-up 分裂 + borrow/merge 重平衡 + bulk-load O(N)) | C / Python / Go | 2026-09-11 |
| 015 | `02-Web开发/04-API设计/WebSocket/握手协议/` | WebSocket 握手协议 RFC 6455 §4(HTTP Upgrade + SHA-1+GUID → Sec-WebSocket-Accept + 101 Switching Protocols + 子协议协商) | C / Python / Go | 2026-09-11 |
| 016 | `03-系统编程/01-网络编程/Socket基础/Nagle算法/` | Nagle 算法 vs `TCP_NODELAY`(RFC 896"inhibit sending when unacknowledged data exists"+Linux tcp(7) man page `tcpi_segs_out` 段计数实测;100 次 1 字节 send 在 Nagle on → ~8 段 vs `TCP_NODELAY=1` → 100 段) | C / Python / Go | 2026-09-11 |
| 017 | `03-系统编程/02-进程与线程/哲学家就餐/` | 哲学家就餐问题(Dijkstra 1965;Naive 死锁 + Resource Hierarchy 资源分级破循环等待 + Tanenbaum 监视器 1 mutex + N condvar + state[];pthread_cond_wait 原子释放 + 谓词循环 + 三种语言 mutex/cond 标准库对照) | C / Python / Go | 2026-09-11 |
| 018 | `03-系统编程/03-内存管理/分配器/bump-allocator/` | Bump (arena) 分配器(mmap 单块 + 单调 offset + 二进制 `(x+a-1)&~(a-1)` 向上对齐 + O(1) 分配/重置;alignment 必须为 2 的幂;多块版需 next 链表,匿名 struct 不能自引用) | C / Python / Go | 2026-09-11 |
| 019 | `03-系统编程/03-内存管理/分配器/slab-allocator/` | Slab 分配器(Bonwick 1994 简化版:kmem_cache + 三链表 partial/full/free + slab 内 bitmap + ctz 找第一空位;SLAB_OBJ_MAX=126/slab;O(1) partial 头分配;生产加 slab coloring + per-CPU array) | C / Python / Go | 2026-09-11 |
| 020 | `03-系统编程/03-内存管理/垃圾回收/gc-tri-color/` | 三色标记 GC(Dijkstra 1978 "On-the-Fly GC":white/gray/black 三色 + tri-color 不变式"无黑→白"边 + worklist gray 栈;stop-the-world 版 demo 5 个图:可达链/循环/断连子图/钻石共享/孤立子树;O(E+V) mark + O(heap) sweep;能回收 refcount 不能的循环) | C / Python / Go | 2026-09-11 |
| 021 | `03-系统编程/04-文件系统/mmap内存映射/` | mmap(2) 文件/匿名内存映射(MAP_SHARED 共享 page cache + msync 持久化;MAP_PRIVATE 写时复制 CoW 不影响源文件;MAP_ANONYMOUS|MAP_SHARED 父子进程 IPC;ftruncate 后访问越界 → SIGBUS 边界;offset 必须页对齐、length 页向上取整;fd 可立即 close 不影响映射) | C / Python / Go | 2026-09-11 |
| 022 | `03-系统编程/04-文件系统/ext4-Journaling/` | ext4 Journaling / JBD2(预写日志:journal_header_t 12 字节 magic=0xC03B3998 + blocktype + sequence;5 类块 descriptor/commit/jsb v1+2/revoke;descriptor 含 blocknr 标签描述 data block 最终落盘位置;recovery 重放已 commit 事务丢弃 incomplete;JBD2 大端 vs ext4 小端;JBD2_FLAG_ESCAPE 处理 data 前 4B 与 magic 碰撞) | C / Python / Go | 2026-09-11 |
| 023 | `03-系统编程/04-文件系统/Page-Cache/` | Linux page cache + writeback(`posix_fadvise` 6 advice: NORMAL/RANDOM/SEQUENTIAL/WILLNEED/DONTNEED/NOREUSE;`sync_file_range` 精细 writeback 3 flag 组合;dirty page 触发条件:`vm.dirty_background_ratio` 10% / `dirty_ratio` 20% / `dirty_expire_centisecs` 30s / 显式 fsync / umount;/proc/meminfo Cached/Dirty 状态观察;Linux-only demo,Windows 直接 exit 提示) | C / Python / Go | 2026-09-11 |
| 024 | `04-移动开发/01-iOS/内存管理/ARC/` | Swift / Objective-C ARC 内存管理(strong retain 默认 / weak 可选自动置 nil / unowned 非可选不自动置 nil / Person-Apartment + Customer-CreditCard + HTMLElement 闭包 3 例;引用计数仅适用类实例,struct/enum 不归 ARC 管;Objective-C 对应 `__weak` / `__unsafe_unretained` / Block `__weak typeof(self)` 捕获列表;CFGetRetainCount 验证 retain/release 等价行为) | Swift / Objective-C | 2026-09-11 |
| 025 | `04-移动开发/01-iOS/并发编程/GCD同步原语/` | GCD DispatchQueue / DispatchSemaphore / DispatchGroup / DispatchWorkItemFlags.barrier(主队列 vs 全局并发 vs 自定义;QoS 5 等级;.barrier 在并发队列独占但全局并发队列无效;dispatch_semaphore 限并发上限 3 时 10×200ms 任务实测 ≈ 0.67s;DispatchGroup enter/leave 配对 + notify;main.sync 死锁警告) | Swift / Objective-C | 2026-09-11 |
| 026 | `04-移动开发/01-iOS/事件循环/RunLoop/` | RunLoop modes / sources / timers / observers(每线程一实例 lazy 创建;CFRunLoopObserver 监听 kCFRunLoopActivity 6 个位 entry/beforeTimers/beforeSources/beforeWaiting/afterWaiting/exit;5 个标准 mode default/connection/modal/eventTracking/commonModes;Timer scheduledTimer 必须 invalidate 否则 RunLoop↔Timer↔Closure↔Self 循环;ScrollView 滚动切到 eventTracking mode 使 default mode 的 timer 暂停 → 注册到 commonModes 解决) | Swift / Objective-C | 2026-09-11 |
| 027 | `04-移动开发/02-Android/并发编程/Handler消息机制/` | Android Handler / Looper / MessageQueue 异步消息机制(Handler(Looper) post/sendMessage/postDelayed + HandlerThread 自带 Looper 的工作线程 + nativePollOnce epoll 阻塞唤醒 + Message.sPool 对象池复用 + onNewIntent 与 setIntent 配合 + 5 个 demo:post Runnable 跨线程 / sendMessage 四元组 / HandlerThread 串行 / postDelayed 定时 / 内存泄漏静态内部类 + WeakReference 修复) | Kotlin / Java | 2026-09-11 |
| 028 | `04-移动开发/02-Android/组件生命周期/Activity启动模式/` | Android Activity launchMode 5 种(standard 默认创建新实例 + singleTop 栈顶复用 onNewIntent + singleTask 同 affinity 唯一并清空上方 + singleInstance 全设备独占任务 + singleInstancePerTask API 31+ 文档型)+ Intent flags 运行时优先级(NEW_TASK / SINGLE_TOP / CLEAR_TOP)+ onNewIntent 必须 setIntent(intent) 更新 getIntent() + taskAffinity 自定义任务分组) | Kotlin / Java | 2026-09-11 |
| 029 | `04-移动开发/02-Android/UI框架/Compose重组/` | Jetpack Compose 重组机制(mutableStateOf 自动重组 + by 委托 setValue + remember(key1, key2) 缓存昂贵计算 + derivedStateOf 仅在结果变化时触发下游重组避免滚动每帧重组 + Modifier.offset{} / drawBehind{} lambda 版本跳过 Composition 阶段直接 Layout/Draw + Backwards write 反模式无限重组循环 + Snapshot 系统 MVCC 多版本快照追踪 + Slot Table 增量更新) | Kotlin | 2026-09-11 |
| 030 | `04-移动开发/03-跨平台/React-Native/Bridge-vs-JSI/` | React Native New Architecture(Bridge 异步 JSON 队列 → JSI 同步 C++ 接口直接函数调用;Fabric C++ 渲染管线 + React 18 并发渲染 + 跨平台共享 C++ 渲染核心;TurboModules 懒加载 on first access + Codegen 编译期 TS spec → C++/Java/ObjC 胶水类型校验;2 个 JS 文件 + 2 个 TS 文件纯 Node.js 模拟 legacy bridge async/JSON/queue/JIT 全特性 + JSI HostObject 同步直接调用 + lazy module 实例化 + 新旧架构 5 维度对比表) | TypeScript / JavaScript | 2026-09-11 |
| 031 | `04-移动开发/03-跨平台/Flutter/三棵树/` | Flutter 三棵树(Widget 不可变配置声明 + 跨 frame 比对复用 + build() 返回新实例;Element 跨帧持久实例 create/mount/update/activate/deactivate/unmount 6 阶段生命周期 + ComponentElement vs RenderObjectElement 两类;RenderObject layout/paint 可变 + performLayout() 接收父 BoxConstraints + 5 类 RenderBox/FixedBox/ExpandedBox/Container/Row 模拟;Box Constraint 模型自顶向下约束 + 自底向上 size O(n) DFS + tight/loose/bounded 三类约束传播;build→layout→paint→composite 4 阶段渲染管线 vsync 信号触发 + Impeller 默认渲染器 + Skia WASM web fallback;5 个独立 Dart demo 覆盖三棵树 + 约束模型 + 完整管线) | Dart | 2026-09-11 |
| 032 | `04-移动开发/03-跨平台/Kotlin-Multiplatform/expect-actual/` | KMP expect/actual 声明合并机制(expect 函数 randomUUID + 属性 platformTag + 类 AtomicRef typealias + 枚举 LogLevel + 函数 writeLogMessage 5 种声明类型全部覆盖;commonMain 仅声明无实现、androidMain 走 java.util.UUID + AtomicReference typealias + android.util.Log、iosMain 走 platform.Foundation.NSUUID + synchronized AtomicRef + NSLog;commonMain/iosMain intermediate source set 层级 + iosArm64Main/iosX64Main/iosSimulatorArm64Main 平台 source set 共享 iosMain;Main.kt 不感知平台差异直接调用 expect 声明) | Kotlin | 2026-09-11 |
| 033 | `05-AI与机器学习/01-深度学习/反向传播/` | 反向传播链式法则(d2l.ai §5.3 公式 5.3.1~14:1-hidden-layer MLP 前向 z=W¹x, h=φ(z), o=W²h, L=ℓ(o,y), s=λ/2·(‖W¹‖²+‖W²‖²), J=L+s;反向 ∂J/∂W¹=(∂J/∂z)·xᵀ+λW¹, ∂J/∂W²=(∂J/∂o)·hᵀ+λW², ∂J/∂z=(W²ᵀ·∂J/∂o)⊙φ'(z);中间变量必须缓存以供反向使用;中心差分数值梯度 max err < 1e-7;mini-batch SGD 200 步 acc 0.25→1.00 验证学习能力) | Python | 2026-09-11 |
| 034 | `05-AI与机器学习/01-深度学习/卷积/` | 2D 卷积 im2col + col2im 完整 forward/backward(arXiv 2408.12561 §Preliminaries Eq 3/4/5:X (N,C,H,W) → im2col → cols (C·kh·kw, N·Hout·Wout),W 展平为 W_col (Cin·kh·kw, Cout),Y_col = W_col·cols = GEMM;反向 dW = cols·dY_colᵀ, db = ΣdY, dX = W_col·dY_col 后 col2im 累加回输入;零拷贝视图用 np.lib.stride_tricks;col2im 必须 += 而非 = 因同一输入像素被多个输出窗口共享;中心差分 dW max err ≈ 1e-12) | Python | 2026-09-11 |
| 035 | `05-AI与机器学习/01-深度学习/注意力/` | Scaled Dot-Product + Multi-Head Self-Attention(Vaswani et al. 2017 *Attention Is All You Need* §3.2 公式 1: Attention(Q,K,V)=softmax(QKᵀ/√d_k)V;公式 2: MultiHead=Concat(head₁..head_h)·W^O;公式 3: head_i=Attention(Q·W_i^Q, K·W_i^K, V·W_i^V);缩放因子 √d_k 抵消点积方差 d_k 增长防止 softmax 饱和;h=8, d_k=d_v=d_model/h=64 基础模型;causal mask 把上三角设 -∞ 保证 decoder 位置 i 只见 0..i;三种使用方式 encoder-decoder/encoder self/decoder self attention) | Python | 2026-09-11 |
| 036 | `05-AI与机器学习/02-强化学习/Q-Learning/` | Q-Learning 表格方法(Watkins & Dayan 1992;off-policy Bellman 更新 Q(s,a)←Q(s,a)+α[r+γ max_a' Q(s',a')-Q(s,a)];epsilon-greedy 探索 π_e=(1-ε) argmax Q + ε uniform;自纠正性:高估 Q 值被探索后自动下调;γ=0 时无法传播目标奖励;4x4 GridWorld 1000 ep 成功率 89%;5 demo: basic 收敛/epsilon 对比/gamma 对比/自纠正性/学习曲线) | Python | 2026-09-12 |
| 037 | `05-AI与机器学习/02-强化学习/REINFORCE/` | REINFORCE 策略梯度(Williams 1992;策略梯度定理 ∇J=E[Σ∇log π(a_t|s_t)·A(s_t,a_t)];对数导数技巧将期望梯度转化为对数概率梯度;softmax 线性策略 ∂log π(a|s)/∂θ[s][a']=I(a'=a)-π(a'|s);rewards-to-go G_t=Σγ^{k-t}r_k 因果性只用 t 以后奖励;baseline V(s) 不改变期望但降低方差 E[∇log π·G]=E[∇log π·(G-b(s))];5 demo: 无 baseline/有 baseline/方差对比/折扣因子影响/学习曲线) | Python | 2026-09-12 |
| 038 | `05-AI与机器学习/02-强化学习/PPO/` | PPO 近端策略优化(Schulman et al. 2017 arXiv:1707.06347;裁剪替代目标 L^clip=E[min(r_t·A_t, clip(r_t,1-ε,1+ε)·A_t)] 取悲观下界;策略比率 r_t=π_θ/π_{θ_old};多 epoch 复用同一 batch vs REINFORCE 只用一次;GAE 优势 Â_t=Σ(γλ)^l δ_{t+l} 平衡偏差与方差;价值函数 MSE L^critic=E[||V_ϕ(s_t)-R̂_t||²];ε=0.2 默认值;5 demo: basic 训练/clip 参数对比/策略比率分布/优势估计/价值损失追踪) | Python | 2026-09-12 |
| 039 | `05-AI与机器学习/03-LLM/BPE分词/` | BPE 分词器(Gage 1994 压缩算法 → Sennrich et al. ACL 2016 arXiv:1508.07909 引入 NMT 子词分段;训练 = 词频表上迭代合并最高频相邻符号对,`</w>` 词尾标记区分词内/词边界;编码 = 按 merge 规则学习顺序 rank 贪心应用而非按新文本频率,保证切分唯一确定;经典语料 low/lower/newest/widest 第一轮 (e,s) 9 次 → es;未见词 lowest → low + est</w> 泛化;byte-level BPE(GPT-2)以 256 字节为基础词表理论 OOV 为零;训练朴素 O(N·V) 优先队列 O(N log V);3 demo: 经典训练/未见词编码/词表-序列长度权衡) | Python | 2026-09-12 |
| 040 | `05-AI与机器学习/03-LLM/KV-Cache/` | KV Cache 与自回归解码 + PagedAttention(Kwon et al. SOSP 2023 arXiv:2309.06180 + vLLM 官方博客;历史 token K/V 不随新 token 变化 → 缓存后每步投影 O(n²d)→O(nd),QK 点积 O(n) 仍在省的是 K/V 重复投影;两版输出逐元素一致 1e-9 容差 + 乘加计数对比;显存公式 2×seq_len×layers×n_kv_heads×head_dim×dtype_bytes,LLaMA-13B(MHA 40×40×128) 2048 token ≈1.68GB 与官方 1.7GB 一致,Llama-3-8B GQA(32×8×128)同长度仅 0.27GB;PagedAttention = OS 虚拟内存分页:块=页/token=字节/序列=进程/块表=页表,逻辑连续物理不连续按需分配,浪费仅最后一块尾部 <4% vs 预分配 60-80%;并行采样 CoW 共享省 55%;3 demo: 一致性与计算量/显存账单/分页模拟) | Python | 2026-09-12 |
| 041 | `05-AI与机器学习/03-LLM/采样策略/` | 解码采样策略 temperature/top-k/top-p(HF 官方博客 How to generate + Inference for PROs + GenerationConfig 文档;自回归 P(w_1:T|W_0)=∏P(w_t|w_1:t-1,W_0);temperature p=softmax(z/T) T<1 更尖 T→0 退化为 greedy(HF 官方确认等价);top-k 固定 k 候选(常用 5-50)尖峰分布仍带长尾/平坦分布过早截断;top-p nucleus 按概率降序累积到质量 p 为止候选数动态,尖峰留 1-2 个平坦留全部;截断后必须重归一化;min_tokens_to_keep=1;softmax 减 max 防溢出;贪心 GPT-2 复读现象是采样策略存在理由;4 demo: 温度扫描与熵/top-k 行为/top-p 动态候选/20000 次采样频率近似概率) | Python | 2026-09-12 |
| 042 | `05-AI与机器学习/04-计算机视觉/Sobel边缘检测/` | Sobel 边缘检测(OpenCV 2.4 Sobel Derivatives 官方教程出自 Learning OpenCV;Gx=[[-1,0,1],[-2,0,2],[-1,0,1]] 检测垂直边缘/Gy 转置检测水平边缘;梯度幅值 G=sqrt(Gx²+Gy²) 工程近似 |Gx|+|Gy|;方向 θ=atan2(Gy,Gx) 垂直边缘走向;Sobel=高斯平滑+微分联合对噪声鲁棒;OpenCV filter2D 执行相关而非卷积,Gx 不对称两者差一符号;CV_8U 截断陷阱:负梯度直接存 uint8 全变 0 白→黑边缘消失,须 CV_16S/CV_64F→abs→uint8;ksize=3 精度不足时 Scharr [-3,0,3] 更准;4 demo: 垂直边仅 Gx 响应/水平边仅 Gy/方块四边+左右边缘 Gx 符号相反/CV_8U 截断陷阱) | Python | 2026-09-12 |
| 043 | `05-AI与机器学习/04-计算机视觉/Hough直线检测/` | Hough 变换直线检测(Hough 1962 专利 + Duda & Hart 1972 极坐标化;ρ=x·cosθ+y·sinθ 绕开 y=mx+b 垂线斜率无穷大;图像点↔参数空间正弦曲线对偶,共线点曲线交汇成峰;累加器行=ρ(1px,上限对角线长,须允许负 ρ)列=θ(1°,半平面 [0°,180°));threshold 语义=最少共线点数即最小直线长度;进 Hough 前必须 Canny/阈值二值化;量化误差使峰散落相邻格必须做参数空间 NMS;OpenCV 教程 100x100 水平线 (ρ=50,θ=90°) 完整算例;概率 Hough 随机采样降计算量 + minLineLength/maxLineGap;4 demo: 水平线 (ρ=6,θ=90°)/对角线 y=x → (ρ=0,θ=135°)/方块轮廓 4 峰/断线缺口 4 点仍检出) | Python | 2026-09-12 |
| 044 | `05-AI与机器学习/04-计算机视觉/IoU与NMS/` | IoU 与非极大值抑制 NMS(torchvision.ops 官方文档:nms "iteratively removes lower scoring boxes which have an IoU greater than iou_threshold with another (higher scoring) box",严格大于;框格式 (x1,y1,x2,y2);贪心:按分数降序保留最高分并抑制 IoU>thr 的剩余框;IoU=交/并,union=area_a+area_b−inter 容斥,union>0 防零面积除零;batched_nms offset 技巧:每类框加 idx×(max_coord+1) 偏移使异类框几何永不相交,一次全局 NMS 等价逐类;逐类 NMS 不能省否则低分类别被吃掉;平票 CPU/GPU 保留哪个不保证一致(文档明示);典型 thr 0.45-0.5 密集场景调 0.6;4 demo: torchvision 文档标准例 5 框保留[0,3]/阈值敏感性/逐类 NMS vs offset 技巧等价性/退化情形(相同框/内含框/零面积/平票)) | Python | 2026-09-12 |

---

> 写入规则:每 demo 完成后 append 一行(用现有 ID 续号)。监控:agent 巡查时若行数 > 100 → 触发 rotate(见 STATE.md §4)。
| 045 | `06-DevOps/01-容器化/Namespace隔离/` | Linux namespaces(man7 namespaces(7):8 类 ns 表 + unshare/clone/setns 三 API;unshare(2):CLONE_NEW* flags 表、EINVAL/EPERM 条件、uid_map"新ns起始ID 父ns起始ID 长度"三字段格式、setgroups=deny 前置、NEWUSER 授予新 ns 全套 capabilities 且同调用免 CAP_SYS_ADMIN;pid_namespaces(7):CLONE_NEWPID 只影响后续子进程、PID ns 单向不可回退、新 ns 须重挂 procfs;3 demo: UTS 父子主机名隔离/user ns 无特权全套 caps/PID ns 子进程 PID=1+新 procfs;坑:多线程进程 unshare(NEWUSER) EINVAL、挂 procfs 前须 MS_REC|MS_PRIVATE 断传播、Go 无 fork 用 exec 自我复制+SYS_SETHOSTNAME 直调) | C / Python / Go | 2026-09-12 |
| 046 | `06-DevOps/01-容器化/Cgroups-v2/` | cgroup v2(docs.kernel.org admin-guide/cgroup-v2:单一层级 mount -t cgroup2、subtree_control 写"+cpu +memory"为子代启用、cgroup.procs 写 PID 即迁移、/proc/pid/cgroup 格式"0::$PATH"、内部进程约束(非根组有进程不可启用 domain 控制器)、层级收紧;cpu 控制器:cpu.max"$MAX $PERIOD"带宽配额默认"max 100000"、cpu.weight 1-10000 默认 100、cpu.stat 的 usage_usec/nr_periods/nr_throttled/throttled_usec、内核 cfs_bandwidth 按周期发放配额耗尽进 throttled 队列;memory 控制器:current 含匿名页+page cache+内核结构+TCP 缓冲、min/low 保护、high 软限节流不杀、max 硬限先回收后 OOM kill、events 计数 oom_kill;3 demo: 建组自迁移观察 current/16MiB max 触发 OOM kill/50% 配额忙循环限流;坑:有进程的组 rmdir EBUSY 须先迁回 root、malloc 不触碰不记账、控制器写在父组 subtree_control) | C / Python / Go | 2026-09-12 |
| 047 | `06-DevOps/01-容器化/OCI镜像格式/` | OCI image-spec(specs.opencontainers.org:config.md 的 DiffID=sha256(未压缩 tar)≠blob digest=sha256(压缩后内容)、ChainID 递归 ChainID(L0)=DiffID(L0) 且 ChainID(前缀|Ln)=Digest(前缀+" "+DiffID(Ln)) 空格参与哈希、ImageID=SHA256(config JSON) 内容寻址、rootfs.diff_ids 自底向上;manifest.md:schemaVersion 必须为 2 兼容 Docker、layers[0]=base 栈序、descriptor{mediaType,digest,size}、层媒体类型 tar/+gzip/+zstd;layer.md:.wh.<name> 白出只删父层条目、.wh..wh..opq 隐藏父层目录全部子项、目标已存在语义=unlink+重建、白出标记应用后必须隐藏;demo 从零构建 3 层镜像(base/app 含 whiteout/cleanup 含 opaque)→写 image-layout(blobs/sha256+index.json+oci-layout)→重读校验 digest/DiffID/ChainID→按层序解包比对文件树;坑:DiffID 与 blob digest 混淆、ChainID 空格、gzip mtime 不定则 digest 不可复现、config JSON 序列化字节参与 ImageID) | Python / Go | 2026-09-12 |
| 048 | `06-DevOps/02-CI-CD/DAG流水线调度/` | CI 流水线 DAG 调度(GitLab docs needs 语义: stage barrier 全部完成才进下一 stage vs DAG 前置一完成立即启动、needs:[] 立即运行、扇出/扇入/菱形、optional 可选依赖;GitHub Actions docs: jobs 默认并行+needs 数组依赖、失败则依赖者跳过除非 if:always();Kahn 分层拓扑:入度计数+队列、出队数<节点数即环、O(V+E);关键路径 ef[j]=dur[j]+max(ef[need]);GitLab 博客 A1min/B5min/C只依赖A算例 barrier 8min vs DAG 4min;4 demo: 菱形分层+关键路径/环检测创建即失败/barrier vs DAG/失败传播 SKIPPED;坑:同一层输出顺序不唯一须排序、环是创建时错误非运行时、wall-clock 节省≠计算量节省) | C / Python / Go | 2026-09-12 |
| 049 | `06-DevOps/02-CI-CD/内容寻址缓存/` | CI 构建缓存(actions/cache 官方: key/restore-keys/path 三输入、key≤512 字符、精确命中 cache-hit=true、miss 时 job 成功后自动新建、缓存创建后不可修改只能新 key、restore-keys 有序前缀逐个尝试取最近创建的部分匹配、单仓库 5GB 上限按最近访问淘汰+一周未访问也淘汰、敏感数据禁入缓存因 PR 可读;caching-strategies.md: 锁文件哈希作 key 是最常见策略、OS 隔离 key、run_id/commit SHA 短生命周期缓存;demo 实现 sha256 键派生(hashFiles 语义,C 版手写 FIPS 180-4 SHA-256 已对照 hashlib 验证)+精确/部分/未命中三级查找+LRU+TTL 淘汰;坑: cache-hit 是字符串'true'非布尔、restore-keys 前缀从长到短、部分命中仍要执行构建只是打底) | C / Python / Go | 2026-09-12 |
| 050 | `06-DevOps/02-CI-CD/蓝绿与金丝雀发布/` | 部署策略(Argo Rollouts 官方: Recreate 先删旧再启新两版本从不共存但必然停机、RollingUpdate 逐实例替换无停机但共存、BlueGreen 双 RS 并存只切 activeService selector 回滚瞬时因旧 RS 未缩容、Canary setWeight 步进+AnalysisRun 指标门控自动晋升/回滚即渐进式交付;官方选型表: 蓝绿无需流量管理器/兼容队列 worker/爆炸半径大,金丝雀需流量管理器做细粒度/爆炸半径低、无管理器时权重粒度=副本粒度;4 demo 用统一 LCG 模拟 1000 请求: recreate 10% 停机窗口 vs rolling 0 错误、蓝绿坏版本检测后瞬时切回零额外错误、金丝雀 20% 错误率 10% 步即中止、好版本三步晋升;坑: 数据库 schema 须双向兼容否则切回失败、分析门控要时间窗口聚合防低流量扰动) | C / Python / Go | 2026-09-12 |
| 051 | `06-DevOps/03-监控与可观测性/Prometheus指标模型/` | Prometheus 数据模型与 exposition 文本格式 0.0.4(官方 exposition_formats: 行导向 \\n 分行、# HELP 文档串只转义 \\\\ 与 \\n、# TYPE 必须 counter/gauge/histogram/summary/untyped 且出现在首个样本之前否则 untyped、样本 EBNF name{labels} value [ts]、标签值仅 \\\\ \\" \\n 三种转义、value 按 Go ParseFloat 且 NaN/+Inf/-Inf 合法、timestamp int64 毫秒、同指标行须连续成组且 name+labels 唯一否则 undefined;histogram 展开约定 x_bucket{le} 累积桶+x_sum+x_count、必含 le="+Inf" 且值==_count、桶按上界升序;内容协商: text 0.0.4 为兜底、OpenMetrics 1.0 含 UNIT/INFO/# EOF、OM 2.0 CompositeValue 单行+st@ 起始时间戳;demo 解析官方完整示例并校验三条不变式+转义 round-trip+无 TYPE 默认 untyped+NaN/+Inf;坑: 毫秒(text) vs 秒(OpenMetrics)、从右往左切 value/ts 因标签值可含空格花括号) | C / Python / Go | 2026-09-12 |
| 052 | `06-DevOps/03-监控与可观测性/分布式追踪与TraceContext/` | W3C Trace Context(W3C REC trace-context-1: traceparent=version-trace_id-parent_id-trace_flags 四段 ABNF、trace_id 32 小写 hex 全链路不变、parent_id 16 hex 每跳换成本跳 span_id、trace_id/parent_id 全零非法 MUST ignore、version ff 禁止、version>00 布局匹配则按 v0 解释、flags 位域须掩码 (flags&0x01)==0x01 禁止 ==1 判等防 0x03 漏判、flags 是建议非强制;tracestate 厂商键值最多 32 项最左为 traceparent 书写方;OTel: Trace=Span 的 DAG、Span{name,trace_id,span_id,parent_id,start/end,attributes,status}、Context+Propagation 两子概念、TracerProvider/Tracer/Exporter 三层;demo 模拟 client→A→B→C 逐跳传播还原 Span 树+5 类非法头拒绝+位掩码+非法上游静默忽略另起新 trace;坑: 大写 hex 非法(HEXDIGLC)、非法≠报错而是静默重开、span 命名低基数 get_account 合理 get_account/42 反模式) | C / Python / Go | 2026-09-12 |
| 053 | `06-DevOps/03-监控与可观测性/直方图分位数估算/` | histogram_quantile 插值算法(官方 functions 页: φ<0→-Inf φ>1→+Inf φ=NaN→NaN、桶数<2 或缺 +Inf→NaN、桶内均匀分布假设线性插值 rank=φ×total、最高桶特例返回次高桶上界不外插、最低桶上界>0 时下界假设 0、非单调累积计数先忽略相对差<1e-12 浮点噪声再强制抬升并打 annotation;practices/histograms 220ms 尖峰算例: 桶{0.1,0.2,0.3,0.45} 全落 (0.2,0.3] p95=0.2+0.1×0.95=0.295 误差 75ms 且逼近 SLO 错觉、+100ms 后跳 (0.3,0.45] 估算不连续;native histogram factor 1.1 同场景误差仅 ≤19ms 指数桶+指数插值;summary 分位数 avg 统计无意义 BAD! 须 sum by (le) 后 quantile;坑: 桶边界按 SLO 密集布、rate() 先于聚合、histogram_quantile(0/1) 得估算 min/max) | C / Python / Go | 2026-09-12 |
