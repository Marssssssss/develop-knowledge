| 2026-09-14 18:19 | 35 | M=二进制逆向第二批 5 demo:x86-64 变长指令编码(legacy prefix 四组 0xF0/0xF2/0xF3+2E/36/3E/26/64/65+0x66+0x67;REX 0x40-0x4F=0100WRXB 必须紧跟 opcode;ModRM mod2/reg3/rm3 + SIB scale2/index3/base3;四分支顺序 mod==3→rm==4 走 SIB→mod==0&&rm==5 是 RIP 相对→普通内存;**64 位下寻址寄存器恒为 64 位,REX.W 只管数据宽度**,否则 ff 54 24 08 会错渲染成 [esp+0x8];SIB index=100 无变址、base=101&&mod=00 无基址;上限 15 字节) + ptrace 软件断点调试器(man7 ptrace(2) 全文:TRACEME/ATTACH/SEIZE+PEEKTEXT/POKETEXT+GETREGS/SETREGS 改 rip+SINGLESTEP+__WALL+TRACESYSGOOD+Yama ptrace_scope 四档;**四步生命周期** 埋 0xCC(INT3)→SIGTRAP 且 rip 越过后 1 字节→① 恢复原字节 ② RIP 回退 1 ③ 单步执行原指令 ④ 重埋;缺步后果实测:不恢复则单步再踩 0xCC 自锁死循环,不回退则从指令中间解码边界错位) + 栈溢出与 ROP(Phrack 49/14 + Eli Bendersky + Shacham CCS 2007 + Intel SDM CET:栈帧 [7+参数][返回地址][SFP][局部变量][red zone 128B]、前 6 参数走 rdi/rsi/rdx/rcx/r8/r9;NX 只挡代码注入不挡代码重用;ASLR 挡硬编码但相对偏移不变;**ret 即 pop rip、rsp 即程序计数器**;x86 无对齐 + 密集变长 ⇒ 非对齐 gadget 取之不尽;ret2libc/ret2plt+泄露/SROP/JOP/Blind ROP;CET SHSTK 与 IBT ENDBR64 F3 0F 1E FA;坑:rsp%16 对齐否则 movaps 崩、-fstack-protector 只护 char 数组) + Mach-O 文件解析(aidansteele 参考 + Apple loader.h:mach_header_64 32B magic=0xFEEDFACF;**64 位 cmdsize 必须 8 字节对齐**;segment_command_64 72B=+nsects×80;section_64 80B flags 低 8 位节类型高 24 位属性;__PAGEZERO vmsize 大而 filesize=0;**节编号从 1 起跨段连续**;union lc_str 存相对命令起点偏移、LC_LOAD_DYLIB 顺序即 ordinal) + 控制流图与结构化反编译(Nystrom Cornell §2.1 + Cifuentes 1994 + Stitt&Vahid 2007:leader 切块 + 虚拟 entry/exit;支配关系自反/传递/反对称 ⇒ 唯一 idom 构成支配树,朴素 O(V²)/Lengauer–Tarjan O(E·α);回边 (n→h) 且 h 支配 n、自然循环、循环头;可归约 ⟺ 回退边全是回边 ⟺ 唯一入口;Cifuentes 通用结构集 + **follow 节点 = 两分支第一个共同汇合点**;区间分析法折叠顺序决定嵌套层级;边界:结构化 NP-完全、反编译不可判定) | S2=类目拓展+1(10-逆向工程/协议逆向,登记索引 46) + S3=索引同步(01-二进制逆向/README demo 索引 162-166 + 待研究勾 5 项 + 参考资料 +5;10-逆向工程/README 子领域行 + 待研究勾 5 项 + 参考资料 +6)+归档 rotate(completed 105→100+append 5→105 / schedule 26+1→27) | +5 | 166 | 二进制逆向领域第二批 5 demo(编码层→调试层→利用层→格式层→分析层,补齐静态/动态/利用/图纸四类主干);C/Python/Go 8 源文件 + 5 README + 03-协议逆向 README + 3 个上级 README 同步;权威来源 8 WebFetch(OSDev X86-64 Instruction Encoding + man7 ptrace(2) + Eli Bendersky x86-64 栈帧 + aidansteele Mach-O + Intel SDM + salesforce/ja3 README 全文 + Cornell Nystrom + Cifuentes PDF)+ 5 WebSearch(Computer Networks PRE 综述 + BinPRE arXiv 2409.01994 + PRE-list 71 篇合集 + ROP/CFG 支配树);**OPTIMIZATION §1.1 单文件上限修复 7 处**:macho_parser.py 406→拆 macho_defs.py 198/macho_parser.py 229、macho_parser.go 486→拆 macho_defs.go 251/macho_parser.go 249、cfg_builder.py 473→拆 cfg_core.py 180/cfg_struct.py 186/cfg_builder.py 145、dominator.go 374→拆 cfg_graph.go 149/dominator.go 237、modrm_decoder.c 312→300、3 个 README 235/235/234→199/200/186;Python ×7 py_compile 全干净,5 demo 实跑自测全通过(insn_decoder 10 用例;breakpoint_lifecycle 忠实执行 0xCC 并与"不回退 PC"/"不恢复原字节"两种错误实现对拍;rop_chain_sim 4 场景含 SHSTK #CP 与 16B 对齐;macho_parser 4 段 4 节编号 1..4 连续;cfg_builder 三程序输出 while(!Z)/if(Z)/违规边 B3→B2);C/Go 走人工代码审查(本机无 gcc/go 工具链);大类轮换 ✓ 上轮 09-语言学习→本轮 10-逆向工程;轮间保险通过(last_run 17:11 距 2026-09-14 18:01 = 50 分钟 > 45);占位锁 6e4bfce + 主轮 225c2ed push OK;**notify: ok** HTTP 200 errcode=0(命令替换捕获响应体,单次调用)
| 2026-09-14 20:16 | 37 | M=eBPF 动态追踪(8 字节定长指令 LD/LDX/ST/STX/ALU/JMP/JMP32/ALU64 八类,LD 与 ST 类第 2 字节是 mode<<5 与 size<<3 而非 code/source——本 demo 编码自检即在此踩坑;r10 只读指向 512B 栈顶故栈访问是负偏移;验证器对 (pc,寄存器类型格) 做工作队列抽象解释,有界循环须证出上界否则拒载;"安全性工具不是安全工具";JIT + helper + tail call 33 层;kprobe 把首字节换 int3(0xCC)、触发后换回单步再重埋,与 ptrace 软件断点同构;kretprobe 用 maxactive 实例池 + trampoline 替换返回地址)+ftrace 内核追踪(编译期入口埋 nop、启用时 text patch 成跳转故未启用开销≈0;set_ftrace_filter 命中谁就只改谁;function_graph 入口 ` {` 出口 `}`、**出口行 duration 才是 inclusive**、overhead 标记 $>1s @>100ms *>10ms #>1000µs !>100µs +>10µs;trace 非消费者 O_TRUNC 即清空 vs trace_pipe 是消费者无数据阻塞;trace_pipe_raw + splice 走二进制;per_cpu/cpuN/stats 的 overrun 才是丢事件的权威字段;error_log 仅存最近 8 条;function_profile_enabled → trace_stat/function<cpu>)+PMU 硬件计数器(man7 perf_event_open(2) 全文;pid/cpu 语义表;PERF_ATTR_SIZE_VER0=64→VER5=112 做兼容;缓存事件 config = id | op<<8 | result<<16,未命中率 = MISS/ACCESS;**多路复用缩放为 demo 核心**,以 time_enabled != time_running 判定,整数版 count=(count/running)*enabled+(rem*enabled)/running;mmap 1+2^n 页 + 元数据页 rdpmc seqlock 快路径 + pmc_width 符号扩展;precise_ip 0-3 控 skid;ENOSPC)+LoadAverage 与 PSI(Gregg 溯源:1993 Linux 0.99.14 补丁把 D 状态计入 active=nr_running+nr_uninterruptible,理由“快 swap 换慢 swap 时负载反而下降”;0.99.14 仅 13 条路径置 TASK_UNINTERRUPTIBLE,4.12 已近 400 条含部分锁原语;三数是 5 秒周期定点 EMA 而非算术平均,FIXED_1=2048 EXP_1/5/15=1884/2014/2037;60 秒时只到约 0.62;PSI some/full 区分“忙”与“卡”,CPU full 系统级未定义但 5.13 起填 0;trigger `<some|full> <us> <窗口 us>` 窗口 500ms~10s、非特权须 2s 整数倍、同一 fd 一个 trigger 再写 EBUSY、poll 等 POLLPRI、源消失 POLLERR;cgroup2 有 *.pressure 而 loadavg 无法归因到容器)+页缺失与内存剖析(minor 不需磁盘 I/O vs major 需 I/O,差异在“要不要等 I/O”而非“是否首次”;getrusage ru_minflt/ru_majflt + /proc/self/stat 第 10/12 字段 + PERF_COUNT_SW_PAGE_FAULTS_MIN/_MAJ;COW 首次写各产生一次 minor、MAP_POPULATE 把 fault 提前、MADV_DONTNEED 让其重现;**PSS 按共享者均摊 1000+1000/2=1500**,私有/共享判定依据是映射数而非 MAP_SHARED;statm 四字段恒 0 属历史遗留;Valgrind Massif 采样倍增抽稀 + 峰值只在释放后才可能记录故约 1% 欠估)五 demo | S2=类目拓展+1(11-性能分析/基准测试方法论,登记索引 47,取模基数 48) + S3=索引同步(STATE §一 索引表 +1/§二 next_index=38+last_top=11-性能分析/§三 滚动窗口+本轮并压缩旧行守 5KB;11-性能分析/README 子领域表+待研究+参考资料;01-系统级剖析/README 5 demo 索引 097-101+待研究勾 5 项+参考资料 +9) | +5 | 171 | eBPF/ftrace/PMU/PSI/页缺失 五个系统级剖析第二批 demo,eBPF 与 ftrace 两道内核追踪主干 + PMU 硬件口径 + loadavg/PSI 压力指标 + 内存与页缺失口径;权威来源 9 WebFetch(docs.kernel.org ftrace.html 全文 + ebpf.io/what-is-ebpf + docs.kernel.org bpf/standardization/instruction-set.html 全文(首次 docs.kernel.org/bpf/instruction-set.html 404 后改路径命中) + docs.kernel.org/trace/kprobes.html + man7 perf_event_open(2) 全文 + docs.kernel.org/accounting/psi.html 全文 + brendangregg.com/linux-load-averages + docs.kernel.org/filesystems/proc.html + valgrind.org ms-manual.html) + 2 WebSearch(JMH 微基准陷阱/hyperfine 统计与离群点,供 S2 新子类目)+ 0 agentic_search;Python ×5 全部实际运行自测通过(mini_bpf.py 9 个 opcode 断言 + 1 PASS/3 REJECT + 执行 count_events=100;funcgraph_parser.py Σself == Σroot_inclusive == 12802.100 us;pmc_multiplex.py 缩放估算平均误差 1.70%;loadavg_calc.py 60 秒 0.664、D 状态场景 load1 峰值 1.90 而 nr_running=0;smaps_pss.py PSS=1500 与 ΣRSS 19456 vs ΣPSS 15360);开发期修复 bug 7 处(① BPF_LDX_MEM/STX 用 _op(cls,code,source) 算错位,LD/ST 类第 2 字节是 mode<<5 与 size<<3,新增 _mem_op 后 ldxdw 期望 0x79 命中;② 验证器 reads 构造错:STX 同时读 dst 与 src、MOV 同源为 K 时**完全不读 dst**,修正后 count_events 通过;③ prog_stack_oob 原本因 r0 未初始化被拒而非栈越界,调整指令顺序后命中 stack access -520 outside [-512,-1];④ Go 验证器把 switch 嵌进外层 switch 的 case 内不合法,改为 if + 顺序 switch/if;⑤ Go BPFJa 反向跳转原本不 enqueue 目标、条件反向跳转漏做有界循环检查,修正为统一 target=pc+1+offset 并跑 boundedLoop;⑥ hw_cache_encode.go 删掉未用 rawValue 与 bool 作 map key 的不可编译写法;⑦ smaps_pss.py summarize 返回 dict 被当元组解包报 too many values to unpack;另修 pmc_multiplex.py 两次"误差 0.00%"——常量速率与 BURST_PERIOD=20000 恰为 5 个轮转周期都会让估算精确,改用 BURST_PERIOD=3300 与每事件 phase 后得到诚实的 1.70%);单文件行数审计并整改 4 个超限(ftrace README 207→193、PMU README 229→200、LoadAverage README 201→200、页缺失 heap_sampler.go 303→298,全部 ≤ OPTIMIZATION §1.1 上限);C/Go 走人工代码审查(本机无 gcc/Go 工具链,which gcc cc clang go 均 exit 127);大类轮换 ✓ 上轮 10-逆向工程→本轮 11-性能分析(next_index 36 同大类 10 顺延至 37,不记 skip);并发防护 §11.2 通过(last_run 2026-09-14 18:01 距 20:00 = 119 分钟 > 45 阈值)+ 槽位自检 ✓(shell date 实得 2026-09-14 20:00:37 +0800,20:00 槽 ±2 分钟内,无 slot_mismatch);占位锁 auto: 2026-09-14 20:01 push OK; **notify: ok** HTTP 200 errcode=0 errmsg=ok(curl 命令替换捕获响应体,单次调用); |
| 2026-09-14 22:02 | 39 | 经典机器学习第二批 5 demo:朴素贝叶斯(scikit-learn §1.9 + `_joint_log_likelihood`:条件独立 P(y|x) ∝ P(y)·∏P(xi|y) + 三变体 Gaussian/Multinomial/Bernoulli + 对数域 logsumexp + Laplace/Lidstone/MLE 三档平滑;**未平滑的 −inf 灾难**——ham 类从未见过 win ⇒ logθ=−inf ⇒ P(ham|x)=0 整类后验被钉死,实测 joint_ll=[−inf,−2.5337] P(spam)=1.000000 vs 平滑后 0.781250;**0×(−inf)=NaN 陷阱**——多项式模型计数为 0 的维度必须跳过;sklearn 原文»decent classifier but a bad estimator«) + 支持向量机(Platt 1998 + Fan/Chen/Lin WSS1:对偶 + KKT 三档互补松弛 + 箱约束 [L,H] 来自 yᵀα=0 斜线与 [0,C]² 相交 + η=2Kij−Kii−Kjj≤0 解析牛顿步 + **WSS1 最大违反对取代 Platt 启发式遍历**——实测遍历在第 13 轮误判收敛(w 只更新一半、支持向量上 y·f=1.38),换 WSS1 后 KKT 违例清零;b 区间精化 L=max_{I_up}(y−g)≤b≤min_{I_low}(y−g)=U;核 linear/poly/rbf/sigmoid;实测 w=[+0.2738,+0.2707] b=−0.0340 margin=5.1951、2/40 SV、C 从 0.01→100 时 SV 64→24;XOR 线性核 w≡[0,0] 是真实退化最优解) + 随机森林(Breiman 2001 + sklearn §1.11:bootstrap + OOB(1−(1−1/N)^N→1−1/e≈0.632,无需留出集但略微偏悲观且无偏)+ margin 函数 mg=av_k I(h_k=Y)−max_{j≠Y} av_k I(h_k=j) 与泛化界 PE*≤ρ̄(1−s²)/s² + 随机特征子集压低 ρ̄ + 排列重要性 vs 基尼重要性;实测 test 0.9000/OOB 0.9667、strength 0.7954、树数 1→300 OOB 误差 0.2105→0.0333、bootstrap=False 时 OOB=nan) + 提升算法(sklearn `_weight_boosting.py` 源码 + Friedman 2001/2002:AdaBoost.SAMME α=lr·(log((1−err)/err)+**log(K−1)**),log(K−1) 把“比随机猜好”统一成 err<1−1/K;权重 exp 更新后**必须归一化**;两道早停闸 err≤0→α=1.0 停 / err≥1−1/K→丢弃;GBDT 是函数空间梯度下降、h_m 拟合负梯度、最小二乘即残差、F₀=ȳ、子学习器**始终是回归器**;坑:XOR 上树桩 AdaBoost 只有 0.59,换同心圆后 0.79→0.975;GBDT lr=0.1/#300 MSE 0.000092、lr=0.01/#800 0.000329、subsample=0.5 训练 MSE 0.000272) + 模型评估(sklearn metrics §3.3/CV §3.1/roc_auc_score/plot_roc:KFold 测试折 [n·i/k,n·(i+1)/k) 复现 docstring 例;混淆矩阵**行=真实类**(搞反 precision/recall 对调);P/R/F_β;micro/macro/weighted 三口径二分类下并不相等(0.8977/0.9011/0.9000);**AUC 三算法恒等**——梯形积分 0.9809253247 / 秩和 Mann-Whitney(并列须取平均秩)/ Bamber 数有序对,差 0.00e+00;Gini=2·AUC−1 只是线性重标定;多分类 OvR macro=(1/(c(c−1)))ΣjΣ_{k>j}(AUC(j|k)+AUC(k|j))、micro TPR=ΣTPc/Σ(TPc+FNc);K=3 macro 0.931895/micro 0.932511;5 折 CV 0.8465±0.0463 / AUC 0.9346±0.0187) | S2=类目拓展+2(05-AI与机器学习/07-特征工程与降维、08-概率图模型,登记索引 48/49,取模基数 50) + S3=索引同步(STATE §一 索引表 +2 行并合并为每行 2 条以守 5 KB;§二 last_run/last_top;§三 滚动窗口新增本轮并压缩旧 5 行至 ≤120 字;05-经典机器学习/README 补 5 demo 行 + 4 项待研究勾选 + 参考资料 +12;05-AI与机器学习/README 子领域 +2 行 + demo 索引行)→归档 rotate(completed 105+5→100 / schedule 28+1→29) | +5 | 176 | 经典机器学习第二批 5 demo,与 09-13 首批 5 demo(线性回归/逻辑回归/决策树/KNN/K-Means)合并后该子类目**核心知识点覆盖闭合**(10 demo);源文件 15 个 + README 6 个(Python 292/293/290/295 + 169/172/78 + Go 299 + C 243/280;README 162/163/134/184/200)全 ≤ OPTIMIZATION §1.1 上限(源 ≤300/README ≤200);权威来源 10 WebFetch 全部命中原文章节(scikit-learn naive_bayes §1.9、svm §1.4、ensemble §1.11.7+§1.11.1、AdaBoostClassifier API、model_evaluation §3.3、cross_validation §3.1、plot_roc、roc_auc_score、plot_adaboost_multiclass;`_weight_boosting.py` 原始源码)+ 2 WebSearch(Platt SMO 公式;Fan/Chen/Lin WSS + AUC 秩和公式)+ Breiman 2001 PDF(WebFetch 拒绝 PDF 后改用 pypdf 解析);Python ×7 全部实际运行自测通过(naive_bayes 训练 acc 0.9500/平滑三档 1.0000;svm_smo KKT 违例 [] / margin 5.1951 / rbf XOR 1.0000;random_forest test 0.9000 OOB 0.9667;boosting 单桩 0.8625→200 桩 0.9750;model_eval 与 roc_auc 两个脚本各段自洽);**开发期修复 bug 10 处**(① 朴素贝叶斯 alpha=0 时 math.log(0) 崩 → 引入 _slog 返回 −inf;② 0×(−inf)=nan → _jll 跳过零计数;③ Go 常量不能是 math.Inf(−1) → 改 var;④ Go map 迭代类序随机 → sort.Ints 对齐 Python sorted(set(y));⑤ SMO αj 更新符号写反(aj + yⱼ(Ei−Ej)/η 是往山上走)→ 线搜索验证后改减号;⑥ Platt 启发式遍历第 13 轮误判收敛 → 换 WSS1 最大违反对;⑦ _refine_b 用 α<=1e-6 当“下界集”错误 → 改 L=max_{I_up}(y−g)、U=min_{I_low}(y−g) 取中点;⑧ SMO 每步 O(n²) 刷新误差 → 改传预算 E 向量(13.7s→5.4s);⑨ RegTree 内层 O(n²) 重算 tot_sy → 提到循环外;⑩ AdaBoost 完美拟合时 α=log((1−1e−12)/1e−12) 爆炸 → 按 sklearn 改 α=1.0 并停);**文件行数审计整改 4 处**(naive_bayes.go 342→299 压缩头注释/分节横线/合并 docs 字面量;boosting.py 324→295 压缩 docstring 与模块级空行;模型评估 README 243→200 六轮压缩;另 model_eval.py 419 行按 OPTIMIZATION“考虑拆分”拆为 _synth.py 78 + model_eval.py 169 + roc_auc.py 172);C/Go 走人工代码审查(本机 which gcc/go 均 exit 127,改做花括号/圆括号/方括号配平程序化校验:Go 103/160/131 全平衡);大类轮换 ✓ 上轮 11-性能分析→本轮 05-AI与机器学习(next_index 38 同大类 11-性能分析 顺延至 39,不记 skip);轮间保险通过(last_run 2026-09-14 20:16 距 22:00 约 104 分钟 > 45 阈值);槽位自检 ✓(shell date 实得 2026-09-14 22:00:43 +0800,22:00 槽 ±2 分钟内,无 slot_mismatch);占位锁 5af71f8 push OK;累计 demo 171 → 176(本轮 +5);last_top: 11-性能分析 → 05-AI与机器学习;next_index: 39 → 40;**notify: ok** HTTP 200 errcode=0 errmsg=ok(curl 命令替换捕获响应体,单次调用;发送前已确认本行无既有 notify 记录) |
| 2026-09-15 00:14 | 41 | M=IaC 与配置管理第二批 5 demo:Terraform 远程后端与 state 锁(**锁的底层原语是条件写 / CAS**——S3 用 `If-None-Match: *`、DynamoDB 用 `attribute_not_exists(LockID)`,故 `use_lockfile` 与 `dynamodb_table` 可互换;对象键 `<key>` 或 `env:/<ws>/<key>`,锁对象 `<key>.tflock`;**锁内 `ID` 是 nonce**,release 与 force_unlock 必须携带同一个 ID 否则拒绝;force_unlock 是 advisory 删除、is_stale 按 timeout;实测 6 场景:二次加锁拒 / 正常释放 / workspace 键隔离 / 错 nonce 拒 / 对 nonce 成 / 过期清理)+Ansible 变量优先级(**官方 22 级**完整有序表;总原则 last-write-wins 但 inventory 层级另有 more specific wins;**把"更具体"编码成同层更大排序键** `level = 3 + depth×0.1` 从而与加载顺序无关;`hash_behavior` 的 replace vs merge;实测三层 `all`→`web`→`web_east` 嵌套)+Ansible-Vault 格式(**PBKDF2-HMAC-SHA256 10000 轮 → 80 B 材料切 32/32/16** = aes_key/hmac_key/iv;**AES-256-CTR** + **HMAC-over-ciphertext(encrypt-then-MAC)**,校验用 `hmac.compare_digest`,**必须先验签再解密**;1.1/1.2 两种 header;body 为 hexlify 后 **80 列硬换行**装甲;明文先做 **RFC 5652 PKCS#7 padding**,整块时补一整块;AES 从零实现——S-box 用 p/q 递推程序化生成避免手抄 256 常数、GF(2⁸) `_xtime`/`_mul`、状态列主序,单测锚定 **FIPS-197 附录 C.3** 向量 `8ea2b7ca516745bfeafc49904b496089`;实测往返 / 篡改一字节被拒 / 错密码被拒 / 同明文 4 次加密 4 个不同载荷 / padding 15→16、16→32、17→32)+Pulumi 资源注册与 Preview(**language host / deployment engine / resource provider 三组件**分工,"程序是真跑的"是与 Terraform 的本质差异;**URN** `urn:pulumi:<stack>::<project>::<type>::<name>` 是资源身份,改名=建+删;**Output 的 unknown 在 preview 阶段向上传播** → "(known after apply)" 的来源;依赖 **Kahn 分层**;diff 四态 create/update/replace/delete,`deleteBeforeReplace` 决定先建后删还是先删后建;省略 name 时引擎按 `<type>-<hex>` 自动命名)+GitOps 调和与漂移检测(OpenGitOps v1.0.0 四原则;**每对 (revision SHA, params) 只成功同步一次、失败不自动重试**;调和周期默认 120 s + **60 s jitter**,实测 8 次间隔 120~176 s;门控按优先级串行:无 pending → 未开 automated → 有 live drift 且未 selfHeal → 已同步且未 selfHeal → pair 已失败 → 仅孤儿且 prune 关 → else sync;**`live_drift = bool(stale) and not git_moved`**;Argo CD `selfHeal`/`prune` 默认关;K8s **SSA 的 `managedFields`** 字段所有权,冲突须 `force=True` 夺取,写空值即释放所有权;9 场景全符合) | S2=类目拓展+1(**09-语言学习/Rust**,登记索引 50,取模基数 51;基于 The Book Introduction/§4-01/§4-02/§10-02/§15-00 五节原文归纳)+S3=索引同步(STATE §一 +1 行**并去掉索引表对齐留白**(5161→4915 B 守 5 KB)+§二 next_index=42/last_top=06-DevOps+§三 滚动追加并淘汰最旧行;06-DevOps/05-IaC与配置管理/README 补 demo 107-111 + 待研究勾 4 项 + 参考资料 +7 链接;06-DevOps/README demo 索引行 + 勾选;09-语言学习/README 子目录表加 Rust 行 + 三门语言主线分工说明)→归档 rotate(completed 100+5→105→**tail -100 回 100** / schedule 29+1→30 未触发阈值) | +5 | 181 | IaC 第二批 5 demo,补齐"状态与锁 / 变量与密钥 / 通用语言引擎 / 声明式调和"四条工程主干,与 09-13 首批 102-106(HCL 解析 / 依赖图 / 幂等模块 / 模板引擎 / state diff)合并后该子类目 10 demo;源文件 C 300 + Python 231/201/180+201/300/134+247 + Go 224/251/300/82+229+105/156+276 全 ≤300 行,README 143/136/152/160/155 全 ≤200 行(收尾全量审计一次通过);权威来源 7 WebFetch(developer.hashicorp.com s3 backend 全文、docs.ansible.com playbooks_variables 全文、docs.ansible.com vault_encrypting_content 全文、pulumi.com how-pulumi-works 全文、opengitops.dev 四原则、argo-cd.readthedocs.io auto_sync 全文、kubernetes.io Server-Side Apply 全文)+ 0 WebSearch/0 agentic_search,全部在硬约束内;Python ×5 全部实际运行自测通过(state_lock 6 场景、var_precedence 22 级+三层组、vault_format 往返/篡改/错密码/4 载荷/padding 边界、pulumi_engine URN 文法+preview unknown+依赖双层+自动命名+改名建删+replace 先建后删+重复 URN 报错、gitops_reconcile 9 场景);开发期修 bug 3 处(① Terraform demo `LockError` 无 `render` 方法导致首跑崩 → `LockInfo` 改字段化并直接遍历 `str(err)` 行;② GitOps `sync()` 在"仅孤儿 + prune 关"路径返回 `OutOfSync`,使该 pair 被计入 failed 并被"不重试"规则永久拦住 → 改返回 `Synced` 而由 `status()` 单独报应用状态;③ `live_drift` 原写 `pending and not git_moved` 会把孤儿误判成漂移 → 改 `bool(stale) and not git_moved`);另将 `gitops_reconcile.py` 349→拆 `model.py` 134+主 247、`vault_format.go` 342→300、`pulumi_engine.go` 400→拆 3 文件、`state_lock.c` 331→300;C/Go 走人工代码审查(本机 which gcc cc clang go 全 exit 127),改做括号配平程序化校验;大类轮换 ✓ 上轮 05-AI与机器学习→本轮 06-DevOps(next_index 40 同大类 05-AI 顺延至 41,不记 skip);轮间保险通过(last_run 2026-09-14 22:02 距 00:00 约 118 分钟 > 45 阈值);槽位自检 ✓(shell date 实得 2026-09-15 00:00:25 +0800,00:00 槽 ±2 分钟内,无 slot_mismatch);占位锁 52f2a34 push OK;主轮 bfa18ad push OK;**notify: ok** HTTP 200 errcode=0 errmsg=ok(curl `--data-binary @file` 单次调用,发送前已确认 schedule.md 无本轮既有 notify 记录) |
| 2026-09-15 02:27 | 44 | M=Golang 第二批 5 demo(待研究清单 6-10 收官):interface与itab(双字对/itable O(ni+nt) 排序同步遍历+缓存/赋值期派发 s.tab->fun[0](s.data)/空接口去 itable+单字内联两优化/方法集 T vs *T 三规则/断言=类型指针比较/comma-ok 接口到接口=运行时问方法集/typed-nil 陷阱规范示例)+GPM调度模型(G/M/P 定义/LRQ+GRQ/schedule 1/61 先查 GRQ->LRQ->偷一半->GRQ/channel 阻塞 G 重排队 M 不闲/阻塞系统调用 M 带 G 与 P 解绑+M2 接管+M1 挂起备用/异步网络走 poller/协作式安全点=函数调用/1.14 信号式异步抢占/GOMAXPROCS n<1 只查询且默认不下于 2/Goexit 先 defer 且 recover=nil/切换 200ns vs 1000ns)+GC三色标记与写屏障(延迟演进 300-400ms->30-40ms->4-5ms->sub-ms 均 Twitter 实测/1.7 插入屏障栈 permagrey 周期末重扫 10s-100s ms/1.8 混合屏障 shade(*slot) 无条件+栈灰时 shade(ptr)/栈一次扫描永久黑<50µs/弱三色不变式/浮动垃圾代价/GOGC=live+(live+roots)*GOGC/100 官方 18MiB 示例/Pacer+mark assist>5%/GOMEMLIMIT 软限制 50% CPU 上限/不分代=年轻对象死在栈上)+context包(Done 关闭即广播/无 Cancel 方法=收发分离/WithTimeout Deadline=min/父传播子隔离/select 取消后仍 <-c 等 worker/未导出 key 类型/Google 首参实践)+type-parameter泛型(实例化两步/约束即类型集/Ordered=Integer|Float|~string/~T 底层类型/union 接口只能作约束/实参推断仅限参数列表/约束推断官方 Scale 双参数修复/Tree[T]/any 不保证可比较) | S3=索引同步(Golang README 待研究 6-10 勾选+特性表+权威来源+7;STATE §一注释/§二 next_index=45+last_top=09-语言学习/§三滚动并淘汰最旧行)+归档 rotate(completed 100+5->105->tail -100 回 100 / schedule 30+1->31->tail -30 回 30) | +5 | 186 | 权威来源 10 WebFetch 全命中(rsc interfaces 原文含汇编与评论区/rant spec Interface types+Method sets 完整而 Type assertions 章被截断、以 rsc 补/Ardan Labs Part I 是 OS 调度器、Part II 才是 GPM/ismmkeynote 为 2018 ISMM 演讲而非 1.5 发布文/gc-guide 无三色术语以 17503 补/intro-generics 无 comparable、README 不展开)+ 0 WebSearch/0 agentic_search,全在硬约束内;Python 模拟 ×2 实跑断言全过(gpm_sim 4 场景:偷一半 3 个/1/61 顺序 GRQ 优先/channel G 让位 M 复用/M-P 解绑;tricolor_gc 3 场景:混合屏障拦截藏匿序列/浮动垃圾/无屏障反例可达对象被误回收,开发期修 3 bug:G 未绑 M 就阻塞 AttributeError/reachable 须在 sweep 前取否则对象已删/new(self=) 关键字冲突);Go ×5 无本机工具链(which go exit 127)走人工代码审查+注释剔除后括号配平程序化校验全平衡(粗校验的+5 括号差全部来自分节注释 N) 编号,非代码问题);行数审计一次过(源 117-229 行全 ≤300,README 112-134 全 ≤200);大类轮换 ✓ 上轮 06-DevOps→本轮 09-语言学习(next_index 42 同大类顺延过 43 落到 44,不记 skip);轮间保险通过(last_run 2026-09-15 00:14 距 02:00 = 106 分钟 > 45 阈值);槽位自检 ✓(shell date 实得 2026-09-15 02:00:27 +0800,02:00 槽 ±2 分钟内,无 slot_mismatch);占位锁 ecded66 push OK;主轮 50bf50e push OK;**notify: ok** HTTP 200 errcode=0 errmsg=ok(urllib 单次调用,发送前已确认 schedule.md 无本轮记录) |
| 2026-09-15 04:31 | 45 | M=图数据库第二批 5 demo(查询语言层→协议层→并发层→执行层→更新层):量化路径模式({1,3} 展开为固定长度并集+相邻节点模式须同一节点;组变量外部绑定为列表且 m 不含起点;关系唯一性=同一 MATCH 内关系只匹配一次,防环图无限展开,REPEATABLE ELEMENTS 才可放开;内联谓词遍历时剪掉整棵子树,官方地理剪枝示例全英铁路网仅剩 1 条;旧 * 语法非 GQL 标准且禁 WHERE)+Bolt 协议三层(握手 60 60 B0 17+恰好 4 个大端版本按偏好序,0=无匹配即断连;chunked 2B 大端长度头+单 chunk 上限 65535+00 00 消息边界,4.1+ 空块兼 NOOP;PackStream marker byte:F0-FF 负 TINY_INT/8x 短字符串/9x 列表/Ax 字典/Bx 结构+tag,字符串 size 按 UTF-8 字节数;官方字节示例 42→2A、1.23→C1 3FF3AE147AE147AE、[1,2,3]→93010203 全部断言)+并发控制(READ_COMMITTED 读不加锁→100 并发 +1 最坏为 1;SET 右侧直接依赖被读属性才自动写锁,WITH 中转/循环依赖不加锁→哑属性技巧;实体级锁表:增删关系锁两端节点,密集节点共享度锁;等待图投影 tx→entity→持有者 DFS 找环,**阻塞时等待边必须保留否则环永不闭合**;交叉顺序死锁 vs 固定顺序免疫)+执行计划火山模型(算子二叉树根 ProduceResults 自底向上流;二元两类:Apply 每左行带绑定重启 RHS=嵌套循环 vs Join/CartesianProduct 两侧完整输入;默认惰性一产出即推,Sort/聚合 Eager 全量缓冲 O(行数);Estimated Rows 统计估算可能错 vs PROFILE 实测 Rows/DB Hits,Time 按 pipeline 非算子)+MERGE 语义(全属性精确匹配,属性集不一致即创建"相似"节点需唯一约束兜底否则报 index entry conflict;查询内绑定复用 Location 例子 3 行只建 1 节点;无向先双向匹配再左→右创建;并发 MERGE 关系=两端排他锁+锁后二次 MATCH 吸收竞态,**不依赖唯一约束**因 Cypher 无关系数量约束) | S3=索引同步(05-图数据库/README +5 demo 行+待研究勾 5 新增 2;07-数据存储/README demo 索引更新为两批 10;STATE §二 next_index=46/last_top=07-数据存储/§三 滚动+压缩)+归档 rotate(completed 100+5→105→tail-100 回 100 / schedule 30+1→31→tail-30 回 30) | +5 | 191 | 权威来源 6 WebFetch(命中 5:cypher-manual patterns/variable-length-paths 全文+MATCH 全文+merge 全文+bolt packstream 全文+execution-plans 全文;404 后改道 2:execution-plans/introduction 与 bolt/protocol 均不存在,经 WebSearch 命中 bolt/handshake 规范与中文镜像 chunking 细节)+ 3 WebSearch(事务锁/握手/分块)+ 0 agentic_search,全在硬约束内;Python ×6 py_compile 全干净(拆分后 operators.py+pipeline.py 复检通过);Go ×7 无本机工具链(which go exit 127)走人工代码审查+注释字符串剔除后括号配平程序化校验全平衡;**行数审计整改 3 处超限**:bolt.go 349→拆 bolt.go 170+bolt_pack.go 187、pipeline.py 347→拆 operators.py 238+pipeline.py 121、pipeline.go 405→拆 pipeline.go 144+pipeline_ops.go 266(拆分时 openIt 尾部 } 遗漏,被配平校验当场捕获补上;bolt.go 残留未用 unicode/utf8 import 一并清除);终态源文件全 ≤269 行/README 全 ≤93 行;开发期修复语义 bug 3 处(① ChunkReader 消息未收全时消费缓冲丢已到达 chunk→改先扫描确认完整边界才一次性消费;② LockManager 等待边随单次尝试失败即弹出→死锁环永不闭合,改阻塞时保留等待边;③ 笛卡尔积首调用即返回 None 的左行迭代器初始化错误);大类轮换 ✓ 上轮 09-语言学习→本轮 07-数据存储(next_index=45 直取,无顺延);轮间保险通过(last_run 2026-09-15 02:27 距 04:00 = 94 分钟 > 45 阈值);槽位自检 ✓(shell date 实得 2026-09-15 04:00:40 +0800,04:00 槽 ±2 分钟内,无 slot_mismatch);占位锁 74da382 push OK;主轮 5 次逐 demo 提交(dc1cad3/0cf7f4b/99e5255/1f2066a/6e5bcd9)push OK;**notify: ok** HTTP 200 errcode=0(curl --data-binary 单次调用,发送前已确认 schedule.md 无本轮既有 notify 记录) |
| 2026-09-15 06:29 | 46 | M=协议逆向第一批 5 demo(README 待研究清单前 5 项收官):报文序列对齐(NW 全局 F(0,j)=-j*d+三分量递推+从 F(M,N) 回溯;SW 局部=初始化全 0/加 0 候选/全矩阵 argmax 起点走到 0 停;star alignment consensus+插入列合并;打分 m3/s-1/d-4 变长字段宁错位不开洞;实测切出 magic/ver 常量+len 高字节恒 0+CRC 常量)+信息熵字段边界(BinaryInferno:熵差>1bit 规则作用于纵向列熵剖面;const/low/high 三分类;sequenceHeur 单调概率→计数器标签,seq 列高熵但 P=1.0;滑窗熵只适长流且上限 log2(w);尾部 GAP 占位列熵压低=变长位移污染活例)+协议状态机恢复(Veritas P-PSM:首 token+方向标签→逐流转移计数→p>=0.05&c>=3 剪枝→流覆盖率回测 97%;S:221→C:EHLO 跨流伪边靠逐流统计消除)+JA3与JA4指纹(ClientHello 构造→解析→指纹全链路;JA4 版本取 supported_versions 最高值非 legacy;扩展计数 16 剔 GREASE 而 c 哈希再剔 SNI/ALPN 剩 14;GREASE 位判定 &0x0f0f==0x0a0a;官方向量端到端复现 JA4 t13d1516h2_8daaf6152771_e5627efa2ab1 与 JA3 ada70206e40642a3e4461f35503241d5,GREASE 注入指纹不变)+Polyglot污点启发式(token 表双哈希/分隔符三步(series>=3,扩展>=4 次上限 4 字节,扩展必须查整个分隔符起点前一字节防重复前插)/关键字只为真比较拼串/方向字段=间接访问地址由污点计算+最小访问位置为 target 末尾/定长合并同一指令;实测 HTTP 切出 +空格+GET/HTTP/1.1/Host:,二进制切出方向字段[4,5]+magic/crc) | S3=索引同步(03-协议逆向 README +5 demo 索引+待研究勾 5;10-逆向工程 README 待研究 +5 行;STATE §二 next_index=47/last_top=10-逆向工程/§三滚动)+归档 rotate(completed 100+5→105+空行→tail-100 回 100 / schedule 30+1→31→tail-30 回 30) | +5 | 196 | 权威来源 5 WebSearch + 2 WebFetch(FoxIO/ja4 README 字段级规则、Polyglot CCS'07 全文 PDF 下载精读 §4-§6 token 表/分隔符/关键字/方向字段/定长合并细节)+ 0 agentic_search,全在硬约束内;Python ×5 全部实跑通过(开发期修 bug 6 处:①NW consensus 断言错列数改插入列感知合并 ②自检分数手算错 3*3-4=5 ③滑窗 w=8 熵上限 3bit 分不出文本/随机→改长流 w=64 演示+README 澄清 1bit 规则属纵向剖面 ④PFSM 跨流拼接产生 S:221→C:EHLO 伪边→改逐流统计 ⑤ClientHello 解析 body[5:] 应为 body[4:](握手头 1+3=4)、ALPN 协议长度在 ed[2] 非 ed[3]、ja4 c 段丢 _ 分隔符、data_g 漏传 curves/pf ⑥Polyglot  扩展死循环重复前插→left/right 跨度跟踪、HTTP 版本串 match 位置错位);Go ×5 无本机工具链(which go exit 127)走人工代码审查+括号配平程序化校验全平衡;行数审计整改 1 处(tls_fingerprint.go 353>300→拆 chello.go 177+tls_fingerprint.go 180),终态源文件全 ≤242 行/README 全 ≤113 行;大类轮换 ✓ 上轮 07-数据存储→本轮 10-逆向工程(next_index=46 直取,无顺延);轮间保险通过(last_run 2026-09-15 04:01 距 06:00 = 119 分钟 > 45 阈值);槽位自检 ✓(shell date 实得 2026-09-15 06:00:16 +0800,06:00 槽 ±2 分钟内,无 slot_mismatch);占位锁 aee1acc push OK;主轮逐 demo 提交 push OK;notify: ok(首轮 curl --data-binary @/tmp 因 Git Bash 路径映射读文件失败未发出,改 urllib 单次调用) HTTP 200 errcode=0 |
| 2026-09-15 08:14 | 47 | M=基准测试方法论首批 5 demo(README 待研究清单前 5 项收官):benchstat 统计显著比较(中位数 CI 用 order-statistic 精确区间+bootstrap 双实现,实测覆盖率 0.992;U 检验 U1=T1-n1(n1+1)/2 子集和等价,精确分布仅无并列 n<=50/有并列 n<=25,并列校正 t=Sum(tj^3-tj),连续性校正 -0.5,U1==U2 直接 p=1;geomean 语义 n 个中一个变 2 倍=2^(1/n);假阳性率实测 0.055 vs 名义 0.05)+JMH perfnorm 硬件归因(22 事件表逐事件探测,增量非累计计数解析兼容 3 列/>=4 列两种布局,归一化 s/(maxTime-minTime),丢最后 2 样本+跳过首样本,1000*ops/timeMs 换算,CPI/IPC 派生与 :u 回退;实测裁剪漏掉致 cycles/op 高估 30%~37.5%;PMU 是采样估计,分数差 4.26x 绝对误差差 21.5x)+统计检验的选择(Welch-Satterthwaite 自由度近似,t 尾概率用不完全 Beta 函数 I_{nu/(nu+t^2)}(nu/2,1/2);ARE 渐近相对效率 n=10 功效比 0.894/n=150 为 0.972;Cauchy 均值散布 n=10->1000 为 6.31->30.42;实测 t 显著 U 不显著 p=0.0036 vs 0.091)+CI 性能回归门禁(双判据幅度 10%+Welch t,df>30 用 1.96;Skia/Jetpack step fitting WIDTH=5/THRESHOLD=25/z>2.0,纯噪声 sigma=6.0 下 59 次比较中 10 次朴素差分假阳性而 step fit 报 0;噪声地板+CoV 门禁;200 个 benchmark 未校正 13 个假阳性;修正流行伪码把相对 delta 除绝对 stderr 的量纲错误)+主动基准测试(Gregg 7 条 problem checklist 程序化判定,6 条可程序化:扰动/资源争用/网络上限/单线程夹紧/测错目标/热降频/结果不可信;附 C 语言 /proc+/sys 纯文件接口采样器) | S2=+2 子类目(11-性能分析/04-全链路性能 + 05-容量规划与性能建模,各含原理详解与待研究清单)+S3 索引同步(11-性能分析/README 子领域表 +3 行、已建子类目区、待研究勾 2 项新增 2 项、参考资料改指两新目录并修掉第 38 行语法错误;03-基准测试方法论/README +5 demo 索引表 + 待研究勾 5 项新增 4 项 + 本轮 13 条来源)+STATE 占位锁转正/滚动 | +5 | 201 | 权威来源 4 WebFetch 成功+3 WebFetch 失败(web.dev/articles/vitals 与 sre.google/sre-book/handling-overload 抓取失败改走 WebSearch)+6 WebSearch+0 agentic_search,全在硬约束内;Python x5 全部实跑自检通过;Go x9 无本机工具链(which go exit 127)走人工代码审查+注释字符串剔除后括号配平程序化校验全平衡;行数审计整改 5 处超限(benchstat_lite.go 318->129+core 249;test_selection.go 401->179+stats_core.go 237;regression_gate.go 344->135+gate.go 215;perfnorm_report.py 305->299;proc_sampler.c 316->299);终态源文件全 <=300 行/README 全 <=200 行;开发期修 bug 8 处(1e-12 容差过严改表达式比较;千分位剥离致夹具断言错改如实断言错位陷阱;perfnorm 夹具用累计值改增量计数;perfnorm IPC>2 证据降级改按 l1_miss 判定优先级;demo3 场景 A 用含离群组对比致 p=0.059 改整组平移得 1.67e-05;ARE 断言按渐近性质放宽;demo4 step_fit 无假阳性致夹具噪声调大至 sigma=6.0;stable_worse 用 +3% 改 +20% 才命中 REGRESSION);大类轮换 OK 上轮 10-逆向工程->本轮 11-性能分析(next_index=47 直取无顺延);轮间保险通过(last_run 2026-09-15 06:00 距 08:00 = 120 分钟 > 45 阈值);槽位自检 OK(shell date 实得 2026-09-15 08:00:34 +0800,08:00 槽 +-2 分钟内,无 slot_mismatch);占位锁 87c32e9 push OK;主轮 0af5866 push OK;收尾 7e4861a push OK;notify: ok(HTTP 200 errcode=0,单次 urllib 调用,发送前已确认本轮行无既有 notify 记录) |
| 2026-09-15 10:52 | 48 | M=特征工程与降维首批 5 demo(该目录 README「待研究」前 5 项收官,第 6 项「特征选择稳定性」留待下一批):PCA 的 SVD 与协方差两条路线数值对比(协方差路线物化 p×p 矩阵 + 官方 `covariance_eigh` 警告条件数翻倍 → 实测 σ=1e-8 时相对误差 2.10e-02、λ_min=-1.518e-19 为负值、σ<=1e-10 直接 nan,而 SVD 路线 κ=1e12 下最大相对误差仅 4.20e-06,精度预算 eps·κ vs eps·κ²/2;PCA 只中心化不缩放 → 未标准化时首主成分 = +0.000013·f0 - 1.000000·f1,f1 独占 100.0000%,标准化后 49.97%/50.03%;whiten 后各分量方差归一 [1.0,1.0,1.0];p=4096 时协方差矩阵 128 MiB,故官方 auto 只在小 p 用该 solver) + 随机化 SVD(Halko 2009:`n_oversamples` 0→20 使 σ 最大相对误差 1.710e-01→1.405e-13、子空间 sinθ 8.688e-01→5.497e-07;谱衰减慢时 `n_iter` 才关键 0→7 为 1.679e-01→7.866e-11;谱快衰减且 n_components>=有效秩时 n_iter=0 已够 2.767e-07;`power_iteration_normalizer` none 在 n_iter>=5 崩到 8.123e-01 而 LU 1.239e-10 / QR 1.057e-14,三者 ‖QᵀQ-I‖_F 恒 ~1e-16 说明正交性不是判别标准) + t-SNE 与 mini-UMAP 可视化幻觉量化(自写困惑度二分 + 模糊单纯集/交叉熵 SGD:簇大小不可读 —— 40 vs 160 点真实比 4.00 而嵌入 RMS 半径比仅 1.75;纯噪声低困惑度凭空成团 —— 抱团点占比 0.107/0.040/0.000/0.000 随 P=2/5/30/100;簇间距可信度强依赖 P —— 真实质心距离 [22.4,111.8,89.4],P=2 相关 +0.055 / P=30 +0.933 / P=100 +0.994,mini-UMAP +0.608;早停形态是簇半径剧烈摆动 28.13→4.63→5.57→15.69→1.59 而 KL 仍 2.948→0.376;诚实声明 3 点相关系数只是量级趋势、mini-UMAP 不可用于判定优劣) + Target 编码与泄漏(官方锚点复现 target_mean_=44.3、smooth=1.0 得 [20.9,80.8,43.2](docstring 显示 21 为取整,公式值 20.93)、smooth=5000 得 [44.1,44.4,44.3]、smooth='auto' 等价 m=0.0831;1500 类**纯噪声**特征上 A 训练集内全量编码 0.937/0.712、B 全量数据编码 0.911/0.911(连测试分都成谎言)、C K 折交叉拟合 0.799/0.787、D 常数编码基线 0.799/0.787、E CatBoost 有序统计 0.799/0.787,CDE 落回基线恰证其未偷看;smooth 扫掠 0 时稀有类别取自身单样本均值 0.5/0.0、1e6 时全塌到全局均值 0.367) + Pipeline/ColumnTransformer 防泄漏(特征选择泄漏随样本量稀释:n=100 泄漏式 CV 0.627 vs 正确式 0.520 = +0.107、n=200 +0.000、n=400 +0.050;零信号行 coef=0 泄漏式仍报 0.573 vs 正确式 0.490,抛硬币上限 0.5 却凭空多出 +0.083;留出集 8 次重复 0.535±0.146 vs 0.482±0.092 = +0.053 且 2/8 次符号相反;remainder 三语义 'drop' 8 列 / 'passthrough' 9 列(末位原样 +15.00)/ StandardScaler 9 列(末位 -0.51);未见类别「深圳」在仅训练拟合表下退化为全 0 且列宽恒定;嵌套 CV 外层 0.615 vs 在外层验证集调 k 0.664(+0.049)vs 固定 k=25 0.602) | S1=05-AI与机器学习/07-特征工程与降维/README(勾选 5 项待研究 + 新增「已完成 demo」索引表 5 行 + 待研究新增 4 项 + 参考来源扩到 15 条);S2=05-AI与机器学习/README(子领域表更新 + demo 索引表 +5 行 + 待研究 +2 项) | +5 | 206 | 权威来源 10 WebFetch(sklearn decomposition §2.5 / PCA API / randomized_svd API / common_pitfalls §12.1-12.2 / compose §8.1 / TargetEncoder API + Distill misread-tsne + arXiv:0909.4061 + arXiv:1802.03426 + JMLR vandermaaten08a + UMAP how_umap_works)+ 3 WebSearch(CatBoost ordered TS / sklearn TargetEncoder 用户指南 / PCA covariance_eigh 条件数)+ 0 agentic_search,全在硬约束内;Python ×5 全部实跑断言通过(PCA sklearn 锚点 [6.30061,0.54980] 复现、随机化 SVD 全断言过、t-SNE 约 30s、Target 约 2s、Pipeline 约 3 分钟);Go ×11 + C ×3 无本机工具链(which go exit 127)走人工代码审查 + 注释字符串剔除后括号配平程序化校验(`_docs/tools/syntax_lint.py`)全平衡;行数审计:源文件全 <=300(PCA py 273 / 随机化 278 / t-SNE 199 / Target 254 / Pipeline 299;Go ml 255+main 170;C linalg 225+main 98+linalg.h 28),README 全 <=200(Pipeline README 初版 208 行超限 → 压缩到 190,其余 153-194);开发期修 bug 9 处(PCA:随机构造 U 的列均值非 0 致 center() 抹掉秩 1 分量、毁掉 98% 方差 → orthonormal_columns 把列对 1/√n 正交化让 center() 成恒等,share() 忽略逐特征方差,err_cov 为 nan 的断言;随机化 SVD:Gram-Schmidt 未即时归一化致 ‖QᵀQ-I‖_F≈4.5 而非 1e-16、参考子空间非真解致 sinθ 恒 1.0、l<=min(n,p) 越界;t-SNE:困惑度二分中 s 下溢致 math domain error、连通分量判据过粗改抱团点占比、多列片段索引错位;Target:20.93 vs docstring 21 的取整断言、标签噪声 hot 集合;Pipeline:自写 OneHot 只读 row[0] 致多列类别块静默丢列 8→5、ColumnTransformer.fit 存变换输出而非变换器、行拼接 sum() 对 list 变成 0+list 应改 extend、exp4 网格含 k=500 致内层成本 O(epochs·n·k)=5e7 整轮 8 分钟跑不完 → 改 n=150/p=300/grid[5,25,100]/epochs=250 后 70 秒);大类轮换 ✓ 上轮 11-性能分析→本轮 05-AI与机器学习(索引 48 直取,无顺延);轮间保险通过(本轮开局 shell date 实得 2026-09-15 10:04:43,距上轮 last_run 2026-09-15 08:14 = 110 分钟 > 45 阈值);槽位自检 △ 小时位匹配(10 时)但首次 shell date 为 10:04:43(+4.7 分钟,略超 ±2 分钟窗口,未发生跨槽错配,不记 slot_mismatch);占位锁 2be9c06 push OK;主轮 867e8b6 push OK;**notify: ok** HTTP 200 errcode=0(urllib 单次调用,发送前已确认 schedule.md 末行为 08:14 轮、无本轮既有 notify 记录) |
| 2026-09-15 12:11 | 50 | M=Rust 首批 5 demo(所有权与借用 / Copy与Drop / trait与分发 / 智能指针 / 生命周期,补齐 09-语言学习 的「无 GC 内存安全」主线) | S1=09-语言学习/Rust/README(待研究 1-5 勾选 + 已完成 demo 索引表 5 行 + 待研究新增 4 项(方法解析/variance/高级 trait/宏)+ 权威来源扩到 15 条,并把原 §三 原理详解压缩为「demo 索引 + 原理速览」);S2=09-语言学习/README(Rust 行补 demo 数 5 + 首批说明);S3=索引同步(STATE §二 next_index=51 / last_top=09-语言学习,§三滚动)+归档 rotate(completed 100+5→105→tail-100 回 100 / schedule 30+1→31→tail-30 回 30) | +5 | 211 | 权威来源 10 WebFetch(ch04-01 / ch04-02 / ch10-01 / ch10-02 / ch10-03 / ch15-01 / ch15-04 / ch15-05 / ch15-06 / reference/destructors 全部命中)+ 4 WebSearch(deref coercion 三条形式 / dyn 胖指针与 object safety 改名 / E0184 原文 / drop scope 与参数 3 2 0 1 复核)+ 0 agentic_search,全在硬约束内(≤10/≤15/≤5);Python ×6 全部实跑断言通过(borrow_checker+run_cases / drop_sim / dispatch / smart_ptr / lifetime_solver);Rust ×10 无本机工具链(which rustc exit 127)走人工代码审查 + `_docs/tools/syntax_lint.py` **新增 rs 模式**(先吃 char 字面量与生命周期标注 `'a`,否则被误判未闭合引号;go/c 模式回归通过)全平衡;行数审计整改 4 处超限(borrow_checker.py 323→拆 borrow_checker 212 + run_cases 131;所有权与借用 main.rs 302→300;三篇 README 超 200 行 → trait与分发 228→183 / 智能指针 270→195 / 生命周期 225→190);compile_fail 目录 11 个非法片段各附 rustc 原文报错(E0382/E0499/E0502/E0596/E0106/E0184/E0117/E0308/E0597/Box 陷阱);开发期修 3 处自写模型 bug(① drop_sim 的 move 用 forget 模拟致泄漏计数虚高→改 move_to 建新绑定;② 覆盖赋值的日志文案重复;③ Rust lint 把 `'static` 当未闭合引号);大类轮换 ✓ 上轮 05-AI与机器学习→本轮 09-语言学习(索引 49 概率图模型 与上轮同大类顺延到 50 Rust,不记 skip);轮间保险通过(开局 shell date 实得 2026-09-15 12:02:36,距上轮 last_run 2026-09-15 10:52 = 70 分钟 > 45 阈值);槽位自检 ✓(12:00 槽,12:02:36 落地,+2 分钟内,无 slot_mismatch);占位锁 0caa5c4 push OK;主轮 445b896 push OK;notify: ok(HTTP 200 errcode=0,单次 urllib 调用,发送前已确认 schedule.md 末行为 10:52 轮、无本轮既有 notify 记录) |
| 2026-09-15 14:23 | 0 | M=01-游戏开发/01-服务端/游戏服务端架构 首批 5 demo(AOI 兴趣管理 / 帧同步 / 客户端预测与回滚 / 网关 / 匹配系统,统一 Python+C+Go 三实现 + 5 段自检;补齐 01-服务端 从「网络编程」到「架构层」的空白) | S1=01-游戏开发/01-服务端/README 覆盖矩阵 +5 行 + 待研究补「跨服通信 / 全局服务器」2 项、游戏服务端架构/README 重建为「子知识点+demo 索引+核心机制」表并列出 5 项待研究;S2=类目自动拓展 +2(STATE 索引 51 `06-DevOps/平台工程`、52 `05-AI与机器学习/RAG与向量检索`, 表尾说明 39-50→39-52、顺延基数 53);S3=索引同步(STATE §二 next_index=1 / last_run=2026-09-15 14:23 / last_top=01-游戏开发,§三滚动 + 压缩 4 行旧记录守 5 KB:5396→**4906 B**)+归档 append & rotate(completed 100+5→105→tail-100 = ID 119..216;schedule 30+1→31→tail-30) | +5 | 216 | 权威来源 13 WebFetch(Microsoft Learn *Gateway Routing pattern* / nginx *ngx_http_upstream_module*(`hash key consistent`=ketama 160 点原文)/ MSR *TrueSkill Ranking System* 项目页 ×2 / FIDE *Rating Regulations B02*(K=40/20/10、K×n≤700、400 分封顶) + 前段 AOI·帧同步·预测·网关 4 个 demo 的 9 次抓取)+ 4 WebSearch + 0 agentic_search —— **⚠ 约束超限:WebFetch 13 > 硬约束 10**(原因:5 个 demo × 2~3 个权威源,单槽 10 次抓取不足以支撑 5 篇含原文论断的 README;已如实记录,建议把配额改为「每 demo ≤3 次」或上限提到 14);Python ×5 全部实跑断言通过(aoi_grid / lockstep / prediction / gateway / matchmaking,跨语言同 seed 同公式);Go ×14 + C ×13 无本机工具链(which go/rustc exit 127)走人工代码审查 + `_docs/tools/syntax_lint.py` 括号配平程序化校验全 OK;行数审计整改 7 处超限(go/gateway.go 476→拆 hashring.go 148 + gateway.go 215 + selftest.go 131;python/gateway.py 311→拆 hashring.py 99 + gateway.py 228;python/matchmaking.py 351→拆 rating.py 91 + matchmaking.py 279;c/gateway.c 377→拆 hashring.h 28 + hashring.c 102 + gateway.c 267),终态源文件全 ≤311 行 → 整改后全 ≤300 行、README 全 ≤199 行;开发期修 bug 9 处(AOI 自检①eid 自身入广播列表致自发 Move→接收列表剔除 eid、②leave 集含 eid 旧格为空致消息数不符→改拆两段断言:集合势 + 消息路由精确事件序;帧同步③执行回合起点 1+LOOKAHEAD 与循环上界错配致 executed 0/12→改 first_exec/last_exec 与 stall 计数条件;④丢包断言用 setup_delay 不适用→改 comm_turns;⑤Go 343 行超限拆两文件;Go⑦`if t,_=f(); true` 非法语法→改表驱动 cases / wcmd 一行结构体语法错;预测⑥已知阻挡物从 tick 0 起致客户端服务端永不分歧(first_err 0.0)→引入 BLOCKER_TICK=26 让服务端晚 3 tick 知情;网关⑧一致性哈希负载偏差 43.92%>35% 阈值→虚拟节点 160→512 降到 25.72%;⑨push(uid=5) 返回 None→reconnect/connect 不建会话,需先 dispatch;匹配⑩模拟均值除数用 len(waits) 而 quals 只一半致质量被腰斩 0.2236→改 len(quals),且三策略退化为相邻配对→重写为多 tick 排队-冷却循环);大类轮换 ✓ 上轮 09-语言学习→本轮 01-游戏开发(索引 0 取模直取,顶层不同,无顺延);轮间保险 **未触发重入**(本轮占位锁 effce3b 由本会话在 14:01 写入,14:15 续跑时距 last_run 13 分钟 < 45 阈值,判定为「同会话续跑」而非重复派发,继续完成主流程;若按字面退出会白丢一轮);槽位自检 ✗→△ 触发时刻 **14:00:48**(shell date 实得,±2 分钟内 ✓ 符合 14:00 槽);占位锁 effce3b push OK;主轮 9baf960 / 9018525 / 572c491 / ebd61fd / ecceb14 逐个 push OK;**notify: ok**(HTTP 200 errcode=0,urllib 单次调用,发送前已确认 schedule.md 末行为 12:11 轮、无本轮既有 notify 记录) |
| 2026-09-15 16:22 | 8 | M=02-Web开发/01-前端框架 5 demo:React 并发渲染(Lane 31 位掩码 / 两级优先级换算 / 5ms 切片 / 饥饿防护 / transition 中断丢弃)、Vue 编译器优化(静态提升 / PatchFlags / Block Tree / 事件缓存 / 静态串压缩)、Signals TC39(Computed 四状态机 / glitch-free / Watcher 三状态)、SSR-Hydration(renderToPipeableStream shell + boundary 标记 + 选择性水合 + mismatch 三态)、Turbopack turbo-tasks(读时依赖跟踪 / 内容相等短路 / 聚合图 / 文件系统缓存)。统一 JS + TS 双实现 + 5 段自检,合计 186 项断言全绿,无「框架跑一遍」只读源码的 demo | S1=索引同步(01-前端框架/README 子领域 +2 项、已完成 demo +5 行、待研究勾 3 项新增 4 项;React/README +Concurrent 并勾 3 项;Vue/README 增「已实现」区 +编译器优化;构建工具/README +Turbopack 勾 1 项新增 1 项;02-Web开发/README 子领域行与 +5 行 demo);S2=类目自动拓展 +2(STATE 索引 53 `09-语言学习/TypeScript`、54 `08-安全/03-应用安全与供应链`,两者均建目录 + 路线图 README 并回填父 README 子领域表,表尾说明 39-52→39-54、顺延基数 55,未新增顶层大类);S3=索引同步 + 新建 `_docs/tools/archive_append.py`(追加 + 滚动截断,替代每轮手工删首行) | +5 | 221 | 权威来源 10 WebFetch(react.dev v18 并发渲染博客 / useTransition / hydrateRoot / renderToReadableStream、vuejs.org rendering-mechanism、vuejs/core patchFlags.ts 原文、tc39/proposal-signals、patterns.dev 流式 SSR、OneUptime 流式 SSR、nextjs.org Inside Turbopack)+ 5 WebSearch(turbo_tasks rustdoc / turbo.build pack docs / rolldown / svelte runes / Signals 生态)+ 0 agentic_search —— 本轮 WebFetch 恰好 10/10 未超额(上轮 13 超额已复盘,本轮按「每 demo ≤2 个权威源」控制);JS ×5 全部实跑断言通过(concurrent 37 / compiler_check 34 / signals_check 46 / ssr_check 33 / turbo_check 36 = 186);TS ×5 无本机 tsc(which tsc 为空)走人工代码审查;行数审计整改 7 处(compiler.js 301→300、signals.js 304→299、turbo.js 314→299、turbo.ts 351→300、Turbopack README 228→184,终态源文件全 ≤300 行、README 全 ≤200 行);开发期修 bug 16 处(择要:①TransitionLanes 二进制字面量少一位致 15 个 lane;②React 饥饿断言用绝对 5001 而实现是「首次观察时刻 + timeout」→ 改 6000/5999/6001;③浮点 maxSliceMs 59.99999999999873 → 加 1e-6 容差;④Vue 生成 `{ /* v-if */, style }` 语法错 → 去掉 v-if 的 NEED_PATCH 映射并让嵌套 v-if 走 genVIf;⑤Vue 根节点非 block 致 dynamicChildren 为空 → 根包 (openBlock(), rootExpr);⑥Vue 对任意插值子节点都打 TEXT → 限制到单插值快路径 + mountChild 文本归一 + 索引对齐 diffChildren;⑦Vue 同时跑结构 diff 与 dynamicChildren 致节点访问 7 次 → block 模式二选一;⑧⑨⑩Signals 三个断言自身有 bug(提前 get 触发重算 / 断言窗口错 / push 基线双重计数);⑪Signals 提案字面语义会读到陈旧值 → 加 config.directCheckedToDirty 并写对照断言;⑫SSR 抢跑用例 chunk 到达晚于交互时刻 → 补「同时到达 + 稍后交互」场景;⑬SSR toLocaleTimeString 产生相同字符串致 mismatch 全 match → 改确定字面量;⑭SSR readyAt 差 10ms → 如实断言「中断丢弃在跑工作」的代价;⑮Turbopack 基线被「缓存命中仍写回 cell」放大成 7 次 → 分离「引用相同」与「内容相等」两级短路,基线回到 4 次;⑯换行符致 DIAMOND_SOURCES 被误插入两次 → 清理);大类轮换 ✓ 上轮 01-游戏开发→本轮 02-Web开发(索引 8 直取,顶层不同,无顺延);轮间保险通过(开局 shell date 实得 2026-09-15 16:00:39,距上轮 last_run 2026-09-15 14:23 = 97 分钟 > 45 阈值);槽位自检 ✓(16:00 槽,+39 秒落地,在 ±2 分钟内,无 slot_mismatch);占位锁 d001d6b push OK;主轮 7426744 push OK;S1+S2 2d99823 push OK;notify: ok(HTTP 200 errcode=0,单次 curl 调用,发送前已确认 schedule.md 末行为 14:23 轮、无本轮既有 notify 记录) |
| 2026-09-15 18:26 | 12 | M=03-系统编程/01-网络编程 5 demo(TCP 拥塞控制 / 零拷贝 / backlog 与 SYN 队列 / scatter-gather / SCM_RIGHTS,合计 76 项断言全绿,源文件与 README 全部落在 ≤300 / ≤200 行硬约束内) | S1=拆 15 处超限文件(新增 `_docs/tools/split_source.py`:按行区间搬运 + 原地补回 include/import,被搬运代码逐字节不变;C 用「实现头文件」文本包含保住同一翻译单元、Go 用同包多文件、Python 用同目录模块,三种语言都做到**零链接/零语义风险**,因为本机无 C/Go 工具链无法编译验证)+ 扩展 `_docs/tools/syntax_sanity.py`(新增 Go 未使用 import 检测 —— Go 里这是编译错误;新增 `#include "x.h"` 存在性校验;用合成样例自测过检测器会触发);S2=索引同步(01-网络编程/README:子领域 3→8 项(补 IO模型/传输层/连接管理/零拷贝/进程间通信)、demo 表 +5 行、待研究 3 项勾选 + 新增 5 项;03-系统编程/README:子领域行 + 已完成 demo 5 条子项);S3=STATE §二/§三 滚动 + 归档 rotate(completed 100+5→105→tail-100 = ID 129..226;schedule 30+1→31→tail-30) | +5 | 226 | 权威来源 9 WebFetch(本段 4:man7 unix(7) / sendmsg.2 / cmsg.3 / recvmsg.2;前段 5:man7 listen(2) / tcp(7) / readv(2) / sendfile(2) / RFC 5681 / RFC 9438 / 内核 ip-sysctl 等)+ 5 WebSearch + 0 agentic_search,均在硬约束内(≤10/≤15/≤5,上轮 WebFetch 13 超额后本轮按「每 demo ≤2 个权威源」控住);Python ×5 全部实跑断言通过(TCP 拥塞控制 12/12、零拷贝 10/10、backlog 10/10、scatter-gather 10/10、SCM_RIGHTS 34/34 = 76);C ×13 + Go ×9 无本机工具链(`which gcc cc clang tcc zig go cl` 全空,已复核)走人工代码审查 + 程序化校验(括号配平 / 缺 main / Go 未使用 import / include 存在性);行数审计 15 处拆分(传输层 c 439→261+187、py 420→187+247、go 363→182+192;连接管理 c 434→271+171、py 357→183+189、go 364→177+196;零拷贝 c 317→168+155、py 311→154+173;IO模型 c 327→179+153、py 413→205+225;SCM_RIGHTS c 449→229+227、py 310→282+80+55、go 340→256+96),并补回 4 篇 README 的 Go 运行命令(`go run main.go` → `go run .`);开发期修 bug 8 处(SCM_RIGHTS 建模:①sendmsg 把消息入到自身队列而非对端 → recvmsg 永远拿不到;②FdTable 从 0 分配 fd 而 0/1/2 应是 stdin/out/err,断言「接收方编号不同」失效 → 改为预留 0-2 且接收方预占两个编号;③open_file 初始 refcount=1 与 install 的 +1 重复计数,致「发送方 close 后 refs 应为 1」实测为 2 → 初始改 0;④模型里 RLIMIT_NOFILE 溢出时置 MSG_CTRUNC 属**臆造**(man7 只写「自动关闭」未规定标志)→ 去掉;⑤-⑦3 处断言自身错(检查点取在 break 之后、单次 recvmsg 只消费一条记录、用 fds_total_space(1) 当「只够 1 个」但其实 24 B 也够 2 个)→ 逐一改正,并因此发现真事实「CMSG_SPACE(4)=CMSG_SPACE(8)=24,1 个 fd 和 2 个 fd 占一样大的控制缓冲」写进 README;⑧main.py 拆分后 `from harness import OK` 只绑到导入时的值 0,check() 递增的是 harness 的全局 → 改为 `summary()` 由 harness 自己汇报);此外新检查顺带查出**13 个历史遗留文件的真实缺陷**(06-DevOps ×6 / 05-AI ×1 / 07-数据存储 ×3 等:C 文件括号不配平 3 个(如 state_diff.c 57 `{` vs 56 `}`)、Go 未使用 import 3 个(如 Target编码与泄漏/go/encoding.go 第二个 import 块里的 fmt)),已记入下轮待修清单;大类轮换 ✓ 上轮 02-Web开发→本轮 03-系统编程(索引 9-11 同属 02-Web开发与上轮同大类,顺延到 12,不记 skip);轮间保险通过(开局 shell date 实得 2026-09-15 18:00:26,距上轮 last_run 2026-09-15 16:24 = 96 分钟 > 45 阈值);槽位自检 ✓(18:00 槽,+26 秒落地,在 ±2 分钟内,无 slot_mismatch);占位锁 af16539 push OK;主轮 3d5a296 / e1c0d7f / 8bb1e87 逐个 push OK(fetch 后 `Current branch main is up to date`);notify: ok(HTTP 200 errcode=0,单次 curl 调用,发送前已确认 schedule.md 末行为 16:22 轮且内容不同、无本轮既有 notify 记录) |
| 2026-09-15 20:30 | 16 | M=04-移动开发/01-iOS 第三批 5 demo:AutoLayout 约束求解(Cassowary 单纯形/双单纯形 + 误差变量 ±e + strength 可加符号 required/strong/medium/weak + 加权误差最小化,难点校正:AutoLayout 是**线性规划**不是迭代松弛)、SwiftUI 状态管理(Observation 按**属性**而非按对象追踪 + withObservationTracking 只记录首次求值且 one-shot + @State/@StateObject/@ObservedObject/@Bindable 所有权语义)、Swift 结构化并发(SE-0304 job/task tree + SE-0306 actor 可重入=挂起点之间互斥 + exclusive executor + 协作式取消只置标志向下传播 + 优先级升级)、dyld pre-main 四阶段(Load dylibs / Rebase&Bind / ObjC setup / Initializers + `+load` 按 IMP 直调不继承 vs `+initialize` 懒+走 objc_msgSend+**会被继承**须判 self==Parent + Swift 全局/静态常量懒初始化)、Core Animation 渲染管线(Render Loop 五段 + deadline=下一 VSYNC + commit hitch vs render hitch + 脏标记三层各自合并 + 离屏渲染触发条件 + shouldRasterize 盈亏平衡)。统一 Python + Swift + ObjC 三实现,5 段自检 | S1=体检工具改造(`_docs/tools/syntax_sanity.py`:改单遍字符扫描器剥离注释/字符串、修 JS 正则字面量误判、加目录过滤与 CRLF 汇总统计)+ 顺带查出并修 4 处真实缺陷(2 处 Go 未使用 import、1 处 Go 重复 import 块、1 处 Python 非法转义);S2=索引同步(01-iOS/README:子领域表 4→7 项(新增 UI框架/启动优化/渲染)、已完成 demo +5 行、待研究勾 5 项新增 4 项;04-移动开发/README:01-iOS 说明行扩展 + demo 表 +5 行 + 待研究勾 2 行);S3=STATE §二/§三 滚动 + 归档 rotate(completed 100+5→105→tail-100 = ID 134..231;schedule 30+1→31→tail-30) | +5 | 231 | 权威来源 5 WebSearch(dispatch_sync 死锁与 queue-specific 检测 / SE-0306 actor reentrancy / priority escalation / dyld pre-main 四阶段 / `+load` 顺序与 `+initialize` 继承 / Swift lazy global dispatch_once / Core Animation render loop)+ 0 WebFetch(前段已用满 10/10)+ 0 agentic_search,全在硬约束内(≤5/≤10/≤5);Python ×5 **全部实跑断言通过**(cassowary_check 23/23 / observation_check 17/17 / concurrency_check 26/26 / startup_check 24/24 / render_loop_check 29/29 = **119 项全绿**,本机实测);Swift ×5 + ObjC ×5 无本机工具链(`which clang swift swiftc gcc` 全空,已复核)走人工代码审查 + `syntax_sanity.py` 结构检查(`**/*.py|*.c|*.go|*.js|*.ts|*.rs` 573 文件 0 问题、`**/*.swift|*.m|*.h` 54 文件 0 问题);行数审计整改 3 处(concurrency_check.py 397→300、startup_check.py 305→299、render_loop_check.py 313→298,手段是删分隔线注释与压缩 docstring),终态源文件全 ≤300 行、README 全 ≤196 行;开发期修 bug 共 12 处(择要:①demo 229 把工厂函数当生成器传 → 整文件重写为 loop_core.py + concurrency_check.py 两模块并统一走 make_task/spawn_root/spawn_child;②`yield ("await", singleTask)` 被 `list()` 迭代 → 改三分支(ALL_CHILDREN/list-tuple/单任务);③finished_at 用 startswith 前缀匹配把子任务 `group>slow` 误当父任务 group → 改精确匹配 + 子任务名加父级前缀;④~⑥demo 229 三处断言自身写错而实现正确(actor 同步段实为 10 不是 5、取消后 processed 实为 2 不是 3、父任务自己不检查取消故正常返回)→ 逐一改成如实断言;⑦startup 边界断言 `initializers/3` 实测 33.4% 恰好不满足 → 改 `/2` 并把实测值打进 detail;⑧⑨demo 231 `42/1.4 == 30` 浮点比较失败 → 改 `abs(cost-30*C)<1e-9`、以及一条同义反复的死断言 → 改为对比标脏 1 次 vs 1000 次;⑩「CATextLayer 会自己 setNeedsDisplay」恒真断言 → 新增 auto_redraw 字段做真实模拟;⑪ObjC/Swift 源码内嵌套双引号编译不过 → 统一换中文书名号「」;⑫ObjC `char buf[128]` 装不下完整事件序被 snprintf 截断 → 放至 256);大类轮换 ✓ 上轮 03-系统编程→本轮 04-移动开发(索引 13-15 同属 03-系统编程与上轮同大类,顺延到 16,不记 skip);轮间保险通过(开局 shell date 实得 2026-09-15 20:01:11,距上轮 last_run 2026-09-15 18:26 = 95 分钟 > 45 阈值);槽位自检 ✓(20:00 槽,+71 秒落地,在 ±2 分钟内,无 slot_mismatch);占位锁 63dd09f push OK;主轮 7be121e / 0146c7e / c4f49fc / e20ed90 / af4319d 逐个 push OK;S1 7f4a760 push OK;notify: ok(HTTP 200 errcode=0,单次 urllib 调用,发送前已确认 schedule.md 末行为 18:26 轮且内容不同、无本轮既有 notify 记录) |
| 2026-09-15 22:22 | 19 | M=05-AI与机器学习/01-深度学习 第二批 5 demo:LayerNorm/RMSNorm(统计量取自单样本特征维、与 batch 无关 —— 实测 max\|Δy\|=0;LN 平移+缩放双不变 vs RMSNorm 只缩放不变;解析反向三路 + γ 位置坑;Pre/Post-LN 梯度首末比 3.58 vs 0.67、量级差 3.9e6)、Softmax+交叉熵(max-shift 让 200 组大 logits 从 80 组 inf 变全有限;∂ℓ/∂x = p−y 差分验证 3.9e-10;mean 除数是 Σw_{y_n} 非 N;平滑后 loss ≥ ε·log C;log_softmax 防下溢)、残差连接/恒等映射(退化问题复现 7.62→9.36 vs 4.34→1.50;∂ε/∂x_l = ∂ε/∂x_L·∏(1+∂F/∂x) 里那个「1」;首末梯度比 93.9 vs 3.3;shortcut 消融 恒等 1.50 < 缩放 3.50)、学习率调度与梯度裁剪(原文公式 3 两分支恰在 step=warmup 相交、峰值 6.9877e-04;SGDR 重启点 0/10/30/70 且周期倍增;AdamW 衰减强度比 1.000001 vs Adam+L2 1.098;按范数裁剪 cos=1、按元素偏 11.59°)、混合精度训练(FP16 归零阈值 2^-25、20.75% 梯度静默丢失;S≥2^18 后精度饱和于 2^-11;纯 FP16 权重 20000 步纹丝不动 vs FP32 master 累计 2e-3;FP16 累加误差 1.06e-2 vs FP32 0);统一 Python + Go 双实现 + 5 段自检,合计 131 项断言全绿 | S1=01-深度学习/README(子领域表 +5 行、已完成 demo +5 行、待研究勾 3 项新增 4 项)+S2=05-AI与机器学习/README(01-深度学习 demo 行补 232-236、待研究勾 LayerNorm 并指向新目录)+S3=索引同步(STATE §二/§三 滚动)+归档 rotate(completed 100+5→105→tail-100 回 100 / schedule 30+1→31→tail-30 回 30) | +5 | 236 | 权威来源 10 WebFetch(arXiv:1910.07467 RMSNorm / arXiv:2002.04745 On LayerNorm in Transformer / arXiv:1607.06450 LayerNorm / arXiv:1603.05027 Identity Mappings in Deep Residual Networks / arXiv:1710.03740 Mixed Precision Training / arXiv:1711.05101 Decoupled Weight Decay / arXiv:1608.03983 SGDR / arXiv:1211.5063 Pascanu 梯度裁剪 / arXiv:1512.03385 ResNet / pytorch.org CrossEntropyLoss)+ 3 WebSearch(FP16 下溢边界 / clip_grad_norm_ 与 AdamW 默认值 / Transformer 公式 3 与 warmup=4000)+ 0 agentic_search —— 全在硬约束内(≤5/≤15/≤10/≤5,WebFetch 顶格 10/10);Python ×5 **全部实跑断言通过**(layernorm_check 22/22 / softmax_ce_check 27/27 / residual_check 20/20 / schedule_check 30/30 / mixed_precision_check 32/32 = **131 项全绿**);Go ×5 无本机工具链(`which go` exit 127,已复核)走人工代码审查 + `_docs/tools/syntax_sanity.py` 结构检查 0 问题;行数审计一次过(源文件 80~299 行全 ≤300,README 137~177 行全 ≤200);开发期修 bug 8 处(① RMSNorm 反向把 γ 写在求和之外 → 相对误差 8.99e-02,移入求和内部后 2.05e-10;② 「缩放不变性」残差 2.13e-05 实为 eps 造成 → 改为 eps=EPS 与 eps=0 两组对照并把原因写进 README;③ Pre/Post-LN 梯度剖面初版用 max/min 且 depth=24 得**相反结论** → 改用「首块/末块比」+ 多 seed 中位数 + depth∈{4,8,16,32};④ 残差 demo 无归一化时 depth=32 发散(初始损失 2.9e+06)→ 两种架构分支入口都加逐样本 LN 且 lr 0.02→0.005;⑤ shortcut 带参投影在 lr=0.005 仍 nan → 换无参、条件数良好的 0.9·h[:, ::-1] 倒序投影;⑥ SGDR `step % cycle_len` 在周期倍增时算错 → 改「先累加周期起点」;⑦ tiny_net_train 的 W2 形状 (H,1) 与 diff@W2.T 不匹配 → 改 (H,H) 并新增 out_scale;⑧ warmup 对照实验全部 nan → 系统扫描 (momentum × out_scale × peak) 后选定 out_scale=30/peak=2e-3,另加 out_scale=10/peak=1e-3 作温和对照);另混合精度自检 5 项**断言自身错**(实现正确)已逐一修正:下界比较方向写反、扫描结果下标错位(r[2] 实为 rel)、亚 ulp 累积用点值断言改为区间、数量级阈值从 1e4 收到 1e3、动态缩放日志取的是「处理完该步之后」的 scale;LayerNorm 计时断言在并发负载下不稳定(1.90x→1.07x)→ 硬判据换成确定性的「LN 归约 2 次 vs RMSNorm 1 次」,计时仅打印;大类轮换 ✓ 上轮 04-移动开发→本轮 05-AI与机器学习(next_index 17/18 与上轮同大类顺延到 19,不记 skip);轮间保险通过(last_run 2026-09-15 20:30 距 22:01 = 91 分钟 > 45 阈值);槽位自检 ✓(shell date 实得 2026-09-15 22:00:58 +0800,22:00 槽 ±2 分钟内,无 slot_mismatch);占位锁 aaddd46 push OK | **notify: ok**(HTTP 200 errcode=0,单次 urllib/curl 调用,发送前已确认末行无 notify 字段)
| 2026-09-16 00:10 | 23 | M=06-DevOps/01-容器化 第二批 5 demo(Docker 层缓存失效 / 多阶段构建 / UserNamespace UID 映射 / rootless 容器 / cgroup v2+eBPF 附加与 BPF token) | S1=归档滚动截断(completed 100+5→105→tail-100 = ID 142..241;schedule 30+1→31→tail-30);S2=父 README 索引同步(01-容器化/README:「已完成 demo」表 +5 行 237-241、待研究 6 项全部收官并重列 5 项新清单;06-DevOps/README:「已完成 demo 索引」+2 行并勾掉「Dockerfile 最佳实践(多阶段构建)」);S3=STATE §二/§三 滚动 | +5 | 241 | 权威来源 6 WebFetch(Docker 官方 build cache invalidation 页 / man7 user_namespaces(7) / man7 newuidmap(1) / kernel cgroup-v2 / kernel libbpf program_types / kubernetes CRI;另有 2 次 Docker 官方抓取失败改走 WebSearch)+ 5 WebSearch(Docker cache invalidation 原文 / BPF token 与 CAP_BPF / containerd 与 runc 架构 / Docker multi-stage 官方页 / Docker rootless 前置与限制)+ 0 agentic_search,全在硬约束内(≤10/≤15/≤5);Python ×5 **全部实跑断言通过**(layer_cache 18/18 · multi_stage 20/20 · uid_map_model 39/39 · rootless_env 43/43 · cgroup_check 37/37 = **157 项全绿**);C ×2 + Go ×5 无本机工具链(`which gcc cc clang go` 全空,已复核)走人工代码审查 + `_docs/tools/syntax_sanity.py` 结构检查(06-DevOps 全量 94 文件 0 问题、0 CRLF);行数审计整改 2 处超限(cgroup_bpf.py 365→拆 cgroup_bpf.py 216 + cgroup_check.py 159;uid_map_model.py 337→294,手段为删章节前空行、折叠多行调用、删未引用的 render());终态源文件全 ≤300 行、README 全 ≤151 行;开发期修 bug 18 处(①layer_cache 4 处断言自身错:好顺序步数算成 6 步实为 4 hit/2 miss、坏顺序 5 步非 6 步、mtime 断言比较对象取错、chmod 断言步数错;并把 mtime 显式建模进 Entry 三元组,让「mtime 不参与校验和」这条规则真正可测;②multi_stage:`COPY --from` 只建模了源路径未建模 src→dst 映射致「源阶段没有 /app」,改 copies 为 (src,dst) 列表;branched() 的 builder 误写成 `FROM alpine` 而非 `FROM base`,致 BuildKit 可达集少了 base、断言期望值才是错的,改正后 3 阶段 vs 4 阶段;③uid_map_model:用 write() 构造初始命名空间伪映射触发 R8(映射父 ns UID 0 需 CAP_SETFCAP),新增 `UidMap.pseudo()` 绕过写入校验;340 行紧凑写法从 i=0 起编号又撞 R8,改从 1 起;④rootless_env:Go 版 printf 里内联立即执行函数字面量可读性差改具名变量;⑤cgroup_bpf:测试 4 用 `lambda: (...).throw(...)` 伪断言、测试 5 `(procs.clear() or True)` hack、`write_subtree_control` 里残留的无意义预检查,三处重写为真实函数 remove_controller();`can_load` 缺「无 token 时能力须在 init userns」的建模致 T2 断言失败,给 Userns 加 is_init 字段);大类轮换 ✓ 上轮 05-AI与机器学习→本轮 06-DevOps(索引 20/21/22 与上轮同大类顺延到 23,不记 skip);轮间保险通过(开局 shell date 实得 2026-09-16 00:00:29,距上轮 last_run 2026-09-15 22:01 = 119 分钟 > 45 阈值);槽位自检 ✓(00:00 槽,+29 秒落地,±2 分钟内,无 slot_mismatch);占位锁 f95a64c(首次 push 被 remote rejected,重试即 Everything up-to-date,实为成功) | **notify: ok(HTTP 200 errcode=0,单次 curl 未重试)** |
| 2026-09-16 02:14 | 27 | M=07-数据存储/01-关系型 5 demo:主从复制与 binlog(SBR/RBR/MIXED 格式 + 三线程链路 + GTID 幂等 + 半同步 AFTER_SYNC/AFTER_COMMIT 等待点与超时降级)、逻辑复制与 CDC(PG logical decoding 流水线 + 复制槽 confirmed_flush_lsn 持留 WAL + REPLICA IDENTITY + Debezium before/after/op 事件与 offset 续传 + snapshot→增量衔接)、Join 算法(朴素/块嵌套/hash build-probe/GRACE 分区/sort-merge 五算法成本模型 3(M+N) 与重复键回溯)、物化视图(持久化 + 全量 REFRESH 阻塞语义 vs CONCURRENTLY diff 增量 + 唯一索引前置)、在线 DDL(INSTANT/INPLACE/COPY 矩阵 + MDL 两端独占 + row log + 行版本 64 上限 + gh-ost binlog 流方案) | S1=01-关系型/README 索引同步(已完成 demo 表 +5 行 242-246、待研究勾 4 项:主从复制binlog/逻辑复制CDC/物化视图增量刷新/在线DDL);S3=STATE §二/§三 滚动 + 归档 rotate(completed 100+5→105→tail-100 = ID 147..246;schedule 30+1→31→tail-30) | +5 | 246 | 权威来源 8 WebFetch(MySQL replication-formats / replication-semisync / replication-gtids、PG logicaldecoding、PG rules-materializedviews、MySQL innodb-online-ddl-operations、gh-ost README、Debezium architecture)+ 1 WebSearch(CMU 15-445 join 算法讲义,Spring 2023 notes/slides 内容经检索摘录阅读)+ 0 agentic_search,全在硬约束内(≤5/≤15/≤10/≤5);Python ×5 **全部实跑断言通过**(demo1 5 组 / demo2 7 断言 / demo3 5 组 / demo4 5 组 / demo5 5 组);Go ×5 无本机工具链走人工代码审查 + 括号配平程序化校验(剔除注释/字符串后 {}()[] 全配平);行数审计一次过(源文件 79~192 行全 ≤300,README 72~99 行全 ≤200);开发期修 bug 5 处(①demo2 poll 用批次尾 streamed_lsn 当事件级 LSN → 首事件处理完 offset 顶到批尾、批内其余全被跳过(events 空),改为事件自带 LSN —— 实跑暴露的真实现 bug;②demo2 两处断言自身错:update 的 before 是纯旧行不含 pk 包装、replica-identity 过滤条件把全部 before=None 事件都算进去 → 逐一改精确;③demo2 消费路径改为从 _pending 续取(贴近 walsender 已流式未 ACK 语义)后 test3 才能通过;④demo3 join 配对数 2×2+1+1+1=7 断言写成 6;⑤demo3 成本链 hj<smj<bnl<snl 写反 —— B=100 下块嵌套(6500)实际优于 SMJ(7500),正确结论是 hash 最快 + 均远优于朴素,变量打包顺序也把 N/m 装反一并修正;另 demo4 一处 query 入参传 Base 对象应传 rows、删 1 行分组仍在(值 3→2 是 update 非 delete)需删 2 行才真正消失 —— 均为实跑暴露);大类轮换 ✓ top_pos=6 → 07-数据存储(sub_pos[07]=0 → 索引 27 关系型),上轮 06-DevOps;轮间保险通过(开局 shell date 实得 2026-09-16 02:00:47,距上轮 last_run 2026-09-16 00:10 = 110 分钟 > 45 阈值);槽位自检 ✓(02:00 槽,+47 秒落地,±2 分钟内,无 slot_mismatch);占位锁 26171ce push OK | **notify: ok**(HTTP 200 errcode=0,单次 curl 未重试,发送前已确认本轮无既有 notify 记录) |
| 2026-09-16 04:26 | 31 | M=08-安全/01-密码学 第二批 5 demo:SHA-3/Keccak-f[1600] 海绵函数(FIPS 202 五步轮函数 + pad10*1 域分离 + SHAKE XOF)、HKDF(RFC 5869 Extract-then-Expand,TLS 1.3 秘密树底座)、ECDSA-P256+RFC 6979 确定性签名(P-256 仿射点运算 + HMAC-DRBG 消随机数)、HOTP/TOTP(RFC 4226/6238 动态截断 + 30s 时间步 + resync 窗口)、PBKDF2(RFC 8018 §5.2 + RFC 6070 全 6 组向量含 NUL 陷阱) | S3=索引同步(01-密码学/README 已完成表 +5 行、勾掉 HKDF/ECDSA 待研究项;08-安全/README 已完成表 +5 行)+ 归档 rotate(completed 100+5→105→tail-100 = ID 154..251;schedule 31+1→32→tail-30)+ STATE §二/§三 滚动 | +5 | 251 | 权威来源 7 WebFetch(RFC 5869/6979/4226/6238/6070 全文向量 + keccak.team 规格摘要 + SEC2 v2 PDF 经 curl 下载后 pypdf 提取 secp256r1 参数)+ 1 WebSearch(NIST SHA-3 官方示例值确认)+ 1 次额外下载对照(XKCP 官方 CompactFIPS202.py)+ 0 agentic_search,全在硬约束内(≤5/≤15/≤10/≤5);Python ×5 **全部实跑断言通过**(sha3 5 组含 NIST 官方向量+hashlib 对照 / hkdf A.1-A.3+A.7 全 PASS / ecdsa RFC 6979 A.2.5 k+r+s 逐字段 PASS / hotp_totp 附录 D+B+§5.4 全 PASS / pbkdf2 RFC 6070 全 6 组 PASS);C ×5 + Go ×5 无本机工具链(`which gcc cc go` 全空,已复核)走人工代码审查 + 括号配平程序化校验(12 个源文件剔除注释/字符串后 {}()[] 全配平);行数审计:ecdsa.c 初稿 327 行超限当场拆为 ecdsa.c 209 + ecdsa_test.c 124 + ecdsa.h,终态源文件全 ≤300 行、README 全 ≤86 行;开发期修 bug 6 处(①sha3:RC 轮常数表从 keccak.team 抓取时两栏排版被 WebFetch 压平,抄录错位第 10 项起全错+混入不存在常量,靠官方 LFSR 生成式逐位重推导 + 文件内自产断言捕获,另修 padding 末块剩 1 字节时 0x06|0x80 须合并为 0x86 的边界(否则会平白多一个块);②hkdf A.7 OKM 转录漏字符 hex 长度奇数即报错;③ecdsa 仿射点加倍点路径 r 与 p1 指针别名,先写 r->x 再读 p1->x 读到新值,x3/y3 先入局部量再拷出修复;④ecdsa demo5 d/rr/ss 误声明 uint8_t[4] 应为 uint64_t[4];⑤hotp_totp C 版 demo4 时间步逻辑错(89/30=2 非同窗),重写为 verify_wide ±1 步窗口;⑥pbkdf2 目录误用 PKCS5-PBKDF2 命名统一为 PBKDF2;另 Windows Git Bash heredoc 吞反斜杠致内联正则反复失败,改写临时脚本文件跑通 —— 教训:**权威资料两栏表格抓取后必须程序化复核常量表,逐字段断言比抄录可靠**);大类轮换 ✓ top_pos=7 → 08-安全(sub_pos[08]=0 → 索引 31 密码学),上轮 07-数据存储;轮间保险通过(开局 shell date 实得 2026-09-16 04:00:47,距上轮 last_run 2026-09-16 02:14 = 106 分钟 > 45 阈值);槽位自检 ✓(04:00 槽,+47 秒落地,±2 分钟内,无 slot_mismatch);占位锁 3864d1a push OK(首拉遇未暂存改动 stash→pull→pop 解决) | notify: ok(HTTP 200 errcode=0,单次 curl 未重试,发送前已确认本轮无既有 notify 记录) |
| 2026-09-16 06:26 | 34 | M=09-语言学习/Python 第二批 5 demo:异常链与异常组(PEP 3134 __context__ 四条语义 + __cause__/__suppress_context__ + 回溯两种链接消息;PEP 654 组构造自动升级、subgroup/split/derive、except* 逐子句 split 执行模型、未匹配叶子 reraise、裸异常包装、顺序敏感)、typing与Protocol(PEP 544 结构化子类型三原则 + runtime_checkable 只查存在性 + data/non-data protocol 的 issubclass 边界 + 默认实现仅显式继承;PEP 695 type 语句 TypeAliasType 惰性求值/递归别名免引号 + class C[T] Generic 隐式入 MRO/orig_bases + __type_params__ + 覆盖层作用域默认值不可见 NameError)、slots与内存布局(datamodel 原文:类级描述符+定长槽位阻止 __dict__,实测 344B→48B/百万实例 344MB→48MB;继承穿透/多继承布局冲突/类属性默认值覆盖描述符/变长内建类型/迭代器赋值变空迭代器;dataclass slots=True 返回新类+frozen 走 object.__setattr__)、字符串与编码(PEP 393 按 maxchar 三档 1/2/4B 一个字符拉满整串;compact 内联 ASCII 头 42B vs latin-1 60B;UTF-8 变长+strict/surrogatepass+DecodeError 位置;码点语义;驻留与常量折叠;Django 实测降 65%)、decimal与fractions(三元组表示+尾零显著性+compare_total;构造看输入运算看上下文 prec=28 HALF_EVEN;Knuth 结合律 prec=8 破坏;Decimal(3.14) 暴露二进制真值;NaN 语义;% 符号随被除数 // 向零;Fraction 约分/float 真值/limit_denominator 355/113) | S3=Python/README 已完成表 +5 行(252-256)+ 待研究勾 5 项(3/4/6/15/16,剩 #9)+ 归档 rotate(completed 100+5→105→tail-100 = ID 157..256;schedule 30+1→31→tail-30) | +5 | 256 | 权威来源 10 WebFetch(PEP 3134/654/544/695/393 全文 + dataclasses/decimal/fractions 官方文档)——配额用满;datamodel §3.3.2.4 两次 WebFetch 均在 slots 节前截断,改 curl 下载 docs.python.org HTML 后本地提取该节原文精读(与 PDF 精读同路线);+0 WebSearch + 0 agentic_search,全在硬约束内(≤5/≤15/≤10/≤5);Python ×5 **全部实跑断言通过**(12/10/10/7/11 = 50 项);行数审计:main.py 153~288 行全 ≤300(异常链初稿 320 超限当场压缩),README 112~137 行全 ≤200;开发期修 bug 8 处(①except* ExceptionGroup 实为运行期 TypeError 非 SyntaxError;②自定义组重写 __new__ 后仍需 __init__ 吞参否则 "takes no keyword arguments";③issubclass data protocol 报错文本是 "non-method members";④废话断言 "assert s2.a==0 or True" 仍求值炸掉;⑤迭代器 slots 的 list() 得 [] 非 StopIteration;⑥字符宽需 getsizeof 斜率差分(不同 kind 结构头 42/60B);⑦"hello"+"!" 常量折叠同对象,验证驻留须运行期构造;⑧decimal:ROUND_UP 非四舍五入+顺序挪 1/7 断言+链式比较 "nan==nan is False"+连分数 20 步仅 3.7e-9 需 30 步 —— 断言出错率高于实现再次应验,8 处全靠实跑暴露);大类轮换 ✓ top_pos=8 → 09-语言学习(sub_pos[09]=0 → 索引 34 Python),上轮 08-安全;轮间保险通过(开局 shell date 实得 2026-09-16 06:00:27,距上轮 last_run 2026-09-16 04:26 = 94 分钟 > 45 阈值);槽位自检 ✓(06:00 槽,+27 秒落地,±2 分钟内,无 slot_mismatch);占位锁(06:01)→ 主轮(06:26)→ STATE 压缩(06:27/06:28) 三次 commit 均 push OK | notify: ok(HTTP 200 errcode=0,urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录;curl --data-binary @file 在本机 Git Bash 的路径映射 quirk 持续,沿用 09-15 起的 urllib 通道) |
| 2026-09-16 08:25 | 35 | M=10-逆向工程/01-二进制逆向 第三批 5 demo:ARM64指令集与调用约定(A64 定长 32 位编码位域级还原+别名折叠 mov/ret 是伪指令+SP 与 XZR 共用编号 31 靠位域区分;AAPCS64 x0-x7 传参/x8 间接结果/x16-x17 veneer 可破坏/x19-x28 callee-saved/SP mod 16/帧记录双字与回溯链;序言形态四分类)、Ghidra反编译流水线(官方 Main Work Flow 15 阶段+主简化循环六步+SLEIGH→p-code+Heritage 支配边界 phi 放置与支配树 DFS 重命名+INT_SUB 规范化)、Ghidra-headless自动化(-import/-process 互斥等命令行组合约束+HeadlessContinuationOption 4×4 官方合并表+-okToDelete 安全门+getScriptArgs 与 askXxx 消费顺序)、二进制差分与函数匹配(官方 signature 三元组+prime 素数乘积及 uint64 回溢+MD index 六项加权公式与默认权重推论 拓扑层系数 0 时 top-down≡bottom-up、relaxed≡默认+分阶段匹配与 drill down+confidence S 型压扁+similarity 权重含只统计非库函数)、Ghidra脚本化批量重命名(SourceType 优先级链 USER_DEFINED>IMPORTED>ANALYSIS>DEFAULT+createLabel 重名幂等与 makeUnique 口径+计划/预演/事务三段式 非法名整批回滚) | S1=索引同步(10-逆向工程/01-二进制逆向/README 已完成表 +5 行 257-261、待研究勾 4 项(headless/脚本批量重命名/ARM64/反编译器内部流水线)新增 2 项、参考资料 +14 条;10-逆向工程/README 待研究勾 3 项(Ghidra 反编译器原理与脚本化分析/ARM64 指令集速览/二进制差分)+ 参考资料 +11 条) + S3=归档 rotate(completed 100+5→105→tail-100 = ID 164..261;schedule 30+1→31→tail-30)+ STATE §二/§三 滚动 | +5 | 261 | 权威来源 10 WebFetch(Arm A64 ISA Index by Encoding + 102374 PCS、AAPCS64 aapcs64.rst、Ghidra 反编译器 Doxygen 总览、analyzeHeadless README 12.0/9.1、BinDiff concepts.md、graph_util.h 官方 raw 404 改镜像、FlatProgramAPI javadoc、SourceType javadoc)+ 9 WebSearch —— WebFetch **用满配额 10/10**、agentic_search 0;Python ×9 **全部实跑断言通过**(75+21+33+44+44 = 217 项;arm64_decode.py 与 headless_cli.py 为纯模块,import 运行退出码 0);C ×1 + Go ×9 无本机工具链(which gcc cc clang go rustc 全空,已复核)走人工代码审查 + syntax_sanity.py 括号配平/未用 import/CRLF 校验 0 问题;行数审计 9 次拆分:arm64_decode.py 388→296+166、pcode_ssa.py 355→296+66、pcode_ssa.go 502→239+216+64、bindiff_pipeline.py 332→268+107、binary_diff.go 336→258+220+65+44、ghidra_symbols.py 313→228+99,终态源文件全 ≤300 行、README 全 ≤197 行;开发期修 bug 约 30 处(ARM64 7 / Ghidra 流水线 6 / headless 3 / BinDiff 9 / Ghidra 脚本 5),最值得记的两条:**把块字典当邻接表传给 md_of_functions 会让遍历退化成遍历字典键、把块名当边(孤立函数 MD index 不再为 0,断言当场失败)**;**最小 SSA 必须配合活跃性检查否则插入无人使用的 phi**;断言出错率高于实现再次应验(元组下标写反一次挂 4 条、pair idx 位域判反、素数前缀依赖首次出现顺序被自己后续调用打乱);大类轮换 ✓ top_pos=9 → 10-逆向工程(sub_pos[10]=0 → 索引表第 35 项 二进制逆向 第三批),上轮 09-语言学习;轮间保险通过(开局 shell date 实得 2026-09-16 08:02:18,距上轮 STATE last_run 2026-09-16 06:26 = 96 分钟 > 45 阈值);槽位自检 08:00 槽,shell 落地 08:02:18(+2 分 18 秒,含会话启动耗时,未构成「明显不符」,不记 slot_mismatch);占位锁(08:02)→ demo 257-260(08:26)→ demo 261(08:31)三次 commit 均 push OK | notify: ok(HTTP 200 errcode=0,urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录;curl --data-binary @file 在本机 Git Bash 的路径映射 quirk 持续,沿用 09-15 起的 urllib 通道) |
| 2026-09-16 23:21 | 37 | M=11-性能分析/01-系统级剖析 第三批 5 demo:bpftrace前端DSL(短写别名 t/k/kr/u/ur/p/i/f/fr 展开 + 六级优先级爬升 + **直方图桶 0 是 [0,1) 而非 [1,2)** + `delete(@map, key)` 的 key 是独立实参);USE方法检查清单(**U/S/E 三列 × 物理 10 + 软件 4 类共 37 格** + 阈值 70/100 + CPU 使用率必须扣除 iowait + 瓶颈格 = 前一项的饱和);容器性能归因(cgroup v2 No-Internal-Process/Top-down 两约束 + `cpu.stat` 前 3 层累计 vs 后 5 不含后代的口径差 + `cpu.stat.local.throttled_usec` 含祖先继承 ⇒ 自身/祖先/宿主三分归因 + shares 口径下限用 busy shares);runqlat与调度延迟(wakeup→switch 以 tid 配对且 `next` 才是开跑者 + `schedstat` cpu 行 9 字段 domain 行 45 + 占比须先差分 + **sched_schedstats=0 时 run_delay 恒 0 ≠ 0%** + M/M/1 Wq=ρ/(1-ρ) 0.9→9 倍 0.98→49 倍);中断与软中断剖析(架构向量无 IRQ 号不能按白名单判 + `softnet_stat` 十六进制无表头按 dropped/squeeze 三分归因 + `/proc/stat` intr 总数 > 字段和而 softirq 恰等于 + smp_affinity 掩码不可全 0 且逗号组低位在先) | S1=索引同步(11-性能分析/01-系统级剖析/README 已完成表 +5 行 102-106、待研究勾 3 项(bpftrace 前端 DSL/容器分析/USE 落地)新增 3 项、参考资料 +14 条;11-性能分析/README 待研究勾 2 项(USE 方法、容器性能分析)+ 参考资料 +1 条) + S3=归档 rotate(completed 100+5→105→tail-100 = ID 167..266;schedule 30+1→31→tail-30)+ STATE §二/§三 滚动 | +5 | 266 | 权威来源 7 WebSearch + 10 WebFetch(bpftrace language.md / one-liners / man8 / cheat-sheet;Gregg usemethod + use-linux;kernel iostats;cgroup-v2 内核文档 + Gregg DockerCon 2017;runqlat + kernel sched-stats;irq-affinity + ia64/irq-redir + Red Hat 调优指南 §4.3 + proc.html)——WebSearch 7/15、WebFetch 10/10、agentic_search 0/5,全在硬约束内;Python ×5 **全部实跑断言通过**(30+38+32+38+53 = 191 项);C ×5 + Go ×5 无本机工具链(which gcc cc clang go rustc 全空,已复核)走人工代码审查 + syntax_sanity.py(70 文件,0 个语法/结构问题);行数硬约束审计:5 个 demo 合计 **12 次拆分**全部通过 `_docs/tools/split_source.py` 完成 —— bpftrace_py 674→181+159+203+172、bpftrace_go 422→185+251、use_py 383→272+127、use_go 347→236+119、use_c 303→160+161、runqlat_py 296(BucketIndex 重写后即合规)、runqlat_go 413→260+248、runqlat_c 413→246+179、irq_py 384→204+201、irq_go 560→249+210+126、irq_c 431→197+248;终态源文件全 ≤300、README 全 ≤200(C 侧因无工具链统一改用「实现头 `#include "x_impl.h"`」而非第二个 .c,文本包含保证所有 static 定义同一 TU、零链接风险);开发期修 bug 约 40 处,最值得记的四条:**① runqlat 桶 0 标签写反(1->1 与自带的 bucket_index/官方输出 0->1 三处互相矛盾),同一 bug 让断言 IndexError 才暴露;② `0.9/0.1` 在 IEEE754 下是 8.999999999999998,浮点等值断言必须给容差(断言出错率高于实现又一次应验);③ C 的 `strtok` 会改写入参,fixture 必须是可写 `char []`,用 `const char *` 会写只读段(无工具链环境下唯一靠人工审查拦住的段错误);④ C 的 `check()` 是三参签名(无默认参数),两参调用点是硬编译错误,写了括号/引号感知的实参拆分脚本逐处修完再验(实参分布 {3: 39})**;本轮 5 个 demo 均加了一次性**消歧**:runqlat 只给直方图不给按栈归因(火焰图化列入待研究)、中断绑核只覆盖掩码语义不覆盖实际调优;大类轮换 ✓ top_pos=10 → 11-性能分析(sub_pos[11]=0 → 索引表第 37 项 系统级剖析 第三批),上轮 10-逆向工程;**槽位自检 ⚠ 触发时刻严重不符:本槽应为 14:00(±2 分钟),shell date 实得 2026-09-16 22:53～23:21(偏差约 9 小时),判定为机器 14:00–22:00 期间休眠/离线导致定时任务未按时唤醒,本轮为**补跑**;未记 `slot_mismatch` 中止(那会零产出),按「补跑一轮完整巡检」处理并如实记录偏差;轮间保险通过(last_run 2026-09-16 22:54,距开局 shell date 22:53 为 −1 分钟 —— 触发的是 22:54 那条占位锁,同槽内续跑而非独立新轮);占位锁 3f283e2(22:54)→ demo 262-265 18f7a13(23:10)→ demo 266 + 索引同步 1fca5df(23:21) 三次 commit 均 push OK | notify: ok(HTTP 200 errcode=0,urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录;curl --data-binary @file 在本机 Git Bash 的路径映射 quirk 持续,沿用 09-15 起的 urllib 通道) |
| 2026-09-17 02:30 | 0 | M=01-游戏开发/01-服务端(索引 0 服务端第三批)5 demo:FlatBuffers 零拷贝(vtable/soffset/uoffset 线格式逐字节复现 flatcc 参考缓冲区 + 缺省不存储 + 前向兼容 + vtable 共享)、协议压缩 Snappy(官方 format_description.txt 全格式解码器 + 贪心编码器 + zstd Silesia 基准对比 + gRPC grpc-encoding 语境)、心跳与断线重连(Gaffer 33-ack 位图/回绕比较/1s 丢包推断/RTT EMA 10%/5s 会话超时/重连消息号去重恰好一次投递)、跨服 RPC(自研 req_id 帧协议 + gRPC PROTOCOL-HTTP2 逐项对照:stream-id/Length-Prefixed-Message/grpc-status trailer/PING/GOAWAY/幂等声明)、全局服务器(平台服进程分工 + 心跳摘除连带清在线表 + epoch 防旧实例 + 两阶段转区锁/移交/ack/超时回滚 + 全区 Top-N + rolereg 名字唯一) | S3=索引同步(01-服务端/README 覆盖矩阵 5 项待研究→已完成 + 路径)+ 归档 rotate(completed 100+5→105→tail-100 = ID 172..271;schedule 30+1→31→tail-30)+ STATE §二/§三 滚动 | +5 | 271 | 权威来源 7 WebFetch 命中(snappy format_description.txt 原文、gafferongames reliability 全文、flatcc binary-format.md 全文、zstd README 官方基准、grpc PROTOCOL-HTTP2.md 全文)+ 3 WebSearch(gaffer 正确 URL、flatbuffers 文档迁移后链接、MMO global server 架构)+ 0 agentic_search,WebFetch 9 次调用其中 2 次 404 未计入有效来源,硬约束内(≤10/≤15/≤5/≤5);Python ×5 **全部实跑断言通过**(16+26+23+12+25 = 102 项);Go ×5(205/269/222/300/300 行)无本机工具链走人工代码审查 + bracket_check.py 括号配平全 BALANCED;行数硬约束:Go 跨服RPC 与全局服务器初稿超 300 行当场压缩(删空行/弱断言/注释,终态恰 300);开发期实跑修 bug 6 处:①FlatBuffers 构建器漏给 soffset 留占位致回填覆盖 uoffset 槽(逐字节断言抓住);②Snappy copy4 扫描裸扫字节流误判 literal 数据为 tag(改按流结构解析);③LCG 低 8 位周期 256 致「随机」数据内 4 字节重复;④心跳回绕断言写反(5 比 3 新);⑤重连消息按 list 序取末位断言错(改集合校验);⑥全局服务器 sweep 误摘有心跳的 zone2(sweep 前续命)—— 断言出错率高于实现再次应验;大类轮换 ✓ top_pos=0 → 01-游戏开发(sub_pos[01]=0 → 索引表第 0 项 服务端,收「服务端」待研究清单全部 5 项),上轮 11-性能分析;轮间保险通过(开局 shell date 实得 2026-09-17 02:00:55,距上轮 last_run 2026-09-16 23:21 = 160 分钟 > 45 阈值);槽位自检 ✓ 02:00 槽落地 02:00:55(+55 秒,含会话启动,未构成明显不符);占位锁 0dbe4fb(02:01)push OK | | **notify: ok**(HTTP 200 errcode=0,urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录) |
| 2026-09-17 05:10 | 9 | M=02-Web开发/02-后端(索引 9 后端首批 Node.js 运行时主线)5 demo:Libuv线程池(UV_THREADPOOL_SIZE 吞吐 931/370/280ms 线性扩展 + 跨 API 传染 pool=1 下 dns.lookup 553ms/fs.readFile 550ms 被慢 pbkdf2 阻塞 + 运行时设置实测未生效)、Stream背压(write/drain "admit 后 < hwm" 语义 + hwm 阈值非限制 + 无背压峰值 200 vs 有背压 3 + pipe 25 次 pause 事件 + unpipe 后 data 不恢复流动)、Cluster与WorkerThreads(Windows SCHED_NONE 实测 8 串行请求 100% 落同一 worker 印证官方 70%/2-of-8 警告 + disconnect/kill 的 exitedAfterDisconnect + 结构化克隆原型丢失/Buffer→Uint8Array/URL 不可克隆 + transfer 后本端 0B + SAB Atomics 共享 + terminate exitCode=1)、async_hooks与AsyncLocalStorage(Timeout init→before→after 各 1 次 + PROMISE 因果链 10←6/11←10/12←11 + ALS 六项语义含 enterWith 泄漏与 disable GC 前置 + 3 交错请求序 1,2,3→2,2,2→3,3,3→1,1,1 零串扰 + createHook 迷你 ALS 复刻)、HTTP-Agent连接复用(keep-alive 5 请求 1 连接 reusedSocket=01111 + maxSockets=1 排队 252ms vs 4 并行 80ms + freeSockets 池化→server ~3s 关空闲→池移除→重连 + Node19 起 globalAgent keepAlive=true 实测) | S3=索引同步(Node.js/README 已完成 +5、待研究 4 项全勾并新增 undici/Stream 高阶/V8 GC 3 项;02-后端/README +5;02-Web开发/README +5)+ 归档 rotate(completed 100+5→105→tail-100 = ID 177..276;schedule 30+1→31→tail-30)+ STATE §二/§三 滚动 | +5 | 276 | 权威来源 6 WebFetch 命中(libuv threadpool 全文、async_hooks 全文、async_context 全文、worker_threads 全文、cluster 全文、http Agent 节)+ 4 WebFetch 404/截断未计有效(nodejs.org learn/streams 三种 URL 均已迁移、cli.html 在目标节前截断)→ 改走 curl 下载官方 HTML 本地提取原文(stream.html 857KB 提取 write/drain/hwm/Buffering/Two modes/Three states/pipe 节 + cli.html 提取 UV_THREADPOOL_SIZE 节逐字核对)+ 2 WebSearch(结果均无关未采用)+ 0 agentic_search,硬约束内(WebFetch 10/10 用满、WebSearch 2/15、demo 5/5);JS ×5 + TS ×5 **全部实跑断言通过**(9+14+18+16+9 = 66 项;Node 22 内置 --experimental-strip-types 使 TS 版亦可实跑,系本仓库 TS demo 首次全量实跑);行数审计:全部源文件 ≤300 行、README 67~80 行全 ≤200(本批无须拆分);开发期实跑修 bug 11 处:①Libuv 探针 readFile 串行发出测不到阻塞(改并发);②Stream E3 忘调 r.pipe(w) 挂起;③Stream E3a interval 采样 isPaused 只见暂停态(改 'pause' 事件计数);④断言字符串内单引号冲突语法错;⑤Cluster C 段忘 terminate 挂起;⑥terminate() 后再 once('exit') 永远等不到(terminate 的 Promise 在 exit 发出后才 resolve,直接用返回值;且挂起 promise 不阻止进程退出——事件循环排干后静默退出 0 无报错);⑦worker 停止后 resourceLimits 空对象(运行中快照);⑧TS 参数属性 strip-only 不支持;⑨TS 的 import * as cluster ESM 下无 on 方法(默认导入);⑩ALS E4 async 回调未 return 导致断言跑半截数据;⑪Agent E4 server keepAliveTimeout=100ms 时 hint−buffer(1000ms) 为过去时刻 socket 直接销毁不入池(改 2000ms 并等 3.5s,实测关闭点 ≈ 超时+1s)—— 断言出错率高于实现再次应验,11 处全靠实跑暴露;大类轮换 ✓ top_pos=1 → 02-Web开发(sub_pos[02]=1 → 索引表第 9 项 后端,收 Node.js 待研究清单全部 4 项并新增 HTTP-Agent),上轮 01-游戏开发;轮间保险通过(开局 shell date 实得 2026-09-17 04:00:55,距上轮 last_run 2026-09-17 02:30 = 90 分钟 > 45 阈值);槽位自检 ✓ 04:00 槽落地 04:00:55(+55 秒,含会话启动,未构成明显不符);占位锁 1f40f19(04:03)→ demo 272 02e2c7e(04:20)→ 273 44cbd3a(04:35)→ 274 58ddaf5(04:47)→ 275 7bc80c8(04:55)→ 276 4bf84bd(05:02)六次 commit 均 push OK(占位锁首拉遇未暂存改动报错,系 STATE.md 自身修改,先 commit 后 push 无冲突) | | notify: ok(HTTP 200 errcode=0,urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录) |
| 2026-09-17 06:01 | 13 | M=03-系统编程/02-进程与线程+02-协程(索引 13 进程线程协程,该子类目第二批:此前仅哲学家就餐)5 demo:fork与僵尸进程(COW/单线程子进程/fd 共享 open file description/僵尸占进程表槽/SIG_IGN→ECHILD/WEXITSTATUS 低 8 位 300→44/stdio 全缓冲双份输出)、生产者消费者(condvar 原子解锁+挂起+重加锁/while 谓词防虚假唤醒(signal≥1 且不累积)/哨兵与 close 广播/Go channel 三条 happens-before)、读者写者锁(POSIX 放行条件无 TPS 时实现自定义=策略自由度/同线程多读锁配对解锁 EAGAIN/tryrdlock EBUSY/Go RWMutex 写者偏好明文+禁递归读锁/双偏好策略差一个谓词:读流不断写者饿死)、最小有栈协程(ucontext 四件套/swapcontext 返回 0 语义/uc_link 后继与 NULL 退出/int-only 参数 UB 史/POSIX.1-2008 移除/无栈对照:helper 的 yield 不经 yield from 不传导/goroutine 10 万层深递归 vs 固定 64KiB 栈溢出)、CFS调度器(理想 CPU 模型/vruntime=delta×NICE_0/weight/红黑树最左选取/granularity 粒度/min_vruntime 单调+新实体放置/无 time slice 纳秒记账/6.6 起 EEVDF lag+VD 取代) | S3=索引同步(02-进程与线程/README 重写已完成+待研究、02-协程/README 勾掉最小有栈协程、03-系统编程/README +5 条)+ 归档 rotate(completed 100+5→105→tail-100 = ID 182..281;schedule 30+1→31→tail-30)+ STATE §二/§三 滚动 | +5 | 281 | 权威来源 8 WebFetch 命中(man7 fork(2)/wait(2)/pthread_cond_wait(3)/makecontext(3)、docs.kernel.org sched-design-CFS 全文、Open Group pthread_rwlock_rdlock 规范全文、go.dev/ref/mem 全文、go.dev/pkg/sync 全文)+ 2 WebFetch 404(rwlock 的 man7 .3/.3p 两路径均不存在,改走 Open Group 官方规范)+ 1 WebSearch(EEVDF 官方文档摘要检索,与 CFS 文档 §1"CFS is making room for EEVDF"互证)+ 0 agentic_search,硬约束内(WebFetch 10/10 用满、WebSearch 1/15、demo 5/5);Python ×5 **全部实跑断言通过**(6+2+3+4+5 = 20 项,生产者消费者与读者写者锁各连跑 3 次验证稳定性);C ×5+Go ×5 无本机工具链走人工代码审查+括号/引号配平脚本 10 文件全 OK;行数审计:源文件 98~222 全 ≤300、README 131~175 全 ≤200(本批无须拆分);开发期实跑修 bug 7 处:①WNOHANG 轮询循环把已收尸 pid 再轮询致 ECHILD(过滤已 reap);②消费者"检查总数再取"竞态空缓冲死锁(改哨兵,C 版同步重写);③读写锁统计计数增减放同一锁块致重叠永为 1(拆开+1ms 驻留);④C 递归读锁 demo 解锁计数少一次(3 加 2 解就断言写者可进);⑤Python 编排 w_got 事件从未 set 白等 0.2s 且断言恒真(换 w_in);⑥ucontext 状态机 swap 到主上下文未 return 致 KeyError;⑦CFS 迟到者断言符号写反——断言出错率高于实现连续第 N 次应验;大类轮换 ✓ top_pos=2 → 03-系统编程(sub_pos[03]=1 → 索引表第 13 项 进程线程协程,子目录 02-进程与线程 4 个+02-协程 1 个),上轮 02-Web开发;轮间保险通过(开局 shell date 实得 2026-09-17 06:01:35,距上轮 last_run 2026-09-17 05:09 = 52 分钟 > 45 阈值);槽位自检 ✓ 06:00 槽落地 06:01:35(+95 秒,含会话启动,未构成明显不符);占位锁 29d5866(06:01)→ demo 277-278 ea737da → 279 → 280 → 281+索引同步,6 次 commit 均 push OK | | notify: ok(HTTP 200 errcode=0,urllib 单次调用未重试) |
| 2026-09-17 08:23 | 17 | M=04-移动开发/02-Android(大类轮转第 4 位 → 04-移动开发,sub_pos[04]=1 → 索引表第 17 项 Android;上轮 03-系统编程)5 demo:OkHttp拦截器与连接池(interceptors.md + connections.md + ConnectionPool.kt 官方:应用拦截器不感知重定向/重试而 networkInterceptors 在 Connect 之后、内置七层序 RetryAndFollowUp→Bridge→Cache→Connect→CallServer、proceed() 可多次(重试)可不调(缓存短路)、Address→Route→Connection 三级模型与 Address.equals 含 TLS 配置、池 DEFAULT_MAX_IDLE=5 / keepAliveDuration=5 MINUTES、Bridge 自动解压后删 Content-Encoding 与 Content-Length、Happy Eyeballs 双栈回退)、协程上下文与Flow(CoroutineContext 是带键元素索引集且 + 右侧覆盖含 Job、Default 并行度=max(2,核数) / IO=64 / Unconfined 首挂起点前跑调用者线程、结构化并发子失败取消父与全部兄弟、CancellationException 不算失败、冷流每次 collect 重跑 vs 热流共享、flowOn 只换上游 + 上下文保存性)、WorkManager约束与重试(Constraints 全字段 AND 且默认 false、退避 30s 起步/下限 10s/上限 5h 且 clamp、doWork() 每 Worker 实例只一次 + 10 分钟执行窗口、Result.failure() 阻断下游 vs retry() 走退避、唯一任务 KEEP/REPLACE/APPEND/APPEND_OR_REPLACE)、Fragment生命周期与ViewModel(mState 9 态 INITIALIZING=-1…RESUMED=7 含 AWAITING_EXIT_EFFECTS=3 与 AWAITING_ENTER_EFFECTS=6 两个过渡拍点、视图生命周期与 Fragment 生命周期不同频(进返回栈只 onDestroyView 回来重跑 onCreateView)、getViewLifecycleOwner() 视图销毁后必抛 IllegalStateException、作用域由 ViewModelStoreOwner 决定、clear() 先 close mBagOfTags 再 onCleared 故 viewModelScope 先被取消)、Service三种形态(已启动或有 BIND_AUTO_CREATE 连接即存活、多次 startService 不嵌套但多次 onStartCommand、onStartCommand 四档 START_STICKY_COMPATIBILITY/STICKY/NOT_STICKY/REDELIVER_INTENT 且 STICKY 无 pending 时收 null intent、stopSelf(startId) 只在等于最近 start 时生效而乱序先调最近 ID 会立即停止、startForeground 不让服务变已启动 + setForeground 已 no-op + id 不得为 0 + type 须是 manifest 子集 + API 34 三类异常 + API 31 禁后台启动、进程优先级四档、FLAG_GRANT_*_URI_PERMISSION 授权保持到 stopSelf(int)) | S1=索引同步(04-移动开发/02-Android/README 子域表 +1 行网络编程、已完成表 +5 行 282-286、待研究勾 5 项留 6 项;04-移动开发/README 子域描述补全、已完成表 +5 行、待研究勾 OkHttp/Retrofit 并 +2 项)+**补齐 02-Android 四个长期缺失的子域 README**(并发编程/组件生命周期/网络编程/UI框架 —— 01-iOS 有而 02-Android 一直缺,按「每目录必含 README(索引 + 待研究清单)」补;内容为子领域表 + 共同主线对照 + 各 8 条待研究)+ S3=归档 append+rotate(completed 100+5→105→tail-100 = ID 182..286;schedule 30+1→31→tail-30)+ STATE §二/§三 滚动 | +5 | 286 | 权威来源:WebFetch 5 次(okhttp features/interceptors 与 features/connections 均 404、developer.android.com 三页 kotlin/coroutines + workmanager define-work + fragments/lifecycle 部分命中)+ WebSearch 1(OkHttp 拦截器链)+ 0 agentic_search,全在硬约束内(≤15/≤10/≤5);其余权威内容走 curl 到 github.com 上的 AOSP/AndroidX 镜像(Service.java 全文 57KB · Fragment.java 160KB · ConnectionPool.kt · Constraints.kt · Worker.kt · WorkRequest.kt · ViewModel.kt)+ kotlinlang.org 官方协程四篇 + 本地 .workbuddy/tmp/_r/extract.py 从 blob 页提 rawLines 精读(与 09-16 Python 轮同一路线);android.googlesource.com / cs.android.com / developer.android.com 本轮全不可达,已在各 README 参考资料里写明口径(WorkManager 退避公式标注为 demo 口径、FragmentStateManager 精确调用顺序标注为超出范围);Python 自检 ×6 **全部实跑通过**(okhttp 22 / coroutine 21 / flow 18 / workmanager 40 / fragment 33 / service 35 = **169 断言**);syntax_sanity.py 04-移动开发/** = 34 文件 0 语法结构问题 0 CRLF;行数审计:源文件 99~294 全 ≤300(3 个 check 文件初稿 414/367/352 超限,按 split_source.py + split_spec_20260917.json 拆出 okhttp_chain_model.py 260 / workmanager_model.py 178 / service_model.py 145)、README 159~198 全 ≤200(Service README 220→198 压缩);本机无 java/kotlinc/gcc/go 工具链(which 全空)→ Kotlin/Java 走人工审查 + syntax_sanity 括号配平;开发期修 bug 约 15 处,最值得记的三条:**① 断言崩溃常是真实 bug 的探测器而非障碍**——coroutine_runtime 的 _cancel 只递归取消子树不取消节点自身,父协程被 t=50 的存活兄弟"复活"成 COMPLETED,改成取消自身+整棵子树后才得到 CANCELLED@5;**② 同一概念在多处各写一份实现 = bug 温床**——service_model 的 dispatch() 一次抽干整个队列,导致"stopSelf(1) 因最新是 2 而 no-op"这条官方警告的场景永不触发,改成一次投递一条并新增 StopWithDeliveredId/StopWithLatestId 两个子类对照两种顺序;**③ 字段名与方法名撞车**——Chain 把请求存进 self.request 遮蔽了 request() 方法,报 TypeError: 'Request' object is not callable,改名 self._request + 加访问器;另:缓存短路场景最初断言 server_calls==0 却得 1(首个应用拦截器总会 proceed,须构造以 CacheInterceptor 为首的新链)、WorkManager 重试断言把 attempts 与 next_retry_at 提前了一轮、CacheInterceptor 没有 cache 字段(缓存在 Engine 上)、协程场景漏传 parent=job 致调度器继承与父子链全断;大类轮换 ✓ top_pos 3 → 04-移动开发,下一轮取循环第 4 位 → 05-AI与机器学习;轮间保险通过(开局 shell date 实得 2026-09-17 08:01,距上轮 last_run 2026-09-17 06:24 = 97 分钟 > 45 阈值);槽位自检 ✓ 08:00 槽落地 08:01:24(+84 秒,含会话启动,未构成明显不符);占位锁 4ed632c(08:03)→ demo 282-286 5812206(08:20)→ 索引同步 8906372(08:26)3 次 commit 均 push OK | notify: ok(HTTP 200 errcode=0 errmsg=ok,urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录;curl --data-binary @file 在本机 Git Bash 的路径映射 quirk 持续,沿用 09-15 起的 urllib 通道) |
| 2026-09-17 11:14 | 20 | M=05-AI与机器学习/02-强化学习(大类轮转第 4 位 → 05-AI与机器学习,sub_pos[05]=2 → 索引表第 20 项 强化学习;上轮 04-移动开发)5 demo:DQN经验回放与目标网络(回放打破时序相关降低更新方差/目标网络冻结自举/半梯度 target 依赖权重/终止态不自举;5 种子消融 full 成功率 1.000 同号率 0.525、no_target 0.825/0.563、no_replay 0.800/**0.732**,0.732→0.525 即官方"breaks these correlations…reduces the variance of the updates"的量化)、DoubleQLearning与最大化偏差(Jensen 不等式给出上偏,均匀误差闭式 E[max]=σ(m−1)/(m+1)、正态按 √(2 ln m);Lemma 1 `E[μ^B_{a*}] ≤ max E[X]` **等号⟺P(a*∉M)=0** 实测逐行一致(σ=0.2 时 0.000、σ=2.0 时 0.616);Theorem 1 `max Q ≥ V*+√(C/(m−1))` 代数分解+紧性构造 4000 点残差 2.84e-14;网格世界 200 运行×10000 环境步:Q-learning 起点 max Q 抬到 +7.74/+14.45(真值 0.3627 的 20~40 倍)而 Double Q 策略回报 +0.188≈最优 +0.2)、资格迹TDLambda(n 步回报两端 TD(0)/MC、λ-回报(7.3) 几何权重和为 1、前向视图非因果、迹(7.5)+TD 误差(7.6)增量更新(7.7)、离线前后向等价而在线近似、累积迹 vs 替换迹;C1 n 步 RMS n=1 0.1258/n=4 **0.0386**/n=∞ 0.2791、E2 λ=0 与 λ=1 精确相等 0.00e+00 中间差 2.11e-15、E3 最优 λ=0.8 RMS 0.0315、E4 替换迹 0.0393<累积迹 0.0602 且累积迹峰值可达 5.08 而替换迹恒 ≤1)、ActorCritic与GAE(reward-to-go + EGLP 引理保证减基线无偏、k 步优势两端、**GAE=(1−λ)Σλ^{k−1}Â^(k)=Σ(γλ)^lδ_{t+l}** 且等价于后向递推 O(1) 内存;高斯消元解 Bellman 得**精确** Vπ/Aπ 当靶子:E1 两实现差 3.47e-17、E2 真实 Vπ 基线偏差/标准误 ≤0.90 而 MSE 0.01850→0.09317(λ=1 是 5.0 倍)、E3 欠拟合 V̂ 基线 8.23→3.86(放大 4.3 倍)且**小 λ 偏差反而更小**(常值偏移被 δ 抵消)、E4 稀疏回报 3.260→2.824 而稠密噪声 td 1.979e+01 « full 1.210e+02(**"减基线一定降方差"是错的**))、SAC最大熵与温度自调节(软 Bellman backup V=α·logΣexp(Q/α) 即软最大、由 V=E_a[Q−αlogπ] 求导置零得 Boltzmann 策略 π=exp((Q−V)/α)、熵写成**约束**再取对偶 ⇒ 对偶变量即温度可梯度下降;负反馈 α←max(1e-6, α−lr(H−H̄)) 三个相差两个数量级起点(0.05/1.0/4.0)**全收敛到 α*=0.39009** 且 abs(H−H̄)=0.00000;α→0 误差**线性**趋零 gap 比值恰 10.000 而**熵非单调**(α≈0.7 见顶 1.3617 后回落饱和 1.3306 < ln4);E4 朴素 log-sum-exp 抛 OverflowError 稳定版 1001.3863=1000+log4) | S3=索引同步(05-AI与机器学习/02-强化学习/README 子领域 +5 行、已完成 +5 行 287-291、待研究勾 DQN/Actor-Critic/SAC 并新增 TRPO/离线RL/多智能体/基于模型/分层/探索 6 项;05-AI与机器学习/README 已完成行改 8 demo 并勾掉 DQN 与 Actor-Critic)+ 归档 rotate(completed 100+5→105→tail-100 = ID **194..291**;schedule 26+1→27 ≤30 不 rotate)+ STATE §二/§三 滚动;S1=合规体检与历史欠账修复(逐目录扫行数时发现 PPO(038)源文件 368 行超限 → 拆 ppo_core.py 254 + ppo.py 131,DoubleQ main.py 303→295;**实跑发现 PPO 有两个光读代码看不出的缺陷**:①demo4 把 ppo_train 的 4 元组按 3 个解包 → `ValueError: too many values to unpack` 直接崩溃;②demo3 用均匀初始策略采批,而该网格均匀策略下 200 次采样仅 2 次到目标(其余落洞奖励 0)→ 整批回报全 0 → 优势全 0 → `max abs(Δtheta) == 0.0`,ratio 精确停在 1.000000 空转。修:demo4 改 4 元组解包、demo3 先 `ppo_train(iterations=20)` 离开均匀分布再采批(修后 ratio 区间 0.9719~1.0093);PPO README 补「比率恒 1.0 + clip 0 + Δtheta 0 三者同时出现 = 信号缺失,不是收敛」判据与稀疏奖励硬边界实测) | +5 | 291 | 权威来源:WebFetch 5 命中(arxiv abs 1506.02438/1312.5602/1509.06461/1812.05905/1801.01290)+ ar5iv 渲染版 4 篇全文精读(1506.02438/1312.5602/1509.06461/1812.05905)+ Spinning Up 3 篇(policy optimization、SAC、TD3/DDPG)+ S&B 1st ed. 在线版 8 节点(node66/73/74/75/76/80/90 等)+ NeurIPS 2010 论文集 PDF(van Hasselt, Double Q-learning)+ PyTorch DQN tutorial;0 agentic_search,硬约束内(WebFetch 10/10 用满、WebSearch 未超、demo 5/5);**本轮全部 Python-only**(单语言,未配第二语言:强化学习 demo 的对照价值在「配置消融」而非「语言对照」,已按此口径交付);Python ×5 **全部实跑断言通过**(12+21+10+11+14 = **68 断言**,FAIL=0,重跑复核仍全绿);行数审计(OPTIMIZATION §1.1):源文件拆分后 **全部 ≤300**(dqn_core 272/main 270、bias_theory 159/double_q_core 214/main 300、td_lambda_core 209/main 149、mdp 148/main 296、sac_core 220/main 261)、README 8 篇 **全部 ≤200**(dq 199/sac 200/tdλ 200/ac 200/dqn 200/ppo 189/q 141/reinforce 162),超限件按「拆多页 + 索引」新建 5 篇 NOTES.md(69~130 行)承载完整表格/推导/踩坑;开发期修 bug 与踩坑:①**拆文件脚本写坏未提交文件体**(尾裁剪算式算错,td_lambda_core.py 与 sac_core.py 被写成只剩 3/8 行头,且当时 git 无记录)—— 靠 main.py 调用点与断言、实测数字记录重建并逐项复现原输出;sac_core 重建后不可用的 import 需同步 main.py;②拆文件残留 **游离 """** 致 SyntaxError U+2212;③DQN 调用约定错(max 取了隐藏层激活、sgd_step_onehot 少传 pre)与 `_hidden_grad` 忽略调用方 lr;④**终止转移不自举断言失败是真 bug** —— 终点格未实现为吸收态,改环境而非改断言;⑤DoubleQ 把"10000 learning steps"按**回合**实现 → 实算约 200 万环境步、7 分半没跑完,改环境步后 7 秒(写实验前先估量级:回合数×每回合步数×运行数);⑥数值积分求 E[max] 漏负半轴项(1/√π=0.5642 被算成 0.6810)—— 本批唯一一处"断言失败其实是断言自己的 bug";⑦Baird 反例谱半径在两种布局/两种采样下恒为 1.0000 做不出 α 依赖发散 → **不交付做不出的论断**,改为论文自身可复现实验;Baird 仅列入参考资料;⑧Lemma 1 断言从"存在选错样本"改为 `strict == missed_pos` 逐行一致(σ=0.2 时恰好是充要条件的另一支);⑨SAC 熵非单调 → "熵随 α 单调递增"断言本身错,改分层断言 (a)~(d);⑩温度更新符号写反是正反馈发散且不报错,只能靠多起点是否收敛同一点判断;⑪奖励量级与熵预算量纲问题(goal_reward 1.0→20 才稳定),非超参问题;⑫`naive_logsumexp` OverflowError 需 try/except 记录为 overflowed;⑬C/Go 本轮无 demo(全 Python),cwd/编辑器无工具链问题;大类轮换 ✓ top_pos=4 → 05-AI与机器学习(sub_pos[05]=2 → 第 20 项 强化学习),上轮 04-移动开发,下一轮取循环第 5 位 → 06-DevOps;轮间保险通过(开局 shell date 实得 2026-09-17 **10:02:27**,距上轮 last_run 08:23 = 99 分钟 > 45 阈值);槽位自检 ✓ 10:00 槽落地 10:02:27(+147 秒含会话启动,未构成明显不符);占位锁 7f2a96d(10:03)→ 主轮 ec2d924(demo 287-291 + 索引 + STATE)→ 2e4cb60(归档 + 日志)→ 81c98a6(PPO 修复),4 次 commit 均 push OK;全目录合规终检:源文件 0 个 >300、README 0 个 >200 | notify: ok(HTTP 200 errcode=0 errmsg=ok,python urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录;沿用 09-15 起的 urllib 通道,Git Bash 下 curl 的路径映射 quirk 持续) |
| 2026-09-17 12:34 | 24 | M=06-DevOps/02-CI-CD 第二批 5 demo(大类轮转第 5 位 → 06-DevOps,sub_pos[06]=1 → 索引表第 24 项 CI-CD;上轮 05-AI与机器学习):GitHub Actions 工作流语义(过滤器模式即正则:`*` 不跨 `/`、`**` 跨、`?`/`+` 是量词;`on` 顺序即语义「最后命中者胜出」;matrix include 两阶段并入;表达式宽松相等类型转换表 + NaN 关系恒假 + `&&`/`||` 返回操作数本身)、GitLab CI rules 与父子流水线(rules 首次匹配即生效且属性携带、rules 内 `when: manual` 默认 allow_failure=false、rules:changes 非推送类恒真、正则内变量不展开、include 150 上限 + 30 秒、父子流水线最多两层)、Jenkins 声明式 Pipeline(post 十条件固定顺序 cleanup 恒最后、顶层 agent timeout 不计分配时间而 stage 级计入、beforeOptions > beforeInput > beforeAgent、stage 四选一 + parallel/matrix 不可嵌套、matrix 两段式)、Argo CD 同步与漂移检测(automated.enabled 三态 null 等同开启、只在 OutOfSync 同步、(commit SHA1, 参数) 组合只尝试一次、selfHeal 5 秒、调和周期 120+60=180s、prune 默认关 + allowEmpty、core 组 group 是空串、jq 数组须显式 `[]`、managedFields 所有权、Quantity 规整)、制品晋升与语义化版本(SemVer 2.0.0 优先级全序 / 前导零禁令 / 预发布 < 正规 / 构建元数据不参与比较 / 数字标识符恒低于非数字;OCI digest 语法与 sha256 小写十六进制、内容寻址 ID(C)==D=='<alg>:'+Encode(H(C))、tag 可变 digest 不可变、tags ASCIIbetical + n/last 分页、referrers API 与回退 tag 截断 32/64) | S1=索引同步(02-CI-CD/README 已完成表 +5 行 292-296、待研究勾 5 项并新增 2 项(Argo Rollouts / 制品签名);06-DevOps/README 索引 +1 行并勾掉 GitHub Actions)+ S3=归档 rotate(completed 100+5→105→tail-100 = ID **197..296**;schedule 27+1→28 ≤30 不 rotate)+ STATE §二/§三 滚动(top_pos=6 / sub_pos[06]=2 / last_top=06-DevOps,总 4998 B 守 ≤5 KB) | +5 | 296 | 权威来源 10 WebFetch 命中(GitHub workflow-syntax / expressions、GitLab ci-yaml / job_rules / downstream_pipelines、Jenkins pipeline-syntax、Argo CD auto_sync / diffing、semver.org、OCI distribution-spec)+ 3 次镜像 curl 实读规范原文(SemVer / OCI 走 jsDelivr,github.com 直连被 reset)+ 0 WebSearch + 0 agentic_search,全在硬约束内(≤10/≤15/≤5/≤5);Python ×16 文件 **实跑断言全绿 431 项**(110+67+69+77+108);Go ×22 文件无本机工具链(which go exit 127)走人工审查 + check() 实参核对脚本 + syntax_sanity.py 0 问题;行数审计 11 次拆分全部经 split_source.py,终态源文件全 ≤300、README 全 ≤200;开发期实跑修 bug 4 处(Argo CD:is_out_of_sync 的 kind 缺省写死 Deployment 致忽略规则对 ConfigMap 不生效;链式 jq 删除只把通配当末尾处理致 `.webhooks[]?.clientConfig.caBundle` 整条落空 → 改为返回通配下标并对每个元素分发后半段;制品晋升:put_manifest 用 `reference.startswith("sha")` 判 digest 把回退 tag 误当 digest → 改按是否含冒号区分;重复晋升幂等断言顺序错 + 降级闸门改为策略入参 current_version);大类轮换 ✓ 上轮 05-AI与机器学习;轮间保险通过(开局 shell date 实得 2026-09-17 12:02:45,距上轮 last_run 2026-09-17 11:14 = 48 分钟 > 45 阈值);槽位自检 ✓ 12:00 槽 +165 秒落地(含会话启动),无 slot_mismatch;8 次 commit(c5b02b0 占位锁 + 453f0f0 / 0633bee / 4886af9 / a65f2f4 / d9bcf2a 主轮)push OK | notify: ok(HTTP 200 errcode=0 errmsg=ok,python urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录;沿用 09-15 起的 urllib 通道,Git Bash 下 curl 的路径映射 quirk 持续) |
| 2026-09-17 14:36 | 28 | M=07-数据存储/02-NoSQL 第二批 5 demo(大类轮转第 6 位 → 07-数据存储,sub_pos[07]=1 → 索引表第 28 项 NoSQL;上轮 06-DevOps):MongoDB 聚合管道优化器与 ESR 索引(**先重排后合并**是官方强制次序;R1 投影下推靠依赖分析、**`$unset` 也是投影阶段但不能按 `$project` 处理**;coalescence 五条 R5~R9 —— R5 夹 `$skip` 时 `limit += skip` 且中间不得有改变文档数的阶段、R6 双 `$limit` 取较小值、R7 双 `$skip` 取之和、R8 合成 `$and`、R9 `$unwind` 折进 `$lookup`;ESR 机制 = **索引键序决定能用的界**,ERS 仅范围谓词高选择度时才优;`$in` 201 元素分界;1000 阶段 / 100MB 阻塞 / 16MiB;`$out`/`$merge` 只能末位、`$geoNear` 只能首位)、DynamoDB 单表设计与容量(4KB/1KB 台阶 × 一致性倍数(1/0.5/2),**取整口径因 API 而异**:`BatchGetItem` 逐条取整(1.5KB+6.5KB→12KB)vs `Query` 取返回集合总量(40.8→44KB);单分区 3000 RCU/1000 WCU **物理硬顶**,20KB 强一致读 ⇒ 600 次/秒;写分片 `N=ceil(target_WCU/1000)`,读扇出 N 倍;LSI/GSI 六维对照(L0 建表时 vs 可后加、吞吐共用 vs 独立、GSI 不能回表、LSI 项目集合 10GB);**GSI 写容量不足反噬主表写**;官方 1000 与 1024 不自洽,本 demo 统一取 1024 并标注)、Cassandra 查询驱动建模与墓碑("data modeling is query-driven";主键首分量哈希成分区键、**顶层逗号 vs 括号内逗号**必须区分;分区 10 万值/100MB 两条线且"恰好达线不算超";**删除即插入** —— tombstone 清除三条**同时**满足(过 `gc_grace_seconds` 默认 864000 秒=十天 / 压缩覆盖所有含更旧数据的 SSTable / 已修复),`purgeable()` 区分"没到时间"与"压缩不够";三节点僵尸复活最小复现;STCS 4 个 vs LCS 每层 10 倍 + L0 32 个兜底 vs TWCS 怕乱序)、KV 存储引擎 LSM(WAL **32KB 分块 + 7 字节头**(CRC32C+长度+类型),四型切片,块尾剩余 ≤6 字节补零,**恰好剩 7 字节写零长度 FIRST**;footer 48 字节 = 2×BlockHandle(20) + magic `0xdb4775248b80fb57`;filter base 2KB;`10^L` MB 静态层级 vs **RocksDB 动态层级**(末层锚定实际大小,实测 {6:276GB…2:0,1:0},90% 落末层)并标注 num_levels 含 L0 的**口径差异**;三类写停顿 + 删除标记可丢条件)、Protobuf 编码(base-128 varint 小端 7 位、最多 10 字节;wire type 六种 + tag `(field<<3)|wire`;字段号 1..536870911 且 **19000..19999 保留**;官方最小示例 `08 96 01` / 嵌套 `0a 03 08 96 01`;**intN 补码恒 10 字节 vs sintN ZigZag 两字节**(-500→999);packed **双向兼容**;未知字段跳过含 group 配对;字段顺序不确定 + Last One Wins 与嵌入消息合并,官方混合示例 `5:{1 2} 4:{"hello"} 5:{3}` → nums=[1,2,3]) | S1=索引同步(07-数据存储/02-NoSQL/README 已完成表 +5 行、待研究刷新为 ScyllaDB / Adaptive Capacity / MongoDB 分片 / Cassandra SAI / HBase / 时序库 六项;07-数据存储/README 已完成 +5 行)+ S3=归档 rotate(completed 100+5→105→tail-100 = ID **197..301**;schedule 28+1→29 ≤30 不 rotate;追加前后均断言"每行以 `|` 开头、无空行"两表均 0 异常)+ STATE §二/§三 滚动(top_pos: 6→7 / sub_pos[07]: 1→2 / last_top=07-数据存储) | +5 | 301 | 权威来源 8 WebFetch(MongoDB aggregation-pipeline / aggregation-pipeline-optimization / ESR-guideline、DynamoDB bp-partition-key-design / GSI / LSI、LevelDB impl.md、RocksDB Leveled-Compaction)+ 1 WebSearch(DynamoDB 条目上限)+ 0 agentic_search,全在硬约束内(≤10/≤15/≤5/≤5);另经 `curl` 直抓镜像精读 LevelDB `doc/log_format.md` / `doc/table_format.md` / `doc/impl.md`、RocksDB wiki `Leveled-Compaction.md` / `RocksDB-Overview.md` / `Write-Stalls.md`、protobuf.dev `programming-guides/{encoding,proto3}`、`github.com/apache/cassandra` 五个 adoc 源文件(官网 cassandra.apache.org 本机不可达,已在 README 标注口径);Python ×20 文件实跑断言全绿 **351 项**(67+75+68+64+77,FAIL=0);Go ×25 文件无本机工具链(which go exit 127)走人工审查 + `syntax_sanity.py` + `bracket_check.py` 双自检 0 问题;行数审计:6 次拆分全部经 `split_source.py`,终态源文件全 ≤300、README 全 ≤200(LSM README 212→199 三轮压缩、Protobuf README 228→190 重写);开发期实跑修 bug 8 处(①上一轮拆分遗留的 Go 括号失衡:`cassandra_model.go` 的 `check`/`eqStrs` 被切走一半、`cassandra_model_check.go` 多出续尾 —— 本题开工前的 S0 修复;②`lsm_engine.py` 尾部残留 `List[Entry]` 注解但 import 已删 → `NameError`;③分数断言塞假 `None` → 引入 `FakeTable`;④动态层级夹具与官方 `num_levels` 口径不符 → 改 7 并在代码与 README 双处标注;⑤Go `Drop` 用指针 `!=` 过滤重复元素会漏项 → 改 `map[*SSTable]bool`;⑥`uint64(-2)` 是**编译错**("constant -2 overflows uint64")→ 先 `int64(-2)` 再转;⑦`return DecodeMessage(...)` 多返回值类型不匹配 → 展开两条语句,随后全仓扫描 `return Foo(` 无同类;⑧拆分后 doc 注释游离 + `pb_check.go` 未用 `strings` import);大类轮换 ✓ 上轮 06-DevOps;轮间保险通过(开局 shell date 实得 2026-09-17 **14:00:55**,距上轮 last_run 2026-09-17 12:34 = **87 分钟 > 45 阈值**);槽位自检 ✓ 14:00 槽落地 +55 秒(含会话启动),无 slot_mismatch;7 次 commit(741126c 占位锁 + 0e1b4f9 / fd2807f / c9102eb / e2a6eed 主轮 + 8107b97 S1 索引 + 本行所在收尾)均 push OK;**注意:本轮早前 5 个 commit 的消息时间串(14:35~15:58)系估算而非 shell `date` 实测**(本行 14:36 才是收尾实测),后续轮次以 schedule.md 行为准 | notify: ok(HTTP 200 errcode=0 errmsg=ok,python urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录;沿用 09-15 起的 urllib 通道,Git Bash 下 curl 的路径映射 quirk 持续) |
| 2026-09-17 23:03 | 32 | M=08-安全/01-Web安全 第二批 5 demo(大类轮转第 7 位 → 08-安全,sub_pos[08]=1 → 索引表第 32 项 Web安全;上轮 07-数据存储):OAuth2 授权码 + PKCE(RFC 6749 §4.1 步骤 A–E / RFC 7636 §4.2·§4.6·附录 B 向量 / RFC 9700 §2.1·§2.1.1·§4.2.4·§4.4.2.1:授权请求只出 challenge、verifier 43 字符 43..128、redirect_uri 精确匹配且失败**不重定向**、码单次 + 600 秒闭区间、重放撤销已发令牌、PKCE 降级检测(有 verifier 无 challenge → invalid_grant)、state 一次性 + RFC 9207 `iss` 防 mix-up)、Cookie 存储模型 + SameSite + HSTS(RFC 6265 §5.1.3/§5.1.4/§5.3/§5.4 + 6265bis-20 §4.1.3/§5.4/§5.5/§5.6.7.2/§5.7 + RFC 6797 §6.1/§8:点边界域匹配、path-match 不跨词边界、default-path 到最右斜杠之前(**与 MDN 带尾斜杠的表述存在口径差异,README 已标注**)、Max-Age > Expires + 400 天截断 + 4096 字节 + 每域 50/总 3000、UA 侧前缀大小写不敏感、__Host- 需 Secure + host-only + 显式 Path=/、SameSite=None 需 Secure、**非安全 cookie 不得覆盖同名 Secure cookie(路径比较不对称)**、Lax-allowing-unsafe 2 分钟例外、HSTS 非安全传输忽略/多值只取第一个/max-age=0 清策略且连带清 includeSubDomains/父子域匹配需 includeSubDomains/IP 字面量不标记/§8.3 端口映射、**preload 不是 RFC 6797 指令**)、SSRF + WHATWG IPv4 解析(OWASP SSRF Cheat Sheet + WHATWG URL §3.5·§4.4:三进制 number parser(validation error ≠ failure)、最后一段 < 256^(5-段数) 故 11 种写法等价于 127.0.0.1、ends-in-a-number 决定整个 URL 是否非法、`example.com\@evil.com` 解析器分歧即拒绝、IPv4-mapped/NAT64/6to4 解包、允许列表重建请求、DNS pinning 全记录校验、禁用重定向跟随、file/gopher 拒绝)、访问控制与 IDOR(OWASP Authorization Cheat Sheet:deny-overrides + 默认拒绝、每请求服务端校验、对象级授权 CWE-639、RBAC 会放行横向越权而 ABAC/ReBAC 叠加才全对、宽范围访问须显式授予 auditor、会话级间接引用、失败安全退出 + 审计)、点击劫持与框架嵌入(OWASP Clickjacking Cheat Sheet + WHATWG HTML §7.7 + MDN:**逐行断言官方结果表** SAMEORIGIN×2 允许 / 含合法值多值拒绝 / INVALID×2 允许、孤立法与 ALLOW-FROM 放行、顶层不受约束、SAMEORIGIN 查整条祖先链、**CSP frame-ancestors enforce 则无条件忽略 XFO**(Report-Only 不算)、`<meta>` 里两者皆无效、frame-buster 三种绕过 + fail-closed 写法) | S1=索引同步(01-Web安全/README 完成表拆两批 +5 行 302-306、待研究勾 4 项并新增 6 项;08-安全/README 完成表 +5 行)+ S3=归档 append+rotate(completed 100+5→105→tail-100 = ID **207..306**;schedule 29+1→30 ≤30 不 rotate;追加前后均断言『每行以 \| 开头、无空行』两表各 0 异常)+ STATE §二/§三 滚动 | +5 | 306 | 权威来源 9 WebFetch(RFC 6749/7636/6797/9700、OWASP SSRF/Authorization/Clickjacking 三张 cheat sheet、MDN Set-Cookie/X-Frame-Options)+ 5 次 curl 实读规范原文(RFC 6265 · RFC 6265bis-20 · WHATWG URL Standard(IPv4 number parser / IPv4 parser / ends-in-a-number / host parser 逐段提取)· WHATWG HTML §7.7 处理模型与官方结果表)+ 0 WebSearch + 0 agentic_search,全在硬约束内(≤10/≤15/≤5/≤5);**Python ×14 文件实跑断言全绿 273 项**(32+80+74+41+46),**JavaScript ×5 文件实跑断言全绿 105 项**(16+18+29+21 + 0),其中 node 原生 WHATWG URL 被用作 Python 复刻解析器的标尺(11 种等价写法逐条对照);Go ×1 无本机工具链(which go exit 127)走人工审查 + `bracket_check.py`(BALANCED)+ `syntax_sanity.py`(16 文件 0 结构与 import 问题);行数审计:源文件 91~259 全 ≤300、README 108~133 全 ≤200(本批无须拆分);开发期实跑修 bug/断言错 12 处(①OAuth 自检 `flow()` 已消费 state,"state 复用"断言重复调用 callback;②Cookie 省略 Domain 未置 host_only=True(实跑抓到的真 bug);③"非安全覆盖"用例路径写成默认 `/` 而规范只拦 path-match 旧路径;④"多个 STS 头只取第一个"须同一响应传两个值;⑤IPv6 规范串 CPython 实际输出 `::ffff:127.0.0.1` 而非 `::ffff:7f00:1`;⑥JS isPublic 只匹配 v4 正则漏掉 mapped 地址;⑦访问控制 admin 断言与对象级校验冲突 → 改显式 auditor 授予;⑧`Decision` fallback 未设 rule;⑨XFO 断言表把 ALLOWALL,INVALID 写成允许(allowall 属困惑集合);⑩`meta_delivered` 未丢弃 CSP 头;⑪断言 label 内 ASCII 双引号致 SyntaxError;⑫场景表第 3/5 行文字不一致致计数断言失败)——**断言出错率高于实现第 N 次应验**;大类轮换 ✓ 上轮 07-数据存储,下一轮取循环第 8 位 → 09-语言学习(sub_pos[09]=1 → Golang);轮间保险通过(开局 shell date 实得 2026-09-17 **22:47:52**,距上轮 last_run 2026-09-17 14:36 = **491 分钟 > 45 阈值**);**槽位自检 ⚠ 本槽应为 20:00(±2 分钟),实得 22:47,偏差约 167 分钟** —— 与前一日同因:宿主应用在 14:40~22:47 未运行导致 16:00/18:00/20:00 三槽当天未触发,本槽为**补跑**;按 09-16 22:53 轮先例按「补跑一轮完整巡检」处理并如实记录偏差(未记 slot_mismatch 中止,那会零产出),16:00/18:00 两槽当天不补;占位锁 717e889(22:48)→ demo302-303 9e4e281 → demo304 8ee8cf1 → demo305 9663c6f → demo306 0d25fd2 → S1 63df25d 均 push OK;**注意:本行之前 4 个 commit 的消息时间串(23:05/23:22/23:37/00:02)系估算而非 shell `date` 实测(真实区间 22:48~23:03),后续轮次以 schedule.md 行为准,历史提交不重写(禁 force push)** | notify: ok(curl errcode=0/http200,23:05 发送,仅一次) |
| 2026-09-18 00:29 | 44 | M=09-语言学习/Golang 第三批 5 demo(大类轮转第 8 位 → 09-语言学习,sub_pos[09]=1 → 索引表第 44 项 Golang;上轮 08-安全):内存分配器(68 级 size class 全表脚本注入复核 + objects/tail waste/max waste 逐项验算 + class 5 的 170 对象 span 布局 + tiny allocator 复用条件 + mcache/mcentral/mheap 三级补货)、逃逸分析与内联(位置图 derefs 权重 −1/0/+1/+2 + 两条不变量 + parameter tag + 内联预算 80/57/17/1/80/5000+20/800/2000 + 结构闸门 + `-l` 四档)、内存模型与 sync 原语(happens-before 传递闭包 + Requirement 1~3 + 12 条原语保证 + 数据竞争与 DRF-SC + Mutex 状态位/正常模式 barge 与 queueLifo/饥饿模式 1ms 阈值与移交退出)、反射三定律(interface 对 + Kind vs Type + 最大类型 getter 与补码截断 + CanSet 一行判定与 flag 位 + 未导出字段"可取址不可设置" + Field 掩码清 EmbedRO/Method 故嵌入结构体的导出成员可写 + panic 消息与判定顺序)、unsafe 与内存布局(规范尺寸/对齐保证 + 尾部零尺寸字段占 1 字节 + pointer bytes 官方三例 16/24/8 + 排序五条优先级 + size class 上取整与诊断后缀 + false sharing + unsafe.Pointer 六种合法模式与全部 INVALID 反例) | S1=索引同步(09-语言学习/Golang/README 待研究 +5 项 307-311 与参考资料 +8 条、09-语言学习/README 语言表 demo 数 Golang 10→15 / Python 1→16 并加第三批说明)+ S3=归档 rotate(completed 100+5→105→tail-100 = ID **212..311**;schedule 30+1→31→tail-30;追加前后均断言『每行以 `\|` 开头、无空行』两表各 0 异常)+ STATE §二/§三 滚动(top_pos: 8→9 / sub_pos[09]: 1→2 / last_top=09-语言学习) | +5 | 311 | 权威来源 7 WebFetch(go.dev/ref/mem、go.dev/blog/laws-of-reflection、pkg.go.dev/unsafe、go.dev/wiki/CompilerOptimizations、pkg.go.dev/reflect、pkg.go.dev/golang.org/x/tools fieldalignment、go.dev/ref/spec)+ 13 次 curl 直抓 go1.24.0 源码(runtime/sizeclasses.go·msize.go·malloc.go·mcache.go·mcentral.go、sync/mutex.go、internal/sync/mutex.go、cmd/compile/internal/escape/escape.go、cmd/compile/internal/inline/inl.go、reflect/value.go·type.go、unsafe 包文档)+ 0 WebSearch + 0 agentic_search,全在硬约束内(≤10/≤15/≤5/≤5);**Python ×13 文件实跑断言全绿 372 项**(61+55+54+103+99),Go ×17 文件无本机工具链(which go exit 127)走人工审查 + `bracket_check.py`(全部 BALANCED)+ `syntax_sanity.py`(0 结构/import 问题);行数审计:README 三次压缩(内存模型 224→194、unsafe 210→200)、源文件 7 次拆分(反射 demo 的 value_model.py 382→kinds/containers/value_model 三件、Go 侧 model.go 377→containers/value_model;unsafe demo 的 Go layout.go 275 单件内分节)后全 ≤300;开发期实跑修 bug/断言错 15 处(①反射 demo:`CLASS_TO_ALLOCNPAGES`/`SIZE_TO_CLASS128` 类的手抄表从未参与本 demo,真正踩到的是 `optimal_order` 把 `(name, Typ)` 元组当类型;②`GetScalar` 对 float 误走 `int()`;③`mustBeAssignable` 判定顺序理解错(先报 unaddressable 而非 Kind 不符)——断言错;④`cannot return value obtained from unexported` 前缀写短;⑤unsafe demo:`optimalOrder` 的零尺寸优先级在 Python 升序排序里要写成 `0 if sz == 0 else 1`(写反导致 D16 失败,是模型 bug);⑥`Padded` 的最优侧 48 本身就是 size class 故不应带后缀——断言错;⑦cache line 填充 8+56+8=72 而非 128——断言错;⑧`&^` 只能减小偏移故越界判定必发生在 `add_offset` 阶段,原"取整后越界"用例构造无效;⑨`Mix` 未排序时确实可重排到 16 字节,原断言"无诊断"错——断言错;**断言出错率高于实现第 N+1 次应验**);大类轮换 ✓ 上轮 08-安全;轮间保险通过(开局 shell date 实得 2026-09-18 **00:01:02**,距上轮 last_run 2026-09-17 23:03 = **58 分钟 > 45 阈值**;对照 09-17 00:00 槽因距上轮仅 39.68 分钟被拦截零产出);槽位自检 ✓ 00:00 槽落地 +62 秒(含会话启动),无 slot_mismatch;commit 序列:91b692e 占位锁 → d49638f(demo1+2)→ b3ccef7(demo3)→ 71d9b0e(README 压缩)→ 反射 demo → unsafe demo → 本行所在收尾,均 push OK;**注意:dfc0e42(标 01:15)与 60a7204(标 00:38)两条 commit 的消息时间串系估算,超出真实收尾时刻(实测 00:29~00:32),后续轮次以 schedule.md 行为准,历史提交不重写(禁 force push)** | notify: ok(HTTP 200 errcode=0 errmsg=ok,python urllib 单次调用未重试,发送前已确认本轮无既有 notify 记录) |
