| 635 | `07-数据存储/03-缓存/CDN一致性哈希/` | CDN 一致性哈希：libketama 与 nginx chash 对照（RJ/ketama `libketama/ketama.c` 加 nginx `src/core/ngx_crc32.c` 加 `ngx_crc32.h` 加 `src/http/modules/ngx_http_upstream_hash_module.c` 原文实读）：libketama 用 md5、一次摘要切 4 个 uint32、每节点 `floorf(pct*40*n)*4` 个点、查找判据是「落在 (前一点, 本点]」、越界回滚到第 0 个点；nginx 用表驱动 crc32（init 0xffffffff、final 异或，与 zlib 一致）、每节点 `weight*160` 个点、`crc32(HOST 加 NUL 加 PORT 加 PREV_HASH)` 链式、排序后相邻去重、查找是「第一个 >= hash」、越界返回 len 由调用方回绕、`tries > 20` 回退 round-robin。**两个易错点**：①`ngx_crc32_update(&base_hash, (u_char *) "", 1)` 的 `""` 是 1 字节 NUL、长度也是 1，它真的喂了一个字节而不是空操作 ②`pct` 是 C 的 `float` 且 `floorf` 收成单精度，等权 **7** 台时单精度路径得 ks=40 而全程 double 得 39（少 4 个点），本 demo 用 float32 显式降级复刻。实测口径：10000 个键下 3 到 4 台重迁移 ketama 24.6% 与 chash 27.8%（理想 25.0%） | Python(ketama 147 加 ngx_chash 168 加 main 64 加 两份自检 111/123，95 断言实跑全绿) / Go(ketama 131 加 chash 144 加 main 65，四项静态检查全 OK) | 2026-09-23 |
| 636 | `08-安全/01-密码学/MLDSA后量子签名/` | ML-DSA 后量子签名（FIPS 204 实读）：模数为什么是 `Q = 8380417`、签名里除了 `z` 与 `c` 为什么还要一段 **hint 提示位**，以及取整与拒绝采样 | Python + Go | 2026-09-23 |
| 637 | `08-安全/01-密码学/FROST门限签名/` | FROST 两轮门限签名（RFC 9591）：Shamir 分片与拉格朗日插值、**绑定因子**为何必要、两轮协议与可识别中止 | Python + Go | 2026-09-23 |
| 638 | `08-安全/01-密码学/HPKE混合公钥加密/` | HPKE（RFC 9180）：`DHKEM(X25519, HKDF-SHA256)` + `HKDF-SHA256` + `AES-128-GCM`，一个 KEM、**两个 `suite_id`**、由序号 XOR 出来的 nonce，逐条对齐附录 A.1 官方向量 | Python + Go | 2026-09-23 |
| 639 | `08-安全/01-密码学/AES密钥包装/` | AES 密钥包装：RFC 3394（AES-KW）的 6n 步 Feistel 与默认 IV `A6A6A6A6A6A6A6A6`；RFC 5649（AES-KWP）把初值换成 `A65959A6` 加 MLI 并补三条完整性检查 | Python + Go | 2026-09-23 |
| 640 | `08-安全/01-密码学/SPAKE2口令认证密钥交换/` | SPAKE2 PAKE（RFC 9382）：`pA = w*M + X` 与 `K = h*x*(pB - w*N)`、TT 里 **`len()` 是 8 字节小端**、`w` 定长大端编码防计时、`Ke ‖ Ka = Hash(TT)` 与 `KcA ‖ KcB` 的切分在两侧相反、M 与 N 的离散对数必须未知、不支持 augmentation 故服务端存口令等价物；附录 B 四组向量全字段比对 | Python（665 断言实跑全绿，含 P-256 群律与离线字典攻击演示）/ Go | 2026-09-23 |
| 641 | `08-安全/01-密码学/Shamir秘密共享与阈值恢复/` | Shamir 秘密共享三套对照：素数域 GF(2^31-1)（少于 t 份时**任意目标秘密都能伪造出第 t 份份额**，即信息论零泄漏）、Vault 的 GF(2^8) 版（Rijndael 多项式、`a^254` 求逆链、每字节一条多项式、份额末尾挂 x 且 x 属于 [1,255]）、SLIP-0039（秘密在 x=255、摘要在 x=254、`D = HMAC(R,S)[:4]` 加 R、RS1024 校验和）；AES 域常量 `0x57·0x83 = 0xC1` 钉住实现 | Python（389 断言实跑全绿，含 GF(256) 求逆全枚举）/ Go | 2026-09-23 |
| 642 | `08-安全/01-密码学/JOSE令牌JWS与JWE/` | JOSE JWS/JWE（RFC 7515/7516/7518/8725）：AAD 是 **ASCII(BASE64URL(Protected))** 不是整串、`alg` 管密钥而 `enc` 管内容、`dir` 的加密密钥段是空串、A128CBC-HS256 把 32 字节 CEK 切成 MAC_KEY 加 ENC_KEY 且 `AL` 是 AAD 的**位**长、RFC 3394 解包回收默认 IV 即完整性校验；从零实现 AES-128（S 盒由 GF 逆加仿射变换现算）并复现 FIPS 197 与 RFC 7516 A.3 的**整串**官方 JWE | Python（64 断言实跑全绿）/ Go（stdlib crypto） | 2026-09-23 |
| 643 | `08-安全/01-密码学/恒定时间与侧信道防护/` | 常量时间与侧信道：OpenSSL `constant_time.h` 的掩码原语（`msb = 0 - (a>>31)`、`is_zero = msb(~a & (a-1))`、`lt` 的符号位修正、`select` 两侧都要 `value_barrier`），BearSSL 执行模型的三类泄漏源（内存访问、条件跳转、少数指令），以及 memcmp / 表查找 / 模幂阶梯 / PKCS#7 去填充四处朴素实现与常量时间实现的**观测序列对照** | Python（1702 断言实跑全绿，含 lt32 的 40×40 全枚举）/ Go | 2026-09-23 |
| 644 | `08-安全/01-密码学/证书透明化Merkle审计/` | 证书透明化（RFC 6962）：`MTH` 的叶子前缀 0x00 与节点前缀 0x01（域分隔为抗第二原像）、空树根是 `SHA-256("")`、**非满树时「看索引比特定左右」的口诀会拼反**（n=3、m=2 反例）、PATH 与 SUBPROOF 的递归定义、一致性证明节点数上界 `ceil(log2(n))+1`、SCT 与 STH 的 TLS 编码（LogID 是日志公钥 DER 的 SHA-256、timestamp 是毫秒、SCT 用 3 字节而 extensions 用 2 字节长度前缀、两者 signature_type 分别 0 与 1） | Python（385 断言实跑全绿，含 n=1..22 全尺寸穷举）/ Go | 2026-09-23 |
| 645 | `09-语言学习/TypeScript/satisfies与类型注解/` | satisfies 与类型注解(TS 4.9 发布说明实读)：三种写法分工 —— 类型注解**抬结果类型**(`palette.green` 变成 `string 加 RGB`，于是 `.toUpperCase()` 报 TS2339)、`satisfies` **只校验不改写**(且目标类型会当上下文类型，把 `[255,0,0]` 从 `number[]` 引导推断成元组 RGB)、`as` 断言绕过检查;`satisfies` 也做多余属性检查(`Record<具名联合, unknown>` 拦住 `platypus` ⇒ TS2353)，换成带索引签名的 `Record<string, …>` 后这条检查**失效**(**不多不少**与**值都符合某类型**是两句不同的话)；运行时断言证明 `satisfies` 是纯编译期的(取 key 差集、可写性与原型都不受影响，然而 JS 里照样能塞进编译器拒绝的 key) | Python 自检(4 fixture 错误码精确比对 加 12 条运行时断言实跑全绿) / TypeScript 5.6.3 实测 | 2026-09-23 |
| 646 | `09-语言学习/TypeScript/模板字面量类型与类型级解析/` | 模板字面量类型四条规则：字面对字面是拼接、插值位是联合则**笛卡尔积**(多个位一起乘)、四个 intrinsic 由编译器内实现(TS 4.1 的 `applyStringMapping` 直接调 `toUpperCase`/`toLowerCase`/`charAt(0).toUpperCase` 加 `slice(1)`，故 `Uppercase<"ß">` 是 `"SS"` 且**非 locale 感知**)、`infer` 做模式匹配(注释必须**先**处理「后面还有一段」的分支才能递归到第二个路径参数)。`${Key}Changed` 反向推断出 Key 再用 `Type[Key]` 查回调参数(`string 加 keyof Type` 是必要收窄)。类型等价用 `(<T>() => T extends A ? 1 : 2)` 的那对 `Eq`/`Expect`(不等即 TS2344)，并配负控证明它不恒真。运行时对账发现两处不一致：`Split<"", D>` 得 `[]` 而 `String.prototype.split` 得 `[""]`；「无参数」类型侧是 `never` 而运行时是空数组 | Python 自检(3 fixture 加 Eq 负控探针 加 15 条运行时断言全绿) / TypeScript 5.6.3 实测 | 2026-09-23 |
| 647 | `09-语言学习/TypeScript/类型实例化深度与上限/` | 编译期递归的三道护栏，常量抄自本机 tsc.js：`instantiateTypeWithAlias` 在 `instantiationDepth === 100 或 instantiationCount >= 5e6` 报 TS2589(注意是 `===` 不是 `>=`)、`getConditionalType` 在 `tailCount === 1e3` 报 TS2589、`checkCrossProductUnion` 在估算规模 `>= 1e5` 报 TS2590、`removeSubtypes` 在 `count === 1e5` 且估算值 **严格大于** 1e6 时报 TS2590。4.5 引入的尾递归消除只认**单支尾调用**，且源码里 `if (newRoot.aliasSymbol) tailCount++` 意味着**内联的条件类型不计数**。现场边界扫描把阈值钉死：非尾递归 48 字符放行 / 49 报警；尾递归 999 / 1000(与 `1e3` 严格相等)；联合交叉积 316 的平方等于 99856 放行、317 的平方等于 100489 报警(把 `1e5` 夹住)。`getCrossProductUnionSize` 里任一位置是 `never` 则整积为 0 | Python 自检(4 fixture 加 6 组边界探针 加 17 条运行时断言全绿) / TypeScript 5.6.3 实测 | 2026-09-23 |
| 648 | `09-语言学习/TypeScript/声明合并与模块增强/` | 声明合并按 namespace / type / value 三格分账：class 加 interface 合法(都在 type 格)，class 加 class 与 class 加 var 都撞 value 格 ⇒ TS2300；同名非函数成员类型冲突 ⇒ TS2717；函数成员按**组**重载且**后写的组整体前排**、形参是单个字符串字面量的**特化签名冒泡到顶**(fixture 让「合并顺序」与「书写顺序」给出不同返回类型，配负控：删掉后写组后同一赋值报 TS2741)。namespace 合并里**未导出成员只在原 block 可见** ⇒ TS2304，运行时同样是对每段一个 IIFE，跨 block 读取直接 ReferenceError。模块增强只能按导出名做，增强 default ⇒ **TS2427**；手册那句「不能声明新的顶层声明」在 5.6.3 上**并未强制**( probe 放行)，真会报错的是在已 ambient 的上下文里再写 `declare` ⇒ TS1038 | Python 自检(7 fixture 加 overload 负控 加 增强边界探针 加 12 条运行时断言全绿) / TypeScript 5.6.3 实测 | 2026-09-23 |
| 649 | `09-语言学习/TypeScript/装饰器与元数据反射/` | 两套装饰器：标准版语境对象**不能装饰参数**(实测 TS1206)且不支持 `emitDecoratorMetadata`(单开后者 ⇒ **TS5052 全局选项错误**，无行号)，源码里 `getTypeMetadata` 开头 `if (!legacyDecorators) return void 0` 就是那道门。遗留版三个 key 按 `design:type` 加 `design:paramtypes` 加 `design:returntype` 拼进数组，谁能有谁没有由三个谓词决定(`shouldAddReturnTypeMetadata` **只**认 MethodDeclaration，故 getter/setter 没有 returntype；类级 paramtypes 要求存在带 body 的构造函数)。`__decorate` 的循环是 `for (var i = decorators.length - 1; i >= 0; i--)` ⇒ 同一成员上三个 key 的**运行次序**是返回值加参数加类型，叠多个装饰器时**工厂自上而下求值、装饰器自下而上应用**。序列化表实测 38 个成员：标量转包装器、`null`/`void`/`never` 转成 `void 0`、元组与 `readonly T[]` 得 Array、类引用得类本身而 `any`/`unknown`/类型字面量/IndexedAccess/映射类型/`typeof` 一并降级成 Object；联合里 `never` 被跳过、`unknown` 与 `any` 直接判 Object，其余成员序列化后不等则 Object | Python 自检(4 fixture 加 成员级 key 加 38 条序列化表 加 求值应用顺序，全绿) / TypeScript 5.6.3 实测 | 2026-09-23 |
| 650 | `10-逆向工程/01-二进制逆向/ELF符号哈希查找/` | ELF 符号哈希查找（glibc master `elf/simple-dl-new-hash.h` 加 `elf/simple-dl-hash.h` 加 `elf/dl-setup_hash.c` 加 `elf/dl-lookup.c` 加 `sysdeps/generic/ldsodefs.h` 原文实读）：SysV `_dl_elf_hash` 每轮 `h=(h<<4)+c; hi=h&0xf0000000; h^=hi>>24; h&=0x0fffffff` 故结果恒小于 2^28，GNU `_dl_new_hash` 是 `h=5381; h=h*33+c` 全 32 位 ⇒ 两套哈希**不可混用**（实测 `aa`：SysV 1649 模 8 得 1、GNU 5863207 模 8 得 7）。DT_GNU_HASH 表头四字 nbuckets 加 symbias 加 bitmask_nwords（源码直接 assert 是 2 的幂）加 shift；bloom 字下标是 **`(h/64)&(nwords-1)`**（`h=64` 时得 1，写成 `h&1` 得 0）；判据 `(w>>h1)&(w>>h2)&1` 且 `h2=(h>>shift)&63`（shift=0 时两个位是同一个）；chain 项 `((hv^h)>>1)==0` **只比较高 31 位** —— 实测 `s0`=5863752 与 `s1`=5863753 会互相成为候选，靠名字比较区分；`l_gnu_chain_zero = hash32 - symbias` 让下标直接就是符号索引（`ELF_MACHINE_HASH_SYMIDX` 默认是 `hasharr - chain_zero`）；bloom 只负责拦截不负责判定（60 符号表上 `q0` 放行但 chain 落空）；SysV 的 `STN_UNDEF==0` 同时是空桶与链尾哨兵 ⇒ 符号 0 永远查不到 | Python(elf_hash 234 加 main 66 加 selfcheck 229，488 断言实跑全绿) / Go(elf_hash 275 加 main 63，四项静态检查全 OK) | 2026-09-24 |
| 651 | `10-逆向工程/01-二进制逆向/WindowsX64展开数据/` | Windows x64 表驱动展开（Microsoft Learn《x64 exception handling》83 KB 实读 + mingw-w64 `winnt.h` 的 `UNW_FLAG_*`）：`.pdata` 的 RUNTIME_FUNCTION 是三个 image-relative ULONG，指向 `.xdata` 的 UNWIND_INFO（`UBYTE:3 version` 加 `UBYTE:5 flags` 加 prolog size 加 count 加 `UBYTE:4`×2 的 frame reg/offset scaled）。三处最易记反：①`count of unwind codes` 数的是**槽**不是操作码（SAVE_NONVOL 占 2、FAR 占 3、ALLOC_LARGE 视 info 占 2 或 3），遍历必须按槽数跳 ②数组按 prolog 偏移**降序**且总补齐到偶数项（`count=1` 也要 8 字节）③chained info 落在 `UnwindCode[(CountOfCodes+1)&~1]` 而不是 `CountOfCodes`（实测 count=3 与 4 都在槽 4、5 才到槽 6）。9 个 UWOP 的撤销式：ALLOC_SMALL = `info*8+8`（8~128）、ALLOC_LARGE 两形态、SET_FPREG 把 `RSP = FP - 16*scaled`、SAVE_XMM128 缩放是 **16** 而非 8、PUSH_MACHFRAME 的 `RSP += 40/48`。`SAVE_*` 的基准在无 FP 时是 RSP、否则是 `FP - 16*scaled`。三分支：prolog 内只撤销 `offset <= rip偏移` 的节点（实测 rip+0/+1/+5 三种结果） | Python(winunwind 294 加 main 加 selfcheck 237，67 断言实跑全绿) / Go(winunwind 295 加 main 70，四项静态检查全 OK) | 2026-09-24 |
| 652 | `10-逆向工程/01-二进制逆向/控制流平坦化与反平坦化/` | OLLVM 控制流平坦化（obfuscator-llvm llvm-4.0 `Flattening.cpp` 加 `Utils.cpp` 加 `CryptoUtils.cpp` 原文实读）：放弃条件是「任一块终结指令是 invoke」或「块数 ≤1」；入口以条件分支或多后继结尾时先 `splitBasicBlock` 并插到最前；`switchVar` 初值 = `scramble32(0,key)`，第 i 块的 case = `scramble32(i,key)`；0 后继不动、1 后继存常量、2 后继变 `SelectInst`。**坑**：`findCaseDest(succ)` 落空时用 `scramble32(getNumCases()-1)` 兜底，而此时 `getNumCases()` 已等于块数 ⇒ 兜底值**恰好等于最后一块的 case**（配负控：写成块数会偏一格）。case 值来自 `scramble32`：四轮 AES T 表混合后 XOR `LOAD32H(key)`；四张表不独立 —— `TE1=ROTR8(TE0)`、`TE2=ROTR16`、`TE3=ROTR24`，本 demo 由 GF(2^8) 逆加仿射现算 S 盒再生成 TE0 并逐项对上源码字面量 `c66363a5/f87c7c84/ee777799/f67b7b8d`；全零 key 下四轮输入字节全同 ⇒ 结果四字节相同（实测 `0x76767676`）。菱形 CFG 的「原执行序列」与「状态机执行序列」逐条件对拍一致 | Python(flatten 274 加 main 64 加 selfcheck 214，72 断言实跑全绿) / Go(flatten 274 加 main 61，四项静态检查全 OK) | 2026-09-24 |
| 653 | `10-逆向工程/01-二进制逆向/符号执行与约束求解/` | 符号位向量与状态机（claripy `claripy/ast/bv.py` 加 angr `angr/sim_state.py` 加 `angr/sim_manager.py` 原文实读）：claripy 位序与直觉相反 —— **`a[31]` 是最左（最高）位**，`a[31:30]` 是最高两位；由此 `chop(bits)` 的第一个元素是最高段（源码 `reversed([self[(n+1)*bits-1:n*bits]...])`，实测 `0x01020304.chop(8)=[01,02,03,04]`）、`get_byte(0)` 是最高字节（`pos=(size+7)//8-1-index`）、`concat(*args)` 里 self 在最高位。隐式位宽：`_from_int` 把整数**静默截断到左操作数的位宽**（`BVV(1,8)+300` 位宽 8、值 45）、`_from_Bool` 把 True 变成 `BVV(1,like.length)`。angr 侧：`_integral_stashes` 七项、`ALL="_ALL"` 与 `DROP="_DROP"`、`step` 无后继进 deadended、不可解默认丢弃而 `save_unsat=True` 进 unsat、**`explore` 第一行是 `num_find += len(found)`**（已有 1 个 found 时会继续找第 2 个）、avoid 的搬移先于 find、5 层二分后 active=32 | Python(symex_bv 289 加 symex_state 265 加 main 64 加 selfcheck 231，86 断言实跑全绿) / Go(symex 216 加 manager 193 加 main 69，四项静态检查全 OK) | 2026-09-24 |
| 654 | `10-逆向工程/01-二进制逆向/SLEIGH处理器规范/` | Ghidra SLEIGH 处理器规范语言最小引擎（`GhidraDocs/languages/html/sleigh_definitions.html` 加 `sleigh_tokens.html` 加 `sleigh_constructors.html` 加 `6502.slaspec` 原文实读）：`define endian` 必须是第一条且只能一次；token 位宽须 8 的倍数、字段区间是闭区间且**最低位标 0**、字段可任意重复与重叠；**大于 1 字节的 token 位编号受字节序影响**（先把字节按字节序拼成整数再从最低位编号 —— 实测 `12 34`：大端整数 0x1234 时 hi8=0x12，小端整数 0x3412 时 hi8=0x34），可用 `endian=little` 单独覆盖；属性只有 signed/hex/dec 且**默认十六进制**（`dec` 暂不支持，0xFE 作 signed 是 -2 显示 `-0x2`）。`attach variables fieldlist registerlist` 两侧**不要求等长** —— fieldlist 里**每个**字段都变成同一张查表、索引从 0 起。构造函数五段，空标识符 = 根指令表（`instruction` 保留但不得写在表头）；**约束比的是字段的原始整数编码，attach 之后的含义在位模式里不适用**；单独出现的标识符即操作数（链接到本地符号与全局 family 符号）；`...` 引入下一个 token 处理变长（实测 `A9 99` 解出 lda 长度 2 操作数 0x99，只给 1 字节则不匹配） | Python(sleigh 275 加 sleigh_pattern 99 加 main 81 加 selfcheck 246，67 断言实跑全绿) / Go(sleigh 299 加 main 110，四项静态检查全 OK) | 2026-09-24 |
| 655 | `11-性能分析/05-容量规划与性能建模/Little定律与排队守恒/` | Little 定律与排队守恒（Wikipedia 《Little's law》/《M/M/1 queue》 + cloudandsre 排队论 + Harchol-Balter Ch.1 公开样章实读）：`L = λW` 三用（定并发/反推隐性等待/算吞吐上限）、M/M/1 四稳态量与 `λW == L`、`λWq == Lq`、`W = Wq + S`、`L = Lq + ρ` 四条代数恒等式、拐点表 `W/S = 1/(1−ρ)`（70%→80% 多付 50%、90%→95% 翻倍）、死队列判据（10000 深、500/s 排空、2s 超时 → 作废 9000/有效 1000，边界取「严格小于超时」）、事件驱动仿真在 ρ=0.5 与 0.8 两个负载点上实证 `L̂ ≈ λ̂Ŵ`（相对误差 <2%，λ̂ 用离开计数保证与 Ŵ 同批） | Py 49 断言 + Go 2 文件 | 2026-09-24 02:00 |
| 656 | `11-性能分析/05-容量规划与性能建模/MMc与ErlangC容量拐点/` | M/M/c、Erlang C 与容量拐点（Wikipedia 《Erlang (unit)》/《M/M/c queue》/《Pollaczek–Khinchine formula》实读）：Erlang B 的闭式与两种递推三写法互证、`C = B/(1−ρ(1−B))` 关系式、M/M/c 的 `π₀/W/Wq/L/Lq` 与 `c=1` 精确退化到 M/M/1、"c 台慢的 vs 1 台快的"均值口径下快机恒优、M/G/1 P-K 的方差惩罚（Var=0 时 Wq 减半、Var=9/μ² 时 5 倍）、M/M/1/K 的 `λ_a = μ(1−π₀)` 流量守恒与 `K→∞` 收敛 | Py 153 断言 + Go 3 文件 | 2026-09-24 02:00 |
| 657 | `11-性能分析/05-容量规划与性能建模/USL三参数拟合与峰值定位/` | USL 三参数拟合与峰值定位（Gunther perfdynamics USL 原文 + R 包 usl 源码 `R/usl.R` 与 CRAN vignette 实读）：自写 LM + 坐标下降拟合 α/β/γ，在 raytracer 上复现 `α=0.0577708 / β=0 / γ=21.848844`（SSE 697.2378、RSE 9.34）、在 specsdm91 上复现 `α=0.0277285 / β=0.0001044 / γ=89.995233`（SSE 27453.7、RSE 82.8）、`Nmax=√((1−α)/β)` 与数值 argmax 对拍、`Xlim = γ/α` 与 `Nopt=1/α`、β 减半后峰值 96.5→139.4 的 what-if、R 的 `efficiency()` 口径（观测值/(γN)）Min/Max 逐项对齐 | Py 73 断言 + Go 2 文件 | 2026-09-24 02:00 |
| 658 | `11-性能分析/05-容量规划与性能建模/扩展律Amdahl与Gustafson与USL/` | Amdahl / Gustafson / USL 三条扩展律（HPC 101《Limits of Parallelism》+ Cornell CVW《Amdahl's Law》+ UVA RC 性能分析页 + Gunther USL 原文实读）：`S = 1/[(1−P)+P/N]` 与 `S = N−(1−P)(N−1)` 两问两答、hpc101 的 `P=0.9,N=100 → 90.1`、Cornell 三种等价写法、`P` 在两式含义不同（写成 `P+N(1−P)` 会错，`P=0.5` 时数值相同故单点测不出）、`β=0,γ=1` 时 USL 在 6×9 个点上逐点等于 Amdahl、`1/α` 即 Amdahl 天花板、UVA「99% 并行才撑到 ~100 进程」与反解公式 | Py 231 断言 + Go 2 文件 | 2026-09-24 02:00 |
| 659 | `11-性能分析/05-容量规划与性能建模/过载保护与自适应并发/` | 过载保护与自适应并发（Google SRE Book Ch.21《Handling Overload》+ Envoy《Adaptive Concurrency》文档 + Netflix `VegasLimit.java`/`Log10RootIntFunction.java` 源码实读）：四个 criticality 的阈值单调不变量（被拒集合恒为前缀）、客户端自适应限流 `S = K·C` 稳态与「后端拒:接 = K−1」复现原书 1:1（K=2）与 1:10（K=1.1）两个结论、Envoy `gradient=(minRTT+B)/sampleRTT` 与 `limit_new=gradient·limit+√limit` 的 buffer 成对断言、VegasLimit 的 `LOG10/α=3·LOG10/β=6·LOG10`、queueSize 四分支边界（含 `==threshold`、`==beta` 含等号）与探针判据（jitter 必须注入确定性源） | Py 150 断言 + Go 3 文件 | 2026-09-24 02:00 |
| 660 | `12-项目工程/01-数据结构与集合选型/CPythonDict紧凑布局与开放寻址/` | CPython dict 紧凑布局与开放寻址（cpython main 分支 `Objects/dictobject.c` 258 KB 与 `Include/internal/pycore_dict.h` 16 KB 实读）：`dk_indices`/`dk_entries` 分离与四个计数器、`USABLE_FRACTION(n)=((n)<<1)/3` 与 `GROWTH_RATE=used*3`、`calculate_log2_keysize=bit_length(minsize-1)`（故 `GROWTH_RATE(3)=9` 给出 16 而非 8）、`get_log2_bytes` 四档索引宽度（1/2/4/8）、perturb 探测 `i=(i*5+perturb+1)&mask` 与 `perturb>>=5` 的真实下标序列 `documented_probe_order(8)=[0,1,6,7,4,5,2,3]`、`DKIX_EMPTY=-1`/`DKIX_DUMMY=-2`、删除**不回增** `dk_usable`（删 3 个后 `dk_size` 仍为 8）只有 `dictresize` 才压实 | Py 302 断言 + Go 2 文件 | 2026-09-24 04:00 |
| 661 | `12-项目工程/01-数据结构与集合选型/JavaHashMap树化与扩容拆分/` | OpenJDK HashMap 树化与扩容拆分（jdk master `src/java.base/share/classes/java/util/HashMap.java` 100 KB 实读）：六个常量、`tableSizeFor` 的 `-1 >>> nlz(cap-1)` 且 Java 移位量 `& 31`、`spread` 的 `h ^ (h>>>16)` 扰动非幂等、`resize()` 三分支（`newThr` 只在 `oldCap>=16` 时翻倍）、`treeifyBin` 两道门（TREEIFY_THRESHOLD=8 与 MIN_TREEIFY_CAPACITY=64）、`TreeNode.split` 的三种结局；`new HashMap()` 与 `new HashMap(0)` 是两条不同路径（前者 threshold=0，后者首 put 即 cap=2/thr=1） | Py 99 断言 + Go 2 文件 | 2026-09-24 04:00 |
| 662 | `12-项目工程/01-数据结构与集合选型/GoMap可扩展散列与SwissTable/` | Go map 可扩展散列与 Swiss Table（golang/go master `src/internal/runtime/maps/map.go` 27 KB、`table.go` 41 KB、`group.go` 11 KB 与薄壳 `src/runtime/map.go` 实读）：`maxTableCapacity=1024`、`maxAvgGroupLoad=7`、`ctrlEmpty=0x80`/`ctrlDeleted=0xFE`、`h1=h>>7`/`h2=h&0x7f`、三角探测 `probeSeq`、rehash 后按负载决定 grow 还是 split、`installTableSplit` 目录翻倍、`pruneTombstones` 10% 门槛（35 个墓碑放弃、175 个清光并归还 growthLeft）、**单组 8 槽表永不产生墓碑** | Py 1003 断言 + Go 3 文件 | 2026-09-24 04:00 |
| 663 | `12-项目工程/01-数据结构与集合选型/SwissTable控制字节与容量映射/` | Swiss Table 控制字节与容量映射两派对照（abseil-cpp `absl/container/internal/raw_hash_set.h` 176 KB 与 `hashtable_control_bytes.h` 20 KB、rust-lang/hashbrown `src/raw.rs` 197 KB 与 `src/control/tag.rs` 实读）：Abseil 侧 `ctrl_t{kEmpty=-128,kDeleted=-2,kSentinel=-1}`、`kWidth=16`、容量恒为 `2^k−1`、`H2=hash>>57`、`NextCapacity(n)=n*2+1`、`kMaxCapacityForLoadFactorOne=kWidth*4-1`；hashbrown 侧 `Tag::EMPTY=0xFF`/`DELETED=0x80`、桶数取 `2^k`、`Tag::full` 判最高 7 位、`ProbeSeq` 的 stride 每轮 `+= WIDTH`、`capacity_to_growth(127)=112` 而非 111 | Py 784 断言 + Go 2 文件 | 2026-09-24 04:00 |
| 664 | `12-项目工程/01-数据结构与集合选型/动态数组增长因子实读取证/` | 动态数组增长因子实读取证（gcc `bits/stl_vector.h`、llvm `libcxx/include/__vector/vector.h`、MSVC STL `stl/inc/vector`、Go `runtime/slice.go`、CPython `Objects/listobject.c`、Rust `library/alloc/src/raw_vec/mod.rs` 六家实读）：六家增长公式与容量序列、`_M_check_len` 的 `if (__n < size()) __n = size()` 使 push_back 恰好 2× 而批量 insert 精确分配、MSVC `cap+cap/2` 给 `1,2,3,4,6,9,13`、Go 256 阈值后 `newcap += (newcap+3*256)>>2` 比值 `2.0→1.625→1.481` 收敛到 1.25、CPython 复现源码注释序列 `0,4,8,16,24,32,40,52,64,76`、Rust `min_non_zero_cap` 让 `Vec<u64>` 首个容量是 4；旧块复用闭式判据 `r^i(2−r)>=1`（r=2 恒为 0 永不复用，i=2 的正根是黄金比例 φ≈1.618），取整只会让复用更晚 | Py 152 断言 + Go 2 文件 | 2026-09-24 04:00 |
| 665 | `01-游戏开发/08-动画/glTF蒙皮与骨骼动画/` | 线性混合蒙皮（KhronosGroup/glTF@main `Specification.adoc` 的 Skins 三节与 Animations 节原文实读）：`jointMatrix(j) = globalJoint(j) · inverseBind(j)`，先乘 inverseBind 再走骨骼节点变换；规范示例里 **node_3/node_4（mesh 节点及其父节点）的变换被忽略**，只有关节祖先的变换生效（改它们到任意值蒙皮结果逐分量不变）；绑定姿态下所有 jointMatrix 为**单位阵**，蒙皮还原原顶点。权重编码：单集合 ≤4 关节、非负、同顶点内同关节不得有两个非零权重（故 `[0,1,1,1]` 配四份 0.25 非法而 `[0,1,0,1]` 配 `[0.5,0.5,0,0]` 合法）、unorm8 归一化前整数和 **MUST 为 255**（朴素四舍五入给 256，须用最大余数法重分配）、unorm16 为 65535、官方校验阈值 `2e-7 × 非零权重数`。真实管线**不做齐次除法**：权重和 0.5 时顶点朝原点收缩一半。LBS 固有缺陷：±45° 各半混合得 `cos45° = 0.7071`，收缩 29.3% | Python(skinning 240 加 selfcheck 192 加 main 55，40 断言实跑全绿) / Go(293，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 666 | `01-游戏开发/08-动画/动画采样与四元数插值/` | glTF Appendix C 四种取值公式原文实读：STEP 恒取 `v_k`、LINEAR 为 lerp、rotation 的 LINEAR 为 **SLERP**（`a = arccos(abs(dot))`、`s = sign(dot)`）、CUBICSPLINE 每帧三值 `(a_k, v_k, b_k)` 且切线项带 `t_d` 因子。命中关键帧原值直出、区间外 clamp 到**最近端点**（STEP 在 `t=5` 而末帧在 2 时给 `v_n=20` 而非倒数第二个）。最短路：把 `q_{k+1}` 取反后规范公式结果**逐分量不变**，朴素 slerp 走长弧给 135° 而非 45°。切线单位是「每秒」：取割线速度时精确退化成 LINEAR（0.26/1.0/1.77 三点比对）、零切线退化为 smoothstep（`t=0.25` 给 1.5625 而非 2.5）、`t=0.5` 是对称点切线贡献会抵消故不能用来做缩放对比、对真实时间求导才等于切线 | Python(sampler 247 加 selfcheck 210 加 main 38，53 断言实跑全绿) / Go(255，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 667 | `01-游戏开发/08-动画/Godot轨道插值与循环模式/` | Godot 4 `Animation::_find` 与 `_interpolate` 逐行转写（godotengine/godot@master `scene/resources/animation.cpp` 原文实读）：`len = _find(keys, length) + 1` 使**超过 length 的关键帧被丢弃**（关键帧 0/0.5/1/2、`length=1.5` 时在 1.8 处取到 20 而非 999）；命中用 `is_equal_approx`（CMP_EPSILON=1e-5）并**提前 return**，因此会跳过后面的 `limit` 越界检查；`FindMode` 三档（NEAREST 不判、APPROX 用容差、EXACT 用等号）。三种 LoopMode 的 idx/next 与端点记账：LOOP_NONE 在端点处 `delta` 保持 0 故 `c=0`（`t=0.75` 给 10、`t=-0.1` 给 0、`t=length` 给末帧值），LOOP_LINEAR 绕回（`t=0.75` 给 5、`t=-0.1` 的 `c=0.8` 给 2），PINGPONG 下标由 `round(pingpong(i+0.5, len)-0.5)` 折成三角波（len=5 时 0..9 映射为 0 1 2 3 4 4 3 2 1 0）。另有 UPDATE_DISCRETE 强制 NEAREST、`transition == 0` 直接取 idx 值 | Python(track 235 加 selfcheck 141 加 main 20，58 断言实跑全绿) / Go(228，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 668 | `01-游戏开发/08-动画/Godot混合空间1D与2D/` | `AnimationNodeBlendSpace1D::_process` 与 2D 的 `_process`/`_blend_triangle` 原文实读：一维在**最近的两侧**之间线性插值（点位无需排序，源码是线性扫描；非均匀间距按实际距离算，`0` 与 `3` 之间取 1 给 2/3 比 1/3）。**平局取向相反**：插值模式 `closest` 用 `>=` 判定取下标**大**者，离散模式用 `<` 判定取下标**小**者（点位 `[-1,1]`、`pos=0` 时分别是 1 与 0）。sync 的目标长度是加权平均 `Σw·len / Σw` 且**排除长度 ≤ CMP_EPSILON 的点**，播放速率取其倒数。二维重心坐标用点积形式求解（`v=(d11·d20-d01·d21)/denom`），三点共线 `denom==0` 时退化为 `[1,0,0]`；点在三角形外时回退到最近边的投影并按 `c = dist(a, closest) / dist(a, b)` 在两端点间插值，第三个顶点权重置 0（`(2,0)` 给 `[0,1,0]`、`(1,1)` 给 `[0,0.5,0.5]`） | Python(blendspace 210 加 selfcheck 152 加 main 22，59 断言实跑全绿) / Go(275，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 669 | `01-游戏开发/08-动画/Godot状态机与交叉淡入/` | `animation_node_state_machine.cpp` 原文实读：交叉淡入 `fade_blend = MIN(1.0, fading_pos/fading_time)`、`fading_pos += abs(delta)`（负 dt 也推进）、`p_seek` 时不推进；`fade_blend` 与 `1-fade_blend` **都会被兜到 CMP_EPSILON**，故淡入结束时旧状态仍保留 `1e-5` 权重（源码注释：为让 discrete 关键帧继续被处理）。推进判据：自动推进时 `priority <= priority_best`（初值 1e20）⇒ **数值越小越优先、平局取下标更大者**；`ADVANCE_MODE_DISABLED` 直接 continue；`_check_advance_condition` 在 `advance_mode != AUTO` 时恒 false。**travel 路径非空时只认「当前到 path[0]」那条过渡，既不看条件也不比优先级**。`_transition_to_next_recursive` 遇带淡入的过渡置位后 break（一帧一步），无淡入时会在同一帧走完整条路径；`transition_path` 做环路检测防止 A 与 B 互相指向时死循环。`SWITCH_MODE_SYNC` 把新状态 seek 到旧位置 | Python(statemachine 207 加 selfcheck 227 加 main 45，72 断言实跑全绿) / Go(272，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 670 | `02-Web开发/05-流量治理与限流/01-GCRA与漏桶计量/` | GCRA 与漏桶计量（ITU-T I.371 的两种等价描述，经 Wikipedia《Generic cell rate algorithm》实读，加 Brandur《Rate Limiting, Cells, and GCRA》原文）：虚拟调度维护理论到达时间 TAT，一致判据 `ta 大于等于 TAT 减 τ`（I.371 原文写严格大于，边界 `ta 等于 TAT 减 τ` 上两种写法结论相反，两条断言成对钉住），非一致时 TAT 不变，一致时 `TAT = max(TAT, ta) 加 T`（max 挡掉空档期攒信用）。与连续状态漏桶（漏水速率 1、桶容量 T 加 τ、判据 X撇 小于等于 τ）在 300 组随机到达序列上逐事件等价，判定与 retry_after 全同；不变量是 `TAT = LCT 加 X`。τ 口径有真实文献分歧：I.371 扣 τ 得突发 floor(τ/T) 加 1，Brandur 扣 τ 加 T 多给一张（rate=10 tau=0.4 时 5 对 6）。时间桶（网格对齐）在 0.9s 与 1.1s 各放 60 次即 0.2 秒内 120 次，GCRA(tau=0) 同期只放 2 次。被拒不推进 TAT 单独立断言：连续 50 次被拒后 retry_after 恒为 0.08、TAT 不变 | Python(gcra.py 129 加 selfcheck_gcra.py 121 加 main.py 26，25 断言实跑全绿) / Go(gcra.go 138，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 671 | `02-Web开发/05-流量治理与限流/02-RateLimit响应头部/` | RateLimit 响应头部（draft-ietf-httpapi-ratelimit-headers-11 全文 67097 B 实读，加 RFC 6585 §4 429）：两个字段都是 SF List of Items。RateLimit-Policy 的 q 必填非负整数、qu 取自注册表（requests / content-bytes / concurrent-requests，缺省 requests）、w 非负**且非零**、pk 为 Byte Sequence；RateLimit 的 r 必填、t 非负且 0 合法 —— w 与 t 的零语义不对称单独成对断言。客户端优先级链照抄：畸形字段 MUST 被忽略（不是报错也不是沿用旧值）、Retry-After 存在时 MUST 优先（retry_after=1 配 t=7 也照用 1）、MUST NOT 假设后续响应仍有该字段。多条 Item 并存时规范未定义合并方式，本实现取最保守（最小 r/t）并在 README 标注系推导非条文。三种 problem type（quota-exceeded 429 / temporary-reduced-capacity 503 / abnormal-usage-detected 429）与 violated-policies 字符串数组。规范原文笔误照录：§5.1 写 `429 Bad Request` 而 RFC 6585 §4 的短语是 Too Many Requests，§5.2 写 `503 Server Unavailable` | Python(ratelimit_headers.py 208 加 selfcheck_ratelimit_headers.py 111 加 main.py 43，35 断言实跑全绿) / Go(ratelimit_headers.go 216，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 672 | `02-Web开发/05-流量治理与限流/03-令牌桶与预热限流/` | Guava RateLimiter 逐行转写（google/guava@master 的 RateLimiter.java 22178 B 与 SmoothRateLimiter.java 19744 B 实读）：reserveEarliestAvailable 返回的是**旧的** nextFreeTicketMicros，故首次 acquire 恒返回 0（源码注释「15 fresh permits 需要 3 秒」说的是内部时钟位移，第二次调用才返回 3.0s），这条用两条断言钉住。resync 只在 now 大于 nextFreeTicket 时补令牌且被 maxPermits 钳住（闲置 1000s 也不超额）；nextFreeTicketMicros 用 LongMath.saturatedAdd，1e14 张请求钳到 Long_MAX 不回绕。SmoothBursty 的 storedPermitsToWaitTime 恒为 0、coolDown 等于 stableInterval、maxBurstSeconds 默认 1.0。SmoothWarmingUp 的 thresholdPermits=0.5*warmup/stable、maxPermits=threshold 加 2*warmup/(stable 加 cold)、slope=(cold 减 stable)/(max 减 threshold)，两条面积恒等式（梯形等于 warmupPeriod、左半段等于 warmupPeriod/2）各一条断言，冷启动取光 10 张恰 1.5 秒。**两种实现的初态相反**：oldMaxPermits==0 时 Bursty 给 0 而 WarmingUp 给满桶（注释 initial state is cold），加 inf 分支也相反。tryAcquire 走 queryEarliestAvailable **不触发 resync**，判据用的是陈旧值 | Python(guava_ratelimiter.py 153 加 selfcheck_guava_ratelimiter.py 135 加 main.py 39，39 断言实跑全绿) / Go(warmingup.go 175，无虚派发故两钩子做成函数字段注入，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 673 | `02-Web开发/05-流量治理与限流/04-滑动窗口计数/` | Sentinel LeapArray 滑动窗口（alibaba/Sentinel@master 的 LeapArray.java 15020 B 与 WindowWrap.java 2684 B 实读）：构造期三条 AssertUtil（sampleCount 大于 0、intervalMs 大于 0、能整除）；idx = (t/windowLength) mod array.length，windowStart 向下取整。currentWindow 四分支逐条断言：空桶新建、同 windowStart 复用、更大则**复用对象加 reset**（不是新建）、时间回拨返回临时桶且**不写回数组**（连续两次拿到两个对象）。过期判据是**严格大于**：time 减 start 等于 interval 时不过期、等于 interval 加 1 才过期。持续流量下最老桶距现在最多 interval 减 1 毫秒故全部有效，isWindowDeprecated 只在流量中断（数组里躺着上一纪元的桶）时生效 —— t=1001 时有效桶从 2 掉到 1、补一次 add 回到 2。边界突发对比（100 次/秒、t=900 打满）：固定窗口 t=1001 再放 100 共 200 次，sampleCount=2 与 10 的滑动窗口 t=1001 放 0 次，直到 t=1501 旧桶过期才重新放行 | Python(leap_array.py 136 加 selfcheck_leap_array.py 127 加 main.py 45，37 断言实跑全绿) / Go(leap_array.go 186，CAS 加 updateLock 换切片加互斥锁，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 674 | `02-Web开发/05-流量治理与限流/05-描述符匹配与分布式配额/` | envoyproxy/ratelimit 描述符匹配与配额判定（@main 的 config_impl.go 27363 B、base_limiter.go 12171 B、cache_key.go 2992 B、time.go 2224 B 实读）：GetLimit 三条候选按序（key_value 精确、上一层 wildcardEntries、回落到只按 key 的默认条目），**深度必须完全匹配**才采用 limit（i == len(entries) 减 1），Envoy 侧 limit_override 完全绕过配置树且 shadow_mode 写死 false（连 descriptor 不在配置里也不检查）。wildcardMatch 六条边界断言（单段全等、前后缀、总固定长度、中间段顺序）。配置期校验：unlimited 与 unit 互斥、非 unlimited 必须有合法 unit、空 key 与重复 finalKey 报错。判定阈值**严格大于**（after 大于 requests_per_unit 才过限），记账用大于等于（before 已过限则整份 addend 计 OverLimit，否则只计超出部分并把 threshold 减 max(near, before) 记进 NearLimit）。shadow_mode 下过限也返回 OK 且仍记 OverLimit；local cache 命中直接 OVER_LIMIT 且 LimitRemaining=0；hits_addend 可大于 1 也可为负。缓存键 = prefix 加 domain 加 每个 entry 的 key_value 加窗口桶起点（month 单位走已验证的 UTC 日历月 MonthStartUnix，MonthExpirationSeconds 实测 2026-03-15 12:00Z 得 1425600） | Python(ratelimit_service.py 266 加 selfcheck_ratelimit_service.py 211 加 main.py 71，58 断言实跑全绿) / Go(ratelimit_service.go 253，bracket_check BALANCED 加 go_sanity 过) | 2026-09-24 |
| 675 | `03-系统编程/01-网络编程/传输层/TCP-QUICKACK与USER-TIMEOUT/` | TCP_QUICKACK 与 TCP_USER_TIMEOUT（tcp(7) 与 Linux 源码实读）：`TCP_QUICKACK` 是**一次性**开关，配额 `rcv_wnd/(2*rcv_mss)` 上限 `TCP_MAX_QUICKACKS=16`、商为 0 兜底成 2，而 `tcp_in_quickack_mode` 里 pingpong 会一票否决。延时确认常量在 HZ=1000 下是 `TCP_ATO_MIN = TCP_DELACK_MIN = HZ/25 = 40 ms`、`TCP_DELACK_MAX = HZ/5 = 200 ms`；ato 递推式 `a ← floor(a/2) + m` 的整数不动点是 `2m−1` 而不是 `2m`（m=99 时收敛到 197，靠自身增长永远到不了 200），且 m 存在「既不小于 ato 也不大于 rto」的空档档位什么都不做。`TCP_USER_TIMEOUT` 有三条读取路径：重传定时器钳制（到点返回 1 jiffy 而不是 0）、`retransmits_timed_out`（传入非零 timeout 就完全绕开 `tcp_model_timeout`，后者在 `tcp_retries2=15` 下给出 924.6 s ≈ 15.4 分钟）、零窗口探测定时器钳制（剩余时间有 `TCP_TIMEOUT_MIN=2` 下限）；`user_timeout=0` 表示用系统默认而不是立刻超时，它不改重传节奏只改放弃时刻，但与 keepalive 并用时覆盖 keepalive | Python(dack_model.py 219 加 selfcheck_dack.py 189 加 main.py 98，85 断言实跑全绿) / Go(dack.go 231 加 main.go 33 加 selfcheck_dack.go 150，人工审查加括号加 go_sanity 加 go_crossref 加 syntax_sanity 四项静态检查全 OK) | 2026-09-24 |
| 676 | `03-系统编程/01-网络编程/零拷贝/MSG-ZEROCOPY与vmsplice/` | MSG_ZEROCOPY 与 vmsplice（内核 `Documentation/networking/msg_zerocopy.rst` 全文 9078 B 与 `include/uapi/linux/errqueue.h` 实读）：必须先 `setsockopt(SO_ZEROCOPY)` 声明意图，否则内核**静默忽略**该 flag。counter 按**系统调用次数**计数而不是字节，长度 0 与失败（ENOBUFS）都不增，UINT_MAX 之后回绕。通知走 socket error queue 的 `sock_extended_err`：`ee_origin = SO_EE_ORIGIN_ZEROCOPY = 5`、`ee_errno` 恒 0（否则会阻塞该 socket 上的 send 与 recv）、区间 `[ee_info, ee_data]` 是**闭区间**；新通知若恰好延长队尾上界就丢弃新包只抬高上界，故 TCP 有序时 outstanding 恒为 1，u32 回绕处仍算连续。loopback 到本机（含 packet socket 与 tun）**必定**退化成 deferred copy 并在 `ee_code` 置 `SO_EE_CODE_ZEROCOPY_COPIED=1`，所以完成通知不等于传输完成。`vmsplice` 的 fd 必须是 pipe、`nr_segs` 上限 `IOV_MAX=1024`、`SPLICE_F_GIFT` 要求基址与长度都页对齐且此后不得再改该内存，且只有写端是真 splice、读端实际只是拷贝 | Python(zcopy_model.py 142 加 selfcheck_zcopy.py 144 加 main.py 88，57 断言实跑全绿) / Go(zcopy.go 148 加 main.go 142，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 677 | `03-系统编程/01-网络编程/IO模型/MSG-OOB紧急数据/` | MSG_OOB 紧急数据与 SIGURG（`net/ipv4/tcp_input.c` 的 `tcp_check_urg` 与 `tcp_urg`、`net/ipv4/tcp.c` 的 `tcp_recv_urg` 与 `SIOCATMARK`、`include/net/tcp.h` 的 `tcp_inq` 实读）：默认 BSD 解释下 `urg_ptr` 先减 1 再加 seq，指向紧急数据的**最后一个**字节（`tcp_stdurg=1` 才是 RFC 1122 口径，两者实测差一个字节）。三条守卫分别拦掉「已读过的指针」「落在已收报文里的指针即不回放」「不比现有更新的指针」。`urg_data` 高 8 位是三态 `TCP_URG_VALID` 与 `TCP_URG_NOTYET` 与 `TCP_URG_READ`，**低 8 位就是那个紧急字节本身**，这是「Linux 的紧急数据只有 1 字节」的直接证据。`SIOCATMARK = urg_data && urg_seq == copied_seq`；`tcp_inq` 在有未处理紧急数据时把可读字节数**截断到标记处**（队列里明明有 3 字节也报 0）；`tcp_recvmsg` 的 `urg_offset` 截断**不看** `SO_OOBINLINE`，该标志只决定 `urg_offset == 0` 那一格是当普通数据拷走还是跳过并记 `urg_hole`。连续两次带 MSG_OOB 的发送不会让两个字节都消失，源码那段 "Double Dutch" 注释明确说指望它消失的应用是 buggy 的 | Python(urg_model.py 176 加 selfcheck_urg.py 168 加 main.py 99，61 断言实跑全绿；另 NOTES.md 95 行存长代码块) / Go(urg.go 187 加 main.go 145，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 678 | `03-系统编程/01-网络编程/传输层/BBR拥塞控制/` | BBR 拥塞控制（`net/ipv4/tcp_bbr.c` 全文 42814 B 实读）：增益是**定点整数**，`BBR_SCALE=8`、`BBR_UNIT=256`，带宽单位是 packets 每微秒左移 `BW_SCALE=24` 位。`high_gain = BBR_UNIT*2885/1000 + 1 = 739`（≈2/ln2，源码注释说这是能让 pacing rate 每 RTT 翻倍的最小增益），`drain_gain = BBR_UNIT*1000/2885 = 88`，两者各自在整数除法上截断，乘积右移 8 位是 **254 而不是 256**，即 drain 不是 high 的精确倒数。PROBE_BW 的 8 相增益循环是 5/4 加 3/4 加六个 1.0，起始相位取 `CYCLE_LEN-1-rand(7)` 后**立即**再推进一次，故实际取值集合是 0 与 2 到 7 —— **永远不可能是 1**。`bbr_bdp` 最后一次除法是向上取整（源码说为避免负反馈环）；`bbr_quantization_budget` 三步是先加 `3*tso_segs`、再抬到偶数、最后仅在 PROBE_BW 且相位 0 时再加 2。pacing rate 比估计带宽低 `bbr_pacing_margin_percent=1`，乘法顺序是为避免 u64 溢出。full_bw 判据是连续 3 轮（且非 app-limited 且 round_start）增长低于 25%，之后 STARTUP 进 DRAIN，并可能在同一轮 fall through 直接到 PROBE_BW | Python(bbr_model.py 195 加 selfcheck_bbr.py 223 加 main.py 93，101 断言实跑全绿) / Go(bbr.go 197 加 main.go 113，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 679 | `03-系统编程/01-网络编程/协议解析/QUIC变长整数与帧/` | QUIC 变长整数与帧（RFC 9000 全文 403442 B 的 §12.4 与 §16 与 §19.1 到 §19.3 与附录 A.1 实读）：变长整数把长度塞进首字节最高两位，`length = 1 << prefix`，四档分别给出 6 与 14 与 30 与 62 个可用位。同一值的非最短编码是合法的（附录 A.1 明说 `0x25` 与 `0x4025` 都解码为 37），**唯一例外是帧类型**必须最短编码。报文负载至少要含一帧，空负载是 `PROTOCOL_VIOLATION`；帧永不跨包；未知帧类型是 `FRAME_ENCODING_ERROR` 而不是跳过；所有帧幂等。ACK 帧的区间算术是 `smallest = largest − first_ack_range`，后续每段 `largest = previous_smallest − gap − 2`，且「gap 里的包数比 Gap 字段的编码值多 1」，任何一步算出负数即 `FRAME_ENCODING_ERROR`。`ACK Delay` 要乘上 `2^ack_delay_exponent`，只有类型 0x03 才带三个 ECN Counts | Python(quic_varint.py 163 加 selfcheck_quic.py 125 加 main.py 88，76 断言实跑全绿) / Go(quic_varint.go 196 加 main.go 186，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 680 | `04-移动开发/01-iOS/启动优化/链式修正/` | dyld 链式修正（`apple-oss-distributions/dyld@main` 的 `include/mach-o/fixup-chains.h` 12659 B、`common/ChainedFixups.cpp` 71827 B、`common/MachOAnalyzer.cpp` 251320 B 实读）：页内 `dyld_chained_starts_in_image` 的 `page_start` 是 **12 位**且只有 `>= 0x8000` 才表示偏移区，解码必须**先判 `DYLD_CHAINED_PTR_START_NONE`（0x8000）再判 `..._START_MULTI`（0x8000 起的多链索引）**，两者数值同为 0x8000 而语义不同；段起始页表溢出时走 `page_extra`。64 位槽位三态由最高位区分：`DYLD_CHAINED_PTR_64`（rebase，高 8 位是 `next`，余 36 位 target + 12 位 high8）、`DYLD_CHAINED_PTR_64_OFFSET`（bind，`next` 移低、`addend` 8 位）、`DYLD_CHAINED_PTR_64_KERNEL_CACHE` 与 `..._OFFSET_CACHE`；arm64e 的 `DYLD_CHAINED_PTR_ARM64E` 用 `auth` 位区分 bind/rebase 与 `diversity`/`addrDiv` 拆解，`DYLD_CHAINED_PTR_ARM64E_USERLAND24` 把 `next` 压到 24 位。链遍历是**先算出 next 再回调**，`next > endPage` 才断链（严格大于，等于当前页末端仍继续）。回写校验必须**读回位域再比**，直接用赋值前的变量比较会让超宽的 next / addend / ordinal 悄悄通过 | Python(chainfix_const.py 64 加 chainfix_format.py 300 加 chainfix_walk.py 103 加 chainfix_model.py 23 加 selfcheck_chainfix.py 211 加 main.py 75，87 断言实跑全绿) / Go(chainfix.go 235 加 chainfix_walk.go 88 加 main.go 83，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 681 | `04-移动开发/01-iOS/内存管理/nanov2分配器/` | libmalloc NanoV2（`apple-oss-distributions/libmalloc` 的 `src/nanov2_zone.h` 10983 B、`src/nanov2_malloc.c` 129396 B、`src/nano_malloc_common.h` 3019 B、`src/nano_zone_common.h` 2229 B、`src/nano_malloc_common.c` 11449 B 实读）：**256B 是 NanoV2 与可扩展堆的分界**。块元数据里 `free_count` 的口径是「空闲槽位数减 1」——满块回绕到 `0x3FF`，所以 `slot_full`（`free_count == 0`）的真实含义是「拿完这一格就没有了」而不是「零空闲」。`next_slot` 是 **1-based**，另有 7 个特殊值 `SLOT_NULL` / `SLOT_GUARD` / `SLOT_BUMP` / `SLOT_FULL` / `SLOT_CAN_MADVISE` / `SLOT_MADVISING` / `SLOT_MADVISED` 参与状态机。「放空」分支只在「原来不满且释放后恰等于 slots-1」时成立，满块释放第一格并不走这条路径。块号与元数据下标的换算是一次高低 6 位互换（对合，做两次回原地）。iOS 变体 `region_bits=0`，整个进程只有一个 region | Python(nanov2_const.py 101 加 nanov2_model.py 181 加 selfcheck_nanov2.py 221 加 main.py 73，107 断言实跑全绿) / Go(nanov2.go 289 加 main.go 73，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 682 | `04-移动开发/01-iOS/Swift语言特性/宏展开/` | swift-syntax 宏展开编排（`swiftlang/swift-syntax` 的 `Sources/SwiftSyntaxMacros/MacroSystem.swift` 57610 B、`Sources/SwiftSyntaxMacros/MacroExpansion.swift` 24641 B、`MacroExpansionContext.swift` 7316 B、`AttachedMacro.swift`、`FreestandingMacro.swift` 实读）：11 个 `MacroRole` 到协议名的映射表里，`memberAttribute` 与 `member` 只挂在 `DeclGroup`/`Decl` 上而 `peer` 不挂 `DeclGroup`。`inferFreestandingMacroRole` 的试探顺序是 expression → declaration → codeItem，一旦某角色成功就停止，故一个宏同时符合两个角色时行为取决于顺序而非声明。`collapse` 按角色决定分隔符：`memberAttribute` 用空格、`preamble` 用单个换行、`accessor` **仅当宿主声明本来没有 accessorBlock** 才补花括号、`body` 总是包花括号。独立宏展开是三态 success / failure / **notAMacro**（名字解析不到宏算第三种，不算失败）。递归检测按**实现类型**而非名字，`push`/`pop` 由 `withExpandedNode` 精确配对，所以「同一宏被两个不同名字展开」会被误判为递归而「宏自我递归时内层返回 nil 会让外层跟着 failure」 | Python(macro_const.py 52 加 macro_system.py 117 加 macro_expand.py 105 加 selfcheck_macro.py 236 加 main.py 82，71 断言实跑全绿) / Go(macroexpand.go 262 加 main.go 84，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 683 | `04-移动开发/01-iOS/Swift语言特性/参数包/` | Swift 变长泛型参数包（`swiftlang/swift-evolution` 的 `proposals/0393-parameter-packs.md` 57368 B、`0398-variadic-types.md` 11555 B、`0399-tuple-of-value-pack-expansion.md` 7201 B、`0408-pack-iteration.md` 8213 B 实读）：捕获集只记当前层，**内层 repeat 的类型不向外穿透**。同形状要求只在两处做推断——「尾随 where 子句」与「参数类型 / 返回类型」，其余位置只做检查不推断；可推断的形状只有「抽象」一种，一旦钉成具体长度即视为 conflict。变长类型有单包限制（一个类型里最多一个包），实参绑定分前缀 / 后缀两侧。最少实参数等于非包形参的个数而非包展开长度。实存属性自身不能是包扩展类型。`for-in repeat` 是**惰性**求值而 `repeat` 表达式是**全量**求值，SE-0399 之后抽象元组在四组输出（元组构造 / 函数实参 / 返回值 / 模式匹配）上的差异全部落地 | Python(packs_model.py 255 加 selfcheck_packs.py 168 加 main.py 74，49 断言实跑全绿) / Go(packs.go 286 加 main.go 66，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 684 | `04-移动开发/01-iOS/运行时/类布局与脏内存/` | objc4 类布局（`apple-oss-distributions/objc4@main` 的 `runtime/objc-runtime-new.h` 115042 B、`runtime/objc-runtime-new.mm` 339258 B、`runtime/PointerUnion.h` 7097 B 实读）：`class_data_bits_t` 只有一个字，同时塞下 `class_ro_t*`/`class_rw_t*` 与 3 个 FAST 标志，靠三类位两两不相交——真机 `FAST_DATA_MASK=0x0f00007ffffffff8` 保留 bit3–38 与 bit56–59，非真机 `0x0f007ffffffffff8` **多留 bit39–46 共 8 位**（差集恰为 `0x7f8000000000`）。`has_rw_pointer` 在 64 位只看 bit63，32 位因 `FAST_IS_RW_POINTER=0` 退化为「非空且 `flags(bits) & RW_REALIZED`」，于是 32 位下 setData 一个没置 `RW_REALIZED` 的 rw 之后判据仍为假，`safe_ro()` 会把 `class_rw_t*` 当 `class_ro_t*` 解出来。`setData` 只留旧字的低 3 位（32 位是低 2 位）。`flags(bits)` 用 `ptrauth_strip` **故意不验签**（源码原话），被篡改的字照样读出正确 flags，而 `data()` 走 auth 会失败。`ro_or_rw_ext` 是 `PointerUnion`，标签是**最低位**（T1 原样、T2 置 1，取值 `auth & ~1`），「打了标签的空指针」值为 1 时 `isNull()` 为假但 `operator bool()` 也为假。`extAlloc` 里 `rwe->version` 元类为 7、非元类为 0；**深拷贝只对方法生效**——相对列表逐个 duplicate 再逐个 attachLists，因为新的总插在最前，**顺序被反转**；而属性与协议从来不深拷贝（源码注释自承 "This is probably wrong"） | Python(rwbits_const.py 160 加 rwbits_bits.py 150 加 rwbits_lists.py 110 加 rwbits_model.py 242 加 rwbits_check.py 41 加 selfcheck_rwbits.py 185 加 selfcheck_rwmodel.py 200 加 main.py 76，111 断言实跑全绿) / Go(rwconst.go 118 加 rwbits.go 181 加 rwlists.go 138 加 rwclass.go 262 加 rwalloc.go 78 加 main.go 134，人工审查加四项静态检查全 OK) | 2026-09-24 |
| 685 | `05-AI与机器学习/01-深度学习/位置编码/RoPE旋转位置编码/` | RoPE 旋转位置编码（`modeling_rope_utils.py` 61743 B 与 `modeling_llama.py` 20897 B 原文实读）：inv_freq 只有 dim 加 2 个，`emb = cat(freqs, freqs)` 使 cos 前后半段逐位相同，与 `rotate_half` 的「前负后正」配套；六种 rope_type —— default 是 `1 除 base^(2i 除 dim)`、linear 直接 `inv_freq 除 factor`（等价于缩 position_ids）、dynamic NTK 用 `base 乘 ((factor 乘 L 除 L0) 减 (factor 减 1))^(dim 除 (dim 减 2))` 且 seq_len 被 max_position_embeddings 兜住、YaRN 低频外推高频内插（`beta_fast=32` 与 `beta_slow=1` 在 dim=64 加 base=10000 加 L0=8192 下给 (12,25)，故 i 小于 12 等于 default、i=31 等于 default 除 factor）、llama3 按波长三档（base=10000 加 dim=64 加 old=8192 的中频段恰是下标 21 到 24）、proportional 分母用 head_dim 且未旋转段补 0。rotate_half 只在 dim 为偶数时两次等于取负（奇数长度两半不等长）。相对位置：(3,11) 与 (0,8) 与 (30,38) 分数同为 1.030313044 而 (0,5) 给 0.508189646，旋转保范数 4.080441153 | Python(rope 183 加 selfcheck_rope 206 加 main 64，62 断言实跑全绿) / Go(rope 204 加 main 52，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 686 | `05-AI与机器学习/01-深度学习/GRU门控机制/` | GRU 门控机制（`torch/nn/modules/rnn.py` 76133 B 原文实读）：gate_size 三分支（LSTM 4H、GRU 3H、RNN H），weight_ih 行序是 W_ir 加 W_iz 加 W_in，初始化 stdv 等于 1 除 sqrt(H) 且 bias 也在 U 区间内。**源码 Note 自承与原论文不同** —— PyTorch 是 `n = tanh(Win x 加 bin 加 r ⊙ (Whn h 加 bhn))`，原论文是 `n = tanh(Win x 加 bin 加 Whn(r ⊙ h) 加 bhn)`；二者只在「Whn 非对角且 bhn 非零」时分叉（实测 0.148885034 对 0.571669966），bhn=0 或 Whn 对角时又相等。门语义：z 趋 1 时 h 直通、z 趋 0 时 h 等于 n、z=0.5 时等权混合；r 趋 0 时 n 与 h 无关。长程梯度：r=0 且 W_hz=0 时 ∂h_T 除 ∂h_0 精确等于 ∏z_t（T=5 实测 z=0.768525 给 0.268095415、z=0.916827 给 0.647795149、z 趋 1 给 1.000000000）。双向 h_n[1] 等于倒序喂入的反向层末态；dropout 只作用于非最后层；GRUCell 只接受 1D 加 2D 输入。**源码中不存在 peephole 参数，故不实现** | Python(gru 191 加 selfcheck_gru 297 加 main 66，56 断言实跑全绿) / Go(gru 201 加 main 68，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 687 | `05-AI与机器学习/01-深度学习/卷积变体/` | 卷积变体（`torch/nn/modules/conv.py` 79713 B 原文实读）：Conv2d 输出公式 `H_out 等于 floor((H_in 加 2pad 减 dilation(k 减 1) 减 1) 除 stride 加 1)`，空洞等效核 `k_eff 等于 d(k 减 1) 加 1`；`padding='same'` 源码明确只支持 stride=1。groups 用**支持集探针**验证连接图 —— groups=2 时 in0 加 in1 只点亮 out0 加 out1、depthwise 一一对应、multiplier K=2 时 in_j 到 out[2j] 与 out[2j+1]、groups=1 时每个输入通道点亮全部输出通道（负控）；参数量 C_in=3 加 C_out=16 加 k=3 时标准 448 对 DW+PW 94（21.0%）。转置卷积：`min 等于 (H_in 减 1)stride 减 2pad 加 d(k 减 1) 加 1`，合法 output_size 区间是 [min, min 加 stride 减 1]，越界抛 `valid sizes range`；stride=1 时区间退化成单点。padding_mode 只建模 zeros 加 replicate 加 circular（reflect 未建模，源码只给取值列表，实现在 C++ 侧） | Python(conv 142 加 selfcheck_conv 194 加 main 60，54 断言实跑全绿) / Go(conv 180 加 main 60，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 688 | `05-AI与机器学习/01-深度学习/量化与校准/` | 量化与校准（`observer.py` 80449 B 加 `fake_quantize.py` 23466 B 加 `utils.py` 29609 B 原文实读）：`_calculate_qparams` 三分支 —— affine 是 `(max_pos 减 min_neg) 除 (qmax 减 qmin)` 且 `zp 等于 qmin 减 round(min_neg 除 scale)` 再 clamp，symmetric 是 `max( 减 min_neg, max_pos) 除 ((qmax 减 qmin) 除 2)` 且 quint8 的 zp 恒 128，float_qparams 是 `(max 减 min) 除 (qmax 减 qmin)` 且 zp 等于 减 min 除 scale。**关键对照**：ReLU 后的 [0,1] 上 affine 给 scale 1 除 255 加 zp 0 而 symmetric 给 2 除 255 加 zp 128（分辨率差一倍、负半边浪费）；min==max=0.3 时 scale 是 0.3 除 255 而非 0（min_neg 被压到 0）；round 是半数取偶（0.5 格到 0、1.5 格到 2、2.5 格到 2）。MovingAverageMinMaxObserver 首帧直接取、之后 EMA；未跑 observer 时返回 scale 1.0 加 zp 0。HistogramObserver 的 L2 搜索把被排除的桶 clamp 到边界档，因此**裁的是空区间而不是离群点**：均匀分布只走 1 格就 break，两端各 1 个离群点也只裁最右 1 桶，只有 200 个样本的离群簇才会让另一侧被裁掉 | Python(quant 290 加 selfcheck_quant 227 加 main 67，85 断言实跑全绿) / Go(quant 203 加 histogram 118 加 main 85，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 689 | `05-AI与机器学习/01-深度学习/FlashAttention在线Softmax/` | FlashAttention 在线 softmax（`flash_attn/flash_attn_triton.py` 41112 B 原文实读）：`_fwd_kernel` 维护的是 `(m_i, lse_i)` 而非教科书常见的 `(m_i, l_i)`，递推式 `m_ij 等于 max(max(qk) 乘 scale, lse_i)`、`p 等于 exp(qk 乘 scale 减 m_ij)`、`acc 乘等于 exp(m_i 减 m_ij)`（m_i 是上一轮的 max）、`lse_i 等于 m_ij 加 log(exp(lse_i 减 m_ij) 加 l_ij)`，收尾 `acc 乘等于 exp(m_i 减 lse_i)`。**两个易错点**：① m_ij 的比较基准是 lse_i 而不是 m_i（因 lse 恒大于等于 m，抬高基准后两个缩放互相抵消，本 demo 用开关两种都跑、断言逐位相同）② 收尾乘 exp(m_i 减 lse_i) 就是除以归一化分母，天然避开 l_i 趋 0 的除零。分块不变性：128×128 加 8×8 加 3×7 加 24×1 与朴素实现偏差均小于 1e-15。因果掩码靠 `end_n 等于 min((start_m+1) 乘 BLOCK_M, seqlen_k)` 跳过整块加块内写 负inf 两道保险，支持集探针证明改 k[6..9] 不影响第 0 到 5 行。数值稳定性：logit 5000 时朴素 exp 溢出而在线版 lse 有限且输出收敛到 v[0] | Python(flash 152 加 selfcheck_flash 192 加 main 66，48 断言实跑全绿) / Go(flash 200 加 main 104，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 690 | `06-DevOps/01-容器化/CNI插件模型与IPAM/` | CNI 插件模型与 host-local IPAM（cni SPEC.md 55356 B 与 plugins 的 range.go 加 range_set.go 加 allocator.go 原文实读）：链式执行四类语义 —— ADD 正序且首个插件无 prevResult、DEL 逆序但每个插件都拿到 add 的最终结果、CHECK 同样用最终结果且 disableCheck 直接短路、GC 不带附件参数且出错继续收集全部错误；请求派生会注入 cniVersion 与 name 并剥离 capabilities。Range 规范化 —— 网关与 RangeStart 默认都取 .1、RangeEnd 取 lastIP 排除广播故 /24 得 .254、ones 大于 masklen 减 2 使 /31 与 /32 直接报 too small to allocate from、子网带主机位报 host bits set、Contains 在网络地址 .0 上为假。RangeIter 轮询 —— 起点是 store 的 lastReservedIP、cur 等于 RangeEnd 时回绕到下一个 range 的 RangeStart、等于网关递归跳过、绕回 startIP 即穷尽，故单 /24 首个可用地址是 .2 共 253 个、网关挪到 .5 后 .1 变成可分配总数不变、两个 /24 共 506 个且跨 range 后首个是 10.1.1.2。分配器三条拒绝路径（集合外 / 等于网关 / 已被占用）与 SPEC 禁止的重复分配；/29 下首次 .2 释放后再分配得 .3，只有遍历完才回到 .2 | Python(cni_range 200 加 cni_ipam 228 加 selfcheck_cni_ipam 257 加 main 66，65 断言实跑全绿) / Go(cni_range 279 加 cni_ipam 239 加 main 84，bracket_check 全 BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 691 | `06-DevOps/01-容器化/OCI分发协议与镜像拉取/` | OCI Distribution Spec（spec.md 54228 B 原文实读）：end-1 到 end-14 共 18 个端点行与 14 个错误码全量建模。反直觉状态码 —— 分块出错统一 416 而非 412、删除不支持是 405 UNSUPPORTED 而非 501、查上传进度是 204 加 Range 头而非 200。整体上传两条路（POST 再 PUT 与 end-4b 单次 POST 带 digest 可回 201 或 202）。分块上传三条 416 判据 —— 首个 chunk 必须从 0 起、chunk 必须连续、Content-Length 必须与 range 长度一致，且 Content-Range 须匹配正则；响应回的是累计 Range 而非本次范围。跨仓库挂载命中源 blob 回 201、未命中回 202 退化为普通上传。manifest PUT 会校验 config 与每一层是否已在仓库，缺失报 MANIFEST_BLOB_UNKNOWN 并在 detail 列出缺失 digest；end-7b 一次 PUT 可挂多个 tag。标签按 ASCIIbetical 排序、last 参数非包含、n=0 必须返回空列表且不带 Link 头。referrers 返回 image index，支持时不得返回 404、查无结果返回空 manifests、过滤生效时带 OCI-Filters-Applied、digest 语法非法回 400 | Python(dist_registry 281 加 selfcheck_dist 279 加 main 89，66 断言实跑全绿) / Go(dist_registry 256 加 main 78，bracket_check 全 BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 692 | `06-DevOps/01-容器化/runc创建流程与同步协议/` | runc create 双进程握手（sync.go 5382 B 加 process_linux.go 36776 B 加 standard_init_linux.go 10272 B 加 rootfs_linux.go 原文实读）：9 个同步常量组成四对握手（procMountPlease 与 procMountFd、procHooks 与 procHooksDone、procReady 与 procRun、procSeccomp 与 procSeccompDone）加通用 procError。syncT 的 File 不走 JSON 而用 SCM_RIGHTS 传 fd，靠 syncFlagHasFd 位标记；readSync 严格版还会拒绝非预期的 arg 与 file。syncParentHooks 位于 prepareRootfs 内部、挂载已就绪但尚未切根处，故 Prestart 与 CreateRuntime 由父进程跑且状态恒为 creating、CreateContainer 由子进程在切根前跑。**seccomp 有两个插入点** —— 无 NoNewPrivileges 时紧跟 procReady 之后 finalizeNamespace 之前（特权操作须在丢 capabilities 前完成），有 NoNewPrivileges 时在 LookPath 之后 close_pipe 之前（尽可能晚、紧贴 execve）。父进程在 procReady 分支先 updateState 再发 procRun，否则 runc create 被杀会漏掉 runc-init 进程；收尾靠 seenProcReady 判定 procReady not received。exec fifo 写单字节 0 阻塞，是容器能停在 created 状态的原因；UnsafeCloseFrom(PassedFilesCount 加 3) 是 CVE-2024-21626 的修复 | Python(runc_sync 147 加 runc_create 234 加 selfcheck_runc 228 加 main 51，58 断言实跑全绿) / Go(runc_create 224 加 main 69，bracket_check 全 BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 693 | `06-DevOps/01-容器化/pivotRoot与挂载传播/` | pivot_root 与 shared subtree（man7 pivot_root(2) 20967 B 与 runc rootfs_linux.go 52735 B 原文实读）：六条限制与 errno 分派 —— 非目录 ENOTDIR、与当前根同挂载或 new_root 为 / 是 EBUSY、put_old 不在 new_root 之下是非挂载点与非 SHARED 三类是 EINVAL、缺 CAP_SYS_ADMIN 是 EPERM；man 页明确 new_root 与 put_old 可以是同一目录，pivot_root(".", ".") 把旧根堆到新根之上从而免掉临时目录。四种传播 —— shared 在对等组内互相扩散且 slave 从 master 单向接收、slave 自身事件不回传、private 与 unbindable 不扩散、unbindable 不能作为 bind 源；MS_REC 是递归修饰符，对本来 private 的挂载做 rslave 得到的是无主 slave。runc 的 prepareRoot 依次是给 / 打 rslave、沿父目录上溯给 rootfs 父挂载去共享（flag 由 rootPropagation 决定）、把 rootfs bind 到自身以满足「new_root 必须是挂载点」；pivotRoot 之后先 rslave 再 MNT_DETACH（用 rslave 而非 rprivate 是为了避开 devicemapper 竞态）。msMoveRoot 只遮 Root 为 / 的完整 procfs 与 sysfs 且排除 rootfs 之下的，rootless 下 umount 失败改用 tmpfs 盖住 | Python(pivot_model 293 加 selfcheck_pivot 237 加 main 95，43 断言实跑全绿) / Go(pivot_model 216 加 rootfs_move 108 加 main 83，bracket_check 全 BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 694 | `06-DevOps/01-容器化/cgroup线程模式与cpuset分区/` | cgroup v2 线程模式与 cpuset 分区（cgroup-v2.rst 135851 B 与 cgroup-v1/cpusets.rst 37976 B 原文实读）：cgroup.type 四种读值 —— domain、domain threaded（thread root）、threaded、domain (invalid)，转 threaded 单向不可逆且 invalid 拓扑上的操作一律 EOPNOTSUPP；domain 会在「有子变 threaded」或「自己有进程且开了 threaded 控制器」时自动变 domain threaded。转 threaded 两条件 —— 父须是有效 domain 或 threaded cgroup，父若是未线程化 domain 则不得有域控制器或已填充的 domain 子 cgroup，**根 cgroup 豁免**；threaded 控制器只有 cpu、cpuset、perf_event、pids 四个。thread root 是整棵子树的资源域，cgroup.procs 只在它这里可读且含子树全部 PID。cpuset.cpus.partition 三值 member/root/isolated，读值多两种 root invalid 与 isolated invalid 降级态；local 分区父是有效 partition root 且可不预写 exclusive，remote 分区必须沿层级预写且祖先（除根外）都不能是 partition root。有效性三条件 —— 父是有效 partition root、exclusive.effective 非空、cpus.effective 非空除非无任务；兄弟抢同一批 CPU 会让双方 exclusive.effective 同时落空，父转 member 会连累子 local 分区失效但改回可恢复 | Python(cg_threaded 220 加 selfcheck_cgthreaded 234 加 main 73，47 断言实跑全绿) / Go(cg_threaded 300 加 main 61，bracket_check 全 BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 695 | `07-数据存储/04-搜索引擎/倒排表压缩与跳表/` | Lucene 104 postings 的块压缩与跳表（ForUtil.java 加 PForUtil.java 加 Lucene104PostingsFormat.java 原文实读）：一个 term 的倒排表切 256 值一块，先做 d-gap 再走 PForDelta。token 单字节同时编码两件事——高 3 位是 numExceptions（故 MAX_EXCEPTIONS=7 不是拍脑袋而是只剩 3 位可放）、低 5 位是 bitsPerValue；patch 下标要塞进 1 字节故源码 assert BLOCK_SIZE 小于等于 256。位数试探的循环是「先记录后累加」：进入 b 这一轮时 cumulativeExceptions 是「比 b 位更宽的值的个数」，所以 numExceptions 取的是上一轮累计值，实测 255 乘 1 加 1 乘 300（max_bits=9）被一路压到 1 位 1 个例外；下限是 maxBits 减 8 而不是 0，因为例外的高位只有 1 字节。全等捷径发生在掩码**之后**：源码先把例外写回原数组再判 allEqual，故 bitsPerValue 被写成 0、正文退化成一个 vInt，此时 patch 区记录的高位必须**先左移回去**再落盘（解码端统一按 hi 左移 bitsPerValue 还原，而这里 bitsPerValue 是 0）。ForUtil 不是朴素位拼接：bpv 1..8 用 collapse8 把 4 个值交叠进一个 32 位字形成 4 条 8 位泳道，掩码摊成 0x01010101 使一次移位同时推进 4 条泳道，落盘按 bpv 乘 8 个 32 位大端字写出故字节序是泳道转置的（bpv=8 时前 8 字节为 0,64,128,192,1,65,129,193）。跳过一个块不必解码，仅靠 token 就能算出 skipBytes。**注意** Lucene 104 已不走 MultiLevelSkipListWriter，而是自定 LEVEL1_FACTOR=32 与 LEVEL1_NUM_DOCS=8192 的两级跳表 | Python(bitio 加 forutil 213 加 pfor 134 加 skiplist 107 加 selfcheck_postings 293 加 main 81，5942 断言实跑全绿) / Go(postings 277 加 go.mod，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 696 | `07-数据存储/04-搜索引擎/BlockMaxWAND与动态剪枝/` | BlockMaxWAND 与动态剪枝（MaxScoreBulkScorer.java 原文实读）：纯 SHOULD 子句求并集时不再逐篇全量打分，而是靠索引里预存的 impacts（freq 与 norm 的竞争对上界）把文档切成窗口，每窗把子句分成 essential 与 non-essential 两类，只让 essential 驱动候选生成、其余只补分与竞争过滤。三层结构——outer window 由 impacts 块边界自适应决定、inner window 恒为 INNER_WINDOW_SIZE = 1 左移 12 用 bitset 收集命中以省掉优先队列反复重排。**排序键是 maxWindowScore 除 cost 而不是 maxWindowScore**：常见情况下最大分数与文档频率负相关故两者等价，但自定义打分或高 boost 下会分叉，实测两个上界同为 2.0 而 cost 为 10 与 1000 时 cost 大的那个先被划成 non-essential。essential 子句是从数组**尾部倒填**的，所以第 k 个被判为 essential 的落在 allScorers[n 减 1 减 k]，转写成下标数组时若不反转就会把子句认错。firstEssentialScorer 等于 n 表示整窗判空。只有一个 essential 时回退成「必需子句」：从后往前退 firstRequiredScorer，只要去掉某个 non-essential 子句后连理论上限都够不着阈值就把它升级为交集，实测上界 [1,1,3] 阈值 2.5 时退到 2，而 [1,1,1] 阈值 2.5 时一路退到 0。sumUpperBound 只在超过 2 个值时放大（numValues 小于等于 2 时浮点求和与顺序无关），minRequiredScore 刻意用 float 的 ulp 而非 double 的 ulp 以加快收敛 | Python(scorerutil 113 加 wand 246 加 search 139 加 selfcheck_wand 272 加 main，81 断言实跑全绿) / Go(maxscore 278 加 search 183，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 697 | `07-数据存储/04-搜索引擎/BKD树多维点索引/` | BKD 树多维点索引（apache/lucene@main 的 org.apache.lucene.util.bkd 包 BKDConfig 加 BKDUtil 加 BKDWriter 加 BKDReader 原文实读）：k-d 树的分裂逻辑加 B+ 树的块布局，一个字段里所有点共用一棵树，内节点只存「切哪个维度、切在哪儿」、真正的点按 512 个一块塞进叶子块。树形完全由点数决定——numLeaves = ceil(pointCount 除 maxPointsInLeafNode)，与数据分布无关，故能 one-pass 自底向上打包、查询时用 nodeID 做 O(1) 定位；内节点的包围盒**不落盘**，查询时靠 pushBoundsLeft 与 pushBoundsRight 沿路径推出来。getNumLeftLeafNodes 把叶子尽量铺成满二叉树并把不满的部分也尽量往左塞，两条断言保证 numLeft 大于等于 numRight 且 numLeft 小于等于 2 乘 numRight。切维度先走「2 倍」规则——某维被切次数还不到最多那个维度的一半且在当前包围盒里取值不全等就**无条件切它**，避免「维度 1 从头切到尾、维度 2 一次没碰」的病态树在只约束维度 2 的查询上退化成全表扫描。叶子块落地前算三样东西：每维公共前缀长度、sortedDim（在仍有差异的维度里挑该字节上不同取值最少的那个再按它排序，为的是让后续行程编码好压）与 leafCardinality（按整个点去重、忽略 docID，且从 1 起算）。索引前缀编码把一个 vInt 塞进三件事：code = (firstDiffByteDelta 乘 (1 加 bytesPerDim) 加 prefix) 乘 numIndexDims 加 splitDim。negativeDeltas 是**无条件赋值而非取反**——沿根到叶的任一路径上同一维度的 split 值必定单调（往左只会变小、往右只会变大），带上左右子树这个符号后 delta 恒为正，源码紧接着就是 assert firstDiffByteDelta 大于 0 | Python(bkd 加 bkdtree 加 checkutil 加 selfcheck_bkd 224 加 selfcheck_pack 141 加 main，4383 加 1941 共 6324 断言实跑全绿) / Go(bkd 160 加 node 170 加 pack 106 加 search 198，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 698 | `07-数据存储/04-搜索引擎/高亮器与摘要片段生成/` | Lucene UnifiedHighlighter 的摘要片段生成（UnifiedHighlighter.java 62493 B 加 PassageScorer.java 加 Passage.java 加 FieldHighlighter.java 加 LengthGoalBreakIterator.java 加 DefaultPassageFormatter.java 原文实读）：五类 OffsetSource（POSTINGS、TERM_VECTORS、ANALYSIS、POSTINGS_WITH_TERM_VECTORS、NONE_NEEDED）与 getOptimizedOffsetSource 的升降级判据——不需要 offsets 或词权重恒为 1 时降级、要权重或短语高亮时升级。PassageScorer 是一套迷你 BM25：k1=1.2、b=0.75、pivot=87，norm(start) = 1 加 1 除 log(pivot 加 start) 使越靠前的词得分越高，weight 用 numDocs = 1 加 contentLength 除 pivot 反推 IDF 并与 norm(1) 取得一致，tf 是 freq 除 (freq 加 k1 乘 (1 减 b 加 b 乘 passageLen 除 pivot))。top-N 用有界小堆按 (score, startOffset) 比较并复用被淘汰的对象。DefaultPassageFormatter 只在片段不连续时才补省略号、会把重叠的匹配区间合并、并做 owasp 转义（只转义 & < > 三个字符，且只作用于片段实际覆盖到的那段文本）。LengthGoalBreakIterator 有 min 与 closest-to-length 两种取断点策略，following 用 delta = int(f32(lengthGoal 乘 (1 减 fragmentAlignment)))、preceding 用 delta = int(f32(lengthGoal 乘 fragmentAlignment)) 做中心对齐 | Python(passage 136 加 breakiter 206 加 highlighter 267 加 selfcheck_highlight 283 加 main 121，75 断言实跑全绿) / Go(score 129 加 breakiter 199 加 highlight 250 加 main 69，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 699 | `07-数据存储/04-搜索引擎/FST与前缀补全suggester/` | Lucene Completion Suggester 的 FST 前缀补全（NRTSuggester.java 15279 B 加 NRTSuggesterBuilder 加 CompletionTokenStream 加 CompletionAnalyzer 加 PrefixCompletionQuery 加 FuzzyCompletionQuery 加 SuggestField 原文实读）：权重用 encode(w) = Integer.MAX_VALUE 减 w 编码，使 FST 的「升序」遍历天然等价于「权重降序」输出。payload 布局是 surface 加 PAYLOAD_SEP 加 vint(docId)，常量 MAX_DOC_ID_LEN_WITH_SEP = 6 限制单个 docId 连同分隔符最多 6 字节，ParseSurfaceForm 找的是**第一个**分隔符。队列上界 getMaxTopNSearcherQueueSize 是启发式：topN 乘候选路径数再除 liveDocsRatio（numDocs 除 maxDocs），带 filter 时再加 numDocs 的一半，最终被 MAX_TOP_N_QUEUE_SIZE = 5000 截断。skipDuplicates 在部分路径上就剪枝而不是等到终点。图展开上限 DEFAULT_MAX_GRAPH_EXPANSIONS = 10000；CompletionTokenStream 用 POS_SEP = 0x001F 与 HOLE = 0x001E 分隔位置增量与占位 | Python(suggester 298 加 selfcheck_suggester 183 加 main 89，68 断言实跑全绿) / Go(suggester 144 加 searcher 188 加 main 59，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 700 | `07-数据存储/04-搜索引擎/模糊查询与编辑距离自动机/` | Lucene FuzzyQuery 与 LevenshteinAutomata（FuzzyQuery.java 10929 B 加 LevenshteinAutomata.java 11921 B 加 Lev1 加 Lev2 加 Lev1T 三个 ParametricDescription 原文实读）：MAXIMUM_SUPPORTED_DISTANCE = 2（编辑距离大于 2 直接不支持）、defaultMaxExpansions = 50，三条入参校验分别是 prefixLength 非负、maxEdits 在 0..2 之间、maxExpansions 非 0。floatToEdits 三分支——similarity 为 0.0 走精确匹配，否则按 (1 减 similarity) 乘 termLen 截断取整；其中「1 减 0.8」在二进制浮点下是 0.19999999999999996，乘 10 得 1.9999999999999996，截断后是 **1 而不是 2**，所以 similarity=0.8 且 termLen=10 时返回 1（本 demo 按源码原样保留并在 README 记录该陷阱，未做「修正」）。OSA 与真 Damerau-Levenshtein 的差异用 ca 到 abc 钉死：OSA=3、DL=2，Lucene 用的是 OSA。ParametricDescription 的 size = minErrors.length 乘 (w 加 1)，isAccept 判据是 w 减 offset 加 minErrors[state] 小于等于 n，getPosition = absState 取模 (w 加 1)。特征向量的构造是**先左移再置位**，故最早出现的字符落在最高位（vector(a,0,3) 是 0b100 而不是 0b001）。**口径声明**：Lev2TParametricDescription.java 因 jsDelivr 超时未取到（落盘 0 字节），n=2 一律走 Lev2 骨架，带转置的 n=2 断言严格只对非 T 变体成立 | Python(editdist 60 加 fuzzy 213 加 selfcheck_fuzzy 204 加 main 92，111 断言实跑全绿) / Go(fuzzy 253 加 editdist 92 加 main 74，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 701 | `07-数据存储/04-搜索引擎/索引排序与提前终止/` | Lucene 索引排序（Sort.java 5973 B 加 SortField.java 23074 B 加 IndexWriterConfig.java 23634 B 原文实读）：SortField.getIndexSorter() 只对 STRING、INT、LONG、DOUBLE、FLOAT 五类返回非 null，其余（含 SCORE、DOC、自定义比较器）一律 null；IndexWriterConfig.setIndexSort 是「全有或全无」——只要有一个字段取不到 IndexSorter 整个 sort 就被拒绝，不存在「部分生效」。Sort.getPrimarySortField 遇到三类情况**跳过而非返回**：字段名为 null、该段内根本没有这个字段、skipper 稠密且 min 等于 max（该段取值唯一故排不排都一样）。Sort 有两个不同入口：new Sort() 得到 [FIELD_SCORE]，而显式传空数组会抛「There must be at least 1 sort field」，Python 侧因此补了 from_array 类方法、Go 侧拆成 NewSort 与 NewSortFromArray 两个函数。SortField 的 toString 形如 <int: "a">，重写时按「类型[!]: "字段"」显式拼接以避免嵌套尖括号。**口径声明**：SortingMergePolicy 与 EarlyTerminatingSortingCollector 在 main 分支取不到，本 demo 只建模到「能提前终止的前提条件（索引顺序与查询顺序一致）」，终止结论本身未断言 | Python(sortmodel 236 加 selfcheck_sort 202 加 main 84，66 断言实跑全绿) / Go(sortmodel 291 加 main 79，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 702 | `07-数据存储/04-搜索引擎/FST的TopN搜索与零输出补全/` | Lucene Util.TopNSearcher 的有界 TopN 搜索与零输出补全（fst_Util.java 30741 B 内的 TopNSearcher 段落原文实读）：用有界优先队列（maxQueueDepth）替代无界收集，被淘汰的路径直接丢弃。三条关键不变量——① 根节点**不参与** output 上推：把 root 也算进去会把每条路径的总 output 整体平移（实测得到 [((1,2),3),((1,3),5),((9,),0)] 而对上期望 5、7、2 才发现），root 没有入边可以吸收 min、也不需要有零弧 ② 零弧不变式只约束非根节点，根的出弧可以是非零 ③ 空串只在 allowEmptyString 为真时才入队（成对用例：假得 1 条、真得 2 条）。「0 output completion」是指当已有 topN 减 1 个结果且 maxQueueDepth 等于 topN 时把 queue 置 null，此后走 assert foundZero 的兜底路径保证仍能枚举出零输出的后缀；isComplete 的判据是 rejectCount 加 topN 小于等于 maxQueueDepth。depth=1 的极端配置会导致过度剪枝（结果数小于 3）但 isComplete 仍为真（0 加 1 小于等于 1），本 demo 把它做成「过度剪枝」的演示而非正确性断言 | Python(fstsearch 348 加 selfcheck_fsttopn 217 加 main 118，40 断言实跑全绿) / Go(fst 195 加 topn 248 加 main 136，bracket_check BALANCED 加 go_sanity 加 go_crossref 全过) | 2026-09-24 |
| 703 | `08-安全/01-Web安全/HTTP请求走私CL.TE与TE.CL/` | RFC 9112 §6.3 消息体长度**八条判定的优先级顺序**(HEAD 与 1xx/204/304 无 body、CONNECT 2xx 是隧道、TE 与 CL 并存时 TE 覆盖且是走私嫌疑、chunked 非最终编码时请求 400 而响应读到关闭、非法 CL 除非逗号列表全合法且全相同否则 400、请求无声明即 0、响应读到关闭)与 §7.1.3 chunked 解码(chunk-size 是 1*HEXDIG 且允许前导零、chunk-ext 以分号起头且 MUST 被忽略、last-chunk 之后是 trailer section 的空行终止)。CL.TE：前端按 CL=6 取 `0\r\n\r\nG`，后端按 chunked 在 `0\r\n\r\n` 处结束，残留 1 字节。TE.CL：前端按 chunked 取走 16 字节块，后端按 CL=4 只吃掉块长行 `10\r\n`，残留 23 字节请求前缀。两跳模拟器 `two_hop` 返回后端连接上的残留字节 | Python(smuggle.py 加 selfcheck_smuggle.py，57 断言实跑全绿) / Go(smuggle.go 175 加 smuggle_chunk.go 195 加 selfcheck_smuggle.go 127，367 行超限后按「分帧判定 / chunked 与两跳」拆分) | 2026-09-25 |
| 704 | `08-安全/01-Web安全/FetchMetadata与资源隔离策略/` | W3C Fetch Metadata §2 四个头都是 RFC 9651 Structured Field(Dest 与 Mode 与 Site 是 sf-token、User 是 sf-boolean)。§2.3 Sec-Fetch-Site 算法：初值 same-origin；用户显式触发的导航短路为 none 且不遍历；遍历 url list 时同源是 **continue** 不赋值，否则先置 cross-site，不同站才 break，同站则改为 same-site。§4.1 重定向走完整个 url list：example.com 到 子域 到 example.net 再到 example.com 的结果仍是 cross-site，且最后一个 url 因 break **根本没被检查**(实测只检查 3 个)。§4.2 Sec- 前缀使四头成为 forbidden response-header name，JS 无法伪造。§2.2 与 §2.3 要求服务端忽略 Mode 与 Site 的非法值，对 Dest 无此要求 | Python(fetchmeta.py 加 selfcheck_fetchmeta.py，53 断言实跑全绿) / Go(fetchmeta.go 198 加 selfcheck_fetchmeta.go 127) | 2026-09-25 |
| 705 | `08-安全/01-Web安全/DOMClobbering与命名属性访问/` | HTML §7.2.2.3 supported property names 是三部分并集(document-tree child navigable target name property set、仅 embed 与 form 与 img 与 object 四种标签的非空 name 内容属性、所有带 ID 元素的 ID)，按 tree order 忽略后出现的重复：div 与 a 的 name 不参与而 img 与 form 的参与。property set 是**两段循环**：先按名去重只留第一个，再按同源过滤 —— 规范 spices 例子里跨源 iframe 占住重名后被过滤，把同源的同名 iframe 一起带走，`window.spices` 为 undefined；反过来同源排前则 supported names 有值且 named objects 两个都算，取 tree order 上第一个的 WindowProxy。取值优先级 navigable 大于 单元素 大于 HTMLCollection | Python(domclobber.py 加 selfcheck_domclobber.py，35 断言实跑全绿) / Go(domclobber.go 200 加 selfcheck_domclobber.go 99) | 2026-09-25 |
| 706 | `08-安全/01-Web安全/账户安全与凭据填充防护/` | NIST SP 800-63B §3.1.1.2 口令长度门槛(单因素 SHALL 至少 15、多因素 MAY 更短但 SHALL 至少 8、SHOULD 允许最长至少 64)与四个 SHALL NOT(不得施加组成规则、不得定期改密、不得存未认证可读的 hint、不得用 KBA)：纯小写与纯数字与纯空格的 15 位都必须通过。Unicode 规则：每个**码点**算一个字符(15 个中文通过而 14 个被拒，emoji 算 1)，接受 Unicode 时哈希前做 NFC(分解式 e 加 U+0301 归一到合成式 U+00E9)。黑名单对**整串**比对而非子串：黑名单含 password 时 mypassword1 不被拦。完整口令校验不截断(70 字符少一个即失败)。§3.2.2 连续失败上限 100 且**按账户**隔离；look-up secret 至少 6 位；带外 secret 10 分钟内有效且单次使用；激活密钥连续失败不超过 10(第 10 次即锁) | Python(accmgr.py 加 selfcheck_accmgr.py，71 断言实跑全绿) / Go(accmgr.go 231 加 selfcheck_accmgr.go 170，NFC 用最小组合表) | 2026-09-25 |
| 707 | `08-安全/01-Web安全/JWT访问令牌profile与受众校验/` | RFC 9068 §2.1 typ MUST 是 at+jwt 或 application/at+jwt(立项目的就是防止 OIDC ID Token 被资源服务器当访问令牌接受，typ 为 JWT 或缺 typ 一律拒)，alg MUST NOT 是 none，AS 与 RS MUST 支持 RS256。§2.2 七个 REQUIRED 声明 iss 与 exp 与 aud 与 sub 与 client_id 与 iat 与 jti(缺任一即拒，client_id 与 jti 最常被漏)。§4 六步校验顺序 typ 到 声明完整性 到 加密协商 到 iss 精确相等 到 aud 含自身资源指示值 到 验签 到 exp，失败一律 invalid_token：iss 是精确串比较(斜杠后缀与大小写与尾随空格都算不等)、exp 是严格小于(恰好等于即失效，MAY 给几分钟余量)、aud 可为数组只要含自身。§5 跨 JWT 混淆要求同一签发者对不同资源用互不相同的 aud，两个资源服务器交叉验证 | Python(jwtat.py 加 selfcheck_jwtat.py，49 断言实跑全绿) / Go(jwtat.go 148 加 selfcheck_jwtat.go 137) | 2026-09-25 |
| 708 | `09-语言学习/Python/pickle与对象序列化/` | `__reduce__` 六元组契约第 6 项 (obj,state) 回调优先于 `__setstate__`；返回字符串=在**对象自身模块**按名取全局且要求『对象即全局名本体』(非本体实例借单例名被 PicklingError not the same object 拒)；默认 state 四态(None/`__dict__`/(None,slots)/(dict,slots)，dict+slots 共存须显式把 `__dict__` 写进 `__slots__`)；memo 身份保持(共享对象恢复后 `is` 同一、自引用不死循环)，记账指令按协议分代 BINPUT(2/3)→MEMOIZE(4)；按引用序列化门槛(局部类/lambda 报 AttributeError local object)；NEWOBJ vs REDUCE 与 **NEWOBJ_EX**(带 kwargs 专用指令，协议 4+)；**默认 `object.__new__` 时 `__getnewargs_ex__` 的 kwargs 被整条静默丢弃**(降级 REDUCE 直调类，`__init__` 只收默认值)；BUILD 的 state 优先于 NEWOBJ(_EX) 的 `__new__` 产出；copyreg.pickle 全局注册与 Pickler.dispatch_table 局部覆写；PEP 574 带外缓冲(buffer_callback 分离主流、协议<5 报 PicklingError)；persistent_id/persistent_load 外部引用 | Python(main.py 292，26 断言实跑全绿) | 2026-09-25 |
| 709 | `09-语言学习/Python/contextvars与异步上下文/` | PEP 567。get 三级回退(方法参>声明默认>LookupError)；Token 一次性(二次 reset 报 RuntimeError already been used once、跨变量 reset 报 ValueError different ContextVar、old_value 哨兵 Token.MISSING、3.12 尚无 with 协议)；copy_context 快照语义(快照后外部修改进不来、ctx.run 里的 set 记在快照上退出即弹出)；Context 实现 Mapping(len/in/[]/迭代/keys，未设置取 [] 抛 KeyError)；重入规则(顺序多次 run 合法、嵌套 run 自己报 RuntimeError cannot enter context already entered)；线程边界(新线程全新空上下文不继承、子线程 set 不回传)；threading.local 池化串扰对照(绑定物理线程，上一请求残留被下一请求读到 vs ContextVar 挂快照天然干净)；asyncio 两个快照时机对照本机源码：Task 创建时 tasks.py:122 copy_context(任务里的 set 全被含住)、call_soon 调度时 events.py:34 Handle.__init__(调度后改值回调仍见旧值) | Python(main.py 209，20 断言实跑全绿) | 2026-09-25 |
| 710 | `09-语言学习/Python/enum与dataclasses运行时/` | dataclass hash 矩阵(eq+frozen=False → `__hash__=None` 不可哈希、eq+frozen 自动生成按字段元组、eq=False 退回身份)；frozen 的 `__setattr__` 抛 FrozenInstanceError；可变默认值装饰时 ValueError(判据=不可哈希)；字段序基类在前、子类重声明占原位、**重声明不带默认时继承基类默认**；InitVar 三坑(不进 fields()、不进实例 `__dict__`、但 `iv.calc` 读到的是**类属性 5** 而非传入的 100)；replace() 重跑 `__init__`+`__post_init__`、asdict() 递归 deepcopy；**装饰器 vs 元类时序**：`__init_subclass__` 触发时 `@dataclass` 还没跑(hasattr `__dataclass_fields__` 为 False)，Enum 反面=父枚举有成员时定义新成员子类直接 TypeError cannot extend；枚举单例(按名/按值/属性三路同归)、别名(迭代只出正主、`__members__` 全含、同一身份)、EnumType 拦截成员重赋值、pickle 按枚举名.成员名；`_missing_` 钩子(返回 None 才 ValueError)；**3.12 的 in 仍按值宽松包含**(1 in Enum 为 True)；函数式 API start 起值 | Python(main.py 246，22 断言实跑全绿) | 2026-09-25 |
| 711 | `09-语言学习/Python/属性查找全链路/` | CPython `PyObject_GenericGetAttr`+`slot_tp_getattro` 的纯 Python 复刻(generic_getattr/py_getattr)与真实访问对拍：四级优先级=数据描述符(有 `__set__`/`__delete__`)>实例 `__dict__`>非数据描述符>类属性，查无→AttributeError→`__getattr__` 兜底；property 永远压 dict、函数是非数据描述符所以方法能被实例值遮蔽(运行期才炸 str is not callable)、slot 空槽读值=member_descriptor.__get__ 抛 AttributeError(空槽≠没有属性)；`__get__` 三种绑定(函数经实例→bound method `__self__`、经类→裸函数无 `__func__` 可剥、staticmethod 原样、classmethod 绑类)；`__getattribute__` 每次 vs `__getattr__` 只在失败(含前者内部主动抛的)；赋值/删除同构路由(数据描述符 `__set__`/`__delete__` 优先于写 dict)；`__dict__`/`__weakref__` 本身是 getset 描述符、slot member_descriptor 带 `__set__`；特殊方法只看类型槽(len() 忽略实例 `__len__`)；模块级 `__getattr__`(PEP 562) 惰性导出 | Python(main.py 227，14 断言实跑全绿) | 2026-09-25 |
| 712 | `09-语言学习/Python/缓冲协议与memoryview/` | PEP 3118。视图属性(nbytes/itemsize/len(=元素个数)/shape/strides/ndim/format/readonly/suboffsets=() 简单缓冲无间接层、buffer_info() 是 3.13+ 本机 3.12 尚无)；零拷贝(写直通底层、切片是共享内存的子视图且 sub.obj is 原对象、比较按内容跨 bytes/bytearray/memoryview、tobytes/tolist/bytes() 才拷贝)；resize 闸门(有活跃导出即 BufferError Existing exports、子视图也持导出计数、release 幂等、release 后使用报 ValueError、with 块自动 release)；cast 重解释(8 字节 cast('h')→4 个 LE int16 长度按 itemsize 缩、要求 1-D C 连续对齐、struct.pack_into 直写视图)；连续性(带步长/反转视图 c_contiguous=False)；toreadonly 共享内存拒写；只读+格式 B/b/c 的 1-D 视图 hash==hash(tobytes()) 切片同样成立、**可写视图 hash 抛 ValueError 而非 TypeError** | Python(main.py 178，21 断言实跑全绿) | 2026-09-25 |
| 713 | `10-逆向工程/02-移动端逆向/class-dump与ObjC运行时还原/` | 按 class-dump CDObjectiveC2Processor.m 的读取序静态还原 ObjC:__objc_classlist/catlist/protolist 三入口;objc_class 八指针序(isa→superclass→cache→vtable→data→r1..r3),data 字段低 3 位是标志(bit0=isSwiftClass,掩码 &~7 后才是 ro 地址,实测藏 0b101);class_ro_t 72 字节字段序(flags/instanceStart/instanceSize/reserved + 7 指针);**RO_META=(1<<0)** 标在 ro.flags——类与元类同名靠此区分;method_list_t 头 entsize **低 2 位是 fixup 标记解析须 &~3**,经典 method_t={name,types,imp} 三指针;ivar_t 对齐特例 alignment_raw==~0 → 1<<WORD_SHIFT;category 八指针序与 protocol 五指针序 | Python(objcdump.py 170 加 selfcheck 9 断言) / Go(objcdump.go 166 同语义复刻) | 2026-09-25 |
| 714 | `10-逆向工程/02-移动端逆向/smali与jadx反编译偏差/` | 汇编/反编译两条还原路径的保真度模型:官方访问标志位值(bridge=0x40/synthetic=0x1000/native=0x100/constructor=0x10000/declared-synchronized=0x20000);smali 侧结构无损(全标志、指令数、debug info 有则有无则无),jadx 侧 synthetic/bridge 转注释、行号取 debug info 剥离时退化近似值、**反编译失败块显式 fallback 留痕**(README『无法 100%』的工程化落点);deobfuscator 是视图层改名映射;desugar 覆盖面(lambda/方法引用/接口默认静态方法编译期就变换,try-with-resources AGP3.0+ 才全级别,**MethodHandle.invoke/invokeExact 明确不支持**,type annotations 仅编译期) | Python(deviation.py 100 加 selfcheck 8 断言) / Go(deviation.go 76) | 2026-09-25 |
| 715 | `10-逆向工程/02-移动端逆向/JNI与RegisterNatives还原/` | JNI 规范 mangling 全表:Java_ + 转义 FQCN(各段先转义再用 _ 连接,段内 _ → _1)+ 方法名,重载加 __ + **参数部分**签名(; → _2、[ → _3、/ → _,非 ASCII → _0XXXX **小写** hex);解析序先短名后长名,**长名仅在与另一 native 重载时必需**(规范 Cls1 例子:Java 方法同名不占符号库);RegisterNatives 是 JNIEnv 表第 215 项,JNINativeMethod{name,sig,fnPtr} 24 字节可字节级解析(hook 点一次拿到明文名+真实地址);第二参 jobject/jclass;目标非 native → NoSuchMethodError 负返回;UnregisterNatives 回到链接前(壳反注册对抗的规范依据) | Python(jnimap.py 126 加 selfcheck 13 断言) / Go(jnimap.go 68) | 2026-09-25 |
| 716 | `10-逆向工程/02-移动端逆向/Android加固壳与脱壳/` | dex 头三段验收:magic 8 字节、checksum=adler32(**除 magic 与自身,覆盖含 signature 字段故须先定稿 signature 再算 checksum**)、signature=SHA-1(再除自身),改 1 字节双失配;DexPathList.findClass 顺序遍历 dexElements 首中返回(壳 stub 在前不挡真身,同名先到先得);makeDexElements 的 IOException 进 suppressed 不快速失败;**addDexPath 用 concat 追加到尾部**(b/7726934,新 dex 优先级最低);BaseDexClassLoader 查类固定三段序 sharedLibraryLoaders → pathList → sharedLibraryLoadersAfter(后组 OEM 配置);壳模型 boot 后真身类可见,脱壳点=遍历 dexElements 落盘+三段验收 | Python(unpack.py 133 加 selfcheck 6 断言) / Go(unpack.go 88) | 2026-09-25 |
| 717 | `10-逆向工程/02-移动端逆向/AXML二进制Manifest解析/` | AXML chunk 链(ResourceTypes.h 常量):ResChunk_header 三段头(type/headerSize/size),**size 即完整跳距**(未知 chunk 可安全越过);字符串池 28 字节头,双编码 UTF-16(u16 长+高位 1 拼接)/UTF-8(UTF8_FLAG=1<<8:u16 字符长+u8 字节长),**池索引值是相对 stringsStart 的偏移**(高频踩坑);START_ELEMENT headerSize=36,attrExt 的 idIndex/classIndex/styleIndex 是 **1-based** 属性序号;typed value 四类分派(STRING=池索引/INT_DEC/INT_BOOLEAN 非 0 即真/REFERENCE=资源 ID 且 **rawValue 缺失**);构造↔解析共用同一套常量字节级对拍 | Python(axml.py 148 加 selfcheck 8 断言) / Go(axml.go 80) | 2026-09-25 |
| 718 | `11-性能分析/06-数据库性能/选择率与基数估计/` | 官方 69.1 五公式带算例对拍:基数 pg_class relpages/reltuples 按当前页数线性缩放;范围·唯一列 (整桶+桶内线性分数)/桶数=0.100697→1007 行(unique1<1000);等值·不在 MCV (1-Σmcv)/(n_distinct-n_mcv)=0.0014559→15 行;范围·有 MCV=MCV 精确部分 0.01833333+直方图部分×直方图占比 0.96966667=0.307669→3077 行(**直方图不含 MCV 群体,两个群体分开估再合并**);多条件独立相乘 0.0001466→1 行;eqjoinsel 双唯一 (1-nf1)(1-nf2)/max(n1,n2)=0.0001,连接行数=约束后外层 50×内层 10000×sel=50(非 EXPLAIN 内层 rows=1 相乘);n_distinct=-1=全唯一;误差按选择率**平方级放大**(1%×1%=0.01%)——多列相关时计划崩坏的数学根源 | Python(selestim.py 60 加 check 8 断言) / Go(selestim.go) | 2026-09-25 |
| 719 | `11-性能分析/06-数据库性能/IndexOnlyScan与可见性地图/` | 两前置条件:B-tree 恒支持/GiST-SPGiST 部分 opclass/**GIN 不支持**(索引项只存部分值)+查询列 ⊆ 索引列(SELECT x,z 与 WHERE z 均不行);VM 每**堆页**一位=全部行对所有当前与未来事务可见,VACUUM 置位/页修改清零,位未置必须回堆查可见性=退化普通索引扫描;VM 每页 2 位比堆小**四个数量级**常态驻内存;INCLUDE payload 不参与搜索键、UNIQUE(x) INCLUDE(y) 唯一性只约束 x、payload 类型可超出索引能力;部分索引 WHERE success 的谓词列虽不在索引但被全体条目蕴含→免 recheck(9.6+);表达式索引 f(x) 的规划器缺口:SELECT f(x) WHERE f(x)<1 需 INCLUDE (x) 补救 | Python(ionly.py 77 加 check 6 断言) / Go(ionly.go) | 2026-09-25 |
| 720 | `11-性能分析/06-数据库性能/等待事件归因/` | 官方 Table 27.4 十类:Client=**等用户应用 socket**(ClientRead 是应用 think time,DB 侧优化无效)、IO、Lock=重量级锁保护 SQL 可见对象、LWLock=共享内存结构轻量级锁、BufferPin(游标可拖长)、IPC/Timeout/Activity(后台进程主循环)/Extension/InjectionPoint(测试);state=active 且 wait_event_type 为 NULL=正在 CPU 上跑(过半样本=CPU 饱和,该查计划不该加连接);采样法各桶占比=时间占比;归因流程:剔除 idle 会话→分桶→主桶话术;三个高频误归因:ClientRead 算成 DB 慢/idle 会话算瓶颈(该算连接池头上)/Lock 与 LWLock 混谈 | Python(waitevent.py 45 加 check 7 断言) / Go(waitevent.go 64) | 2026-09-25 |
| 721 | `11-性能分析/06-数据库性能/连接池与吞吐非单调/` | pgbouncer 三档:session=整连接独占全特性/transaction=事务粒度归还(**破坏会话级特性**)/statement=语句粒度;复用数学 100 客户端×池 20:session 仅 20 可活动,transaction 全体轮转;default_pool_size=20 且**每 user/database 对**一条;fd 理论最大=max_client_conn+pool_size×库×用户(共用 DB 用户时用户=1);Little 定律 N=X×R(500qps×200ms=100 在途);吞吐曲线解析模型(标注为模型):≤容量线性,超出付上下文切换税 X(16)=348<X(8)=400,峰值池大小=容量;在途超池必排队,加客户端连接只把队列挪到池外 | Python(poolthr.py 54 加 check 6 断言) / Go(poolthr.go 30) | 2026-09-25 |
| 722 | `11-性能分析/06-数据库性能/泛化计划与统计抖动/` | PREPARE 官方口径:无参数恒泛化;auto 模式**前五次执行用定制计划并计算平均代价**,之后生成泛化计划比较,泛化代价不高于平均太多(not so much higher,倍数文档未公布以系数建模)→切换;EXPLAIN EXECUTE 判别:泛化含 $n 占位/定制代入字面值;plan_cache_mode 可 force;**偏斜参数翻转回归**:训练期常见值→泛化胜出,罕见偏斜值被按平均选择率的泛化计划拖慢(经典『无变更突然变慢』);强制重规划三触发:对象 DDL 变更/**规划器统计更新(ANALYZE)**/search_path 变化(9.3+)——统计抖动传染给 prepared 语句 | Python(plancache.py 64 加 check 6 断言) / Go(plancache.go 49) | 2026-09-25 |
| 723 | `12-项目工程/02-设计模式与惯用法/单例代价与依赖注入/` | 单例一次解决两个问题(唯一实例+全局访问)本身违反 SRP;唯一实例保证 + **lazy 首次请求才初始化**(普通构造器做不到);官方四缺点:违反 SRP/掩盖坏设计/**多线程需特殊处理**(锁内双检否则可能创建多个)/**难单元测试**(私有构造+类方法不可覆盖,mock 只能 hack 全局);Fowler 口径:定位器与注入都实现『应用代码与具体实现解耦』,真正的原则是**把配置与使用分开**;定位器=应用类显式向定位器要服务,**每个使用者都依赖定位器本身**;注入=服务自己出现(控制反转),构造签名即完整依赖清单,mock 直接当参数;注入三形态 构造/Setter/接口;IoC 有调试代价『需要自我证明』 | Python(sidi.py 71 加 check 6 断言) / Go(main.go 51) | 2026-09-25 |
| 724 | `12-项目工程/02-设计模式与惯用法/策略与条件分支取舍/` | Context 只持 Strategy 接口引用,构造注入 + set_strategy 运行时切换;Concrete 每算法一类;加新策略=注册新类,**Context 与既有策略零改动**(OCP),分支版则要改每处调用点的 if/elif 树;账本模型(标注):分支成本≈分支数×调用点数,策略成本≈策略数+1——单一调用点且分支少时直接写分支更划算;迁移入口=目录中 Replace Conditional with Polymorphism,每砍一棵分支树做一次 | Python(strat.py 57 加 check 4 断言) / Go(main.go 36) | 2026-09-25 |
| 725 | `12-项目工程/02-设计模式与惯用法/观察者与事件分发/` | 订阅机制两要素=订阅者列表字段+增删方法;通知只走统一 Subscriber 接口(新增订阅者类型不改发布者);名单运行时动态;**通知循环必须遍历快照**——回调里退订下一轮生效,边遍历边删原列表会跳项(实现期最高频 bug);演化路径=订阅管理委托给专职对象升级为集中式事件分发器,任意对象都能当发布者:发布者自持列表是观察者,拆出中间人按主题路由就是 pub/sub(同一机制两个刻度);解耦代价:控制流隐式化(谁在听/几个在听/抛异常怎么办从调用点看不见) | Python(observer.py 49 加 check 5 断言) / Go(main.go 52) | 2026-09-25 |
| 726 | `12-项目工程/02-设计模式与惯用法/访问者双分派与表达式问题/` | 重载反例:节点动态类型未知时按**静态类型**解析一律落到基类重载(页面原文);双分派=元素 accept(visitor) 回调 visit_具体(自己),选择权交还元素无需条件;两轴:**加操作=新增 1 个访问者类,既有零改动**(OCP 高光);**加节点类型=每个访问者都要改**(官方 Cons 第一条)+访问者可能拿不到元素私有字段;表达式问题两轴不可兼得:访问者押操作轴,封闭枚举+模式匹配(Rust Option 文档:无 null 引用,取值先 match)押类型轴——按哪种变化更频繁选型 | Python(visitor.py 75 加 check 5 断言) / Go(main.go 45) | 2026-09-25 |
| 727 | `12-项目工程/02-设计模式与惯用法/模板方法与钩子/` | 骨架在基类模板方法定死调用序,**子类只覆写步骤不覆写模板方法本身**;三类步骤:抽象(必须实现否则骨架一跑即败)/默认(共享可覆写)/**钩子(空体可选,不复写也能跑,置于关键步骤前后)**;官方代价活标本:MonstersAI 压制 collect_resources 默认实现为空——**可能违反 LSP**(是一台 AI 却不再会采集);骨架限制+步骤多难维护;Relations:Template=继承·类级·静态,Strategy=组合·对象级·运行时可换,Factory Method 是其特化(创建对象步骤做成钩子族);Go 无继承,函数字段组合复刻同构骨架 | Python(templ.py 77 加 check 5 断言) / Go(main.go 39) | 2026-09-25 |
| 728 | `01-游戏开发/01-服务端/数据库与缓存/Redis排行榜/` | ZRANK 0 基升序(rank 0=最低分,榜单用 ZREVRANK 反序);**同分元素按二进制字典序局部排序**(ZADD 官方「Elements with the same score」节:把成员当字节数组比较)——默认平局规则既非先到先得也非随机且不可配置;工程答案=复合分 score×TS_MAX+(TS_MAX-1-ts),同分时更早达成者更高,不新增命令,代价是分数展示要解码+double 精度预算;ZINCRBY 已有成员累加新成员视 0 起步 | Python(leaderboard.py 103,4 断言) / Go(leaderboard.go 67) | 2026-09-25 |
| 729 | `01-游戏开发/01-服务端/数据库与缓存/幂等发放与SET-NX/` | SET NX 官方原文=Only set the key if it does not already exist(XX 相反);同一订单重放 N 次只加一次币;**Lua 脚本原子性原文**:执行期间服务器整体阻塞,效果『要么全没发生要么已全部发生』——占键+加币无中间态;两步版(SET NX 后 INCRBY)在两命令间崩溃留悬态:键已占币未加,重放被 NX 挡死,需补偿对账;幂等键配 EX:窗口=重放可能拖多久+余量,过期后同单可再发 | Python(idempotent.py 92,5 断言) / Go(idempotent.go 47) | 2026-09-25 |
| 730 | `01-游戏开发/01-服务端/数据库与缓存/缓存淘汰策略与近似LRU/` | redis.conf 权威清单:8 策略=键范围(volatile=只带过期键/allkeys=全键)×算法(近似 LRU/LFU/随机/TTL)+**noeviction 为默认**(不淘汰写报错,缓存用途要显式改);volatile-* 候选集排除永久键;**LRU/LFU/TTL 都是近似算法**(省内存):maxmemory-samples 默认 5,10 接近真 LRU 但费 CPU,3 更快更不准,上限 64;模型验证:冷热交错 trace 下近似 LRU 50% 仍优于随机 36%(采样偏差小于算法差距) | Python(eviction.py 124,4 断言) / Go(eviction.go 63) | 2026-09-25 |
| 731 | `01-游戏开发/01-服务端/数据库与缓存/MongoDB背包更新操作符/` | $set 字段不存在即新增,**点路径顺手创建嵌套文档**;$inc 正负皆可/不存在则创建并直接置为增量值/**null 字段上报错**/单文档内原子(跨文档要事务);位置 $ 官方原文=查询匹配的**第一个元素**占位符且数组字段必须出现在查询里——背包同名多组道具只改第一组(经典 bug 源,是内嵌建模切格子表的信号);建模取舍:数组内嵌读快写粗,一行一格写准读散 | Python(bagops.py 95,5 断言) / Go(bagops.go 59) | 2026-09-25 |
| 732 | `01-游戏开发/01-服务端/数据库与缓存/RedisStream邮件队列/` | XADD 传 * 服务器自动生成 时间戳-序号 ID(单调,天然消费位点);XREADGROUP '>' 只读从未投递给任何消费者的新条目,指定 ID 读本消费者 pending(崩溃恢复入口);官方原文:server will remember that a given message was delivered to you→pending entries list;同组消费者分摊不重复(A,B,C 两消费者分到 A,C 与 B);消费者名首次出现自动创建;XACK 后离开 pending——至少一次交付的账本,业务侧须自持幂等 | Python(mailqueue.py 108,4 断言) / Go(mailqueue.go 67) | 2026-09-25 |
| 733 | `02-Web开发/06-WebAssembly/01-最小模块手工解码/` | 零工具链手工构造+解码最小 WASM 模块(W3C 二进制格式规范口径):magic=0x00 0x61 0x73 0x6D(即 \0asm)与 version=0x01 0x00 0x00 0x00(小端 u32);section=id+u32 长度+内容且**长度只数内容字节**,custom 段(id 0)语义上被忽略,解码→重编码与原模块逐字节相等;LEB128 变长整数(u32 直切组/s32 符号扩展终止,-1 单字节 0x7F;624485→E5 8E 26,-123456→C0 BB 78);functype 恒以 0x60 开头,export 段为 count+name(字节向量套向量)+kind(0x00=func)+idx;四操作码 0x41 i32.const(s32)/0x20 local.get/0x6A i32.add/0x0B end 搭出导出函数 add;栈机求值:(2^31-1)+1 按 2^32 回绕为 -2^31——WASM 无『溢出』只有回绕 | Python(wasm_min.py 157,5 断言) / Go(wasm_min.go 84) | 2026-09-25 |
| 734 | `03-系统编程/02-协程/Rust-async状态机与Pin/` | Rust async fn 编译成状态机(RFC 2394 口径):**每个 await 一个挂起点**,跨点存活的局部变量搬进状态变体(不跨点的不入)——async 体积膨胀的精确来源;poll 就绪才推进,Pending/Ready 双出口;Pin 三句话(std::pin 原文):钉在原地(值地址不再变,尤其有指针指着它时)/**Unpin 是默认**(绝大多数类型自动 Unpin,Pin 零约束)/**交互看 pointee**(Pin<Box<T>> 行为由 T 是否 Unpin 决定,与 Box 自身无关);Pin::new 只对 Unpin 指向物安全,!Unpin 走 unsafe new_unchecked 或 **Box::pin**(pinning Box 数据上堆);PhantomPinned 是**零大小标记类型**唯一效果变 !Unpin(官方 Unmovable 示例原文『Suppress Unpin so that this cannot be moved out of a Pin』);自引用结构移动即悬垂(模型直接演示指针指旧地址);Drop 保证:ManuallyDrop 抑制析构违反 guarantee(官方 unsound 反例) | Python(rust_async_pin.py 162,6 断言) / Go(rust_async_pin.go 55) | 2026-09-26 |
