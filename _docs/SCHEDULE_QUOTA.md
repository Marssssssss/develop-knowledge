# SCHEDULE_QUOTA.md — 定时巡检的定量推进规范

> 本文件定义每 1.5 小时一轮的自动巡检**「推进多少」**的定量规则。
> 调度频率:**每 1.5 小时一次**(全天 24h,无下午屏蔽),由两个 `HOURLY;INTERVAL=3` 任务错开 1.5 小时合成:
> - 任务 A(错位 0):`FREQ=HOURLY;INTERVAL=3;BYDAY=MO,TU,WE,TH,FR,SA,SU;BYHOUR=0,3,6,9,12,15,18,21;BYMINUTE=0`
> - 任务 B(错位 1.5h):`FREQ=HOURLY;INTERVAL=3;BYDAY=MO,TU,WE,TH,FR,SA,SU;BYHOUR=1,4,7,10,13,16,19,22;BYMINUTE=30`
> - 合成触发序列:`00:00 01:30 03:00 04:30 06:00 07:30 09:00 10:30 12:00 13:30 15:00 16:30 18:00 19:30 21:00 22:30` —— 每天 16 个触发点,间隔稳定 90 分钟
>
> **为什么必须开两个任务**:RFC 5545 §3.3.10 明确规定 `INTERVAL` 必须为**正整数**,不支持 `INTERVAL=1.5`。只能通过两个错开整数倍周期的 HOURLY 任务合成(LCM=3h,偏移 1.5h)。
>
> 配套文档:
> - `STATE.md` — 轮询索引与状态(主状态文件,≤ 5 KB 永久恒定)
> - `OPTIMIZATION.md` — 资源硬约束(搜索次数、token 预算)+ §2.4 状态文件瘦身
> - `AGENT_RULES.md` — Agent 行为规范

## 一、度量单位

**1 个新 demo = 1 个推进单位**。

> 1 个新 demo 指:新建一个含 `README.md` + 至少 1 个代码文件的子目录,例如 `01-游戏开发/01-服务端/网络编程/IO多路复用/epoll/`。

**为什么用 demo 数**:
- 与项目主目标「按领域沉淀原理级 demo」直接对齐
- 衡量直观(看 `archive/completed.md` 的 demo 表即可)
- 不会出现「凑 KB/凑动作数」的反向激励

## 二、每轮结构:5 主 + ≤ 2 副

| 任务 | 数量 | 内容 |
| --- | --- | --- |
| **主任务 (M)** | **必 5** | 5 个新 demo(全部位于所选索引对应的细分领域,路径形式) |
| **副任务 (S)** | **≤ 2,可 0** | 三选一(可重复或并用),见下表 |

**副任务 S 三选一**:

| 选项 | 何时用 | 例 |
| --- | --- | --- |
| S1 · 已有 demo 增强 | 上一轮写过同一领域,且有可补的 | `select/` demo 补 Rust 实现 |
| S2 · 目录结构补全 / **类目自动拓展** | 当前索引所属父类目缺主流子类目，或准备下一轮深挖 | 新建 `02-Web开发/02-后端/Node.js/` 子目录 + README；或按 AGENT_RULES §一.5 自动补缺失子类目（如 `09-语言学习` 补 Golang）并登记 STATE.md 索引表，每轮 ≤ 2 个 |
| S3 · 索引/记忆同步 | 阶段收尾,补全索引 | 更新 STATE.md(第三节)+ 追加到 `archive/{completed,schedule}.md`、daily log 摘要 |

**主任务实施细则**(2026-09-11 新增):
- 3 个 demo 全部落在同一次选中的索引(如索引 12 → 全部写在 `03-系统编程/01-网络编程/` 下)
- 单 demo 文件遵守 `OPTIMIZATION.md` §1.1(单源文件 ≤ 300 行,README ≤ 200 行)
- 3 个 demo 都用「内容来源铁律」(权威资料先查再写)
- 跨语种时同一 demo 内 3 种语言实现不算 3 个 demo,只算 1 个

## 三、降级规则

- **上一轮已完成 ≥ 3 demo** → 本轮**主任务可降级为 S1/S2/S3** × ≤ 2 + 至多 3 个零碎动作(版本号微调、修正笔误等)
- **上一轮失败或被跳过** → 本轮**主任务必做**(M),不允许连续 2 轮无新 demo
- **同一领域连续 ≥ 5 轮无新 demo** → 强制切到下一个索引(避免卡死)

## 四、失败重试与回退

```
单轮失败 → 重试同一索引(最多 3 次)
连续 3 轮同一索引失败 → 跳过该索引,记入 failed_attempts,跳到 next_index + 1
连续跳过 5 个索引 → 暂停自动巡检,发出告警,等用户决策
```

所有重试 / 跳过都写入 [`STATE.md`](./STATE.md)「本轮状态」+ [`archive/schedule.md`](./archive/schedule.md) 调度日志。

## 五、长周期目标(已移除,2026-09-11)

> 旧的「每周 ≥7 / 每月 ≥25」下限表已于 2026-09-11 删除。
> 新配额「3 主/轮 × 1 小时/轮 × 24 轮/天 ≈ 72 demo/天」远高于历史下限,继续沿用反而失去意义;且周/月度下限与实际生产节奏脱节,保留会造成反向激励。
> 如需重设目标,见第八节「变更记录」历史。

## 六、与现有硬约束的关系

`OPTIMIZATION.md` / `MEMORY.md` 里定义的资源上限**仍然有效**:
- 每轮:≤ 3 agentic_search, ≤ 9 WebSearch, ≤ 6 WebFetch, ≤ 3 新 demo
- 新 demo 单文件 ≤ 300 行
- 总 token 预算按既有规则

**本规范与硬约束是「下限 vs 上限」的互补关系**:
- 硬约束 = 不能超过(防过度)
- 本规范 = 不能太少(防敷衍)

## 七、调度日志模板(每轮末尾追加)

```text
| 时间 | 索引 | 主任务 | 副任务 | 推进 | 累计 demo | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| YYYY-MM-DD HH:MM | N | M=epoll demo | S3=索引同步 | +1 | 3 | 正常 |
```

「推进」列只算主任务的新 demo 数。副任务不计推进量。

## 八、变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-11 | 首版定义 |
| 2026-09-11 | 配额上调:每轮 1 主 + ≤1 副 → 3 主 + ≤2 副,总动作 ≤2 → ≤5;删除「长周期目标」整节(产能远高于旧下限,保留无意义);硬约束同步放宽至 ≤3 agentic_search / ≤9 WebSearch / ≤6 WebFetch / ≤3 新 demo;调度频率从 2 小时改为 1 小时 |
| 2026-09-11 | **状态文件瘦身**:拆分 `_docs/SEARCH_PROGRESS.md` → `STATE.md` (≤ 5 KB) + `archive/{completed,schedule}.md`(rotate 100/30);本规范内 SEARCH_PROGRESS 引用同步更新 |
| 2026-09-12 | **调度频率调整**:`RRULE` 的 `BYHOUR` 由默认全 24 小时改为 `0-13,18-23`,**排除 14-17 点(下午两点到六点不巡检)**;每天从 24 触发点降到 20 触发点;BYDAY/BYMINUTE 不变。修改时务必保留 `BYMINUTE=0`,避免频率退化或跳到错误分钟 |
| 2026-09-12 | **频率 → 每 1.5 小时 + 配额 → 5 主/轮 + 取消下午屏蔽**:RRULE 改为两个错开 1.5 小时的 `HOURLY;INTERVAL=3` 任务(因 RFC 5545 §3.3.10 强制 INTERVAL 为正整数,1.5 不能用单条 RRULE 表达),任务 A = 整点错位 0、任务 B = 整点错位 1.5h,合并全天 16 触发点(00:00/01:30/03:00/.../22:30);主任务 3 → 5,副任务 ≤2 不变;硬约束同步放宽至 ≤5 agentic_search / ≤15 WebSearch / ≤10 WebFetch / ≤5 新 demo;**取消** 14-17 点 BYHOUR 屏蔽,24 小时全跑 |
